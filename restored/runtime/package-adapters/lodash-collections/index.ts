// Restored from ref/webview/assets/lodash-hNqvBNwd.js
// Current export-order adapter over Lodash's object and collection helpers.
import {
  clone,
  cloneDeep,
  defaults,
  find,
  forIn,
  forOwn,
  has,
  isUndefined,
  last,
  mapValues,
  max,
  minBy,
  now,
  pick,
  range,
  size,
  sortBy,
  union,
  uniqueId,
  values,
  zipObject,
} from "lodash";
const initLodashCollections = (): void => {};
const initHas = initLodashCollections;
const initDefaults = initLodashCollections;
const initFind = initLodashCollections;
const initLast = initLodashCollections;
const initNow = initLodashCollections;
const initIsUndefined = initLodashCollections;
const initClone = initLodashCollections;
const initCloneDeep = initLodashCollections;
const initMapValues = initLodashCollections;
const initSortBy = initLodashCollections;
const initRange = initLodashCollections;
const initMinBy = initLodashCollections;
const initUniqueId = initLodashCollections;
const initForIn = initLodashCollections;
const initForOwn = initLodashCollections;
const initPick = initLodashCollections;
const initZipObject = initLodashCollections;
const initUnion = initLodashCollections;
const initSize = initLodashCollections;
const initMax = initLodashCollections;
const initValues = initLodashCollections;
export {
  forIn,
  cloneDeep,
  isUndefined,
  initHas,
  has,
  last,
  clone,
  defaults,
  initDefaults,
  find,
  initFind,
  forOwn,
  initLast,
  initNow,
  initIsUndefined,
  values,
  initClone,
  initCloneDeep,
  minBy,
  uniqueId,
  initMapValues,
  initSortBy,
  size,
  initRange,
  initMinBy,
  pick,
  initUniqueId,
  initForIn,
  initForOwn,
  sortBy,
  initPick,
  initZipObject,
  initUnion,
  range,
  zipObject,
  union,
  initLodashCollections,
  initSize,
  initMax,
  initValues,
  mapValues,
  max,
  now,
};
