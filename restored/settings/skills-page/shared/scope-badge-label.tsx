// Restored from ref/webview/assets/skills-page-zCIrhInI.js
// Skills settings page and current-ref export surface.

import type { ReactNode } from "react";

export function ScopeBadgeLabel(scopeBadgeProps: { label: ReactNode }) {
  let { label } = scopeBadgeProps;
  return <span className="text-token-description-foreground">{label}</span>;
}
