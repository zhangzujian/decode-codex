import { n as e } from "./rolldown-runtime-BG2f4sTM.js";
import {
  dn as t,
  zt as n,
} from "./app-initial~avatarOverlayCompositionSurface~index-9fQ9wihu~index-BFCcxPM5~mapbox-gl-DVWlwqb~kppdhley-mFmI6cbL.js";
var r,
  i,
  a = e(() => {
    (t(),
      (r = n()),
      (i = (e) =>
        (0, r.jsxs)(`svg`, {
          width: 20,
          height: 20,
          viewBox: `0 0 20 20`,
          fill: `currentColor`,
          xmlns: `http://www.w3.org/2000/svg`,
          ...e,
          children: [
            (0, r.jsx)(`path`, {
              d: `M12.0303 4.11328C13.4406 2.70317 15.7275 2.70305 17.1377 4.11328C18.5474 5.52355 18.5476 7.81057 17.1377 9.2207L10.8457 15.5117C10.522 15.8354 10.2868 16.0723 10.0547 16.2627L9.82031 16.4395C9.61539 16.5794 9.39783 16.7003 9.1709 16.7998L8.94141 16.8916C8.75976 16.9582 8.57206 17.0072 8.35547 17.0518L7.59082 17.1865L5.19727 17.5859C5.05455 17.6097 4.90286 17.6358 4.77441 17.6455C4.67576 17.653 4.54196 17.6555 4.39648 17.6201L4.24707 17.5703C4.02415 17.4746 3.84119 17.3068 3.72559 17.0957L3.67969 17.0029C3.59322 16.8013 3.59553 16.6073 3.60547 16.4756C3.61519 16.3473 3.6403 16.1963 3.66406 16.0537L4.06348 13.6602C4.1638 13.0582 4.22517 12.6732 4.3584 12.3096L4.45117 12.0791C4.55073 11.8521 4.67152 11.6346 4.81152 11.4297L4.9873 11.1953C5.17772 10.9632 5.4146 10.728 5.73828 10.4043L12.0303 4.11328ZM6.67871 11.3447C6.32926 11.6942 6.14542 11.8803 6.01953 12.0332L5.90918 12.1797C5.81574 12.3165 5.73539 12.4618 5.66895 12.6133L5.60742 12.7666C5.52668 12.9869 5.48332 13.229 5.375 13.8789L4.97656 16.2725L4.97559 16.2744H4.97852L7.37207 15.875L8.08887 15.749C8.25765 15.7147 8.37336 15.6839 8.4834 15.6436L8.63672 15.5811C8.78817 15.5146 8.93356 15.4342 9.07031 15.3408L9.2168 15.2305C9.36965 15.1046 9.55583 14.9207 9.90527 14.5713L14.8926 9.58301L11.666 6.35742L6.67871 11.3447ZM16.1963 5.05371C15.3054 4.16304 13.8616 4.16305 12.9707 5.05371L12.6074 5.41602L15.833 8.64258L16.1963 8.2793C17.0869 7.38845 17.0869 5.94456 16.1963 5.05371Z`,
            }),
            (0, r.jsx)(`path`, {
              d: `M4.58301 1.7832C4.72589 1.7832 4.84877 1.88437 4.87695 2.02441C4.99384 2.60873 5.22432 3.11642 5.58398 3.50391C5.94115 3.88854 6.44253 4.172 7.13281 4.28711C7.27713 4.3114 7.38267 4.43665 7.38281 4.58301C7.38281 4.7295 7.27723 4.8546 7.13281 4.87891C6.44249 4.99401 5.94116 5.27746 5.58398 5.66211C5.26908 6.00126 5.05404 6.43267 4.92676 6.92676L4.87695 7.1416C4.84891 7.28183 4.72601 7.38281 4.58301 7.38281C4.44013 7.38267 4.31709 7.28173 4.28906 7.1416C4.17212 6.55728 3.94179 6.04956 3.58203 5.66211C3.22483 5.27757 2.72347 4.99395 2.0332 4.87891C1.88897 4.85446 1.7832 4.72938 1.7832 4.58301C1.78335 4.43673 1.88902 4.3115 2.0332 4.28711C2.72366 4.17203 3.22481 3.88861 3.58203 3.50391C3.94186 3.11638 4.17214 2.60888 4.28906 2.02441L4.30371 1.97363C4.34801 1.86052 4.45804 1.78333 4.58301 1.7832Z`,
            }),
          ],
        })));
  }),
  o,
  s,
  c = e(() => {
    (t(),
      (o = n()),
      (s = (e) =>
        (0, o.jsx)(`svg`, {
          width: 16,
          height: 16,
          viewBox: `0 0 16 16`,
          fill: `none`,
          xmlns: `http://www.w3.org/2000/svg`,
          ...e,
          children: (0, o.jsx)(`path`, {
            fillRule: `evenodd`,
            clipRule: `evenodd`,
            d: `M10.2168 1.00076C10.4051 0.780861 10.7367 0.754321 10.957 0.942163C11.1773 1.13034 11.2034 1.46196 11.0156 1.6824L10.2764 2.54763C10.7067 3.06638 10.9657 3.73377 10.9658 4.46072C10.9658 4.61885 10.9515 4.77455 10.9277 4.92654C12.679 5.93944 13.8582 7.83132 13.8584 9.99978C13.8584 12.0975 12.7556 13.9373 11.1006 14.9715C10.6866 15.2301 10.1715 15.0344 9.99316 14.6209L9.85742 14.3074C9.28431 14.5436 8.65772 14.6755 8.00098 14.6756C7.34382 14.6756 6.71612 14.5438 6.14258 14.3074L6.00781 14.6209C5.82956 15.0345 5.31438 15.23 4.90039 14.9715C3.24529 13.9373 2.1416 12.0975 2.1416 9.99978C2.14176 7.8317 3.32042 5.93957 5.07129 4.92654C5.04749 4.77459 5.03418 4.61878 5.03418 4.46072C5.03431 3.73352 5.29295 3.06548 5.72363 2.54666L4.98535 1.6824C4.79734 1.46196 4.82363 1.13041 5.04395 0.942163C5.26432 0.754515 5.59599 0.780733 5.78418 1.00076L6.52734 1.8699C6.96102 1.61938 7.46319 1.47442 8 1.47439C8.53701 1.47439 9.03888 1.62021 9.47266 1.87087L10.2168 1.00076ZM7.47559 5.22048C5.87879 5.39381 4.51705 6.34902 3.78027 7.69509C3.95007 7.62307 4.13696 7.58379 4.33301 7.58376C5.11527 7.58376 5.74976 8.21758 5.75 8.99978C5.75 9.78218 5.11541 10.4168 4.33301 10.4168C3.86665 10.4167 3.4544 10.1903 3.19629 9.84255C3.19461 9.89477 3.19239 9.94715 3.19238 9.99978C3.19238 11.5997 3.97406 13.0167 5.17773 13.8914L7.47559 8.55837V5.22048ZM8.52539 8.55837L10.8223 13.8914C12.0262 13.0167 12.8086 11.5999 12.8086 9.99978C12.8086 9.94682 12.8054 9.89413 12.8037 9.84158C12.5457 10.1897 12.1336 10.4167 11.667 10.4168C10.8846 10.4168 10.25 9.78218 10.25 8.99978C10.2502 8.21758 10.8847 7.58376 11.667 7.58376C11.8632 7.58381 12.0499 7.62394 12.2197 7.69607C11.483 6.34987 10.1222 5.39392 8.52539 5.22048V8.55837ZM6.55762 13.3426C7.00287 13.5242 7.48978 13.6258 8.00098 13.6258C8.51191 13.6257 8.99747 13.5231 9.44238 13.3416L8 9.99294L6.55762 13.3426ZM8 2.52517C6.94642 2.52526 6.08525 3.38763 6.08496 4.46072V4.46267C6.68514 4.25509 7.32928 4.14142 8 4.14138C8.67074 4.14138 9.31486 4.25511 9.91504 4.46267L9.91602 4.46072C9.91572 3.38758 9.05365 2.52517 8 2.52517Z`,
            fill: `currentColor`,
          }),
        })));
  });
function l(e) {
  this.content = e;
}
var u = e(() => {
  ((l.prototype = {
    constructor: l,
    find: function (e) {
      for (var t = 0; t < this.content.length; t += 2)
        if (this.content[t] === e) return t;
      return -1;
    },
    get: function (e) {
      var t = this.find(e);
      return t == -1 ? void 0 : this.content[t + 1];
    },
    update: function (e, t, n) {
      var r = n && n != e ? this.remove(n) : this,
        i = r.find(e),
        a = r.content.slice();
      return (
        i == -1 ? a.push(n || e, t) : ((a[i + 1] = t), n && (a[i] = n)),
        new l(a)
      );
    },
    remove: function (e) {
      var t = this.find(e);
      if (t == -1) return this;
      var n = this.content.slice();
      return (n.splice(t, 2), new l(n));
    },
    addToStart: function (e, t) {
      return new l([e, t].concat(this.remove(e).content));
    },
    addToEnd: function (e, t) {
      var n = this.remove(e).content.slice();
      return (n.push(e, t), new l(n));
    },
    addBefore: function (e, t, n) {
      var r = this.remove(t),
        i = r.content.slice(),
        a = r.find(e);
      return (i.splice(a == -1 ? i.length : a, 0, t, n), new l(i));
    },
    forEach: function (e) {
      for (var t = 0; t < this.content.length; t += 2)
        e(this.content[t], this.content[t + 1]);
    },
    prepend: function (e) {
      return (
        (e = l.from(e)),
        e.size ? new l(e.content.concat(this.subtract(e).content)) : this
      );
    },
    append: function (e) {
      return (
        (e = l.from(e)),
        e.size ? new l(this.subtract(e).content.concat(e.content)) : this
      );
    },
    subtract: function (e) {
      var t = this;
      e = l.from(e);
      for (var n = 0; n < e.content.length; n += 2) t = t.remove(e.content[n]);
      return t;
    },
    toObject: function () {
      var e = {};
      return (
        this.forEach(function (t, n) {
          e[t] = n;
        }),
        e
      );
    },
    get size() {
      return this.content.length >> 1;
    },
  }),
    (l.from = function (e) {
      if (e instanceof l) return e;
      var t = [];
      if (e) for (var n in e) t.push(n, e[n]);
      return new l(t);
    }));
});
function d(e, t, n) {
  for (let r = 0; ; r++) {
    if (r == e.childCount || r == t.childCount)
      return e.childCount == t.childCount ? null : n;
    let i = e.child(r),
      a = t.child(r);
    if (i == a) {
      n += i.nodeSize;
      continue;
    }
    if (!i.sameMarkup(a)) return n;
    if (i.isText && i.text != a.text) {
      for (let e = 0; i.text[e] == a.text[e]; e++) n++;
      return n;
    }
    if (i.content.size || a.content.size) {
      let e = d(i.content, a.content, n + 1);
      if (e != null) return e;
    }
    n += i.nodeSize;
  }
}
function f(e, t, n, r) {
  for (let i = e.childCount, a = t.childCount; ;) {
    if (i == 0 || a == 0) return i == a ? null : { a: n, b: r };
    let o = e.child(--i),
      s = t.child(--a),
      c = o.nodeSize;
    if (o == s) {
      ((n -= c), (r -= c));
      continue;
    }
    if (!o.sameMarkup(s)) return { a: n, b: r };
    if (o.isText && o.text != s.text) {
      let e = 0,
        t = Math.min(o.text.length, s.text.length);
      for (
        ;
        e < t && o.text[o.text.length - e - 1] == s.text[s.text.length - e - 1];
      )
        (e++, n--, r--);
      return { a: n, b: r };
    }
    if (o.content.size || s.content.size) {
      let e = f(o.content, s.content, n - 1, r - 1);
      if (e) return e;
    }
    ((n -= c), (r -= c));
  }
}
function p(e, t) {
  return ((x.index = e), (x.offset = t), x);
}
function m(e, t) {
  if (e === t) return !0;
  if (!(e && typeof e == `object`) || !(t && typeof t == `object`)) return !1;
  let n = Array.isArray(e);
  if (Array.isArray(t) != n) return !1;
  if (n) {
    if (e.length != t.length) return !1;
    for (let n = 0; n < e.length; n++) if (!m(e[n], t[n])) return !1;
  } else {
    for (let n in e) if (!(n in t) || !m(e[n], t[n])) return !1;
    for (let n in t) if (!(n in e)) return !1;
  }
  return !0;
}
function ee(e, t, n) {
  let { index: r, offset: i } = e.findIndex(t),
    a = e.maybeChild(r),
    { index: o, offset: s } = e.findIndex(n);
  if (i == t || a.isText) {
    if (s != n && !e.child(o).isText)
      throw RangeError(`Removing non-flat range`);
    return e.cut(0, t).append(e.cut(n));
  }
  if (r != o) throw RangeError(`Removing non-flat range`);
  return e.replaceChild(r, a.copy(ee(a.content, t - i - 1, n - i - 1)));
}
function te(e, t, n, r) {
  let { index: i, offset: a } = e.findIndex(t),
    o = e.maybeChild(i);
  if (a == t || o.isText)
    return r && !r.canReplace(i, i, n)
      ? null
      : e.cut(0, t).append(n).append(e.cut(t));
  let s = te(o.content, t - a - 1, n, o);
  return s && e.replaceChild(i, o.copy(s));
}
function ne(e, t, n) {
  if (n.openStart > e.depth)
    throw new C(`Inserted content deeper than insertion position`);
  if (e.depth - n.openStart != t.depth - n.openEnd)
    throw new C(`Inconsistent open depths`);
  return re(e, t, n, 0);
}
function re(e, t, n, r) {
  let i = e.index(r),
    a = e.node(r);
  if (i == t.index(r) && r < e.depth - n.openStart) {
    let o = re(e, t, n, r + 1);
    return a.copy(a.content.replaceChild(i, o));
  } else if (!n.content.size) return _(a, v(e, t, r));
  else if (!n.openStart && !n.openEnd && e.depth == r && t.depth == r) {
    let r = e.parent,
      i = r.content;
    return _(
      r,
      i.cut(0, e.parentOffset).append(n.content).append(i.cut(t.parentOffset)),
    );
  } else {
    let { start: i, end: o } = se(n, e);
    return _(a, oe(e, i, o, t, r));
  }
}
function ie(e, t) {
  if (!t.type.compatibleContent(e.type))
    throw new C(`Cannot join ` + t.type.name + ` onto ` + e.type.name);
}
function ae(e, t, n) {
  let r = e.node(n);
  return (ie(r, t.node(n)), r);
}
function h(e, t) {
  let n = t.length - 1;
  n >= 0 && e.isText && e.sameMarkup(t[n])
    ? (t[n] = e.withText(t[n].text + e.text))
    : t.push(e);
}
function g(e, t, n, r) {
  let i = (t || e).node(n),
    a = 0,
    o = t ? t.index(n) : i.childCount;
  e &&
    ((a = e.index(n)),
    e.depth > n ? a++ : e.textOffset && (h(e.nodeAfter, r), a++));
  for (let e = a; e < o; e++) h(i.child(e), r);
  t && t.depth == n && t.textOffset && h(t.nodeBefore, r);
}
function _(e, t) {
  return (e.type.checkContent(t), e.copy(t));
}
function oe(e, t, n, r, i) {
  let a = e.depth > i && ae(e, t, i + 1),
    o = r.depth > i && ae(n, r, i + 1),
    s = [];
  return (
    g(null, e, i, s),
    a && o && t.index(i) == n.index(i)
      ? (ie(a, o), h(_(a, oe(e, t, n, r, i + 1)), s))
      : (a && h(_(a, v(e, t, i + 1)), s),
        g(t, n, i, s),
        o && h(_(o, v(n, r, i + 1)), s)),
    g(r, null, i, s),
    new b(s)
  );
}
function v(e, t, n) {
  let r = [];
  return (
    g(null, e, n, r),
    e.depth > n && h(_(ae(e, t, n + 1), v(e, t, n + 1)), r),
    g(t, null, n, r),
    new b(r)
  );
}
function se(e, t) {
  let n = t.depth - e.openStart,
    r = t.node(n).copy(e.content);
  for (let e = n - 1; e >= 0; e--) r = t.node(e).copy(b.from(r));
  return {
    start: r.resolveNoCache(e.openStart + n),
    end: r.resolveNoCache(r.content.size - e.openEnd - n),
  };
}
function ce(e, t) {
  for (let n = e.length - 1; n >= 0; n--) t = e[n].type.name + `(` + t + `)`;
  return t;
}
function le(e) {
  let t = [];
  do t.push(ue(e));
  while (e.eat(`|`));
  return t.length == 1 ? t[0] : { type: `choice`, exprs: t };
}
function ue(e) {
  let t = [];
  do t.push(de(e));
  while (e.next && e.next != `)` && e.next != `|`);
  return t.length == 1 ? t[0] : { type: `seq`, exprs: t };
}
function de(e) {
  let t = he(e);
  for (;;)
    if (e.eat(`+`)) t = { type: `plus`, expr: t };
    else if (e.eat(`*`)) t = { type: `star`, expr: t };
    else if (e.eat(`?`)) t = { type: `opt`, expr: t };
    else if (e.eat(`{`)) t = pe(e, t);
    else break;
  return t;
}
function fe(e) {
  /\D/.test(e.next) && e.err(`Expected number, got '` + e.next + `'`);
  let t = Number(e.next);
  return (e.pos++, t);
}
function pe(e, t) {
  let n = fe(e),
    r = n;
  return (
    e.eat(`,`) && (r = e.next == `}` ? -1 : fe(e)),
    e.eat(`}`) || e.err(`Unclosed braced range`),
    { type: `range`, min: n, max: r, expr: t }
  );
}
function me(e, t) {
  let n = e.nodeTypes,
    r = n[t];
  if (r) return [r];
  let i = [];
  for (let e in n) {
    let r = n[e];
    r.isInGroup(t) && i.push(r);
  }
  return (i.length == 0 && e.err(`No node type or group '` + t + `' found`), i);
}
function he(e) {
  if (e.eat(`(`)) {
    let t = le(e);
    return (e.eat(`)`) || e.err(`Missing closing paren`), t);
  } else if (/\W/.test(e.next)) e.err(`Unexpected token '` + e.next + `'`);
  else {
    let t = me(e, e.next).map(
      (t) => (
        e.inline == null
          ? (e.inline = t.isInline)
          : e.inline != t.isInline && e.err(`Mixing inline and block content`),
        { type: `name`, value: t }
      ),
    );
    return (e.pos++, t.length == 1 ? t[0] : { type: `choice`, exprs: t });
  }
}
function ge(e) {
  let t = [[]];
  return (i(a(e, 0), n()), t);
  function n() {
    return t.push([]) - 1;
  }
  function r(e, n, r) {
    let i = { term: r, to: n };
    return (t[e].push(i), i);
  }
  function i(e, t) {
    e.forEach((e) => (e.to = t));
  }
  function a(e, t) {
    if (e.type == `choice`)
      return e.exprs.reduce((e, n) => e.concat(a(n, t)), []);
    if (e.type == `seq`)
      for (let r = 0; ; r++) {
        let o = a(e.exprs[r], t);
        if (r == e.exprs.length - 1) return o;
        i(o, (t = n()));
      }
    else if (e.type == `star`) {
      let o = n();
      return (r(t, o), i(a(e.expr, o), o), [r(o)]);
    } else if (e.type == `plus`) {
      let o = n();
      return (i(a(e.expr, t), o), i(a(e.expr, o), o), [r(o)]);
    } else if (e.type == `opt`) return [r(t)].concat(a(e.expr, t));
    else if (e.type == `range`) {
      let o = t;
      for (let t = 0; t < e.min; t++) {
        let t = n();
        (i(a(e.expr, o), t), (o = t));
      }
      if (e.max == -1) i(a(e.expr, o), o);
      else
        for (let t = e.min; t < e.max; t++) {
          let t = n();
          (r(o, t), i(a(e.expr, o), t), (o = t));
        }
      return [r(o)];
    } else if (e.type == `name`) return [r(t, void 0, e.value)];
    else throw Error(`Unknown expr type`);
  }
}
function _e(e, t) {
  return t - e;
}
function ve(e, t) {
  let n = [];
  return (r(t), n.sort(_e));
  function r(t) {
    let i = e[t];
    if (i.length == 1 && !i[0].term) return r(i[0].to);
    n.push(t);
    for (let e = 0; e < i.length; e++) {
      let { term: t, to: a } = i[e];
      !t && n.indexOf(a) == -1 && r(a);
    }
  }
}
function ye(e) {
  let t = Object.create(null);
  return n(ve(e, 0));
  function n(r) {
    let i = [];
    r.forEach((t) => {
      e[t].forEach(({ term: t, to: n }) => {
        if (!t) return;
        let r;
        for (let e = 0; e < i.length; e++) i[e][0] == t && (r = i[e][1]);
        ve(e, n).forEach((e) => {
          (r || i.push([t, (r = [])]), r.indexOf(e) == -1 && r.push(e));
        });
      });
    });
    let a = (t[r.join(`,`)] = new E(r.indexOf(e.length - 1) > -1));
    for (let e = 0; e < i.length; e++) {
      let r = i[e][1].sort(_e);
      a.next.push({ type: i[e][0], next: t[r.join(`,`)] || n(r) });
    }
    return a;
  }
}
function be(e, t) {
  for (let n = 0, r = [e]; n < r.length; n++) {
    let e = r[n],
      i = !e.validEnd,
      a = [];
    for (let t = 0; t < e.next.length; t++) {
      let { type: n, next: o } = e.next[t];
      (a.push(n.name),
        i && !(n.isText || n.hasRequiredAttrs()) && (i = !1),
        r.indexOf(o) == -1 && r.push(o));
    }
    i &&
      t.err(
        `Only non-generatable nodes (` +
          a.join(`, `) +
          `) in a required position (see https://prosemirror.net/docs/guide/#generatable)`,
      );
  }
}
function xe(e) {
  let t = Object.create(null);
  for (let n in e) {
    let r = e[n];
    if (!r.hasDefault) return null;
    t[n] = r.default;
  }
  return t;
}
function Se(e, t) {
  let n = Object.create(null);
  for (let r in e) {
    let i = t && t[r];
    if (i === void 0) {
      let t = e[r];
      if (t.hasDefault) i = t.default;
      else throw RangeError(`No value supplied for attribute ` + r);
    }
    n[r] = i;
  }
  return n;
}
function Ce(e, t, n, r) {
  for (let r in t)
    if (!(r in e))
      throw RangeError(`Unsupported attribute ${r} for ${n} of type ${r}`);
  for (let n in e) {
    let r = e[n];
    r.validate && r.validate(t[n]);
  }
}
function we(e, t) {
  let n = Object.create(null);
  if (t) for (let r in t) n[r] = new qe(e, r, t[r]);
  return n;
}
function Te(e, t, n) {
  let r = n.split(`|`);
  return (n) => {
    let i = n === null ? `null` : typeof n;
    if (r.indexOf(i) < 0)
      throw RangeError(
        `Expected value of type ${r} for attribute ${t} on type ${e}, got ${i}`,
      );
  };
}
function Ee(e, t) {
  let n = [];
  for (let r = 0; r < t.length; r++) {
    let i = t[r],
      a = e.marks[i],
      o = a;
    if (a) n.push(a);
    else
      for (let t in e.marks) {
        let r = e.marks[t];
        (i == `_` ||
          (r.spec.group && r.spec.group.split(` `).indexOf(i) > -1)) &&
          n.push((o = r));
      }
    if (!o) throw SyntaxError(`Unknown mark type: '` + t[r] + `'`);
  }
  return n;
}
function De(e) {
  return e.tag != null;
}
function Oe(e) {
  return e.style != null;
}
function ke(e, t, n) {
  return t == null
    ? e && e.whitespace == `pre`
      ? 3
      : n & -5
    : (t ? D : 0) | (t === `full` ? et : 0);
}
function Ae(e) {
  for (let t = e.firstChild, n = null; t; t = t.nextSibling) {
    let e = t.nodeType == 1 ? t.nodeName.toLowerCase() : null;
    e && $e.hasOwnProperty(e) && n
      ? (n.appendChild(t), (t = n))
      : e == `li`
        ? (n = t)
        : e && (n = null);
  }
}
function je(e, t) {
  return (
    e.matches ||
    e.msMatchesSelector ||
    e.webkitMatchesSelector ||
    e.mozMatchesSelector
  ).call(e, t);
}
function Me(e) {
  let t = {};
  for (let n in e) t[n] = e[n];
  return t;
}
function Ne(e, t) {
  let n = t.schema.nodes;
  for (let r in n) {
    let i = n[r];
    if (!i.allowsMarkType(e)) continue;
    let a = [],
      o = (e) => {
        a.push(e);
        for (let n = 0; n < e.edgeCount; n++) {
          let { type: r, next: i } = e.edge(n);
          if (r == t || (a.indexOf(i) < 0 && o(i))) return !0;
        }
      };
    if (o(i.contentMatch)) return !0;
  }
}
function Pe(e) {
  let t = {};
  for (let n in e) {
    let r = e[n].spec.toDOM;
    r && (t[n] = r);
  }
  return t;
}
function Fe(e) {
  return e.document || window.document;
}
function Ie(e) {
  let t = rt.get(e);
  return (t === void 0 && rt.set(e, (t = Le(e))), t);
}
function Le(e) {
  let t = null;
  function n(e) {
    if (e && typeof e == `object`)
      if (Array.isArray(e))
        if (typeof e[0] == `string`) ((t ||= []), t.push(e));
        else for (let t = 0; t < e.length; t++) n(e[t]);
      else for (let t in e) n(e[t]);
  }
  return (n(e), t);
}
function y(e, t, n, r) {
  if (typeof t == `string`) return { dom: e.createTextNode(t) };
  if (t.nodeType != null) return { dom: t };
  if (t.dom && t.dom.nodeType != null) return t;
  let i = t[0],
    a;
  if (typeof i != `string`)
    throw RangeError(`Invalid array passed to renderSpec`);
  if (r && (a = Ie(r)) && a.indexOf(t) > -1)
    throw RangeError(
      `Using an array from an attribute object as a DOM spec. This may be an attempted cross site scripting attack.`,
    );
  let o = i.indexOf(` `);
  o > 0 && ((n = i.slice(0, o)), (i = i.slice(o + 1)));
  let s,
    c = n ? e.createElementNS(n, i) : e.createElement(i),
    l = t[1],
    u = 1;
  if (l && typeof l == `object` && l.nodeType == null && !Array.isArray(l)) {
    u = 2;
    for (let e in l)
      if (l[e] != null) {
        let t = e.indexOf(` `);
        t > 0
          ? c.setAttributeNS(e.slice(0, t), e.slice(t + 1), l[e])
          : e == `style` && c.style
            ? (c.style.cssText = l[e])
            : c.setAttribute(e, l[e]);
      }
  }
  for (let i = u; i < t.length; i++) {
    let a = t[i];
    if (a === 0) {
      if (i < t.length - 1 || i > u)
        throw RangeError(
          `Content hole must be the only child of its parent node`,
        );
      return { dom: c, contentDOM: c };
    } else {
      let { dom: t, contentDOM: i } = y(e, a, n, r);
      if ((c.appendChild(t), i)) {
        if (s) throw RangeError(`Multiple content holes`);
        s = i;
      }
    }
  }
  return { dom: c, contentDOM: s };
}
var b,
  x,
  S,
  C,
  w,
  Re,
  ze,
  Be,
  Ve,
  He,
  Ue,
  T,
  We,
  E,
  Ge,
  Ke,
  qe,
  Je,
  Ye,
  Xe,
  Ze,
  Qe,
  $e,
  D,
  et,
  O,
  k,
  tt,
  nt,
  rt,
  it = e(() => {
    (u(),
      (b = class e {
        constructor(e, t) {
          if (((this.content = e), (this.size = t || 0), t == null))
            for (let t = 0; t < e.length; t++) this.size += e[t].nodeSize;
        }
        nodesBetween(e, t, n, r = 0, i) {
          for (let a = 0, o = 0; o < t; a++) {
            let s = this.content[a],
              c = o + s.nodeSize;
            if (c > e && n(s, r + o, i || null, a) !== !1 && s.content.size) {
              let i = o + 1;
              s.nodesBetween(
                Math.max(0, e - i),
                Math.min(s.content.size, t - i),
                n,
                r + i,
              );
            }
            o = c;
          }
        }
        descendants(e) {
          this.nodesBetween(0, this.size, e);
        }
        textBetween(e, t, n, r) {
          let i = ``,
            a = !0;
          return (
            this.nodesBetween(
              e,
              t,
              (o, s) => {
                let c = o.isText
                  ? o.text.slice(Math.max(e, s) - s, t - s)
                  : o.isLeaf
                    ? r
                      ? typeof r == `function`
                        ? r(o)
                        : r
                      : o.type.spec.leafText
                        ? o.type.spec.leafText(o)
                        : ``
                    : ``;
                (o.isBlock &&
                  ((o.isLeaf && c) || o.isTextblock) &&
                  n &&
                  (a ? (a = !1) : (i += n)),
                  (i += c));
              },
              0,
            ),
            i
          );
        }
        append(t) {
          if (!t.size) return this;
          if (!this.size) return t;
          let n = this.lastChild,
            r = t.firstChild,
            i = this.content.slice(),
            a = 0;
          for (
            n.isText &&
            n.sameMarkup(r) &&
            ((i[i.length - 1] = n.withText(n.text + r.text)), (a = 1));
            a < t.content.length;
            a++
          )
            i.push(t.content[a]);
          return new e(i, this.size + t.size);
        }
        cut(t, n = this.size) {
          if (t == 0 && n == this.size) return this;
          let r = [],
            i = 0;
          if (n > t)
            for (let e = 0, a = 0; a < n; e++) {
              let o = this.content[e],
                s = a + o.nodeSize;
              (s > t &&
                ((a < t || s > n) &&
                  (o = o.isText
                    ? o.cut(Math.max(0, t - a), Math.min(o.text.length, n - a))
                    : o.cut(
                        Math.max(0, t - a - 1),
                        Math.min(o.content.size, n - a - 1),
                      )),
                r.push(o),
                (i += o.nodeSize)),
                (a = s));
            }
          return new e(r, i);
        }
        cutByIndex(t, n) {
          return t == n
            ? e.empty
            : t == 0 && n == this.content.length
              ? this
              : new e(this.content.slice(t, n));
        }
        replaceChild(t, n) {
          let r = this.content[t];
          if (r == n) return this;
          let i = this.content.slice(),
            a = this.size + n.nodeSize - r.nodeSize;
          return ((i[t] = n), new e(i, a));
        }
        addToStart(t) {
          return new e([t].concat(this.content), this.size + t.nodeSize);
        }
        addToEnd(t) {
          return new e(this.content.concat(t), this.size + t.nodeSize);
        }
        eq(e) {
          if (this.content.length != e.content.length) return !1;
          for (let t = 0; t < this.content.length; t++)
            if (!this.content[t].eq(e.content[t])) return !1;
          return !0;
        }
        get firstChild() {
          return this.content.length ? this.content[0] : null;
        }
        get lastChild() {
          return this.content.length
            ? this.content[this.content.length - 1]
            : null;
        }
        get childCount() {
          return this.content.length;
        }
        child(e) {
          let t = this.content[e];
          if (!t) throw RangeError(`Index ` + e + ` out of range for ` + this);
          return t;
        }
        maybeChild(e) {
          return this.content[e] || null;
        }
        forEach(e) {
          for (let t = 0, n = 0; t < this.content.length; t++) {
            let r = this.content[t];
            (e(r, n, t), (n += r.nodeSize));
          }
        }
        findDiffStart(e, t = 0) {
          return d(this, e, t);
        }
        findDiffEnd(e, t = this.size, n = e.size) {
          return f(this, e, t, n);
        }
        findIndex(e) {
          if (e == 0) return p(0, e);
          if (e == this.size) return p(this.content.length, e);
          if (e > this.size || e < 0)
            throw RangeError(`Position ${e} outside of fragment (${this})`);
          for (let t = 0, n = 0; ; t++) {
            let r = this.child(t),
              i = n + r.nodeSize;
            if (i >= e) return i == e ? p(t + 1, i) : p(t, n);
            n = i;
          }
        }
        toString() {
          return `<` + this.toStringInner() + `>`;
        }
        toStringInner() {
          return this.content.join(`, `);
        }
        toJSON() {
          return this.content.length
            ? this.content.map((e) => e.toJSON())
            : null;
        }
        static fromJSON(t, n) {
          if (!n) return e.empty;
          if (!Array.isArray(n))
            throw RangeError(`Invalid input for Fragment.fromJSON`);
          return new e(n.map(t.nodeFromJSON));
        }
        static fromArray(t) {
          if (!t.length) return e.empty;
          let n,
            r = 0;
          for (let e = 0; e < t.length; e++) {
            let i = t[e];
            ((r += i.nodeSize),
              e && i.isText && t[e - 1].sameMarkup(i)
                ? ((n ||= t.slice(0, e)),
                  (n[n.length - 1] = i.withText(n[n.length - 1].text + i.text)))
                : n && n.push(i));
          }
          return new e(n || t, r);
        }
        static from(t) {
          if (!t) return e.empty;
          if (t instanceof e) return t;
          if (Array.isArray(t)) return this.fromArray(t);
          if (t.attrs) return new e([t], t.nodeSize);
          throw RangeError(
            `Can not convert ` +
              t +
              ` to a Fragment` +
              (t.nodesBetween
                ? ` (looks like multiple versions of prosemirror-model were loaded)`
                : ``),
          );
        }
      }),
      (b.empty = new b([], 0)),
      (x = { index: 0, offset: 0 }),
      (S = class e {
        constructor(e, t) {
          ((this.type = e), (this.attrs = t));
        }
        addToSet(e) {
          let t,
            n = !1;
          for (let r = 0; r < e.length; r++) {
            let i = e[r];
            if (this.eq(i)) return e;
            if (this.type.excludes(i.type)) t ||= e.slice(0, r);
            else if (i.type.excludes(this.type)) return e;
            else
              (!n &&
                i.type.rank > this.type.rank &&
                ((t ||= e.slice(0, r)), t.push(this), (n = !0)),
                t && t.push(i));
          }
          return ((t ||= e.slice()), n || t.push(this), t);
        }
        removeFromSet(e) {
          for (let t = 0; t < e.length; t++)
            if (this.eq(e[t])) return e.slice(0, t).concat(e.slice(t + 1));
          return e;
        }
        isInSet(e) {
          for (let t = 0; t < e.length; t++) if (this.eq(e[t])) return !0;
          return !1;
        }
        eq(e) {
          return this == e || (this.type == e.type && m(this.attrs, e.attrs));
        }
        toJSON() {
          let e = { type: this.type.name };
          for (let t in this.attrs) {
            e.attrs = this.attrs;
            break;
          }
          return e;
        }
        static fromJSON(e, t) {
          if (!t) throw RangeError(`Invalid input for Mark.fromJSON`);
          let n = e.marks[t.type];
          if (!n)
            throw RangeError(`There is no mark type ${t.type} in this schema`);
          let r = n.create(t.attrs);
          return (n.checkAttrs(r.attrs), r);
        }
        static sameSet(e, t) {
          if (e == t) return !0;
          if (e.length != t.length) return !1;
          for (let n = 0; n < e.length; n++) if (!e[n].eq(t[n])) return !1;
          return !0;
        }
        static setFrom(t) {
          if (!t || (Array.isArray(t) && t.length == 0)) return e.none;
          if (t instanceof e) return [t];
          let n = t.slice();
          return (n.sort((e, t) => e.type.rank - t.type.rank), n);
        }
      }),
      (S.none = []),
      (C = class extends Error {}),
      (w = class e {
        constructor(e, t, n) {
          ((this.content = e), (this.openStart = t), (this.openEnd = n));
        }
        get size() {
          return this.content.size - this.openStart - this.openEnd;
        }
        insertAt(t, n) {
          let r = te(this.content, t + this.openStart, n);
          return r && new e(r, this.openStart, this.openEnd);
        }
        removeBetween(t, n) {
          return new e(
            ee(this.content, t + this.openStart, n + this.openStart),
            this.openStart,
            this.openEnd,
          );
        }
        eq(e) {
          return (
            this.content.eq(e.content) &&
            this.openStart == e.openStart &&
            this.openEnd == e.openEnd
          );
        }
        toString() {
          return this.content + `(` + this.openStart + `,` + this.openEnd + `)`;
        }
        toJSON() {
          if (!this.content.size) return null;
          let e = { content: this.content.toJSON() };
          return (
            this.openStart > 0 && (e.openStart = this.openStart),
            this.openEnd > 0 && (e.openEnd = this.openEnd),
            e
          );
        }
        static fromJSON(t, n) {
          if (!n) return e.empty;
          let r = n.openStart || 0,
            i = n.openEnd || 0;
          if (typeof r != `number` || typeof i != `number`)
            throw RangeError(`Invalid input for Slice.fromJSON`);
          return new e(b.fromJSON(t, n.content), r, i);
        }
        static maxOpen(t, n = !0) {
          let r = 0,
            i = 0;
          for (
            let e = t.firstChild;
            e && !e.isLeaf && (n || !e.type.spec.isolating);
            e = e.firstChild
          )
            r++;
          for (
            let e = t.lastChild;
            e && !e.isLeaf && (n || !e.type.spec.isolating);
            e = e.lastChild
          )
            i++;
          return new e(t, r, i);
        }
      }),
      (w.empty = new w(b.empty, 0, 0)),
      (Re = class e {
        constructor(e, t, n) {
          ((this.pos = e),
            (this.path = t),
            (this.parentOffset = n),
            (this.depth = t.length / 3 - 1));
        }
        resolveDepth(e) {
          return e == null ? this.depth : e < 0 ? this.depth + e : e;
        }
        get parent() {
          return this.node(this.depth);
        }
        get doc() {
          return this.node(0);
        }
        node(e) {
          return this.path[this.resolveDepth(e) * 3];
        }
        index(e) {
          return this.path[this.resolveDepth(e) * 3 + 1];
        }
        indexAfter(e) {
          return (
            (e = this.resolveDepth(e)),
            this.index(e) + (e == this.depth && !this.textOffset ? 0 : 1)
          );
        }
        start(e) {
          return (
            (e = this.resolveDepth(e)),
            e == 0 ? 0 : this.path[e * 3 - 1] + 1
          );
        }
        end(e) {
          return (
            (e = this.resolveDepth(e)),
            this.start(e) + this.node(e).content.size
          );
        }
        before(e) {
          if (((e = this.resolveDepth(e)), !e))
            throw RangeError(`There is no position before the top-level node`);
          return e == this.depth + 1 ? this.pos : this.path[e * 3 - 1];
        }
        after(e) {
          if (((e = this.resolveDepth(e)), !e))
            throw RangeError(`There is no position after the top-level node`);
          return e == this.depth + 1
            ? this.pos
            : this.path[e * 3 - 1] + this.path[e * 3].nodeSize;
        }
        get textOffset() {
          return this.pos - this.path[this.path.length - 1];
        }
        get nodeAfter() {
          let e = this.parent,
            t = this.index(this.depth);
          if (t == e.childCount) return null;
          let n = this.pos - this.path[this.path.length - 1],
            r = e.child(t);
          return n ? e.child(t).cut(n) : r;
        }
        get nodeBefore() {
          let e = this.index(this.depth),
            t = this.pos - this.path[this.path.length - 1];
          return t
            ? this.parent.child(e).cut(0, t)
            : e == 0
              ? null
              : this.parent.child(e - 1);
        }
        posAtIndex(e, t) {
          t = this.resolveDepth(t);
          let n = this.path[t * 3],
            r = t == 0 ? 0 : this.path[t * 3 - 1] + 1;
          for (let t = 0; t < e; t++) r += n.child(t).nodeSize;
          return r;
        }
        marks() {
          let e = this.parent,
            t = this.index();
          if (e.content.size == 0) return S.none;
          if (this.textOffset) return e.child(t).marks;
          let n = e.maybeChild(t - 1),
            r = e.maybeChild(t);
          if (!n) {
            let e = n;
            ((n = r), (r = e));
          }
          let i = n.marks;
          for (var a = 0; a < i.length; a++)
            i[a].type.spec.inclusive === !1 &&
              (!r || !i[a].isInSet(r.marks)) &&
              (i = i[a--].removeFromSet(i));
          return i;
        }
        marksAcross(e) {
          let t = this.parent.maybeChild(this.index());
          if (!t || !t.isInline) return null;
          let n = t.marks,
            r = e.parent.maybeChild(e.index());
          for (var i = 0; i < n.length; i++)
            n[i].type.spec.inclusive === !1 &&
              (!r || !n[i].isInSet(r.marks)) &&
              (n = n[i--].removeFromSet(n));
          return n;
        }
        sharedDepth(e) {
          for (let t = this.depth; t > 0; t--)
            if (this.start(t) <= e && this.end(t) >= e) return t;
          return 0;
        }
        blockRange(e = this, t) {
          if (e.pos < this.pos) return e.blockRange(this);
          for (
            let n =
              this.depth -
              (this.parent.inlineContent || this.pos == e.pos ? 1 : 0);
            n >= 0;
            n--
          )
            if (e.pos <= this.end(n) && (!t || t(this.node(n))))
              return new He(this, e, n);
          return null;
        }
        sameParent(e) {
          return this.pos - this.parentOffset == e.pos - e.parentOffset;
        }
        max(e) {
          return e.pos > this.pos ? e : this;
        }
        min(e) {
          return e.pos < this.pos ? e : this;
        }
        toString() {
          let e = ``;
          for (let t = 1; t <= this.depth; t++)
            e +=
              (e ? `/` : ``) + this.node(t).type.name + `_` + this.index(t - 1);
          return e + `:` + this.parentOffset;
        }
        static resolve(t, n) {
          if (!(n >= 0 && n <= t.content.size))
            throw RangeError(`Position ` + n + ` out of range`);
          let r = [],
            i = 0,
            a = n;
          for (let e = t; ;) {
            let { index: t, offset: n } = e.content.findIndex(a),
              o = a - n;
            if ((r.push(e, t, i + n), !o || ((e = e.child(t)), e.isText)))
              break;
            ((a = o - 1), (i += n + 1));
          }
          return new e(n, r, a);
        }
        static resolveCached(t, n) {
          let r = Ve.get(t);
          if (r)
            for (let e = 0; e < r.elts.length; e++) {
              let t = r.elts[e];
              if (t.pos == n) return t;
            }
          else Ve.set(t, (r = new ze()));
          let i = (r.elts[r.i] = e.resolve(t, n));
          return ((r.i = (r.i + 1) % Be), i);
        }
      }),
      (ze = class {
        constructor() {
          ((this.elts = []), (this.i = 0));
        }
      }),
      (Be = 12),
      (Ve = new WeakMap()),
      (He = class {
        constructor(e, t, n) {
          ((this.$from = e), (this.$to = t), (this.depth = n));
        }
        get start() {
          return this.$from.before(this.depth + 1);
        }
        get end() {
          return this.$to.after(this.depth + 1);
        }
        get parent() {
          return this.$from.node(this.depth);
        }
        get startIndex() {
          return this.$from.index(this.depth);
        }
        get endIndex() {
          return this.$to.indexAfter(this.depth);
        }
      }),
      (Ue = Object.create(null)),
      (T = class e {
        constructor(e, t, n, r = S.none) {
          ((this.type = e),
            (this.attrs = t),
            (this.marks = r),
            (this.content = n || b.empty));
        }
        get children() {
          return this.content.content;
        }
        get nodeSize() {
          return this.isLeaf ? 1 : 2 + this.content.size;
        }
        get childCount() {
          return this.content.childCount;
        }
        child(e) {
          return this.content.child(e);
        }
        maybeChild(e) {
          return this.content.maybeChild(e);
        }
        forEach(e) {
          this.content.forEach(e);
        }
        nodesBetween(e, t, n, r = 0) {
          this.content.nodesBetween(e, t, n, r, this);
        }
        descendants(e) {
          this.nodesBetween(0, this.content.size, e);
        }
        get textContent() {
          return this.isLeaf && this.type.spec.leafText
            ? this.type.spec.leafText(this)
            : this.textBetween(0, this.content.size, ``);
        }
        textBetween(e, t, n, r) {
          return this.content.textBetween(e, t, n, r);
        }
        get firstChild() {
          return this.content.firstChild;
        }
        get lastChild() {
          return this.content.lastChild;
        }
        eq(e) {
          return (
            this == e || (this.sameMarkup(e) && this.content.eq(e.content))
          );
        }
        sameMarkup(e) {
          return this.hasMarkup(e.type, e.attrs, e.marks);
        }
        hasMarkup(e, t, n) {
          return (
            this.type == e &&
            m(this.attrs, t || e.defaultAttrs || Ue) &&
            S.sameSet(this.marks, n || S.none)
          );
        }
        copy(t = null) {
          return t == this.content
            ? this
            : new e(this.type, this.attrs, t, this.marks);
        }
        mark(t) {
          return t == this.marks
            ? this
            : new e(this.type, this.attrs, this.content, t);
        }
        cut(e, t = this.content.size) {
          return e == 0 && t == this.content.size
            ? this
            : this.copy(this.content.cut(e, t));
        }
        slice(e, t = this.content.size, n = !1) {
          if (e == t) return w.empty;
          let r = this.resolve(e),
            i = this.resolve(t),
            a = n ? 0 : r.sharedDepth(t),
            o = r.start(a),
            s = r.node(a).content.cut(r.pos - o, i.pos - o);
          return new w(s, r.depth - a, i.depth - a);
        }
        replace(e, t, n) {
          return ne(this.resolve(e), this.resolve(t), n);
        }
        nodeAt(e) {
          for (let t = this; ;) {
            let { index: n, offset: r } = t.content.findIndex(e);
            if (((t = t.maybeChild(n)), !t)) return null;
            if (r == e || t.isText) return t;
            e -= r + 1;
          }
        }
        childAfter(e) {
          let { index: t, offset: n } = this.content.findIndex(e);
          return { node: this.content.maybeChild(t), index: t, offset: n };
        }
        childBefore(e) {
          if (e == 0) return { node: null, index: 0, offset: 0 };
          let { index: t, offset: n } = this.content.findIndex(e);
          if (n < e)
            return { node: this.content.child(t), index: t, offset: n };
          let r = this.content.child(t - 1);
          return { node: r, index: t - 1, offset: n - r.nodeSize };
        }
        resolve(e) {
          return Re.resolveCached(this, e);
        }
        resolveNoCache(e) {
          return Re.resolve(this, e);
        }
        rangeHasMark(e, t, n) {
          let r = !1;
          return (
            t > e &&
              this.nodesBetween(
                e,
                t,
                (e) => (n.isInSet(e.marks) && (r = !0), !r),
              ),
            r
          );
        }
        get isBlock() {
          return this.type.isBlock;
        }
        get isTextblock() {
          return this.type.isTextblock;
        }
        get inlineContent() {
          return this.type.inlineContent;
        }
        get isInline() {
          return this.type.isInline;
        }
        get isText() {
          return this.type.isText;
        }
        get isLeaf() {
          return this.type.isLeaf;
        }
        get isAtom() {
          return this.type.isAtom;
        }
        toString() {
          if (this.type.spec.toDebugString)
            return this.type.spec.toDebugString(this);
          let e = this.type.name;
          return (
            this.content.size &&
              (e += `(` + this.content.toStringInner() + `)`),
            ce(this.marks, e)
          );
        }
        contentMatchAt(e) {
          let t = this.type.contentMatch.matchFragment(this.content, 0, e);
          if (!t)
            throw Error(`Called contentMatchAt on a node with invalid content`);
          return t;
        }
        canReplace(e, t, n = b.empty, r = 0, i = n.childCount) {
          let a = this.contentMatchAt(e).matchFragment(n, r, i),
            o = a && a.matchFragment(this.content, t);
          if (!o || !o.validEnd) return !1;
          for (let e = r; e < i; e++)
            if (!this.type.allowsMarks(n.child(e).marks)) return !1;
          return !0;
        }
        canReplaceWith(e, t, n, r) {
          if (r && !this.type.allowsMarks(r)) return !1;
          let i = this.contentMatchAt(e).matchType(n),
            a = i && i.matchFragment(this.content, t);
          return a ? a.validEnd : !1;
        }
        canAppend(e) {
          return e.content.size
            ? this.canReplace(this.childCount, this.childCount, e.content)
            : this.type.compatibleContent(e.type);
        }
        check() {
          (this.type.checkContent(this.content),
            this.type.checkAttrs(this.attrs));
          let e = S.none;
          for (let t = 0; t < this.marks.length; t++) {
            let n = this.marks[t];
            (n.type.checkAttrs(n.attrs), (e = n.addToSet(e)));
          }
          if (!S.sameSet(e, this.marks))
            throw RangeError(
              `Invalid collection of marks for node ${this.type.name}: ${this.marks.map((e) => e.type.name)}`,
            );
          this.content.forEach((e) => e.check());
        }
        toJSON() {
          let e = { type: this.type.name };
          for (let t in this.attrs) {
            e.attrs = this.attrs;
            break;
          }
          return (
            this.content.size && (e.content = this.content.toJSON()),
            this.marks.length && (e.marks = this.marks.map((e) => e.toJSON())),
            e
          );
        }
        static fromJSON(e, t) {
          if (!t) throw RangeError(`Invalid input for Node.fromJSON`);
          let n;
          if (t.marks) {
            if (!Array.isArray(t.marks))
              throw RangeError(`Invalid mark data for Node.fromJSON`);
            n = t.marks.map(e.markFromJSON);
          }
          if (t.type == `text`) {
            if (typeof t.text != `string`)
              throw RangeError(`Invalid text node in JSON`);
            return e.text(t.text, n);
          }
          let r = b.fromJSON(e, t.content),
            i = e.nodeType(t.type).create(t.attrs, r, n);
          return (i.type.checkAttrs(i.attrs), i);
        }
      }),
      (T.prototype.text = void 0),
      (We = class e extends T {
        constructor(e, t, n, r) {
          if ((super(e, t, null, r), !n))
            throw RangeError(`Empty text nodes are not allowed`);
          this.text = n;
        }
        toString() {
          return this.type.spec.toDebugString
            ? this.type.spec.toDebugString(this)
            : ce(this.marks, JSON.stringify(this.text));
        }
        get textContent() {
          return this.text;
        }
        textBetween(e, t) {
          return this.text.slice(e, t);
        }
        get nodeSize() {
          return this.text.length;
        }
        mark(t) {
          return t == this.marks
            ? this
            : new e(this.type, this.attrs, this.text, t);
        }
        withText(t) {
          return t == this.text
            ? this
            : new e(this.type, this.attrs, t, this.marks);
        }
        cut(e = 0, t = this.text.length) {
          return e == 0 && t == this.text.length
            ? this
            : this.withText(this.text.slice(e, t));
        }
        eq(e) {
          return this.sameMarkup(e) && this.text == e.text;
        }
        toJSON() {
          let e = super.toJSON();
          return ((e.text = this.text), e);
        }
      }),
      (E = class e {
        constructor(e) {
          ((this.validEnd = e), (this.next = []), (this.wrapCache = []));
        }
        static parse(t, n) {
          let r = new Ge(t, n);
          if (r.next == null) return e.empty;
          let i = le(r);
          r.next && r.err(`Unexpected trailing text`);
          let a = ye(ge(i));
          return (be(a, r), a);
        }
        matchType(e) {
          for (let t = 0; t < this.next.length; t++)
            if (this.next[t].type == e) return this.next[t].next;
          return null;
        }
        matchFragment(e, t = 0, n = e.childCount) {
          let r = this;
          for (let i = t; r && i < n; i++) r = r.matchType(e.child(i).type);
          return r;
        }
        get inlineContent() {
          return this.next.length != 0 && this.next[0].type.isInline;
        }
        get defaultType() {
          for (let e = 0; e < this.next.length; e++) {
            let { type: t } = this.next[e];
            if (!(t.isText || t.hasRequiredAttrs())) return t;
          }
          return null;
        }
        compatible(e) {
          for (let t = 0; t < this.next.length; t++)
            for (let n = 0; n < e.next.length; n++)
              if (this.next[t].type == e.next[n].type) return !0;
          return !1;
        }
        fillBefore(e, t = !1, n = 0) {
          let r = [this];
          function i(a, o) {
            let s = a.matchFragment(e, n);
            if (s && (!t || s.validEnd))
              return b.from(o.map((e) => e.createAndFill()));
            for (let e = 0; e < a.next.length; e++) {
              let { type: t, next: n } = a.next[e];
              if (!(t.isText || t.hasRequiredAttrs()) && r.indexOf(n) == -1) {
                r.push(n);
                let e = i(n, o.concat(t));
                if (e) return e;
              }
            }
            return null;
          }
          return i(this, []);
        }
        findWrapping(e) {
          for (let t = 0; t < this.wrapCache.length; t += 2)
            if (this.wrapCache[t] == e) return this.wrapCache[t + 1];
          let t = this.computeWrapping(e);
          return (this.wrapCache.push(e, t), t);
        }
        computeWrapping(e) {
          let t = Object.create(null),
            n = [{ match: this, type: null, via: null }];
          for (; n.length;) {
            let r = n.shift(),
              i = r.match;
            if (i.matchType(e)) {
              let e = [];
              for (let t = r; t.type; t = t.via) e.push(t.type);
              return e.reverse();
            }
            for (let e = 0; e < i.next.length; e++) {
              let { type: a, next: o } = i.next[e];
              !a.isLeaf &&
                !a.hasRequiredAttrs() &&
                !(a.name in t) &&
                (!r.type || o.validEnd) &&
                (n.push({ match: a.contentMatch, type: a, via: r }),
                (t[a.name] = !0));
            }
          }
          return null;
        }
        get edgeCount() {
          return this.next.length;
        }
        edge(e) {
          if (e >= this.next.length)
            throw RangeError(`There's no ${e}th edge in this content match`);
          return this.next[e];
        }
        toString() {
          let e = [];
          function t(n) {
            e.push(n);
            for (let r = 0; r < n.next.length; r++)
              e.indexOf(n.next[r].next) == -1 && t(n.next[r].next);
          }
          return (
            t(this),
            e.map((t, n) => {
              let r = n + (t.validEnd ? `*` : ` `) + ` `;
              for (let n = 0; n < t.next.length; n++)
                r +=
                  (n ? `, ` : ``) +
                  t.next[n].type.name +
                  `->` +
                  e.indexOf(t.next[n].next);
              return r;
            }).join(`
`)
          );
        }
      }),
      (E.empty = new E(!0)),
      (Ge = class {
        constructor(e, t) {
          ((this.string = e),
            (this.nodeTypes = t),
            (this.inline = null),
            (this.pos = 0),
            (this.tokens = e.split(/\s*(?=\b|\W|$)/)),
            this.tokens[this.tokens.length - 1] == `` && this.tokens.pop(),
            this.tokens[0] == `` && this.tokens.shift());
        }
        get next() {
          return this.tokens[this.pos];
        }
        eat(e) {
          return this.next == e && (this.pos++ || !0);
        }
        err(e) {
          throw SyntaxError(
            e + ` (in content expression '` + this.string + `')`,
          );
        }
      }),
      (Ke = class e {
        constructor(e, t, n) {
          ((this.name = e),
            (this.schema = t),
            (this.spec = n),
            (this.markSet = null),
            (this.groups = n.group ? n.group.split(` `) : []),
            (this.attrs = we(e, n.attrs)),
            (this.defaultAttrs = xe(this.attrs)),
            (this.contentMatch = null),
            (this.inlineContent = null),
            (this.isBlock = !(n.inline || e == `text`)),
            (this.isText = e == `text`));
        }
        get isInline() {
          return !this.isBlock;
        }
        get isTextblock() {
          return this.isBlock && this.inlineContent;
        }
        get isLeaf() {
          return this.contentMatch == E.empty;
        }
        get isAtom() {
          return this.isLeaf || !!this.spec.atom;
        }
        isInGroup(e) {
          return this.groups.indexOf(e) > -1;
        }
        get whitespace() {
          return this.spec.whitespace || (this.spec.code ? `pre` : `normal`);
        }
        hasRequiredAttrs() {
          for (let e in this.attrs) if (this.attrs[e].isRequired) return !0;
          return !1;
        }
        compatibleContent(e) {
          return this == e || this.contentMatch.compatible(e.contentMatch);
        }
        computeAttrs(e) {
          return !e && this.defaultAttrs
            ? this.defaultAttrs
            : Se(this.attrs, e);
        }
        create(e = null, t, n) {
          if (this.isText)
            throw Error(`NodeType.create can't construct text nodes`);
          return new T(this, this.computeAttrs(e), b.from(t), S.setFrom(n));
        }
        createChecked(e = null, t, n) {
          return (
            (t = b.from(t)),
            this.checkContent(t),
            new T(this, this.computeAttrs(e), t, S.setFrom(n))
          );
        }
        createAndFill(e = null, t, n) {
          if (((e = this.computeAttrs(e)), (t = b.from(t)), t.size)) {
            let e = this.contentMatch.fillBefore(t);
            if (!e) return null;
            t = e.append(t);
          }
          let r = this.contentMatch.matchFragment(t),
            i = r && r.fillBefore(b.empty, !0);
          return i ? new T(this, e, t.append(i), S.setFrom(n)) : null;
        }
        validContent(e) {
          let t = this.contentMatch.matchFragment(e);
          if (!t || !t.validEnd) return !1;
          for (let t = 0; t < e.childCount; t++)
            if (!this.allowsMarks(e.child(t).marks)) return !1;
          return !0;
        }
        checkContent(e) {
          if (!this.validContent(e))
            throw RangeError(
              `Invalid content for node ${this.name}: ${e.toString().slice(0, 50)}`,
            );
        }
        checkAttrs(e) {
          Ce(this.attrs, e, `node`, this.name);
        }
        allowsMarkType(e) {
          return this.markSet == null || this.markSet.indexOf(e) > -1;
        }
        allowsMarks(e) {
          if (this.markSet == null) return !0;
          for (let t = 0; t < e.length; t++)
            if (!this.allowsMarkType(e[t].type)) return !1;
          return !0;
        }
        allowedMarks(e) {
          if (this.markSet == null) return e;
          let t;
          for (let n = 0; n < e.length; n++)
            this.allowsMarkType(e[n].type)
              ? t && t.push(e[n])
              : (t ||= e.slice(0, n));
          return t ? (t.length ? t : S.none) : e;
        }
        static compile(t, n) {
          let r = Object.create(null);
          t.forEach((t, i) => (r[t] = new e(t, n, i)));
          let i = n.spec.topNode || `doc`;
          if (!r[i])
            throw RangeError(
              `Schema is missing its top node type ('` + i + `')`,
            );
          if (!r.text) throw RangeError(`Every schema needs a 'text' type`);
          for (let e in r.text.attrs)
            throw RangeError(`The text node type should not have attributes`);
          return r;
        }
      }),
      (qe = class {
        constructor(e, t, n) {
          ((this.hasDefault = Object.prototype.hasOwnProperty.call(
            n,
            `default`,
          )),
            (this.default = n.default),
            (this.validate =
              typeof n.validate == `string`
                ? Te(e, t, n.validate)
                : n.validate));
        }
        get isRequired() {
          return !this.hasDefault;
        }
      }),
      (Je = class e {
        constructor(e, t, n, r) {
          ((this.name = e),
            (this.rank = t),
            (this.schema = n),
            (this.spec = r),
            (this.attrs = we(e, r.attrs)),
            (this.excluded = null));
          let i = xe(this.attrs);
          this.instance = i ? new S(this, i) : null;
        }
        create(e = null) {
          return !e && this.instance
            ? this.instance
            : new S(this, Se(this.attrs, e));
        }
        static compile(t, n) {
          let r = Object.create(null),
            i = 0;
          return (t.forEach((t, a) => (r[t] = new e(t, i++, n, a))), r);
        }
        removeFromSet(e) {
          for (var t = 0; t < e.length; t++)
            e[t].type == this &&
              ((e = e.slice(0, t).concat(e.slice(t + 1))), t--);
          return e;
        }
        isInSet(e) {
          for (let t = 0; t < e.length; t++) if (e[t].type == this) return e[t];
        }
        checkAttrs(e) {
          Ce(this.attrs, e, `mark`, this.name);
        }
        excludes(e) {
          return this.excluded.indexOf(e) > -1;
        }
      }),
      (Ye = class {
        constructor(e) {
          ((this.linebreakReplacement = null),
            (this.cached = Object.create(null)));
          let t = (this.spec = {});
          for (let n in e) t[n] = e[n];
          ((t.nodes = l.from(e.nodes)),
            (t.marks = l.from(e.marks || {})),
            (this.nodes = Ke.compile(this.spec.nodes, this)),
            (this.marks = Je.compile(this.spec.marks, this)));
          let n = Object.create(null);
          for (let e in this.nodes) {
            if (e in this.marks)
              throw RangeError(e + ` can not be both a node and a mark`);
            let t = this.nodes[e],
              r = t.spec.content || ``,
              i = t.spec.marks;
            if (
              ((t.contentMatch = n[r] || (n[r] = E.parse(r, this.nodes))),
              (t.inlineContent = t.contentMatch.inlineContent),
              t.spec.linebreakReplacement)
            ) {
              if (this.linebreakReplacement)
                throw RangeError(`Multiple linebreak nodes defined`);
              if (!t.isInline || !t.isLeaf)
                throw RangeError(
                  `Linebreak replacement nodes must be inline leaf nodes`,
                );
              this.linebreakReplacement = t;
            }
            t.markSet =
              i == `_`
                ? null
                : i
                  ? Ee(this, i.split(` `))
                  : i == `` || !t.inlineContent
                    ? []
                    : null;
          }
          for (let e in this.marks) {
            let t = this.marks[e],
              n = t.spec.excludes;
            t.excluded =
              n == null ? [t] : n == `` ? [] : Ee(this, n.split(` `));
          }
          ((this.nodeFromJSON = (e) => T.fromJSON(this, e)),
            (this.markFromJSON = (e) => S.fromJSON(this, e)),
            (this.topNodeType = this.nodes[this.spec.topNode || `doc`]),
            (this.cached.wrappings = Object.create(null)));
        }
        node(e, t = null, n, r) {
          if (typeof e == `string`) e = this.nodeType(e);
          else if (!(e instanceof Ke))
            throw RangeError(`Invalid node type: ` + e);
          else if (e.schema != this)
            throw RangeError(
              `Node type from different schema used (` + e.name + `)`,
            );
          return e.createChecked(t, n, r);
        }
        text(e, t) {
          let n = this.nodes.text;
          return new We(n, n.defaultAttrs, e, S.setFrom(t));
        }
        mark(e, t) {
          return (typeof e == `string` && (e = this.marks[e]), e.create(t));
        }
        nodeType(e) {
          let t = this.nodes[e];
          if (!t) throw RangeError(`Unknown node type: ` + e);
          return t;
        }
      }),
      (Xe = class e {
        constructor(e, t) {
          ((this.schema = e),
            (this.rules = t),
            (this.tags = []),
            (this.styles = []));
          let n = (this.matchedStyles = []);
          (t.forEach((e) => {
            if (De(e)) this.tags.push(e);
            else if (Oe(e)) {
              let t = /[^=]*/.exec(e.style)[0];
              (n.indexOf(t) < 0 && n.push(t), this.styles.push(e));
            }
          }),
            (this.normalizeLists = !this.tags.some((t) => {
              if (!/^(ul|ol)\b/.test(t.tag) || !t.node) return !1;
              let n = e.nodes[t.node];
              return n.contentMatch.matchType(n);
            })));
        }
        parse(e, t = {}) {
          let n = new tt(this, t, !1);
          return (n.addAll(e, S.none, t.from, t.to), n.finish());
        }
        parseSlice(e, t = {}) {
          let n = new tt(this, t, !0);
          return (n.addAll(e, S.none, t.from, t.to), w.maxOpen(n.finish()));
        }
        matchTag(e, t, n) {
          for (
            let r = n ? this.tags.indexOf(n) + 1 : 0;
            r < this.tags.length;
            r++
          ) {
            let n = this.tags[r];
            if (
              je(e, n.tag) &&
              (n.namespace === void 0 || e.namespaceURI == n.namespace) &&
              (!n.context || t.matchesContext(n.context))
            ) {
              if (n.getAttrs) {
                let t = n.getAttrs(e);
                if (t === !1) continue;
                n.attrs = t || void 0;
              }
              return n;
            }
          }
        }
        matchStyle(e, t, n, r) {
          for (
            let i = r ? this.styles.indexOf(r) + 1 : 0;
            i < this.styles.length;
            i++
          ) {
            let r = this.styles[i],
              a = r.style;
            if (!(
              a.indexOf(e) != 0 ||
              (r.context && !n.matchesContext(r.context)) ||
              (a.length > e.length &&
                (a.charCodeAt(e.length) != 61 || a.slice(e.length + 1) != t))
            )) {
              if (r.getAttrs) {
                let e = r.getAttrs(t);
                if (e === !1) continue;
                r.attrs = e || void 0;
              }
              return r;
            }
          }
        }
        static schemaRules(e) {
          let t = [];
          function n(e) {
            let n = e.priority == null ? 50 : e.priority,
              r = 0;
            for (; r < t.length; r++) {
              let e = t[r];
              if ((e.priority == null ? 50 : e.priority) < n) break;
            }
            t.splice(r, 0, e);
          }
          for (let t in e.marks) {
            let r = e.marks[t].spec.parseDOM;
            r &&
              r.forEach((e) => {
                (n((e = Me(e))),
                  e.mark || e.ignore || e.clearMark || (e.mark = t));
              });
          }
          for (let t in e.nodes) {
            let r = e.nodes[t].spec.parseDOM;
            r &&
              r.forEach((e) => {
                (n((e = Me(e))), e.node || e.ignore || e.mark || (e.node = t));
              });
          }
          return t;
        }
        static fromSchema(t) {
          return (
            t.cached.domParser ||
            (t.cached.domParser = new e(t, e.schemaRules(t)))
          );
        }
      }),
      (Ze = {
        address: !0,
        article: !0,
        aside: !0,
        blockquote: !0,
        canvas: !0,
        dd: !0,
        div: !0,
        dl: !0,
        fieldset: !0,
        figcaption: !0,
        figure: !0,
        footer: !0,
        form: !0,
        h1: !0,
        h2: !0,
        h3: !0,
        h4: !0,
        h5: !0,
        h6: !0,
        header: !0,
        hgroup: !0,
        hr: !0,
        li: !0,
        noscript: !0,
        ol: !0,
        output: !0,
        p: !0,
        pre: !0,
        section: !0,
        table: !0,
        tfoot: !0,
        ul: !0,
      }),
      (Qe = {
        head: !0,
        noscript: !0,
        object: !0,
        script: !0,
        style: !0,
        title: !0,
      }),
      ($e = { ol: !0, ul: !0 }),
      (D = 1),
      (et = 2),
      (O = 4),
      (k = class {
        constructor(e, t, n, r, i, a) {
          ((this.type = e),
            (this.attrs = t),
            (this.marks = n),
            (this.solid = r),
            (this.options = a),
            (this.content = []),
            (this.activeMarks = S.none),
            (this.match = i || (a & O ? null : e.contentMatch)));
        }
        findWrapping(e) {
          if (!this.match) {
            if (!this.type) return [];
            let t = this.type.contentMatch.fillBefore(b.from(e));
            if (t) this.match = this.type.contentMatch.matchFragment(t);
            else {
              let t = this.type.contentMatch,
                n;
              return (n = t.findWrapping(e.type))
                ? ((this.match = t), n)
                : null;
            }
          }
          return this.match.findWrapping(e.type);
        }
        finish(e) {
          if (!(this.options & D)) {
            let e = this.content[this.content.length - 1],
              t;
            if (e && e.isText && (t = /[ \t\r\n\u000c]+$/.exec(e.text))) {
              let n = e;
              e.text.length == t[0].length
                ? this.content.pop()
                : (this.content[this.content.length - 1] = n.withText(
                    n.text.slice(0, n.text.length - t[0].length),
                  ));
            }
          }
          let t = b.from(this.content);
          return (
            !e &&
              this.match &&
              (t = t.append(this.match.fillBefore(b.empty, !0))),
            this.type ? this.type.create(this.attrs, t, this.marks) : t
          );
        }
        inlineContext(e) {
          return this.type
            ? this.type.inlineContent
            : this.content.length
              ? this.content[0].isInline
              : e.parentNode &&
                !Ze.hasOwnProperty(e.parentNode.nodeName.toLowerCase());
        }
      }),
      (tt = class {
        constructor(e, t, n) {
          ((this.parser = e),
            (this.options = t),
            (this.isOpen = n),
            (this.open = 0),
            (this.localPreserveWS = !1));
          let r = t.topNode,
            i,
            a = ke(null, t.preserveWhitespace, 0) | (n ? O : 0);
          ((i = r
            ? new k(
                r.type,
                r.attrs,
                S.none,
                !0,
                t.topMatch || r.type.contentMatch,
                a,
              )
            : n
              ? new k(null, null, S.none, !0, null, a)
              : new k(e.schema.topNodeType, null, S.none, !0, null, a)),
            (this.nodes = [i]),
            (this.find = t.findPositions),
            (this.needsBlock = !1));
        }
        get top() {
          return this.nodes[this.open];
        }
        addDOM(e, t) {
          e.nodeType == 3
            ? this.addTextNode(e, t)
            : e.nodeType == 1 && this.addElement(e, t);
        }
        addTextNode(e, t) {
          let n = e.nodeValue,
            r = this.top,
            i =
              r.options & et
                ? `full`
                : this.localPreserveWS || (r.options & D) > 0,
            { schema: a } = this.parser;
          if (
            i === `full` ||
            r.inlineContext(e) ||
            /[^ \t\r\n\u000c]/.test(n)
          ) {
            if (!i) {
              if (
                ((n = n.replace(/[ \t\r\n\u000c]+/g, ` `)),
                /^[ \t\r\n\u000c]/.test(n) &&
                  this.open == this.nodes.length - 1)
              ) {
                let t = r.content[r.content.length - 1],
                  i = e.previousSibling;
                (!t ||
                  (i && i.nodeName == `BR`) ||
                  (t.isText && /[ \t\r\n\u000c]$/.test(t.text))) &&
                  (n = n.slice(1));
              }
            } else if (i === `full`)
              n = n.replace(
                /\r\n?/g,
                `
`,
              );
            else if (
              a.linebreakReplacement &&
              /[\r\n]/.test(n) &&
              this.top.findWrapping(a.linebreakReplacement.create())
            ) {
              let e = n.split(/\r?\n|\r/);
              for (let n = 0; n < e.length; n++)
                (n && this.insertNode(a.linebreakReplacement.create(), t, !0),
                  e[n] && this.insertNode(a.text(e[n]), t, !/\S/.test(e[n])));
              n = ``;
            } else n = n.replace(/\r?\n|\r/g, ` `);
            (n && this.insertNode(a.text(n), t, !/\S/.test(n)),
              this.findInText(e));
          } else this.findInside(e);
        }
        addElement(e, t, n) {
          let r = this.localPreserveWS,
            i = this.top;
          (e.tagName == `PRE` || /pre/.test(e.style && e.style.whiteSpace)) &&
            (this.localPreserveWS = !0);
          let a = e.nodeName.toLowerCase(),
            o;
          $e.hasOwnProperty(a) && this.parser.normalizeLists && Ae(e);
          let s =
            (this.options.ruleFromNode && this.options.ruleFromNode(e)) ||
            (o = this.parser.matchTag(e, this, n));
          out: if (s ? s.ignore : Qe.hasOwnProperty(a))
            (this.findInside(e), this.ignoreFallback(e, t));
          else if (!s || s.skip || s.closeParent) {
            s && s.closeParent
              ? (this.open = Math.max(0, this.open - 1))
              : s && s.skip.nodeType && (e = s.skip);
            let n,
              r = this.needsBlock;
            if (Ze.hasOwnProperty(a))
              (i.content.length &&
                i.content[0].isInline &&
                this.open &&
                (this.open--, (i = this.top)),
                (n = !0),
                i.type || (this.needsBlock = !0));
            else if (!e.firstChild) {
              this.leafFallback(e, t);
              break out;
            }
            let o = s && s.skip ? t : this.readStyles(e, t);
            (o && this.addAll(e, o), n && this.sync(i), (this.needsBlock = r));
          } else {
            let n = this.readStyles(e, t);
            n &&
              this.addElementByRule(e, s, n, s.consuming === !1 ? o : void 0);
          }
          this.localPreserveWS = r;
        }
        leafFallback(e, t) {
          e.nodeName == `BR` &&
            this.top.type &&
            this.top.type.inlineContent &&
            this.addTextNode(
              e.ownerDocument.createTextNode(`
`),
              t,
            );
        }
        ignoreFallback(e, t) {
          e.nodeName == `BR` &&
            (!this.top.type || !this.top.type.inlineContent) &&
            this.findPlace(this.parser.schema.text(`-`), t, !0);
        }
        readStyles(e, t) {
          let n = e.style;
          if (n && n.length)
            for (let e = 0; e < this.parser.matchedStyles.length; e++) {
              let r = this.parser.matchedStyles[e],
                i = n.getPropertyValue(r);
              if (i)
                for (let e; ;) {
                  let n = this.parser.matchStyle(r, i, this, e);
                  if (!n) break;
                  if (n.ignore) return null;
                  if (
                    ((t = n.clearMark
                      ? t.filter((e) => !n.clearMark(e))
                      : t.concat(
                          this.parser.schema.marks[n.mark].create(n.attrs),
                        )),
                    n.consuming === !1)
                  )
                    e = n;
                  else break;
                }
            }
          return t;
        }
        addElementByRule(e, t, n, r) {
          let i, a;
          if (t.node)
            if (((a = this.parser.schema.nodes[t.node]), a.isLeaf))
              this.insertNode(a.create(t.attrs), n, e.nodeName == `BR`) ||
                this.leafFallback(e, n);
            else {
              let e = this.enter(a, t.attrs || null, n, t.preserveWhitespace);
              e && ((i = !0), (n = e));
            }
          else {
            let e = this.parser.schema.marks[t.mark];
            n = n.concat(e.create(t.attrs));
          }
          let o = this.top;
          if (a && a.isLeaf) this.findInside(e);
          else if (r) this.addElement(e, n, r);
          else if (t.getContent)
            (this.findInside(e),
              t
                .getContent(e, this.parser.schema)
                .forEach((e) => this.insertNode(e, n, !1)));
          else {
            let r = e;
            (typeof t.contentElement == `string`
              ? (r = e.querySelector(t.contentElement))
              : typeof t.contentElement == `function`
                ? (r = t.contentElement(e))
                : t.contentElement && (r = t.contentElement),
              this.findAround(e, r, !0),
              this.addAll(r, n),
              this.findAround(e, r, !1));
          }
          i && this.sync(o) && this.open--;
        }
        addAll(e, t, n, r) {
          let i = n || 0;
          for (
            let a = n ? e.childNodes[n] : e.firstChild,
              o = r == null ? null : e.childNodes[r];
            a != o;
            a = a.nextSibling, ++i
          )
            (this.findAtPoint(e, i), this.addDOM(a, t));
          this.findAtPoint(e, i);
        }
        findPlace(e, t, n) {
          let r, i;
          for (let t = this.open, a = 0; t >= 0; t--) {
            let o = this.nodes[t],
              s = o.findWrapping(e);
            if (
              s &&
              (!r || r.length > s.length + a) &&
              ((r = s), (i = o), !s.length)
            )
              break;
            if (o.solid) {
              if (n) break;
              a += 2;
            }
          }
          if (!r) return null;
          this.sync(i);
          for (let e = 0; e < r.length; e++)
            t = this.enterInner(r[e], null, t, !1);
          return t;
        }
        insertNode(e, t, n) {
          if (e.isInline && this.needsBlock && !this.top.type) {
            let e = this.textblockFromContext();
            e && (t = this.enterInner(e, null, t));
          }
          let r = this.findPlace(e, t, n);
          if (r) {
            this.closeExtra();
            let t = this.top;
            t.match &&= t.match.matchType(e.type);
            let n = S.none;
            for (let i of r.concat(e.marks))
              (t.type ? t.type.allowsMarkType(i.type) : Ne(i.type, e.type)) &&
                (n = i.addToSet(n));
            return (t.content.push(e.mark(n)), !0);
          }
          return !1;
        }
        enter(e, t, n, r) {
          let i = this.findPlace(e.create(t), n, !1);
          return ((i &&= this.enterInner(e, t, n, !0, r)), i);
        }
        enterInner(e, t, n, r = !1, i) {
          this.closeExtra();
          let a = this.top;
          a.match = a.match && a.match.matchType(e);
          let o = ke(e, i, a.options);
          a.options & O && a.content.length == 0 && (o |= O);
          let s = S.none;
          return (
            (n = n.filter((t) =>
              (a.type ? a.type.allowsMarkType(t.type) : Ne(t.type, e))
                ? ((s = t.addToSet(s)), !1)
                : !0,
            )),
            this.nodes.push(new k(e, t, s, r, null, o)),
            this.open++,
            n
          );
        }
        closeExtra(e = !1) {
          let t = this.nodes.length - 1;
          if (t > this.open) {
            for (; t > this.open; t--)
              this.nodes[t - 1].content.push(this.nodes[t].finish(e));
            this.nodes.length = this.open + 1;
          }
        }
        finish() {
          return (
            (this.open = 0),
            this.closeExtra(this.isOpen),
            this.nodes[0].finish(!!(this.isOpen || this.options.topOpen))
          );
        }
        sync(e) {
          for (let t = this.open; t >= 0; t--)
            if (this.nodes[t] == e) return ((this.open = t), !0);
            else this.localPreserveWS && (this.nodes[t].options |= D);
          return !1;
        }
        get currentPos() {
          this.closeExtra();
          let e = 0;
          for (let t = this.open; t >= 0; t--) {
            let n = this.nodes[t].content;
            for (let t = n.length - 1; t >= 0; t--) e += n[t].nodeSize;
            t && e++;
          }
          return e;
        }
        findAtPoint(e, t) {
          if (this.find)
            for (let n = 0; n < this.find.length; n++)
              this.find[n].node == e &&
                this.find[n].offset == t &&
                (this.find[n].pos = this.currentPos);
        }
        findInside(e) {
          if (this.find)
            for (let t = 0; t < this.find.length; t++)
              this.find[t].pos == null &&
                e.nodeType == 1 &&
                e.contains(this.find[t].node) &&
                (this.find[t].pos = this.currentPos);
        }
        findAround(e, t, n) {
          if (e != t && this.find)
            for (let r = 0; r < this.find.length; r++)
              this.find[r].pos == null &&
                e.nodeType == 1 &&
                e.contains(this.find[r].node) &&
                t.compareDocumentPosition(this.find[r].node) & (n ? 2 : 4) &&
                (this.find[r].pos = this.currentPos);
        }
        findInText(e) {
          if (this.find)
            for (let t = 0; t < this.find.length; t++)
              this.find[t].node == e &&
                (this.find[t].pos =
                  this.currentPos - (e.nodeValue.length - this.find[t].offset));
        }
        matchesContext(e) {
          if (e.indexOf(`|`) > -1)
            return e.split(/\s*\|\s*/).some(this.matchesContext, this);
          let t = e.split(`/`),
            n = this.options.context,
            r = !this.isOpen && (!n || n.parent.type == this.nodes[0].type),
            i = -(n ? n.depth + 1 : 0) + +!r,
            a = (e, o) => {
              for (; e >= 0; e--) {
                let s = t[e];
                if (s == ``) {
                  if (e == t.length - 1 || e == 0) continue;
                  for (; o >= i; o--) if (a(e - 1, o)) return !0;
                  return !1;
                } else {
                  let e =
                    o > 0 || (o == 0 && r)
                      ? this.nodes[o].type
                      : n && o >= i
                        ? n.node(o - i).type
                        : null;
                  if (!e || (e.name != s && !e.isInGroup(s))) return !1;
                  o--;
                }
              }
              return !0;
            };
          return a(t.length - 1, this.open);
        }
        textblockFromContext() {
          let e = this.options.context;
          if (e)
            for (let t = e.depth; t >= 0; t--) {
              let n = e.node(t).contentMatchAt(e.indexAfter(t)).defaultType;
              if (n && n.isTextblock && n.defaultAttrs) return n;
            }
          for (let e in this.parser.schema.nodes) {
            let t = this.parser.schema.nodes[e];
            if (t.isTextblock && t.defaultAttrs) return t;
          }
        }
      }),
      (nt = class e {
        constructor(e, t) {
          ((this.nodes = e), (this.marks = t));
        }
        serializeFragment(e, t = {}, n) {
          n ||= Fe(t).createDocumentFragment();
          let r = n,
            i = [];
          return (
            e.forEach((e) => {
              if (i.length || e.marks.length) {
                let n = 0,
                  a = 0;
                for (; n < i.length && a < e.marks.length;) {
                  let t = e.marks[a];
                  if (!this.marks[t.type.name]) {
                    a++;
                    continue;
                  }
                  if (!t.eq(i[n][0]) || t.type.spec.spanning === !1) break;
                  (n++, a++);
                }
                for (; n < i.length;) r = i.pop()[1];
                for (; a < e.marks.length;) {
                  let n = e.marks[a++],
                    o = this.serializeMark(n, e.isInline, t);
                  o &&
                    (i.push([n, r]),
                    r.appendChild(o.dom),
                    (r = o.contentDOM || o.dom));
                }
              }
              r.appendChild(this.serializeNodeInner(e, t));
            }),
            n
          );
        }
        serializeNodeInner(e, t) {
          let { dom: n, contentDOM: r } = y(
            Fe(t),
            this.nodes[e.type.name](e),
            null,
            e.attrs,
          );
          if (r) {
            if (e.isLeaf)
              throw RangeError(`Content hole not allowed in a leaf node spec`);
            this.serializeFragment(e.content, t, r);
          }
          return n;
        }
        serializeNode(e, t = {}) {
          let n = this.serializeNodeInner(e, t);
          for (let r = e.marks.length - 1; r >= 0; r--) {
            let i = this.serializeMark(e.marks[r], e.isInline, t);
            i && ((i.contentDOM || i.dom).appendChild(n), (n = i.dom));
          }
          return n;
        }
        serializeMark(e, t, n = {}) {
          let r = this.marks[e.type.name];
          return r && y(Fe(n), r(e, t), null, e.attrs);
        }
        static renderSpec(e, t, n = null, r) {
          return y(e, t, n, r);
        }
        static fromSchema(t) {
          return (
            t.cached.domSerializer ||
            (t.cached.domSerializer = new e(
              this.nodesFromSchema(t),
              this.marksFromSchema(t),
            ))
          );
        }
        static nodesFromSchema(e) {
          let t = Pe(e.nodes);
          return ((t.text ||= (e) => e.text), t);
        }
        static marksFromSchema(e) {
          return Pe(e.marks);
        }
      }),
      (rt = new WeakMap()));
  });
function at(e, t) {
  return e + t * qt;
}
function ot(e) {
  return e & Kt;
}
function st(e) {
  return (e - (e & Kt)) / qt;
}
function ct(e, t, n) {
  let r = [];
  for (let i = 0; i < e.childCount; i++) {
    let a = e.child(i);
    (a.content.size && (a = a.copy(ct(a.content, t, a))),
      a.isInline && (a = t(a, n, i)),
      r.push(a));
  }
  return b.fromArray(r);
}
function lt(e, t, n) {
  let r = e.resolve(t),
    i = n - t,
    a = r.depth;
  for (; i > 0 && a > 0 && r.indexAfter(a) == r.node(a).childCount;) (a--, i--);
  if (i > 0) {
    let e = r.node(a).maybeChild(r.indexAfter(a));
    for (; i > 0;) {
      if (!e || e.isLeaf) return !0;
      ((e = e.firstChild), i--);
    }
  }
  return !1;
}
function ut(e, t, n, r) {
  let i = [],
    a = [],
    o,
    s;
  (e.doc.nodesBetween(t, n, (e, c, l) => {
    if (!e.isInline) return;
    let u = e.marks;
    if (!r.isInSet(u) && l.type.allowsMarkType(r.type)) {
      let l = Math.max(c, t),
        d = Math.min(c + e.nodeSize, n),
        f = r.addToSet(u);
      for (let e = 0; e < u.length; e++)
        u[e].isInSet(f) ||
          (o && o.to == l && o.mark.eq(u[e])
            ? (o.to = d)
            : i.push((o = new R(l, d, u[e]))));
      s && s.to == l ? (s.to = d) : a.push((s = new L(l, d, r)));
    }
  }),
    i.forEach((t) => e.step(t)),
    a.forEach((t) => e.step(t)));
}
function dt(e, t, n, r) {
  let i = [],
    a = 0;
  (e.doc.nodesBetween(t, n, (e, o) => {
    if (!e.isInline) return;
    a++;
    let s = null;
    if (r instanceof Je) {
      let t = e.marks,
        n;
      for (; (n = r.isInSet(t));)
        ((s ||= []).push(n), (t = n.removeFromSet(t)));
    } else r ? r.isInSet(e.marks) && (s = [r]) : (s = e.marks);
    if (s && s.length) {
      let r = Math.min(o + e.nodeSize, n);
      for (let e = 0; e < s.length; e++) {
        let n = s[e],
          c;
        for (let e = 0; e < i.length; e++) {
          let t = i[e];
          t.step == a - 1 && n.eq(i[e].style) && (c = t);
        }
        c
          ? ((c.to = r), (c.step = a))
          : i.push({ style: n, from: Math.max(o, t), to: r, step: a });
      }
    }
  }),
    i.forEach((t) => e.step(new R(t.from, t.to, t.style))));
}
function ft(e, t, n, r = n.contentMatch, i = !0) {
  let a = e.doc.nodeAt(t),
    o = [],
    s = t + 1;
  for (let t = 0; t < a.childCount; t++) {
    let c = a.child(t),
      l = s + c.nodeSize,
      u = r.matchType(c.type);
    if (!u) o.push(new V(s, l, w.empty));
    else {
      r = u;
      for (let t = 0; t < c.marks.length; t++)
        n.allowsMarkType(c.marks[t].type) || e.step(new R(s, l, c.marks[t]));
      if (i && c.isText && n.whitespace != `pre`) {
        let e,
          t = /\r?\n|\r/g,
          r;
        for (; (e = t.exec(c.text));)
          ((r ||= new w(
            b.from(n.schema.text(` `, n.allowedMarks(c.marks))),
            0,
            0,
          )),
            o.push(new V(s + e.index, s + e.index + e[0].length, r)));
      }
    }
    s = l;
  }
  if (!r.validEnd) {
    let t = r.fillBefore(b.empty, !0);
    e.replace(s, s, new w(t, 0, 0));
  }
  for (let t = o.length - 1; t >= 0; t--) e.step(o[t]);
}
function pt(e, t, n) {
  return (
    (t == 0 || e.canReplace(t, e.childCount)) &&
    (n == e.childCount || e.canReplace(0, n))
  );
}
function mt(e) {
  let t = e.parent.content.cutByIndex(e.startIndex, e.endIndex);
  for (let n = e.depth, r = 0, i = 0; ; --n) {
    let a = e.$from.node(n),
      o = e.$from.index(n) + r,
      s = e.$to.indexAfter(n) - i;
    if (n < e.depth && a.canReplace(o, s, t)) return n;
    if (n == 0 || a.type.spec.isolating || !pt(a, o, s)) break;
    (o && (r = 1), s < a.childCount && (i = 1));
  }
  return null;
}
function ht(e, t, n) {
  let { $from: r, $to: i, depth: a } = t,
    o = r.before(a + 1),
    s = i.after(a + 1),
    c = o,
    l = s,
    u = b.empty,
    d = 0;
  for (let e = a, t = !1; e > n; e--)
    t || r.index(e) > 0
      ? ((t = !0), (u = b.from(r.node(e).copy(u))), d++)
      : c--;
  let f = b.empty,
    p = 0;
  for (let e = a, t = !1; e > n; e--)
    t || i.after(e + 1) < i.end(e)
      ? ((t = !0), (f = b.from(i.node(e).copy(f))), p++)
      : l++;
  e.step(new H(c, l, o, s, new w(u.append(f), d, p), u.size - d, !0));
}
function gt(e, t, n = null, r = e) {
  let i = vt(e, t),
    a = i && yt(r, t);
  return a ? i.map(_t).concat({ type: t, attrs: n }).concat(a.map(_t)) : null;
}
function _t(e) {
  return { type: e, attrs: null };
}
function vt(e, t) {
  let { parent: n, startIndex: r, endIndex: i } = e,
    a = n.contentMatchAt(r).findWrapping(t);
  if (!a) return null;
  let o = a.length ? a[0] : t;
  return n.canReplaceWith(r, i, o) ? a : null;
}
function yt(e, t) {
  let { parent: n, startIndex: r, endIndex: i } = e,
    a = n.child(r),
    o = t.contentMatch.findWrapping(a.type);
  if (!o) return null;
  let s = (o.length ? o[o.length - 1] : t).contentMatch;
  for (let e = r; s && e < i; e++) s = s.matchType(n.child(e).type);
  return !s || !s.validEnd ? null : o;
}
function bt(e, t, n) {
  let r = b.empty;
  for (let e = n.length - 1; e >= 0; e--) {
    if (r.size) {
      let t = n[e].type.contentMatch.matchFragment(r);
      if (!t || !t.validEnd)
        throw RangeError(
          `Wrapper type given to Transform.wrap does not form valid content of its parent wrapper`,
        );
    }
    r = b.from(n[e].type.create(n[e].attrs, r));
  }
  let i = t.start,
    a = t.end;
  e.step(new H(i, a, i, a, new w(r, 0, 0), n.length, !0));
}
function xt(e, t, n, r, i) {
  if (!r.isTextblock)
    throw RangeError(`Type given to setBlockType should be a textblock`);
  let a = e.steps.length;
  e.doc.nodesBetween(t, n, (t, n) => {
    let o = typeof i == `function` ? i(t) : i;
    if (
      t.isTextblock &&
      !t.hasMarkup(r, o) &&
      wt(e.doc, e.mapping.slice(a).map(n), r)
    ) {
      let i = null;
      if (r.schema.linebreakReplacement) {
        let e = r.whitespace == `pre`,
          t = !!r.contentMatch.matchType(r.schema.linebreakReplacement);
        e && !t ? (i = !1) : !e && t && (i = !0);
      }
      (i === !1 && Ct(e, t, n, a),
        ft(e, e.mapping.slice(a).map(n, 1), r, void 0, i === null));
      let s = e.mapping.slice(a),
        c = s.map(n, 1),
        l = s.map(n + t.nodeSize, 1);
      return (
        e.step(
          new H(
            c,
            l,
            c + 1,
            l - 1,
            new w(b.from(r.create(o, null, t.marks)), 0, 0),
            1,
            !0,
          ),
        ),
        i === !0 && St(e, t, n, a),
        !1
      );
    }
  });
}
function St(e, t, n, r) {
  t.forEach((i, a) => {
    if (i.isText) {
      let o,
        s = /\r?\n|\r/g;
      for (; (o = s.exec(i.text));) {
        let i = e.mapping.slice(r).map(n + 1 + a + o.index);
        e.replaceWith(i, i + 1, t.type.schema.linebreakReplacement.create());
      }
    }
  });
}
function Ct(e, t, n, r) {
  t.forEach((i, a) => {
    if (i.type == i.type.schema.linebreakReplacement) {
      let i = e.mapping.slice(r).map(n + 1 + a);
      e.replaceWith(
        i,
        i + 1,
        t.type.schema.text(`
`),
      );
    }
  });
}
function wt(e, t, n) {
  let r = e.resolve(t),
    i = r.index();
  return r.parent.canReplaceWith(i, i + 1, n);
}
function Tt(e, t, n, r, i) {
  let a = e.doc.nodeAt(t);
  if (!a) throw RangeError(`No node at given position`);
  n ||= a.type;
  let o = n.create(r, null, i || a.marks);
  if (a.isLeaf) return e.replaceWith(t, t + a.nodeSize, o);
  if (!n.validContent(a.content))
    throw RangeError(`Invalid content for node type ` + n.name);
  e.step(
    new H(
      t,
      t + a.nodeSize,
      t + 1,
      t + a.nodeSize - 1,
      new w(b.from(o), 0, 0),
      1,
      !0,
    ),
  );
}
function Et(e, t, n = 1, r) {
  let i = e.resolve(t),
    a = i.depth - n,
    o = (r && r[r.length - 1]) || i.parent;
  if (
    a < 0 ||
    i.parent.type.spec.isolating ||
    !i.parent.canReplace(i.index(), i.parent.childCount) ||
    !o.type.validContent(
      i.parent.content.cutByIndex(i.index(), i.parent.childCount),
    )
  )
    return !1;
  for (let e = i.depth - 1, t = n - 2; e > a; e--, t--) {
    let n = i.node(e),
      a = i.index(e);
    if (n.type.spec.isolating) return !1;
    let o = n.content.cutByIndex(a, n.childCount),
      s = r && r[t + 1];
    s && (o = o.replaceChild(0, s.type.create(s.attrs)));
    let c = (r && r[t]) || n;
    if (!n.canReplace(a + 1, n.childCount) || !c.type.validContent(o))
      return !1;
  }
  let s = i.indexAfter(a),
    c = r && r[0];
  return i.node(a).canReplaceWith(s, s, c ? c.type : i.node(a + 1).type);
}
function Dt(e, t, n = 1, r) {
  let i = e.doc.resolve(t),
    a = b.empty,
    o = b.empty;
  for (let e = i.depth, t = i.depth - n, s = n - 1; e > t; e--, s--) {
    a = b.from(i.node(e).copy(a));
    let t = r && r[s];
    o = b.from(t ? t.type.create(t.attrs, o) : i.node(e).copy(o));
  }
  e.step(new V(t, t, new w(a.append(o), n, n), !0));
}
function Ot(e, t) {
  let n = e.resolve(t),
    r = n.index();
  return At(n.nodeBefore, n.nodeAfter) && n.parent.canReplace(r, r + 1);
}
function kt(e, t) {
  t.content.size || e.type.compatibleContent(t.type);
  let n = e.contentMatchAt(e.childCount),
    { linebreakReplacement: r } = e.type.schema;
  for (let i = 0; i < t.childCount; i++) {
    let a = t.child(i),
      o = a.type == r ? e.type.schema.nodes.text : a.type;
    if (((n = n.matchType(o)), !n || !e.type.allowsMarks(a.marks))) return !1;
  }
  return n.validEnd;
}
function At(e, t) {
  return !!(e && t && !e.isLeaf && kt(e, t));
}
function jt(e, t, n) {
  let r = null,
    { linebreakReplacement: i } = e.doc.type.schema,
    a = e.doc.resolve(t - n),
    o = a.node().type;
  if (i && o.inlineContent) {
    let e = o.whitespace == `pre`,
      t = !!o.contentMatch.matchType(i);
    e && !t ? (r = !1) : !e && t && (r = !0);
  }
  let s = e.steps.length;
  if (r === !1) {
    let r = e.doc.resolve(t + n);
    Ct(e, r.node(), r.before(), s);
  }
  o.inlineContent &&
    ft(e, t + n - 1, o, a.node().contentMatchAt(a.index()), r == null);
  let c = e.mapping.slice(s),
    l = c.map(t - n);
  if ((e.step(new V(l, c.map(t + n, -1), w.empty, !0)), r === !0)) {
    let t = e.doc.resolve(l);
    St(e, t.node(), t.before(), e.steps.length);
  }
  return e;
}
function Mt(e, t, n) {
  let r = e.resolve(t);
  if (r.parent.canReplaceWith(r.index(), r.index(), n)) return t;
  if (r.parentOffset == 0)
    for (let e = r.depth - 1; e >= 0; e--) {
      let t = r.index(e);
      if (r.node(e).canReplaceWith(t, t, n)) return r.before(e + 1);
      if (t > 0) return null;
    }
  if (r.parentOffset == r.parent.content.size)
    for (let e = r.depth - 1; e >= 0; e--) {
      let t = r.indexAfter(e);
      if (r.node(e).canReplaceWith(t, t, n)) return r.after(e + 1);
      if (t < r.node(e).childCount) return null;
    }
  return null;
}
function Nt(e, t, n) {
  let r = e.resolve(t);
  if (!n.content.size) return t;
  let i = n.content;
  for (let e = 0; e < n.openStart; e++) i = i.firstChild.content;
  for (let e = 1; e <= (n.openStart == 0 && n.size ? 2 : 1); e++)
    for (let t = r.depth; t >= 0; t--) {
      let n =
          t == r.depth
            ? 0
            : r.pos <= (r.start(t + 1) + r.end(t + 1)) / 2
              ? -1
              : 1,
        a = r.index(t) + +(n > 0),
        o = r.node(t),
        s = !1;
      if (e == 1) s = o.canReplace(a, a, i);
      else {
        let e = o.contentMatchAt(a).findWrapping(i.firstChild.type);
        s = e && o.canReplaceWith(a, a, e[0]);
      }
      if (s) return n == 0 ? r.pos : n < 0 ? r.before(t + 1) : r.after(t + 1);
    }
  return null;
}
function Pt(e, t, n = t, r = w.empty) {
  if (t == n && !r.size) return null;
  let i = e.resolve(t),
    a = e.resolve(n);
  return Ft(i, a, r) ? new V(t, n, r) : new $t(i, a, r).fit();
}
function Ft(e, t, n) {
  return (
    !n.openStart &&
    !n.openEnd &&
    e.start() == t.start() &&
    e.parent.canReplace(e.index(), t.index(), n.content)
  );
}
function A(e, t, n) {
  return t == 0
    ? e.cutByIndex(n, e.childCount)
    : e.replaceChild(0, e.firstChild.copy(A(e.firstChild.content, t - 1, n)));
}
function j(e, t, n) {
  return t == 0
    ? e.append(n)
    : e.replaceChild(
        e.childCount - 1,
        e.lastChild.copy(j(e.lastChild.content, t - 1, n)),
      );
}
function It(e, t) {
  for (let n = 0; n < t; n++) e = e.firstChild.content;
  return e;
}
function Lt(e, t, n) {
  if (t <= 0) return e;
  let r = e.content;
  return (
    t > 1 &&
      (r = r.replaceChild(
        0,
        Lt(r.firstChild, t - 1, r.childCount == 1 ? n - 1 : 0),
      )),
    t > 0 &&
      ((r = e.type.contentMatch.fillBefore(r).append(r)),
      n <= 0 &&
        (r = r.append(
          e.type.contentMatch.matchFragment(r).fillBefore(b.empty, !0),
        ))),
    e.copy(r)
  );
}
function Rt(e, t, n, r, i) {
  let a = e.node(t),
    o = i ? e.indexAfter(t) : e.index(t);
  if (o == a.childCount && !n.compatibleContent(a.type)) return null;
  let s = r.fillBefore(a.content, !0, o);
  return s && !zt(n, a.content, o) ? s : null;
}
function zt(e, t, n) {
  for (let r = n; r < t.childCount; r++)
    if (!e.allowsMarks(t.child(r).marks)) return !0;
  return !1;
}
function Bt(e) {
  return e.spec.defining || e.spec.definingForContent;
}
function Vt(e, t, n, r) {
  if (!r.size) return e.deleteRange(t, n);
  let i = e.doc.resolve(t),
    a = e.doc.resolve(n);
  if (Ft(i, a, r)) return e.step(new V(t, n, r));
  let o = Gt(i, a);
  o[o.length - 1] == 0 && o.pop();
  let s = -(i.depth + 1);
  o.unshift(s);
  for (let e = i.depth, t = i.pos - 1; e > 0; e--, t--) {
    let n = i.node(e).type.spec;
    if (n.defining || n.definingAsContext || n.isolating) break;
    o.indexOf(e) > -1 ? (s = e) : i.before(e) == t && o.splice(1, 0, -e);
  }
  let c = o.indexOf(s),
    l = [],
    u = r.openStart;
  for (let e = r.content, t = 0; ; t++) {
    let n = e.firstChild;
    if ((l.push(n), t == r.openStart)) break;
    e = n.content;
  }
  for (let e = u - 1; e >= 0; e--) {
    let t = l[e],
      n = Bt(t.type);
    if (n && !t.sameMarkup(i.node(Math.abs(s) - 1))) u = e;
    else if (n || !t.type.isTextblock) break;
  }
  for (let t = r.openStart; t >= 0; t--) {
    let s = (t + u + 1) % (r.openStart + 1),
      d = l[s];
    if (d)
      for (let t = 0; t < o.length; t++) {
        let l = o[(t + c) % o.length],
          u = !0;
        l < 0 && ((u = !1), (l = -l));
        let f = i.node(l - 1),
          p = i.index(l - 1);
        if (f.canReplaceWith(p, p, d.type, d.marks))
          return e.replace(
            i.before(l),
            u ? a.after(l) : n,
            new w(Ht(r.content, 0, r.openStart, s), s, r.openEnd),
          );
      }
  }
  let d = e.steps.length;
  for (
    let s = o.length - 1;
    s >= 0 && (e.replace(t, n, r), !(e.steps.length > d));
    s--
  ) {
    let e = o[s];
    e < 0 || ((t = i.before(e)), (n = a.after(e)));
  }
}
function Ht(e, t, n, r, i) {
  if (t < n) {
    let i = e.firstChild;
    e = e.replaceChild(0, i.copy(Ht(i.content, t + 1, n, r, i)));
  }
  if (t > r) {
    let t = i.contentMatchAt(0),
      n = t.fillBefore(e).append(e);
    e = n.append(t.matchFragment(n).fillBefore(b.empty, !0));
  }
  return e;
}
function Ut(e, t, n, r) {
  if (!r.isInline && t == n && e.doc.resolve(t).parent.content.size) {
    let i = Mt(e.doc, t, r.type);
    i != null && (t = n = i);
  }
  e.replaceRange(t, n, new w(b.from(r), 0, 0));
}
function Wt(e, t, n) {
  let r = e.doc.resolve(t),
    i = e.doc.resolve(n);
  if (
    r.parent.isTextblock &&
    i.parent.isTextblock &&
    r.start() != i.start() &&
    r.parentOffset == 0 &&
    i.parentOffset == 0
  ) {
    let a = r.sharedDepth(n),
      o = !1;
    for (let e = r.depth; e > a; e--) r.node(e).type.spec.isolating && (o = !0);
    for (let e = i.depth; e > a; e--) i.node(e).type.spec.isolating && (o = !0);
    if (!o) {
      for (let e = r.depth; e > 0 && t == r.start(e); e--) t = r.before(e);
      for (let e = i.depth; e > 0 && n == i.start(e); e--) n = i.before(e);
      ((r = e.doc.resolve(t)), (i = e.doc.resolve(n)));
    }
  }
  let a = Gt(r, i);
  for (let t = 0; t < a.length; t++) {
    let n = a[t],
      o = t == a.length - 1;
    if ((o && n == 0) || r.node(n).type.contentMatch.validEnd)
      return e.delete(r.start(n), i.end(n));
    if (
      n > 0 &&
      (o || r.node(n - 1).canReplace(r.index(n - 1), i.indexAfter(n - 1)))
    )
      return e.delete(r.before(n), i.after(n));
  }
  for (let a = 1; a <= r.depth && a <= i.depth; a++)
    if (
      t - r.start(a) == r.depth - a &&
      n > r.end(a) &&
      i.end(a) - n != i.depth - a &&
      r.start(a - 1) == i.start(a - 1) &&
      r.node(a - 1).canReplace(r.index(a - 1), i.index(a - 1))
    )
      return e.delete(r.before(a), n);
  e.delete(t, n);
}
function Gt(e, t) {
  let n = [],
    r = Math.min(e.depth, t.depth);
  for (let i = r; i >= 0; i--) {
    let r = e.start(i);
    if (
      r < e.pos - (e.depth - i) ||
      t.end(i) > t.pos + (t.depth - i) ||
      e.node(i).type.spec.isolating ||
      t.node(i).type.spec.isolating
    )
      break;
    (r == t.start(i) ||
      (i == e.depth &&
        i == t.depth &&
        e.parent.inlineContent &&
        t.parent.inlineContent &&
        i &&
        t.start(i - 1) == r - 1)) &&
      n.push(i);
  }
  return n;
}
var Kt,
  qt,
  Jt,
  Yt,
  Xt,
  Zt,
  M,
  N,
  Qt,
  P,
  F,
  I,
  L,
  R,
  z,
  B,
  V,
  H,
  $t,
  en,
  tn,
  U,
  nn,
  rn = e(() => {
    (it(),
      (Kt = 65535),
      (qt = 2 ** 16),
      (Jt = 1),
      (Yt = 2),
      (Xt = 4),
      (Zt = 8),
      (M = class {
        constructor(e, t, n) {
          ((this.pos = e), (this.delInfo = t), (this.recover = n));
        }
        get deleted() {
          return (this.delInfo & Zt) > 0;
        }
        get deletedBefore() {
          return (this.delInfo & 5) > 0;
        }
        get deletedAfter() {
          return (this.delInfo & 6) > 0;
        }
        get deletedAcross() {
          return (this.delInfo & Xt) > 0;
        }
      }),
      (N = class e {
        constructor(t, n = !1) {
          if (((this.ranges = t), (this.inverted = n), !t.length && e.empty))
            return e.empty;
        }
        recover(e) {
          let t = 0,
            n = ot(e);
          if (!this.inverted)
            for (let e = 0; e < n; e++)
              t += this.ranges[e * 3 + 2] - this.ranges[e * 3 + 1];
          return this.ranges[n * 3] + t + st(e);
        }
        mapResult(e, t = 1) {
          return this._map(e, t, !1);
        }
        map(e, t = 1) {
          return this._map(e, t, !0);
        }
        _map(e, t, n) {
          let r = 0,
            i = this.inverted ? 2 : 1,
            a = this.inverted ? 1 : 2;
          for (let o = 0; o < this.ranges.length; o += 3) {
            let s = this.ranges[o] - (this.inverted ? r : 0);
            if (s > e) break;
            let c = this.ranges[o + i],
              l = this.ranges[o + a],
              u = s + c;
            if (e <= u) {
              let i = c ? (e == s ? -1 : e == u ? 1 : t) : t,
                a = s + r + (i < 0 ? 0 : l);
              if (n) return a;
              let d = e == (t < 0 ? s : u) ? null : at(o / 3, e - s),
                f = e == s ? Yt : e == u ? Jt : Xt;
              return ((t < 0 ? e != s : e != u) && (f |= Zt), new M(a, f, d));
            }
            r += l - c;
          }
          return n ? e + r : new M(e + r, 0, null);
        }
        touches(e, t) {
          let n = 0,
            r = ot(t),
            i = this.inverted ? 2 : 1,
            a = this.inverted ? 1 : 2;
          for (let t = 0; t < this.ranges.length; t += 3) {
            let o = this.ranges[t] - (this.inverted ? n : 0);
            if (o > e) break;
            let s = this.ranges[t + i];
            if (e <= o + s && t == r * 3) return !0;
            n += this.ranges[t + a] - s;
          }
          return !1;
        }
        forEach(e) {
          let t = this.inverted ? 2 : 1,
            n = this.inverted ? 1 : 2;
          for (let r = 0, i = 0; r < this.ranges.length; r += 3) {
            let a = this.ranges[r],
              o = a - (this.inverted ? i : 0),
              s = a + (this.inverted ? 0 : i),
              c = this.ranges[r + t],
              l = this.ranges[r + n];
            (e(o, o + c, s, s + l), (i += l - c));
          }
        }
        invert() {
          return new e(this.ranges, !this.inverted);
        }
        toString() {
          return (this.inverted ? `-` : ``) + JSON.stringify(this.ranges);
        }
        static offset(t) {
          return t == 0 ? e.empty : new e(t < 0 ? [0, -t, 0] : [0, 0, t]);
        }
      }),
      (N.empty = new N([])),
      (Qt = class e {
        constructor(e, t, n = 0, r = e ? e.length : 0) {
          ((this.mirror = t),
            (this.from = n),
            (this.to = r),
            (this._maps = e || []),
            (this.ownData = !(e || t)));
        }
        get maps() {
          return this._maps;
        }
        slice(t = 0, n = this.maps.length) {
          return new e(this._maps, this.mirror, t, n);
        }
        appendMap(e, t) {
          ((this.ownData ||=
            ((this._maps = this._maps.slice()),
            (this.mirror = this.mirror && this.mirror.slice()),
            !0)),
            (this.to = this._maps.push(e)),
            t != null && this.setMirror(this._maps.length - 1, t));
        }
        appendMapping(e) {
          for (let t = 0, n = this._maps.length; t < e._maps.length; t++) {
            let r = e.getMirror(t);
            this.appendMap(e._maps[t], r != null && r < t ? n + r : void 0);
          }
        }
        getMirror(e) {
          if (this.mirror) {
            for (let t = 0; t < this.mirror.length; t++)
              if (this.mirror[t] == e) return this.mirror[t + (t % 2 ? -1 : 1)];
          }
        }
        setMirror(e, t) {
          ((this.mirror ||= []), this.mirror.push(e, t));
        }
        appendMappingInverted(e) {
          for (
            let t = e.maps.length - 1, n = this._maps.length + e._maps.length;
            t >= 0;
            t--
          ) {
            let r = e.getMirror(t);
            this.appendMap(
              e._maps[t].invert(),
              r != null && r > t ? n - r - 1 : void 0,
            );
          }
        }
        invert() {
          let t = new e();
          return (t.appendMappingInverted(this), t);
        }
        map(e, t = 1) {
          if (this.mirror) return this._map(e, t, !0);
          for (let n = this.from; n < this.to; n++) e = this._maps[n].map(e, t);
          return e;
        }
        mapResult(e, t = 1) {
          return this._map(e, t, !1);
        }
        _map(e, t, n) {
          let r = 0;
          for (let n = this.from; n < this.to; n++) {
            let i = this._maps[n].mapResult(e, t);
            if (i.recover != null) {
              let t = this.getMirror(n);
              if (t != null && t > n && t < this.to) {
                ((n = t), (e = this._maps[t].recover(i.recover)));
                continue;
              }
            }
            ((r |= i.delInfo), (e = i.pos));
          }
          return n ? e : new M(e, r, null);
        }
      }),
      (P = Object.create(null)),
      (F = class {
        getMap() {
          return N.empty;
        }
        merge(e) {
          return null;
        }
        static fromJSON(e, t) {
          if (!t || !t.stepType)
            throw RangeError(`Invalid input for Step.fromJSON`);
          let n = P[t.stepType];
          if (!n) throw RangeError(`No step type ${t.stepType} defined`);
          return n.fromJSON(e, t);
        }
        static jsonID(e, t) {
          if (e in P) throw RangeError(`Duplicate use of step JSON ID ` + e);
          return ((P[e] = t), (t.prototype.jsonID = e), t);
        }
      }),
      (I = class e {
        constructor(e, t) {
          ((this.doc = e), (this.failed = t));
        }
        static ok(t) {
          return new e(t, null);
        }
        static fail(t) {
          return new e(null, t);
        }
        static fromReplace(t, n, r, i) {
          try {
            return e.ok(t.replace(n, r, i));
          } catch (t) {
            if (t instanceof C) return e.fail(t.message);
            throw t;
          }
        }
      }),
      (L = class e extends F {
        constructor(e, t, n) {
          (super(), (this.from = e), (this.to = t), (this.mark = n));
        }
        apply(e) {
          let t = e.slice(this.from, this.to),
            n = e.resolve(this.from),
            r = n.node(n.sharedDepth(this.to)),
            i = new w(
              ct(
                t.content,
                (e, t) =>
                  !e.isAtom || !t.type.allowsMarkType(this.mark.type)
                    ? e
                    : e.mark(this.mark.addToSet(e.marks)),
                r,
              ),
              t.openStart,
              t.openEnd,
            );
          return I.fromReplace(e, this.from, this.to, i);
        }
        invert() {
          return new R(this.from, this.to, this.mark);
        }
        map(t) {
          let n = t.mapResult(this.from, 1),
            r = t.mapResult(this.to, -1);
          return (n.deleted && r.deleted) || n.pos >= r.pos
            ? null
            : new e(n.pos, r.pos, this.mark);
        }
        merge(t) {
          return t instanceof e &&
            t.mark.eq(this.mark) &&
            this.from <= t.to &&
            this.to >= t.from
            ? new e(
                Math.min(this.from, t.from),
                Math.max(this.to, t.to),
                this.mark,
              )
            : null;
        }
        toJSON() {
          return {
            stepType: `addMark`,
            mark: this.mark.toJSON(),
            from: this.from,
            to: this.to,
          };
        }
        static fromJSON(t, n) {
          if (typeof n.from != `number` || typeof n.to != `number`)
            throw RangeError(`Invalid input for AddMarkStep.fromJSON`);
          return new e(n.from, n.to, t.markFromJSON(n.mark));
        }
      }),
      F.jsonID(`addMark`, L),
      (R = class e extends F {
        constructor(e, t, n) {
          (super(), (this.from = e), (this.to = t), (this.mark = n));
        }
        apply(e) {
          let t = e.slice(this.from, this.to),
            n = new w(
              ct(t.content, (e) => e.mark(this.mark.removeFromSet(e.marks)), e),
              t.openStart,
              t.openEnd,
            );
          return I.fromReplace(e, this.from, this.to, n);
        }
        invert() {
          return new L(this.from, this.to, this.mark);
        }
        map(t) {
          let n = t.mapResult(this.from, 1),
            r = t.mapResult(this.to, -1);
          return (n.deleted && r.deleted) || n.pos >= r.pos
            ? null
            : new e(n.pos, r.pos, this.mark);
        }
        merge(t) {
          return t instanceof e &&
            t.mark.eq(this.mark) &&
            this.from <= t.to &&
            this.to >= t.from
            ? new e(
                Math.min(this.from, t.from),
                Math.max(this.to, t.to),
                this.mark,
              )
            : null;
        }
        toJSON() {
          return {
            stepType: `removeMark`,
            mark: this.mark.toJSON(),
            from: this.from,
            to: this.to,
          };
        }
        static fromJSON(t, n) {
          if (typeof n.from != `number` || typeof n.to != `number`)
            throw RangeError(`Invalid input for RemoveMarkStep.fromJSON`);
          return new e(n.from, n.to, t.markFromJSON(n.mark));
        }
      }),
      F.jsonID(`removeMark`, R),
      (z = class e extends F {
        constructor(e, t) {
          (super(), (this.pos = e), (this.mark = t));
        }
        apply(e) {
          let t = e.nodeAt(this.pos);
          if (!t) return I.fail(`No node at mark step's position`);
          let n = t.type.create(t.attrs, null, this.mark.addToSet(t.marks));
          return I.fromReplace(
            e,
            this.pos,
            this.pos + 1,
            new w(b.from(n), 0, +!t.isLeaf),
          );
        }
        invert(t) {
          let n = t.nodeAt(this.pos);
          if (n) {
            let t = this.mark.addToSet(n.marks);
            if (t.length == n.marks.length) {
              for (let r = 0; r < n.marks.length; r++)
                if (!n.marks[r].isInSet(t)) return new e(this.pos, n.marks[r]);
              return new e(this.pos, this.mark);
            }
          }
          return new B(this.pos, this.mark);
        }
        map(t) {
          let n = t.mapResult(this.pos, 1);
          return n.deletedAfter ? null : new e(n.pos, this.mark);
        }
        toJSON() {
          return {
            stepType: `addNodeMark`,
            pos: this.pos,
            mark: this.mark.toJSON(),
          };
        }
        static fromJSON(t, n) {
          if (typeof n.pos != `number`)
            throw RangeError(`Invalid input for AddNodeMarkStep.fromJSON`);
          return new e(n.pos, t.markFromJSON(n.mark));
        }
      }),
      F.jsonID(`addNodeMark`, z),
      (B = class e extends F {
        constructor(e, t) {
          (super(), (this.pos = e), (this.mark = t));
        }
        apply(e) {
          let t = e.nodeAt(this.pos);
          if (!t) return I.fail(`No node at mark step's position`);
          let n = t.type.create(
            t.attrs,
            null,
            this.mark.removeFromSet(t.marks),
          );
          return I.fromReplace(
            e,
            this.pos,
            this.pos + 1,
            new w(b.from(n), 0, +!t.isLeaf),
          );
        }
        invert(e) {
          let t = e.nodeAt(this.pos);
          return !t || !this.mark.isInSet(t.marks)
            ? this
            : new z(this.pos, this.mark);
        }
        map(t) {
          let n = t.mapResult(this.pos, 1);
          return n.deletedAfter ? null : new e(n.pos, this.mark);
        }
        toJSON() {
          return {
            stepType: `removeNodeMark`,
            pos: this.pos,
            mark: this.mark.toJSON(),
          };
        }
        static fromJSON(t, n) {
          if (typeof n.pos != `number`)
            throw RangeError(`Invalid input for RemoveNodeMarkStep.fromJSON`);
          return new e(n.pos, t.markFromJSON(n.mark));
        }
      }),
      F.jsonID(`removeNodeMark`, B),
      (V = class e extends F {
        constructor(e, t, n, r = !1) {
          (super(),
            (this.from = e),
            (this.to = t),
            (this.slice = n),
            (this.structure = r));
        }
        apply(e) {
          return this.structure && lt(e, this.from, this.to)
            ? I.fail(`Structure replace would overwrite content`)
            : I.fromReplace(e, this.from, this.to, this.slice);
        }
        getMap() {
          return new N([this.from, this.to - this.from, this.slice.size]);
        }
        invert(t) {
          return new e(
            this.from,
            this.from + this.slice.size,
            t.slice(this.from, this.to),
          );
        }
        map(t) {
          let n = t.mapResult(this.to, -1),
            r =
              this.from == this.to && e.MAP_BIAS < 0
                ? n
                : t.mapResult(this.from, 1);
          return r.deletedAcross && n.deletedAcross
            ? null
            : new e(r.pos, Math.max(r.pos, n.pos), this.slice, this.structure);
        }
        merge(t) {
          if (!(t instanceof e) || t.structure || this.structure) return null;
          if (
            this.from + this.slice.size == t.from &&
            !this.slice.openEnd &&
            !t.slice.openStart
          ) {
            let n =
              this.slice.size + t.slice.size == 0
                ? w.empty
                : new w(
                    this.slice.content.append(t.slice.content),
                    this.slice.openStart,
                    t.slice.openEnd,
                  );
            return new e(
              this.from,
              this.to + (t.to - t.from),
              n,
              this.structure,
            );
          } else if (
            t.to == this.from &&
            !this.slice.openStart &&
            !t.slice.openEnd
          ) {
            let n =
              this.slice.size + t.slice.size == 0
                ? w.empty
                : new w(
                    t.slice.content.append(this.slice.content),
                    t.slice.openStart,
                    this.slice.openEnd,
                  );
            return new e(t.from, this.to, n, this.structure);
          } else return null;
        }
        toJSON() {
          let e = { stepType: `replace`, from: this.from, to: this.to };
          return (
            this.slice.size && (e.slice = this.slice.toJSON()),
            this.structure && (e.structure = !0),
            e
          );
        }
        static fromJSON(t, n) {
          if (typeof n.from != `number` || typeof n.to != `number`)
            throw RangeError(`Invalid input for ReplaceStep.fromJSON`);
          return new e(n.from, n.to, w.fromJSON(t, n.slice), !!n.structure);
        }
      }),
      (V.MAP_BIAS = 1),
      F.jsonID(`replace`, V),
      (H = class e extends F {
        constructor(e, t, n, r, i, a, o = !1) {
          (super(),
            (this.from = e),
            (this.to = t),
            (this.gapFrom = n),
            (this.gapTo = r),
            (this.slice = i),
            (this.insert = a),
            (this.structure = o));
        }
        apply(e) {
          if (
            this.structure &&
            (lt(e, this.from, this.gapFrom) || lt(e, this.gapTo, this.to))
          )
            return I.fail(`Structure gap-replace would overwrite content`);
          let t = e.slice(this.gapFrom, this.gapTo);
          if (t.openStart || t.openEnd)
            return I.fail(`Gap is not a flat range`);
          let n = this.slice.insertAt(this.insert, t.content);
          return n
            ? I.fromReplace(e, this.from, this.to, n)
            : I.fail(`Content does not fit in gap`);
        }
        getMap() {
          return new N([
            this.from,
            this.gapFrom - this.from,
            this.insert,
            this.gapTo,
            this.to - this.gapTo,
            this.slice.size - this.insert,
          ]);
        }
        invert(t) {
          let n = this.gapTo - this.gapFrom;
          return new e(
            this.from,
            this.from + this.slice.size + n,
            this.from + this.insert,
            this.from + this.insert + n,
            t
              .slice(this.from, this.to)
              .removeBetween(this.gapFrom - this.from, this.gapTo - this.from),
            this.gapFrom - this.from,
            this.structure,
          );
        }
        map(t) {
          let n = t.mapResult(this.from, 1),
            r = t.mapResult(this.to, -1),
            i = this.from == this.gapFrom ? n.pos : t.map(this.gapFrom, -1),
            a = this.to == this.gapTo ? r.pos : t.map(this.gapTo, 1);
          return (n.deletedAcross && r.deletedAcross) || i < n.pos || a > r.pos
            ? null
            : new e(
                n.pos,
                r.pos,
                i,
                a,
                this.slice,
                this.insert,
                this.structure,
              );
        }
        toJSON() {
          let e = {
            stepType: `replaceAround`,
            from: this.from,
            to: this.to,
            gapFrom: this.gapFrom,
            gapTo: this.gapTo,
            insert: this.insert,
          };
          return (
            this.slice.size && (e.slice = this.slice.toJSON()),
            this.structure && (e.structure = !0),
            e
          );
        }
        static fromJSON(t, n) {
          if (
            typeof n.from != `number` ||
            typeof n.to != `number` ||
            typeof n.gapFrom != `number` ||
            typeof n.gapTo != `number` ||
            typeof n.insert != `number`
          )
            throw RangeError(`Invalid input for ReplaceAroundStep.fromJSON`);
          return new e(
            n.from,
            n.to,
            n.gapFrom,
            n.gapTo,
            w.fromJSON(t, n.slice),
            n.insert,
            !!n.structure,
          );
        }
      }),
      F.jsonID(`replaceAround`, H),
      ($t = class {
        constructor(e, t, n) {
          ((this.$from = e),
            (this.$to = t),
            (this.unplaced = n),
            (this.frontier = []),
            (this.placed = b.empty));
          for (let t = 0; t <= e.depth; t++) {
            let n = e.node(t);
            this.frontier.push({
              type: n.type,
              match: n.contentMatchAt(e.indexAfter(t)),
            });
          }
          for (let t = e.depth; t > 0; t--)
            this.placed = b.from(e.node(t).copy(this.placed));
        }
        get depth() {
          return this.frontier.length - 1;
        }
        fit() {
          for (; this.unplaced.size;) {
            let e = this.findFittable();
            e ? this.placeNodes(e) : this.openMore() || this.dropNode();
          }
          let e = this.mustMoveInline(),
            t = this.placed.size - this.depth - this.$from.depth,
            n = this.$from,
            r = this.close(e < 0 ? this.$to : n.doc.resolve(e));
          if (!r) return null;
          let i = this.placed,
            a = n.depth,
            o = r.depth;
          for (; a && o && i.childCount == 1;)
            ((i = i.firstChild.content), a--, o--);
          let s = new w(i, a, o);
          return e > -1
            ? new H(n.pos, e, this.$to.pos, this.$to.end(), s, t)
            : s.size || n.pos != this.$to.pos
              ? new V(n.pos, r.pos, s)
              : null;
        }
        findFittable() {
          let e = this.unplaced.openStart;
          for (
            let t = this.unplaced.content, n = 0, r = this.unplaced.openEnd;
            n < e;
            n++
          ) {
            let i = t.firstChild;
            if (
              (t.childCount > 1 && (r = 0), i.type.spec.isolating && r <= n)
            ) {
              e = n;
              break;
            }
            t = i.content;
          }
          for (let t = 1; t <= 2; t++)
            for (let n = t == 1 ? e : this.unplaced.openStart; n >= 0; n--) {
              let e,
                r = null;
              n
                ? ((r = It(this.unplaced.content, n - 1).firstChild),
                  (e = r.content))
                : (e = this.unplaced.content);
              let i = e.firstChild;
              for (let e = this.depth; e >= 0; e--) {
                let { type: a, match: o } = this.frontier[e],
                  s,
                  c = null;
                if (
                  t == 1 &&
                  (i
                    ? o.matchType(i.type) || (c = o.fillBefore(b.from(i), !1))
                    : r && a.compatibleContent(r.type))
                )
                  return {
                    sliceDepth: n,
                    frontierDepth: e,
                    parent: r,
                    inject: c,
                  };
                if (t == 2 && i && (s = o.findWrapping(i.type)))
                  return {
                    sliceDepth: n,
                    frontierDepth: e,
                    parent: r,
                    wrap: s,
                  };
                if (r && o.matchType(r.type)) break;
              }
            }
        }
        openMore() {
          let { content: e, openStart: t, openEnd: n } = this.unplaced,
            r = It(e, t);
          return !r.childCount || r.firstChild.isLeaf
            ? !1
            : ((this.unplaced = new w(
                e,
                t + 1,
                Math.max(n, r.size + t >= e.size - n ? t + 1 : 0),
              )),
              !0);
        }
        dropNode() {
          let { content: e, openStart: t, openEnd: n } = this.unplaced,
            r = It(e, t);
          if (r.childCount <= 1 && t > 0) {
            let i = e.size - t <= t + r.size;
            this.unplaced = new w(A(e, t - 1, 1), t - 1, i ? t - 1 : n);
          } else this.unplaced = new w(A(e, t, 1), t, n);
        }
        placeNodes({
          sliceDepth: e,
          frontierDepth: t,
          parent: n,
          inject: r,
          wrap: i,
        }) {
          for (; this.depth > t;) this.closeFrontierNode();
          if (i) for (let e = 0; e < i.length; e++) this.openFrontierNode(i[e]);
          let a = this.unplaced,
            o = n ? n.content : a.content,
            s = a.openStart - e,
            c = 0,
            l = [],
            { match: u, type: d } = this.frontier[t];
          if (r) {
            for (let e = 0; e < r.childCount; e++) l.push(r.child(e));
            u = u.matchFragment(r);
          }
          let f = o.size + e - (a.content.size - a.openEnd);
          for (; c < o.childCount;) {
            let e = o.child(c),
              t = u.matchType(e.type);
            if (!t) break;
            (c++,
              (c > 1 || s == 0 || e.content.size) &&
                ((u = t),
                l.push(
                  Lt(
                    e.mark(d.allowedMarks(e.marks)),
                    c == 1 ? s : 0,
                    c == o.childCount ? f : -1,
                  ),
                )));
          }
          let p = c == o.childCount;
          (p || (f = -1),
            (this.placed = j(this.placed, t, b.from(l))),
            (this.frontier[t].match = u),
            p &&
              f < 0 &&
              n &&
              n.type == this.frontier[this.depth].type &&
              this.frontier.length > 1 &&
              this.closeFrontierNode());
          for (let e = 0, t = o; e < f; e++) {
            let e = t.lastChild;
            (this.frontier.push({
              type: e.type,
              match: e.contentMatchAt(e.childCount),
            }),
              (t = e.content));
          }
          this.unplaced = p
            ? e == 0
              ? w.empty
              : new w(A(a.content, e - 1, 1), e - 1, f < 0 ? a.openEnd : e - 1)
            : new w(A(a.content, e, c), a.openStart, a.openEnd);
        }
        mustMoveInline() {
          if (!this.$to.parent.isTextblock) return -1;
          let e = this.frontier[this.depth],
            t;
          if (
            !e.type.isTextblock ||
            !Rt(this.$to, this.$to.depth, e.type, e.match, !1) ||
            (this.$to.depth == this.depth &&
              (t = this.findCloseLevel(this.$to)) &&
              t.depth == this.depth)
          )
            return -1;
          let { depth: n } = this.$to,
            r = this.$to.after(n);
          for (; n > 1 && r == this.$to.end(--n);) ++r;
          return r;
        }
        findCloseLevel(e) {
          scan: for (let t = Math.min(this.depth, e.depth); t >= 0; t--) {
            let { match: n, type: r } = this.frontier[t],
              i = t < e.depth && e.end(t + 1) == e.pos + (e.depth - (t + 1)),
              a = Rt(e, t, r, n, i);
            if (a) {
              for (let n = t - 1; n >= 0; n--) {
                let { match: t, type: r } = this.frontier[n],
                  i = Rt(e, n, r, t, !0);
                if (!i || i.childCount) continue scan;
              }
              return {
                depth: t,
                fit: a,
                move: i ? e.doc.resolve(e.after(t + 1)) : e,
              };
            }
          }
        }
        close(e) {
          let t = this.findCloseLevel(e);
          if (!t) return null;
          for (; this.depth > t.depth;) this.closeFrontierNode();
          (t.fit.childCount && (this.placed = j(this.placed, t.depth, t.fit)),
            (e = t.move));
          for (let n = t.depth + 1; n <= e.depth; n++) {
            let t = e.node(n),
              r = t.type.contentMatch.fillBefore(t.content, !0, e.index(n));
            this.openFrontierNode(t.type, t.attrs, r);
          }
          return e;
        }
        openFrontierNode(e, t = null, n) {
          let r = this.frontier[this.depth];
          ((r.match = r.match.matchType(e)),
            (this.placed = j(this.placed, this.depth, b.from(e.create(t, n)))),
            this.frontier.push({ type: e, match: e.contentMatch }));
        }
        closeFrontierNode() {
          let e = this.frontier.pop().match.fillBefore(b.empty, !0);
          e.childCount &&
            (this.placed = j(this.placed, this.frontier.length, e));
        }
      }),
      (en = class e extends F {
        constructor(e, t, n) {
          (super(), (this.pos = e), (this.attr = t), (this.value = n));
        }
        apply(e) {
          let t = e.nodeAt(this.pos);
          if (!t) return I.fail(`No node at attribute step's position`);
          let n = Object.create(null);
          for (let e in t.attrs) n[e] = t.attrs[e];
          n[this.attr] = this.value;
          let r = t.type.create(n, null, t.marks);
          return I.fromReplace(
            e,
            this.pos,
            this.pos + 1,
            new w(b.from(r), 0, +!t.isLeaf),
          );
        }
        getMap() {
          return N.empty;
        }
        invert(t) {
          return new e(
            this.pos,
            this.attr,
            t.nodeAt(this.pos).attrs[this.attr],
          );
        }
        map(t) {
          let n = t.mapResult(this.pos, 1);
          return n.deletedAfter ? null : new e(n.pos, this.attr, this.value);
        }
        toJSON() {
          return {
            stepType: `attr`,
            pos: this.pos,
            attr: this.attr,
            value: this.value,
          };
        }
        static fromJSON(t, n) {
          if (typeof n.pos != `number` || typeof n.attr != `string`)
            throw RangeError(`Invalid input for AttrStep.fromJSON`);
          return new e(n.pos, n.attr, n.value);
        }
      }),
      F.jsonID(`attr`, en),
      (tn = class e extends F {
        constructor(e, t) {
          (super(), (this.attr = e), (this.value = t));
        }
        apply(e) {
          let t = Object.create(null);
          for (let n in e.attrs) t[n] = e.attrs[n];
          t[this.attr] = this.value;
          let n = e.type.create(t, e.content, e.marks);
          return I.ok(n);
        }
        getMap() {
          return N.empty;
        }
        invert(t) {
          return new e(this.attr, t.attrs[this.attr]);
        }
        map(e) {
          return this;
        }
        toJSON() {
          return { stepType: `docAttr`, attr: this.attr, value: this.value };
        }
        static fromJSON(t, n) {
          if (typeof n.attr != `string`)
            throw RangeError(`Invalid input for DocAttrStep.fromJSON`);
          return new e(n.attr, n.value);
        }
      }),
      F.jsonID(`docAttr`, tn),
      (U = class extends Error {}),
      (U = function e(t) {
        let n = Error.call(this, t);
        return ((n.__proto__ = e.prototype), n);
      }),
      (U.prototype = Object.create(Error.prototype)),
      (U.prototype.constructor = U),
      (U.prototype.name = `TransformError`),
      (nn = class {
        constructor(e) {
          ((this.doc = e),
            (this.steps = []),
            (this.docs = []),
            (this.mapping = new Qt()));
        }
        get before() {
          return this.docs.length ? this.docs[0] : this.doc;
        }
        step(e) {
          let t = this.maybeStep(e);
          if (t.failed) throw new U(t.failed);
          return this;
        }
        maybeStep(e) {
          let t = e.apply(this.doc);
          return (t.failed || this.addStep(e, t.doc), t);
        }
        get docChanged() {
          return this.steps.length > 0;
        }
        changedRange() {
          let e = 1e9,
            t = -1e9;
          for (let n = 0; n < this.mapping.maps.length; n++) {
            let r = this.mapping.maps[n];
            (n && ((e = r.map(e, 1)), (t = r.map(t, -1))),
              r.forEach((n, r, i, a) => {
                ((e = Math.min(e, i)), (t = Math.max(t, a)));
              }));
          }
          return e == 1e9 ? null : { from: e, to: t };
        }
        addStep(e, t) {
          (this.docs.push(this.doc),
            this.steps.push(e),
            this.mapping.appendMap(e.getMap()),
            (this.doc = t));
        }
        replace(e, t = e, n = w.empty) {
          let r = Pt(this.doc, e, t, n);
          return (r && this.step(r), this);
        }
        replaceWith(e, t, n) {
          return this.replace(e, t, new w(b.from(n), 0, 0));
        }
        delete(e, t) {
          return this.replace(e, t, w.empty);
        }
        insert(e, t) {
          return this.replaceWith(e, e, t);
        }
        replaceRange(e, t, n) {
          return (Vt(this, e, t, n), this);
        }
        replaceRangeWith(e, t, n) {
          return (Ut(this, e, t, n), this);
        }
        deleteRange(e, t) {
          return (Wt(this, e, t), this);
        }
        lift(e, t) {
          return (ht(this, e, t), this);
        }
        join(e, t = 1) {
          return (jt(this, e, t), this);
        }
        wrap(e, t) {
          return (bt(this, e, t), this);
        }
        setBlockType(e, t = e, n, r = null) {
          return (xt(this, e, t, n, r), this);
        }
        setNodeMarkup(e, t, n = null, r) {
          return (Tt(this, e, t, n, r), this);
        }
        setNodeAttribute(e, t, n) {
          return (this.step(new en(e, t, n)), this);
        }
        setDocAttribute(e, t) {
          return (this.step(new tn(e, t)), this);
        }
        addNodeMark(e, t) {
          return (this.step(new z(e, t)), this);
        }
        removeNodeMark(e, t) {
          let n = this.doc.nodeAt(e);
          if (!n) throw RangeError(`No node at position ` + e);
          if (t instanceof S) t.isInSet(n.marks) && this.step(new B(e, t));
          else {
            let r = n.marks,
              i,
              a = [];
            for (; (i = t.isInSet(r));)
              (a.push(new B(e, i)), (r = i.removeFromSet(r)));
            for (let e = a.length - 1; e >= 0; e--) this.step(a[e]);
          }
          return this;
        }
        split(e, t = 1, n) {
          return (Dt(this, e, t, n), this);
        }
        addMark(e, t, n) {
          return (ut(this, e, t, n), this);
        }
        removeMark(e, t, n) {
          return (dt(this, e, t, n), this);
        }
        clearIncompatible(e, t, n) {
          return (ft(this, e, t, n), this);
        }
      }));
  });
function an(e) {
  !dn &&
    !e.parent.inlineContent &&
    ((dn = !0),
    console.warn(
      `TextSelection endpoint not pointing into a node with inline content (` +
        e.parent.type.name +
        `)`,
    ));
}
function W(e, t, n, r, i, a = !1) {
  if (t.inlineContent) return q.create(e, n);
  for (let o = r - (i > 0 ? 0 : 1); i > 0 ? o < t.childCount : o >= 0; o += i) {
    let r = t.child(o);
    if (!r.isAtom) {
      let t = W(e, r, n + i, i < 0 ? r.childCount : 0, i, a);
      if (t) return t;
    } else if (!a && J.isSelectable(r))
      return J.create(e, n - (i < 0 ? r.nodeSize : 0));
    n += r.nodeSize * i;
  }
  return null;
}
function on(e, t, n) {
  let r = e.steps.length - 1;
  if (r < t) return;
  let i = e.steps[r];
  if (!(i instanceof V || i instanceof H)) return;
  let a = e.mapping.maps[r],
    o;
  (a.forEach((e, t, n, r) => {
    o ??= r;
  }),
    e.setSelection(K.near(e.doc.resolve(o), n)));
}
function sn(e, t) {
  return !t || !e ? e : e.bind(t);
}
function cn(e, t, n) {
  for (let r in e) {
    let i = e[r];
    (i instanceof Function
      ? (i = i.bind(t))
      : r == `handleDOMEvents` && (i = cn(i, t, {})),
      (n[r] = i));
  }
  return n;
}
function ln(e) {
  return e in Q ? e + `$` + ++Q[e] : ((Q[e] = 0), e + `$`);
}
var G,
  K,
  un,
  dn,
  q,
  fn,
  J,
  pn,
  Y,
  mn,
  hn,
  gn,
  _n,
  vn,
  X,
  yn,
  Z,
  bn,
  xn,
  Q,
  Sn,
  Cn = e(() => {
    (it(),
      rn(),
      (G = Object.create(null)),
      (K = class {
        constructor(e, t, n) {
          ((this.$anchor = e),
            (this.$head = t),
            (this.ranges = n || [new un(e.min(t), e.max(t))]));
        }
        get anchor() {
          return this.$anchor.pos;
        }
        get head() {
          return this.$head.pos;
        }
        get from() {
          return this.$from.pos;
        }
        get to() {
          return this.$to.pos;
        }
        get $from() {
          return this.ranges[0].$from;
        }
        get $to() {
          return this.ranges[0].$to;
        }
        get empty() {
          let e = this.ranges;
          for (let t = 0; t < e.length; t++)
            if (e[t].$from.pos != e[t].$to.pos) return !1;
          return !0;
        }
        content() {
          return this.$from.doc.slice(this.from, this.to, !0);
        }
        replace(e, t = w.empty) {
          let n = t.content.lastChild,
            r = null;
          for (let e = 0; e < t.openEnd; e++) ((r = n), (n = n.lastChild));
          let i = e.steps.length,
            a = this.ranges;
          for (let o = 0; o < a.length; o++) {
            let { $from: s, $to: c } = a[o],
              l = e.mapping.slice(i);
            (e.replaceRange(l.map(s.pos), l.map(c.pos), o ? w.empty : t),
              o == 0 &&
                on(e, i, (n ? n.isInline : r && r.isTextblock) ? -1 : 1));
          }
        }
        replaceWith(e, t) {
          let n = e.steps.length,
            r = this.ranges;
          for (let i = 0; i < r.length; i++) {
            let { $from: a, $to: o } = r[i],
              s = e.mapping.slice(n),
              c = s.map(a.pos),
              l = s.map(o.pos);
            i
              ? e.deleteRange(c, l)
              : (e.replaceRangeWith(c, l, t), on(e, n, t.isInline ? -1 : 1));
          }
        }
        static findFrom(e, t, n = !1) {
          let r = e.parent.inlineContent
            ? new q(e)
            : W(e.node(0), e.parent, e.pos, e.index(), t, n);
          if (r) return r;
          for (let r = e.depth - 1; r >= 0; r--) {
            let i =
              t < 0
                ? W(e.node(0), e.node(r), e.before(r + 1), e.index(r), t, n)
                : W(e.node(0), e.node(r), e.after(r + 1), e.index(r) + 1, t, n);
            if (i) return i;
          }
          return null;
        }
        static near(e, t = 1) {
          return (
            this.findFrom(e, t) || this.findFrom(e, -t) || new Y(e.node(0))
          );
        }
        static atStart(e) {
          return W(e, e, 0, 0, 1) || new Y(e);
        }
        static atEnd(e) {
          return W(e, e, e.content.size, e.childCount, -1) || new Y(e);
        }
        static fromJSON(e, t) {
          if (!t || !t.type)
            throw RangeError(`Invalid input for Selection.fromJSON`);
          let n = G[t.type];
          if (!n) throw RangeError(`No selection type ${t.type} defined`);
          return n.fromJSON(e, t);
        }
        static jsonID(e, t) {
          if (e in G)
            throw RangeError(`Duplicate use of selection JSON ID ` + e);
          return ((G[e] = t), (t.prototype.jsonID = e), t);
        }
        getBookmark() {
          return q.between(this.$anchor, this.$head).getBookmark();
        }
      }),
      (K.prototype.visible = !0),
      (un = class {
        constructor(e, t) {
          ((this.$from = e), (this.$to = t));
        }
      }),
      (dn = !1),
      (q = class e extends K {
        constructor(e, t = e) {
          (an(e), an(t), super(e, t));
        }
        get $cursor() {
          return this.$anchor.pos == this.$head.pos ? this.$head : null;
        }
        map(t, n) {
          let r = t.resolve(n.map(this.head));
          if (!r.parent.inlineContent) return K.near(r);
          let i = t.resolve(n.map(this.anchor));
          return new e(i.parent.inlineContent ? i : r, r);
        }
        replace(e, t = w.empty) {
          if ((super.replace(e, t), t == w.empty)) {
            let t = this.$from.marksAcross(this.$to);
            t && e.ensureMarks(t);
          }
        }
        eq(t) {
          return (
            t instanceof e && t.anchor == this.anchor && t.head == this.head
          );
        }
        getBookmark() {
          return new fn(this.anchor, this.head);
        }
        toJSON() {
          return { type: `text`, anchor: this.anchor, head: this.head };
        }
        static fromJSON(t, n) {
          if (typeof n.anchor != `number` || typeof n.head != `number`)
            throw RangeError(`Invalid input for TextSelection.fromJSON`);
          return new e(t.resolve(n.anchor), t.resolve(n.head));
        }
        static create(e, t, n = t) {
          let r = e.resolve(t);
          return new this(r, n == t ? r : e.resolve(n));
        }
        static between(t, n, r) {
          let i = t.pos - n.pos;
          if (((!r || i) && (r = i >= 0 ? 1 : -1), !n.parent.inlineContent)) {
            let e = K.findFrom(n, r, !0) || K.findFrom(n, -r, !0);
            if (e) n = e.$head;
            else return K.near(n, r);
          }
          return (
            t.parent.inlineContent ||
              (i == 0
                ? (t = n)
                : ((t = (K.findFrom(t, -r, !0) || K.findFrom(t, r, !0))
                    .$anchor),
                  t.pos < n.pos != i < 0 && (t = n))),
            new e(t, n)
          );
        }
      }),
      K.jsonID(`text`, q),
      (fn = class e {
        constructor(e, t) {
          ((this.anchor = e), (this.head = t));
        }
        map(t) {
          return new e(t.map(this.anchor), t.map(this.head));
        }
        resolve(e) {
          return q.between(e.resolve(this.anchor), e.resolve(this.head));
        }
      }),
      (J = class e extends K {
        constructor(e) {
          let t = e.nodeAfter,
            n = e.node(0).resolve(e.pos + t.nodeSize);
          (super(e, n), (this.node = t));
        }
        map(t, n) {
          let { deleted: r, pos: i } = n.mapResult(this.anchor),
            a = t.resolve(i);
          return r ? K.near(a) : new e(a);
        }
        content() {
          return new w(b.from(this.node), 0, 0);
        }
        eq(t) {
          return t instanceof e && t.anchor == this.anchor;
        }
        toJSON() {
          return { type: `node`, anchor: this.anchor };
        }
        getBookmark() {
          return new pn(this.anchor);
        }
        static fromJSON(t, n) {
          if (typeof n.anchor != `number`)
            throw RangeError(`Invalid input for NodeSelection.fromJSON`);
          return new e(t.resolve(n.anchor));
        }
        static create(t, n) {
          return new e(t.resolve(n));
        }
        static isSelectable(e) {
          return !e.isText && e.type.spec.selectable !== !1;
        }
      }),
      (J.prototype.visible = !1),
      K.jsonID(`node`, J),
      (pn = class e {
        constructor(e) {
          this.anchor = e;
        }
        map(t) {
          let { deleted: n, pos: r } = t.mapResult(this.anchor);
          return n ? new fn(r, r) : new e(r);
        }
        resolve(e) {
          let t = e.resolve(this.anchor),
            n = t.nodeAfter;
          return n && J.isSelectable(n) ? new J(t) : K.near(t);
        }
      }),
      (Y = class e extends K {
        constructor(e) {
          super(e.resolve(0), e.resolve(e.content.size));
        }
        replace(e, t = w.empty) {
          if (t == w.empty) {
            e.delete(0, e.doc.content.size);
            let t = K.atStart(e.doc);
            t.eq(e.selection) || e.setSelection(t);
          } else super.replace(e, t);
        }
        toJSON() {
          return { type: `all` };
        }
        static fromJSON(t) {
          return new e(t);
        }
        map(t) {
          return new e(t);
        }
        eq(t) {
          return t instanceof e;
        }
        getBookmark() {
          return mn;
        }
      }),
      K.jsonID(`all`, Y),
      (mn = {
        map() {
          return this;
        },
        resolve(e) {
          return new Y(e);
        },
      }),
      (hn = 1),
      (gn = 2),
      (_n = 4),
      (vn = class extends nn {
        constructor(e) {
          (super(e.doc),
            (this.curSelectionFor = 0),
            (this.updated = 0),
            (this.meta = Object.create(null)),
            (this.time = Date.now()),
            (this.curSelection = e.selection),
            (this.storedMarks = e.storedMarks));
        }
        get selection() {
          return (
            this.curSelectionFor < this.steps.length &&
              ((this.curSelection = this.curSelection.map(
                this.doc,
                this.mapping.slice(this.curSelectionFor),
              )),
              (this.curSelectionFor = this.steps.length)),
            this.curSelection
          );
        }
        setSelection(e) {
          if (e.$from.doc != this.doc)
            throw RangeError(
              `Selection passed to setSelection must point at the current document`,
            );
          return (
            (this.curSelection = e),
            (this.curSelectionFor = this.steps.length),
            (this.updated = (this.updated | hn) & -3),
            (this.storedMarks = null),
            this
          );
        }
        get selectionSet() {
          return (this.updated & hn) > 0;
        }
        setStoredMarks(e) {
          return ((this.storedMarks = e), (this.updated |= gn), this);
        }
        ensureMarks(e) {
          return (
            S.sameSet(this.storedMarks || this.selection.$from.marks(), e) ||
              this.setStoredMarks(e),
            this
          );
        }
        addStoredMark(e) {
          return this.ensureMarks(
            e.addToSet(this.storedMarks || this.selection.$head.marks()),
          );
        }
        removeStoredMark(e) {
          return this.ensureMarks(
            e.removeFromSet(this.storedMarks || this.selection.$head.marks()),
          );
        }
        get storedMarksSet() {
          return (this.updated & gn) > 0;
        }
        addStep(e, t) {
          (super.addStep(e, t),
            (this.updated &= -3),
            (this.storedMarks = null));
        }
        setTime(e) {
          return ((this.time = e), this);
        }
        replaceSelection(e) {
          return (this.selection.replace(this, e), this);
        }
        replaceSelectionWith(e, t = !0) {
          let n = this.selection;
          return (
            t &&
              (e = e.mark(
                this.storedMarks ||
                  (n.empty
                    ? n.$from.marks()
                    : n.$from.marksAcross(n.$to) || S.none),
              )),
            n.replaceWith(this, e),
            this
          );
        }
        deleteSelection() {
          return (this.selection.replace(this), this);
        }
        insertText(e, t, n) {
          let r = this.doc.type.schema;
          if (t == null)
            return e
              ? this.replaceSelectionWith(r.text(e), !0)
              : this.deleteSelection();
          {
            if (((n ??= t), !e)) return this.deleteRange(t, n);
            let i = this.storedMarks;
            if (!i) {
              let e = this.doc.resolve(t);
              i = n == t ? e.marks() : e.marksAcross(this.doc.resolve(n));
            }
            return (
              this.replaceRangeWith(t, n, r.text(e, i)),
              !this.selection.empty &&
                this.selection.to == t + e.length &&
                this.setSelection(K.near(this.selection.$to)),
              this
            );
          }
        }
        setMeta(e, t) {
          return ((this.meta[typeof e == `string` ? e : e.key] = t), this);
        }
        getMeta(e) {
          return this.meta[typeof e == `string` ? e : e.key];
        }
        get isGeneric() {
          for (let e in this.meta) return !1;
          return !0;
        }
        scrollIntoView() {
          return ((this.updated |= _n), this);
        }
        get scrolledIntoView() {
          return (this.updated & _n) > 0;
        }
      }),
      (X = class {
        constructor(e, t, n) {
          ((this.name = e),
            (this.init = sn(t.init, n)),
            (this.apply = sn(t.apply, n)));
        }
      }),
      (yn = [
        new X(`doc`, {
          init(e) {
            return e.doc || e.schema.topNodeType.createAndFill();
          },
          apply(e) {
            return e.doc;
          },
        }),
        new X(`selection`, {
          init(e, t) {
            return e.selection || K.atStart(t.doc);
          },
          apply(e) {
            return e.selection;
          },
        }),
        new X(`storedMarks`, {
          init(e) {
            return e.storedMarks || null;
          },
          apply(e, t, n, r) {
            return r.selection.$cursor ? e.storedMarks : null;
          },
        }),
        new X(`scrollToSelection`, {
          init() {
            return 0;
          },
          apply(e, t) {
            return e.scrolledIntoView ? t + 1 : t;
          },
        }),
      ]),
      (Z = class {
        constructor(e, t) {
          ((this.schema = e),
            (this.plugins = []),
            (this.pluginsByKey = Object.create(null)),
            (this.fields = yn.slice()),
            t &&
              t.forEach((e) => {
                if (this.pluginsByKey[e.key])
                  throw RangeError(
                    `Adding different instances of a keyed plugin (` +
                      e.key +
                      `)`,
                  );
                (this.plugins.push(e),
                  (this.pluginsByKey[e.key] = e),
                  e.spec.state &&
                    this.fields.push(new X(e.key, e.spec.state, e)));
              }));
        }
      }),
      (bn = class e {
        constructor(e) {
          this.config = e;
        }
        get schema() {
          return this.config.schema;
        }
        get plugins() {
          return this.config.plugins;
        }
        apply(e) {
          return this.applyTransaction(e).state;
        }
        filterTransaction(e, t = -1) {
          for (let n = 0; n < this.config.plugins.length; n++)
            if (n != t) {
              let t = this.config.plugins[n];
              if (
                t.spec.filterTransaction &&
                !t.spec.filterTransaction.call(t, e, this)
              )
                return !1;
            }
          return !0;
        }
        applyTransaction(e) {
          if (!this.filterTransaction(e))
            return { state: this, transactions: [] };
          let t = [e],
            n = this.applyInner(e),
            r = null;
          for (;;) {
            let i = !1;
            for (let a = 0; a < this.config.plugins.length; a++) {
              let o = this.config.plugins[a];
              if (o.spec.appendTransaction) {
                let s = r ? r[a].n : 0,
                  c = r ? r[a].state : this,
                  l =
                    s < t.length &&
                    o.spec.appendTransaction.call(o, s ? t.slice(s) : t, c, n);
                if (l && n.filterTransaction(l, a)) {
                  if ((l.setMeta(`appendedTransaction`, e), !r)) {
                    r = [];
                    for (let e = 0; e < this.config.plugins.length; e++)
                      r.push(
                        e < a
                          ? { state: n, n: t.length }
                          : { state: this, n: 0 },
                      );
                  }
                  (t.push(l), (n = n.applyInner(l)), (i = !0));
                }
                r && (r[a] = { state: n, n: t.length });
              }
            }
            if (!i) return { state: n, transactions: t };
          }
        }
        applyInner(t) {
          if (!t.before.eq(this.doc))
            throw RangeError(`Applying a mismatched transaction`);
          let n = new e(this.config),
            r = this.config.fields;
          for (let e = 0; e < r.length; e++) {
            let i = r[e];
            n[i.name] = i.apply(t, this[i.name], this, n);
          }
          return n;
        }
        get tr() {
          return new vn(this);
        }
        static create(t) {
          let n = new Z(t.doc ? t.doc.type.schema : t.schema, t.plugins),
            r = new e(n);
          for (let e = 0; e < n.fields.length; e++)
            r[n.fields[e].name] = n.fields[e].init(t, r);
          return r;
        }
        reconfigure(t) {
          let n = new Z(this.schema, t.plugins),
            r = n.fields,
            i = new e(n);
          for (let e = 0; e < r.length; e++) {
            let n = r[e].name;
            i[n] = this.hasOwnProperty(n) ? this[n] : r[e].init(t, i);
          }
          return i;
        }
        toJSON(e) {
          let t = {
            doc: this.doc.toJSON(),
            selection: this.selection.toJSON(),
          };
          if (
            (this.storedMarks &&
              (t.storedMarks = this.storedMarks.map((e) => e.toJSON())),
            e && typeof e == `object`)
          )
            for (let n in e) {
              if (n == `doc` || n == `selection`)
                throw RangeError(
                  "The JSON fields `doc` and `selection` are reserved",
                );
              let r = e[n],
                i = r.spec.state;
              i && i.toJSON && (t[n] = i.toJSON.call(r, this[r.key]));
            }
          return t;
        }
        static fromJSON(t, n, r) {
          if (!n) throw RangeError(`Invalid input for EditorState.fromJSON`);
          if (!t.schema)
            throw RangeError(`Required config field 'schema' missing`);
          let i = new Z(t.schema, t.plugins),
            a = new e(i);
          return (
            i.fields.forEach((e) => {
              if (e.name == `doc`) a.doc = T.fromJSON(t.schema, n.doc);
              else if (e.name == `selection`)
                a.selection = K.fromJSON(a.doc, n.selection);
              else if (e.name == `storedMarks`)
                n.storedMarks &&
                  (a.storedMarks = n.storedMarks.map(t.schema.markFromJSON));
              else {
                if (r)
                  for (let i in r) {
                    let o = r[i],
                      s = o.spec.state;
                    if (
                      o.key == e.name &&
                      s &&
                      s.fromJSON &&
                      Object.prototype.hasOwnProperty.call(n, i)
                    ) {
                      a[e.name] = s.fromJSON.call(o, t, n[i], a);
                      return;
                    }
                  }
                a[e.name] = e.init(t, a);
              }
            }),
            a
          );
        }
      }),
      (xn = class {
        constructor(e) {
          ((this.spec = e),
            (this.props = {}),
            e.props && cn(e.props, this, this.props),
            (this.key = e.key ? e.key.key : ln(`plugin`)));
        }
        getState(e) {
          return e[this.key];
        }
      }),
      (Q = Object.create(null)),
      (Sn = class {
        constructor(e = `key`) {
          this.key = ln(e);
        }
        get(e) {
          return e.config.pluginsByKey[this.key];
        }
        getState(e) {
          return e[this.key];
        }
      }));
  }),
  $,
  wn,
  Tn = e(() => {
    (t(),
      ($ = n()),
      (wn = (e) =>
        (0, $.jsxs)(`svg`, {
          width: 24,
          height: 24,
          viewBox: `0 0 24 24`,
          fill: `none`,
          xmlns: `http://www.w3.org/2000/svg`,
          ...e,
          children: [
            (0, $.jsx)(`path`, {
              d: `M11.0514 2.79347C12.4431 1.40182 14.6998 1.40188 16.0915 2.79347L16.2175 2.92628C16.9501 3.73696 17.243 4.80308 17.0983 5.81886C18.1153 5.67274 19.183 5.96602 19.9948 6.69972L20.1276 6.82569L20.1696 6.86769L20.2956 7.0005C21.5596 8.3994 21.5178 10.5595 20.1696 11.9077L12.8766 19.2007C12.7842 19.2935 12.784 19.444 12.8766 19.5366L14.3737 21.0337C14.652 21.312 14.6519 21.7632 14.3737 22.0415C14.0953 22.3196 13.6441 22.3198 13.3659 22.0415L11.8688 20.5444C11.2194 19.8951 11.2196 18.8423 11.8688 18.1929L19.1608 10.8999C19.9437 10.1171 19.9934 8.87744 19.3083 8.03761L19.1608 7.8755L19.1188 7.83351C18.2837 6.99932 16.9302 6.99885 16.0954 7.83351L10.0895 13.8394C10.0831 13.8458 10.0746 13.8499 10.068 13.856L10.0016 13.9234C9.72335 14.2014 9.2721 14.2014 8.99383 13.9234C8.71573 13.6451 8.7158 13.1939 8.99383 12.9155L15.0837 6.82569C15.9187 5.99069 15.9187 4.63629 15.0837 3.80128C14.2487 2.96644 12.8943 2.96629 12.0593 3.80128L3.99578 11.8648C3.71743 12.143 3.26627 12.1431 2.98797 11.8648C2.70996 11.5864 2.70984 11.1352 2.98797 10.8569L11.0514 2.79347Z`,
              fill: `currentColor`,
            }),
            (0, $.jsx)(`path`, {
              d: `M13.0759 4.80909C13.3541 4.53092 13.8053 4.53098 14.0837 4.80909C14.362 5.0874 14.3619 5.53856 14.0837 5.81691L8.1198 11.7808C7.28504 12.6156 7.28529 13.9691 8.1198 14.8042C8.95483 15.6392 10.3092 15.6392 11.1442 14.8042L17.1081 8.84132C17.3864 8.56315 17.8376 8.56321 18.1159 8.84132C18.3938 9.11965 18.394 9.57091 18.1159 9.84913L12.152 15.813C10.7604 17.2045 8.50369 17.2044 7.11199 15.813C5.72054 14.4213 5.72042 12.1646 7.11199 10.773L13.0759 4.80909Z`,
              fill: `currentColor`,
            }),
          ],
        })));
  });
export {
  c as A,
  b as C,
  w as D,
  Ye as E,
  a as M,
  it as O,
  nt as S,
  He as T,
  gt as _,
  J as a,
  Pt as b,
  K as c,
  Cn as d,
  Qt as f,
  Nt as g,
  Et as h,
  bn as i,
  i as j,
  s as k,
  un as l,
  Ot as m,
  Tn as n,
  xn as o,
  H as p,
  Y as r,
  Sn as s,
  wn as t,
  q as u,
  rn as v,
  S as w,
  Xe as x,
  mt as y,
};
//# sourceMappingURL=app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~settings-command-~ea2v0nlw-CpgLJdNt.js.map
