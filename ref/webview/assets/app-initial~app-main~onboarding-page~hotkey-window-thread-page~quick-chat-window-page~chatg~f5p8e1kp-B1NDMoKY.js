import { n as e, s as t } from "./rolldown-runtime-BG2f4sTM.js";
import {
  E as n,
  St as r,
  _ as i,
  b as a,
  c as o,
  l as s,
  un as c,
  v as l,
  x as u,
  y as d,
  zt as f,
} from "./app-initial~avatarOverlayCompositionSurface~index-9fQ9wihu~index-BFCcxPM5~mapbox-gl-DVWlwqb~kppdhley-mFmI6cbL.js";
import {
  An as p,
  As as m,
  Cn as h,
  Fl as g,
  Ha as _,
  Hc as v,
  Jc as y,
  Kc as b,
  Pi as x,
  Rc as S,
  Ua as C,
  Wo as w,
  Xc as T,
  Yc as ee,
  Zo as te,
  _o as ne,
  b as re,
  cl as E,
  dl as ie,
  el as ae,
  il as D,
  jn as oe,
  ll as O,
  m as se,
  n as k,
  o as ce,
  oo as A,
  p as le,
  pi as ue,
  pl as de,
  rl as fe,
  sn as pe,
  tl as j,
  ui as me,
  vl as he,
  vt as ge,
  w as M,
  wn as _e,
  x as N,
  xl as ve,
} from "./app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~notebook-preview-~ngwudnyz-CS1L_Amm.js";
import {
  a as ye,
  d as P,
  g as be,
  o as xe,
} from "./app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~notebook-preview-~dejrazf4-CutsMNtQ.js";
import {
  b as Se,
  y as Ce,
} from "./app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~notebook-preview-~cc80cam6-Z-xASB4J.js";
import {
  C as we,
  D as Te,
  O as F,
  R as Ee,
  S as De,
  T as Oe,
  W as ke,
  ft as Ae,
  ht as je,
  lt as Me,
  mt as Ne,
  pt as Pe,
} from "./app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~notebook-preview-~l16cgjx9-Bw-xR9Fl.js";
import {
  Ct as Fe,
  Et as Ie,
  Ft as Le,
  Mt as Re,
  Nt as ze,
  Ot as Be,
  Pt as Ve,
  St as He,
  Tt as Ue,
  Xr as We,
  Yr as Ge,
  bt as Ke,
  cn as qe,
  d as Je,
  f as Ye,
  i as Xe,
  ir as Ze,
  n as Qe,
  nt as $e,
  or as et,
  p as tt,
  rt as nt,
  sn as rt,
  sr as it,
  wt as at,
  xt as ot,
} from "./app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~notebook-preview-~g8980z4e-Bj4tUPiN.js";
import {
  B as st,
  V as ct,
} from "./app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~notebook-preview-~iaq4jiqv-Cyrw3qv2.js";
import {
  m as lt,
  p as ut,
} from "./app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~notebook-preview-~nssizff1-C8BmUbuQ.js";
import {
  g as dt,
  h as ft,
} from "./app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~app-main~plugin-d~kw7nl1sl-CSfXbWX3.js";
import {
  Ad as I,
  An as pt,
  Bl as mt,
  Bu as ht,
  C as gt,
  Cc as _t,
  Ci as vt,
  Ct as yt,
  Cu as bt,
  Ei as xt,
  Fd as St,
  Hg as Ct,
  I as wt,
  Kg as Tt,
  Ld as Et,
  Li as Dt,
  Md as Ot,
  Ni as kt,
  Or as At,
  Q as jt,
  Qs as Mt,
  Sc as Nt,
  Si as Pt,
  Su as Ft,
  Ti as It,
  Tu as Lt,
  Us as Rt,
  Vi as zt,
  Vs as Bt,
  Vu as Vt,
  _r as Ht,
  ad as Ut,
  ai as Wt,
  gc as Gt,
  gr as Kt,
  jn as qt,
  kn as Jt,
  kr as Yt,
  ku as Xt,
  mc as Zt,
  nf as Qt,
  ni as $t,
  od as en,
  oi as L,
  rf as tn,
  ri as nn,
  tc as rn,
  wi as an,
  xi as on,
  zl as sn,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~business-checkout~oxnpxkxc-Caq74mjw.js";
import {
  I as cn,
  N as ln,
  V as un,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~business-checkout~mgfw3nls-C1ihWWPl.js";
import {
  S as dn,
  b as fn,
  u as pn,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~pull-request-rout~gocinqla-CZ30ZsFv.js";
import {
  $ as mn,
  Z as hn,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~settings-command-~hox8u96i-DU7nfKAJ.js";
import {
  b as gn,
  x as _n,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~settings-command-~ab38dkdb-BD-y9IYB.js";
import {
  n as vn,
  r as yn,
  s as bn,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~appgen-settings-p~dl1mfg21-CewNvCnh.js";
import {
  _ as xn,
  g as Sn,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~settings-command-~fpqaaiae-BoQBkWFV.js";
import {
  _ as Cn,
  v as wn,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~settings-command-~eurqfy6e-BYVE1srw.js";
import {
  h as Tn,
  t as En,
} from "./app-initial~artifact-tab-content.electron~app-main~pull-request-code-review~new-thread-pane~kxdmdety-Dw15wju7.js";
import {
  M as Dn,
  f as On,
  j as kn,
  u as An,
} from "./app-initial~artifact-tab-content.electron~app-main~new-thread-panel-page~onboarding-page~pr~gh55pyj3-C7xUNhuM.js";
import {
  b as jn,
  x as Mn,
} from "./app-initial~artifact-tab-content.electron~app-main~new-thread-panel-page~onboarding-page~se~ismvjv52-DOg-s8wB.js";
import {
  O as Nn,
  a as Pn,
  c as Fn,
  k as In,
  l as Ln,
  n as Rn,
  t as zn,
} from "./app-initial~app-main~onboarding-page~hotkey-window-thread-page~quick-chat-window-page~chatg~nejl6ozr-CW_aiF_H.js";
import {
  _r as Bn,
  br as Vn,
  gr as Hn,
  hr as Un,
  mr as Wn,
  vr as Gn,
  yr as Kn,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~pull-request-rout~jvsvjxtt-B-CZ9DhL.js";
import {
  A as qn,
  O as Jn,
  _ as Yn,
  j as Xn,
  k as Zn,
  v as Qn,
} from "./app-initial~artifact-tab-content.electron~app-main~appgen-settings-page~pull-request-route~~pdazrfmg-DzasxZnO.js";
import {
  d as $n,
  f as er,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~pull-request-rout~k0tdw7da-C1uINtmZ.js";
import {
  E as tr,
  F as nr,
  G as rr,
  I as ir,
  L as ar,
  R as or,
  S as sr,
  T as cr,
  U as lr,
  V as ur,
  W as dr,
  i as fr,
  r as pr,
  x as mr,
} from "./app-initial~app-main~new-thread-panel-page~onboarding-page~appgen-library-page~hotkey-windo~im5yc0bw-BlqwAlDd.js";
import {
  c as hr,
  s as gr,
} from "./app-initial~app-main~settings-command-menu-section-items~pull-request-route~new-thread-pane~fnoshreu-D5mfSDoa.js";
import {
  n as _r,
  r as vr,
} from "./app-initial~artifact-tab-content.electron~app-main~pull-request-code-review~new-thread-pane~b6mb9dk4-gMHmseWC.js";
import {
  d as yr,
  f as br,
  s as xr,
} from "./app-initial~app-main~pull-request-route~new-thread-panel-page~onboarding-page~projects-inde~ldmpswh9-BbClLuNw.js";
import {
  v as Sr,
  x as Cr,
} from "./app-initial~artifact-tab-content.electron~app-main~settings-command-menu-section-items~pull~olm3nzne-DR0PxsC_.js";
import {
  _ as wr,
  v as Tr,
} from "./app-initial~app-main~appgen-settings-page~pull-request-route~plugin-detail-page~onboarding-~j9yriubg-BNsnCk5_.js";
import {
  c as Er,
  s as Dr,
} from "./app-initial~app-main~onboarding-page~hotkey-window-thread-page~hotkey-window-home-page~quic~nn8qcn6y-BBOcTlwy.js";
import {
  c as Or,
  m as kr,
  n as Ar,
  p as jr,
  s as Mr,
  t as Nr,
  x as Pr,
  y as Fr,
} from "./app-initial~app-main~onboarding-page~projects-index-page~plan-summary-page~hotkey-window-th~e5sxdgia-CO27APJz.js";
import {
  F as Ir,
  I as Lr,
  L as Rr,
  P as zr,
} from "./app-initial~app-main~pull-request-route~onboarding-page~projects-index-page~hotkey-window-t~mgwh6spa-B8-rSDzI.js";
var Br,
  Vr,
  Hr,
  Ur,
  Wr = e(() => {
    (S(),
      ke(),
      (Br = E({
        type: j(`review`),
        view: v([`last-turn`, `branch`, `unstaged`, `staged`]).optional(),
        path: O().min(1).optional(),
      })),
      (Vr = E({
        type: j(`review`),
        baseBranch: O()
          .min(1)
          .describe(
            `Git revision to compare with HEAD. Must resolve locally to a commit. Selects branch view.`,
          ),
        view: j(`branch`).optional(),
        path: O().min(1).optional(),
      })),
      (Hr = E({
        threadId: O()
          .min(1)
          .optional()
          .describe(
            `Visible thread whose Codex panel should receive the tab. Defaults to the calling thread, which must be visible in the targeted main window.`,
          ),
        target: ie([
          E({
            type: j(`file`),
            path: O().min(1),
            line: fe().int().positive().optional(),
          }),
          E({
            type: j(`browser`),
            url: de().optional(),
            tabId: O().min(1).optional(),
          }),
          E({ type: j(`terminal`), sessionId: O().min(1).optional() }),
          Br,
          Vr,
        ]),
        placement: v([`right`, `bottom`]).optional(),
      })),
      (Ur = Hr.extend({ type: j(`windows.tabs.open`), windowId: Ee })));
  });
async function Gr({
  scope: e,
  cursor: t,
  hostId: n,
  includeOutputs: r,
  maxOutputCharsPerItem: i,
  threadId: a,
  turnLimit: o,
}) {
  let { hostId: s, manager: c } = await Bn({
      scope: e,
      threadId: a,
      preferredHostId: n,
    }),
    { thread: l } = await c.readThread(a, { includeTurns: !0 }),
    u = t == null ? l.turns.length : l.turns.findIndex((e) => e.id === t);
  if (u < 0) throw Error(`Unknown cursor for thread ${a}: ${t}`);
  let d = l.turns.slice(0, u),
    f = d.slice(-o).reverse();
  return {
    schemaVersion: 1,
    thread: {
      id: l.id,
      hostId: s,
      title: l.name,
      preview: l.preview,
      status: Kr(l.status),
      cwd: l.cwd,
      createdAt: l.createdAt,
      updatedAt: l.updatedAt,
    },
    page: {
      order: `newest_first`,
      limit: o,
      nextCursor: d.length > f.length ? (f.at(-1)?.id ?? null) : null,
      hasMore: d.length > f.length,
    },
    turns: f.map((e) => qr(e, r, i)),
  };
}
function Kr(e) {
  switch (e.type) {
    case `active`:
      return { type: `active`, activeFlags: e.activeFlags };
    case `idle`:
    case `notLoaded`:
    case `systemError`:
      return { type: e.type };
  }
}
function qr(e, t, n) {
  return {
    id: e.id,
    status: e.status,
    error:
      e.error == null
        ? null
        : {
            message: e.error.message,
            additionalDetails: e.error.additionalDetails,
          },
    startedAt: e.startedAt,
    completedAt: e.completedAt,
    durationMs: e.durationMs,
    items: e.items.map((e) => Jr(e, t, n)),
  };
}
function Jr(e, t, n) {
  switch (e.type) {
    case `userMessage`:
      return { type: e.type, id: e.id, content: e.content.map(Yr) };
    case `agentMessage`:
      return { type: e.type, id: e.id, text: e.text, phase: e.phase };
    case `plan`:
      return { type: e.type, id: e.id, text: e.text };
    case `reasoning`:
      return {
        type: e.type,
        id: e.id,
        summary: e.summary,
        ...(t ? { content: e.content.map((e) => Xr(e, n)) } : {}),
      };
    case `commandExecution`:
      return {
        type: e.type,
        id: e.id,
        command: e.command,
        cwd: e.cwd,
        status: e.status,
        exitCode: e.exitCode,
        durationMs: e.durationMs,
        ...(t && e.aggregatedOutput != null
          ? { output: Xr(e.aggregatedOutput, n) }
          : {}),
      };
    case `fileChange`:
      return {
        type: e.type,
        id: e.id,
        status: e.status,
        changes: e.changes.map((e) => ({
          path: e.path,
          kind: e.kind,
          ...(t ? { diff: Xr(e.diff, n) } : {}),
        })),
      };
    case `mcpToolCall`:
      return {
        type: e.type,
        id: e.id,
        server: e.server,
        tool: e.tool,
        arguments: e.arguments,
        status: e.status,
        durationMs: e.durationMs,
      };
    case `dynamicToolCall`:
      return {
        type: e.type,
        id: e.id,
        tool: e.tool,
        arguments: e.arguments,
        status: e.status,
        success: e.success,
        durationMs: e.durationMs,
      };
    case `collabAgentToolCall`:
      return {
        type: e.type,
        id: e.id,
        tool: e.tool,
        status: e.status,
        senderThreadId: e.senderThreadId,
        receiverThreadIds: e.receiverThreadIds,
        prompt: e.prompt,
        model: e.model,
        reasoningEffort: e.reasoningEffort,
      };
    case `subAgentActivity`:
      return {
        type: e.type,
        id: e.id,
        kind: e.kind,
        agentThreadId: e.agentThreadId,
        agentPath: e.agentPath,
      };
    case `webSearch`:
      return { type: e.type, id: e.id, query: e.query, action: e.action };
    case `imageView`:
      return { type: e.type, id: e.id, path: e.path };
    case `sleep`:
      return { type: e.type, id: e.id, durationMs: e.durationMs };
    case `imageGeneration`:
      return {
        type: e.type,
        id: e.id,
        status: e.status,
        revisedPrompt: e.revisedPrompt,
        result: e.result,
        savedPath: e.savedPath ?? null,
      };
    case `enteredReviewMode`:
    case `exitedReviewMode`:
      return { type: e.type, id: e.id, review: e.review };
    case `hookPrompt`:
      return { type: e.type, id: e.id, fragmentCount: e.fragments.length };
    case `contextCompaction`:
      return { type: e.type, id: e.id };
  }
}
function Yr(e) {
  switch (e.type) {
    case `text`: {
      let t = qe(e.text);
      return {
        type: e.type,
        text: e.text,
        codexDelegation:
          t == null
            ? void 0
            : { sourceThreadId: t.sourceThreadId, input: t.input },
      };
    }
    case `image`:
      return { type: e.type, url: e.url };
    case `localImage`:
      return { type: e.type, path: e.path };
    case `skill`:
    case `mention`:
      return { type: e.type, name: e.name, path: e.path };
  }
}
function Xr(e, t) {
  return e.length <= t
    ? { text: e, truncated: !1 }
    : { text: e.slice(0, t), truncated: !0, originalChars: e.length };
}
var Zr = e(() => {
  (rt(), Hn());
});
function Qr({
  sourceConversationId: e,
  targetConversationId: t,
  diffComments: n,
}) {
  if (e === t || n == null) return n;
  let r = n[e];
  if (r == null) return n;
  let i = { ...n };
  return ((i[t] = r), delete i[e], i);
}
var $r = e(() => {});
function ei(e) {
  return !oi.test(e) || !si.test(e)
    ? null
    : ci.test(e)
      ? `create`
      : li.test(e)
        ? `update`
        : ui.test(e)
          ? null
          : `create`;
}
function ti(e) {
  if (e == null) return !0;
  switch (e) {
    case `inProgress`:
    case `completed`:
      return !0;
    case `failed`:
    case `declined`:
    case `interrupted`:
      return !1;
  }
}
function ni(e) {
  return e?.skillId.toLowerCase() === `visualize` && e.isSkillDefinitionFile;
}
function ri(e, t) {
  let n = [e];
  for (let e of t)
    (e.type === `userMessage` && n.push(e.content),
      e.type === `steeringUserMessage` &&
        e.status === `accepted` &&
        n.push(e.input));
  let r = !1;
  for (let e of n)
    for (let t of e) {
      if (t.type !== `text`) continue;
      hi.test(t.text) && (r = !0);
      let e = 0;
      for (; e < t.text.length;) {
        let n = kt(t.text, e);
        if (n == null) break;
        let r = mi.exec(zt(n.path))?.[1]?.trim().toLowerCase();
        if (r === di || r === fi || r === pi) return `explicit`;
        e = n.end;
      }
    }
  return r ? `soft` : `none`;
}
function ii(e) {
  return [
    e.completedVisualizationCommandCount,
    e.runningVisualizationCommandCount,
  ];
}
function ai({ intl: e, kind: t, isFinished: n, isLeadingSegment: r }) {
  switch (t) {
    case `create`:
      return n
        ? r
          ? e.formatMessage(R.summaryCreatedLeading)
          : e.formatMessage(R.summaryCreated)
        : r
          ? e.formatMessage(R.summaryCreatingLeading)
          : e.formatMessage(R.summaryCreating);
    case `update`:
      return n
        ? r
          ? e.formatMessage(R.summaryUpdatedLeading)
          : e.formatMessage(R.summaryUpdated)
        : r
          ? e.formatMessage(R.summaryUpdatingLeading)
          : e.formatMessage(R.summaryUpdating);
  }
}
var oi,
  si,
  ci,
  li,
  ui,
  di,
  fi,
  pi,
  mi,
  hi,
  R,
  gi = e(() => {
    (M(),
      xe(),
      Dt(),
      (oi = /(?:^|[\\/])visualizations(?:[\\/"'\s;]|$)/i),
      (si =
        /(?:^|\s)(?:apply_patch|mkdir|tee|touch|cp|mv|install)(?:\s|$)|(?:^|[^<])>>?/),
      (ci = /\*\*\* Add File:[^\r\n]*[\\/]visualizations(?:[\\/"'\s;]|$)/i),
      (li = /\*\*\* Update File:[^\r\n]*[\\/]visualizations(?:[\\/"'\s;]|$)/i),
      (ui = /\*\*\* Delete File:[^\r\n]*[\\/]visualizations(?:[\\/"'\s;]|$)/i),
      (di = `${_}@${C}`),
      (fi = `${_}@${h}`),
      (pi = `${_}@${_e}`),
      (mi = /^plugin:\/\/([^?]+)(?:\?|$)/i),
      (hi =
        /\b(?:visuals?|visuali[sz](?:e|ed|es|er|ers|ing|ations?)|(?:flow)?charts?|plot(?:s|ted|ting)?|graphs?|diagrams?|maps?|simulat(?:e|ed|es|ing|ions?|ors?)|labs?|compar(?:e|ed|es|ing|isons?)|scenarios?|explor(?:e|ed|es|ing|ations?)|interactive|adjustable[-\s]+inputs?)\b|\bshow\s+(?:(?:me|us)\s+)?how\b/i),
      (R = ye({
        activeCreating: {
          id: `codex.visualization.activity.active.creating`,
          defaultMessage: `<action>Creating</action> <detail>visualization</detail>`,
          description: `Active tool activity summary while Codex creates an inline visualization`,
        },
        activeUpdating: {
          id: `codex.visualization.activity.active.updating`,
          defaultMessage: `<action>Updating</action> <detail>visualization</detail>`,
          description: `Active tool activity summary while Codex updates an inline visualization`,
        },
        rowCreating: {
          id: `codex.visualization.activity.row.creating`,
          defaultMessage: `<verb>Creating</verb> visualization`,
          description: `Tool activity row while Codex creates an inline visualization`,
        },
        rowCreated: {
          id: `codex.visualization.activity.row.created`,
          defaultMessage: `<verb>Created</verb> visualization`,
          description: `Tool activity row after Codex creates an inline visualization`,
        },
        rowUpdating: {
          id: `codex.visualization.activity.row.updating`,
          defaultMessage: `<verb>Updating</verb> visualization`,
          description: `Tool activity row while Codex updates an inline visualization`,
        },
        rowUpdated: {
          id: `codex.visualization.activity.row.updated`,
          defaultMessage: `<verb>Updated</verb> visualization`,
          description: `Tool activity row after Codex updates an inline visualization`,
        },
        summaryCreatingLeading: {
          id: `codex.visualization.activity.summary.creating.leading`,
          defaultMessage: `Creating visualization`,
          description: `Leading collapsed tool activity summary while Codex creates an inline visualization`,
        },
        summaryCreating: {
          id: `codex.visualization.activity.summary.creating`,
          defaultMessage: `creating visualization`,
          description: `Collapsed tool activity summary while Codex creates an inline visualization`,
        },
        summaryCreatedLeading: {
          id: `codex.visualization.activity.summary.created.leading`,
          defaultMessage: `Created visualization`,
          description: `Leading collapsed tool activity summary after Codex creates an inline visualization`,
        },
        summaryCreated: {
          id: `codex.visualization.activity.summary.created`,
          defaultMessage: `created visualization`,
          description: `Collapsed tool activity summary after Codex creates an inline visualization`,
        },
        summaryUpdatingLeading: {
          id: `codex.visualization.activity.summary.updating.leading`,
          defaultMessage: `Updating visualization`,
          description: `Leading collapsed tool activity summary while Codex updates an inline visualization`,
        },
        summaryUpdating: {
          id: `codex.visualization.activity.summary.updating`,
          defaultMessage: `updating visualization`,
          description: `Collapsed tool activity summary while Codex updates an inline visualization`,
        },
        summaryUpdatedLeading: {
          id: `codex.visualization.activity.summary.updated.leading`,
          defaultMessage: `Updated visualization`,
          description: `Leading collapsed tool activity summary after Codex updates an inline visualization`,
        },
        summaryUpdated: {
          id: `codex.visualization.activity.summary.updated`,
          defaultMessage: `updated visualization`,
          description: `Collapsed tool activity summary after Codex updates an inline visualization`,
        },
      })));
  });
function _i({ summary: e, cwd: t }) {
  return xi({ summary: e, cwd: t })?.isSkillDefinitionFile === !0;
}
function vi({
  summary: e,
  cwd: t,
  intl: n,
  threadDetailLevel: r,
  formatSearchQuery: i,
}) {
  let a = xi({ summary: e, cwd: t });
  if (!a) return null;
  switch (e.type) {
    case `read`:
      return a.isSkillDefinitionFile
        ? an(a)
          ? yi({ isFinished: e.isFinished, intl: n, threadDetailLevel: r })
          : r === `STEPS_PROSE`
            ? bi({ skillName: a.skillName, isFinished: e.isFinished, intl: n })
            : n.formatMessage(
                {
                  id: `localConversationTurn.exploration.skill.definition.read`,
                  defaultMessage: `Read {skillName} skill`,
                  description: `Exploration row for reading a Codex skill definition`,
                },
                { skillName: a.skillName },
              )
        : n.formatMessage(
            {
              id: `localConversationTurn.exploration.skill.read`,
              defaultMessage: `Read {skillName} skill`,
              description: `Exploration row for reading a file from a Codex skill directory`,
            },
            { skillName: a.skillName },
          );
    case `list_files`:
      return n.formatMessage(
        {
          id: `localConversationTurn.exploration.skill.listFiles`,
          defaultMessage: `Listed files in {skillName} skill`,
          description: `Exploration row for listing files in a Codex skill directory`,
        },
        { skillName: a.skillName },
      );
    case `search`:
      if (e.query && e.query.trim().length > 0) {
        let t = i?.(e.query) ?? e.query;
        return n.formatMessage(
          {
            id: `localConversationTurn.exploration.skill.searchFor`,
            defaultMessage: `Searched for {query} in {skillName} skill`,
            description: `Exploration row for searching for a query in a Codex skill directory`,
          },
          { query: t, skillName: a.skillName },
        );
      }
      return n.formatMessage(
        {
          id: `localConversationTurn.exploration.skill.search`,
          defaultMessage: `Searched in {skillName} skill`,
          description: `Exploration row for searching in a Codex skill directory`,
        },
        { skillName: a.skillName },
      );
  }
}
function yi({ isFinished: e, intl: t, threadDetailLevel: n }) {
  return n !== `STEPS_PROSE` || e
    ? t.formatMessage({
        id: `localConversationTurn.exploration.skill.internalKnowledge.read`,
        defaultMessage: `Read Internal Knowledge`,
        description: `Exploration row for reading the canonical Internal Knowledge skill definition`,
      })
    : t.formatMessage(
        {
          id: `localConversationTurn.exploration.skill.internalKnowledge.read.active`,
          defaultMessage: `Reading {skillName}`,
          description: `Simplified exploration row while Codex is reading the canonical Internal Knowledge skill definition`,
        },
        { skillName: on },
      );
}
function bi({ skillName: e, isFinished: t, intl: n }) {
  return t
    ? n.formatMessage(
        {
          id: `localConversationTurn.exploration.skill.definition.read.steps.complete`,
          defaultMessage: `Read {skillName} skill`,
          description: `Simplified exploration row after Codex finishes reading a skill definition`,
        },
        { skillName: e },
      )
    : n.formatMessage(
        {
          id: `localConversationTurn.exploration.skill.definition.read.steps.active`,
          defaultMessage: `Reading {skillName} skill`,
          description: `Simplified exploration row while Codex is reading a skill definition`,
        },
        { skillName: e },
      );
}
function xi({ summary: e, cwd: t }) {
  let n = Si({ summary: e, cwd: t });
  return n ? Pt(n) : null;
}
function Si({ summary: e, cwd: t }) {
  switch (e.type) {
    case `read`:
      return Ci({ path: e.path ?? e.name, cwd: t });
    case `list_files`:
      return e.path ? Ci({ path: e.path, cwd: t }) : null;
    case `search`:
      return e.path ? Ci({ path: e.path, cwd: t }) : null;
  }
}
function Ci({ path: e, cwd: t }) {
  if (!e) return null;
  let n = xt(e);
  return n.length === 0 ? null : t == null ? n : xt(je(t, n));
}
var wi = e(() => {
  (Xt(), Me(), It(), vt());
});
async function Ti({ argumentsValue: e, hostId: t, threadId: n }) {
  let r = Di.safeParse(e);
  if (!r.success) return I(`${Ei} received invalid arguments.`);
  try {
    return (
      await Ln(
        {
          type: `windows.show_thread`,
          windowId: `current`,
          threadId: r.data.threadId,
        },
        { sourceHostId: t, sourceThreadId: n },
      ),
      {
        contentItems: [
          { type: `inputText`, text: JSON.stringify({ navigated: !0 }) },
        ],
        success: !0,
      }
    );
  } catch (e) {
    return I(e instanceof Error ? e.message : `Failed to navigate Codex.`);
  }
}
var Ei,
  Di,
  Oi,
  ki = e(() => {
    (S(),
      Fn(),
      Ot(),
      (Ei = `navigate_to_codex_page`),
      (Di = E({
        threadId: O().min(1).describe(`Thread id to show in Codex.`),
      })),
      (Oi = {
        name: Ei,
        description: `Navigate the most recently focused main Codex window to a thread. Use this when the user asks to open or show a Codex thread in the app.`,
        inputSchema: ae().parse(he(Di)),
      }));
  });
async function Ai({ argumentsValue: e, hostId: t, threadId: n }) {
  let r = Hr.safeParse(e);
  if (!r.success) return I(`${ji} received invalid arguments.`);
  try {
    let e = await Ln(
      { type: `windows.tabs.open`, windowId: `current`, ...r.data },
      { sourceHostId: t, sourceThreadId: n },
    );
    return {
      contentItems: [{ type: `inputText`, text: JSON.stringify(e) }],
      success: !0,
    };
  } catch (e) {
    return I(e instanceof Error ? e.message : `Failed to open Codex tab.`);
  }
}
var ji,
  Mi,
  Ni = e(() => {
    (S(),
      Wr(),
      Fn(),
      Ot(),
      (ji = `open_in_codex`),
      (Mi = {
        name: ji,
        description: `Show a workspace file, browser tab, terminal, or review in a Codex panel. The most recently focused main window is targeted. threadId defaults to the calling thread, and the selected thread must be visible in that window. Use this after creating or editing an artifact when showing the result would help the user. Terminals require a local thread. This only opens Codex UI; use file, browser, or terminal tools to inspect or interact with the content.`,
        inputSchema: ae().parse(he(Hr)),
      }));
  });
function Pi({ hostId: e, queryClient: t }) {
  return !Rr(e) || !Li({ hostId: e, queryClient: t })
    ? []
    : [
        {
          type: `namespace`,
          name: Ki,
          description: `Tools provided by the Plugin Management app in Codex.`,
          tools: [{ type: `function`, ...Yi, deferLoading: !0 }],
        },
      ];
}
async function Fi(e, t) {
  return e.tool === `uninstall_plugin`
    ? Ii(e.arguments, t)
    : I(`Unsupported Plugin Management dynamic tool: ${e.tool}.`);
}
async function Ii(e, { cwd: t, hostId: n, queryClient: r, scope: i }) {
  if (!Rr(n))
    return I(`Plugin uninstall is only supported for local desktop threads.`);
  let a = Ji.safeParse(e);
  if (!a.success) return I(`${qi} received invalid arguments.`);
  if (!(await Ri({ hostId: n, queryClient: r })))
    return I(`Plugin Management is not currently available.`);
  try {
    let { marketplaces: e, marketplaceLoadErrors: o } = await F(
      `send-cli-request-for-host`,
      {
        hostId: n,
        method: `plugin/installed`,
        params: { cwds: t == null || t === `~` ? null : [t] },
      },
    );
    if (o.length > 0)
      return I(`Could not load all installed plugins. Try again.`);
    let s = Bi(pn(e), a.data.plugin);
    if (s.kind === `none`) return Wi({ status: `not_installed` });
    if (s.kind === `separator_insensitive` || s.plugins.length > 1)
      return Wi({
        status: `ambiguous`,
        plugins: s.plugins.map((e) => ({ id: fn(e), name: st(e) })),
      });
    let c = s.plugins[0];
    return (
      await _n({
        hostId: n,
        marketplaceName: c.marketplaceName,
        pluginId: c.plugin.id,
        pluginName: c.plugin.name,
        queryClient: r,
        requestPluginId: fn(c),
        scope: i,
      }),
      Wi({ status: `uninstalled`, plugin: { id: c.plugin.id, name: st(c) } })
    );
  } catch (e) {
    return (
      N.error(`plugin_management_uninstall_failed`, {
        safe: {},
        sensitive: { error: e },
      }),
      I(`Failed to uninstall plugin.`)
    );
  }
}
function Li({ hostId: e, queryClient: t }) {
  let n = t.getQueryState(vn(e));
  return n?.status === `success` && zi(n.data);
}
async function Ri({ hostId: e, queryClient: t }) {
  try {
    return zi(await t.fetchQuery(yn(e)));
  } catch {
    return !1;
  }
}
function zi(e) {
  let t = e?.find((e) => e.id === Gi);
  return t?.isAccessible === !0 && t.isEnabled;
}
function Bi(e, t) {
  let n = e.filter((e) => e.plugin.installed),
    r = Vi(t),
    i = n.filter((e) =>
      [
        e.plugin.id,
        e.plugin.remotePluginId,
        `${e.plugin.name}@${e.marketplaceName}`,
      ].some((e) => e != null && Vi(e) === r),
    );
  if (i.length > 0) return { kind: `exact`, plugins: i };
  let a = Hi(t),
    o = n.filter((e) => [st(e), e.plugin.name].some((e) => Hi(e) === a));
  if (o.length > 0) return { kind: `exact`, plugins: o };
  let s = Ui(t),
    c = n.filter((e) => [st(e), e.plugin.name].some((e) => Ui(e) === s));
  return c.length > 0
    ? { kind: `separator_insensitive`, plugins: c }
    : { kind: `none` };
}
function Vi(e) {
  return e.normalize(`NFKC`).trim().toLowerCase();
}
function Hi(e) {
  return Vi(e).replace(/\s+/gu, ` `);
}
function Ui(e) {
  return Hi(e).replace(/[\s_-]+/gu, ``);
}
function Wi(e) {
  return {
    contentItems: [{ type: `inputText`, text: JSON.stringify(e) }],
    success: !0,
  };
}
var Gi,
  Ki,
  qi,
  Ji,
  Yi,
  Xi = e(() => {
    (M(),
      S(),
      Ot(),
      Te(),
      Lr(),
      ct(),
      gn(),
      dn(),
      bn(),
      re(),
      (Gi = `connector_openai_plugin_management`),
      (Ki = `plugin_management`),
      (qi = `uninstall_plugin`),
      (Ji = E({
        plugin: O()
          .trim()
          .min(1)
          .refine((e) => /[\p{L}\p{N}]/u.test(e)),
      })),
      (Yi = {
        name: qi,
        description: `Uninstall an installed Codex plugin when the user explicitly asks to uninstall or remove it. The explicit request is authorization; do not ask for another confirmation. If the result is ambiguous, ask the user to choose an exact plugin ID before retrying. Do not use this tool for ChatGPT apps, status, or permission questions.`,
        inputSchema: {
          type: `object`,
          properties: {
            plugin: {
              type: `string`,
              description: `The plugin's user-facing name or exact plugin ID.`,
            },
          },
          required: [`plugin`],
          additionalProperties: !1,
        },
      }));
  });
function Zi(e) {
  return new Promise((t, n) => {
    let r = setTimeout(() => {
      n(Error(`Timed out loading thread tool model guidance.`));
    }, ea);
    Qi(e)
      .then(t, n)
      .finally(() => {
        clearTimeout(r);
      });
  });
}
async function Qi(e) {
  let t = [],
    n = null;
  do {
    let r = await e({ cursor: n, includeHidden: !0, limit: $i });
    (t.push(...r.data), (n = r.nextCursor));
  } while (n != null);
  return t;
}
var $i,
  ea,
  ta = e(() => {
    (($i = 100), (ea = 1e3));
  }),
  na,
  ra,
  ia,
  aa,
  oa = e(() => {
    ((na = `read_settings`),
      (ra = `write_settings`),
      (ia = {
        name: na,
        description: `Read Codex settings, effective values after defaults, and the machine-readable setting definitions that Codex is allowed to inspect. Use this to learn how the app can be configured before suggesting or changing settings.`,
        inputSchema: {
          type: `object`,
          properties: {},
          additionalProperties: !1,
        },
      }),
      (aa = {
        name: ra,
        description: `Update one or more Codex settings by writing a partial JSON settings object. Only ordinary settings marked writable for Codex can be changed.`,
        inputSchema: {
          type: `object`,
          properties: {
            settings: {
              type: `object`,
              description: `Partial JSON settings object to update.`,
              additionalProperties: !0,
            },
          },
          required: [`settings`],
          additionalProperties: !1,
        },
      }));
  });
async function sa({ scope: e }) {
  return (
    await e.query.snapshot(we, ne.LOCAL_PROJECTS).fetch(),
    { schemaVersion: 1, projects: ca(e) }
  );
}
function ca(e) {
  return e.get(Tn).map((e) => {
    if (e.projectKind === `local`)
      return {
        projectId: e.projectId,
        projectKind: `local`,
        label: e.label,
        path: e.path ?? void 0,
        hostId: Xe,
        hostDisplayName: null,
      };
    if (e.hostId == null)
      throw Error(`Remote project has no hostId: ${e.projectId}`);
    return {
      projectId: e.projectId,
      projectKind: `remote`,
      label: e.label,
      path: e.path,
      hostId: e.hostId,
      hostDisplayName: e.hostDisplayName ?? null,
    };
  });
}
var la = e(() => {
  (M(), Oe(), Qe(), En());
});
async function ua({
  environment: e = { type: `same-directory` },
  scope: t,
  sourceThreadId: n,
  targetThreadId: r,
  threadSource: i = `subagent`,
}) {
  let a = w(r ?? n),
    o = t.get(yt, a),
    s = t.get(gt, a),
    c = t.get(jt, a);
  switch (e.type) {
    case `same-directory`: {
      let n = await F(`fork-conversation-from-latest`, {
        hostId: o,
        conversationId: a,
        cwd: s ?? void 0,
        workspaceRoots: s == null ? void 0 : [s],
        collaborationMode: c,
        threadSource: i,
      });
      return (
        yr(t, { sourceConversationId: a, targetConversationId: n }),
        { environment: e, sourceThreadId: a, threadId: n, continuation: da }
      );
    }
    case `worktree`: {
      if (s == null)
        throw Error(
          `Cannot fork into a worktree because the source thread has no current directory.`,
        );
      let e = t.get(Dn),
        { clientThreadId: n, pendingWorktreeId: r } = An({
          hostId: o,
          label: e.formatMessage(Qn.forkPendingWorktreeTitle),
          sourceWorkspaceRoot: s,
          startingState: { type: `working-tree` },
          localEnvironmentConfigPath: null,
          launchMode: `fork-conversation`,
          projectAssignment: jn({
            type: `assignment`,
            assignment: t.get(un)?.[a],
            executionHostId: o,
          }),
          prompt: e.formatMessage(Qn.forkPendingWorktreePrompt),
          startConversationParamsInput: null,
          sourceConversationId: a,
          sourceCollaborationMode: c,
          targetTurnId: null,
          threadSource: i,
        });
      return (
        br(t, {
          pendingWorktreeId: r,
          sourceConversationId: a,
          sourceWorkspaceRoot: s,
        }),
        {
          environment: { type: `worktree` },
          clientThreadId: n,
          sourceThreadId: a,
          threadId: null,
          continuation: fa,
        }
      );
    }
  }
}
var da,
  fa,
  pa = e(() => {
    (M(),
      wt(),
      Te(),
      xr(),
      kn(),
      Yn(),
      ln(),
      Mn(),
      On(),
      (da = `The fork contains completed history only. If the source thread was running, the active turn and unfinished response are not in the child. Send a follow-up message to threadId only if the task requires work to continue there.`),
      (fa = `The worktree is being created asynchronously, so no child thread id exists yet. Wait for worktree setup to finish, then send a follow-up message to the child only if the task requires work to continue there.`));
  });
function ma(e) {
  let t = e.name?.trim() || e.preview.trim();
  return t.length > 0 ? t : e.id;
}
function ha(e, t, n, r = () => null) {
  return _a(
    e.map((e) => ga(e, r(e.id))),
    t,
    n,
  );
}
function ga(e, t) {
  let n = Number(e.recencyAt ?? e.updatedAt) * 1e3;
  return {
    threadId: e.id,
    title: ma(e),
    ...(t == null ? {} : { description: t }),
    cwd: e.cwd,
    updatedAt: Number.isFinite(n) ? n : 0,
  };
}
function _a(e, t, n) {
  if (n <= 0) return [];
  let r = t.trim();
  if (r.length === 0) return [];
  let i = [];
  for (let t of e) {
    let e = va(t, r);
    if (!e) continue;
    let a = { candidate: t, match: e };
    if (i.length < n) {
      i.push(a);
      continue;
    }
    let o = ya(i);
    ba(a, i[o]) < 0 && (i[o] = a);
  }
  return (i.sort(ba), i.map(({ candidate: e }) => e));
}
function va(e, t) {
  return (
    xa(e.title, t, 0) ??
    xa(e.description ?? ``, t, 1) ??
    xa(e.branch ?? ``, t, 2) ??
    xa(e.cwd, t, 3)
  );
}
function ya(e) {
  let t = 0;
  for (let n = 1; n < e.length; n += 1) ba(e[t], e[n]) < 0 && (t = n);
  return t;
}
function ba(e, t) {
  let n = e.match.fieldPriority - t.match.fieldPriority;
  if (n !== 0) return n;
  let r = t.match.score - e.match.score;
  return r === 0 ? t.candidate.updatedAt - e.candidate.updatedAt : r;
}
function xa(e, t, n) {
  let r = vr(e, t);
  return r === 0 ? null : { fieldPriority: n, score: r };
}
var Sa = e(() => {
  _r();
});
async function Ca({ scope: e, limit: t, query: n }) {
  let r = n?.trim() ?? ``,
    i = Ea(e, r, t),
    a = await Promise.all(Gn(e).map((e) => wa(e, r, t, i))),
    o = a.flatMap((e) => e.threads),
    s = a.flatMap((e) =>
      e.unavailableHost == null ? [] : [e.unavailableHost],
    ),
    c = (t) => Jt(e, w(t));
  return {
    schemaVersion: 2,
    query: r.length > 0 ? r : null,
    threads: Da(o, r, t, c).map(({ hasUnreadTurn: e, hostId: t, thread: n }) =>
      Oa(n, t, e, c(n.id)),
    ),
    unavailableHosts: s,
  };
}
async function wa(e, t, n, r) {
  try {
    let i = e.getHostId();
    return {
      threads: (t.length === 0
        ? await e.listRecentThreads({ limit: n })
        : await Ta(e, t, n, r)
      ).map((t) => ({ hasUnreadTurn: ja(e, t.id), hostId: i, thread: t })),
    };
  } catch {
    return { threads: [], unavailableHost: Kn(e) };
  }
}
async function Ta(e, t, n, r) {
  let i = await e.searchThreads({ query: t, limit: n }),
    a = new Set(i.map((e) => e.id)),
    o = await Promise.all(
      r
        .filter(({ threadId: e }) => !a.has(e))
        .map(async ({ threadId: t }) => {
          try {
            return (await e.readThread(t)).thread;
          } catch {
            return null;
          }
        }),
    );
  return [...i, ...o.filter((e) => e != null)];
}
function Ea(e, t, n) {
  return t.length === 0
    ? []
    : _a(
        pt(e).map((e) => ({ ...e, cwd: ``, title: ``, updatedAt: 0 })),
        t,
        n,
      );
}
function Da(e, t, n, r) {
  if (t.length === 0)
    return [...e]
      .sort(
        (e, t) =>
          (t.thread.recencyAt ?? t.thread.updatedAt) -
          (e.thread.recencyAt ?? e.thread.updatedAt),
      )
      .slice(0, n);
  let i = new Map(e.map((e) => [e.thread.id, e])),
    a = [];
  for (let o of ha(
    e.map(({ thread: e }) => e),
    t,
    n,
    r,
  )) {
    let e = i.get(o.threadId);
    e != null && a.push(e);
  }
  return a;
}
function Oa(e, t, n, r) {
  let i = {
    id: e.id,
    hostId: t,
    status: e.status.type,
    hasUnreadTurn: n,
    cwd: e.cwd,
    createdAt: e.createdAt,
    updatedAt: e.updatedAt,
  };
  return (
    ka(i, `title`, Ma(e)),
    ka(i, `description`, r),
    ka(i, `preview`, e.preview),
    i
  );
}
function ka(e, t, n) {
  if (n == null) {
    e[t] = null;
    return;
  }
  let r = Aa(n);
  ((e[t] = r.text),
    r.truncated &&
      ((e[`${t}Truncated`] = !0), (e[`${t}OriginalChars`] = r.originalChars)));
}
function Aa(e) {
  return e.length <= Na
    ? { originalChars: e.length, text: e, truncated: !1 }
    : { originalChars: e.length, text: e.slice(0, Na), truncated: !0 };
}
function ja(e, t) {
  let n = w(t);
  return (
    e.getConversation(n)?.hasUnreadTurn ??
    e.getThreadSummaries().find((e) => e.conversationId === n)?.hasUnreadTurn ??
    !1
  );
}
function Ma(e) {
  let t = e.name?.trim() || e.preview.trim();
  return t.length > 0 ? t : e.id;
}
var Na,
  Pa = e(() => {
    (M(), qt(), Sa(), Vn(), (Na = 2e3));
  });
async function Fa({ scope: e, afterCursor: t, hostId: n, threadId: r }) {
  let i = Ra(e, n);
  if (i != null)
    try {
      return await za({
        afterCursor: t,
        hostId: i.hostId,
        manager: i.manager,
        threadId: r,
      });
    } catch {}
  let a = await Bn({ scope: e, requestOptions: eo, threadId: r });
  return za({
    afterCursor: t,
    hostId: a.hostId,
    manager: a.manager,
    threadId: r,
  });
}
async function Ia({
  scope: e,
  afterCursor: t,
  hostId: n,
  ignoreUnchangedTerminal: r = !1,
  signal: i,
  threadId: a,
  timeoutMs: o = 12e4,
}) {
  if ((i?.throwIfAborted(), o === 0)) {
    let o = await Fa({ scope: e, afterCursor: t, hostId: n, threadId: a });
    i?.throwIfAborted();
    let s = Ja(o, r),
      c = r && Ya(o);
    return {
      timedOut: s == null && !c,
      wake: s,
      poll: s == null && !c ? Za(o) : o,
    };
  }
  let s = Date.now() + o,
    c = Ra(e, n),
    l = c ?? (await Bn({ scope: e, requestOptions: eo, threadId: a }));
  i?.throwIfAborted();
  let u = !1,
    d = null,
    f = null,
    p = null,
    m = null,
    h = null,
    g = null,
    _ = x(),
    v = (e) => {
      u || ((u = !0), _.resolve(e));
    },
    y = () => {
      (g?.(),
        (g = null),
        m != null && (globalThis.clearTimeout(m), (m = null)),
        h != null && (globalThis.clearInterval(h), (h = null)),
        v(null));
    };
  i?.addEventListener(`abort`, y, { once: !0 });
  let b = () => {
      if (u || d != null) return;
      let e = l.hostId;
      d = za({
        afterCursor: t,
        hostId: l.hostId,
        manager: l.manager,
        threadId: a,
      })
        .then((t) => {
          if (e !== l.hostId) return;
          let n = Ja(t, r);
          n != null && v(n);
        })
        .catch(() => {})
        .finally(() => {
          e === l.hostId && (d = null);
        });
    },
    S = () =>
      l.manager.addNotificationCallback(
        [`turn/completed`, `thread/status/changed`],
        (e) => {
          if (e.params.threadId !== a || u) return;
          if (e.method === `turn/completed`) {
            ((f = e.params.turn),
              v({ reason: `turnCompleted`, turnId: e.params.turn.id }));
            return;
          }
          p = e.params.status;
          let t = Xa(e.params.status);
          if (t != null) {
            v(t);
            return;
          }
          e.params.status.type === `idle` && b();
        },
      );
  g = S();
  try {
    let n;
    try {
      n = await za({
        afterCursor: t,
        hostId: l.hostId,
        manager: l.manager,
        threadId: a,
      });
    } catch (r) {
      if (c == null) throw r;
      (g?.(), (g = null));
      try {
        ((l = await Bn({ scope: e, requestOptions: eo, threadId: a })),
          i?.throwIfAborted(),
          l.hostId !== c.hostId &&
            ((u = !1), (d = null), (f = null), (p = null), (_ = x())),
          (g = S()),
          (n = await za({
            afterCursor: t,
            hostId: l.hostId,
            manager: l.manager,
            threadId: a,
          })));
      } catch (e) {
        if (!u) throw e;
        let n = await _.promise,
          r = Va({
            afterCursor: t,
            completedTurn: f,
            hostId: l.hostId,
            observedStatus: p,
            threadId: a,
          });
        if (r == null || n == null) throw e;
        return {
          timedOut: !1,
          wake: n,
          poll: n.reason === `turnCompleted` ? r : Za(r),
        };
      }
    }
    i?.throwIfAborted();
    let o = Ja(n, r);
    if (o != null && !u) return { timedOut: !1, wake: o, poll: n };
    if (r && !u && Ya(n)) return { timedOut: !1, wake: null, poll: n };
    let y = Math.max(0, s - Date.now());
    if (y === 0 && !u) return { timedOut: !0, wake: null, poll: Za(n) };
    ((m = globalThis.setTimeout(() => {
      v(null);
    }, y)),
      (h = globalThis.setInterval(() => {
        b();
      }, to)));
    let C = await _.promise;
    i?.throwIfAborted();
    let w;
    try {
      w = await za({
        afterCursor: t,
        completedTurn: f,
        hostId: l.hostId,
        manager: l.manager,
        threadId: a,
      });
    } catch {
      w =
        Va({
          afterCursor: t,
          completedTurn: f,
          hostId: l.hostId,
          observedStatus: p,
          threadId: a,
        }) ?? n;
    }
    return {
      timedOut: C == null,
      wake: C,
      poll: C?.reason === `turnCompleted` ? w : Za(w),
    };
  } finally {
    (i?.removeEventListener(`abort`, y),
      g?.(),
      m != null && globalThis.clearTimeout(m),
      h != null && globalThis.clearInterval(h));
  }
}
async function La({ scope: e, targets: t, timeoutMs: n = 12e4, signal: r }) {
  if ((r?.throwIfAborted(), t.length === 0))
    return { timedOut: !0, wake: null, polls: [] };
  let i = Date.now() + n,
    a = new AbortController(),
    o = x(),
    s = () => {
      (a.abort(r?.reason), o.resolve(void 0));
    };
  r?.addEventListener(`abort`, s, { once: !0 });
  let c = Array(t.length),
    l = t.length;
  t.forEach((t, n) => {
    Ia({
      scope: e,
      ...t,
      ignoreUnchangedTerminal: !0,
      signal: a.signal,
      timeoutMs: Math.max(0, i - Date.now()),
    }).then(
      (e) => {
        ((c[n] = { result: e }),
          --l,
          (e.wake != null || l === 0) && o.resolve(void 0));
      },
      (e) => {
        ((c[n] = {
          error: (e instanceof Error ? e.message : String(e)).slice(0, Qa),
        }),
          --l,
          l === 0 && o.resolve(void 0));
      },
    );
  });
  try {
    (await o.promise, await Promise.resolve(), r?.throwIfAborted());
    let e = c.find((e) => e != null && `result` in e && e.result.wake != null),
      n = c.flatMap((e) => (e != null && `result` in e ? [e.result.poll] : [])),
      i = t.flatMap((e, t) => {
        let n = c[t];
        return n != null && `error` in n
          ? [{ threadId: e.threadId, hostId: e.hostId, message: n.error }]
          : [];
      });
    return e == null || !(`result` in e) || e.result.wake == null
      ? {
          timedOut: c.some(
            (e) => e != null && `result` in e && e.result.timedOut,
          ),
          wake: null,
          polls: n,
          ...(i.length > 0 ? { errors: i } : {}),
        }
      : {
          timedOut: !1,
          wake: {
            ...e.result.wake,
            threadId: e.result.poll.thread.id,
            hostId: e.result.poll.thread.hostId,
          },
          polls: n,
          ...(i.length > 0 ? { errors: i } : {}),
        };
  } finally {
    (r?.removeEventListener(`abort`, s), a.abort());
  }
}
function Ra(e, t) {
  if (t == null) return null;
  let n = Gn(e).find((e) => e.getHostId() === t);
  return n == null ? null : { hostId: t, manager: n };
}
async function za({
  afterCursor: e,
  completedTurn: t,
  hostId: n,
  manager: r,
  threadId: i,
}) {
  ro += 1;
  let a = ro,
    [{ thread: o }, s] = await Promise.all([
      r.readThread(i, { includeTurns: !1, requestOptions: eo }),
      r.listThreadTurns(i, { itemsView: `full`, limit: 1, requestOptions: eo }),
    ]),
    c = s.data[0] ?? null,
    l =
      t != null &&
      c?.id === t.id &&
      c.itemsView === `full` &&
      t.itemsView !== `full`
        ? { ...t, items: c.items, itemsView: c.itemsView }
        : t,
    u =
      l != null &&
      (c == null || (l.id === c.id && c.status === `inProgress`) || Wa(l, c))
        ? l
        : c,
    d = Ha(n, i),
    f =
      l != null &&
      l.itemsView !== `full` &&
      u?.id === l.id &&
      d?.latestTurn?.id === u?.id;
  return Ba(
    Ua({
      hostId: n,
      readOrdinal: a,
      threadId: i,
      candidate: {
        thread: { id: o.id, hostId: n, status: o.status },
        latestTurn:
          u == null
            ? null
            : {
                id: u.id,
                status: u.status,
                error:
                  u.error == null
                    ? null
                    : { message: u.error.message.slice(0, Qa) },
                startedAt: u.startedAt,
                completedAt: u.completedAt,
                durationMs: u.durationMs,
              },
        latestAssistantMessage: Ka(u) ?? (f ? d.latestAssistantMessage : null),
        latestToolMarker: qa(u) ?? (f ? d.latestToolMarker : null),
      },
    }),
    e,
  );
}
function Ba(e, t) {
  let n = `${e.generation}:${e.revision}`,
    r = Ga(t, e.generation),
    i = t != null && (r == null || r > e.revision),
    a = i || r !== e.revision;
  return {
    schemaVersion: 1,
    cursor: n,
    revision: e.revision,
    changed: a,
    ...(i ? { cursorReset: !0 } : {}),
    thread: e.projection.thread,
    latestTurn: e.projection.latestTurn,
    latestAssistantMessageId: e.projection.latestAssistantMessage?.id ?? null,
    latestAssistantMessage: a ? e.projection.latestAssistantMessage : null,
    latestToolMarkerId: e.projection.latestToolMarker?.id ?? null,
    latestToolMarker: a ? e.projection.latestToolMarker : null,
  };
}
function Va({
  afterCursor: e,
  completedTurn: t,
  hostId: n,
  observedStatus: r,
  threadId: i,
}) {
  let a = Ha(n, i);
  if (a == null && t == null) return null;
  let o = t != null && a?.latestTurn?.id === t.id ? a : null,
    s =
      t == null
        ? (a?.latestTurn ?? null)
        : {
            id: t.id,
            status: t.status,
            error:
              t.error == null
                ? null
                : { message: t.error.message.slice(0, Qa) },
            startedAt: t.startedAt,
            completedAt: t.completedAt,
            durationMs: t.durationMs,
          };
  return (
    (ro += 1),
    Ba(
      Ua({
        hostId: n,
        readOrdinal: ro,
        threadId: i,
        candidate: {
          thread: {
            id: i,
            hostId: n,
            status:
              t != null && (r?.type !== `active` || r.activeFlags.length === 0)
                ? { type: `idle` }
                : (r ??
                  a?.thread.status ?? { type: `active`, activeFlags: [] }),
          },
          latestTurn: s,
          latestAssistantMessage:
            t == null
              ? (a?.latestAssistantMessage ?? null)
              : (Ka(t) ?? o?.latestAssistantMessage ?? null),
          latestToolMarker:
            t == null
              ? (a?.latestToolMarker ?? null)
              : (qa(t) ?? o?.latestToolMarker ?? null),
        },
      }),
      e,
    )
  );
}
function Ha(e, t) {
  return z.get(JSON.stringify([e, t]))?.projection ?? null;
}
function Ua({ candidate: e, hostId: t, readOrdinal: n, threadId: r }) {
  let i = JSON.stringify([t, r]),
    a = z.get(i);
  if (a != null && n < a.lastCommittedReadOrdinal) return a;
  let o = e,
    s = a?.projection.latestTurn,
    c = e.latestTurn,
    l = a?.projection.latestAssistantMessage,
    u = e.latestAssistantMessage;
  a != null &&
  s != null &&
  (c == null ||
    Wa(s, c) ||
    (s.id === c.id && s.status !== `inProgress` && c.status === `inProgress`))
    ? (o = { ...a.projection, thread: e.thread })
    : c?.status === `inProgress` &&
      l != null &&
      u != null &&
      l.id === u.id &&
      l.text.length > u.text.length &&
      (o = { ...e, latestAssistantMessage: l });
  let d = JSON.stringify({
      status: o.thread.status,
      turnId: o.latestTurn?.id ?? null,
      turnStatus: o.latestTurn?.status ?? null,
      turnError: o.latestTurn?.error?.message ?? null,
      assistant: o.latestAssistantMessage,
      tool: o.latestToolMarker,
    }),
    f = {
      generation: a?.generation ?? crypto.randomUUID(),
      revision: a == null ? 1 : a.revision + (a.fingerprint === d ? 0 : 1),
      fingerprint: d,
      lastCommittedReadOrdinal: n,
      projection: o,
    };
  if ((z.delete(i), z.set(i, f), z.size > $a)) {
    let e = z.keys().next().value;
    e != null && z.delete(e);
  }
  return f;
}
function Wa(e, t) {
  return e.id === t.id
    ? !1
    : e.startedAt != null && t.startedAt != null && e.startedAt !== t.startedAt
      ? e.startedAt > t.startedAt
      : no.test(e.id) && no.test(t.id)
        ? e.id.toLowerCase() > t.id.toLowerCase()
        : !1;
}
function Ga(e, t) {
  let n = `${t}:`;
  if (e == null || !e.startsWith(n)) return null;
  let r = e.slice(n.length);
  if (!/^\d+$/u.test(r)) return null;
  let i = Number(r);
  return Number.isSafeInteger(i) && i > 0 ? i : null;
}
function Ka(e) {
  if (e == null) return null;
  let t = e.status === `inProgress` ? ze(e.items) : -1;
  for (let n = e.items.length - 1; n >= 0; --n) {
    let r = e.items[n];
    if (r?.type !== `agentMessage`) continue;
    let i = n === t,
      a = Re(r.text, i);
    if ((a.removed && a.content.length === 0) || (i && Le(a.content))) continue;
    let o = We(a.content),
      s = it(
        o == null ? a.content : o.visibleText || o.notificationMessage || ``,
      );
    if (s.length !== 0)
      return {
        id: r.id,
        turnId: e.id,
        phase: r.phase,
        text: s.slice(0, Qa),
        ...(s.length > Qa ? { truncated: !0, originalChars: s.length } : {}),
      };
  }
  return null;
}
function qa(e) {
  if (e == null) return null;
  for (let t = e.items.length - 1; t >= 0; --t) {
    let n = e.items[t];
    if (n != null)
      switch (n.type) {
        case `commandExecution`:
        case `fileChange`:
        case `imageGeneration`:
          return {
            id: n.id,
            turnId: e.id,
            type: n.type,
            name: n.type,
            status: n.status,
          };
        case `collabAgentToolCall`:
        case `dynamicToolCall`:
        case `mcpToolCall`:
          return {
            id: n.id,
            turnId: e.id,
            type: n.type,
            name: n.tool,
            status: n.status,
          };
        case `sleep`:
        case `webSearch`:
          return {
            id: n.id,
            turnId: e.id,
            type: n.type,
            name: n.type,
            status: null,
          };
        case `agentMessage`:
        case `contextCompaction`:
        case `enteredReviewMode`:
        case `exitedReviewMode`:
        case `hookPrompt`:
        case `imageView`:
        case `plan`:
        case `reasoning`:
        case `subAgentActivity`:
        case `userMessage`:
          break;
      }
  }
  return null;
}
function Ja(e, t) {
  return (
    Xa(e.thread.status) ??
    (e.thread.status.type === `idle`
      ? e.latestTurn == null
        ? { reason: `inactiveStatus` }
        : e.latestTurn.status === `inProgress` || (t && Ya(e))
          ? null
          : { reason: `turnCompleted`, turnId: e.latestTurn.id }
      : null)
  );
}
function Ya(e) {
  return (
    !e.changed &&
    e.thread.status.type === `idle` &&
    e.latestTurn != null &&
    e.latestTurn.status !== `inProgress`
  );
}
function Xa(e) {
  switch (e.type) {
    case `systemError`:
    case `notLoaded`:
      return { reason: `inactiveStatus` };
    case `active`:
      return e.activeFlags.length > 0 ? { reason: `actionableStatus` } : null;
    case `idle`:
      return null;
  }
}
function Za(e) {
  let t = JSON.stringify([e.thread.hostId, e.thread.id]),
    n = z.get(t)?.projection;
  return {
    ...e,
    latestAssistantMessage:
      n?.latestAssistantMessage ?? e.latestAssistantMessage,
    latestToolMarker: n?.latestToolMarker ?? e.latestToolMarker,
  };
}
var Qa,
  $a,
  eo,
  to,
  no,
  z,
  ro,
  io = e(() => {
    (M(),
      Ge(),
      et(),
      Ve(),
      Vn(),
      Hn(),
      (Qa = 2e3),
      ($a = 256),
      (eo = {
        priority: `background`,
        source: `tail_history`,
        timeoutMs: 15e3,
      }),
      (to = 5e3),
      (no =
        /^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/iu),
      (z = new Map()),
      (ro = 0));
  });
function ao(e, t) {
  return e.get(fo, t);
}
function oo(e, t, n) {
  let r = ao(e, t);
  (e.set(fo, t, (e) => {
    let t = typeof n == `function` ? n(e) : n;
    return t == null || e == null || t === e || t.revision !== e.revision
      ? t
      : { ...t, revision: e.revision + 1 };
  }),
    ao(e, t)?.revision !== r?.revision && lo(t));
}
function so({ scope: e, operationId: t, waitMs: n, afterRevision: r }) {
  let i = ao(e, t);
  return i == null || n === 0 || (r != null && i.revision > r) || co(i.status)
    ? Promise.resolve(i)
    : new Promise((r) => {
        let i = uo.get(t) ?? new Set();
        uo.set(t, i);
        let a = () => {
            (clearTimeout(o),
              i.delete(a),
              i.size === 0 && uo.delete(t),
              r(ao(e, t)));
          },
          o = setTimeout(a, n);
        i.add(a);
      });
}
function co(e) {
  return e === `success` || e === `warning` || e === `error`;
}
function lo(e) {
  let t = uo.get(e);
  if (t != null) for (let e of t) e();
}
var uo,
  fo,
  po = e(() => {
    (u(), a(), (uo = new Map()), (fo = r(d, (e) => null)));
  });
function mo({ cwd: e, sourceWorktreeRoot: t, repoWorktreeEntries: n }) {
  let r = ho({ cwd: e, sourceWorktreeRoot: t }),
    i = Pe(t);
  return (n ?? [])
    .filter((e) => Pe(e.root) !== i)
    .map((e) => {
      let t = r.length > 0 ? Ae(e.root, r) : e.root;
      return { gitRoot: e.root, workspaceRoot: t };
    });
}
function ho({ cwd: e, sourceWorktreeRoot: t }) {
  let n = Ne(e),
    r = Ne(t).replace(/\/+$/, ``),
    i = Pe(e),
    a = Pe(t).replace(/\/+$/, ``);
  if (i === a) return ``;
  let o = `${a}/`;
  if (!i.startsWith(o)) return ``;
  let s = `${r}/`;
  return n.slice(s.length);
}
var go = e(() => {
  Me();
});
async function _o({ conversationId: e, sourceThreadRunning: t }) {
  t &&
    (await F(`interrupt-conversation`, {
      conversationId: e,
      initiatedBy: `system`,
    }));
}
var vo = e(() => {
    Te();
  }),
  yo,
  bo = e(() => {
    yo = [
      `prepare-host-transfer`,
      `transfer-host-artifacts`,
      `create-new-worktree`,
      `reuse-existing-worktree`,
      `stash-source-changes`,
      `detach-worktree-branch`,
      `checkout-local-branch`,
      `stash-target-worktree-changes`,
      `checkout-worktree-branch`,
      `apply-changes-to-worktree`,
      `apply-changes-to-local`,
      `switching-thread`,
    ];
  });
async function xo({ sourceConversationId: e, targetConversationId: t }) {
  if (e === t) return;
  let { threadIds: n } = await k(`list-pinned-threads`, {});
  n.indexOf(e) !== -1 &&
    (await k(`set-pinned-threads-order`, {
      params: {
        threadIds: n
          .map((n) => (n === e ? t : n))
          .filter((e, t, n) => n.indexOf(e) === t),
      },
    }));
}
var So = e(() => {
  ce();
});
async function Co({
  sourceConversationId: e,
  targetConversationId: t,
  getTitle: n,
  setTitle: r,
}) {
  if (e === t) return;
  let i = n(e);
  i != null && (await r(t, i));
}
var wo = e(() => {});
async function To({
  conversationId: e,
  sourceBranch: t,
  cwd: n,
  destinationWorkspaceRoot: r,
  intl: i,
  appServerManager: a,
  sourceHostConfig: o,
  destinationHostConfig: s,
  transferBrowserState: c,
  setDiffComments: l,
  queryClient: u,
  operationId: d,
  onSwitchingThreadStart: f,
  onSwitchingThreadDone: p,
  onSwitchingThreadFailed: m,
  onTargetCwd: h,
  onTargetConversationId: g,
  onStepStatus: _,
}) {
  let v = a.getConversation(e),
    y = v?.rolloutPath ?? null;
  if (y == null || y.trim().length === 0)
    return {
      status: `error`,
      message: i.formatMessage({
        id: `localConversation.moveToHostWorktree.error.rolloutMissing`,
        defaultMessage: `This chat does not have a rollout file to move`,
        description: `Error shown when cross-host handoff cannot find the source rollout file`,
      }),
      execOutput: null,
    };
  let b = null,
    x = null,
    S = !1;
  if (v?.forkedFromId != null)
    try {
      let e = await L(`git`).request({
        method: `resolve-worktree-for-thread`,
        params: {
          cwd: r,
          conversationId: v.forkedFromId,
          hostConfig: s,
          operationSource: `move_to_host_worktree`,
        },
      });
      ((b = e.worktreeGitRoot),
        (x = e.worktreeWorkspaceRoot),
        (S = e.hasUncommittedChanges));
    } catch {
      N.warning(
        `Failed to resolve a reusable cross-host worktree. Falling back to creating a new worktree.`,
      );
    }
  b != null &&
    x != null &&
    (_?.(`reuse-existing-worktree`, `running`),
    _?.(`reuse-existing-worktree`, `done`));
  let C = await L(`git`).request({
    method: `move-thread-to-host-worktree`,
    params: {
      operationId: d,
      hostConfig: o,
      operationSource: `move_to_host_worktree`,
      sourceCwd: n,
      sourceBranch: t,
      sourceRolloutPath: y,
      destinationHostConfig: s,
      destinationWorkspaceRoot: r,
      destinationWorktreeGitRoot: b,
      destinationWorktreeWorkspaceRoot: x,
      stashDestinationWorktree: S,
    },
  });
  if (C.status === `error`)
    return {
      status: `error`,
      message: i.formatMessage(
        {
          id: `localConversation.moveToHostWorktree.error.move`,
          defaultMessage: `Failed to continue on another host: {message}`,
          description: `Error shown when cross-host handoff git or artifact transfer fails`,
        },
        { message: C.message },
      ),
      execOutput: C.execOutput ?? null,
    };
  (u.invalidateQueries({ queryKey: cr($t(s)) }), f?.());
  let w;
  try {
    w = await F(`fork-conversation-from-rollout-path`, {
      hostId: s.id,
      conversationId: e,
      rolloutPath: C.rolloutPath,
      cwd: C.worktreeWorkspaceRoot,
      workspaceRoots: [C.worktreeWorkspaceRoot],
      addForkedSyntheticItem: !1,
    });
  } catch (t) {
    try {
      await k(`worktree-set-owner-thread`, {
        params: {
          hostId: s.id,
          worktree: C.worktreeGitRoot,
          conversationId: e,
        },
      });
    } catch {}
    return (
      N.warning(
        `Cross-host handoff moved git state, but conversation fork failed: {}`,
        { sensitive: { error: A(t) }, safe: {} },
      ),
      m?.(),
      { status: `success`, conversationId: e }
    );
  } finally {
    await Eo(`Cross-host handoff copied rollout cleanup failed: {}`, () =>
      L(`git`).request({
        method: `cleanup-host-handoff-transfer`,
        params: {
          hostConfig: s,
          operationSource: `move_to_host_worktree`,
          rolloutPath: C.rolloutPath,
        },
      }),
    );
  }
  return (
    g?.(w),
    h?.(C.worktreeWorkspaceRoot),
    await Yt(w, t),
    await Eo(
      `Cross-host handoff succeeded, but worktree ownership metadata could not be updated: {}`,
      () =>
        k(`worktree-set-owner-thread`, {
          params: {
            hostId: s.id,
            worktree: C.worktreeGitRoot,
            conversationId: w,
          },
        }),
    ),
    await Eo(
      `Cross-host handoff succeeded, but browser state could not be transferred: {}`,
      () => c(e, w),
    ),
    await Eo(
      `Cross-host handoff succeeded, but thread title could not be transferred: {}`,
      () =>
        Co({
          sourceConversationId: e,
          targetConversationId: w,
          getTitle: (e) => a.getConversation(e)?.title ?? null,
          setTitle: (e, t) =>
            F(`set-thread-title`, { conversationId: e, title: t }),
        }),
    ),
    await Eo(
      `Cross-host handoff succeeded, but saved diff comments could not be transferred: {}`,
      () => {
        l((t) =>
          Qr({
            sourceConversationId: e,
            targetConversationId: w,
            diffComments: t,
          }),
        );
      },
    ),
    await Eo(
      `Cross-host handoff succeeded, but pinned-thread state could not be transferred: {}`,
      () => xo({ sourceConversationId: e, targetConversationId: w }),
    ),
    p?.(),
    { status: `success`, conversationId: w }
  );
}
async function Eo(e, t) {
  try {
    await t();
  } catch (t) {
    N.warning(e, { sensitive: { error: A(t) }, safe: {} });
  }
}
var Do = e(() => {
  (M(), Te(), At(), nn(), re(), ce(), Wt(), tr(), $r(), So(), wo());
});
async function Oo({
  conversationId: e,
  currentBranch: t,
  cwd: n,
  localGitRoot: r,
  localWorkspaceRoot: i,
  worktreeRoot: a,
  branchCheckedOutElsewhere: o,
  intl: s,
  appServerManager: c,
  transferBrowserState: l,
  setDiffComments: u,
  hostConfig: d,
  operationId: f,
  onSwitchingThreadStart: p,
  onSwitchingThreadDone: m,
  onSwitchingThreadFailed: h,
  onTargetCwd: g,
  onTargetConversationId: _,
}) {
  if (r == null || i == null || a == null || o)
    return {
      status: `error`,
      message: s.formatMessage({
        id: `localConversation.moveToLocal.error.prerequisites`,
        defaultMessage: `Unable to move right now. Check local workspace state and retry.`,
        description: `Error shown when move-to-local cannot start due to missing prerequisites`,
      }),
    };
  let v = ue(i);
  if (v == null)
    return {
      status: `error`,
      message: s.formatMessage(
        {
          id: `localConversation.moveToLocal.error.fork`,
          defaultMessage: `{message}`,
          description: `Error shown when Move to local fails`,
        },
        { message: `Invalid local workspace root` },
      ),
    };
  let y = await L(`git`).request({
    method: `move-thread-to-local`,
    params: {
      operationId: f,
      hostConfig: d,
      operationSource: `move_to_local_dialog`,
      sourceWorktreeCwd: n,
      sourceWorktreeRoot: a,
      localGitRoot: r,
      sourceBranch: t,
    },
  });
  if (y.status === `error`) {
    let e =
      y.rollbackErrors.length > 0
        ? s.formatMessage({
            id: `localConversation.moveToLocal.error.rollbackIssues`,
            defaultMessage: ` Some cleanup steps could not be completed`,
            description: `Suffix appended to move-to-local error message when rollback has issues`,
          })
        : ``;
    return {
      status: `error`,
      message: `${ko({ branch: t, error: y.error, intl: s })}${e}`,
      execOutput: y.execOutput,
    };
  }
  (y.warnings.includes(`drop-source-stash-failed`) &&
    N.warning(`Move to local succeeded, but source stash cleanup failed`),
    p?.());
  let b;
  try {
    b = await F(`fork-conversation-from-latest`, {
      hostId: c.getHostId(),
      conversationId: e,
      cwd: v,
      workspaceRoots: [v],
      addForkedSyntheticItem: !1,
    });
  } catch (t) {
    return (
      N.warning(
        `Move to local git operations succeeded, but conversation fork failed: {}`,
        { sensitive: { error: A(t) }, safe: {} },
      ),
      h?.(),
      { status: `success`, conversationId: e }
    );
  }
  (_?.(b), g?.(v));
  try {
    await k(`worktree-set-owner-thread`, {
      params: { hostId: d.id, worktree: a, conversationId: b },
    });
  } catch (e) {
    N.warning(
      `Move to local succeeded, but worktree ownership metadata could not be updated: {}`,
      { sensitive: { error: A(e) }, safe: {} },
    );
  }
  try {
    l(e, b);
  } catch (e) {
    N.warning(
      `Move to local succeeded, but browser state could not be transferred: {}`,
      { sensitive: { error: A(e) }, safe: {} },
    );
  }
  try {
    await Co({
      sourceConversationId: e,
      targetConversationId: b,
      getTitle: (e) => c.getConversation(e)?.title ?? null,
      setTitle: (e, t) =>
        F(`set-thread-title`, { conversationId: e, title: t }),
    });
  } catch (e) {
    N.warning(
      `Move to local succeeded, but thread title could not be transferred: {}`,
      { sensitive: { error: A(e) }, safe: {} },
    );
  }
  try {
    u((t) =>
      Qr({ sourceConversationId: e, targetConversationId: b, diffComments: t }),
    );
  } catch (e) {
    N.warning(
      `Move to local succeeded, but saved diff comments could not be transferred: {}`,
      { sensitive: { error: A(e) }, safe: {} },
    );
  }
  try {
    await xo({ sourceConversationId: e, targetConversationId: b });
  } catch (e) {
    N.warning(
      `Move to local succeeded, but pinned-thread state could not be transferred: {}`,
      { sensitive: { error: A(e) }, safe: {} },
    );
  }
  return (m?.(), { status: `success`, conversationId: b });
}
function ko({ branch: e, error: t, intl: n }) {
  switch (t) {
    case `invalid-params`:
      return n.formatMessage({
        id: `localConversation.moveToLocal.error.invalidParams`,
        defaultMessage: `Unable to move right now. Check local workspace state and retry`,
        description: `Error shown when move-to-local receives invalid worker parameters`,
      });
    case `branch-checked-out-elsewhere`:
      return n.formatMessage({
        id: `localConversation.moveToLocal.error.branchCheckedOutElsewhere`,
        defaultMessage: `Branch is already checked out in another worktree`,
        description: `Error shown when move-to-local cannot use a branch that is checked out in another worktree`,
      });
    case `source-detach-failed`:
      return n.formatMessage({
        id: `localConversation.moveToLocal.error.sourceDetachFailed`,
        defaultMessage: `Failed to detach the worktree branch`,
        description: `Error shown when move-to-local cannot detach the source worktree`,
      });
    case `local-status-check-failed`:
      return n.formatMessage({
        id: `localConversation.moveToLocal.error.localStatusCheckFailed`,
        defaultMessage: `Unable to determine whether the local workspace is clean`,
        description: `Error shown when move-to-local cannot read the destination workspace status`,
      });
    case `local-destination-has-tracked-changes`:
      return n.formatMessage({
        id: `localConversation.moveToLocal.error.localDestinationHasTrackedChanges`,
        defaultMessage: `Stash or commit your local changes to hand off`,
        description: `Error shown when move-to-local is blocked by local workspace changes`,
      });
    case `local-branch-head-mismatch`:
      return n.formatMessage(
        {
          id: `localConversation.moveToLocal.error.localBranchHeadMismatch`,
          defaultMessage: `Branch “{branch}” already exists at a different commit`,
          description: `Error shown when move-to-local finds that the requested local branch points at a different commit`,
        },
        { branch: e },
      );
    case `checkout-local-failed`:
      return n.formatMessage(
        {
          id: `localConversation.moveToLocal.error.checkoutLocalFailed`,
          defaultMessage: `Failed to check out branch “{branch}” locally`,
          description: `Error shown when move-to-local cannot check out the destination branch`,
        },
        { branch: e },
      );
    case `apply-source-stash-failed`:
      return n.formatMessage({
        id: `localConversation.moveToLocal.error.applySourceStashFailed`,
        defaultMessage: `Failed to apply worktree changes locally`,
        description: `Error shown when move-to-local cannot apply the source worktree changes`,
      });
    case `rollback-failed`:
      return n.formatMessage({
        id: `localConversation.moveToLocal.error.rollbackFailed`,
        defaultMessage: `Failed to restore state after the handoff failed`,
        description: `Error shown when move-to-local cannot restore state after a failed operation`,
      });
    case `unexpected-error`:
      return n.formatMessage({
        id: `localConversation.moveToLocal.error.unexpected`,
        defaultMessage: `Failed to move to local`,
        description: `Fallback error shown when move-to-local fails`,
      });
  }
}
var Ao = e(() => {
  (M(), Te(), re(), ce(), Wt(), $r(), So(), wo());
});
async function jo({
  conversationId: e,
  currentBranch: t,
  cwd: n,
  worktreeCheckoutBranch: r,
  selectedLocalCheckoutBranch: i,
  intl: a,
  appServerManager: o,
  defaultBranch: s,
  transferBrowserState: c,
  setDiffComments: l,
  hostConfig: u,
  queryClient: d,
  operationId: f,
  onStepStatus: p,
  onTargetCwd: m,
  onTargetConversationId: h,
}) {
  let g = r.trim();
  if (g.length === 0)
    return {
      status: `error`,
      message: a.formatMessage({
        id: `localConversation.moveToWorktree.error.branchRequired`,
        defaultMessage: `Worktree branch name is required`,
        description: `Error shown when move-to-worktree is attempted without a target worktree branch name`,
      }),
      execOutput: null,
    };
  let _ = null,
    v = null,
    y = !1,
    b = !1,
    x = `create-new-worktree`;
  try {
    let t = await L(`git`).request({
      method: `resolve-worktree-for-thread`,
      params: {
        cwd: n,
        conversationId: e,
        hostConfig: u,
        operationSource: `move_to_worktree_dialog`,
      },
    });
    ((_ = t.worktreeGitRoot),
      (v = t.worktreeWorkspaceRoot),
      (b = t.hasUncommittedChanges));
  } catch {
    N.warning(
      `Failed to resolve an existing worktree for thread. Falling back to creating a new worktree.`,
    );
  }
  if (_ != null && v != null)
    ((x = `reuse-existing-worktree`), p?.(x, `running`), p?.(x, `done`));
  else {
    p?.(x, `running`);
    try {
      let e = await k(`worktree-create-managed`, {
        params: {
          hostId: u.id,
          cwd: n,
          startingState: { type: `branch`, branchName: t },
          localEnvironmentConfigPath: null,
          streamId: Tt(),
        },
      });
      ((_ = e.worktreeGitRoot),
        (v = e.worktreeWorkspaceRoot),
        (y = !0),
        (b = !1),
        p?.(x, `done`),
        d.invalidateQueries({ queryKey: cr($t(u)) }));
    } catch (e) {
      return (
        p?.(x, `failed`),
        {
          status: `error`,
          message: a.formatMessage(
            {
              id: `localConversation.moveToWorktree.error.createWorktree`,
              defaultMessage: `Failed to create worktree: {message}`,
              description: `Error shown when Move to worktree fails to create a worktree`,
            },
            { message: A(e) },
          ),
          execOutput: null,
        }
      );
    }
  }
  if (_ == null || v == null)
    return {
      status: `error`,
      message: a.formatMessage({
        id: `localConversation.moveToWorktree.error.unknown`,
        defaultMessage: `Failed to move to worktree. Please retry.`,
        description: `Fallback error shown when move-to-worktree fails`,
      }),
      execOutput: null,
    };
  let S = await k(`git-create-branch`, {
    source: `move_to_worktree_dialog`,
    params: { cwd: v, branch: g, hostId: u.id, mode: `worktree` },
  });
  if (S.status === `error`) {
    if (y)
      try {
        await k(`worktree-delete`, {
          params: { hostId: u.id, worktree: _, reason: `new-branch-cleanup` },
        });
      } catch (e) {
        N.warning(`Failed to clean up the newly created worktree: {}`, {
          sensitive: { error: A(e) },
          safe: {},
        });
      } finally {
        d.invalidateQueries({ queryKey: cr($t(u)) });
      }
    return {
      status: `error`,
      message: a.formatMessage(
        {
          id: `localConversation.moveToWorktree.error.prepareBranch`,
          defaultMessage: `Failed to prepare worktree branch “{branch}”: {message}`,
          description: `Error shown when move-to-worktree cannot create or resolve the target worktree branch`,
        },
        { branch: g, message: S.error },
      ),
      execOutput: S.execOutput,
    };
  }
  let C = await L(`git`).request({
    method: `move-thread-to-worktree`,
    params: {
      operationId: f,
      hostConfig: u,
      operationSource: `move_to_worktree_dialog`,
      localCwd: n,
      sourceBranch: t,
      defaultBranch: s,
      localCheckoutBranch: i,
      worktreeCheckoutBranch: g,
      worktreeGitRoot: _,
      worktreeWorkspaceRoot: v,
      stashTargetWorktree: b,
      createdWorktree: y,
    },
  });
  if (C.status === `error`) {
    let e =
      C.rollbackErrors.length > 0
        ? a.formatMessage(
            {
              id: `localConversation.moveToWorktree.error.rollbackIssues`,
              defaultMessage: ` Rollback issues: {issues}`,
              description: `Suffix appended to move-to-worktree error message when rollback has issues`,
            },
            { issues: C.rollbackErrors.join(`, `) },
          )
        : ``;
    return {
      status: `error`,
      message:
        (C.execOutput?.output.length ?? 0) > 0
          ? `${a.formatMessage({ id: `localConversation.moveToWorktree.error.moveWithTerminalOutput`, defaultMessage: `Failed to move to worktree`, description: `Error shown when Move to worktree git operations fail with command output shown separately` })}${e}`
          : a.formatMessage(
              {
                id: `localConversation.moveToWorktree.error.move`,
                defaultMessage: `Failed to move to worktree: {message}`,
                description: `Error shown when Move to worktree git operations fail in the worker`,
              },
              { message: `${C.message}${e}` },
            ),
      execOutput: C.execOutput,
    };
  }
  p?.(`switching-thread`, `running`);
  let w;
  try {
    w = await F(`fork-conversation-from-latest`, {
      hostId: o.getHostId(),
      conversationId: e,
      cwd: v,
      workspaceRoots: [v],
      addForkedSyntheticItem: !1,
    });
  } catch (t) {
    try {
      await k(`worktree-set-owner-thread`, {
        params: { hostId: u.id, worktree: _, conversationId: e },
      });
    } catch {}
    return (
      N.warning(
        `Move to worktree git operations succeeded, but conversation fork failed: {}`,
        { sensitive: { error: A(t) }, safe: {} },
      ),
      p?.(`switching-thread`, `failed`),
      { status: `success`, conversationId: e }
    );
  }
  (h?.(w), m?.(v));
  try {
    await k(`worktree-set-owner-thread`, {
      params: { hostId: u.id, worktree: _, conversationId: w },
    });
  } catch (e) {
    N.warning(
      `Move to worktree succeeded, but worktree ownership metadata could not be updated: {}`,
      { sensitive: { error: A(e) }, safe: {} },
    );
  }
  try {
    c(e, w);
  } catch (e) {
    N.warning(
      `Move to worktree succeeded, but browser state could not be transferred: {}`,
      { sensitive: { error: A(e) }, safe: {} },
    );
  }
  try {
    await Co({
      sourceConversationId: e,
      targetConversationId: w,
      getTitle: (e) => o.getConversation(e)?.title ?? null,
      setTitle: (e, t) =>
        F(`set-thread-title`, { conversationId: e, title: t }),
    });
  } catch (e) {
    N.warning(
      `Move to worktree succeeded, but thread title could not be transferred: {}`,
      { sensitive: { error: A(e) }, safe: {} },
    );
  }
  try {
    l((t) =>
      Qr({ sourceConversationId: e, targetConversationId: w, diffComments: t }),
    );
  } catch (e) {
    N.warning(
      `Move to worktree succeeded, but saved diff comments could not be transferred: {}`,
      { sensitive: { error: A(e) }, safe: {} },
    );
  }
  (C.warnings.includes(`stashed-target-worktree-changes`) &&
    N.warning(
      `Move to worktree stashed uncommitted changes in the reused worktree`,
    ),
    C.warnings.includes(`drop-source-stash-failed`) &&
      N.warning(`Move to worktree succeeded, but source stash cleanup failed`),
    C.warnings.includes(`drop-target-stash-failed`) &&
      N.warning(`Move to worktree succeeded, but target stash cleanup failed`));
  try {
    await xo({ sourceConversationId: e, targetConversationId: w });
  } catch (e) {
    N.warning(
      `Move to worktree succeeded, but pinned-thread state could not be transferred: {}`,
      { sensitive: { error: A(e) }, safe: {} },
    );
  }
  return (
    p?.(`switching-thread`, `done`),
    { status: `success`, conversationId: w }
  );
}
var Mo = e(() => {
  (M(), Ct(), Te(), nn(), re(), ce(), Wt(), tr(), $r(), So(), wo());
});
function No(e) {
  return (0, os.default)(
    [Je(e.get, `host_config`) ?? cs, ...e.get(cn).map(me)],
    ({ id: e }) => e,
  );
}
function Po(e = [cs], t = !1) {
  return {
    name: ds,
    description:
      `Move another Codex thread and its associated git state between its checkout and Codex worktree on its current host. Running threads are interrupted before handoff. Omit destinationHostId for this current-host toggle. The calling thread cannot move itself, and cloud handoff is not supported.` +
      (t
        ? ` You can also choose another host to move the thread to a matching saved-project worktree.`
        : ``) +
      ` Returns quickly with an operationId and revision. The UI continues to show live progress in the original handoff item. For model-visible completion, call get_handoff_status with afterRevision and a 30000-60000 waitMs, then back off if the revision does not change.`,
    inputSchema: {
      type: `object`,
      additionalProperties: !1,
      properties: {
        threadId: {
          type: `string`,
          description: `Other thread id to hand off.`,
        },
        ...(t
          ? {
              destinationHostId: {
                type: `string`,
                description: `Optional host that should run the thread after handoff. Omit to move between the source thread's checkout and Codex worktree on its current host. Choose another host to move to a matching saved-project worktree. Available hosts: ${e.map(({ display_name: e, id: t }) => `${e} (${t})`).join(`, `)}.`,
                enum: e.map(({ id: e }) => e),
              },
            }
          : {}),
        followUpPrompt: {
          type: `string`,
          description: `Optional prompt to send to the destination thread after handoff succeeds.`,
        },
      },
      required: [`threadId`],
    },
  };
}
function Fo() {
  return {
    name: fs,
    description: `Read status for a handoff_thread operation. The user-facing UI already updates in the original handoff item, so avoid frequent polling. Prefer afterRevision with a 30000-60000 waitMs so the call returns only when progress changes or the timeout expires. Poll once after dispatch, then wait longer/back off; do not repeatedly poll unchanged state or narrate unchanged polls.`,
    inputSchema: {
      type: `object`,
      additionalProperties: !1,
      properties: {
        operationId: {
          type: `string`,
          description: `operationId returned by handoff_thread.`,
        },
        afterRevision: {
          type: `number`,
          description: `Optional last revision already seen. When provided with waitMs, wait until the operation revision is greater than this value or the timeout expires.`,
        },
        waitMs: {
          type: `number`,
          description: `Optional maximum milliseconds to wait for a status change, from 0 to ${ss}.`,
        },
      },
      required: [`operationId`],
    },
  };
}
async function Io({
  scope: e,
  appServerRegistry: t,
  argumentsValue: n,
  callId: r,
  callingThreadId: i,
  crossHostHandoffEnabled: a = ut(e, `2256010998`),
  queryClient: o,
}) {
  let s = ls.safeParse(n);
  if (!s.success) return I(`${ds} received invalid arguments.`);
  let c = w(s.data.threadId);
  if (c === i)
    return I(`A thread cannot hand itself off. Choose another thread.`);
  try {
    let n = r,
      l = ao(e, n);
    if (l != null) return Xo(Zo(l));
    let u = No(e),
      d = await Ro({
        scope: e,
        appServerRegistry: t,
        operationId: n,
        hosts: u,
        queryClient: o,
        threadId: c,
      }),
      f =
        s.data.destinationHostId == null
          ? d.hostConfig
          : u.find(({ id: e }) => e === s.data.destinationHostId);
    if (f == null)
      throw Error(
        `Host ${s.data.destinationHostId} is not available for thread handoff.`,
      );
    if (f.id !== d.hostConfig.id && !a)
      throw Error(`Cross-host thread handoff is not enabled.`);
    await _o({ conversationId: c, sourceThreadRunning: tn(d.conversation) });
    let p = f.id === d.hostConfig.id ? await zo(d) : await Bo(d, f);
    return (
      oo(e, n, p.progress),
      Vo({
        callingThreadId: i,
        context: d,
        destinationHostId: p.destinationHostId,
        followUpPrompt: s.data.followUpPrompt,
        run: p.run,
      }),
      Xo(Zo(ao(e, n) ?? p.progress))
    );
  } catch (e) {
    return (
      N.warning(`Thread handoff dynamic tool failed: {}`, {
        sensitive: { error: A(e) },
        safe: { threadId: c },
      }),
      I(e instanceof Error ? e.message : `Thread handoff failed.`)
    );
  }
}
async function Lo({ scope: e, argumentsValue: t }) {
  let n = us.safeParse(t);
  if (!n.success) return I(`${fs} received invalid arguments.`);
  let r = await so({
    afterRevision: n.data.afterRevision ?? null,
    scope: e,
    operationId: n.data.operationId,
    waitMs: n.data.waitMs ?? 0,
  });
  return r == null
    ? I(
        `No thread handoff operation found for operationId ${n.data.operationId}.`,
      )
    : Xo(Zo(r));
}
async function Ro({
  scope: e,
  appServerRegistry: t,
  operationId: n,
  hosts: r,
  queryClient: i,
  threadId: a,
}) {
  let o = t.getMaybeForConversationId(a) ?? t.getDefault();
  await F(`maybe-resume-conversation`, {
    hostId: o.getHostId(),
    conversationId: a,
    model: null,
    serviceTier: null,
    reasoningEffort: null,
    workspaceRoots: [`/`],
    collaborationMode: null,
    showThreadGoalResumeConfirmation: !1,
  });
  let s = o.getConversation(a);
  if (s == null) throw Error(`Thread ${a} could not be loaded for handoff.`);
  if (
    e.get(fr, a) > 0 ||
    (De(e.get, ne.QUEUED_FOLLOW_UPS)?.[a]?.length ?? 0) > 0
  )
    throw Error(
      `Thread ${a} has pending composer state. Send or remove it before handing the thread off.`,
    );
  let c = s.cwd?.trim() ?? ``;
  if (c.length === 0)
    throw Error(`Thread ${a} does not have a workspace to hand off.`);
  let l = r.find(({ id: e }) => e === o.getHostId());
  if (l == null)
    throw Error(`The source host for thread ${a} is not available.`);
  let u = ue(c),
    d = await L(`git`).request({
      method: `stable-metadata`,
      params: { cwd: u, hostConfig: l, operationSource: V },
    });
  if (d == null)
    throw Error(`The source thread workspace is not a git repository.`);
  let { branch: f } = await L(`git`).request({
      method: `current-branch`,
      params: { root: d.root, hostConfig: l, operationSource: V },
    }),
    p = s.title?.trim() ?? ``;
  return {
    operationId: n,
    scope: e,
    queryClient: i,
    threadId: a,
    threadTitle: p.length > 0 ? p : a,
    conversation: s,
    manager: o,
    hostConfig: l,
    cwd: u,
    gitRoot: d.root,
    currentBranch: f,
  };
}
async function zo(e) {
  let { codexHome: t } = await k(`codex-home`, {
    params: { hostId: e.hostConfig.id },
  });
  if (pe(e.cwd, t)) {
    let n = await Wo(e, t),
      r = Ko(e, e.currentBranch);
    return {
      destinationHostId: e.hostConfig.id,
      progress: Jo(e, {
        destinationHostDisplayName: e.hostConfig.display_name,
        destinationHostId: e.hostConfig.id,
        destinationCwd: ue(n.workspaceRoot),
        direction: `to-local`,
        localBranch: r,
        sourceBranch: r,
        steps: Yo([
          `stash-source-changes`,
          `detach-worktree-branch`,
          `checkout-local-branch`,
          `apply-changes-to-local`,
          `switching-thread`,
        ]),
        worktreeBranch: null,
      }),
      run: () =>
        Oo({
          conversationId: e.threadId,
          currentBranch: r,
          cwd: e.cwd,
          localGitRoot: n.gitRoot,
          localWorkspaceRoot: n.workspaceRoot,
          worktreeRoot: e.gitRoot,
          branchCheckedOutElsewhere: !1,
          intl: e.scope.get(Dn),
          appServerManager: e.manager,
          transferBrowserState: qo,
          setDiffComments: (t) => tt(e.scope, `diff_comments`, t),
          hostConfig: e.hostConfig,
          operationId: e.operationId,
          onSwitchingThreadStart: () => B(e, `switching-thread`, `running`),
          onSwitchingThreadDone: () => B(e, `switching-thread`, `done`),
          onSwitchingThreadFailed: () => B(e, `switching-thread`, `failed`),
          onTargetConversationId: (t) => $o(e, t),
          onTargetCwd: (t) => Qo(e, t),
        }),
    };
  }
  let n = e.currentBranch?.trim() ?? ``;
  if (n.length === 0)
    throw Error(
      `The source checkout is detached. Check out a branch before handing it off to a worktree.`,
    );
  let { branch: r } = await L(`git`).request({
      method: `default-branch`,
      params: { root: e.gitRoot, hostConfig: e.hostConfig, operationSource: V },
    }),
    i = n === r ? Ko(e, null) : n,
    a = n === r ? null : await Go(e, n, r);
  return {
    destinationHostId: e.hostConfig.id,
    progress: Jo(e, {
      destinationHostDisplayName: e.hostConfig.display_name,
      destinationHostId: e.hostConfig.id,
      destinationCwd: null,
      direction: `to-worktree`,
      localBranch: a,
      sourceBranch: n,
      steps: Yo([
        `stash-source-changes`,
        `checkout-local-branch`,
        `stash-target-worktree-changes`,
        `checkout-worktree-branch`,
        `apply-changes-to-worktree`,
        `switching-thread`,
      ]),
      worktreeBranch: i,
    }),
    run: () =>
      jo({
        conversationId: e.threadId,
        currentBranch: n,
        cwd: e.cwd,
        worktreeCheckoutBranch: i,
        selectedLocalCheckoutBranch: a,
        intl: e.scope.get(Dn),
        appServerManager: e.manager,
        defaultBranch: r,
        transferBrowserState: qo,
        setDiffComments: (t) => tt(e.scope, `diff_comments`, t),
        hostConfig: e.hostConfig,
        queryClient: e.queryClient,
        operationId: e.operationId,
        onStepStatus: (t, n) => B(e, t, n),
        onTargetConversationId: (t) => $o(e, t),
        onTargetCwd: (t) => Qo(e, t),
      }),
  };
}
async function Bo(e, t) {
  if (e.conversation.rolloutPath.trim().length === 0)
    throw Error(`This chat does not have a rollout file to move`);
  let n = Ko(e, e.currentBranch),
    [{ roots: r }, { origins: i }] = await Promise.all([
      k(`workspace-root-options`, { params: { hostId: t.id } }),
      k(`git-origins`, {
        params: { hostId: e.hostConfig.id, dirs: [e.cwd] },
        source: V,
      }),
    ]),
    { origins: a } = await k(`git-origins`, {
      params: { hostId: t.id, dirs: r },
      source: V,
    }),
    o = hn({
      sourceWorkspaceRoot: e.cwd,
      sourceGitRoot: e.gitRoot,
      sourceGitOrigins: i,
      destinationWorkspaceRoots: r,
      destinationGitOrigins: a,
    });
  if (o == null)
    throw Error(`No matching saved project was found on ${t.display_name}.`);
  return {
    destinationHostId: t.id,
    progress: Jo(e, {
      destinationHostDisplayName: t.display_name,
      destinationHostId: t.id,
      destinationCwd: ue(o),
      direction: `to-host-worktree`,
      localBranch: null,
      sourceBranch: n,
      steps: Yo([
        `prepare-host-transfer`,
        `transfer-host-artifacts`,
        `apply-changes-to-worktree`,
        `switching-thread`,
      ]),
      worktreeBranch: null,
    }),
    run: () =>
      To({
        conversationId: e.threadId,
        sourceBranch: n,
        cwd: e.cwd,
        destinationWorkspaceRoot: ue(o),
        intl: e.scope.get(Dn),
        appServerManager: e.manager,
        sourceHostConfig: e.hostConfig,
        destinationHostConfig: t,
        transferBrowserState: qo,
        setDiffComments: (t) => tt(e.scope, `diff_comments`, t),
        queryClient: e.queryClient,
        operationId: e.operationId,
        onSwitchingThreadStart: () => B(e, `switching-thread`, `running`),
        onSwitchingThreadDone: () => B(e, `switching-thread`, `done`),
        onSwitchingThreadFailed: () => B(e, `switching-thread`, `failed`),
        onTargetConversationId: (t) => $o(e, t),
        onTargetCwd: (t) => Qo(e, t),
        onStepStatus: (t, n) => B(e, t, n),
      }),
  };
}
function Vo({
  callingThreadId: e,
  context: t,
  destinationHostId: n,
  followUpPrompt: r,
  run: i,
}) {
  Ho({
    callingThreadId: e,
    context: t,
    destinationHostId: n,
    followUpPrompt: r,
    run: i,
  });
}
async function Ho({
  callingThreadId: e,
  context: t,
  destinationHostId: n,
  followUpPrompt: r,
  run: i,
}) {
  let a = L(`git`).subscribe(`thread-handoff-progress`, (e) => {
    if (e.operationId !== t.operationId) return;
    let n;
    switch (e.status) {
      case `completed`:
      case `skipped`:
        n = `done`;
        break;
      case `started`:
        n = `running`;
        break;
      case `failed`:
        n = `failed`;
        break;
    }
    B(t, e.step, n);
  });
  try {
    es(t, `running`);
    let a = await i();
    if (a.status === `error`) {
      ts(t, a.message);
      return;
    }
    if (a.conversationId === t.threadId) {
      ns(
        t,
        `Git handoff completed, but the destination thread could not be created.`,
      );
      return;
    }
    ($o(t, a.conversationId),
      Uo(t),
      r != null &&
        (await Un({
          hostId: n,
          model: void 0,
          prompt: r,
          scope: t.scope,
          sourceThreadId: e,
          threadId: a.conversationId,
          thinking: void 0,
        })),
      es(t, `success`));
  } catch (e) {
    (N.warning(`Thread handoff failed unexpectedly: {}`, {
      sensitive: { error: A(e) },
      safe: { operationId: t.operationId },
    }),
      ts(t, e instanceof Error ? e.message : `Thread handoff failed.`));
  } finally {
    a();
  }
}
function Uo(e) {
  F(`archive-conversation`, {
    conversationId: e.threadId,
    cleanupWorktree: !1,
    source: `thread_handoff`,
  }).catch((t) => {
    N.warning(`Thread handoff succeeded, but source archive failed: {}`, {
      sensitive: { error: A(t) },
      safe: { threadId: e.threadId },
    });
  });
}
async function Wo(e, t) {
  let [{ worktrees: n }, { worktrees: r }] = await Promise.all([
      L(`git`).request({
        method: `list-worktrees`,
        params: { cwd: e.cwd, hostConfig: e.hostConfig, operationSource: V },
      }),
      L(`git`).request({
        method: `codex-worktrees`,
        params: { hostConfig: e.hostConfig, operationSource: V },
      }),
    ]),
    i = new Set(r.map(({ dir: e }) => Pe(e))),
    a = mo({
      cwd: e.cwd,
      sourceWorktreeRoot: ue(e.gitRoot),
      repoWorktreeEntries: n.filter(
        ({ root: e }) => !i.has(Pe(e)) && !pe(e, t),
      ),
    })[0];
  if (a == null) throw Error(`No checkout was found for the source worktree.`);
  return a;
}
async function Go(e, t, n) {
  let { branches: r } = await L(`git`).request({
      method: `recent-branches`,
      params: { root: e.gitRoot, hostConfig: e.hostConfig, operationSource: V },
    }),
    i = [n, ...r].find((e) => e != null && e !== t);
  if (i == null)
    throw Error(
      `No other checkout branch is available before handing this thread off to a worktree.`,
    );
  return i;
}
function Ko(e, t) {
  let n = t?.trim() ?? ``;
  if (n.length > 0) return n;
  let r = o(e.scope.get, ge.branchPrefix),
    i = Dr({ branchPrefix: r, conversationTitle: e.conversation.title });
  return i.length > 0 && !i.endsWith(`/`)
    ? i
    : Dr({ branchPrefix: r, conversationTitle: e.threadId });
}
function qo(e, t) {
  se.dispatchMessage(`browser-sidebar-command`, {
    conversationId: e,
    command: { type: `transfer-conversation`, targetConversationId: t },
  });
}
function Jo(
  e,
  {
    destinationHostDisplayName: t,
    destinationHostId: n,
    destinationCwd: r,
    direction: i,
    localBranch: a,
    sourceBranch: o,
    steps: s,
    worktreeBranch: c,
  },
) {
  return {
    destinationCwd: r,
    destinationHostId: n,
    destinationHostDisplayName: t,
    destinationThreadId: null,
    direction: i,
    errorCode: null,
    errorMessage: null,
    localBranch: a,
    operationId: e.operationId,
    retryable: null,
    revision: 0,
    sourceBranch: o,
    status: `queued`,
    steps: s,
    threadTitle: e.threadTitle,
    warningMessage: null,
    worktreeBranch: c,
  };
}
function Yo(e) {
  return e.map((e) => ({ id: e, status: `pending` }));
}
function Xo(e) {
  return {
    contentItems: [{ type: `inputText`, text: JSON.stringify(e) }],
    success: !0,
  };
}
function Zo(e) {
  return {
    destinationCwd: e.destinationCwd ?? void 0,
    destinationHostDisplayName: e.destinationHostDisplayName,
    destinationHostId: e.destinationHostId,
    operationId: e.operationId,
    revision: e.revision,
    status: e.status,
    steps: e.steps,
    threadTitle: e.threadTitle,
    destinationThreadId: e.destinationThreadId ?? void 0,
    errorCode: e.errorCode ?? void 0,
    errorMessage: e.errorMessage ?? void 0,
    retryable: e.retryable ?? void 0,
    warningMessage: e.warningMessage ?? void 0,
  };
}
function B(e, t, n) {
  oo(e.scope, e.operationId, (e) => {
    if (e == null) return null;
    let r = { id: t, status: n },
      i = e.steps.find((e) => e.id === t);
    if (i?.status === n) return e;
    let a =
      i == null
        ? [...e.steps, r].sort(as)
        : e.steps.map((e) => (e.id === t ? r : e));
    return { ...e, steps: a };
  });
}
function Qo(e, t) {
  oo(e.scope, e.operationId, (e) =>
    e == null || e.destinationCwd === t ? e : { ...e, destinationCwd: t },
  );
}
function $o(e, t) {
  oo(e.scope, e.operationId, (e) =>
    e == null || e.destinationThreadId === t
      ? e
      : { ...e, destinationThreadId: t },
  );
}
function es(e, t) {
  oo(e.scope, e.operationId, (e) =>
    e == null || e.status === t ? e : { ...e, status: t },
  );
}
function ts(e, t) {
  oo(e.scope, e.operationId, (e) => rs(e, t));
}
function ns(e, t) {
  oo(e.scope, e.operationId, (e) =>
    e == null || (e.status === `warning` && e.warningMessage === t)
      ? e
      : { ...e, status: `warning`, warningMessage: t },
  );
}
function rs(e, t) {
  if (e == null) return null;
  let n = is(e.steps);
  return e.status === `error` &&
    e.errorCode === `handoff_failed` &&
    e.errorMessage === t &&
    e.retryable === !0 &&
    n === e.steps
    ? e
    : {
        ...e,
        errorCode: `handoff_failed`,
        errorMessage: t,
        retryable: !0,
        status: `error`,
        steps: n,
      };
}
function is(e) {
  if (e.some((e) => e.status === `failed`)) return e;
  let t = e.findIndex((e) => e.status === `running`),
    n = e.findIndex((e) => e.status === `pending`),
    r = t === -1 ? n : t;
  return r === -1
    ? e
    : e.map((e, t) => (t === r ? { ...e, status: `failed` } : e));
}
function as(e, t) {
  return yo.indexOf(e.id) - yo.indexOf(t.id);
}
var os,
  V,
  ss,
  cs,
  ls,
  us,
  ds,
  fs,
  ps = e(() => {
    ((os = t(Ze(), 1)),
      M(),
      S(),
      Wn(),
      Ot(),
      Te(),
      po(),
      Qt(),
      pr(),
      Oe(),
      kn(),
      Er(),
      go(),
      vo(),
      bo(),
      Do(),
      Ao(),
      Mo(),
      le(),
      mn(),
      ln(),
      s(),
      Ye(),
      Qe(),
      lt(),
      re(),
      Me(),
      ce(),
      Wt(),
      (V = `local_remote_dropdown`),
      (ss = 6e4),
      (cs = { id: Xe, display_name: `Local`, kind: `local` }),
      (ls = E({
        threadId: O().min(1),
        destinationHostId: O().min(1).optional(),
        followUpPrompt: O().trim().min(1).optional(),
      })),
      (us = E({
        operationId: O().min(1),
        afterRevision: fe().int().min(0).optional(),
        waitMs: fe().int().min(0).max(ss).optional(),
      })),
      (ds = `handoff_thread`),
      (fs = `get_handoff_status`));
  }),
  ms,
  hs,
  gs,
  _s,
  vs,
  ys,
  bs,
  xs,
  Ss,
  Cs = e(() => {
    ((ms = `list_projects`),
      (hs = `create_thread`),
      (gs = `send_message_to_thread`),
      (_s = [
        `none`,
        `minimal`,
        `low`,
        `medium`,
        `high`,
        `xhigh`,
        `max`,
        `ultra`,
      ]),
      (vs = `Do not specify a model unless the user explicitly requests a specific model. Otherwise omit this field so the new thread uses the user's configured default model.`),
      (ys = {
        description: `Where the project thread should run: directly in the saved project or in a new worktree.`,
        anyOf: [
          {
            type: `object`,
            additionalProperties: !1,
            properties: { type: { type: `string`, enum: [`local`] } },
            required: [`type`],
          },
          {
            type: `object`,
            additionalProperties: !1,
            properties: {
              type: { type: `string`, enum: [`worktree`] },
              startingState: {
                description: `Only specify this when the user explicitly asks to start from a particular existing git state. Use working-tree to include the current checkout and uncommitted changes. Use branch only for a branch or ref that already exists. Otherwise omit this field so the worktree starts from the project's default branch. Do not use this to name a new branch.`,
                anyOf: [
                  {
                    type: `object`,
                    additionalProperties: !1,
                    properties: {
                      type: { type: `string`, enum: [`working-tree`] },
                    },
                    required: [`type`],
                  },
                  {
                    type: `object`,
                    additionalProperties: !1,
                    properties: {
                      type: { type: `string`, enum: [`branch`] },
                      branchName: { type: `string` },
                    },
                    required: [`type`, `branchName`],
                  },
                ],
              },
            },
            required: [`type`],
          },
        ],
      }),
      (bs = {
        name: ms,
        description: `List local and remote projects available for background thread creation. Use a returned projectId with create_thread.`,
        inputSchema: {
          type: `object`,
          additionalProperties: !1,
          properties: {},
        },
      }),
      (xs = {
        name: hs,
        description: `Create a separate Codex thread only when the user explicitly asks for a new or background thread. Use list_projects first, then pass its projectId for repo-scoped work in any local or remote project. Use projectless targets for general tasks. Project targets must choose a local or worktree environment. Direct creation is non-blocking and returns threadId and hostId for wait_threads or set_thread_archived; queued worktrees return clientThreadId and are not waitable until a threadId exists.`,
        inputSchema: {
          type: `object`,
          additionalProperties: !1,
          properties: {
            prompt: {
              type: `string`,
              description: `Initial prompt for the new thread.`,
            },
            target: {
              description: `Where to create the thread.`,
              anyOf: [
                {
                  type: `object`,
                  additionalProperties: !1,
                  properties: {
                    type: { type: `string`, enum: [`project`] },
                    projectId: {
                      type: `string`,
                      description: `Project id returned by list_projects.`,
                    },
                    environment: ys,
                  },
                  required: [`type`, `projectId`, `environment`],
                },
                {
                  type: `object`,
                  additionalProperties: !1,
                  properties: {
                    type: { type: `string`, enum: [`projectless`] },
                    directoryName: {
                      type: `string`,
                      description: `Optional projectless output directory name.`,
                    },
                  },
                  required: [`type`],
                },
              ],
            },
            model: { type: `string`, description: vs },
            thinking: {
              type: `string`,
              description: `Optional reasoning effort override. Must be supported by the selected model.`,
              enum: [..._s],
            },
          },
          required: [`prompt`, `target`],
        },
      }),
      (Ss = {
        name: gs,
        description: `Send a follow-up prompt to an existing Codex thread in the background. Omit model and thinking to keep the thread's current settings.`,
        inputSchema: {
          type: `object`,
          additionalProperties: !1,
          properties: {
            threadId: { type: `string`, description: `Thread id to continue.` },
            hostId: {
              type: `string`,
              description: `Optional host id returned by create_thread or list_threads.`,
            },
            prompt: {
              type: `string`,
              description: `Follow-up prompt to send.`,
            },
            model: { type: `string`, description: `Optional model override.` },
            thinking: {
              type: `string`,
              description: `Optional reasoning effort override. Must be supported by the selected model.`,
              enum: [..._s],
            },
          },
          required: [`threadId`, `prompt`],
        },
      }));
  });
function ws({
  availableHandoffHosts: e,
  availableModels: t = [],
  crossHostHandoffEnabled: n = !1,
  deferLoading: r = !1,
  forkThreadEnabled: i = !0,
} = {}) {
  return [
    ...(i ? [pc] : []),
    Po(e, n),
    Fo(),
    bs,
    Rs(xs, t),
    mc,
    hc,
    gc,
    Rs(Ss, t),
    _c,
    vc,
    yc,
  ].map((e) => (r ? { ...e, deferLoading: !0 } : e));
}
async function Ts({ scope: e, argumentsValue: t, sourceThreadId: n }) {
  if (n == null) return I(`${Ys} missing calling thread id.`);
  let r = rc.safeParse(t);
  if (!r.success) return I(`${Ys} ${U}`);
  try {
    return H(
      await ua({
        scope: e,
        sourceThreadId: n,
        targetThreadId: r.data.threadId,
        environment: r.data.environment ?? { type: `same-directory` },
      }),
    );
  } catch (e) {
    return I(e instanceof Error ? e.message : String(e));
  }
}
async function Es({
  argumentsValue: e,
  getAvailableModels: t,
  scope: n,
  sourceThreadId: r,
}) {
  let i = nc.safeParse(e);
  if (!i.success) return I(`${hs} ${U}`);
  try {
    let e = Fs(n, i.data.target),
      a = e.type === `remoteProject` ? e.hostId : Xe;
    if (i.data.model != null && i.data.thinking != null) {
      let e = t == null ? [] : await t(a),
        n = Ls(i.data.model, i.data.thinking, e);
      if (n != null) return I(n);
    }
    return H({
      ...(await Nn({
        model: i.data.model,
        prompt: i.data.prompt,
        scope: n,
        sourceThreadId: r,
        target: e,
        thinking: i.data.thinking,
      })),
      hostId: a,
    });
  } catch (e) {
    return I(e instanceof Error ? e.message : String(e));
  }
}
async function Ds({ scope: e, argumentsValue: t }) {
  if (!ic.safeParse(t).success) return I(`${ms} ${U}`);
  try {
    return H(await sa({ scope: e }));
  } catch (e) {
    return I(e instanceof Error ? e.message : String(e));
  }
}
async function Os({ scope: e, argumentsValue: t }) {
  let n = ac.safeParse(t);
  if (!n.success) return I(`${Xs} ${U}`);
  try {
    return H(
      await Ca({ scope: e, limit: n.data.limit ?? zs, query: n.data.query }),
    );
  } catch (e) {
    return I(e instanceof Error ? e.message : String(e));
  }
}
async function ks({ scope: e, argumentsValue: t, sourceHostId: n }) {
  let r = oc.safeParse(t);
  if (!r.success) return I(`${Zs} ${U}`);
  try {
    return H(
      await Gr({
        scope: e,
        cursor: r.data.cursor,
        hostId: r.data.hostId ?? n,
        includeOutputs: r.data.includeOutputs ?? !1,
        maxOutputCharsPerItem: r.data.maxOutputCharsPerItem ?? Vs,
        threadId: r.data.threadId,
        turnLimit: r.data.turnLimit ?? Bs,
      }),
    );
  } catch (e) {
    return I(e instanceof Error ? e.message : String(e));
  }
}
async function As({
  argumentsValue: e,
  scope: t,
  sourceHostId: n,
  sourceThreadId: r,
}) {
  let i = lc.safeParse(e);
  if (!i.success) return I(`${gs} ${U}`);
  try {
    return H(
      await Un({
        hostId: i.data.hostId,
        model: i.data.model,
        preferredHostId: n,
        prompt: i.data.prompt,
        scope: t,
        sourceThreadId: r,
        threadId: i.data.threadId,
        thinking: i.data.thinking,
      }),
    );
  } catch (e) {
    return I(e instanceof Error ? e.message : String(e));
  }
}
async function js({
  scope: e,
  argumentsValue: t,
  signal: n,
  sourceHostId: r,
  sourceThreadId: i,
}) {
  let a = cc.safeParse(t);
  if (!a.success) return Is(Qs, a.error);
  let o = a.data.targets.map((e) => ({ ...e, hostId: e.hostId ?? r }));
  if (o.some((e) => e.threadId === i))
    return I(`${Qs} cannot wait on the calling thread.`);
  if (new Set(o.map((e) => e.threadId)).size !== o.length)
    return I(`${Qs} received duplicate targets. Each threadId must be unique.`);
  try {
    return H(
      await La({
        scope: e,
        targets: o,
        signal: n,
        timeoutMs: a.data.timeoutMs ?? Hs,
      }),
    );
  } catch (e) {
    return I(e instanceof Error ? e.message : String(e));
  }
}
async function Ms({ argumentsValue: e, scope: t }) {
  let n = uc.safeParse(e);
  if (!n.success) return I(`${$s} ${U}`);
  try {
    return H(await qn({ ...n.data, scope: t }));
  } catch (e) {
    return I(e instanceof Error ? e.message : String(e));
  }
}
async function Ns({
  argumentsValue: e,
  sourceHostId: t,
  sourceThreadId: n,
  scope: r,
}) {
  let i = dc.safeParse(e);
  if (!i.success) return I(`${ec} ${U}`);
  let a = i.data.threadId ?? n;
  if (a == null) return I(`${ec} missing calling thread id.`);
  try {
    return H(
      await Zn({
        ...i.data,
        hostId: i.data.hostId ?? (a === n ? t : void 0),
        threadId: a,
        scope: r,
      }),
    );
  } catch (e) {
    return I(e instanceof Error ? e.message : String(e));
  }
}
async function Ps({ argumentsValue: e, scope: t, sourceThreadId: n }) {
  let r = fc.safeParse(e);
  if (!r.success) return I(`${tc} ${U}`);
  let i = r.data.threadId ?? n;
  try {
    return H(await Xn({ ...r.data, threadId: i, scope: t }));
  } catch (e) {
    return I(e instanceof Error ? e.message : String(e));
  }
}
function Fs(e, t) {
  if (t.type === `projectless`) return t;
  let n = ca(e).find((e) => e.projectId === t.projectId);
  if (n == null)
    throw Error(
      `Unknown projectId: ${t.projectId}. Call list_projects to find available projects.`,
    );
  return n.projectKind === `remote`
    ? {
        type: `remoteProject`,
        projectId: n.projectId,
        hostId: n.hostId,
        path: n.path,
        environment: t.environment,
      }
    : { type: `project`, projectId: n.projectId, environment: t.environment };
}
function H(e) {
  return {
    contentItems: [{ type: `inputText`, text: JSON.stringify(e ?? null) }],
    success: !0,
  };
}
function Is(e, t) {
  return I(
    `${e} received invalid arguments: ${t.issues.map((e) => `${e.path.join(`.`) || `arguments`}: ${e.message}`).join(`; `)}.`,
  );
}
function Ls(e, t, n) {
  let r = n.find((t) => t.model === e);
  if (r == null)
    return `${hs} could not validate reasoning effort "${t}" for model "${e}". Use a model and reasoning combination listed in the tool description, or omit thinking.`;
  let i = r.supportedReasoningEfforts.map(({ reasoningEffort: e }) => e);
  if (i.includes(t)) return null;
  let a =
    i.length === 0
      ? `This model supports no reasoning effort overrides.`
      : `Supported reasoning efforts: ${i.join(`, `)}.`;
  return `${hs} rejected unsupported model/reasoning combination: "${r.model}" does not support "${t}". ${a}`;
}
function Rs(e, t) {
  if (t.length === 0) return e;
  let n = t
    .map((e) => {
      let t = e.supportedReasoningEfforts
          .map(({ reasoningEffort: e }) => e)
          .join(`, `),
        n = t
          ? `supported reasoning efforts: ${t}`
          : `no reasoning effort overrides`;
      return e.description.trim().length === 0
        ? `${e.model} (${n})`
        : `${e.model} (${e.description.trim()}; ${n})`;
    })
    .join(`, `);
  return {
    ...e,
    inputSchema: {
      ...e.inputSchema,
      properties: {
        ...e.inputSchema.properties,
        model: {
          type: `string`,
          description: `${e.inputSchema.properties.model.description} Models and supported reasoning efforts on the calling host: ${n}. A different destination host's combinations are validated when the tool runs. You may supply a newer model id when explicitly requested, but omit thinking unless its supported reasoning efforts are listed here.`,
        },
      },
    },
  };
}
var U,
  zs,
  Bs,
  Vs,
  Hs,
  Us,
  Ws,
  Gs,
  Ks,
  qs,
  Js,
  Ys,
  Xs,
  Zs,
  Qs,
  $s,
  ec,
  tc,
  nc,
  rc,
  ic,
  ac,
  oc,
  sc,
  cc,
  lc,
  uc,
  dc,
  fc,
  pc,
  mc,
  hc,
  gc,
  _c,
  vc,
  yc,
  bc = e(() => {
    (S(),
      la(),
      In(),
      pa(),
      Pa(),
      Jn(),
      io(),
      Zr(),
      Wn(),
      Ot(),
      ps(),
      Qe(),
      Cs(),
      (U = `received invalid arguments.`),
      (zs = 10),
      (Bs = 1),
      (Vs = 2e3),
      (Hs = 12e4),
      (Us = 8),
      (Ws = v(_s)),
      (Gs = T(`type`, [
        D({ type: j(`working-tree`) }),
        D({ type: j(`branch`), branchName: O().min(1) }),
      ])),
      (Ks = T(`type`, [
        D({ type: j(`local`) }),
        D({ type: j(`worktree`), startingState: Gs.optional() }),
      ])),
      (qs = T(`type`, [
        D({ type: j(`project`), projectId: O().min(1), environment: Ks }),
        D({ type: j(`projectless`), directoryName: O().min(1).optional() }),
      ])),
      (Js = T(`type`, [
        E({ type: j(`same-directory`) }),
        E({ type: j(`worktree`) }),
      ])),
      (Ys = `fork_thread`),
      (Xs = `list_threads`),
      (Zs = `read_thread`),
      (Qs = `wait_threads`),
      ($s = `set_thread_pinned`),
      (ec = `set_thread_archived`),
      (tc = `set_thread_title`),
      (nc = E({
        prompt: O().min(1),
        target: qs,
        model: O().min(1).optional(),
        thinking: Ws.optional(),
      })),
      (rc = E({ threadId: O().min(1).optional(), environment: Js.optional() })),
      (ic = E({})),
      (ac = E({
        query: O().optional(),
        limit: fe().int().min(1).max(50).optional(),
      })),
      (oc = E({
        threadId: O().min(1),
        hostId: O().min(1).optional(),
        cursor: O().min(1).optional(),
        turnLimit: fe().int().min(1).max(10).optional(),
        includeOutputs: y().optional(),
        maxOutputCharsPerItem: fe().int().min(0).max(2e4).optional(),
      })),
      (sc = E({
        threadId: O().min(1),
        hostId: O().min(1).optional(),
        afterCursor: O().min(1).optional(),
      })),
      (cc = E({
        targets: b(sc).min(1).max(Us),
        timeoutMs: fe().int().min(0).max(Hs).optional(),
      })),
      (lc = E({
        threadId: O().min(1),
        hostId: O().min(1).optional(),
        prompt: O().min(1),
        model: O().min(1).optional(),
        thinking: Ws.optional(),
      })),
      (uc = E({ threadId: O().min(1), pinned: y() })),
      (dc = E({
        threadId: O().min(1).optional(),
        hostId: O().min(1).optional(),
        archived: y(),
      })),
      (fc = E({ threadId: O().min(1).optional(), title: O().min(1) })),
      (pc = {
        name: Ys,
        description: `Fork a Codex thread. Omit threadId to fork the calling thread, or pass a threadId to fork that specific thread. A same-directory fork returns a child threadId immediately; a worktree fork returns a clientThreadId while worktree setup creates the child. Forks contain completed history only: if the source thread is running, the active turn and unfinished response are not copied. Send a follow-up message to the child only if the task requires work to continue there.`,
        inputSchema: {
          type: `object`,
          additionalProperties: !1,
          properties: {
            threadId: {
              type: `string`,
              description: `Optional source thread id to fork. Omit to fork the calling thread.`,
            },
            environment: {
              description: `Where the fork should run. Omit for a same-directory fork.`,
              anyOf: [
                {
                  type: `object`,
                  additionalProperties: !1,
                  properties: {
                    type: { type: `string`, enum: [`same-directory`] },
                  },
                  required: [`type`],
                },
                {
                  type: `object`,
                  additionalProperties: !1,
                  properties: { type: { type: `string`, enum: [`worktree`] } },
                  required: [`type`],
                },
              ],
            },
          },
        },
      }),
      (mc = {
        name: Xs,
        description: `List Codex threads across the local host and connected remote hosts. Usually omit query: the much faster unfiltered call returns recent threads with descriptions and is sufficient for normal discovery. Only pass query when you need to search older threads for a specific substring; querying can make this call significantly slower.`,
        inputSchema: {
          type: `object`,
          additionalProperties: !1,
          properties: {
            query: {
              type: `string`,
              description: `Specific substring to search for in older threads. Omit for normal recent-thread discovery because querying can make this call significantly slower.`,
            },
            limit: {
              type: `number`,
              description: `Maximum number of thread summaries to return.`,
            },
          },
        },
      }),
      (hc = {
        name: Zs,
        description: `Read recent status and turn summaries for one Codex thread without opening it. Use page cursors from earlier responses to read older turns.`,
        inputSchema: {
          type: `object`,
          additionalProperties: !1,
          properties: {
            threadId: { type: `string`, description: `Thread id to inspect.` },
            hostId: {
              type: `string`,
              description: `Optional host id returned by create_thread or list_threads.`,
            },
            cursor: {
              type: `string`,
              description: `Optional cursor for older turns.`,
            },
            turnLimit: {
              type: `number`,
              description: `Maximum number of turns to return.`,
            },
            includeOutputs: {
              type: `boolean`,
              description: `Whether to include truncated tool or command outputs.`,
            },
            maxOutputCharsPerItem: {
              type: `number`,
              description: `Maximum output characters to keep for each included output item.`,
            },
          },
          required: [`threadId`],
        },
      }),
      (gc = {
        name: Qs,
        description: `Wait for the first of up to eight Codex threads to complete or need attention. Use timeoutMs: 0 for an immediate snapshot. Commentary never wakes the wait. An up-to-date cursor omits previously delivered final text; a timeout includes compact progress for all targets. Per-target failures are returned in errors.`,
        inputSchema: {
          type: `object`,
          additionalProperties: !1,
          properties: {
            targets: {
              type: `array`,
              minItems: 1,
              maxItems: Us,
              description: `Threads to wait for. The first target that completes or needs attention wins.`,
              items: {
                type: `object`,
                additionalProperties: !1,
                properties: {
                  threadId: {
                    type: `string`,
                    minLength: 1,
                    description: `Thread id to wait for.`,
                  },
                  hostId: {
                    type: `string`,
                    minLength: 1,
                    description: `Optional host id returned by create_thread or list_threads.`,
                  },
                  afterCursor: {
                    type: `string`,
                    minLength: 1,
                    description: `Optional cursor returned by an earlier wait.`,
                  },
                },
                required: [`threadId`],
              },
            },
            timeoutMs: {
              type: `integer`,
              minimum: 0,
              maximum: Hs,
              description: `Maximum event-wait time in milliseconds. A bounded snapshot fetch for fresh progress may add latency. Defaults to 120000.`,
            },
          },
          required: [`targets`],
        },
      }),
      (_c = {
        name: $s,
        description: `Pin or unpin a Codex thread in the background.`,
        inputSchema: {
          type: `object`,
          additionalProperties: !1,
          properties: {
            threadId: {
              type: `string`,
              description: `Thread id to pin or unpin.`,
            },
            pinned: {
              type: `boolean`,
              description: `Whether the thread should be pinned.`,
            },
          },
          required: [`threadId`, `pinned`],
        },
      }),
      (vc = {
        name: ec,
        description: `Archive or unarchive a Codex thread in the background.`,
        inputSchema: {
          type: `object`,
          additionalProperties: !1,
          properties: {
            threadId: {
              type: `string`,
              minLength: 1,
              description: `Thread id to archive or unarchive. Omit to target the calling thread.`,
            },
            hostId: {
              type: `string`,
              minLength: 1,
              description: `Optional host id returned by create_thread, list_threads, or wait_threads.`,
            },
            archived: {
              type: `boolean`,
              description: `Whether the thread should be archived.`,
            },
          },
          required: [`archived`],
        },
      }),
      (yc = {
        name: tc,
        description: `Rename a Codex thread in the background.`,
        inputSchema: {
          type: `object`,
          additionalProperties: !1,
          properties: {
            threadId: {
              type: `string`,
              description: `Thread id to rename. Omit to target the calling thread.`,
            },
            title: { type: `string`, description: `New thread title.` },
          },
          required: [`title`],
        },
      }));
  });
async function xc({
  availableHandoffHosts: e,
  appshotsAvailable: t = !1,
  appshotsEnabled: n = !1,
  authMethod: r,
  crossHostHandoffEnabled: i = !1,
  voiceCallEndEnabled: a = !1,
  enabledReasoningEfforts: o,
  featureOverrides: s,
  hostId: c,
  includeUltraReasoningEffort: l,
  isAuthLoading: u,
  listExperimentalFeatures: d,
  listModels: f,
  modelAvailabilityConfig: p,
  queryClient: m,
  navigateToCodexPageEnabled: h = !0,
  speakToUserEnabled: g = !1,
  speakToUserToolDescription: _,
  mode: v = `default`,
  supportsDynamicToolNamespaces: y = !0,
  threadStartKind: b = `default`,
}) {
  let x = Rr(c),
    S = s?.[bt] === !0,
    C = s?.[Ft] === !0,
    w = s?.[ot] === !0,
    T = Pi({ hostId: c, queryClient: m }),
    [ee, te] = await Promise.all([
      x ? Cc(d) : !1,
      S
        ? Sc({
            authMethod: r,
            enabledReasoningEfforts: o,
            includeUltraReasoningEffort: l,
            isAuthLoading: u,
            listModels: f,
            modelAvailabilityConfig: p,
          })
        : [],
    ]),
    ne = [
      ...(x ? [Nt()] : []),
      ...(s?.open_in_codex === !0 ? [Mi] : []),
      ...(x && h ? [Oi] : []),
      St,
      ...(x && ee ? [Zt] : []),
      ...(S
        ? ws({
            availableHandoffHosts: e,
            availableModels: te,
            crossHostHandoffEnabled: i,
            forkThreadEnabled: !0,
          }).filter((e) => !0)
        : []),
      ...(x && C ? [ia, aa] : []),
      ...(b === `conversational_onboarding` ? [zn] : []),
      ...(w && b !== `conversational_onboarding` ? [...Ke, Ue] : []),
    ].map((e) => ({
      type: `function`,
      ...e,
      ...(Tc.has(e.name) ? {} : { deferLoading: !0 }),
    }));
  return y
    ? [
        {
          type: `namespace`,
          name: W,
          description: `Tools provided by the Codex app.`,
          tools: ne,
        },
        ...T,
      ]
    : ne;
}
async function Sc({
  authMethod: e,
  enabledReasoningEfforts: t,
  includeUltraReasoningEffort: n,
  isAuthLoading: r,
  listModels: i,
  modelAvailabilityConfig: a,
}) {
  if (r) return [];
  try {
    let r = await Zi(i),
      { models: o } = ft({
        authMethod: e,
        availableModels: a.availableModels,
        defaultModel: a.defaultModel,
        enabledReasoningEfforts: t,
        includeUltraReasoningEffort: n,
        models: r,
        useHiddenModels: a.useHiddenModels,
      });
    return o.map((e) => ({
      ...e,
      supportedReasoningEfforts:
        r.find((t) => t.model === e.model)?.supportedReasoningEfforts ??
        e.supportedReasoningEfforts,
    }));
  } catch {
    return [];
  }
}
async function Cc(e, t = null) {
  try {
    let n = await e({ cursor: t, limit: wc });
    return or(n.data)
      ? !0
      : n.nextCursor == null
        ? !1
        : await Cc(e, n.nextCursor);
  } catch {
    return !1;
  }
}
var wc,
  W,
  Tc,
  Ec = e(() => {
    (_t(),
      ki(),
      Pn(),
      Be(),
      Ni(),
      Xi(),
      Et(),
      ta(),
      Gt(),
      ar(),
      oa(),
      bc(),
      Lr(),
      dt(),
      Lt(),
      (wc = 100),
      (W = `codex_app`),
      (Tc = new Set([St.name, Zt.name, Ei, He, Fe, at, Ie, Rn])));
  });
function Dc(e) {
  throw Error(`Unexpected value: ${String(e)}`);
}
var Oc = e(() => {});
function kc({ item: e, mcpServerStatuses: t }) {
  if (e.isMcpAppWidgetSuperseded === !0) return `not-mcp-app`;
  let n = e.result?.type === `success` ? e.result.raw : null;
  return e.mcpAppResourceUri != null ||
    Sr({
      mcpServerStatuses: t,
      server: e.invocation.server,
      tool: e.invocation.tool,
      toolResult: n,
    }) != null
    ? `mcp-app`
    : n == null
      ? `not-mcp-app`
      : t == null
        ? `maybe-mcp-app`
        : `not-mcp-app`;
}
function Ac({ item: e, mcpServerStatuses: t }) {
  return kc({ item: e, mcpServerStatuses: t }) === `mcp-app`;
}
function jc({ item: e, mcpServerStatuses: t }) {
  return kc({ item: e, mcpServerStatuses: t }) !== `not-mcp-app`;
}
function Mc(e) {
  return e.some((e) =>
    e.kind !== `item` || e.item.type !== `mcp-tool-call`
      ? !1
      : e.item.result?.type === `success` ||
        (!e.item.completed &&
          e.item.result == null &&
          e.item.mcpAppResourceUri == null),
  );
}
var Nc = e(() => {
  Cr();
});
function Pc(
  e,
  {
    mcpServerStatuses: t,
    resolvedApps: n = [],
    shouldKeepMcpAppToolCallsExpandedByDefault: r,
    modelProvider: i = null,
    isTurnCancelled: a = !1,
  } = {},
) {
  return e.kind === `multi-agent-group`
    ? { type: `other` }
    : e.kind === `web-search-group`
      ? {
          type: `web-search`,
          count: e.items.length,
          runningCount: e.items.filter((e) => !e.completed).length,
        }
      : e.entry.kind === `exploration`
        ? Bc(e.entry)
        : Fc(e.entry.item, {
            mcpServerStatuses: t,
            resolvedApps: n,
            shouldKeepMcpAppToolCallsExpandedByDefault: r,
            modelProvider: i,
            isTurnCancelled: a,
          });
}
function Fc(
  e,
  {
    mcpServerStatuses: t,
    resolvedApps: n = [],
    shouldKeepMcpAppToolCallsExpandedByDefault: r,
    modelProvider: i = null,
    isTurnCancelled: a = !1,
  } = {},
) {
  if (e.type === `assistant-message`) return { type: `assistant-message` };
  if (e.type === `patch`)
    return { ...Vc(e, a), ...zc(e.automaticApprovalReviews) };
  if (e.type === `exec`) {
    if (
      Ic(e, i) ||
      (e.parsedCmd.type === `read` &&
        !e.parsedCmd.isFinished &&
        _i({ summary: e.parsedCmd, cwd: e.cwd }))
    )
      return { type: `other` };
    let t = !e.parsedCmd.isFinished,
      n = e.parsedCmd.type === `unknown` ? e.parsedCmd.cmd : null,
      r = n != null && /^\s*mkdir(?:\s|$)/.test(n),
      a = n != null && ti(e.executionStatus) && ei(n),
      o = n != null && Lc(n),
      s = zc(e.automaticApprovalReviews);
    return a
      ? { type: `exec`, isInProgress: t, visualizationActivityKind: a, ...s }
      : r
        ? { type: `exec`, isInProgress: t, createsFolder: !0, ...s }
        : o && (t || e.output?.exitCode === 0)
          ? { type: `exec`, isInProgress: t, searchesWeb: !0, ...s }
          : { type: `exec`, isInProgress: t, ...s };
  }
  if (e.type === `mcp-tool-call`)
    return Ac({ item: e, mcpServerStatuses: t }) ||
      (r === !0 && jc({ item: e, mcpServerStatuses: t }))
      ? { type: `other` }
      : {
          type: `mcp-tool-call`,
          isInProgress: !e.completed,
          source: Mt({
            functionName: e.functionName,
            invocation: e.invocation,
            resolvedApps: n,
            source: e.source,
          }),
          ...zc(e.automaticApprovalReviews),
        };
  if (e.type === `automatic-approval-review`)
    switch (e.status) {
      case `denied`:
      case `timedOut`:
        return {
          type: `automatic-approval-review`,
          id: e.id,
          status: e.status,
        };
      case `aborted`:
      case `approved`:
      case `inProgress`:
        return { type: `other` };
    }
  return e.type === `web-search`
    ? { type: `web-search`, count: 1, runningCount: +!e.completed }
    : e.type === `reasoning` ||
        e.type === `todo-list` ||
        e.type === `worked-for` ||
        e.type === `multi-agent-action` ||
        e.type === `proposed-plan` ||
        e.type === `realtime-transcript` ||
        e.type === `user-message` ||
        e.type === `userInput` ||
        e.type === `user-input-response` ||
        e.type === `mcp-server-elicitation` ||
        e.type === `permission-request` ||
        e.type === `turn-diff` ||
        e.type === `plan-implementation` ||
        e.type === `system-error` ||
        e.type === `steered` ||
        e.type === `stream-error` ||
        e.type === `subagent-activity` ||
        e.type === `remote-task-created` ||
        e.type === `context-compaction` ||
        e.type === `personality-changed` ||
        e.type === `forked-from-conversation` ||
        e.type === `model-changed` ||
        e.type === `model-rerouted` ||
        e.type === `auto-review-interruption-warning` ||
        e.type === `generated-image` ||
        e.type === `image-view` ||
        e.type === `automation-update` ||
        e.type === `dynamic-tool-call` ||
        e.type === `worktree-init`
      ? { type: `other` }
      : Dc(e);
}
function Ic(e, t) {
  return !1;
}
function Lc(e) {
  if (!/^\s*curl(?:\s|$)/.test(e) || Wc.test(e) || Gc.test(e) || Kc.test(e))
    return !1;
  let t = e.match(/\bhttps?:\/\/[^\s'"<>]+/gi);
  return t == null ? !1 : t.some(Rc);
}
function Rc(e) {
  try {
    let t = new URL(e).hostname.toLowerCase();
    return t !== `localhost` && !t.startsWith(`127.`);
  } catch {
    return !1;
  }
}
function zc(e) {
  let t = [];
  for (let n of e ?? [])
    switch (n.status) {
      case `denied`:
      case `timedOut`:
        t.push({ id: n.id, status: n.status });
        break;
      case `aborted`:
      case `approved`:
      case `inProgress`:
        break;
    }
  return t.length === 0 ? {} : { automaticApprovalReviewFailures: t };
}
function Bc(e) {
  let t = [],
    n = [],
    r = [],
    i = [],
    a = [],
    o = 0,
    s = 0,
    c = 0,
    l = 0;
  for (let u of e.items)
    if (u.type === `exec`) {
      if (
        (t.push(...(u.automaticApprovalReviews ?? [])),
        u.parsedCmd.type === `search`)
      ) {
        ((o += 1), u.parsedCmd.isFinished || (s += 1));
        continue;
      }
      if (u.parsedCmd.type === `list_files`) {
        ((c += 1), u.parsedCmd.isFinished || (l += 1));
        continue;
      }
      if (u.parsedCmd.type === `read`) {
        let e = Hc(u.parsedCmd.path ?? u.parsedCmd.name, u.cwd);
        if (_i({ summary: u.parsedCmd, cwd: u.cwd })) {
          (i.push(e), u.parsedCmd.isFinished || a.push(e));
          continue;
        }
        (n.push(e), u.parsedCmd.isFinished || r.push(e));
      }
    }
  return {
    type: `exploration`,
    readPaths: n,
    runningReadPaths: r,
    loadedToolPaths: i,
    runningLoadedToolPaths: a,
    searchCount: o,
    runningSearchCount: s,
    listCount: c,
    runningListCount: l,
    ...zc(t),
  };
}
function Vc(e, t) {
  if (
    Object.keys(e.changes).length === 0 &&
    e.visualizationActivities != null &&
    (e.success === !1 || (e.success == null && t))
  )
    return { type: `other` };
  let n = [],
    r = [],
    i = [],
    a = 0,
    o = 0,
    s = [],
    c = [],
    l = [],
    u = [];
  for (let [d, f] of Object.entries(e.changes)) {
    let p = mr(Bt(d, f), d);
    if (((o += (p?.added ?? 0) + (p?.deleted ?? 0)), f.type === `add`)) {
      (n.push(d),
        e.success == null && t
          ? i.push(d)
          : (e.success ?? (r.push(d), (a += Uc(f.content)))));
      continue;
    }
    if (f.type === `delete`) {
      (l.push(d), e.success ?? u.push(d));
      continue;
    }
    (s.push(d), e.success ?? c.push(d));
  }
  let d =
    e.visualizationActivities == null ||
    e.success === !1 ||
    (e.success == null && t)
      ? void 0
      : {
          activities: e.visualizationActivities.map(({ path: e, kind: t }) => ({
            path: xt(e),
            kind: t,
          })),
          isInProgress: e.success == null,
        };
  return {
    type: `patch`,
    createdPaths: n,
    runningCreatedPaths: r,
    stoppedCreatedPaths: i,
    runningCreatedLineCount: a,
    changedLineCount: o,
    editedPaths: s,
    runningEditedPaths: c,
    deletedPaths: l,
    runningDeletedPaths: u,
    visualizationActivity: d,
  };
}
function Hc(e, t) {
  let n = xt(e);
  return t == null ? n : xt(je(t, n));
}
function Uc(e) {
  let t = e.replace(
    /\r\n/g,
    `
`,
  ).split(`
`);
  return t.length > 0 && t[t.length - 1] === `` ? t.length - 1 : t.length;
}
var Wc,
  Gc,
  Kc,
  qc = e(() => {
    (Oc(),
      Me(),
      gi(),
      Rt(),
      sr(),
      Nc(),
      rn(),
      It(),
      wi(),
      (Wc = /(?:^|\s)(?:-X\s*|--request(?:=|\s+))(?:POST|PUT|PATCH|DELETE)\b/i),
      (Gc =
        /(?:^|\s)(?:--data(?:-[^\s=]+)?|--json|--form|--upload-file)(?:=|\s|$)/),
      (Kc = /(?:^|\s)-(?:d|F|T)(?:=|\s|$)/));
  });
function Jc(e, t) {
  let n = e.favIconUrl?.trim();
  if (n == null || n.length === 0) return null;
  let r = (e.url ?? e.pendingUrl ?? ``).trim();
  if (r.length === 0 || t == null) return n;
  try {
    let e = new URL(t.getURL(`/_favicon/`));
    return (
      e.searchParams.set(`pageUrl`, r),
      e.searchParams.set(`size`, `32`),
      e.toString()
    );
  } catch {
    return n;
  }
}
var Yc = e(() => {
  (S(),
    D({
      favIconUrl: O().optional(),
      pendingUrl: O().optional(),
      status: v([`loading`, `complete`, `unloaded`]).optional(),
      url: O().optional(),
    }));
});
function Xc(e) {
  let t = (0, nl.c)(23),
    { item: n, tabId: r, variant: a, agentActivityIcon: o } = e,
    { data: s } = Qc(n, r),
    c = s?.faviconUrl ?? null,
    l = s?.title ?? null,
    u;
  if (l != null) {
    let e;
    (t[0] !== n.completed || t[1] !== l
      ? ((e = n.completed
          ? (0, G.jsx)(P, {
              id: `localConversation.chromeExtensionToolCall.getTabContext.completedWithTitle`,
              defaultMessage: `Read "{title}"`,
              description: `Completed label for reading the contents of a Chrome tab with a known page title.`,
              values: { title: l },
            })
          : (0, G.jsx)(P, {
              id: `localConversation.chromeExtensionToolCall.getTabContext.activeWithTitle`,
              defaultMessage: `Reading "{title}"`,
              description: `In-progress label for reading the contents of a Chrome tab with a known page title.`,
              values: { title: l },
            })),
        (t[0] = n.completed),
        (t[1] = l),
        (t[2] = e))
      : (e = t[2]),
      (u = e));
  } else {
    let e;
    (t[3] === n.completed
      ? (e = t[4])
      : ((e = n.completed
          ? (0, G.jsx)(P, {
              id: `localConversation.chromeExtensionToolCall.getTabContext.completed`,
              defaultMessage: `Read tab`,
              description: `Completed label for reading the contents of a Chrome tab.`,
            })
          : (0, G.jsx)(P, {
              id: `localConversation.chromeExtensionToolCall.getTabContext.active`,
              defaultMessage: `Reading tab`,
              description: `In-progress label for reading the contents of a Chrome tab.`,
            })),
        (t[3] = n.completed),
        (t[4] = e)),
      (u = e));
  }
  let d = !n.completed,
    f = a !== `summary-text` && `min-w-0 truncate`,
    p;
  t[5] === f ? (p = t[6]) : ((p = i(f)), (t[5] = f), (t[6] = p));
  let m;
  t[7] !== u || t[8] !== d || t[9] !== p
    ? ((m = (0, G.jsx)(Fr, { active: d, className: p, children: u })),
      (t[7] = u),
      (t[8] = d),
      (t[9] = p),
      (t[10] = m))
    : (m = t[10]);
  let h = m;
  if (a === `row` && o !== void 0) {
    let e;
    return (
      t[11] !== o || t[12] !== h
        ? ((e = (0, G.jsx)(Mr, { icon: o, summary: h })),
          (t[11] = o),
          (t[12] = h),
          (t[13] = e))
        : (e = t[13]),
      e
    );
  }
  let g =
      a === `row`
        ? `text-token-conversation-summary-leading`
        : `text-token-conversation-summary-trailing group-hover/activity-header:text-token-foreground`,
    _;
  t[14] === g
    ? (_ = t[15])
    : ((_ = i(`text-size-chat`, g)), (t[14] = g), (t[15] = _));
  let v;
  t[16] !== c || t[17] !== a
    ? ((v =
        a !== `summary-text` && c != null
          ? (0, G.jsx)(nr, {
              src: c,
              className: `icon-xs text-token-input-placeholder-foreground`,
              showFallbackWhileLoading: !1,
            })
          : null),
      (t[16] = c),
      (t[17] = a),
      (t[18] = v))
    : (v = t[18]);
  let y;
  return (
    t[19] !== h || t[20] !== _ || t[21] !== v
      ? ((y = (0, G.jsxs)(jr, { className: _, children: [v, h] })),
        (t[19] = h),
        (t[20] = _),
        (t[21] = v),
        (t[22] = y))
      : (y = t[22]),
    y
  );
}
function Zc(e) {
  let t = (0, nl.c)(2),
    { item: n, tabId: r } = e,
    { data: i } = Qc(n, r),
    a;
  return (
    t[0] === i
      ? (a = t[1])
      : ((a =
          i?.faviconUrl == null
            ? null
            : (0, G.jsx)(nr, {
                src: i.faviconUrl,
                className: `icon-xs text-token-input-placeholder-foreground`,
                showFallbackWhileLoading: !1,
              })),
        (t[0] = i),
        (t[1] = a)),
    a
  );
}
function Qc(e, t) {
  let n = (0, nl.c)(9),
    r = !e.completed,
    i;
  n[0] === t ? (i = n[1]) : ((i = () => $c(t)), (n[0] = t), (n[1] = i));
  let a;
  n[2] !== e.callId || n[3] !== t
    ? ((a = [`chrome-tab-context-tool-call-metadata`, e.callId, t]),
      (n[2] = e.callId),
      (n[3] = t),
      (n[4] = a))
    : (a = n[4]);
  let o;
  return (
    n[5] !== r || n[6] !== i || n[7] !== a
      ? ((o = { enabled: r, queryFn: i, queryKey: a, staleTime: 1 / 0 }),
        (n[5] = r),
        (n[6] = i),
        (n[7] = a),
        (n[8] = o))
      : (o = n[8]),
    g(o)
  );
}
async function $c(e) {
  let t = el();
  if (t?.tabs == null) return null;
  try {
    let n = await t.tabs.get(e);
    return { faviconUrl: Jc(n, t.runtime), title: tl(n) };
  } catch {
    return null;
  }
}
function el() {
  return globalThis.chrome ?? null;
}
function tl(e) {
  let t = e.title?.trim();
  return t == null || t.length === 0 ? null : t;
}
var nl,
  G,
  rl = e(() => {
    ((nl = c()), ve(), l(), xe(), Yc(), ir(), Or(), Pr(), kr(), (G = f()));
  });
function il(e) {
  let t = cl.safeParse(e.arguments);
  return t.success ? (0, sl.jsx)(Zc, { item: e, tabId: t.data.tabId }) : null;
}
function al(e, t, n) {
  let r = cl.safeParse(e.arguments);
  return r.success
    ? (0, sl.jsx)(Xc, {
        item: e,
        tabId: r.data.tabId,
        variant: t,
        agentActivityIcon: n,
      })
    : null;
}
function ol(e) {
  return cl.safeParse(e.arguments).success ? e.callId : null;
}
var sl,
  cl,
  ll,
  ul = e(() => {
    (S(),
      en(),
      Vt(),
      rl(),
      (sl = f()),
      (cl = E({ tabId: fe().int().nonnegative() })),
      (ll = [
        {
          getCompletedSummaryPartKey: ol,
          namespace: Ut,
          render: al,
          renderAgentActivityIcon: il,
          tool: ht,
        },
      ]));
  });
function dl() {
  return (0, hl.jsx)(Sn, {
    className: `icon-xs shrink-0 text-token-conversation-body`,
  });
}
function fl(e, t, n) {
  return ml(e, e.completed ? `readCompleted` : `readActive`, t, n);
}
function pl(e, t, n) {
  return ml(e, e.completed ? `writeCompleted` : `writeActive`, t, n);
}
function ml(e, t, n, r) {
  return n === `row` && r !== void 0
    ? (0, hl.jsx)(Mr, {
        icon: r,
        summary: (0, hl.jsx)(Fr, {
          active: !e.completed,
          className: `min-w-0 truncate`,
          children: (0, hl.jsx)(P, { ..._l[t] }),
        }),
      })
    : (0, hl.jsxs)(jr, {
        className: i(
          `text-size-chat`,
          n === `row`
            ? `text-token-conversation-summary-leading`
            : `text-token-conversation-summary-trailing group-hover/activity-header:text-token-foreground`,
        ),
        children: [
          n === `summary-text` ? null : dl(),
          (0, hl.jsx)(Fr, {
            active: !e.completed,
            className: i(n !== `summary-text` && `min-w-0 truncate`),
            children: (0, hl.jsx)(P, { ..._l[t] }),
          }),
        ],
      });
}
var hl,
  gl,
  _l,
  vl = e(() => {
    (l(),
      xe(),
      Ec(),
      oa(),
      xn(),
      Or(),
      Pr(),
      kr(),
      (hl = f()),
      (gl = [
        { namespace: W, render: fl, renderAgentActivityIcon: dl, tool: na },
        { namespace: W, render: pl, renderAgentActivityIcon: dl, tool: ra },
      ]),
      (_l = ye({
        readActive: {
          id: `localConversation.settingsToolCall.read.active`,
          defaultMessage: `Reading settings`,
          description: `In-progress label for reading Codex settings.`,
        },
        readCompleted: {
          id: `localConversation.settingsToolCall.read.completed`,
          defaultMessage: `Read settings`,
          description: `Completed label for reading Codex settings.`,
        },
        writeActive: {
          id: `localConversation.settingsToolCall.write.active`,
          defaultMessage: `Updating settings`,
          description: `In-progress label for updating Codex settings.`,
        },
        writeCompleted: {
          id: `localConversation.settingsToolCall.write.completed`,
          defaultMessage: `Updated settings`,
          description: `Completed label for updating Codex settings.`,
        },
      })));
  });
function yl(e) {
  let t = (0, Sl.c)(18),
    { children: n, compact: r, status: a } = e,
    o = r === void 0 ? !1 : r,
    s = o ? `gap-2` : `gap-3`,
    c;
  t[0] === s
    ? (c = t[1])
    : ((c = i(`flex items-center`, s)), (t[0] = s), (t[1] = c));
  let l;
  t[2] === a
    ? (l = t[3])
    : ((l = (0, K.jsx)(bl, { status: a })), (t[2] = a), (t[3] = l));
  let u = o
      ? `text-size-chat text-token-conversation-summary-leading`
      : `text-base leading-6 tracking-[-0.13px]`,
    d;
  t[4] !== o || t[5] !== a || t[6] !== u
    ? ((d = i(
        u,
        !o && {
          "font-medium text-token-foreground": a === `running`,
          "text-token-foreground": a === `done`,
          "text-token-editor-error-foreground": a === `failed`,
          "text-token-description-foreground": a === `pending`,
        },
      )),
      (t[4] = o),
      (t[5] = a),
      (t[6] = u),
      (t[7] = d))
    : (d = t[7]);
  let f;
  t[8] === a
    ? (f = t[9])
    : ((f = (0, K.jsx)(`span`, {
        className: `sr-only`,
        children: (0, K.jsx)(xl, { status: a }),
      })),
      (t[8] = a),
      (t[9] = f));
  let p;
  t[10] !== n || t[11] !== d || t[12] !== f
    ? ((p = (0, K.jsxs)(`div`, { className: d, children: [f, n] })),
      (t[10] = n),
      (t[11] = d),
      (t[12] = f),
      (t[13] = p))
    : (p = t[13]);
  let m;
  return (
    t[14] !== c || t[15] !== l || t[16] !== p
      ? ((m = (0, K.jsxs)(`div`, { className: c, children: [l, p] })),
        (t[14] = c),
        (t[15] = l),
        (t[16] = p),
        (t[17] = m))
      : (m = t[17]),
    m
  );
}
function bl(e) {
  let t = (0, Sl.c)(6),
    { status: n } = e,
    r;
  bb0: switch (n) {
    case `running`: {
      let e;
      (t[0] === Symbol.for(`react.memo_cache_sentinel`)
        ? ((e = (0, K.jsx)(Ce, { className: `icon-xs` })), (t[0] = e))
        : (e = t[0]),
        (r = e));
      break bb0;
    }
    case `done`: {
      let e;
      (t[1] === Symbol.for(`react.memo_cache_sentinel`)
        ? ((e = (0, K.jsx)(gr, { className: `icon-xs` })), (t[1] = e))
        : (e = t[1]),
        (r = e));
      break bb0;
    }
    case `failed`: {
      let e;
      (t[2] === Symbol.for(`react.memo_cache_sentinel`)
        ? ((e = (0, K.jsx)(wr, {
            className: `icon-xs text-token-editor-error-foreground`,
          })),
          (t[2] = e))
        : (e = t[2]),
        (r = e));
      break bb0;
    }
    case `pending`: {
      let e;
      (t[3] === Symbol.for(`react.memo_cache_sentinel`)
        ? ((e = (0, K.jsx)(zr, { className: `icon-xs` })), (t[3] = e))
        : (e = t[3]),
        (r = e));
    }
  }
  let i;
  return (
    t[4] === r
      ? (i = t[5])
      : ((i = (0, K.jsx)(`span`, {
          "aria-hidden": !0,
          className: `flex h-4 w-4 shrink-0 items-center justify-center text-token-text-secondary`,
          children: r,
        })),
        (t[4] = r),
        (t[5] = i)),
    i
  );
}
function xl(e) {
  let t = (0, Sl.c)(4),
    { status: n } = e;
  switch (n) {
    case `running`: {
      let e;
      return (
        t[0] === Symbol.for(`react.memo_cache_sentinel`)
          ? ((e = (0, K.jsx)(P, {
              id: `progressStep.status.running`,
              defaultMessage: `In progress:`,
              description: `Screen reader status for a progress step that is currently running`,
            })),
            (t[0] = e))
          : (e = t[0]),
        e
      );
    }
    case `done`: {
      let e;
      return (
        t[1] === Symbol.for(`react.memo_cache_sentinel`)
          ? ((e = (0, K.jsx)(P, {
              id: `progressStep.status.done`,
              defaultMessage: `Completed:`,
              description: `Screen reader status for a completed progress step`,
            })),
            (t[1] = e))
          : (e = t[1]),
        e
      );
    }
    case `failed`: {
      let e;
      return (
        t[2] === Symbol.for(`react.memo_cache_sentinel`)
          ? ((e = (0, K.jsx)(P, {
              id: `progressStep.status.failed`,
              defaultMessage: `Failed:`,
              description: `Screen reader status for a failed progress step`,
            })),
            (t[2] = e))
          : (e = t[2]),
        e
      );
    }
    case `pending`: {
      let e;
      return (
        t[3] === Symbol.for(`react.memo_cache_sentinel`)
          ? ((e = (0, K.jsx)(P, {
              id: `progressStep.status.pending`,
              defaultMessage: `Pending:`,
              description: `Screen reader status for a progress step that has not started`,
            })),
            (t[3] = e))
          : (e = t[3]),
        e
      );
    }
  }
}
var Sl,
  K,
  Cl = e(() => {
    ((Sl = c()), l(), xe(), Se(), hr(), Ir(), Tr(), (K = f()));
  });
function wl(e) {
  let t = (0, El.c)(10),
    {
      compact: n,
      direction: r,
      localBranch: i,
      sourceBranch: a,
      step: o,
      worktreeBranch: s,
    } = e,
    c = n === void 0 ? !1 : n,
    l = o.status,
    u;
  t[0] !== r || t[1] !== i || t[2] !== a || t[3] !== o.id || t[4] !== s
    ? ((u = Tl(o.id, r, i, a, s)),
      (t[0] = r),
      (t[1] = i),
      (t[2] = a),
      (t[3] = o.id),
      (t[4] = s),
      (t[5] = u))
    : (u = t[5]);
  let d;
  return (
    t[6] !== c || t[7] !== o.status || t[8] !== u
      ? ((d = (0, q.jsx)(yl, { compact: c, status: l, children: u })),
        (t[6] = c),
        (t[7] = o.status),
        (t[8] = u),
        (t[9] = d))
      : (d = t[9]),
    d
  );
}
function Tl(e, t, n, r, i) {
  if (e === `rolling-back-changes`)
    return (0, q.jsx)(P, {
      id: `localConversation.threadHandoff.step.rollingBackChanges`,
      defaultMessage: `Rolling back changes`,
      description: `Progress step shown after a task handoff step fails while cleanup is still in progress`,
    });
  switch (e) {
    case `prepare-host-transfer`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.prepareHostTransfer`,
        defaultMessage: `Preparing files for transfer`,
        description: `Progress step shown while preparing cross-host git and rollout artifacts`,
      });
    case `transfer-host-artifacts`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.transferHostArtifacts`,
        defaultMessage: `Copying files to the destination host`,
        description: `Progress step shown while copying cross-host git and rollout artifacts`,
      });
    case `create-new-worktree`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.createNewWorktree`,
        defaultMessage: `Creating a new worktree`,
        description: `Progress step shown while creating a new worktree during task handoff`,
      });
    case `reuse-existing-worktree`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.reuseExistingWorktree`,
        defaultMessage: `Reusing the existing worktree`,
        description: `Progress step shown while reusing an existing worktree during task handoff`,
      });
    case `stash-source-changes`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.stashSourceChanges`,
        defaultMessage: `Stashing uncommitted changes`,
        description: `Progress step shown while stashing source changes during task handoff`,
      });
    case `checkout-local-branch`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.checkoutLocalBranch`,
        defaultMessage: `Checking out {branch} locally`,
        description: `Progress step shown while checking out a branch locally during task handoff`,
        values: { branch: n ?? r },
      });
    case `stash-target-worktree-changes`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.stashTargetWorktreeChanges`,
        defaultMessage: `Stashing worktree changes`,
        description: `Progress step shown while stashing pre-existing worktree changes during task handoff`,
      });
    case `checkout-worktree-branch`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.checkoutWorktreeBranch`,
        defaultMessage: `Checking out {branch} in worktree`,
        description: `Progress step shown while checking out a branch in the worktree during task handoff`,
        values: { branch: i ?? r },
      });
    case `detach-worktree-branch`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.detachWorktreeBranch`,
        defaultMessage: `Detaching branch from worktree`,
        description: `Progress step shown while detaching the worktree branch during handoff back to local`,
      });
    case `apply-changes-to-worktree`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.applyChangesToWorktree`,
        defaultMessage: `Applying uncommitted changes to worktree`,
        description: `Progress step shown while applying changes to the worktree during task handoff`,
      });
    case `apply-changes-to-local`:
      return (0, q.jsx)(P, {
        id: `localConversation.threadHandoff.step.applyChangesToLocal`,
        defaultMessage: `Applying uncommitted changes locally`,
        description: `Progress step shown while applying changes locally during task handoff`,
      });
    case `switching-thread`:
      return t === `to-worktree`
        ? (0, q.jsx)(P, {
            id: `localConversation.threadHandoff.step.moveThreadToWorktree`,
            defaultMessage: `Moving chat to worktree`,
            description: `Progress step shown while moving the task to a worktree after the git handoff`,
          })
        : t === `to-host-worktree`
          ? (0, q.jsx)(P, {
              id: `localConversation.threadHandoff.step.moveThreadToHostWorktree`,
              defaultMessage: `Moving chat to the destination worktree`,
              description: `Progress step shown while moving the task to a destination-host worktree after transfer`,
            })
          : (0, q.jsx)(P, {
              id: `localConversation.threadHandoff.step.moveThreadToLocal`,
              defaultMessage: `Moving chat to local`,
              description: `Progress step shown while moving the task to local after the git handoff`,
            });
  }
}
var El,
  q,
  Dl = e(() => {
    ((El = c()), xe(), Cl(), (q = f()));
  });
function Ol(e) {
  let t = (0, Nl.c)(26),
    { agentActivityIcon: r, item: a, operation: o, variant: s } = e,
    c =
      a != null && a.completed && a.success === !0
        ? Ml(a.contentItems ?? null)
        : null,
    l = n(fo, o == null ? (c?.operationId ?? a.callId) : o.id),
    u = o ?? l,
    d = l?.threadTitle ?? c?.threadTitle ?? null,
    f = l?.destinationHostDisplayName ?? c?.destinationHostDisplayName ?? null,
    p =
      u == null
        ? c == null
          ? (a?.completed ?? !1)
          : co(c.status)
        : co(u.status),
    m =
      u == null
        ? a?.success
        : u.status === `error`
          ? !1
          : u.status === `success` || u.status === `warning`,
    h = `running`;
  p && (h = m === !1 ? `failed` : `completed`);
  let g;
  t[0] !== p || t[1] !== f || t[2] !== o || t[3] !== m || t[4] !== d
    ? ((g =
        o == null
          ? (0, J.jsx)(jl, {
              completed: p,
              destinationHostDisplayName: f,
              success: m,
              threadTitle: d,
            })
          : (0, J.jsx)(kl, { operation: o })),
      (t[0] = p),
      (t[1] = f),
      (t[2] = o),
      (t[3] = m),
      (t[4] = d),
      (t[5] = g))
    : (g = t[5]);
  let _ = g;
  if (s !== `row`) {
    let e;
    t[6] === s
      ? (e = t[7])
      : ((e =
          s === `summary-text`
            ? null
            : (0, J.jsx)(Cn, {
                className: `icon-xs shrink-0 text-token-conversation-body`,
              })),
        (t[6] = s),
        (t[7] = e));
    let n = !p,
      r = s !== `summary-text` && `min-w-0 truncate`,
      a;
    t[8] === r ? (a = t[9]) : ((a = i(r)), (t[8] = r), (t[9] = a));
    let o;
    t[10] !== _ || t[11] !== n || t[12] !== a
      ? ((o = (0, J.jsx)(Fr, { active: n, className: a, children: _ })),
        (t[10] = _),
        (t[11] = n),
        (t[12] = a),
        (t[13] = o))
      : (o = t[13]);
    let c;
    return (
      t[14] !== e || t[15] !== o
        ? ((c = (0, J.jsxs)(jr, {
            className: `text-size-chat text-token-conversation-summary-trailing group-hover/activity-header:text-token-foreground`,
            children: [e, o],
          })),
          (t[14] = e),
          (t[15] = o),
          (t[16] = c))
        : (c = t[16]),
      c
    );
  }
  let v;
  t[17] === r
    ? (v = t[18])
    : ((v =
        r ??
        (0, J.jsx)(Cn, {
          className: `icon-xs shrink-0 text-token-conversation-body`,
        })),
      (t[17] = r),
      (t[18] = v));
  let y;
  t[19] === u
    ? (y = t[20])
    : ((y = u?.steps.map((e) =>
        (0, J.jsx)(
          wl,
          {
            compact: !0,
            direction: u.direction,
            localBranch: u.localBranch,
            sourceBranch: u.sourceBranch,
            step: e,
            worktreeBranch: u.worktreeBranch,
          },
          e.id,
        ),
      )),
      (t[19] = u),
      (t[20] = y));
  let b;
  return (
    t[21] !== h || t[22] !== _ || t[23] !== v || t[24] !== y
      ? ((b = (0, J.jsx)(Nr, {
          indentContent: !1,
          icon: v,
          status: h,
          summary: _,
          children: y,
        })),
        (t[21] = h),
        (t[22] = _),
        (t[23] = v),
        (t[24] = y),
        (t[25] = b))
      : (b = t[25]),
    b
  );
}
function kl(e) {
  let t = (0, Nl.c)(10),
    { operation: n } = e,
    r;
  t[0] === n
    ? (r = t[1])
    : ((r = (0, J.jsx)(Al, { operation: n })), (t[0] = n), (t[1] = r));
  let i = r;
  if (n.status === `queued` || n.status === `running`) {
    let e;
    return (
      t[2] === i
        ? (e = t[3])
        : ((e = (0, J.jsx)(P, {
            id: `localConversation.threadHandoff.inline.active`,
            defaultMessage: `Handing off to {destination}`,
            description: `In-progress label for handing the current task to another run location`,
            values: { destination: i },
          })),
          (t[2] = i),
          (t[3] = e)),
      e
    );
  }
  if (n.status === `error`) {
    let e;
    return (
      t[4] === i
        ? (e = t[5])
        : ((e = (0, J.jsx)(P, {
            id: `localConversation.threadHandoff.inline.failed`,
            defaultMessage: `Failed to hand off to {destination}`,
            description: `Failed label for handing the current task to another run location`,
            values: { destination: i },
          })),
          (t[4] = i),
          (t[5] = e)),
      e
    );
  }
  if (n.status === `warning`) {
    let e;
    return (
      t[6] === i
        ? (e = t[7])
        : ((e = (0, J.jsx)(P, {
            id: `localConversation.threadHandoff.inline.warning`,
            defaultMessage: `Handoff to {destination} needs attention`,
            description: `Warning label for handing the current task to another run location`,
            values: { destination: i },
          })),
          (t[6] = i),
          (t[7] = e)),
      e
    );
  }
  let a;
  return (
    t[8] === i
      ? (a = t[9])
      : ((a = (0, J.jsx)(P, {
          id: `localConversation.threadHandoff.inline.completed`,
          defaultMessage: `Handed off to {destination}`,
          description: `Completed label for handing the current task to another run location`,
          values: { destination: i },
        })),
        (t[8] = i),
        (t[9] = a)),
    a
  );
}
function Al(e) {
  let t = (0, Nl.c)(4),
    { operation: n } = e;
  if (n.direction === `to-local`) {
    let e;
    return (
      t[0] === Symbol.for(`react.memo_cache_sentinel`)
        ? ((e = (0, J.jsx)(P, {
            id: `localConversation.threadHandoff.inline.destination.local`,
            defaultMessage: `local`,
            description: `Destination noun used in an inline task handoff status`,
          })),
          (t[0] = e))
        : (e = t[0]),
      e
    );
  }
  if (n.direction === `to-worktree`) {
    let e;
    return (
      t[1] === Symbol.for(`react.memo_cache_sentinel`)
        ? ((e = (0, J.jsx)(P, {
            id: `localConversation.threadHandoff.inline.destination.worktree`,
            defaultMessage: `worktree`,
            description: `Destination noun used in an inline task handoff status`,
          })),
          (t[1] = e))
        : (e = t[1]),
      e
    );
  }
  let r;
  return (
    t[2] === n.request.destinationLabel
      ? (r = t[3])
      : ((r = (0, J.jsx)(J.Fragment, { children: n.request.destinationLabel })),
        (t[2] = n.request.destinationLabel),
        (t[3] = r)),
    r
  );
}
function jl(e) {
  let t = (0, Nl.c)(12),
    {
      completed: n,
      destinationHostDisplayName: r,
      success: i,
      threadTitle: a,
    } = e;
  if (a == null || r == null) {
    if (!n) {
      let e;
      return (
        t[0] === Symbol.for(`react.memo_cache_sentinel`)
          ? ((e = (0, J.jsx)(P, {
              id: `localConversation.appControlToolCall.threadsHandoff.active`,
              defaultMessage: `Handing off chat`,
              description: `In-progress label for handing off a Codex task when its title or destination host is unavailable`,
            })),
            (t[0] = e))
          : (e = t[0]),
        e
      );
    }
    if (i === !1) {
      let e;
      return (
        t[1] === Symbol.for(`react.memo_cache_sentinel`)
          ? ((e = (0, J.jsx)(P, {
              id: `localConversation.appControlToolCall.threadsHandoff.failed`,
              defaultMessage: `Failed to hand off chat`,
              description: `Failed label for handing off a Codex task when its title or destination host is unavailable`,
            })),
            (t[1] = e))
          : (e = t[1]),
        e
      );
    }
    let e;
    return (
      t[2] === Symbol.for(`react.memo_cache_sentinel`)
        ? ((e = (0, J.jsx)(P, {
            id: `localConversation.appControlToolCall.threadsHandoff.completed`,
            defaultMessage: `Handed off chat`,
            description: `Completed label for handing off a Codex task when its title or destination host is unavailable`,
          })),
          (t[2] = e))
        : (e = t[2]),
      e
    );
  }
  if (!n) {
    let e;
    return (
      t[3] !== r || t[4] !== a
        ? ((e = (0, J.jsx)(P, {
            id: `localConversation.appControlToolCall.threadsHandoffToHost.active`,
            defaultMessage: `Handing off {threadTitle} to {destinationHostDisplayName}`,
            description: `In-progress label for handing off a Codex task to a host`,
            values: { destinationHostDisplayName: r, threadTitle: a },
          })),
          (t[3] = r),
          (t[4] = a),
          (t[5] = e))
        : (e = t[5]),
      e
    );
  }
  if (i === !1) {
    let e;
    return (
      t[6] !== r || t[7] !== a
        ? ((e = (0, J.jsx)(P, {
            id: `localConversation.appControlToolCall.threadsHandoffToHost.failed`,
            defaultMessage: `Failed to hand off {threadTitle} to {destinationHostDisplayName}`,
            description: `Failed label for handing off a Codex task to a host`,
            values: { destinationHostDisplayName: r, threadTitle: a },
          })),
          (t[6] = r),
          (t[7] = a),
          (t[8] = e))
        : (e = t[8]),
      e
    );
  }
  let o;
  return (
    t[9] !== r || t[10] !== a
      ? ((o = (0, J.jsx)(P, {
          id: `localConversation.appControlToolCall.threadsHandoffToHost.completed`,
          defaultMessage: `Handed off {threadTitle} to {destinationHostDisplayName}`,
          description: `Completed label for handing off a Codex task to a host`,
          values: { destinationHostDisplayName: r, threadTitle: a },
        })),
        (t[9] = r),
        (t[10] = a),
        (t[11] = o))
      : (o = t[11]),
    o
  );
}
function Ml(e) {
  let t = e?.find((e) => e.type === `inputText`)?.text;
  if (t == null) return null;
  try {
    let e = JSON.parse(t),
      n = Pl.safeParse(e);
    return n.success ? n.data : null;
  } catch {
    return null;
  }
}
var Nl,
  J,
  Pl,
  Fl = e(() => {
    ((Nl = c()),
      l(),
      u(),
      xe(),
      S(),
      po(),
      wn(),
      Dl(),
      Pr(),
      kr(),
      Ar(),
      (J = f()),
      (Pl = D({
        destinationHostDisplayName: O().min(1),
        operationId: O().min(1),
        status: v([`queued`, `running`, `success`, `warning`, `error`]),
        threadTitle: O().min(1),
      })));
  });
function Il(e) {
  let t = (0, Ll.c)(30),
    { compact: n, result: r } = e,
    a = be(),
    o = `clientThreadId` in r,
    s;
  t[0] !== a || t[1] !== o
    ? ((s = a.formatMessage(o ? Rl.openWorktreeSetup : Rl.openCreatedThread)),
      (t[0] = a),
      (t[1] = o),
      (t[2] = s))
    : (s = t[2]);
  let c = s,
    l;
  t[3] !== o || t[4] !== r.clientThreadId || t[5] !== r.threadId
    ? ((l = () => {
        se.dispatchHostMessage({
          type: `navigate-to-route`,
          path: oe(o ? r.clientThreadId : w(r.threadId)),
        });
      }),
      (t[3] = o),
      (t[4] = r.clientThreadId),
      (t[5] = r.threadId),
      (t[6] = l))
    : (l = t[6]);
  let u = l,
    d = n ? `size-7 rounded-md` : `size-8 rounded-lg`,
    f;
  t[7] === d
    ? (f = t[8])
    : ((f = i(
        `flex shrink-0 items-center justify-center bg-token-bg-secondary text-token-text-secondary`,
        d,
      )),
      (t[7] = d),
      (t[8] = f));
  let p = n ? `icon-xs` : `icon-sm`,
    m;
  t[9] === p
    ? (m = t[10])
    : ((m = (0, Y.jsx)($n, { className: p, "aria-hidden": !0 })),
      (t[9] = p),
      (t[10] = m));
  let h;
  t[11] !== f || t[12] !== m
    ? ((h = (0, Y.jsx)(`span`, { className: f, children: m })),
      (t[11] = f),
      (t[12] = m),
      (t[13] = h))
    : (h = t[13]);
  let g = n ? `compact` : `default`,
    _ = o ? Rl.worktreeThreadQueued : Rl.threadCreated,
    v;
  t[14] === _
    ? (v = t[15])
    : ((v = (0, Y.jsx)(P, { ..._ })), (t[14] = _), (t[15] = v));
  let y = o ? Rl.openSetup : Rl.openThread,
    b;
  t[16] === y
    ? (b = t[17])
    : ((b = (0, Y.jsx)(lr, { label: (0, Y.jsx)(P, { ...y }) })),
      (t[16] = y),
      (t[17] = b));
  let x;
  t[18] !== v || t[19] !== b || t[20] !== h || t[21] !== g
    ? ((x = (0, Y.jsx)(dr, { icon: h, padding: g, title: v, trailing: b })),
      (t[18] = v),
      (t[19] = b),
      (t[20] = h),
      (t[21] = g),
      (t[22] = x))
    : (x = t[22]);
  let S;
  t[23] !== c || t[24] !== u || t[25] !== x
    ? ((S = (0, Y.jsx)(`button`, {
        type: `button`,
        "aria-label": c,
        className: `w-full cursor-interaction text-left hover:bg-token-list-hover-background/30 focus-visible:ring-1 focus-visible:ring-token-focus-border focus-visible:outline-none focus-visible:ring-inset`,
        onClick: u,
        children: x,
      })),
      (t[23] = c),
      (t[24] = u),
      (t[25] = x),
      (t[26] = S))
    : (S = t[26]);
  let C = S,
    T;
  return (
    t[27] !== n || t[28] !== C
      ? ((T = n ? C : (0, Y.jsx)(ur, { children: C })),
        (t[27] = n),
        (t[28] = C),
        (t[29] = T))
      : (T = t[29]),
    T
  );
}
var Ll,
  Y,
  Rl,
  zl = e(() => {
    ((Ll = c()),
      l(),
      M(),
      xe(),
      er(),
      le(),
      rr(),
      (Y = f()),
      (Rl = ye({
        threadCreated: {
          id: `localConversation.appControlToolCall.threadCreated`,
          defaultMessage: `Chat created`,
          description: `Title for a card shown after a Codex task is created.`,
        },
        openCreatedThread: {
          id: `localConversation.appControlToolCall.openCreatedThread`,
          defaultMessage: `Open chat`,
          description: `Accessible label for opening a newly created Codex task.`,
        },
        openThread: {
          id: `localConversation.appControlToolCall.openThread`,
          defaultMessage: `Open chat`,
          description: `Button label for opening a newly created Codex task.`,
        },
        worktreeThreadQueued: {
          id: `localConversation.appControlToolCall.worktreeThreadQueued`,
          defaultMessage: `Worktree chat queued`,
          description: `Title for a card shown after a worktree-backed Codex task is queued.`,
        },
        openWorktreeSetup: {
          id: `localConversation.appControlToolCall.openWorktreeSetup`,
          defaultMessage: `Open worktree setup`,
          description: `Accessible label for opening setup for a queued worktree Codex task.`,
        },
        openSetup: {
          id: `localConversation.appControlToolCall.openSetup`,
          defaultMessage: `Open setup`,
          description: `Button label for opening setup for a queued worktree Codex task.`,
        },
      })));
  });
function X(e) {
  return (0, Q.jsx)(
    e.tool === `create_thread` &&
      e.completed &&
      e.success === !0 &&
      Wl(e.contentItems ?? null) != null
      ? $n
      : Cn,
    { className: `icon-xs shrink-0 text-token-conversation-body` },
  );
}
function Bl(e, t, n) {
  return ql.safeParse(e.arguments).success
    ? (0, Q.jsx)(Ol, { agentActivityIcon: n, item: e, variant: t })
    : null;
}
function Vl(e) {
  let t = ql.safeParse(e.arguments);
  return t.success
    ? JSON.stringify([t.data.threadId, t.data.destinationHostId ?? null])
    : null;
}
function Hl(e) {
  let t = Jl.safeParse(e.arguments);
  return t.success ? t.data.operationId : null;
}
function Z(e, t, n) {
  let r = Ul(e);
  if (r == null) return null;
  let a =
    e.tool === `create_thread` && e.completed && e.success === !0 && t === `row`
      ? Wl(e.contentItems ?? null)
      : null;
  if (a != null) return (0, Q.jsx)(Il, { result: a });
  let o =
      t === `row` &&
      (e.tool === `read_thread` || e.tool === `send_message_to_thread`)
        ? Yl.safeParse(e.arguments)
        : null,
    s = o?.success === !0 ? o.data.threadId : null,
    c = t === `row` && n !== void 0,
    l = c ? `summary-text` : t,
    u = (0, Q.jsxs)(jr, {
      className: i(
        `text-size-chat`,
        l === `row`
          ? `text-token-conversation-summary-leading`
          : `text-token-conversation-summary-trailing group-hover/activity-header:text-token-foreground`,
      ),
      children: [
        l === `summary-text` ? null : X(e),
        (0, Q.jsx)(Fr, {
          active: !e.completed,
          className: i(
            l !== `summary-text` && `min-w-0 truncate`,
            s != null && `group-hover:!text-token-foreground`,
          ),
          children: (0, Q.jsx)(P, { ...Ql[r] }),
        }),
      ],
    }),
    d =
      s == null
        ? u
        : (0, Q.jsx)(`button`, {
            type: `button`,
            className: `group max-w-full cursor-interaction rounded-md text-left focus-visible:ring-1 focus-visible:ring-token-focus-border focus-visible:outline-none`,
            onClick: () => {
              let e = w(s);
              se.dispatchHostMessage({
                type: `navigate-to-route`,
                path: mt() ? p(e) : oe(e),
              });
            },
            children: u,
          });
  return c ? (0, Q.jsx)(Mr, { icon: n, summary: d }) : d;
}
function Ul(e) {
  switch (e.tool) {
    case Ys: {
      let t = Kl.safeParse(e.arguments);
      return t.success
        ? t.data.environment?.type === `worktree`
          ? e.completed
            ? `threadsForkInWorktreeCompleted`
            : `threadsForkInWorktreeActive`
          : e.completed
            ? `threadsForkCompleted`
            : `threadsForkActive`
        : null;
    }
    case hs: {
      let t = Gl.safeParse(e.arguments);
      return t.success
        ? t.data.target.type === `project` &&
          t.data.target.environment?.type === `worktree`
          ? e.completed
            ? `threadsCreateInWorktreeCompleted`
            : `threadsCreateInWorktreeActive`
          : e.completed
            ? `threadsCreateCompleted`
            : `threadsCreateActive`
        : null;
    }
    case Xs:
      return e.completed ? `threadsListCompleted` : `threadsListActive`;
    case Zs:
      return e.completed ? `threadsReadCompleted` : `threadsReadActive`;
    case gs:
      return e.completed
        ? `threadsSendMessageCompleted`
        : `threadsSendMessageActive`;
    case fs:
      return Jl.safeParse(e.arguments).success
        ? e.completed
          ? `threadsHandoffStatusCompleted`
          : `threadsHandoffStatusActive`
        : null;
    case $s:
      return e.completed
        ? `threadsSetPinnedCompleted`
        : `threadsSetPinnedActive`;
    case ec:
      return e.completed
        ? `threadsSetArchivedCompleted`
        : `threadsSetArchivedActive`;
    case tc:
      return e.completed ? `threadsSetTitleCompleted` : `threadsSetTitleActive`;
    default:
      return null;
  }
}
function Wl(e) {
  let t = e?.find((e) => e.type === `inputText`)?.text;
  if (t == null) return null;
  try {
    let e = JSON.parse(t),
      n = Xl.safeParse(e);
    return n.success ? n.data : null;
  } catch {
    return null;
  }
}
var Q,
  Gl,
  Kl,
  ql,
  Jl,
  Yl,
  Xl,
  Zl,
  Ql,
  $l = e(() => {
    (l(),
      M(),
      xe(),
      S(),
      Ec(),
      ps(),
      bc(),
      sn(),
      er(),
      wn(),
      le(),
      Or(),
      Pr(),
      Fl(),
      zl(),
      kr(),
      (Q = f()),
      (Gl = D({
        target: D({ type: O(), environment: D({ type: O() }).optional() }),
      })),
      (Kl = D({ environment: D({ type: O() }).optional() })),
      (ql = D({
        destinationHostId: O().min(1).optional(),
        threadId: O().min(1),
      })),
      (Jl = D({ operationId: O().min(1) })),
      (Yl = D({ threadId: O().min(1) })),
      (Xl = ie([
        D({ threadId: O().min(1) }),
        D({ clientThreadId: ee((e) => typeof e == `string` && te(e)) }),
      ])),
      (Zl = [
        {
          getCompletedSummaryPartKey: Ul,
          namespace: W,
          render: Z,
          renderAgentActivityIcon: X,
          tool: Ys,
        },
        {
          getCompletedSummaryPartKey: Ul,
          namespace: W,
          render: Z,
          renderAgentActivityIcon: X,
          tool: hs,
        },
        {
          getCompletedSummaryPartKey: Vl,
          namespace: W,
          render: Bl,
          renderAgentActivityIcon: X,
          standaloneInConversation: !0,
          tool: ds,
        },
        {
          getCompletedSummaryPartKey: Hl,
          namespace: W,
          render: Z,
          renderAgentActivityIcon: X,
          summaryOnlyInConversationGroup: !0,
          tool: fs,
        },
        {
          continuesLiveActivityBetweenCalls: !0,
          namespace: W,
          render: Z,
          renderAgentActivityIcon: X,
          tool: Xs,
        },
        {
          continuesLiveActivityBetweenCalls: !0,
          namespace: W,
          render: Z,
          renderAgentActivityIcon: X,
          tool: Zs,
        },
        { namespace: W, render: Z, renderAgentActivityIcon: X, tool: gs },
        { namespace: W, render: Z, renderAgentActivityIcon: X, tool: $s },
        { namespace: W, render: Z, renderAgentActivityIcon: X, tool: ec },
        { namespace: W, render: Z, renderAgentActivityIcon: X, tool: tc },
      ]),
      (Ql = ye({
        threadsForkActive: {
          id: `localConversation.appControlToolCall.threadsFork.active`,
          defaultMessage: `Creating new chat`,
          description: `In-progress label for continuing a chat in a new chat`,
        },
        threadsForkCompleted: {
          id: `localConversation.appControlToolCall.threadsFork.completed`,
          defaultMessage: `Created new chat`,
          description: `Completed label for continuing a chat in a new chat`,
        },
        threadsForkInWorktreeActive: {
          id: `localConversation.appControlToolCall.threadsForkInWorktree.active`,
          defaultMessage: `Creating chat in new worktree`,
          description: `In-progress label for continuing a chat in a new worktree`,
        },
        threadsForkInWorktreeCompleted: {
          id: `localConversation.appControlToolCall.threadsForkInWorktree.completed`,
          defaultMessage: `Created chat in new worktree`,
          description: `Completed label for continuing a chat in a new worktree`,
        },
        threadsCreateActive: {
          id: `localConversation.appControlToolCall.threadsCreate.active`,
          defaultMessage: `Creating chat`,
          description: `In-progress label for creating a Codex task.`,
        },
        threadsCreateCompleted: {
          id: `localConversation.appControlToolCall.threadsCreate.completed`,
          defaultMessage: `Created chat`,
          description: `Completed label for creating a Codex task.`,
        },
        threadsCreateInWorktreeActive: {
          id: `localConversation.appControlToolCall.threadsCreateInWorktree.active`,
          defaultMessage: `Creating worktree chat`,
          description: `In-progress label for creating a Codex worktree task.`,
        },
        threadsCreateInWorktreeCompleted: {
          id: `localConversation.appControlToolCall.threadsCreateInWorktree.completed`,
          defaultMessage: `Created worktree chat`,
          description: `Completed label for creating a Codex worktree task.`,
        },
        threadsListActive: {
          id: `localConversation.appControlToolCall.threadsList.active`,
          defaultMessage: `Listing chats`,
          description: `In-progress label for listing Codex tasks.`,
        },
        threadsListCompleted: {
          id: `localConversation.appControlToolCall.threadsList.completed`,
          defaultMessage: `Listed chats`,
          description: `Completed label for listing Codex tasks.`,
        },
        threadsReadActive: {
          id: `localConversation.appControlToolCall.threadsRead.active`,
          defaultMessage: `Reading chat`,
          description: `In-progress label for reading a Codex task.`,
        },
        threadsReadCompleted: {
          id: `localConversation.appControlToolCall.threadsRead.completed`,
          defaultMessage: `Read chat`,
          description: `Completed label for reading a Codex task.`,
        },
        threadsHandoffStatusActive: {
          id: `localConversation.appControlToolCall.threadsHandoffStatus.active`,
          defaultMessage: `Checking handoff status`,
          description: `In-progress label for checking an asynchronous Codex task handoff operation.`,
        },
        threadsHandoffStatusCompleted: {
          id: `localConversation.appControlToolCall.threadsHandoffStatus.completed`,
          defaultMessage: `Checked handoff status`,
          description: `Completed label for checking an asynchronous Codex task handoff operation.`,
        },
        threadsSendMessageActive: {
          id: `localConversation.appControlToolCall.threadsSendMessage.active`,
          defaultMessage: `Sending message to chat`,
          description: `In-progress label for sending a message to a Codex task.`,
        },
        threadsSendMessageCompleted: {
          id: `localConversation.appControlToolCall.threadsSendMessage.completed`,
          defaultMessage: `Sent message to chat`,
          description: `Completed label for sending a message to a Codex task.`,
        },
        threadsSetArchivedActive: {
          id: `localConversation.appControlToolCall.threadsSetArchived.active`,
          defaultMessage: `Updating chat archive`,
          description: `In-progress label for archiving or unarchiving a Codex chat.`,
        },
        threadsSetArchivedCompleted: {
          id: `localConversation.appControlToolCall.threadsSetArchived.completed`,
          defaultMessage: `Updated chat archive`,
          description: `Completed label for archiving or unarchiving a Codex chat.`,
        },
        threadsSetPinnedActive: {
          id: `localConversation.appControlToolCall.threadsSetPinned.active`,
          defaultMessage: `Updating chat pin`,
          description: `In-progress label for pinning or unpinning a Codex chat.`,
        },
        threadsSetPinnedCompleted: {
          id: `localConversation.appControlToolCall.threadsSetPinned.completed`,
          defaultMessage: `Updated chat pin`,
          description: `Completed label for pinning or unpinning a Codex chat.`,
        },
        threadsSetTitleActive: {
          id: `localConversation.appControlToolCall.threadsSetTitle.active`,
          defaultMessage: `Renaming chat`,
          description: `In-progress label for renaming a Codex chat.`,
        },
        threadsSetTitleCompleted: {
          id: `localConversation.appControlToolCall.threadsSetTitle.completed`,
          defaultMessage: `Renamed chat`,
          description: `Completed label for renaming a Codex chat.`,
        },
      })));
  });
function eu(e) {
  return iu.find((t) => t.namespace === e.namespace && t.tool === e.tool);
}
function tu(e) {
  return eu(e)?.continuesLiveActivityBetweenCalls === !0;
}
function nu(e) {
  return eu(e)?.summaryOnlyInConversationGroup === !0;
}
function ru(e) {
  let t = eu(e)?.getCompletedSummaryPartKey?.(e);
  return `${e.namespace}:${e.tool}:${t ?? ``}`;
}
var iu,
  au = e(() => {
    (ul(), vl(), $l(), (iu = [...ll, ...Zl, ...gl]));
  });
function ou(e) {
  switch (e.parsedCmd.type) {
    case `read`:
    case `search`:
    case `list_files`:
      return !0;
    case `format`:
    case `lint`:
    case `noop`:
    case `test`:
    case `unknown`:
      return !1;
  }
}
var su = e(() => {});
function cu(e, { dynamicToolCallRenderer: t, mcpServerStatuses: n } = {}) {
  switch (e.type) {
    case `exec`:
      return $(du(e), `groupable`);
    case `patch`:
      return $(du(e), `groupable`);
    case `web-search`:
      return e.query.trim().length === 0 ? null : $(e, `groupable`);
    case `mcp-tool-call`:
      return $(
        du(e),
        uu({ item: e, mcpServerStatuses: n }) ? `standalone` : `groupable`,
      );
    case `dynamic-tool-call`:
      return $(
        e,
        (t ?? eu(e))?.standaloneInConversation === !0
          ? `standalone`
          : `groupable`,
      );
    case `image-view`:
      return $(e, `standalone`);
    case `mcp-server-elicitation`:
      return !e.completed || e.elicitation.kind === `unsupportedOpenAIForm`
        ? null
        : $(
            e,
            e.elicitation.kind === `openaiForm` &&
              Object.values(e.elicitation.schema.properties).some(nt)
              ? `groupable`
              : `standalone`,
          );
    case `automatic-approval-review`:
      switch (e.status) {
        case `approved`:
          return null;
        case `inProgress`:
          return $(e, `groupable`);
        case `aborted`:
        case `denied`:
        case `timedOut`:
          return $(e, `standalone`);
      }
    case `assistant-message`:
    case `auto-review-interruption-warning`:
    case `context-compaction`:
    case `multi-agent-action`:
    case `realtime-transcript`:
    case `stream-error`:
    case `subagent-activity`:
    case `system-error`:
    case `user-input-response`:
    case `user-message`:
    case `worked-for`:
    case `worktree-init`:
      return $(e, `standalone`);
    case `reasoning`:
    case `automation-update`:
    case `forked-from-conversation`:
    case `generated-image`:
    case `model-changed`:
    case `model-rerouted`:
    case `permission-request`:
    case `personality-changed`:
    case `plan-implementation`:
    case `proposed-plan`:
    case `remote-task-created`:
    case `todo-list`:
    case `turn-diff`:
    case `userInput`:
    case `steered`:
      return null;
  }
}
function lu(e, t) {
  return e.type === `exec` &&
    ti(e.executionStatus) &&
    ei(e.parsedCmd.cmd) != null &&
    t?.type === `patch` &&
    t.visualizationActivities != null &&
    t.visualizationActivities.length > 0
    ? !0
    : e.type === `exec` && e.parsedCmd.type === `read`
      ? ni(xi({ summary: e.parsedCmd, cwd: e.cwd }))
      : !1;
}
function $(e, t) {
  return { item: e, grouping: t };
}
function uu({ item: e, mcpServerStatuses: t }) {
  return e.source?.kind === `computerUse` ||
    e.invocation.server === `computer-use`
    ? !0
    : Ac({ item: e, mcpServerStatuses: t });
}
function du(e) {
  let t = e.automaticApprovalReviews;
  if (t == null) return e;
  let n = t.filter((e) => e.status !== `approved`);
  if (n.length > 0 && n.length === t.length) return e;
  let { automaticApprovalReviews: r, ...i } = e;
  return n.length === 0 ? i : { ...i, automaticApprovalReviews: n };
}
var fu = e(() => {
  (gi(), Nc(), $e(), wi(), au());
});
function pu(e) {
  if (!e) return !1;
  switch (e.type) {
    case `user-message`:
    case `realtime-transcript`:
    case `turn-diff`:
    case `system-error`:
    case `stream-error`:
    case `steered`:
    case `remote-task-created`:
    case `personality-changed`:
    case `forked-from-conversation`:
    case `model-changed`:
    case `model-rerouted`:
    case `auto-review-interruption-warning`:
    case `automation-update`:
    case `plan-implementation`:
    case `worked-for`:
    case `generated-image`:
    case `worktree-init`:
    case `subagent-activity`:
    case `image-view`:
      return !1;
    case `context-compaction`:
      return !e.completed;
    case `assistant-message`:
    case `proposed-plan`:
    case `reasoning`:
      return !e.completed;
    case `exec`:
      return e.output?.exitCode === void 0;
    case `patch`:
      return e.success == null;
    case `mcp-tool-call`:
    case `dynamic-tool-call`:
      return !e.completed;
    case `automatic-approval-review`:
      return e.status === `inProgress`;
    case `multi-agent-action`:
      return e.status === `inProgress`;
    case `web-search`:
      return !e.completed;
    case `todo-list`:
      return e.plan.some((e) => e.status !== `completed`);
    case `userInput`:
      return !e.completed;
    case `user-input-response`:
    case `mcp-server-elicitation`:
    case `permission-request`:
      return !e.completed;
  }
}
var mu = e(() => {});
function hu({
  agentItems: e,
  isTurnInProgress: t,
  isAnyNonAgentItemInProgress: n,
}) {
  let r = [],
    i = null,
    a = !1,
    o = !1,
    s = (e) => {
      (i &&
        i.length > 0 &&
        r.push({ kind: `exploration`, items: i, status: e }),
        (i = null));
    };
  for (let [t, n] of e.entries())
    if (!lu(n, e[t + 1])) {
      if (gu(n)) {
        if (i) {
          i.push(n);
          continue;
        }
        i = [n];
        continue;
      }
      if (n.type === `reasoning`) {
        i && i.push(n);
        continue;
      }
      (i && s(`explored`), r.push({ kind: `item`, item: n }));
    }
  if (i) {
    let e = i.some((e) => pu(e));
    ((a = t && (!n || e)), s(a ? `exploring` : `explored`));
  } else {
    let e = (0, _u.default)(r);
    e?.kind === `item` &&
      ((o = pu(e.item)),
      e.item.type === `reasoning` && e.item.completed === !1 && (o = !1));
  }
  return {
    renderableAgentItems: r,
    isExploring: a,
    isAnyNonExploringAgentItemInProgress: o,
  };
}
function gu(e) {
  return !(
    e.type !== `exec` ||
    !ou(e) ||
    (e.parsedCmd.type === `read` &&
      !e.parsedCmd.isFinished &&
      _i({ summary: e.parsedCmd, cwd: e.cwd }))
  );
}
var _u,
  vu = e(() => {
    ((_u = t(m(), 1)), su(), fu(), mu(), wi());
  });
function yu({ units: e, isActivitySliceClosed: t }) {
  let n = [],
    r = xu(e, t);
  for (let { startIndex: t, endIndex: i } of r) {
    let r = t;
    for (; r < i;) {
      let t = e[r];
      if (t == null || !Su(t)) {
        r += 1;
        continue;
      }
      let a = r,
        o = Cu();
      for (; r < i;) {
        let t = e[r];
        if (t == null || !Su(t)) break;
        (wu(o, t), (r += 1));
      }
      n.push({ startIndex: a, endIndex: r, summary: Tu(o) });
    }
  }
  return n;
}
function bu(e) {
  let t = Cu();
  for (let n of e) Su(n) && wu(t, n);
  return Tu(t);
}
function xu(e, t) {
  let n = [];
  for (let [t, r] of e.entries()) r.type === `assistant-message` && n.push(t);
  if (n.length === 0)
    return !t || e.length === 0 ? [] : [{ startIndex: 0, endIndex: e.length }];
  let r = [];
  for (let [i, a] of n.entries()) {
    let o = n[i + 1] ?? (t ? e.length : null);
    o == null || a + 1 >= o || r.push({ startIndex: a + 1, endIndex: o });
  }
  return r;
}
function Su(e) {
  return e.type === `assistant-message` || e.type === `other`
    ? !1
    : e.type === `exploration` ||
        e.type === `patch` ||
        e.type === `exec` ||
        e.type === `mcp-tool-call` ||
        e.type === `automatic-approval-review` ||
        e.type === `web-search`
      ? !0
      : Dc(e);
}
function Cu() {
  return {
    createdPaths: new Set(),
    runningCreatedPaths: new Set(),
    stoppedCreatedPaths: new Set(),
    runningCreatedLineCount: 0,
    changedLineCount: 0,
    editedPaths: new Set(),
    runningEditedPaths: new Set(),
    deletedPaths: new Set(),
    runningDeletedPaths: new Set(),
    visualizationActivitiesByPath: new Map(),
    visualizationCommandKind: null,
    exploredPaths: new Set(),
    runningExploredPaths: new Set(),
    loadedToolPaths: new Set(),
    runningLoadedToolPaths: new Set(),
    searchCount: 0,
    runningSearchCount: 0,
    listCount: 0,
    runningListCount: 0,
    automaticApprovalReviewFailureIds: new Set(),
    deniedRequestCount: 0,
    timedOutRequestCount: 0,
    commandCount: 0,
    runningCommandCount: 0,
    completedWebSearchCommandCount: 0,
    runningFolderCreationCommandCount: 0,
    completedVisualizationCommandCount: 0,
    runningVisualizationCommandCount: 0,
    runningWebSearchCommandCount: 0,
    mcpToolCallCount: 0,
    mcpToolCallSources: new Map(),
    webSearchCount: 0,
    runningWebSearchCount: 0,
  };
}
function wu(e, t) {
  if (t.type === `exploration`) {
    Eu(e, t.automaticApprovalReviewFailures);
    for (let n of t.readPaths) e.exploredPaths.add(n);
    for (let n of t.runningReadPaths) e.runningExploredPaths.add(n);
    for (let n of t.loadedToolPaths) e.loadedToolPaths.add(n);
    for (let n of t.runningLoadedToolPaths) e.runningLoadedToolPaths.add(n);
    ((e.searchCount += t.searchCount),
      (e.runningSearchCount += t.runningSearchCount),
      (e.listCount += t.listCount),
      (e.runningListCount += t.runningListCount));
    return;
  }
  if (t.type === `patch`) {
    Eu(e, t.automaticApprovalReviewFailures);
    for (let n of t.createdPaths) e.createdPaths.add(n);
    for (let n of t.runningCreatedPaths) e.runningCreatedPaths.add(n);
    for (let n of t.stoppedCreatedPaths) e.stoppedCreatedPaths.add(n);
    ((e.runningCreatedLineCount += t.runningCreatedLineCount),
      (e.changedLineCount += t.changedLineCount));
    for (let n of t.editedPaths) e.editedPaths.add(n);
    for (let n of t.runningEditedPaths) e.runningEditedPaths.add(n);
    for (let n of t.deletedPaths) e.deletedPaths.add(n);
    for (let n of t.runningDeletedPaths) e.runningDeletedPaths.add(n);
    if (t.visualizationActivity != null)
      for (let n of t.visualizationActivity.activities) {
        let r = e.visualizationActivitiesByPath.get(n.path);
        e.visualizationActivitiesByPath.set(n.path, {
          kind:
            r?.kind === `create` || n.kind === `create` ? `create` : `update`,
          isInProgress:
            r?.isInProgress === !0 || t.visualizationActivity.isInProgress,
        });
      }
    return;
  }
  if (t.type === `exec`) {
    if (
      (Eu(e, t.automaticApprovalReviewFailures),
      (e.commandCount += 1),
      t.visualizationActivityKind != null &&
        ((e.visualizationCommandKind =
          e.visualizationCommandKind === `create` ||
          t.visualizationActivityKind === `create`
            ? `create`
            : `update`),
        t.isInProgress
          ? (e.runningVisualizationCommandCount += 1)
          : (e.completedVisualizationCommandCount += 1)),
      t.isInProgress)
    ) {
      ((e.runningCommandCount += 1),
        t.createsFolder === !0 && (e.runningFolderCreationCommandCount += 1),
        t.searchesWeb === !0 && (e.runningWebSearchCommandCount += 1));
      return;
    }
    t.searchesWeb === !0 && (e.completedWebSearchCommandCount += 1);
    return;
  }
  if (t.type === `automatic-approval-review`) {
    Du(e, t);
    return;
  }
  if (t.type === `mcp-tool-call`) {
    if (
      (Eu(e, t.automaticApprovalReviewFailures),
      (e.mcpToolCallCount += 1),
      t.source != null)
    ) {
      let n = e.mcpToolCallSources.get(t.source.key);
      e.mcpToolCallSources.set(t.source.key, {
        logoUrl: t.source.logoUrl,
        logoUrlDark: t.source.logoUrlDark,
        name: t.source.name,
        nativeAppReference: t.source.nativeAppReference,
        count: (n?.count ?? 0) + 1,
        runningCount: (n?.runningCount ?? 0) + +!!t.isInProgress,
      });
    }
    return;
  }
  if (t.type === `web-search`) {
    ((e.webSearchCount += t.count),
      (e.runningWebSearchCount += t.runningCount));
    return;
  }
  return Dc(t);
}
function Tu(e) {
  let t = [...e.visualizationActivitiesByPath.values()],
    n =
      e.visualizationCommandKind != null || t.length > 0
        ? {
            kind:
              e.visualizationCommandKind === `create` ||
              t.some(({ kind: e }) => e === `create`)
                ? `create`
                : `update`,
            isInProgress:
              e.runningVisualizationCommandCount > 0 ||
              t.some(({ isInProgress: e }) => e),
          }
        : void 0;
  return {
    createdFileCount: e.createdPaths.size,
    runningCreatedFileCount: e.runningCreatedPaths.size,
    stoppedCreatedFileCount: e.stoppedCreatedPaths.size,
    runningCreatedLineCount: e.runningCreatedLineCount,
    changedLineCount: e.changedLineCount,
    editedFileCount: e.editedPaths.size,
    runningEditedFileCount: e.runningEditedPaths.size,
    deletedFileCount: e.deletedPaths.size,
    runningDeletedFileCount: e.runningDeletedPaths.size,
    exploredFileCount: e.exploredPaths.size,
    runningExploredFileCount: e.runningExploredPaths.size,
    loadedToolCount: e.loadedToolPaths.size,
    runningLoadedToolCount: e.runningLoadedToolPaths.size,
    searchCount: e.searchCount,
    runningSearchCount: e.runningSearchCount,
    listCount: e.listCount,
    runningListCount: e.runningListCount,
    deniedRequestCount: e.deniedRequestCount,
    timedOutRequestCount: e.timedOutRequestCount,
    commandCount: e.commandCount,
    runningCommandCount: e.runningCommandCount,
    completedWebSearchCommandCount: e.completedWebSearchCommandCount,
    runningFolderCreationCommandCount: e.runningFolderCreationCommandCount,
    visualizationActivity: n,
    completedVisualizationCommandCount: e.completedVisualizationCommandCount,
    runningVisualizationCommandCount: e.runningVisualizationCommandCount,
    runningWebSearchCommandCount: e.runningWebSearchCommandCount,
    mcpToolCallCount: e.mcpToolCallCount,
    mcpToolCallSources: [...e.mcpToolCallSources.entries()].map(([e, t]) => ({
      key: e,
      logoUrl: t.logoUrl,
      logoUrlDark: t.logoUrlDark,
      name: t.name,
      nativeAppReference: t.nativeAppReference,
      count: t.count,
      runningCount: t.runningCount,
    })),
    webSearchCount: e.webSearchCount,
    runningWebSearchCount: e.runningWebSearchCount,
  };
}
function Eu(e, t) {
  for (let n of t ?? []) Du(e, n);
}
function Du(e, t) {
  if (!e.automaticApprovalReviewFailureIds.has(t.id))
    switch ((e.automaticApprovalReviewFailureIds.add(t.id), t.status)) {
      case `denied`:
        e.deniedRequestCount += 1;
        return;
      case `timedOut`:
        e.timedOutRequestCount += 1;
        return;
    }
}
var Ou = e(() => {
  Oc();
});
function ku(e) {
  return e.replaceAll(/\uE200[^\uE201]*\uE201/g, ``);
}
var Au = e(() => {});
function ju(e) {
  if (!e.includes(`:::writing`)) return e;
  let t = [],
    n = !1,
    r = null;
  for (let i of e.split(`
`)) {
    let e = r != null;
    if (((r = Mu(r, i)), e || r != null)) {
      t.push(i);
      continue;
    }
    if (Nu.test(i)) {
      n = !0;
      continue;
    }
    if (n && Pu.test(i)) {
      n = !1;
      continue;
    }
    t.push(i);
  }
  return t.join(`
`);
}
function Mu(e, t) {
  let n = t.match(/^ {0,3}(`{3,}|~{3,})(.*)$/);
  if (n == null) return e;
  let r = n[1] ?? ``,
    i = r.startsWith("`") ? "`" : `~`;
  return e == null
    ? i === "`" && (n[2] ?? ``).includes("`")
      ? null
      : { length: r.length, marker: i }
    : i === e.marker && r.length >= e.length && (n[2] ?? ``).trim().length === 0
      ? null
      : e;
}
var Nu,
  Pu,
  Fu = e(() => {
    (Kt(),
      (Nu = RegExp(`^ {0,3}:::${Ht}(?:\\{.*)?\\s*$`)),
      (Pu = /^ {0,3}:::\s*$/));
  });
export {
  Ms as $,
  $r as $t,
  qc as A,
  Zi as At,
  Xs as B,
  ki as Bt,
  zl as C,
  mo as Ct,
  Dl as D,
  na as Dt,
  wl as E,
  pa as Et,
  Mc as F,
  ji as Ft,
  Qs as G,
  ii as Gt,
  ec as H,
  xi as Ht,
  W as I,
  Ai as It,
  Ds as J,
  ri as Jt,
  Es as K,
  ai as Kt,
  xc as L,
  Ni as Lt,
  Ac as M,
  Ki as Mt,
  Nc as N,
  Fi as Nt,
  Fc as O,
  ra as Ot,
  jc as P,
  Xi as Pt,
  Ns as Q,
  Qr as Qt,
  Ec as R,
  Ei as Rt,
  Il as S,
  _o as St,
  Fl as T,
  ua as Tt,
  $s as U,
  wi as Ut,
  Zs as V,
  vi as Vt,
  tc as W,
  _i as Wt,
  ks as X,
  ti as Xt,
  Os as Y,
  gi as Yt,
  As as Z,
  R as Zt,
  tu as _,
  Do as _t,
  yu as a,
  gs as at,
  au as b,
  bo as bt,
  vu as c,
  ds as ct,
  pu as d,
  No as dt,
  Zr as en,
  Ps as et,
  cu as f,
  ps as ft,
  ou as g,
  Oo as gt,
  su as h,
  Ao as ht,
  ku as i,
  ms as it,
  Lc as j,
  ta as jt,
  Pc as k,
  oa as kt,
  hu as l,
  Lo as lt,
  lu as m,
  jo as mt,
  ju as n,
  Wr as nn,
  bc as nt,
  bu as o,
  Cs as ot,
  fu as p,
  Mo as pt,
  Ts as q,
  ei as qt,
  Au as r,
  Ur as rn,
  hs as rt,
  Ou as s,
  fs as st,
  Fu as t,
  Gr as tn,
  js as tt,
  mu as u,
  Io as ut,
  ru as v,
  To as vt,
  Ol as w,
  go as wt,
  nu as x,
  vo as xt,
  eu as y,
  yo as yt,
  Ys as z,
  Ti as zt,
};
//# sourceMappingURL=app-initial~app-main~onboarding-page~hotkey-window-thread-page~quick-chat-window-page~chatg~f5p8e1kp-B1NDMoKY.js.map
