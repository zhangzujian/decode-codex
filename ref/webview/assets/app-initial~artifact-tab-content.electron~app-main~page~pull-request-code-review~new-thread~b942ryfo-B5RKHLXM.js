const __vite__mapDeps = (
  i,
  m = __vite__mapDeps,
  d = m.f ||
    (m.f = [
      "./ayu-dark-D4crUA5F.js",
      "./rolldown-runtime-BG2f4sTM.js",
      "./catppuccin-latte-zOcFwNaN.js",
      "./catppuccin-mocha-Cjutq3dD.js",
      "./dark-plus-BcK_Lxq4.js",
      "./dracula-CkmxuQCA.js",
      "./everforest-dark-DNwyXI-O.js",
      "./everforest-light-BgZujp1P.js",
      "./github-dark-default-B3U1wxKH.js",
      "./github-light-default-xyCLROFf.js",
      "./gruvbox-dark-medium-DzojUsna.js",
      "./gruvbox-light-medium-DLCqPW_M.js",
      "./light-plus-HJjakdiH.js",
      "./material-theme-darker-CPmP8lV-.js",
      "./monokai-DpEr2NbN.js",
      "./night-owl-BcTDS1K4.js",
      "./nord-C0MzpZym.js",
      "./one-dark-pro-FzVQBXsn.js",
      "./one-light-DHYBffPd.js",
      "./rose-pine-dawn-B9YdL2fA.js",
      "./rose-pine-moon-CUQmuvpy.js",
      "./solarized-dark-C1VH3Rq7.js",
      "./solarized-light-Ccw5Eg4G.js",
      "./tokyo-night-BQQ8aTW5.js",
      "./absolutely-dark-B54BBN-X.js",
      "./absolutely-light-CP_4VWV1.js",
      "./codex-dark-DgyInWLc.js",
      "./codex-light-CVyGr2nP.js",
      "./linear-dark-B6ksvH-w.js",
      "./linear-light-B89a5hM8.js",
      "./lobster-dark-C4twhOT5.js",
      "./matrix-dark-Duu_8jmb.js",
      "./notion-dark-oh-FVaYU.js",
      "./notion-light-Dyidta1Z.js",
      "./oscurange-Bbr-xYpZ.js",
      "./proof-light-C-6lYh5j.js",
      "./raycast-dark-CV9ttB_S.js",
      "./raycast-light-uOF9veQj.js",
      "./sentry-dark-D9jNDc5T.js",
      "./temple-dark-CPV4J_lA.js",
      "./vercel-dark-DqypS-8A.js",
      "./vercel-light-BkOrl59b.js",
      "./xcode-dark-Df4KCYjm.js",
      "./xcode-light-DoItkfXh.js",
      "./abap-DxuQO2b8.js",
      "./actionscript-3-CieH__f6.js",
      "./ada-CS8346Dn.js",
      "./angular-html-UdoV1con.js",
      "./angular-html-Biujys-T.js",
      "./html-BRrREXeW.js",
      "./javascript-D7u5fdtc.js",
      "./css-DwMVe6gT.js",
      "./angular-ts-vZLvkGuL.js",
      "./scss-Cecnb7_n.js",
      "./apache-Dlp9vCqD.js",
      "./apex-Cx5f9vbJ.js",
      "./apl-CyU4rL0N.js",
      "./xml-Di9eh4ZK.js",
      "./java-BL7Gys0X.js",
      "./json-CVGwZZsE.js",
      "./applescript-TGhSYpQm.js",
      "./ara-C4TgWDTj.js",
      "./asciidoc-BFA4dLe-.js",
      "./asm-CXw8Dlb9.js",
      "./astro-W4hA_sUN.js",
      "./typescript-L00BrwX1.js",
      "./postcss-DGpB8E3D.js",
      "./tsx-C3G-rhHV.js",
      "./awk-jhlAG1FL.js",
      "./ballerina-DZdMimCC.js",
      "./bat-ClNum6z5.js",
      "./beancount-Bti-eV3T.js",
      "./berry-C6Oy-9vm.js",
      "./bibtex-D2Zuw43D.js",
      "./bicep-BV5aqUmf.js",
      "./blade-BkFEOJDj.js",
      "./html-derivative-D-lfd1ER.js",
      "./sql-3h3DuCuD.js",
      "./bsl-D2t9I2_S.js",
      "./sdbl-w9KyeF0Z.js",
      "./c-BmyAN9uB.js",
      "./c-Bko5H9oW.js",
      "./cadence-ZZQD7dZZ.js",
      "./cairo-BGnxcrn3.js",
      "./python-B48f4hLL.js",
      "./clarity-Jx84T4hq.js",
      "./clojure-CJrKn15m.js",
      "./cmake-KgGHQiyL.js",
      "./cmake-D_LmDKCQ.js",
      "./cobol-DMB6nn2Q.js",
      "./codeowners-DdstKaNr.js",
      "./codeql-DelfTVmv.js",
      "./coffee-B5pcpOkO.js",
      "./common-lisp-BQFvTA-U.js",
      "./coq-C127Ok58.js",
      "./cpp-FyRC_YXp.js",
      "./cpp-DgpjnNOZ.js",
      "./regexp-CGA3ydyB.js",
      "./glsl-KNTSf2KY.js",
      "./crystal-D0O7CT8b.js",
      "./shellscript-BuUBicVT.js",
      "./csharp-dWOu_Z9v.js",
      "./csharp-C10UVsLW.js",
      "./css-V-D1sJxY.js",
      "./csv-C2W0euoY.js",
      "./csv-qlLYxb6A.js",
      "./cue-DAU6h1eV.js",
      "./cypher-BgkdepPV.js",
      "./d-979M2VZt.js",
      "./dart-D_H5G3KO.js",
      "./dax-CtLPlU6I.js",
      "./desktop-DCkZF_fi.js",
      "./diff-BzFlkiCH.js",
      "./diff-CFg07s4i.js",
      "./docker-B6hc43zL.js",
      "./dotenv-DCAY4dW3.js",
      "./dream-maker-BDscK0je.js",
      "./edge-CWv-0KHn.js",
      "./elixir-z9SSFN0q.js",
      "./elm-Dm6xryLg.js",
      "./emacs-lisp-08grgfz1.js",
      "./erb-3scvucIY.js",
      "./ruby-DpGIiVzp.js",
      "./haml-0D2asiiY.js",
      "./graphql-gb-QKuYj.js",
      "./jsx-Dxgazdu2.js",
      "./lua-Chx3Xp78.js",
      "./yaml-DxTXIO1-.js",
      "./erlang-Buwg4T7D.js",
      "./markdown-DtkKtvD3.js",
      "./fennel-CfcAYwTx.js",
      "./fish-B1FCMeeT.js",
      "./fluent-BYDic30O.js",
      "./fortran-fixed-form-BbbChIYF.js",
      "./fortran-free-form-n6UKBDPH.js",
      "./fortran-free-form-CqBp4yPH.js",
      "./fsharp-C5yVDSse.js",
      "./gdresource-DnXtqM4p.js",
      "./gdshader-D4QaaEDv.js",
      "./gdscript-CHXlrDAm.js",
      "./gdscript-CWUNS79S.js",
      "./gdshader-LwbvDd7e.js",
      "./genie-BZs60kAO.js",
      "./gherkin-HraT_eBV.js",
      "./git-commit-DNx_Ajsr.js",
      "./git-rebase-Cs3wX5ZZ.js",
      "./gleam-DrYb8_r9.js",
      "./glimmer-js-BM4JRk62.js",
      "./glimmer-ts-jgKbaQ7P.js",
      "./glsl-C2DGYntX.js",
      "./gnuplot-CY0pRbZS.js",
      "./go-Cq6UZW8L.js",
      "./go-D1UY6-lM.js",
      "./graphql-BZFKNm7t.js",
      "./groovy-91J0aXvO.js",
      "./hack-j5PJB0dN.js",
      "./haml-4JVWm-3n.js",
      "./handlebars-DhrgIUMA.js",
      "./haskell-CkTiDu-A.js",
      "./haxe-CEcCCwTA.js",
      "./haxe-lzgeyMpX.js",
      "./hcl-CRYOyEwg.js",
      "./hjson-CrPQ-eVJ.js",
      "./hlsl-DCUHJD93.js",
      "./hlsl-vOVLNhha.js",
      "./html-U70y7bkx.js",
      "./html-derivative-DIA0fB0y.js",
      "./http-oJLH9DwK.js",
      "./hurl-CnznnXyE.js",
      "./hxml-DQCR-9Aw.js",
      "./hy-DZzqSxRj.js",
      "./imba-Vbfs4RA8.js",
      "./ini-Dtvfewn7.js",
      "./java-D9QdBNwZ.js",
      "./javascript-DvJiI4BN.js",
      "./jinja-ClX0KWKp.js",
      "./jison-CI3yLa7S.js",
      "./json-mzGplh77.js",
      "./json5-DnR-vtQX.js",
      "./jsonc-ucncoNT8.js",
      "./jsonl-BAZgty2a.js",
      "./jsonnet-JwJoQ3yG.js",
      "./jssm-90TpF08_.js",
      "./jsx-CsrZUv5w.js",
      "./julia-DQDw9j-A.js",
      "./r-B2GDZmTG.js",
      "./kdl-DFPXMwwh.js",
      "./kotlin-DaCiEX6O.js",
      "./kusto-C0SYUvTL.js",
      "./latex-CunzpGV0.js",
      "./tex-sMvm-iQd.js",
      "./lean-C10ROJ-Q.js",
      "./less-CWKu4QOZ.js",
      "./less-DJmGSenT.js",
      "./liquid-CqLRGdm8.js",
      "./llvm--vqeDrbk.js",
      "./log-lPfpuUBt.js",
      "./logo-CNBzc-Lh.js",
      "./lua-BfJX8OWW.js",
      "./luau-BxUOJZlq.js",
      "./make-DrMRNqKV.js",
      "./markdown-D8taA4DT.js",
      "./marko-DUunA5fr.js",
      "./matlab-Cs6W9s25.js",
      "./mdc-BvBFWg9R.js",
      "./mdx-Cmvb6FXn.js",
      "./mermaid-CPoqeGkL.js",
      "./mipsasm-ChwQ2pdz.js",
      "./mojo-CL4ckX0C.js",
      "./move-3VCSOm88.js",
      "./narrat-CWyCaX3m.js",
      "./nextflow-CW2eZB5d.js",
      "./nginx-CGuqvTXw.js",
      "./nim-Bx41sJJC.js",
      "./nix-COLYZJH5.js",
      "./nushell-B5b4kHlf.js",
      "./objective-c-D4Gn-j4f.js",
      "./objective-cpp-DCnZ80Ja.js",
      "./ocaml-DSoT0tsl.js",
      "./openscad-sTPJecY0.js",
      "./pascal-BfFCEeSO.js",
      "./perl-DRNGkJyZ.js",
      "./php-u3PWnPKC.js",
      "./php-CBDkyndG.js",
      "./pkl-CrvJXQTz.js",
      "./plsql-CM0MXXr-.js",
      "./po-CgJm0ldQ.js",
      "./polar-BMtpq-uK.js",
      "./postcss-CuYKKrDz.js",
      "./powerquery-GgO_-WMD.js",
      "./powershell-0yM8JrML.js",
      "./prisma-7Yu0zXx7.js",
      "./prolog-qL3LRv5Y.js",
      "./proto-BYEI3gE7.js",
      "./pug-BNlsll0M.js",
      "./puppet-CpAVMDpy.js",
      "./purescript-DDMKqqLy.js",
      "./python-CMSGC7OP.js",
      "./qml-Cyv_rvGe.js",
      "./qmldir-CfVhRUnj.js",
      "./qss-BuA5M-EL.js",
      "./r-DD3XXSfB.js",
      "./racket-y96I3IL-.js",
      "./raku-FGrSszDT.js",
      "./razor-D88oIyzu.js",
      "./reg-oQghgDHR.js",
      "./regexp-BYAVqG0d.js",
      "./rel-DWKNrgZc.js",
      "./riscv-DTL-geNI.js",
      "./rosmsg-DXrlBQzq.js",
      "./rst-Dw_wtorp.js",
      "./ruby-Bgo5X_b6.js",
      "./rust-BT01z89n.js",
      "./sas-UJ1zf83f.js",
      "./sass-BmBNR1GI.js",
      "./scala-DCUSuZ-K.js",
      "./scheme-CYTG-RX4.js",
      "./scss-DFvoAVuL.js",
      "./sdbl-CsXK_PRI.js",
      "./shaderlab-DpWszKJf.js",
      "./shellscript-D55t4LDA.js",
      "./shellsession-BBePPcZ9.js",
      "./smalltalk-CL5G0l5h.js",
      "./solidity-D76fNJ2_.js",
      "./soy-BGNSUI2T.js",
      "./sparql-D92gkpiV.js",
      "./turtle-DYWuF75l.js",
      "./splunk-mrXB0zn8.js",
      "./sql-DD1EB_Zf.js",
      "./ssh-config-COCT1jnt.js",
      "./stata-Dd8qx77H.js",
      "./stylus-B_7mYYre.js",
      "./stylus-DmqSIfJW.js",
      "./svelte-T8pEnV9v.js",
      "./swift-Cp25HjPd.js",
      "./system-verilog-b-Yh1wgm.js",
      "./systemd-C4zIfg6u.js",
      "./talonscript-tJHY__4w.js",
      "./tasl-IFhozMeK.js",
      "./tcl-DAr4ELhm.js",
      "./templ-CSSxJxfL.js",
      "./terraform-hw6CZhLp.js",
      "./tex-1KfC2u42.js",
      "./toml-CWhaK_1f.js",
      "./ts-tags-C_MJ9MMp.js",
      "./tsv-3sTulsqv.js",
      "./tsx-CXV4yfgF.js",
      "./turtle-nsQACJ8b.js",
      "./twig-ChlV5HWI.js",
      "./typescript-Buwqmzid.js",
      "./typespec-DwbBEZXS.js",
      "./typst-CQZAUkim.js",
      "./v-BRalsuVD.js",
      "./vala-UwwN6RG6.js",
      "./vb-Dbm737n_.js",
      "./verilog-C0XdXfNK.js",
      "./vhdl-53H-MRF4.js",
      "./viml-Duu1KGxi.js",
      "./vue-CBIoSZEr.js",
      "./vue-BZIMd9x2.js",
      "./vue-html-3rupQSQT.js",
      "./vue-vine-CvDrzVSo.js",
      "./vyper-DOfDsHjD.js",
      "./wasm-B5C64WKa.js",
      "./wenyan-ClaHfQiT.js",
      "./wgsl-Bxe-ns-a.js",
      "./wikitext-CLwu6_Yj.js",
      "./wit-DIUDPGBY.js",
      "./wolfram-CZyRGOns.js",
      "./xml-BpVTI6cz.js",
      "./xsl-Ddphdz43.js",
      "./yaml-T0Eno5aE.js",
      "./zenscript-C6hv8CXE.js",
      "./zig-CAEu7uPI.js",
      "./andromeeda-Cc0sG9OA.js",
      "./aurora-x-DUM8kaRP.js",
      "./ayu-dark-DDRREaEs.js",
      "./catppuccin-frappe-DZfuiHN5.js",
      "./catppuccin-latte-CffoFhl6.js",
      "./catppuccin-macchiato-C9UmGfOh.js",
      "./catppuccin-mocha-BbX4ZWN8.js",
      "./dark-plus-DqYPLMqm.js",
      "./dracula-Dli1Qj-E.js",
      "./dracula-soft-CJCKwMWA.js",
      "./everforest-dark-C3_GWKG6.js",
      "./everforest-light-DEr4yUvA.js",
      "./github-dark-jYxIfJAK.js",
      "./github-dark-default-CWz2Ao5C.js",
      "./github-dark-dimmed-BYVv3soK.js",
      "./github-dark-high-contrast-CFUFE7p6.js",
      "./github-light-BHPhwNco.js",
      "./github-light-default-C11W-_SM.js",
      "./github-light-high-contrast-D1nUAVED.js",
      "./gruvbox-dark-hard-gf8b3rIv.js",
      "./gruvbox-dark-medium-CAOl-TJK.js",
      "./gruvbox-dark-soft-D1_RRDnf.js",
      "./gruvbox-light-hard-_Jf3GyJv.js",
      "./gruvbox-light-medium-BL2GKYVe.js",
      "./gruvbox-light-soft-4hjdjbPW.js",
      "./houston-DNjEfTXn.js",
      "./kanagawa-dragon-Xy--2nev.js",
      "./kanagawa-lotus-BqZTu85J.js",
      "./kanagawa-wave-D8AtmMaZ.js",
      "./laserwave-CEBVhjHt.js",
      "./light-plus-BBLgA5ch.js",
      "./material-theme-B4fAqikf.js",
      "./material-theme-darker-ClTJk-QW.js",
      "./material-theme-lighter-DYH9t9Wb.js",
      "./material-theme-ocean-Dq627U5V.js",
      "./material-theme-palenight-BP4N-rZS.js",
      "./min-dark-NsHtCGvn.js",
      "./min-light-2O6cMpAZ.js",
      "./monokai-CKPe4T2y.js",
      "./night-owl-BeiaVnS1.js",
      "./nord-BwfDtjt_.js",
      "./one-dark-pro-DUxcR8F2.js",
      "./one-light-BAzbWKLN.js",
      "./plastic-Bmm031RA.js",
      "./poimandres-CjJE8fFa.js",
      "./red-JZJ4ESLC.js",
      "./rose-pine-s8Ev0U3t.js",
      "./rose-pine-dawn-CB-gBMzR.js",
      "./rose-pine-moon-CSDlPKbG.js",
      "./slack-dark-x1wBI9DE.js",
      "./slack-ochin-BeFBHAe0.js",
      "./snazzy-light-B9GZiL8C.js",
      "./solarized-dark-BlXYcUpI.js",
      "./solarized-light-CKahZpeh.js",
      "./synthwave-84-D33cPQ9Z.js",
      "./tokyo-night-DxpII2Zf.js",
      "./vesper-SMH2DGX_.js",
      "./vitesse-black-Ce7yGFhR.js",
      "./vitesse-dark-DSf3AXh4.js",
      "./vitesse-light-nf2LzGA0.js",
      "./wasm-CAhMFZE6.js",
      "./pierre-dark-D1f1axGh.js",
      "./pierre-dark-soft-CK0OEJfm.js",
      "./pierre-dark-vibrant-JtxWxkjr.js",
      "./pierre-dark-protanopia-deuteranopia-io05zghc.js",
      "./pierre-dark-tritanopia-7Blk6S2W.js",
      "./pierre-light-DEv95pIt.js",
      "./pierre-light-soft-TPLnnocw.js",
      "./pierre-light-vibrant-Fd0fBJFl.js",
      "./pierre-light-protanopia-deuteranopia-BXyOu9LV.js",
      "./pierre-light-tritanopia-B0Toikcn.js",
      "./andromeeda-BjHkedMI.js",
      "./aurora-x-CDuvSF5C.js",
      "./ayu-light-DFsrtA8V.js",
      "./ayu-mirage-Dkr7cBIo.js",
      "./catppuccin-frappe-DSdDdD8r.js",
      "./catppuccin-macchiato-BCZfgPsl.js",
      "./dracula-soft-CsaHCQck.js",
      "./github-dark-DzYeK2Us.js",
      "./github-dark-dimmed-CTa0Kgn1.js",
      "./github-dark-high-contrast-C9braW9T.js",
      "./github-light-Bp5ibGIl.js",
      "./github-light-high-contrast-B3zN_Jiy.js",
      "./gruvbox-dark-hard-CVhtCRxN.js",
      "./gruvbox-dark-soft-BoMFM66L.js",
      "./gruvbox-light-hard-yIR7_agU.js",
      "./gruvbox-light-soft-D_Lt_2kp.js",
      "./horizon-BI4McSDO.js",
      "./horizon-bright-B-BSNqvV.js",
      "./houston-C60H8_Lv.js",
      "./kanagawa-dragon-CNKo0Sep.js",
      "./kanagawa-lotus-B1CXMJRL.js",
      "./kanagawa-wave-BlH46o84.js",
      "./laserwave-Bt9rc1h_.js",
      "./material-theme-BeT-D04D.js",
      "./material-theme-lighter-rGvkHBph.js",
      "./material-theme-ocean-BcMG6ksT.js",
      "./material-theme-palenight-DSd1sHBQ.js",
      "./min-dark-Cj3j3fK8.js",
      "./min-light-BejKsFnZ.js",
      "./night-owl-light-D0d-QV9l.js",
      "./plastic-qd5GJRza.js",
      "./poimandres-DsFFYPV2.js",
      "./red-BpkevTrS.js",
      "./rose-pine-CZuangkl.js",
      "./slack-dark-C2q7QxSE.js",
      "./slack-ochin-D26VOPqZ.js",
      "./snazzy-light-BgO3kAsz.js",
      "./synthwave-84-CbTL_48L.js",
      "./vesper-DZQUcIy4.js",
      "./vitesse-black-BsJLk9y4.js",
      "./vitesse-dark-Cogd6TIZ.js",
      "./vitesse-light-DeTfENL4.js",
    ]),
) => i.map((i) => d[i]);
import { n as e, r as t, s as n } from "./rolldown-runtime-BG2f4sTM.js";
import {
  dn as r,
  l as i,
  m as a,
  un as o,
} from "./app-initial~avatarOverlayCompositionSurface~index-9fQ9wihu~index-BFCcxPM5~mapbox-gl-DVWlwqb~kppdhley-Hrn9ylUK.js";
import {
  It as s,
  Mt as c,
  w as l,
} from "./app-initial~avatarOverlayCompositionSurface~artifact-tab-content.electron~notebook-preview-~ngwudnyz-DEp-3H1N.js";
import {
  o as u,
  s as d,
} from "./app-initial~avatarOverlayCompositionSurface~index~index-9fQ9wihu~index-BFCcxPM5~mapbox-gl-D~ch0na97m-DGJ4C9j8.js";
import {
  $ as f,
  Y as p,
  et as m,
  nt as h,
  q as g,
  tt as _,
} from "./app-initial~artifact-tab-content.electron~notebook-preview-panel~app-main~pull-request-rout~e3eqjujm-ColXhptb.js";
function v(e, t) {
  for (let [n, r] of Object.entries(t)) {
    if (r == null) {
      e.style.getPropertyValue(n).length > 0 && e.style.removeProperty(n);
      continue;
    }
    e.style.getPropertyValue(n) !== r && e.style.setProperty(n, r);
  }
}
var y = e(() => {});
function b(e, t) {
  let n = he[t];
  return {
    accent: O(e?.accent) ?? n.accent,
    contrast: x(e?.contrast, n.contrast),
    fonts: w(e?.fonts),
    ink: O(e?.ink) ?? n.ink,
    opaqueWindows: e?.opaqueWindows ?? n.opaqueWindows,
    semanticColors: T(e?.semanticColors, n.semanticColors),
    surface: O(e?.surface) ?? n.surface,
  };
}
function x(e, t) {
  return e == null || Number.isNaN(e)
    ? t
    : Math.min(100, Math.max(0, Math.round(e)));
}
function S(e, t) {
  let n = ee(e, t);
  return E(n, t === `light` ? D(n) : ne(n));
}
function C(e, t, n) {
  (e.classList.toggle(`electron-dark`, n === `dark`),
    e.classList.toggle(`electron-light`, n === `light`),
    v(e, S(t, n)));
}
function ee(e, t) {
  let n = re(e.contrast, t),
    r = oe(e.surface),
    i = oe(e.ink);
  return {
    accent: oe(e.accent),
    contrast: n,
    editorBackground: t === `light` ? j(r, _e, 0.12) : j(r, i, 0.07),
    ink: i,
    surface: r,
    surfaceUnder: ie(e, r, i, t),
    theme: e,
    variant: t,
  };
}
function w(e) {
  return { code: te(e?.code), ui: te(e?.ui) };
}
function T(e, t) {
  return {
    diffAdded: O(e?.diffAdded) ?? t.diffAdded,
    diffRemoved: O(e?.diffRemoved) ?? t.diffRemoved,
    skill: O(e?.skill) ?? t.skill,
  };
}
function te(e) {
  let t = e?.trim() ?? ``;
  return t.length > 0 ? t : null;
}
function E(e, t) {
  return {
    "--codex-base-accent": e.theme.accent,
    "--codex-base-contrast": String(e.theme.contrast),
    "--codex-base-ink": e.theme.ink,
    "--codex-base-surface": e.theme.surface,
    "--color-accent-blue": e.theme.accent,
    "--color-accent-purple": e.theme.semanticColors.skill,
    "--color-background-accent": t.accentBackground,
    "--color-background-accent-active": t.accentBackgroundActive,
    "--color-background-accent-hover": t.accentBackgroundHover,
    "--color-background-button-primary": t.buttonPrimaryBackground,
    "--color-background-button-primary-active": t.buttonPrimaryBackgroundActive,
    "--color-background-button-primary-hover": t.buttonPrimaryBackgroundHover,
    "--color-background-button-primary-inactive":
      t.buttonPrimaryBackgroundInactive,
    "--color-background-button-secondary": t.buttonSecondaryBackground,
    "--color-background-button-secondary-active":
      t.buttonSecondaryBackgroundActive,
    "--color-background-button-secondary-hover":
      t.buttonSecondaryBackgroundHover,
    "--color-background-button-secondary-inactive":
      t.buttonSecondaryBackgroundInactive,
    "--color-background-button-tertiary": t.buttonTertiaryBackground,
    "--color-background-button-tertiary-active":
      t.buttonTertiaryBackgroundActive,
    "--color-background-button-tertiary-hover": t.buttonTertiaryBackgroundHover,
    "--color-background-control": t.controlBackground,
    "--color-background-control-opaque": t.controlBackgroundOpaque,
    "--color-background-editor-opaque": pe(e.editorBackground),
    "--color-background-elevated-primary": t.elevatedPrimary,
    "--color-background-elevated-primary-opaque": t.elevatedPrimaryOpaque,
    "--color-background-elevated-secondary": t.elevatedSecondary,
    "--color-background-elevated-secondary-opaque": t.elevatedSecondaryOpaque,
    "--color-background-panel": ae(e),
    "--color-background-surface": e.theme.surface,
    "--color-background-surface-under": e.surfaceUnder,
    "--color-border": t.border,
    "--color-border-focus": t.borderFocus,
    "--color-border-heavy": t.borderHeavy,
    "--color-border-light": t.borderLight,
    "--color-decoration-added": e.theme.semanticColors.diffAdded,
    "--color-decoration-deleted": e.theme.semanticColors.diffRemoved,
    "--color-editor-added": k(
      oe(e.theme.semanticColors.diffAdded),
      e.variant === `light` ? 0.15 : 0.23,
    ),
    "--color-editor-deleted": k(
      oe(e.theme.semanticColors.diffRemoved),
      e.variant === `light` ? 0.15 : 0.23,
    ),
    "--color-icon-accent": t.iconAccent,
    "--color-icon-primary": t.iconPrimary,
    "--color-icon-secondary": t.iconSecondary,
    "--color-icon-tertiary": t.iconTertiary,
    "--color-simple-scrim": t.simpleScrim,
    "--color-text-accent": t.textAccent,
    "--color-text-on-accent": pe(le(e.accent)),
    "--color-text-button-primary": t.textButtonPrimary,
    "--color-text-button-secondary": t.textButtonSecondary,
    "--color-text-button-tertiary": t.textButtonTertiary,
    "--color-text-foreground": t.textForeground,
    "--color-text-foreground-secondary": t.textForegroundSecondary,
    "--color-text-foreground-tertiary": t.textForegroundTertiary,
  };
}
function D(e) {
  let t = j(e.surface, _e, 0.09 + e.contrast * 0.04),
    n = j(e.surface, _e, 0.08 + e.contrast * 0.08),
    r = j(e.surface, _e, 0.16 + e.contrast * 0.12);
  return {
    accentBackground: A(e.surface, e.accent, 0.11 + e.contrast * 0.04),
    accentBackgroundActive: A(e.surface, e.accent, 0.13 + e.contrast * 0.05),
    accentBackgroundHover: A(e.surface, e.accent, 0.12 + e.contrast * 0.045),
    border: k(e.ink, 0.06 + e.contrast * 0.04),
    borderFocus: e.theme.accent,
    borderHeavy: k(e.ink, 0.09 + e.contrast * 0.06),
    borderLight: k(e.ink, 0.04 + e.contrast * 0.02),
    buttonPrimaryBackground: e.theme.ink,
    buttonPrimaryBackgroundActive: k(e.ink, 0.1 + e.contrast * 0.12),
    buttonPrimaryBackgroundHover: k(e.ink, 0.05 + e.contrast * 0.06),
    buttonPrimaryBackgroundInactive: k(e.ink, 0.18 + e.contrast * 0.14),
    buttonSecondaryBackground: k(e.ink, 0.04 + e.contrast * 0.02),
    buttonSecondaryBackgroundActive: k(e.ink, 0.03 + e.contrast * 0.02),
    buttonSecondaryBackgroundHover: k(e.ink, 0.04 + e.contrast * 0.03),
    buttonSecondaryBackgroundInactive: k(e.ink, 0.01 + e.contrast * 0.02),
    buttonTertiaryBackground: k(e.ink, 0),
    buttonTertiaryBackgroundActive: k(e.ink, 0.16 + e.contrast * 0.08),
    buttonTertiaryBackgroundHover: k(e.ink, 0.08 + e.contrast * 0.04),
    controlBackground: k(t, 0.96),
    controlBackgroundOpaque: pe(t),
    elevatedPrimary: k(r, 0.96),
    elevatedPrimaryOpaque: pe(r),
    elevatedSecondary: k(n, 0.96),
    elevatedSecondaryOpaque: pe(n),
    iconAccent: e.theme.accent,
    iconPrimary: e.theme.ink,
    iconSecondary: k(e.ink, 0.65 + e.contrast * 0.1),
    iconTertiary: k(e.ink, 0.45 + e.contrast * 0.1),
    simpleScrim: k(ge, 0.08 + e.contrast * 0.04),
    textAccent: e.theme.accent,
    textButtonPrimary: e.theme.surface,
    textButtonSecondary: e.theme.ink,
    textButtonTertiary: k(e.ink, 0.45 + e.contrast * 0.1),
    textForeground: e.theme.ink,
    textForegroundSecondary: k(e.ink, 0.65 + e.contrast * 0.1),
    textForegroundTertiary: k(e.ink, 0.45 + e.contrast * 0.1),
  };
}
function ne(e) {
  let t = j(e.surface, e.ink, 0.06 + e.contrast * 0.05),
    n = j(e.accent, _e, 0.3 + e.contrast * 0.15),
    r = j(e.surface, ge, 0.38 + e.contrast * 0.12),
    i = j(e.surface, e.ink, 0.08 + e.contrast * 0.08);
  return {
    accentBackground: A(ge, e.accent, 0.2 + e.contrast * 0.08),
    accentBackgroundActive: A(ge, e.accent, 0.22 + e.contrast * 0.12),
    accentBackgroundHover: A(ge, e.accent, 0.21 + e.contrast * 0.1),
    border: k(e.ink, 0.06 + e.contrast * 0.04),
    borderFocus: k(n, 0.7 + e.contrast * 0.1),
    borderHeavy: k(e.ink, 0.12 + e.contrast * 0.06),
    borderLight: k(e.ink, 0.03 + e.contrast * 0.02),
    buttonPrimaryBackground: pe(r),
    buttonPrimaryBackgroundActive: k(e.ink, 0.07 + e.contrast * 0.05),
    buttonPrimaryBackgroundHover: k(e.ink, 0.04 + e.contrast * 0.03),
    buttonPrimaryBackgroundInactive: k(e.ink, 0.02 + e.contrast * 0.02),
    buttonSecondaryBackground: k(e.ink, 0.04 + e.contrast * 0.02),
    buttonSecondaryBackgroundActive: k(e.ink, 0.09 + e.contrast * 0.05),
    buttonSecondaryBackgroundHover: k(e.ink, 0.06 + e.contrast * 0.03),
    buttonSecondaryBackgroundInactive: k(e.ink, 0.02 + e.contrast * 0.03),
    buttonTertiaryBackground: k(e.ink, 0.02 + e.contrast * 0.015),
    buttonTertiaryBackgroundActive: k(e.ink, 0.07 + e.contrast * 0.05),
    buttonTertiaryBackgroundHover: k(e.ink, 0.05 + e.contrast * 0.03),
    controlBackground: k(t, 0.96),
    controlBackgroundOpaque: pe(t),
    elevatedPrimary: k(i, 0.96),
    elevatedPrimaryOpaque: pe(i),
    elevatedSecondary: k(e.ink, 0.02 + e.contrast * 0.02),
    elevatedSecondaryOpaque: A(e.surface, e.ink, 0.04 + e.contrast * 0.05),
    iconAccent: pe(n),
    iconPrimary: k(e.ink, 0.82 + e.contrast * 0.14),
    iconSecondary: k(e.ink, 0.65 + e.contrast * 0.1),
    iconTertiary: k(e.ink, 0.45 + e.contrast * 0.1),
    simpleScrim: k(e.ink, 0.08 + e.contrast * 0.04),
    textAccent: pe(n),
    textButtonPrimary: pe(r),
    textButtonSecondary: A(e.ink, e.surface, 0.7 + e.contrast * 0.1),
    textButtonTertiary: k(e.ink, 0.45 + e.contrast * 0.1),
    textForeground: e.theme.ink,
    textForegroundSecondary: k(e.ink, 0.65 + e.contrast * 0.1),
    textForegroundTertiary: k(e.ink, 0.42 + e.contrast * 0.13),
  };
}
function O(e) {
  if (e == null) return;
  let t = e.trim();
  if (/^#[0-9a-fA-F]{6}$/.test(t)) return t.toLowerCase();
}
function re(e, t) {
  let n = ye[t],
    r = n / 100,
    i = e / 100 + ((e - n) / 60) * be;
  return e <= n ? i : r + (i - r) * xe;
}
function ie(e, t, n, r) {
  let i = ye[r],
    a = Se[r] + (e.contrast - i) * Ce[r];
  return r === `light` ? A(t, n, a) : A(t, ge, a);
}
function ae(e) {
  let t = e.variant === `light` ? _e : e.ink;
  return A(e.surface, t, we[e.variant] + e.contrast * Te[e.variant]);
}
function oe(e) {
  let t = e.slice(1);
  return {
    blue: Number.parseInt(t.slice(4, 6), 16),
    green: Number.parseInt(t.slice(2, 4), 16),
    red: Number.parseInt(t.slice(0, 2), 16),
  };
}
function k(e, t) {
  return `rgba(${e.red}, ${e.green}, ${e.blue}, ${ce(t)})`;
}
function A(e, t, n) {
  return fe(j(e, t, n));
}
function j(e, t, n) {
  let r = Math.min(1, Math.max(0, n));
  return {
    blue: se(e.blue, t.blue, r),
    green: se(e.green, t.green, r),
    red: se(e.red, t.red, r),
  };
}
function se(e, t, n) {
  return Math.round(e + (t - e) * n);
}
function ce(e) {
  return Math.min(1, Math.max(0, e))
    .toFixed(3)
    .replace(/0+$/, ``)
    .replace(/\.$/, ``);
}
function le(e) {
  return fe(e) === he.light.accent ? _e : ue(e) > ve ? ge : _e;
}
function ue(e) {
  return de(e.red) * 0.2126 + de(e.green) * 0.7152 + de(e.blue) * 0.0722;
}
function de(e) {
  let t = e / 255;
  return t <= 0.04045 ? t / 12.92 : ((t + 0.055) / 1.055) ** 2.4;
}
function fe(e) {
  return `#${me(e.red)}${me(e.green)}${me(e.blue)}`;
}
function pe(e) {
  return `rgb(${e.red}, ${e.green}, ${e.blue})`;
}
function me(e) {
  return e.toString(16).padStart(2, `0`);
}
var he,
  ge,
  _e,
  ve,
  ye,
  be,
  xe,
  Se,
  Ce,
  we,
  Te,
  Ee = e(() => {
    (y(),
      (he = {
        dark: {
          accent: `#339cff`,
          contrast: 60,
          fonts: { code: null, ui: null },
          ink: `#ffffff`,
          opaqueWindows: !1,
          semanticColors: {
            diffAdded: `#40c977`,
            diffRemoved: `#fa423e`,
            skill: `#ad7bf9`,
          },
          surface: `#181818`,
        },
        light: {
          accent: `#339cff`,
          contrast: 45,
          fonts: { code: null, ui: null },
          ink: `#1a1c1f`,
          opaqueWindows: !1,
          semanticColors: {
            diffAdded: `#00a240`,
            diffRemoved: `#ba2623`,
            skill: `#924ff7`,
          },
          surface: `#ffffff`,
        },
      }),
      (ge = { blue: 0, green: 0, red: 0 }),
      (_e = { blue: 255, green: 255, red: 255 }),
      (ve = 0.179),
      (ye = { dark: he.dark.contrast, light: he.light.contrast }),
      (be = 0.7),
      (xe = 2),
      (Se = { dark: 0.16, light: 0.04 }),
      (Ce = { dark: 0.0015, light: 0.0012 }),
      (we = { dark: 0.03, light: 0.18 }),
      (Te = { dark: 0.03, light: 0.008 }));
  });
function De(e, t) {
  let n = Pe(t);
  return (
    n.find((t) => t.id === e) || (n.find((e) => e.id === tt) ?? n[0] ?? vt[0])
  );
}
function Oe(e) {
  return Pe(e).sort((e, t) => gt.compare(e.label, t.label));
}
function ke() {
  return vt
    .flatMap((e) => Object.values(e.registrationByVariant))
    .filter((e) => e != null);
}
function Ae(e) {
  return yt.some((t) => t === e);
}
function je(e, t) {
  let n = De(e, t).registrationByVariant[t];
  if (n == null) throw Error(`Missing ${t} code theme registration`);
  return n;
}
async function Me(e, t) {
  return je(e, t).loadChromeThemeSeed();
}
function Ne(e, t) {
  return t == null ? !0 : e.registrationByVariant[t] != null;
}
function Pe(e) {
  return vt.filter((t) => Ne(t, e));
}
function Fe(e) {
  return _t[e]();
}
function M(e, t, n) {
  let r = n.dark,
    i = n.light;
  return N(e, t, {
    dark: r == null ? void 0 : { load: () => Fe(r), name: r },
    light: i == null ? void 0 : { load: () => Fe(i), name: i },
  });
}
function N(e, t, n) {
  let r = {},
    i = n.dark,
    a = n.light;
  return (
    i != null && (r.dark = Ie(i.name, `dark`, i.load)),
    a != null && (r.light = Ie(a.name, `light`, a.load)),
    { id: e, label: t, registrationByVariant: r }
  );
}
function Ie(e, t, n) {
  let r = null,
    i = async () => (
      (r ??= n().then((e) =>
        typeof e == `object` && e && `default` in e ? e.default : e,
      )),
      r
    );
  return {
    load: i,
    loadChromeThemeSeed: () => i().then((e) => Re(Le(e, t), e.chromeTheme)),
    name: e,
  };
}
function Le(e, t) {
  let n = he[t],
    r = Be(e.colors, nt) ?? n.surface,
    i = Be(e.colors, rt) ?? n.ink,
    a = Ve(e, r, i) ?? n.accent;
  return { accent: a, ink: i, semanticColors: ze(e, r, i, a, t), surface: r };
}
function Re(e, t) {
  return t == null
    ? e
    : {
        ...e,
        ...t,
        fonts: t.fonts == null ? e.fonts : { ...e.fonts, ...t.fonts },
        semanticColors:
          t.semanticColors == null
            ? e.semanticColors
            : { ...e.semanticColors, ...t.semanticColors },
      };
}
function ze(e, t, n, r, i) {
  let a = he[i];
  return {
    diffAdded:
      Be(e.colors, at) ?? Ue(e, t, n, ut, dt) ?? a.semanticColors.diffAdded,
    diffRemoved:
      Be(e.colors, ot) ?? Ue(e, t, n, ft, pt) ?? a.semanticColors.diffRemoved,
    skill:
      Be(e.colors, st) ??
      Ue(e, t, n, mt, ht) ??
      (!qe(r, t) && !qe(r, n) ? r : a.semanticColors.skill),
  };
}
function Be(e, t) {
  if (e != null)
    for (let n of t) {
      let t = Ge(e[n]);
      if (t != null) return t;
    }
}
function Ve(e, t, n) {
  for (let r of it) {
    let i = Ge(e.colors?.[r], { minimumAlpha: ct, minimumChromaticRange: lt });
    if (i != null && !qe(i, t) && !qe(i, n)) return i;
  }
  let r,
    i = -1;
  for (let a of He(e)) {
    let e = Ge(a.settings?.foreground, {
      minimumAlpha: ct,
      minimumChromaticRange: lt,
    });
    if (e == null || qe(e, t) || qe(e, n)) continue;
    let o = Je(e, t, n);
    o > i && ((r = e), (i = o));
  }
  return r;
}
function He(e) {
  return [...(e.tokenColors ?? []), ...(e.settings ?? [])];
}
function Ue(e, t, n, r, i) {
  let a,
    o = -1;
  for (let s of We(e)) {
    if (qe(s, t) || qe(s, n)) continue;
    let e = Ke(s);
    if (e == null) continue;
    let c = Xe(e);
    if (c == null || !Ze(c, r)) continue;
    let l = Je(s, t, n) - Qe(c, i) * 2;
    l > o && ((a = s), (o = l));
  }
  return a;
}
function We(e) {
  let t = Object.values(e.colors ?? {}),
    n = He(e).map((e) => e.settings?.foreground),
    r = new Set();
  for (let e of [...t, ...n]) {
    let t = Ge(e, { minimumAlpha: ct, minimumChromaticRange: lt });
    t != null && r.add(t);
  }
  return [...r];
}
function Ge(e, t) {
  let n = Ke(e);
  if (n == null) return;
  let { minimumAlpha: r = 0.98, minimumChromaticRange: i = 0 } = t ?? {};
  if (!(n.alpha < r) && !($e(n) < i)) return et(n);
}
function Ke(e) {
  if (e == null) return;
  let t = e.trim();
  if (!/^#[0-9a-fA-F]{6}([0-9a-fA-F]{2})?$/.test(t)) return;
  let n = t.length === 9 ? t.slice(7, 9) : `ff`;
  return {
    alpha: Number.parseInt(n, 16) / 255,
    blue: Number.parseInt(t.slice(5, 7), 16),
    green: Number.parseInt(t.slice(3, 5), 16),
    red: Number.parseInt(t.slice(1, 3), 16),
  };
}
function qe(e, t) {
  let n = Ke(e),
    r = Ke(t);
  return n == null || r == null ? !1 : Ye(n, r) < 42;
}
function Je(e, t, n) {
  let r = Ke(e),
    i = Ke(t),
    a = Ke(n);
  return r == null || i == null || a == null
    ? 0
    : $e(r) + Ye(r, i) / 4 + Ye(r, a) / 4;
}
function Ye(e, t) {
  return Math.sqrt(
    (e.red - t.red) ** 2 + (e.green - t.green) ** 2 + (e.blue - t.blue) ** 2,
  );
}
function Xe(e) {
  let t = e.red / 255,
    n = e.green / 255,
    r = e.blue / 255,
    i = Math.max(t, n, r),
    a = i - Math.min(t, n, r);
  if (a === 0) return null;
  let o;
  return (
    (o =
      i === t
        ? (((n - r) / a) % 6) * 60
        : i === n
          ? ((r - t) / a + 2) * 60
          : ((t - n) / a + 4) * 60),
    (o + 360) % 360
  );
}
function Ze(e, t) {
  return t.min <= t.max ? e >= t.min && e <= t.max : e >= t.min || e <= t.max;
}
function Qe(e, t) {
  let n = Math.abs(e - t);
  return Math.min(n, 360 - n);
}
function $e(e) {
  return Math.max(e.red, e.green, e.blue) - Math.min(e.red, e.green, e.blue);
}
function et(e) {
  return `#${e.red.toString(16).padStart(2, `0`)}${e.green.toString(16).padStart(2, `0`)}${e.blue.toString(16).padStart(2, `0`)}`;
}
var tt,
  nt,
  rt,
  it,
  at,
  ot,
  st,
  ct,
  lt,
  ut,
  dt,
  ft,
  pt,
  mt,
  ht,
  gt,
  _t,
  vt,
  yt,
  bt = e(() => {
    (l(),
      Ee(),
      d(),
      (tt = s.CODEX),
      (nt = [
        `editor.background`,
        `sideBar.background`,
        `editorGroupHeader.tabsBackground`,
        `panel.background`,
        `activityBar.background`,
      ]),
      (rt = [
        `editor.foreground`,
        `sideBarTitle.foreground`,
        `sideBar.foreground`,
        `foreground`,
      ]),
      (it = [
        `activityBarBadge.background`,
        `textLink.foreground`,
        `editorCursor.foreground`,
        `focusBorder`,
        `button.background`,
        `activityBar.activeBorder`,
      ]),
      (at = [
        `gitDecoration.addedResourceForeground`,
        `gitDecoration.untrackedResourceForeground`,
        `terminal.ansiGreen`,
        `terminal.ansiBrightGreen`,
      ]),
      (ot = [
        `gitDecoration.deletedResourceForeground`,
        `terminal.ansiRed`,
        `terminal.ansiBrightRed`,
      ]),
      (st = [
        `charts.purple`,
        `terminal.ansiMagenta`,
        `terminal.ansiBrightMagenta`,
      ]),
      (ct = 0.45),
      (lt = 24),
      (ut = { max: 170, min: 80 }),
      (dt = 125),
      (ft = { max: 15, min: 345 }),
      (pt = 0),
      (mt = { max: 320, min: 210 }),
      (ht = 265),
      (gt = new Intl.Collator(void 0, { sensitivity: `base` })),
      (_t = {
        "ayu-dark"() {
          return u(
            () => import(`./ayu-dark-D4crUA5F.js`),
            __vite__mapDeps([0, 1]),
            import.meta.url,
          );
        },
        "catppuccin-latte"() {
          return u(
            () => import(`./catppuccin-latte-zOcFwNaN.js`),
            __vite__mapDeps([2, 1]),
            import.meta.url,
          );
        },
        "catppuccin-mocha"() {
          return u(
            () => import(`./catppuccin-mocha-Cjutq3dD.js`),
            __vite__mapDeps([3, 1]),
            import.meta.url,
          );
        },
        "dark-plus"() {
          return u(
            () => import(`./dark-plus-BcK_Lxq4.js`),
            __vite__mapDeps([4, 1]),
            import.meta.url,
          );
        },
        dracula() {
          return u(
            () => import(`./dracula-CkmxuQCA.js`),
            __vite__mapDeps([5, 1]),
            import.meta.url,
          );
        },
        "everforest-dark"() {
          return u(
            () => import(`./everforest-dark-DNwyXI-O.js`),
            __vite__mapDeps([6, 1]),
            import.meta.url,
          );
        },
        "everforest-light"() {
          return u(
            () => import(`./everforest-light-BgZujp1P.js`),
            __vite__mapDeps([7, 1]),
            import.meta.url,
          );
        },
        "github-dark-default"() {
          return u(
            () => import(`./github-dark-default-B3U1wxKH.js`),
            __vite__mapDeps([8, 1]),
            import.meta.url,
          );
        },
        "github-light-default"() {
          return u(
            () => import(`./github-light-default-xyCLROFf.js`),
            __vite__mapDeps([9, 1]),
            import.meta.url,
          );
        },
        "gruvbox-dark-medium"() {
          return u(
            () => import(`./gruvbox-dark-medium-DzojUsna.js`),
            __vite__mapDeps([10, 1]),
            import.meta.url,
          );
        },
        "gruvbox-light-medium"() {
          return u(
            () => import(`./gruvbox-light-medium-DLCqPW_M.js`),
            __vite__mapDeps([11, 1]),
            import.meta.url,
          );
        },
        "light-plus"() {
          return u(
            () => import(`./light-plus-HJjakdiH.js`),
            __vite__mapDeps([12, 1]),
            import.meta.url,
          );
        },
        "material-theme-darker"() {
          return u(
            () => import(`./material-theme-darker-CPmP8lV-.js`),
            __vite__mapDeps([13, 1]),
            import.meta.url,
          );
        },
        monokai() {
          return u(
            () => import(`./monokai-DpEr2NbN.js`),
            __vite__mapDeps([14, 1]),
            import.meta.url,
          );
        },
        "night-owl"() {
          return u(
            () => import(`./night-owl-BcTDS1K4.js`),
            __vite__mapDeps([15, 1]),
            import.meta.url,
          );
        },
        nord() {
          return u(
            () => import(`./nord-C0MzpZym.js`),
            __vite__mapDeps([16, 1]),
            import.meta.url,
          );
        },
        "one-dark-pro"() {
          return u(
            () => import(`./one-dark-pro-FzVQBXsn.js`),
            __vite__mapDeps([17, 1]),
            import.meta.url,
          );
        },
        "one-light"() {
          return u(
            () => import(`./one-light-DHYBffPd.js`),
            __vite__mapDeps([18, 1]),
            import.meta.url,
          );
        },
        "rose-pine-dawn"() {
          return u(
            () => import(`./rose-pine-dawn-B9YdL2fA.js`),
            __vite__mapDeps([19, 1]),
            import.meta.url,
          );
        },
        "rose-pine-moon"() {
          return u(
            () => import(`./rose-pine-moon-CUQmuvpy.js`),
            __vite__mapDeps([20, 1]),
            import.meta.url,
          );
        },
        "solarized-dark"() {
          return u(
            () => import(`./solarized-dark-C1VH3Rq7.js`),
            __vite__mapDeps([21, 1]),
            import.meta.url,
          );
        },
        "solarized-light"() {
          return u(
            () => import(`./solarized-light-Ccw5Eg4G.js`),
            __vite__mapDeps([22, 1]),
            import.meta.url,
          );
        },
        "tokyo-night"() {
          return u(
            () => import(`./tokyo-night-BQQ8aTW5.js`),
            __vite__mapDeps([23, 1]),
            import.meta.url,
          );
        },
      }),
      (vt = [
        M(s.AYU, `Ayu`, { dark: `ayu-dark` }),
        M(s.CATPPUCCIN, `Catppuccin`, {
          dark: `catppuccin-mocha`,
          light: `catppuccin-latte`,
        }),
        N(s.ABSOLUTELY, `Absolutely`, {
          dark: {
            load: () =>
              u(
                () => import(`./absolutely-dark-B54BBN-X.js`),
                __vite__mapDeps([24, 1]),
                import.meta.url,
              ),
            name: `Absolutely Dark`,
          },
          light: {
            load: () =>
              u(
                () => import(`./absolutely-light-CP_4VWV1.js`),
                __vite__mapDeps([25, 1]),
                import.meta.url,
              ),
            name: `Absolutely Light`,
          },
        }),
        N(s.CODEX, `Codex`, {
          dark: {
            load: () =>
              u(
                () => import(`./codex-dark-DgyInWLc.js`),
                __vite__mapDeps([26, 1]),
                import.meta.url,
              ),
            name: `Codex Dark`,
          },
          light: {
            load: () =>
              u(
                () => import(`./codex-light-CVyGr2nP.js`),
                __vite__mapDeps([27, 1]),
                import.meta.url,
              ),
            name: `Codex Light`,
          },
        }),
        M(s.DRACULA, `Dracula`, { dark: `dracula` }),
        M(s.EVERFOREST, `Everforest`, {
          dark: `everforest-dark`,
          light: `everforest-light`,
        }),
        M(s.GITHUB, `GitHub`, {
          dark: `github-dark-default`,
          light: `github-light-default`,
        }),
        M(s.GRUVBOX, `Gruvbox`, {
          dark: `gruvbox-dark-medium`,
          light: `gruvbox-light-medium`,
        }),
        N(s.LINEAR, `Linear`, {
          dark: {
            load: () =>
              u(
                () => import(`./linear-dark-B6ksvH-w.js`),
                __vite__mapDeps([28, 1]),
                import.meta.url,
              ),
            name: `Linear Dark`,
          },
          light: {
            load: () =>
              u(
                () => import(`./linear-light-B89a5hM8.js`),
                __vite__mapDeps([29, 1]),
                import.meta.url,
              ),
            name: `Linear Light`,
          },
        }),
        N(s.LOBSTER, `Lobster`, {
          dark: {
            load: () =>
              u(
                () => import(`./lobster-dark-C4twhOT5.js`),
                __vite__mapDeps([30, 1]),
                import.meta.url,
              ),
            name: `Lobster Dark`,
          },
        }),
        M(s.MATERIAL, `Material`, { dark: `material-theme-darker` }),
        N(s.MATRIX, `Matrix`, {
          dark: {
            load: () =>
              u(
                () => import(`./matrix-dark-Duu_8jmb.js`),
                __vite__mapDeps([31, 1]),
                import.meta.url,
              ),
            name: `Matrix Dark`,
          },
        }),
        M(s.MONOKAI, `Monokai`, { dark: `monokai` }),
        M(s.NIGHT_OWL, `Night Owl`, { dark: `night-owl` }),
        M(s.NORD, `Nord`, { dark: `nord` }),
        N(s.NOTION, `Notion`, {
          dark: {
            load: () =>
              u(
                () => import(`./notion-dark-oh-FVaYU.js`),
                __vite__mapDeps([32, 1]),
                import.meta.url,
              ),
            name: `Notion Dark`,
          },
          light: {
            load: () =>
              u(
                () => import(`./notion-light-Dyidta1Z.js`),
                __vite__mapDeps([33, 1]),
                import.meta.url,
              ),
            name: `Notion Light`,
          },
        }),
        N(s.OSCURANGE, `Oscurange`, {
          dark: {
            load: () =>
              u(
                () => import(`./oscurange-Bbr-xYpZ.js`),
                __vite__mapDeps([34, 1]),
                import.meta.url,
              ),
            name: `Oscurange`,
          },
        }),
        M(s.ONE, `One`, { dark: `one-dark-pro`, light: `one-light` }),
        N(s.PROOF, `Proof`, {
          light: {
            load: () =>
              u(
                () => import(`./proof-light-C-6lYh5j.js`),
                __vite__mapDeps([35, 1]),
                import.meta.url,
              ),
            name: `Proof Light`,
          },
        }),
        N(s.RAYCAST, `Raycast`, {
          dark: {
            load: () =>
              u(
                () => import(`./raycast-dark-CV9ttB_S.js`),
                __vite__mapDeps([36, 1]),
                import.meta.url,
              ),
            name: `Raycast Dark`,
          },
          light: {
            load: () =>
              u(
                () => import(`./raycast-light-uOF9veQj.js`),
                __vite__mapDeps([37, 1]),
                import.meta.url,
              ),
            name: `Raycast Light`,
          },
        }),
        M(s.ROSE_PINE, `Rose Pine`, {
          dark: `rose-pine-moon`,
          light: `rose-pine-dawn`,
        }),
        N(s.SENTRY, `Sentry`, {
          dark: {
            load: () =>
              u(
                () => import(`./sentry-dark-D9jNDc5T.js`),
                __vite__mapDeps([38, 1]),
                import.meta.url,
              ),
            name: `Sentry Dark`,
          },
        }),
        M(s.SOLARIZED, `Solarized`, {
          dark: `solarized-dark`,
          light: `solarized-light`,
        }),
        M(s.TOKYO_NIGHT, `Tokyo Night`, { dark: `tokyo-night` }),
        N(s.TEMPLE, `Temple`, {
          dark: {
            load: () =>
              u(
                () => import(`./temple-dark-CPV4J_lA.js`),
                __vite__mapDeps([39, 1]),
                import.meta.url,
              ),
            name: `Temple Dark`,
          },
        }),
        N(s.VERCEL, `Vercel`, {
          dark: {
            load: () =>
              u(
                () => import(`./vercel-dark-DqypS-8A.js`),
                __vite__mapDeps([40, 1]),
                import.meta.url,
              ),
            name: `Vercel Dark`,
          },
          light: {
            load: () =>
              u(
                () => import(`./vercel-light-BkOrl59b.js`),
                __vite__mapDeps([41, 1]),
                import.meta.url,
              ),
            name: `Vercel Light`,
          },
        }),
        M(s.VSCODE_PLUS, `VS Code Plus`, {
          dark: `dark-plus`,
          light: `light-plus`,
        }),
        N(s.XCODE, `Xcode`, {
          dark: {
            load: () =>
              u(
                () => import(`./xcode-dark-Df4KCYjm.js`),
                __vite__mapDeps([42, 1]),
                import.meta.url,
              ),
            name: `Xcode Dark`,
          },
          light: {
            load: () =>
              u(
                () => import(`./xcode-light-DoItkfXh.js`),
                __vite__mapDeps([43, 1]),
                import.meta.url,
              ),
            name: `Xcode Light`,
          },
        }),
      ]),
      (yt = vt.map((e) => e.id)));
  }),
  xt,
  St,
  Ct,
  wt,
  Tt,
  Et,
  Dt,
  Ot,
  kt,
  At,
  jt,
  Mt,
  Nt,
  P,
  Pt,
  Ft,
  It,
  Lt,
  Rt,
  zt,
  Bt,
  Vt,
  Ht,
  Ut,
  Wt,
  Gt,
  F = e(() => {
    ((xt = `diffs-container`),
      (St = (() => {
        try {
          return !1;
        } catch {
          return !1;
        }
      })()),
      (Ct = /(?=^From [a-f0-9]+ .+$)/m),
      (wt = /(?=^diff --git)/gm),
      (Tt = /(?<=\n)/),
      (Et = /^(---|\+\+\+)\s+([^\t\r\n]+)/),
      (Dt = /^(---|\+\+\+)\s+[ab]\/([^\t\r\n]+)/),
      (Ot = /^diff --git (?:"a\/(.+?)"|a\/(.+?)) (?:"b\/(.+?)"|b\/(.+?))$/),
      (kt = /^index ([0-9a-f]+)\.\.([0-9a-f]+)(?: (\d+))?$/i),
      (At = `header-prefix`),
      (jt = `header-filename-suffix`),
      (Mt = `header-metadata`),
      (Nt = `header-custom`),
      (P = { dark: `pierre-dark`, light: `pierre-light` }),
      (Pt = `data-theme-css`),
      (Ft = `data-unsafe-css`),
      (It = `data-diffs-scrollbar-measure`),
      (Lt = `--diffs-scrollbar-gutter-measured`),
      (Rt = 1e5),
      (zt = {
        hunkLineCount: 50,
        lineHeight: 20,
        diffHeaderHeight: 44,
        spacing: 8,
      }),
      (Bt = { ...zt, hunkLineCount: 1 }),
      (Vt = { paddingTop: 8, paddingBottom: 8, gap: 8 }),
      (Ht = { omega: 0.015, positionEpsilon: 0.5, velocityEpsilon: 0.05 }),
      (Ut = Object.freeze({ fromStart: 0, fromEnd: 0 })),
      (Wt = {
        startingLine: 0,
        totalLines: 1 / 0,
        bufferBefore: 0,
        bufferAfter: 0,
      }),
      (Gt = {
        startingLine: 0,
        totalLines: 0,
        bufferBefore: 0,
        bufferAfter: 0,
      }));
  });
function Kt(e) {
  (Yt.add(e), (Xt ??= requestAnimationFrame(Jt)));
}
function qt(e) {
  (Yt.delete(e),
    Yt.size === 0 && Xt != null && (cancelAnimationFrame(Xt), (Xt = null)));
}
function Jt(e) {
  let t = new Set(Yt);
  Yt.clear();
  for (let n of t)
    try {
      n(e);
    } catch (e) {
      console.error(e);
    }
  Xt = Yt.size > 0 ? requestAnimationFrame(Jt) : null;
}
var Yt,
  Xt,
  Zt = e(() => {
    ((Yt = new Set()), (Xt = null));
  });
function Qt(e, t, n) {
  if (e === t || e == null || t == null) return e === t;
  let r = new Set(n),
    i = Object.keys(e),
    a = new Set(Object.keys(t));
  for (let n of i)
    if ((a.delete(n), !r.has(n) && (!(n in t) || e[n] !== t[n]))) return !1;
  for (let e of Array.from(a)) if (!r.has(e)) return !1;
  return !0;
}
var $t = e(() => {});
function en(e, t) {
  return e == null || t == null || typeof e == `string` || typeof t == `string`
    ? e === t
    : e.dark === t.dark && e.light === t.light;
}
var tn = e(() => {});
function nn(e, t) {
  let n = e?.theme ?? P,
    r = t?.theme ?? P,
    i = rn(e),
    a = rn(t);
  return en(n, r) && Qt(e, t, [`theme`, `parseDiffOptions`]) && Qt(i, a);
}
function rn(e) {
  if (e != null && `parseDiffOptions` in e) return e.parseDiffOptions;
}
var an = e(() => {
  (F(), $t(), tn());
});
function on(e, t) {
  return (
    e?.start === t?.start &&
    e?.end === t?.end &&
    e?.side === t?.side &&
    e?.endSide === t?.endSide
  );
}
var sn = e(() => {});
function cn({
  scrollTop: e,
  scrollHeight: t,
  height: n,
  fitPerfectly: r = !1,
  fitPerfectlyOverscroll: i = 0,
  overscrollSize: a,
}) {
  let o = n + a * 2,
    s = r ? n + i * 2 : o;
  if (((t = Math.max(t, s)), o >= t || r)) {
    let n = Math.max(e - i, 0),
      r = Math.min(e + s, t);
    return { top: n, bottom: Math.max(r, n) };
  }
  let c = e + n / 2 - o / 2,
    l = c + o;
  return (
    c < 0 && (c = 0),
    l > t && (l = t),
    (c = Math.floor(Math.max(c, 0))),
    { top: c, bottom: Math.ceil(Math.max(Math.min(l, t), c)) }
  );
}
var ln = e(() => {});
function un() {
  return typeof window > `u` || typeof window.matchMedia != `function`
    ? !1
    : window.matchMedia(`(prefers-reduced-motion: reduce)`).matches;
}
var dn = e(() => {});
function fn(e, t) {
  return (
    e?.cacheKey === t?.cacheKey &&
    e?.contents === t?.contents &&
    e?.name === t?.name &&
    e?.lang === t?.lang
  );
}
var pn = e(() => {});
function mn(e) {
  return { type: `text`, value: e };
}
function I({ tagName: e, children: t = [], properties: n = {} }) {
  return { type: `element`, tagName: e, properties: n, children: t };
}
function hn({ name: e, width: t = 16, height: n = 16, properties: r }) {
  return I({
    tagName: `svg`,
    properties: { width: t, height: n, viewBox: `0 0 16 16`, ...r },
    children: [
      I({ tagName: `use`, properties: { href: `#${e.replace(/^#/, ``)}` } }),
    ],
  });
}
function gn(e) {
  let t = e.children[0];
  for (; t != null;) {
    if (t.type === `element` && t.tagName === `code`) return t;
    t = `children` in t ? t.children[0] : null;
  }
}
function _n(e) {
  return I({ tagName: `div`, properties: { "data-gutter": `` }, children: e });
}
function vn(e, t, n, r = {}) {
  return I({
    tagName: `div`,
    properties: {
      "data-line-type": e,
      "data-column-number": t,
      "data-line-index": n,
      ...r,
    },
    children:
      t == null
        ? void 0
        : [
            I({
              tagName: `span`,
              properties: { "data-line-number-content": `` },
              children: [mn(`${t}`)],
            }),
          ],
  });
}
function yn(e, t, n) {
  return I({
    tagName: `div`,
    properties: {
      "data-gutter-buffer": t,
      "data-buffer-size": n,
      "data-line-type": t === `annotation` ? void 0 : e,
      style:
        t === `annotation`
          ? `grid-row: span ${n};`
          : `grid-row: span ${n};min-height:calc(${n} * 1lh);`,
    },
  });
}
var L = e(() => {});
function bn() {
  return I({
    tagName: `button`,
    properties: { "data-utility-button": ``, type: `button` },
    children: [
      hn({ name: `diffs-icon-plus`, properties: { "data-icon": `` } }),
    ],
  });
}
var xn = e(() => {
  L();
});
function Sn(e, t) {
  return e.lineNumber === t.lineNumber && e.side === t.side;
}
var Cn = e(() => {}),
  wn,
  Tn = e(() => {
    wn = [
      `area`,
      `base`,
      `basefont`,
      `bgsound`,
      `br`,
      `col`,
      `command`,
      `embed`,
      `frame`,
      `hr`,
      `image`,
      `img`,
      `input`,
      `keygen`,
      `link`,
      `meta`,
      `param`,
      `source`,
      `track`,
      `wbr`,
    ];
  }),
  En,
  Dn = e(() => {
    ((En = class {
      constructor(e, t, n) {
        ((this.normal = t), (this.property = e), n && (this.space = n));
      }
    }),
      (En.prototype.normal = {}),
      (En.prototype.property = {}),
      (En.prototype.space = void 0));
  });
function On(e, t) {
  let n = {},
    r = {};
  for (let t of e) (Object.assign(n, t.property), Object.assign(r, t.normal));
  return new En(n, r, t);
}
var kn = e(() => {
  Dn();
});
function An(e) {
  return e.toLowerCase();
}
var jn = e(() => {}),
  R,
  Mn = e(() => {
    ((R = class {
      constructor(e, t) {
        ((this.attribute = t), (this.property = e));
      }
    }),
      (R.prototype.attribute = ``),
      (R.prototype.booleanish = !1),
      (R.prototype.boolean = !1),
      (R.prototype.commaOrSpaceSeparated = !1),
      (R.prototype.commaSeparated = !1),
      (R.prototype.defined = !1),
      (R.prototype.mustUseProperty = !1),
      (R.prototype.number = !1),
      (R.prototype.overloadedBoolean = !1),
      (R.prototype.property = ``),
      (R.prototype.spaceSeparated = !1),
      (R.prototype.space = void 0));
  }),
  Nn = t({
    boolean: () => z,
    booleanish: () => B,
    commaOrSpaceSeparated: () => U,
    commaSeparated: () => Ln,
    number: () => V,
    overloadedBoolean: () => In,
    spaceSeparated: () => H,
  });
function Pn() {
  return 2 ** ++Fn;
}
var Fn,
  z,
  B,
  In,
  V,
  H,
  Ln,
  U,
  Rn = e(() => {
    ((Fn = 0),
      (z = Pn()),
      (B = Pn()),
      (In = Pn()),
      (V = Pn()),
      (H = Pn()),
      (Ln = Pn()),
      (U = Pn()));
  });
function zn(e, t, n) {
  n && (e[t] = n);
}
var Bn,
  Vn,
  Hn = e(() => {
    (Mn(),
      Rn(),
      (Bn = Object.keys(Nn)),
      (Vn = class extends R {
        constructor(e, t, n, r) {
          let i = -1;
          if ((super(e, t), zn(this, `space`, r), typeof n == `number`))
            for (; ++i < Bn.length;) {
              let e = Bn[i];
              zn(this, Bn[i], (n & Nn[e]) === Nn[e]);
            }
        }
      }),
      (Vn.prototype.defined = !0));
  });
function Un(e) {
  let t = {},
    n = {};
  for (let [r, i] of Object.entries(e.properties)) {
    let a = new Vn(r, e.transform(e.attributes || {}, r), i, e.space);
    (e.mustUseProperty &&
      e.mustUseProperty.includes(r) &&
      (a.mustUseProperty = !0),
      (t[r] = a),
      (n[An(r)] = r),
      (n[An(a.attribute)] = r));
  }
  return new En(t, n, e.space);
}
var Wn = e(() => {
    (jn(), Hn(), Dn());
  }),
  Gn,
  Kn = e(() => {
    (Wn(),
      Rn(),
      (Gn = Un({
        properties: {
          ariaActiveDescendant: null,
          ariaAtomic: B,
          ariaAutoComplete: null,
          ariaBusy: B,
          ariaChecked: B,
          ariaColCount: V,
          ariaColIndex: V,
          ariaColSpan: V,
          ariaControls: H,
          ariaCurrent: null,
          ariaDescribedBy: H,
          ariaDetails: null,
          ariaDisabled: B,
          ariaDropEffect: H,
          ariaErrorMessage: null,
          ariaExpanded: B,
          ariaFlowTo: H,
          ariaGrabbed: B,
          ariaHasPopup: null,
          ariaHidden: B,
          ariaInvalid: null,
          ariaKeyShortcuts: null,
          ariaLabel: null,
          ariaLabelledBy: H,
          ariaLevel: V,
          ariaLive: null,
          ariaModal: B,
          ariaMultiLine: B,
          ariaMultiSelectable: B,
          ariaOrientation: null,
          ariaOwns: H,
          ariaPlaceholder: null,
          ariaPosInSet: V,
          ariaPressed: B,
          ariaReadOnly: B,
          ariaRelevant: null,
          ariaRequired: B,
          ariaRoleDescription: H,
          ariaRowCount: V,
          ariaRowIndex: V,
          ariaRowSpan: V,
          ariaSelected: B,
          ariaSetSize: V,
          ariaSort: null,
          ariaValueMax: V,
          ariaValueMin: V,
          ariaValueNow: V,
          ariaValueText: null,
          role: null,
        },
        transform(e, t) {
          return t === `role` ? t : `aria-` + t.slice(4).toLowerCase();
        },
      })));
  });
function qn(e, t) {
  return t in e ? e[t] : t;
}
var Jn = e(() => {});
function Yn(e, t) {
  return qn(e, t.toLowerCase());
}
var Xn = e(() => {
    Jn();
  }),
  Zn,
  Qn = e(() => {
    (Xn(),
      Wn(),
      Rn(),
      (Zn = Un({
        attributes: {
          acceptcharset: `accept-charset`,
          classname: `class`,
          htmlfor: `for`,
          httpequiv: `http-equiv`,
        },
        mustUseProperty: [`checked`, `multiple`, `muted`, `selected`],
        properties: {
          abbr: null,
          accept: Ln,
          acceptCharset: H,
          accessKey: H,
          action: null,
          allow: null,
          allowFullScreen: z,
          allowPaymentRequest: z,
          allowUserMedia: z,
          alt: null,
          as: null,
          async: z,
          autoCapitalize: null,
          autoComplete: H,
          autoFocus: z,
          autoPlay: z,
          blocking: H,
          capture: null,
          charSet: null,
          checked: z,
          cite: null,
          className: H,
          cols: V,
          colSpan: null,
          content: null,
          contentEditable: B,
          controls: z,
          controlsList: H,
          coords: V | Ln,
          crossOrigin: null,
          data: null,
          dateTime: null,
          decoding: null,
          default: z,
          defer: z,
          dir: null,
          dirName: null,
          disabled: z,
          download: In,
          draggable: B,
          encType: null,
          enterKeyHint: null,
          fetchPriority: null,
          form: null,
          formAction: null,
          formEncType: null,
          formMethod: null,
          formNoValidate: z,
          formTarget: null,
          headers: H,
          height: V,
          hidden: In,
          high: V,
          href: null,
          hrefLang: null,
          htmlFor: H,
          httpEquiv: H,
          id: null,
          imageSizes: null,
          imageSrcSet: null,
          inert: z,
          inputMode: null,
          integrity: null,
          is: null,
          isMap: z,
          itemId: null,
          itemProp: H,
          itemRef: H,
          itemScope: z,
          itemType: H,
          kind: null,
          label: null,
          lang: null,
          language: null,
          list: null,
          loading: null,
          loop: z,
          low: V,
          manifest: null,
          max: null,
          maxLength: V,
          media: null,
          method: null,
          min: null,
          minLength: V,
          multiple: z,
          muted: z,
          name: null,
          nonce: null,
          noModule: z,
          noValidate: z,
          onAbort: null,
          onAfterPrint: null,
          onAuxClick: null,
          onBeforeMatch: null,
          onBeforePrint: null,
          onBeforeToggle: null,
          onBeforeUnload: null,
          onBlur: null,
          onCancel: null,
          onCanPlay: null,
          onCanPlayThrough: null,
          onChange: null,
          onClick: null,
          onClose: null,
          onContextLost: null,
          onContextMenu: null,
          onContextRestored: null,
          onCopy: null,
          onCueChange: null,
          onCut: null,
          onDblClick: null,
          onDrag: null,
          onDragEnd: null,
          onDragEnter: null,
          onDragExit: null,
          onDragLeave: null,
          onDragOver: null,
          onDragStart: null,
          onDrop: null,
          onDurationChange: null,
          onEmptied: null,
          onEnded: null,
          onError: null,
          onFocus: null,
          onFormData: null,
          onHashChange: null,
          onInput: null,
          onInvalid: null,
          onKeyDown: null,
          onKeyPress: null,
          onKeyUp: null,
          onLanguageChange: null,
          onLoad: null,
          onLoadedData: null,
          onLoadedMetadata: null,
          onLoadEnd: null,
          onLoadStart: null,
          onMessage: null,
          onMessageError: null,
          onMouseDown: null,
          onMouseEnter: null,
          onMouseLeave: null,
          onMouseMove: null,
          onMouseOut: null,
          onMouseOver: null,
          onMouseUp: null,
          onOffline: null,
          onOnline: null,
          onPageHide: null,
          onPageShow: null,
          onPaste: null,
          onPause: null,
          onPlay: null,
          onPlaying: null,
          onPopState: null,
          onProgress: null,
          onRateChange: null,
          onRejectionHandled: null,
          onReset: null,
          onResize: null,
          onScroll: null,
          onScrollEnd: null,
          onSecurityPolicyViolation: null,
          onSeeked: null,
          onSeeking: null,
          onSelect: null,
          onSlotChange: null,
          onStalled: null,
          onStorage: null,
          onSubmit: null,
          onSuspend: null,
          onTimeUpdate: null,
          onToggle: null,
          onUnhandledRejection: null,
          onUnload: null,
          onVolumeChange: null,
          onWaiting: null,
          onWheel: null,
          open: z,
          optimum: V,
          pattern: null,
          ping: H,
          placeholder: null,
          playsInline: z,
          popover: null,
          popoverTarget: null,
          popoverTargetAction: null,
          poster: null,
          preload: null,
          readOnly: z,
          referrerPolicy: null,
          rel: H,
          required: z,
          reversed: z,
          rows: V,
          rowSpan: V,
          sandbox: H,
          scope: null,
          scoped: z,
          seamless: z,
          selected: z,
          shadowRootClonable: z,
          shadowRootDelegatesFocus: z,
          shadowRootMode: null,
          shape: null,
          size: V,
          sizes: null,
          slot: null,
          span: V,
          spellCheck: B,
          src: null,
          srcDoc: null,
          srcLang: null,
          srcSet: null,
          start: V,
          step: null,
          style: null,
          tabIndex: V,
          target: null,
          title: null,
          translate: null,
          type: null,
          typeMustMatch: z,
          useMap: null,
          value: B,
          width: V,
          wrap: null,
          writingSuggestions: null,
          align: null,
          aLink: null,
          archive: H,
          axis: null,
          background: null,
          bgColor: null,
          border: V,
          borderColor: null,
          bottomMargin: V,
          cellPadding: null,
          cellSpacing: null,
          char: null,
          charOff: null,
          classId: null,
          clear: null,
          code: null,
          codeBase: null,
          codeType: null,
          color: null,
          compact: z,
          declare: z,
          event: null,
          face: null,
          frame: null,
          frameBorder: null,
          hSpace: V,
          leftMargin: V,
          link: null,
          longDesc: null,
          lowSrc: null,
          marginHeight: V,
          marginWidth: V,
          noResize: z,
          noHref: z,
          noShade: z,
          noWrap: z,
          object: null,
          profile: null,
          prompt: null,
          rev: null,
          rightMargin: V,
          rules: null,
          scheme: null,
          scrolling: B,
          standby: null,
          summary: null,
          text: null,
          topMargin: V,
          valueType: null,
          version: null,
          vAlign: null,
          vLink: null,
          vSpace: V,
          allowTransparency: null,
          autoCorrect: null,
          autoSave: null,
          disablePictureInPicture: z,
          disableRemotePlayback: z,
          prefix: null,
          property: null,
          results: V,
          security: null,
          unselectable: null,
        },
        space: `html`,
        transform: Yn,
      })));
  }),
  $n,
  er = e(() => {
    (Jn(),
      Wn(),
      Rn(),
      ($n = Un({
        attributes: {
          accentHeight: `accent-height`,
          alignmentBaseline: `alignment-baseline`,
          arabicForm: `arabic-form`,
          baselineShift: `baseline-shift`,
          capHeight: `cap-height`,
          className: `class`,
          clipPath: `clip-path`,
          clipRule: `clip-rule`,
          colorInterpolation: `color-interpolation`,
          colorInterpolationFilters: `color-interpolation-filters`,
          colorProfile: `color-profile`,
          colorRendering: `color-rendering`,
          crossOrigin: `crossorigin`,
          dataType: `datatype`,
          dominantBaseline: `dominant-baseline`,
          enableBackground: `enable-background`,
          fillOpacity: `fill-opacity`,
          fillRule: `fill-rule`,
          floodColor: `flood-color`,
          floodOpacity: `flood-opacity`,
          fontFamily: `font-family`,
          fontSize: `font-size`,
          fontSizeAdjust: `font-size-adjust`,
          fontStretch: `font-stretch`,
          fontStyle: `font-style`,
          fontVariant: `font-variant`,
          fontWeight: `font-weight`,
          glyphName: `glyph-name`,
          glyphOrientationHorizontal: `glyph-orientation-horizontal`,
          glyphOrientationVertical: `glyph-orientation-vertical`,
          hrefLang: `hreflang`,
          horizAdvX: `horiz-adv-x`,
          horizOriginX: `horiz-origin-x`,
          horizOriginY: `horiz-origin-y`,
          imageRendering: `image-rendering`,
          letterSpacing: `letter-spacing`,
          lightingColor: `lighting-color`,
          markerEnd: `marker-end`,
          markerMid: `marker-mid`,
          markerStart: `marker-start`,
          navDown: `nav-down`,
          navDownLeft: `nav-down-left`,
          navDownRight: `nav-down-right`,
          navLeft: `nav-left`,
          navNext: `nav-next`,
          navPrev: `nav-prev`,
          navRight: `nav-right`,
          navUp: `nav-up`,
          navUpLeft: `nav-up-left`,
          navUpRight: `nav-up-right`,
          onAbort: `onabort`,
          onActivate: `onactivate`,
          onAfterPrint: `onafterprint`,
          onBeforePrint: `onbeforeprint`,
          onBegin: `onbegin`,
          onCancel: `oncancel`,
          onCanPlay: `oncanplay`,
          onCanPlayThrough: `oncanplaythrough`,
          onChange: `onchange`,
          onClick: `onclick`,
          onClose: `onclose`,
          onCopy: `oncopy`,
          onCueChange: `oncuechange`,
          onCut: `oncut`,
          onDblClick: `ondblclick`,
          onDrag: `ondrag`,
          onDragEnd: `ondragend`,
          onDragEnter: `ondragenter`,
          onDragExit: `ondragexit`,
          onDragLeave: `ondragleave`,
          onDragOver: `ondragover`,
          onDragStart: `ondragstart`,
          onDrop: `ondrop`,
          onDurationChange: `ondurationchange`,
          onEmptied: `onemptied`,
          onEnd: `onend`,
          onEnded: `onended`,
          onError: `onerror`,
          onFocus: `onfocus`,
          onFocusIn: `onfocusin`,
          onFocusOut: `onfocusout`,
          onHashChange: `onhashchange`,
          onInput: `oninput`,
          onInvalid: `oninvalid`,
          onKeyDown: `onkeydown`,
          onKeyPress: `onkeypress`,
          onKeyUp: `onkeyup`,
          onLoad: `onload`,
          onLoadedData: `onloadeddata`,
          onLoadedMetadata: `onloadedmetadata`,
          onLoadStart: `onloadstart`,
          onMessage: `onmessage`,
          onMouseDown: `onmousedown`,
          onMouseEnter: `onmouseenter`,
          onMouseLeave: `onmouseleave`,
          onMouseMove: `onmousemove`,
          onMouseOut: `onmouseout`,
          onMouseOver: `onmouseover`,
          onMouseUp: `onmouseup`,
          onMouseWheel: `onmousewheel`,
          onOffline: `onoffline`,
          onOnline: `ononline`,
          onPageHide: `onpagehide`,
          onPageShow: `onpageshow`,
          onPaste: `onpaste`,
          onPause: `onpause`,
          onPlay: `onplay`,
          onPlaying: `onplaying`,
          onPopState: `onpopstate`,
          onProgress: `onprogress`,
          onRateChange: `onratechange`,
          onRepeat: `onrepeat`,
          onReset: `onreset`,
          onResize: `onresize`,
          onScroll: `onscroll`,
          onSeeked: `onseeked`,
          onSeeking: `onseeking`,
          onSelect: `onselect`,
          onShow: `onshow`,
          onStalled: `onstalled`,
          onStorage: `onstorage`,
          onSubmit: `onsubmit`,
          onSuspend: `onsuspend`,
          onTimeUpdate: `ontimeupdate`,
          onToggle: `ontoggle`,
          onUnload: `onunload`,
          onVolumeChange: `onvolumechange`,
          onWaiting: `onwaiting`,
          onZoom: `onzoom`,
          overlinePosition: `overline-position`,
          overlineThickness: `overline-thickness`,
          paintOrder: `paint-order`,
          panose1: `panose-1`,
          pointerEvents: `pointer-events`,
          referrerPolicy: `referrerpolicy`,
          renderingIntent: `rendering-intent`,
          shapeRendering: `shape-rendering`,
          stopColor: `stop-color`,
          stopOpacity: `stop-opacity`,
          strikethroughPosition: `strikethrough-position`,
          strikethroughThickness: `strikethrough-thickness`,
          strokeDashArray: `stroke-dasharray`,
          strokeDashOffset: `stroke-dashoffset`,
          strokeLineCap: `stroke-linecap`,
          strokeLineJoin: `stroke-linejoin`,
          strokeMiterLimit: `stroke-miterlimit`,
          strokeOpacity: `stroke-opacity`,
          strokeWidth: `stroke-width`,
          tabIndex: `tabindex`,
          textAnchor: `text-anchor`,
          textDecoration: `text-decoration`,
          textRendering: `text-rendering`,
          transformOrigin: `transform-origin`,
          typeOf: `typeof`,
          underlinePosition: `underline-position`,
          underlineThickness: `underline-thickness`,
          unicodeBidi: `unicode-bidi`,
          unicodeRange: `unicode-range`,
          unitsPerEm: `units-per-em`,
          vAlphabetic: `v-alphabetic`,
          vHanging: `v-hanging`,
          vIdeographic: `v-ideographic`,
          vMathematical: `v-mathematical`,
          vectorEffect: `vector-effect`,
          vertAdvY: `vert-adv-y`,
          vertOriginX: `vert-origin-x`,
          vertOriginY: `vert-origin-y`,
          wordSpacing: `word-spacing`,
          writingMode: `writing-mode`,
          xHeight: `x-height`,
          playbackOrder: `playbackorder`,
          timelineBegin: `timelinebegin`,
        },
        properties: {
          about: U,
          accentHeight: V,
          accumulate: null,
          additive: null,
          alignmentBaseline: null,
          alphabetic: V,
          amplitude: V,
          arabicForm: null,
          ascent: V,
          attributeName: null,
          attributeType: null,
          azimuth: V,
          bandwidth: null,
          baselineShift: null,
          baseFrequency: null,
          baseProfile: null,
          bbox: null,
          begin: null,
          bias: V,
          by: null,
          calcMode: null,
          capHeight: V,
          className: H,
          clip: null,
          clipPath: null,
          clipPathUnits: null,
          clipRule: null,
          color: null,
          colorInterpolation: null,
          colorInterpolationFilters: null,
          colorProfile: null,
          colorRendering: null,
          content: null,
          contentScriptType: null,
          contentStyleType: null,
          crossOrigin: null,
          cursor: null,
          cx: null,
          cy: null,
          d: null,
          dataType: null,
          defaultAction: null,
          descent: V,
          diffuseConstant: V,
          direction: null,
          display: null,
          dur: null,
          divisor: V,
          dominantBaseline: null,
          download: z,
          dx: null,
          dy: null,
          edgeMode: null,
          editable: null,
          elevation: V,
          enableBackground: null,
          end: null,
          event: null,
          exponent: V,
          externalResourcesRequired: null,
          fill: null,
          fillOpacity: V,
          fillRule: null,
          filter: null,
          filterRes: null,
          filterUnits: null,
          floodColor: null,
          floodOpacity: null,
          focusable: null,
          focusHighlight: null,
          fontFamily: null,
          fontSize: null,
          fontSizeAdjust: null,
          fontStretch: null,
          fontStyle: null,
          fontVariant: null,
          fontWeight: null,
          format: null,
          fr: null,
          from: null,
          fx: null,
          fy: null,
          g1: Ln,
          g2: Ln,
          glyphName: Ln,
          glyphOrientationHorizontal: null,
          glyphOrientationVertical: null,
          glyphRef: null,
          gradientTransform: null,
          gradientUnits: null,
          handler: null,
          hanging: V,
          hatchContentUnits: null,
          hatchUnits: null,
          height: null,
          href: null,
          hrefLang: null,
          horizAdvX: V,
          horizOriginX: V,
          horizOriginY: V,
          id: null,
          ideographic: V,
          imageRendering: null,
          initialVisibility: null,
          in: null,
          in2: null,
          intercept: V,
          k: V,
          k1: V,
          k2: V,
          k3: V,
          k4: V,
          kernelMatrix: U,
          kernelUnitLength: null,
          keyPoints: null,
          keySplines: null,
          keyTimes: null,
          kerning: null,
          lang: null,
          lengthAdjust: null,
          letterSpacing: null,
          lightingColor: null,
          limitingConeAngle: V,
          local: null,
          markerEnd: null,
          markerMid: null,
          markerStart: null,
          markerHeight: null,
          markerUnits: null,
          markerWidth: null,
          mask: null,
          maskContentUnits: null,
          maskUnits: null,
          mathematical: null,
          max: null,
          media: null,
          mediaCharacterEncoding: null,
          mediaContentEncodings: null,
          mediaSize: V,
          mediaTime: null,
          method: null,
          min: null,
          mode: null,
          name: null,
          navDown: null,
          navDownLeft: null,
          navDownRight: null,
          navLeft: null,
          navNext: null,
          navPrev: null,
          navRight: null,
          navUp: null,
          navUpLeft: null,
          navUpRight: null,
          numOctaves: null,
          observer: null,
          offset: null,
          onAbort: null,
          onActivate: null,
          onAfterPrint: null,
          onBeforePrint: null,
          onBegin: null,
          onCancel: null,
          onCanPlay: null,
          onCanPlayThrough: null,
          onChange: null,
          onClick: null,
          onClose: null,
          onCopy: null,
          onCueChange: null,
          onCut: null,
          onDblClick: null,
          onDrag: null,
          onDragEnd: null,
          onDragEnter: null,
          onDragExit: null,
          onDragLeave: null,
          onDragOver: null,
          onDragStart: null,
          onDrop: null,
          onDurationChange: null,
          onEmptied: null,
          onEnd: null,
          onEnded: null,
          onError: null,
          onFocus: null,
          onFocusIn: null,
          onFocusOut: null,
          onHashChange: null,
          onInput: null,
          onInvalid: null,
          onKeyDown: null,
          onKeyPress: null,
          onKeyUp: null,
          onLoad: null,
          onLoadedData: null,
          onLoadedMetadata: null,
          onLoadStart: null,
          onMessage: null,
          onMouseDown: null,
          onMouseEnter: null,
          onMouseLeave: null,
          onMouseMove: null,
          onMouseOut: null,
          onMouseOver: null,
          onMouseUp: null,
          onMouseWheel: null,
          onOffline: null,
          onOnline: null,
          onPageHide: null,
          onPageShow: null,
          onPaste: null,
          onPause: null,
          onPlay: null,
          onPlaying: null,
          onPopState: null,
          onProgress: null,
          onRateChange: null,
          onRepeat: null,
          onReset: null,
          onResize: null,
          onScroll: null,
          onSeeked: null,
          onSeeking: null,
          onSelect: null,
          onShow: null,
          onStalled: null,
          onStorage: null,
          onSubmit: null,
          onSuspend: null,
          onTimeUpdate: null,
          onToggle: null,
          onUnload: null,
          onVolumeChange: null,
          onWaiting: null,
          onZoom: null,
          opacity: null,
          operator: null,
          order: null,
          orient: null,
          orientation: null,
          origin: null,
          overflow: null,
          overlay: null,
          overlinePosition: V,
          overlineThickness: V,
          paintOrder: null,
          panose1: null,
          path: null,
          pathLength: V,
          patternContentUnits: null,
          patternTransform: null,
          patternUnits: null,
          phase: null,
          ping: H,
          pitch: null,
          playbackOrder: null,
          pointerEvents: null,
          points: null,
          pointsAtX: V,
          pointsAtY: V,
          pointsAtZ: V,
          preserveAlpha: null,
          preserveAspectRatio: null,
          primitiveUnits: null,
          propagate: null,
          property: U,
          r: null,
          radius: null,
          referrerPolicy: null,
          refX: null,
          refY: null,
          rel: U,
          rev: U,
          renderingIntent: null,
          repeatCount: null,
          repeatDur: null,
          requiredExtensions: U,
          requiredFeatures: U,
          requiredFonts: U,
          requiredFormats: U,
          resource: null,
          restart: null,
          result: null,
          rotate: null,
          rx: null,
          ry: null,
          scale: null,
          seed: null,
          shapeRendering: null,
          side: null,
          slope: null,
          snapshotTime: null,
          specularConstant: V,
          specularExponent: V,
          spreadMethod: null,
          spacing: null,
          startOffset: null,
          stdDeviation: null,
          stemh: null,
          stemv: null,
          stitchTiles: null,
          stopColor: null,
          stopOpacity: null,
          strikethroughPosition: V,
          strikethroughThickness: V,
          string: null,
          stroke: null,
          strokeDashArray: U,
          strokeDashOffset: null,
          strokeLineCap: null,
          strokeLineJoin: null,
          strokeMiterLimit: V,
          strokeOpacity: V,
          strokeWidth: null,
          style: null,
          surfaceScale: V,
          syncBehavior: null,
          syncBehaviorDefault: null,
          syncMaster: null,
          syncTolerance: null,
          syncToleranceDefault: null,
          systemLanguage: U,
          tabIndex: V,
          tableValues: null,
          target: null,
          targetX: V,
          targetY: V,
          textAnchor: null,
          textDecoration: null,
          textRendering: null,
          textLength: null,
          timelineBegin: null,
          title: null,
          transformBehavior: null,
          type: null,
          typeOf: U,
          to: null,
          transform: null,
          transformOrigin: null,
          u1: null,
          u2: null,
          underlinePosition: V,
          underlineThickness: V,
          unicode: null,
          unicodeBidi: null,
          unicodeRange: null,
          unitsPerEm: V,
          values: null,
          vAlphabetic: V,
          vMathematical: V,
          vectorEffect: null,
          vHanging: V,
          vIdeographic: V,
          version: null,
          vertAdvY: V,
          vertOriginX: V,
          vertOriginY: V,
          viewBox: null,
          viewTarget: null,
          visibility: null,
          width: null,
          widths: null,
          wordSpacing: null,
          writingMode: null,
          x: null,
          x1: null,
          x2: null,
          xChannelSelector: null,
          xHeight: V,
          y: null,
          y1: null,
          y2: null,
          yChannelSelector: null,
          z: null,
          zoomAndPan: null,
        },
        space: `svg`,
        transform: qn,
      })));
  }),
  tr,
  nr = e(() => {
    (Wn(),
      (tr = Un({
        properties: {
          xLinkActuate: null,
          xLinkArcRole: null,
          xLinkHref: null,
          xLinkRole: null,
          xLinkShow: null,
          xLinkTitle: null,
          xLinkType: null,
        },
        space: `xlink`,
        transform(e, t) {
          return `xlink:` + t.slice(5).toLowerCase();
        },
      })));
  }),
  rr,
  ir = e(() => {
    (Wn(),
      Xn(),
      (rr = Un({
        attributes: { xmlnsxlink: `xmlns:xlink` },
        properties: { xmlnsXLink: null, xmlns: null },
        space: `xmlns`,
        transform: Yn,
      })));
  }),
  ar,
  or = e(() => {
    (Wn(),
      (ar = Un({
        properties: { xmlBase: null, xmlLang: null, xmlSpace: null },
        space: `xml`,
        transform(e, t) {
          return `xml:` + t.slice(3).toLowerCase();
        },
      })));
  });
function sr(e, t) {
  let n = An(t),
    r = t,
    i = R;
  if (n in e.normal) return e.property[e.normal[n]];
  if (n.length > 4 && n.slice(0, 4) === `data` && fr.test(t)) {
    if (t.charAt(4) === `-`) {
      let e = t.slice(5).replace(dr, lr);
      r = `data` + e.charAt(0).toUpperCase() + e.slice(1);
    } else {
      let e = t.slice(4);
      if (!dr.test(e)) {
        let n = e.replace(ur, cr);
        (n.charAt(0) !== `-` && (n = `-` + n), (t = `data` + n));
      }
    }
    i = Vn;
  }
  return new i(r, t);
}
function cr(e) {
  return `-` + e.toLowerCase();
}
function lr(e) {
  return e.charAt(1).toUpperCase();
}
var ur,
  dr,
  fr,
  pr = e(() => {
    (Hn(),
      Mn(),
      jn(),
      (ur = /[A-Z]/g),
      (dr = /-[a-z]/g),
      (fr = /^data[-\w.:]+$/i));
  }),
  mr,
  hr,
  gr = e(() => {
    (kn(),
      Kn(),
      Qn(),
      er(),
      nr(),
      ir(),
      or(),
      pr(),
      (mr = On([Gn, Zn, tr, rr, ar], `html`)),
      (hr = On([Gn, $n, tr, rr, ar], `svg`)));
  });
function _r(e, t, n, r) {
  return r.settings.bogusComments
    ? `<?` +
        p(
          e.value,
          Object.assign({}, r.settings.characterReferences, { subset: yr }),
        ) +
        `>`
    : `<!--` + e.value.replace(vr, i) + `-->`;
  function i(e) {
    return p(
      e,
      Object.assign({}, r.settings.characterReferences, { subset: br }),
    );
  }
}
var vr,
  yr,
  br,
  xr = e(() => {
    (g(), (vr = /^>|^->|<!--|-->|--!>|<!-$/g), (yr = [`>`]), (br = [`<`, `>`]));
  });
function Sr(e, t, n, r) {
  return (
    `<!` +
    (r.settings.upperDoctype ? `DOCTYPE` : `doctype`) +
    (r.settings.tightDoctype ? `` : ` `) +
    `html>`
  );
}
var Cr = e(() => {});
function wr(e, t) {
  let n = t || {};
  return (e[e.length - 1] === `` ? [...e, ``] : e)
    .join((n.padRight ? ` ` : ``) + `,` + (n.padLeft === !1 ? `` : ` `))
    .trim();
}
var Tr = e(() => {});
function Er(e) {
  return e.join(` `).trim();
}
var Dr = e(() => {});
function Or(e) {
  return typeof e == `object` ? (e.type === `text` ? kr(e.value) : !1) : kr(e);
}
function kr(e) {
  return e.replace(Ar, ``) === ``;
}
var Ar,
  jr = e(() => {
    Ar = /[ \t\n\f\r]/g;
  }),
  Mr = e(() => {
    jr();
  });
function Nr(e) {
  return t;
  function t(t, n, r) {
    let i = t ? t.children : Fr,
      a = (n || 0) + e,
      o = i[a];
    if (!r) for (; o && Or(o);) ((a += e), (o = i[a]));
    return o;
  }
}
var W,
  Pr,
  Fr,
  Ir = e(() => {
    (Mr(), (W = Nr(1)), (Pr = Nr(-1)), (Fr = []));
  });
function Lr(e) {
  return t;
  function t(t, n, r) {
    return Rr.call(e, t.tagName) && e[t.tagName](t, n, r);
  }
}
var Rr,
  zr = e(() => {
    Rr = {}.hasOwnProperty;
  });
function Br(e, t, n) {
  let r = W(n, t, !0);
  return (
    !r ||
    (r.type !== `comment` && !(r.type === `text` && Or(r.value.charAt(0))))
  );
}
function Vr(e, t, n) {
  let r = W(n, t);
  return !r || r.type !== `comment`;
}
function Hr(e, t, n) {
  let r = W(n, t);
  return !r || r.type !== `comment`;
}
function Ur(e, t, n) {
  let r = W(n, t);
  return r
    ? r.type === `element` &&
        (r.tagName === `address` ||
          r.tagName === `article` ||
          r.tagName === `aside` ||
          r.tagName === `blockquote` ||
          r.tagName === `details` ||
          r.tagName === `div` ||
          r.tagName === `dl` ||
          r.tagName === `fieldset` ||
          r.tagName === `figcaption` ||
          r.tagName === `figure` ||
          r.tagName === `footer` ||
          r.tagName === `form` ||
          r.tagName === `h1` ||
          r.tagName === `h2` ||
          r.tagName === `h3` ||
          r.tagName === `h4` ||
          r.tagName === `h5` ||
          r.tagName === `h6` ||
          r.tagName === `header` ||
          r.tagName === `hgroup` ||
          r.tagName === `hr` ||
          r.tagName === `main` ||
          r.tagName === `menu` ||
          r.tagName === `nav` ||
          r.tagName === `ol` ||
          r.tagName === `p` ||
          r.tagName === `pre` ||
          r.tagName === `section` ||
          r.tagName === `table` ||
          r.tagName === `ul`)
    : !n ||
        !(
          n.type === `element` &&
          (n.tagName === `a` ||
            n.tagName === `audio` ||
            n.tagName === `del` ||
            n.tagName === `ins` ||
            n.tagName === `map` ||
            n.tagName === `noscript` ||
            n.tagName === `video`)
        );
}
function Wr(e, t, n) {
  let r = W(n, t);
  return !r || (r.type === `element` && r.tagName === `li`);
}
function Gr(e, t, n) {
  let r = W(n, t);
  return !!(
    r &&
    r.type === `element` &&
    (r.tagName === `dt` || r.tagName === `dd`)
  );
}
function Kr(e, t, n) {
  let r = W(n, t);
  return (
    !r || (r.type === `element` && (r.tagName === `dt` || r.tagName === `dd`))
  );
}
function qr(e, t, n) {
  let r = W(n, t);
  return (
    !r || (r.type === `element` && (r.tagName === `rp` || r.tagName === `rt`))
  );
}
function Jr(e, t, n) {
  let r = W(n, t);
  return !r || (r.type === `element` && r.tagName === `optgroup`);
}
function Yr(e, t, n) {
  let r = W(n, t);
  return (
    !r ||
    (r.type === `element` &&
      (r.tagName === `option` || r.tagName === `optgroup`))
  );
}
function Xr(e, t, n) {
  let r = W(n, t);
  return !!(
    r &&
    r.type === `element` &&
    (r.tagName === `tbody` || r.tagName === `tfoot`)
  );
}
function Zr(e, t, n) {
  let r = W(n, t);
  return (
    !r ||
    (r.type === `element` && (r.tagName === `tbody` || r.tagName === `tfoot`))
  );
}
function Qr(e, t, n) {
  return !W(n, t);
}
function $r(e, t, n) {
  let r = W(n, t);
  return !r || (r.type === `element` && r.tagName === `tr`);
}
function ei(e, t, n) {
  let r = W(n, t);
  return (
    !r || (r.type === `element` && (r.tagName === `td` || r.tagName === `th`))
  );
}
var ti,
  ni = e(() => {
    (Mr(),
      Ir(),
      zr(),
      (ti = Lr({
        body: Hr,
        caption: Br,
        colgroup: Br,
        dd: Kr,
        dt: Gr,
        head: Br,
        html: Vr,
        li: Wr,
        optgroup: Jr,
        option: Yr,
        p: Ur,
        rp: qr,
        rt: qr,
        tbody: Zr,
        td: ei,
        tfoot: Qr,
        th: ei,
        thead: Xr,
        tr: $r,
      })));
  });
function ri(e) {
  let t = W(e, -1);
  return !t || t.type !== `comment`;
}
function ii(e) {
  let t = new Set();
  for (let n of e.children)
    if (
      n.type === `element` &&
      (n.tagName === `base` || n.tagName === `title`)
    ) {
      if (t.has(n.tagName)) return !1;
      t.add(n.tagName);
    }
  let n = e.children[0];
  return !n || n.type === `element`;
}
function ai(e) {
  let t = W(e, -1, !0);
  return (
    !t ||
    (t.type !== `comment` &&
      !(t.type === `text` && Or(t.value.charAt(0))) &&
      !(
        t.type === `element` &&
        (t.tagName === `meta` ||
          t.tagName === `link` ||
          t.tagName === `script` ||
          t.tagName === `style` ||
          t.tagName === `template`)
      ))
  );
}
function oi(e, t, n) {
  let r = Pr(n, t),
    i = W(e, -1, !0);
  return n &&
    r &&
    r.type === `element` &&
    r.tagName === `colgroup` &&
    ti(r, n.children.indexOf(r), n)
    ? !1
    : !!(i && i.type === `element` && i.tagName === `col`);
}
function si(e, t, n) {
  let r = Pr(n, t),
    i = W(e, -1);
  return n &&
    r &&
    r.type === `element` &&
    (r.tagName === `thead` || r.tagName === `tbody`) &&
    ti(r, n.children.indexOf(r), n)
    ? !1
    : !!(i && i.type === `element` && i.tagName === `tr`);
}
var ci,
  li = e(() => {
    (Mr(),
      Ir(),
      ni(),
      zr(),
      (ci = Lr({ body: ai, colgroup: oi, head: ii, html: ri, tbody: si })));
  });
function ui(e, t, n, r) {
  let i = r.schema,
    a = i.space === `svg` ? !1 : r.settings.omitOptionalTags,
    o =
      i.space === `svg`
        ? r.settings.closeEmptyElements
        : r.settings.voids.includes(e.tagName.toLowerCase()),
    s = [],
    c;
  i.space === `html` && e.tagName === `svg` && (r.schema = hr);
  let l = di(r, e.properties),
    u = r.all(i.space === `html` && e.tagName === `template` ? e.content : e);
  return (
    (r.schema = i),
    u && (o = !1),
    (l || !a || !ci(e, t, n)) &&
      (s.push(`<`, e.tagName, l ? ` ` + l : ``),
      o &&
        (i.space === `svg` || r.settings.closeSelfClosing) &&
        ((c = l.charAt(l.length - 1)),
        (!r.settings.tightSelfClosing ||
          c === `/` ||
          (c && c !== `"` && c !== `'`)) &&
          s.push(` `),
        s.push(`/`)),
      s.push(`>`)),
    s.push(u),
    !o && (!a || !ti(e, t, n)) && s.push(`</` + e.tagName + `>`),
    s.join(``)
  );
}
function di(e, t) {
  let n = [],
    r = -1,
    i;
  if (t) {
    for (i in t)
      if (t[i] !== null && t[i] !== void 0) {
        let r = fi(e, i, t[i]);
        r && n.push(r);
      }
  }
  for (; ++r < n.length;) {
    let t = e.settings.tightAttributes ? n[r].charAt(n[r].length - 1) : void 0;
    r !== n.length - 1 && t !== `"` && t !== `'` && (n[r] += ` `);
  }
  return n.join(``);
}
function fi(e, t, n) {
  let r = sr(e.schema, t),
    i = e.settings.allowParseErrors && e.schema.space === `html` ? 0 : 1,
    a = +!e.settings.allowDangerousCharacters,
    o = e.quote,
    s;
  if (
    (r.overloadedBoolean && (n === r.attribute || n === ``)
      ? (n = !0)
      : (r.boolean || r.overloadedBoolean) &&
        (typeof n != `string` || n === r.attribute || n === ``) &&
        (n = !!n),
    n == null || n === !1 || (typeof n == `number` && Number.isNaN(n)))
  )
    return ``;
  let c = p(
    r.attribute,
    Object.assign({}, e.settings.characterReferences, {
      subset: pi.name[i][a],
    }),
  );
  return n === !0 ||
    ((n = Array.isArray(n)
      ? (r.commaSeparated ? wr : Er)(n, {
          padLeft: !e.settings.tightCommaSeparatedLists,
        })
      : String(n)),
    e.settings.collapseEmptyAttributes && !n)
    ? c
    : (e.settings.preferUnquoted &&
        (s = p(
          n,
          Object.assign({}, e.settings.characterReferences, {
            attribute: !0,
            subset: pi.unquoted[i][a],
          }),
        )),
      s !== n &&
        (e.settings.quoteSmart &&
          _(n, o) > _(n, e.alternative) &&
          (o = e.alternative),
        (s =
          o +
          p(
            n,
            Object.assign({}, e.settings.characterReferences, {
              subset: (o === `'` ? pi.single : pi.double)[i][a],
              attribute: !0,
            }),
          ) +
          o)),
      c + (s && `=` + s));
}
var pi,
  mi = e(() => {
    (h(),
      Tr(),
      gr(),
      Dr(),
      g(),
      ni(),
      li(),
      (pi = {
        name: [
          [
            `	
\f\r &/=>`.split(``),
            `	
\f\r "&'/=>\``.split(``),
          ],
          [
            `\0	
\f\r "&'/<=>`.split(``),
            `\0	
\f\r "&'/<=>\``.split(``),
          ],
        ],
        unquoted: [
          [
            `	
\f\r &>`.split(``),
            `\0	
\f\r "&'<=>\``.split(``),
          ],
          [
            `\0	
\f\r "&'<=>\``.split(``),
            `\0	
\f\r "&'<=>\``.split(``),
          ],
        ],
        single: [
          [`&'`.split(``), `"&'\``.split(``)],
          [`\0&'`.split(``), `\0"&'\``.split(``)],
        ],
        double: [
          [`"&`.split(``), `"&'\``.split(``)],
          [`\0"&`.split(``), `\0"&'\``.split(``)],
        ],
      }));
  });
function hi(e, t, n, r) {
  return n &&
    n.type === `element` &&
    (n.tagName === `script` || n.tagName === `style`)
    ? e.value
    : p(
        e.value,
        Object.assign({}, r.settings.characterReferences, { subset: gi }),
      );
}
var gi,
  _i = e(() => {
    (g(), (gi = [`<`, `&`]));
  });
function vi(e, t, n, r) {
  return r.settings.allowDangerousHtml ? e.value : hi(e, t, n, r);
}
var yi = e(() => {
  _i();
});
function bi(e, t, n, r) {
  return r.all(e);
}
var xi = e(() => {});
function Si(e) {
  throw Error("Expected node, not `" + e + "`");
}
function Ci(e) {
  throw Error("Cannot compile unknown node `" + e.type + "`");
}
var wi,
  Ti = e(() => {
    (f(),
      xr(),
      Cr(),
      mi(),
      yi(),
      xi(),
      _i(),
      (wi = m(`type`, {
        invalid: Si,
        unknown: Ci,
        handlers: {
          comment: _r,
          doctype: Sr,
          element: ui,
          raw: vi,
          root: bi,
          text: hi,
        },
      })));
  });
function Ei(e, t) {
  let n = t || ki,
    r = n.quote || `"`,
    i = r === `"` ? `'` : `"`;
  if (r !== `"` && r !== `'`)
    throw Error("Invalid quote `" + r + "`, expected `'` or `\"`");
  return {
    one: Di,
    all: Oi,
    settings: {
      omitOptionalTags: n.omitOptionalTags || !1,
      allowParseErrors: n.allowParseErrors || !1,
      allowDangerousCharacters: n.allowDangerousCharacters || !1,
      quoteSmart: n.quoteSmart || !1,
      preferUnquoted: n.preferUnquoted || !1,
      tightAttributes: n.tightAttributes || !1,
      upperDoctype: n.upperDoctype || !1,
      tightDoctype: n.tightDoctype || !1,
      bogusComments: n.bogusComments || !1,
      tightCommaSeparatedLists: n.tightCommaSeparatedLists || !1,
      tightSelfClosing: n.tightSelfClosing || !1,
      collapseEmptyAttributes: n.collapseEmptyAttributes || !1,
      allowDangerousHtml: n.allowDangerousHtml || !1,
      voids: n.voids || wn,
      characterReferences: n.characterReferences || Ai,
      closeSelfClosing: n.closeSelfClosing || !1,
      closeEmptyElements: n.closeEmptyElements || !1,
    },
    schema: n.space === `svg` ? hr : mr,
    quote: r,
    alternative: i,
  }.one(Array.isArray(e) ? { type: `root`, children: e } : e, void 0, void 0);
}
function Di(e, t, n) {
  return wi(e, t, n, this);
}
function Oi(e) {
  let t = [],
    n = (e && e.children) || ji,
    r = -1;
  for (; ++r < n.length;) t[r] = this.one(n[r], r, e);
  return t.join(``);
}
var ki,
  Ai,
  ji,
  Mi = e(() => {
    (Tn(), gr(), Ti(), (ki = {}), (Ai = {}), (ji = []));
  }),
  Ni = e(() => {
    Mi();
  });
function Pi(
  {
    enableTokenInteractionsOnWhitespace: e,
    enableGutterUtility: t,
    lineHoverHighlight: n,
    onGutterUtilityClick: r,
    onLineClick: i,
    onLineEnter: a,
    onLineLeave: o,
    onLineNumberClick: s,
    onTokenClick: c,
    onTokenEnter: l,
    onTokenLeave: u,
    renderGutterUtility: d,
    __debugPointerEvents: f,
    enableLineSelection: p,
    controlledSelection: m,
    onLineSelected: h,
    onLineSelectionStart: g,
    onLineSelectionChange: _,
    onLineSelectionEnd: v,
  },
  y,
  b,
  x,
) {
  return {
    enableTokenInteractionsOnWhitespace: e,
    enableGutterUtility: Fi({
      enableGutterUtility: t,
      renderGutterUtility: d,
      onGutterUtilityClick: r,
    }),
    usesCustomGutterUtility: d != null,
    lineHoverHighlight: n,
    onGutterUtilityClick: r,
    onHunkExpand: y,
    onMergeConflictActionClick: x,
    onLineClick: i,
    onLineEnter: a,
    onLineLeave: o,
    onLineNumberClick: s,
    onTokenClick: c,
    onTokenEnter: l,
    onTokenLeave: u,
    __debugPointerEvents: f,
    enableLineSelection: p,
    controlledSelection: m,
    onLineSelected: h,
    onLineSelectionStart: g,
    onLineSelectionChange: _,
    onLineSelectionEnd: v,
    getLineIndex: b,
  };
}
function Fi({
  enableGutterUtility: e,
  renderGutterUtility: t,
  onGutterUtilityClick: n,
}) {
  if (n != null && t != null)
    throw Error(
      `Cannot use both 'onGutterUtilityClick' and 'renderGutterUtility'. Use only one gutter utility API.`,
    );
  return e ?? !1;
}
function Ii(e) {
  return e != null && `kind` in e && e.kind === `line`;
}
function Li(e) {
  return e != null && `kind` in e && e.kind === `token`;
}
function Ri(e) {
  return Ii(e) || Li(e);
}
function zi(e) {
  return `type` in e && e.type === `line-info`;
}
function Bi(e) {
  return `kind` in e && e.kind === `merge-conflict-action`;
}
function Vi(e) {
  return e === `current` || e === `incoming` || e === `both`;
}
function Hi(e, t) {
  let n = e?.querySelector(t);
  return n instanceof HTMLElement ? n : void 0;
}
function Ui(e) {
  let t = [],
    n = e;
  for (; n != null;) (t.push(n), (n = n.parentNode));
  return t;
}
function Wi(e) {
  let t = e.closest(`[data-line], [data-column-number]`);
  if (t instanceof HTMLElement) return t;
  let n = e.closest(
    `[data-line-annotation], [data-gutter-buffer="annotation"]`,
  );
  if (!(n instanceof HTMLElement)) return;
  let r = n.previousElementSibling;
  return r instanceof HTMLElement &&
    (r.hasAttribute(`data-line`) || r.hasAttribute(`data-column-number`))
    ? r
    : void 0;
}
function Gi(e) {
  let t = e.closest(`[slot^="annotation-"]`);
  if (t instanceof HTMLElement) return t.getAttribute(`slot`) ?? void 0;
  if (e instanceof HTMLElement) {
    let t = e.getAttribute(`name`) ?? void 0;
    return t != null && t.startsWith(`annotation-`) ? t : void 0;
  }
}
function Ki(e) {
  if (e == null) return;
  let t = /^annotation-(?:(additions|deletions)-)?(\d+)$/.exec(e);
  if (t == null) return;
  let n = Number.parseInt(t[2], 10);
  if (!(!Number.isFinite(n) || n <= 0)) return { lineNumber: n, side: t[1] };
}
function qi(e) {
  return e != null && typeof e.elementFromPoint == `function`;
}
function Ji(e, t) {
  switch (e) {
    case `change-deletion`:
      return `deletions`;
    case `change-addition`:
      return `additions`;
    default:
      return t.hasAttribute(`data-deletions`) ? `deletions` : `additions`;
  }
}
function Yi(e) {
  let t = e.getAttribute(`data-line-type`);
  if (t != null)
    switch (t) {
      case `change-deletion`:
      case `change-addition`:
      case `context`:
      case `context-expanded`:
        return t;
      default:
        return;
    }
}
function Xi(e) {
  for (let t of e)
    if (
      t instanceof HTMLElement &&
      (t.hasAttribute(`data-utility-button`) ||
        t.hasAttribute(`data-gutter-utility-slot`) ||
        t.getAttribute(`slot`) === `gutter-utility-slot` ||
        t.getAttribute(`name`) === `gutter-utility-slot`)
    )
      return !0;
  return !1;
}
function Zi(e = `none`, t, ...n) {
  switch (e) {
    case `none`:
      return;
    case `both`:
      break;
    case `click`:
      if (t !== `click`) return;
      break;
    case `move`:
      if (t !== `move`) return;
      break;
  }
  console.log(...n);
}
var Qi,
  $i = e(() => {
    (sn(),
      Cn(),
      xn(),
      Ni(),
      (Qi = class {
        mode;
        options;
        hoveredLine;
        hoveredToken;
        pre;
        gutterUtilityLine;
        gutterUtilityContainer;
        gutterUtilityButton;
        gutterUtilitySlot;
        interactiveLinesAttr = !1;
        interactiveLineNumbersAttr = !1;
        hasPointerListeners = !1;
        hasDocumentPointerListeners = !1;
        selectedRange = null;
        activeLineHighlightSide;
        activeLineNumberOnly = !1;
        editorAttached = !1;
        proposedSelectedRange;
        renderedSelectionRange;
        selectionAnchor;
        queuedSelectionRender;
        pointerSession = { mode: `idle` };
        constructor(e, t) {
          ((this.mode = e), (this.options = t));
        }
        setOptions(e) {
          this.options = e;
        }
        cleanUp() {
          (this.pre?.removeEventListener(`click`, this.handlePointerClick),
            this.pre?.removeEventListener(
              `pointerdown`,
              this.handlePointerDown,
            ),
            this.pre?.removeEventListener(
              `pointermove`,
              this.handlePointerMove,
            ),
            this.pre?.removeEventListener(
              `pointerleave`,
              this.handlePointerLeave,
            ),
            this.pre?.removeAttribute(`data-interactive-lines`),
            this.pre?.removeAttribute(`data-interactive-line-numbers`),
            (this.pre = void 0),
            this.gutterUtilityContainer?.remove(),
            (this.gutterUtilityLine = void 0),
            (this.gutterUtilityContainer = void 0),
            (this.gutterUtilityButton = void 0),
            (this.gutterUtilitySlot = void 0),
            this.clearHoveredLine(),
            this.clearHoveredToken(),
            this.detachDocumentPointerListeners(),
            this.clearPointerSession(),
            this.queuedSelectionRender != null &&
              (cancelAnimationFrame(this.queuedSelectionRender),
              (this.queuedSelectionRender = void 0)),
            (this.interactiveLinesAttr = !1),
            (this.interactiveLineNumbersAttr = !1),
            (this.hasPointerListeners = !1));
        }
        setup(e) {
          this.setSelectionDirty();
          let { usesCustomGutterUtility: t = !1, enableGutterUtility: n = !1 } =
            this.options;
          (this.pre !== e && (this.cleanUp(), (this.pre = e)),
            n
              ? this.ensureGutterUtilityNode(t)
              : this.gutterUtilityContainer != null &&
                (this.gutterUtilityContainer.remove(),
                (this.gutterUtilityLine = void 0),
                (this.gutterUtilityContainer = void 0),
                (this.gutterUtilityButton = void 0),
                (this.gutterUtilitySlot = void 0),
                this.pointerSession.mode === `gutterSelecting` &&
                  (this.clearPointerSession(),
                  this.detachDocumentPointerListeners())),
            this.syncPointerListeners(e),
            this.updateInteractiveLineAttributes(),
            this.renderSelection(),
            this.placeUtility());
        }
        setSelectionDirty() {
          this.renderedSelectionRange = void 0;
        }
        setEditorAttached(e) {
          this.editorAttached !== e &&
            ((this.editorAttached = e),
            this.setSelectionDirty(),
            this.renderSelection());
        }
        isSelectionDirty() {
          return this.renderedSelectionRange === null;
        }
        setSelection(e, t) {
          let n = !(
            e === this.selectedRange ||
            on(e ?? void 0, this.selectedRange ?? void 0)
          );
          (!this.isSelectionDirty() && !n) ||
            ((this.proposedSelectedRange = void 0),
            (this.selectedRange = e),
            (this.activeLineHighlightSide = t?.activeLineSide),
            (this.activeLineNumberOnly = t?.lineNumberOnly ?? !1),
            this.renderSelection(),
            this.placeUtility(),
            n && t?.notify !== !1 && this.notifySelectionCommitted());
        }
        getSelection() {
          return this.selectedRange;
        }
        getHoveredLine = () => {
          let e = this.gutterUtilityLine ?? this.hoveredLine;
          if (e != null) {
            if (this.mode === `diff` && e.type === `diff-line`)
              return { lineNumber: e.lineNumber, side: e.annotationSide };
            if (this.mode === `file` && e.type === `line`)
              return { lineNumber: e.lineNumber };
          }
        };
        handlePointerClick = (e) => {
          let {
            onHunkExpand: t,
            onLineClick: n,
            onLineNumberClick: r,
            onTokenClick: i,
            onMergeConflictActionClick: a,
          } = this.options;
          (t == null && n == null && r == null && a == null && i == null) ||
            (this.options.onGutterUtilityClick != null &&
              Xi(e.composedPath())) ||
            (Zi(
              this.options.__debugPointerEvents,
              `click`,
              `FileDiff.DEBUG.handlePointerClick:`,
              e,
            ),
            this.handlePointerEvent({ eventType: `click`, event: e }));
        };
        handlePointerMove = (e) => {
          if (e.pointerType !== `mouse`) return;
          let {
            lineHoverHighlight: t = `disabled`,
            onLineEnter: n,
            onLineLeave: r,
            onTokenEnter: i,
            onTokenLeave: a,
            enableGutterUtility: o = !1,
          } = this.options;
          (t === `disabled` &&
            !o &&
            n == null &&
            r == null &&
            i == null &&
            a == null) ||
            (Zi(
              this.options.__debugPointerEvents,
              `move`,
              `FileDiff.DEBUG.handlePointerMove:`,
              e,
            ),
            this.handlePointerEvent({ eventType: `move`, event: e }));
        };
        handlePointerLeave = (e) => {
          let { __debugPointerEvents: t } = this.options;
          if (
            (Zi(t, `move`, `FileDiff.DEBUG.handlePointerLeave: no event`),
            this.hoveredLine == null && this.hoveredToken == null)
          ) {
            Zi(
              t,
              `move`,
              `FileDiff.DEBUG.handlePointerLeave: returned early, no hovered line or token`,
            );
            return;
          }
          (this.hoveredToken != null &&
            (this.options.onTokenLeave?.(this.hoveredToken, e),
            this.clearHoveredToken()),
            this.hoveredLine != null &&
              (this.options.onLineLeave?.({ ...this.hoveredLine, event: e }),
              this.clearHoveredLine()),
            this.placeUtility());
        };
        handlePointerEvent({ eventType: e, event: t }) {
          let { __debugPointerEvents: n } = this.options,
            r = t.composedPath();
          Zi(n, e, `FileDiff.DEBUG.handlePointerEvent:`, {
            eventType: e,
            composedPath: r,
          });
          let i = this.resolvePointerTarget(r);
          Zi(
            n,
            e,
            `FileDiff.DEBUG.handlePointerEvent: resolvePointerTarget result:`,
            i,
          );
          let {
            onLineClick: a,
            onLineNumberClick: o,
            onLineEnter: s,
            onLineLeave: c,
            onTokenClick: l,
            onTokenEnter: u,
            onTokenLeave: d,
            onHunkExpand: f,
            onMergeConflictActionClick: p,
          } = this.options;
          switch (e) {
            case `move`: {
              let e = Ri(i) && this.hoveredLine?.lineElement === i.lineElement;
              ((Li(i) && this.hoveredToken?.tokenElement === i.tokenElement) ||
                (this.hoveredToken != null &&
                  (d?.(this.hoveredToken, t), this.clearHoveredToken()),
                Li(i) &&
                  (this.setHoveredToken(this.toTokenEventBaseProps(i)),
                  u?.(this.hoveredToken, t))),
                e ||
                  (this.hoveredLine != null &&
                    (c?.({ ...this.hoveredLine, event: t }),
                    this.clearHoveredLine()),
                  Ri(i)
                    ? (this.setHoveredLine(this.toEventBaseProps(i)),
                      this.placeUtility(),
                      s?.({ ...this.hoveredLine, event: t }))
                    : this.placeUtility()));
              break;
            }
            case `click`: {
              if (i == null) break;
              if (Bi(i) && p != null) {
                p(i);
                break;
              }
              if (zi(i) && f != null) {
                f(
                  i.hunkIndex,
                  i.all || t.shiftKey ? `both` : i.direction,
                  i.all || t.shiftKey ? 1 / 0 : void 0,
                );
                break;
              }
              if (!Ri(i)) break;
              Li(i) && l != null && l(this.toTokenEventBaseProps(i), t);
              let e = this.toEventBaseProps(i);
              o != null && i.numberColumn
                ? o({ ...e, event: t })
                : a?.({ ...e, event: t });
              break;
            }
          }
        }
        syncPointerListeners(e) {
          let {
              __debugPointerEvents: t,
              lineHoverHighlight: n = `disabled`,
              onLineClick: r,
              onLineNumberClick: i,
              onLineEnter: a,
              onLineLeave: o,
              onTokenClick: s,
              onTokenEnter: c,
              onTokenLeave: l,
              onHunkExpand: u,
              onMergeConflictActionClick: d,
              enableGutterUtility: f = !1,
              enableLineSelection: p = !1,
              onGutterUtilityClick: m,
            } = this.options,
            h = m != null,
            g =
              n !== `disabled` ||
              r != null ||
              i != null ||
              a != null ||
              o != null ||
              s != null ||
              c != null ||
              l != null ||
              u != null ||
              d != null ||
              f ||
              p ||
              h;
          g && !this.hasPointerListeners
            ? (e.addEventListener(`click`, this.handlePointerClick),
              e.addEventListener(`pointerdown`, this.handlePointerDown),
              e.addEventListener(`pointermove`, this.handlePointerMove),
              e.addEventListener(`pointerleave`, this.handlePointerLeave),
              (this.hasPointerListeners = !0),
              Zi(
                t,
                `click`,
                `FileDiff.DEBUG.attachEventListeners: Attaching click events for:`,
                (() => {
                  let e = [];
                  return (
                    (t === `both` || t === `click`) &&
                      (r != null && e.push(`onLineClick`),
                      i != null && e.push(`onLineNumberClick`),
                      u != null && e.push(`expandable hunk separators`),
                      d != null && e.push(`merge conflict actions`)),
                    e
                  );
                })(),
              ),
              Zi(
                t,
                `move`,
                `FileDiff.DEBUG.attachEventListeners: Attaching pointer move event`,
              ),
              Zi(
                t,
                `move`,
                `FileDiff.DEBUG.attachEventListeners: Attaching pointer leave event`,
              ))
            : !g &&
              this.hasPointerListeners &&
              (e.removeEventListener(`click`, this.handlePointerClick),
              e.removeEventListener(`pointerdown`, this.handlePointerDown),
              e.removeEventListener(`pointermove`, this.handlePointerMove),
              e.removeEventListener(`pointerleave`, this.handlePointerLeave),
              (this.hasPointerListeners = !1));
          let _ =
              this.pointerSession.mode === `selecting` ||
              this.pointerSession.mode === `pendingSingleLineUnselect`,
            v = this.pointerSession.mode === `gutterSelecting`;
          ((!p && _) || (!h && v)) &&
            (this.clearPointerSession(),
            this.detachDocumentPointerListeners(),
            (this.selectionAnchor = void 0),
            this.clearPendingSingleLineState());
        }
        updateInteractiveLineAttributes() {
          if (this.pre == null) return;
          let {
              onLineClick: e,
              onLineNumberClick: t,
              enableLineSelection: n = !1,
            } = this.options,
            r = e != null,
            i = t != null || n;
          (r && !this.interactiveLinesAttr
            ? (this.pre.setAttribute(`data-interactive-lines`, ``),
              (this.interactiveLinesAttr = !0))
            : !r &&
              this.interactiveLinesAttr &&
              (this.pre.removeAttribute(`data-interactive-lines`),
              (this.interactiveLinesAttr = !1)),
            i && !this.interactiveLineNumbersAttr
              ? (this.pre.setAttribute(`data-interactive-line-numbers`, ``),
                (this.interactiveLineNumbersAttr = !0))
              : !i &&
                this.interactiveLineNumbersAttr &&
                (this.pre.removeAttribute(`data-interactive-line-numbers`),
                (this.interactiveLineNumbersAttr = !1)));
        }
        handlePointerDown = (e) => {
          if (
            (e.pointerType === `mouse` && e.button !== 0) ||
            this.pre == null ||
            this.pointerSession.mode !== `idle`
          )
            return;
          let t = e.composedPath();
          Xi(t) && this.options.onGutterUtilityClick != null
            ? this.startGutterSelectionFromPointerDown(e)
            : (e.pointerType !== `mouse` && this.revealUtilityFromGutterPath(t),
              this.startLineSelectionFromPointerDown(e));
        };
        startLineSelectionFromPointerDown(e) {
          let { enableLineSelection: t = !1 } = this.options;
          if (!t) return;
          let n = this.resolveSelectionInfo(e, {
            source: `event-path`,
            requireNumberColumn: !0,
          });
          if (n == null) return;
          let { pre: r } = this;
          if (r == null) return;
          let { lineNumber: i, eventSide: a, lineIndex: o } = n;
          if (e.shiftKey && this.selectedRange != null) {
            let t = this.getIndexesFromSelection(
              this.selectedRange,
              r.getAttribute(`data-diff-type`) === `split`,
            );
            if (t == null) return;
            let n = t.start <= t.end ? o >= t.start : o <= t.end;
            ((this.selectionAnchor = {
              lineNumber: n ? this.selectedRange.start : this.selectedRange.end,
              side: n
                ? this.selectedRange.side
                : (this.selectedRange.endSide ?? this.selectedRange.side),
            }),
              this.updateSelection(i, a, !1),
              this.notifySelectionStart(this.getCurrentSelectionRange()),
              (this.pointerSession = {
                mode: `selecting`,
                pointerId: e.pointerId,
              }),
              this.attachDocumentPointerListeners());
            return;
          }
          if (
            this.selectedRange?.start === i &&
            this.selectedRange?.end === i
          ) {
            let t = { lineNumber: i, side: a };
            ((this.selectionAnchor = t),
              (this.pointerSession = {
                mode: `pendingSingleLineUnselect`,
                pointerId: e.pointerId,
                anchor: t,
                pending: t,
              }),
              this.attachDocumentPointerListeners());
            return;
          }
          (this.options.controlledSelection === !0
            ? (this.proposedSelectedRange = null)
            : (this.selectedRange = null),
            this.placeUtility(),
            (this.selectionAnchor = { lineNumber: i, side: a }),
            this.updateSelection(i, a, !1),
            this.notifySelectionStart(this.getCurrentSelectionRange()),
            (this.pointerSession = {
              mode: `selecting`,
              pointerId: e.pointerId,
            }),
            this.attachDocumentPointerListeners());
        }
        startGutterSelectionFromPointerDown(e) {
          let { enableLineSelection: t = !1, onGutterUtilityClick: n } =
            this.options;
          if (n == null) return;
          let r = this.currentSelectionEnds(),
            i =
              r?.bottom ??
              this.resolveSelectionPoint(e, {
                source: `event-path`,
                excludeUtility: !1,
              }),
            a = r?.top ?? i;
          i == null ||
            a == null ||
            (e.preventDefault(),
            e.stopPropagation(),
            (this.pointerSession = {
              mode: `gutterSelecting`,
              pointerId: e.pointerId,
              anchor: a,
              current: i,
            }),
            t &&
              ((this.selectionAnchor = {
                lineNumber: a.lineNumber,
                side: a.side,
              }),
              this.updateSelection(i.lineNumber, i.side, !1),
              this.notifySelectionStart(this.getCurrentSelectionRange())),
            this.attachDocumentPointerListeners());
        }
        handleDocumentPointerMove = (e) => {
          let { enableLineSelection: t = !1 } = this.options;
          switch (this.pointerSession.mode) {
            case `idle`:
              return;
            case `gutterSelecting`: {
              if (e.pointerId !== this.pointerSession.pointerId) return;
              e.preventDefault();
              let n = this.resolveSelectionPoint(e, {
                source: `coordinates-first`,
              });
              if (n == null) return;
              ((this.pointerSession.current = n),
                t === !0 && this.updateSelection(n.lineNumber, n.side));
              return;
            }
            case `selecting`: {
              if (e.pointerId !== this.pointerSession.pointerId) return;
              e.preventDefault();
              let t = this.resolveSelectionInfo(e, {
                source: `coordinates-first`,
                requireNumberColumn: !1,
              });
              if (t == null || this.selectionAnchor == null) return;
              this.updateSelection(t.lineNumber, t.eventSide);
              return;
            }
            case `pendingSingleLineUnselect`: {
              if (e.pointerId !== this.pointerSession.pointerId) return;
              e.preventDefault();
              let t = this.resolveSelectionInfo(e, {
                source: `coordinates-first`,
                requireNumberColumn: !1,
              });
              if (t == null || this.selectionAnchor == null) return;
              let n = { lineNumber: t.lineNumber, side: t.eventSide };
              if (Sn(this.pointerSession.pending, n)) return;
              (this.updateSelection(t.lineNumber, t.eventSide, !1),
                this.notifySelectionStart(this.getCurrentSelectionRange()),
                this.notifySelectionChangeDelta(),
                (this.pointerSession = {
                  mode: `selecting`,
                  pointerId: e.pointerId,
                }));
              return;
            }
          }
        };
        handleDocumentPointerUp = (e) => {
          let { enableLineSelection: t = !1, onGutterUtilityClick: n } =
            this.options;
          switch (this.pointerSession.mode) {
            case `idle`:
              return;
            case `gutterSelecting`: {
              if (e.pointerId !== this.pointerSession.pointerId) return;
              e.preventDefault();
              let r = this.resolveSelectionPoint(e, {
                source: `coordinates-first`,
              });
              (r != null &&
                ((this.pointerSession.current = r),
                t && this.updateSelection(r.lineNumber, r.side)),
                n?.(
                  this.buildSelectedLineRange(
                    this.pointerSession.anchor,
                    this.pointerSession.current,
                  ),
                ),
                (this.selectionAnchor = void 0),
                t &&
                  (this.notifySelectionEnd(this.getCurrentSelectionRange()),
                  this.notifySelectionCommitted(),
                  this.clearProposedSelection()),
                this.clearPointerSession(),
                this.detachDocumentPointerListeners());
              return;
            }
            case `pendingSingleLineUnselect`:
              if (e.pointerId !== this.pointerSession.pointerId) return;
              (e.preventDefault(),
                this.updateSelection(null, void 0, !1),
                (this.selectionAnchor = void 0),
                this.clearPendingSingleLineState(),
                this.detachDocumentPointerListeners(),
                this.notifySelectionEnd(this.getCurrentSelectionRange()),
                this.notifySelectionCommitted(),
                this.clearProposedSelection());
              return;
            case `selecting`:
              if (e.pointerId !== this.pointerSession.pointerId) return;
              (e.preventDefault(),
                (this.selectionAnchor = void 0),
                this.detachDocumentPointerListeners(),
                this.clearPointerSession(),
                this.notifySelectionEnd(this.getCurrentSelectionRange()),
                this.notifySelectionCommitted(),
                this.clearProposedSelection());
          }
        };
        handleDocumentPointerCancel = (e) => {
          switch (this.pointerSession.mode) {
            case `idle`:
              return;
            case `gutterSelecting`:
            case `selecting`:
            case `pendingSingleLineUnselect`:
              if (
                `pointerId` in this.pointerSession &&
                e.pointerId !== this.pointerSession.pointerId
              )
                return;
              ((this.selectionAnchor = void 0),
                this.clearProposedSelection(),
                this.clearPendingSingleLineState(),
                this.clearPointerSession(),
                this.detachDocumentPointerListeners());
          }
        };
        clearHoveredLine() {
          this.hoveredLine != null &&
            (this.hoveredLine.lineElement.removeAttribute(`data-hovered`),
            this.hoveredLine.numberElement.removeAttribute(`data-hovered`),
            (this.hoveredLine = void 0));
        }
        setHoveredLine(e) {
          let { lineHoverHighlight: t = `disabled` } = this.options;
          (this.hoveredLine != null && this.clearHoveredLine(),
            (this.hoveredLine = e),
            t !== `disabled` &&
              ((t === `both` || t === `line`) &&
                this.hoveredLine.lineElement.setAttribute(`data-hovered`, ``),
              (t === `both` || t === `number`) &&
                this.hoveredLine.numberElement.setAttribute(
                  `data-hovered`,
                  ``,
                )));
        }
        clearHoveredToken() {
          this.hoveredToken != null && (this.hoveredToken = void 0);
        }
        setHoveredToken(e) {
          (this.hoveredToken != null && this.clearHoveredToken(),
            (this.hoveredToken = e));
        }
        ensureGutterUtilityNode(e) {
          if (
            (this.gutterUtilityContainer ??
              ((this.gutterUtilityContainer = document.createElement(`div`)),
              this.gutterUtilityContainer.setAttribute(
                `data-gutter-utility-slot`,
                ``,
              )),
            e)
          )
            (this.gutterUtilityButton != null &&
              (this.gutterUtilityButton.remove(),
              (this.gutterUtilityButton = void 0)),
              this.gutterUtilitySlot ??
                ((this.gutterUtilitySlot = document.createElement(`slot`)),
                (this.gutterUtilitySlot.name = `gutter-utility-slot`)),
              this.gutterUtilitySlot.parentNode !==
                this.gutterUtilityContainer &&
                this.gutterUtilityContainer.replaceChildren(
                  this.gutterUtilitySlot,
                ));
          else {
            if (
              (this.gutterUtilitySlot?.remove(),
              (this.gutterUtilitySlot = void 0),
              this.gutterUtilityButton == null)
            ) {
              let e = document.createElement(`div`);
              e.innerHTML = Ei(bn());
              let t = e.firstElementChild;
              if (!(t instanceof HTMLButtonElement))
                throw Error(
                  `InteractionManager.ensureGutterUtilityNode: Node element should be a button`,
                );
              (t.remove(), (this.gutterUtilityButton = t));
            }
            this.gutterUtilityButton.parentNode !==
              this.gutterUtilityContainer &&
              this.gutterUtilityContainer.replaceChildren(
                this.gutterUtilityButton,
              );
          }
        }
        revealUtilityFromGutterPath(e) {
          if (this.placeUtilityFromSelection()) return;
          let t = this.resolvePointerTarget(e);
          Ii(t) &&
            t.numberColumn &&
            this.showUtilityOnLine(this.toEventBaseProps(t));
        }
        placeUtility() {
          if (!this.placeUtilityFromSelection()) {
            if (this.hoveredLine != null) {
              this.showUtilityOnLine(this.hoveredLine);
              return;
            }
            this.hideUtility();
          }
        }
        placeUtilityFromSelection() {
          let e = this.currentSelectionEnds();
          if (e == null) return !1;
          let t = this.targetForSelectionPoint(e.bottom);
          return (
            t == null
              ? this.hideUtility()
              : this.showUtilityOnLine(this.toEventBaseProps(t)),
            !0
          );
        }
        showUtilityOnLine(e) {
          this.gutterUtilityContainer != null &&
            ((this.gutterUtilityLine = e),
            e.numberElement.appendChild(this.gutterUtilityContainer));
        }
        hideUtility() {
          (this.gutterUtilityContainer?.remove(),
            (this.gutterUtilityLine = void 0));
        }
        currentSelectionEnds() {
          let e = this.getCurrentSelectionRange();
          return e == null ? void 0 : this.selectionEnds(e);
        }
        selectionEnds(e) {
          let t = { lineNumber: e.start, side: e.side },
            n = { lineNumber: e.end, side: e.endSide ?? e.side },
            r = this.selectionPointRowIndex(t),
            i = this.selectionPointRowIndex(n);
          if (!(r == null || i == null))
            return r > i ? { top: n, bottom: t } : { top: t, bottom: n };
        }
        selectionPointRowIndex(e) {
          let t = this.getLineIndex(e.lineNumber, e.side);
          if (t != null) return this.isSplitDiff() ? t[1] : t[0];
        }
        targetForSelectionPoint(e) {
          if (this.pre == null) return;
          let t = this.getLineIndex(e.lineNumber, e.side);
          if (t == null) return;
          let n = this.mode === `diff` ? `${t[0]},${t[1]}` : `${t[0]}`,
            r = this.pre.querySelectorAll(
              `[data-column-number="${e.lineNumber}"][data-line-index="${n}"]`,
            );
          for (let t of r) {
            if (!(t instanceof HTMLElement)) continue;
            let n = this.resolvePointerTarget(Ui(t));
            if (
              Ii(n) &&
              !(this.mode === `diff` && e.side != null && n.side !== e.side)
            )
              return n;
          }
        }
        attachDocumentPointerListeners() {
          this.hasDocumentPointerListeners ||=
            (document.addEventListener(
              `pointermove`,
              this.handleDocumentPointerMove,
            ),
            document.addEventListener(
              `pointerup`,
              this.handleDocumentPointerUp,
            ),
            document.addEventListener(
              `pointercancel`,
              this.handleDocumentPointerCancel,
            ),
            !0);
        }
        detachDocumentPointerListeners() {
          this.hasDocumentPointerListeners &&=
            (document.removeEventListener(
              `pointermove`,
              this.handleDocumentPointerMove,
            ),
            document.removeEventListener(
              `pointerup`,
              this.handleDocumentPointerUp,
            ),
            document.removeEventListener(
              `pointercancel`,
              this.handleDocumentPointerCancel,
            ),
            !1);
        }
        clearPointerSession() {
          this.pointerSession = { mode: `idle` };
        }
        clearPendingSingleLineState() {
          this.pointerSession.mode === `pendingSingleLineUnselect` &&
            (this.pointerSession = { mode: `idle` });
        }
        selectionInfoFromPath(e, t) {
          let n = this.resolvePointerTarget(e);
          if (Ii(n) && !(t && !n.numberColumn) && n.splitLineIndex != null)
            return {
              lineIndex: n.splitLineIndex,
              lineNumber: n.lineNumber,
              eventSide: this.mode === `diff` ? n.side : void 0,
            };
        }
        resolveSelectionInfo(e, t) {
          let n = this.resolveSelectionPath(e, t);
          return n == null
            ? void 0
            : this.selectionInfoFromPath(n, t.requireNumberColumn);
        }
        selectionPointFromPath(e) {
          let t = this.resolvePointerTarget(e);
          if (Ii(t))
            return {
              lineNumber: t.lineNumber,
              side: this.mode === `diff` ? t.side : void 0,
            };
        }
        resolveSelectionPoint(e, t) {
          let n = this.resolveSelectionPath(e, t);
          return n == null ? void 0 : this.selectionPointFromPath(n);
        }
        resolveSelectionPath(e, t) {
          let n = t.excludeUtility !== !1;
          switch (t.source) {
            case `event-path`:
              return this.pathFromEventPath(e.composedPath(), n);
            case `coordinates-first`: {
              let t = this.pathFromCoordinates(e, n);
              return t === void 0
                ? this.pathFromEventPath(e.composedPath(), n)
                : (t ?? void 0);
            }
          }
        }
        pathFromCoordinates(e, t) {
          let n = this.hitTest(e);
          if (n !== void 0)
            return n === null ? null : (this.pathFromElement(n, t) ?? null);
        }
        pathFromEventPath(e, t) {
          if (!(t && Xi(e))) {
            for (let n of e)
              if (n instanceof Element) return this.pathFromElement(n, t);
          }
        }
        pathFromElement(e, t) {
          let n = Ui(e);
          if (t && Xi(n)) return;
          let r = Wi(e);
          return r == null ? this.pathFromAnnotationSlot(e) : Ui(r);
        }
        pathFromAnnotationSlot(e) {
          let t = Ki(Gi(e));
          if (t == null) return;
          let n = this.targetForSelectionPoint(t);
          return n == null ? void 0 : Ui(n.lineElement);
        }
        hitTest(e) {
          if (!Number.isFinite(e.clientX) || !Number.isFinite(e.clientY))
            return;
          let t = this.pre?.getRootNode(),
            n = qi(t) ? t : qi(document) ? document : void 0;
          if (n != null) return n.elementFromPoint(e.clientX, e.clientY);
        }
        getLineIndex(e, t) {
          let { getLineIndex: n } = this.options;
          return n == null ? [e - 1, e - 1] : n(e, t);
        }
        getCurrentSelectionRange() {
          return this.proposedSelectedRange === void 0
            ? this.selectedRange
            : this.proposedSelectedRange;
        }
        clearProposedSelection() {
          this.proposedSelectedRange = void 0;
        }
        updateSelection(e, t, n = !0) {
          let r = this.getCurrentSelectionRange(),
            i;
          if (e == null) i = null;
          else {
            let n = this.selectionAnchor?.side ?? t,
              r = this.selectionAnchor?.lineNumber ?? e;
            i = this.buildSelectionRange(r, e, n, t);
          }
          on(r ?? void 0, i ?? void 0) ||
            ((this.activeLineHighlightSide = void 0),
            (this.activeLineNumberOnly = !1),
            this.options.controlledSelection === !0
              ? (this.proposedSelectedRange = i)
              : ((this.selectedRange = i),
                (this.queuedSelectionRender ??= requestAnimationFrame(
                  this.renderSelection,
                ))),
            this.placeUtility(),
            n && this.notifySelectionChangeDelta());
        }
        getIndexesFromSelection(e, t) {
          if (this.pre == null) return;
          let n = this.getLineIndex(e.start, e.side),
            r = this.getLineIndex(e.end, e.endSide ?? e.side);
          return n != null && r != null
            ? { start: t ? n[1] : n[0], end: t ? r[1] : r[0] }
            : void 0;
        }
        highlightLineNumberOnly() {
          return this.activeLineHighlightSide == null
            ? this.activeLineNumberOnly || this.editorAttached
            : this.activeLineNumberOnly;
        }
        renderSelection = () => {
          if (
            (this.queuedSelectionRender != null &&
              (cancelAnimationFrame(this.queuedSelectionRender),
              (this.queuedSelectionRender = void 0)),
            this.pre == null ||
              this.renderedSelectionRange === this.selectedRange)
          )
            return;
          let e = this.pre.querySelectorAll(`[data-selected-line]`);
          for (let t of e) t.removeAttribute(`data-selected-line`);
          if (
            ((this.renderedSelectionRange = this.selectedRange),
            this.selectedRange == null)
          )
            return;
          let { children: t } = this.pre;
          if (t.length === 0) return;
          if (t.length > 2)
            throw (
              console.error(t),
              Error(
                `InteractionManager.renderSelection: Somehow there are more than 2 code elements...`,
              )
            );
          let n = this.pre.getAttribute(`data-diff-type`) === `split`,
            r = this.getIndexesFromSelection(this.selectedRange, n);
          if (r == null)
            throw (
              console.error({ rowRange: r, selectedRange: this.selectedRange }),
              Error(`InteractionManager.renderSelection: No valid rowRange`)
            );
          let i = r.start === r.end,
            a = Math.min(r.start, r.end),
            o = Math.max(r.start, r.end),
            s = this.highlightLineNumberOnly();
          for (let e of t) {
            let t = this.activeLineHighlightSide;
            if (
              t != null &&
              ((t === `additions` && e.hasAttribute(`data-deletions`)) ||
                (t === `deletions` && e.hasAttribute(`data-additions`)))
            )
              continue;
            let [r, c] = e.children,
              l = c.children.length;
            if (l !== r.children.length)
              throw Error(
                `InteractionManager.renderSelection: gutter and content children dont match, something is wrong`,
              );
            for (let e = 0; e < l; e++) {
              let t = c.children[e],
                l = r.children[e];
              if (!(t instanceof HTMLElement) || !(l instanceof HTMLElement))
                continue;
              let u = this.parseLineIndex(t, n);
              if ((u ?? 0) > o) break;
              if (u == null || u < a) continue;
              let d = i ? `single` : u === a ? `first` : u === o ? `last` : ``;
              (l.setAttribute(`data-selected-line`, d),
                !s &&
                  (t.setAttribute(`data-selected-line`, d),
                  l.nextSibling instanceof HTMLElement &&
                    t.nextSibling instanceof HTMLElement &&
                    (t.nextSibling.hasAttribute(`data-line-annotation`) ||
                      t.nextSibling.hasAttribute(
                        `data-merge-conflict-actions`,
                      )) &&
                    (i
                      ? ((d = `last`),
                        t.setAttribute(`data-selected-line`, `first`))
                      : u === a
                        ? (d = ``)
                        : u === o && t.setAttribute(`data-selected-line`, ``),
                    t.nextSibling.setAttribute(`data-selected-line`, d),
                    l.nextSibling.setAttribute(`data-selected-line`, d))));
            }
          }
        };
        notifySelectionCommitted() {
          this.options.onLineSelected?.(
            this.getCurrentSelectionRange() ?? null,
          );
        }
        notifySelectionChangeDelta() {
          this.options.onLineSelectionChange?.(
            this.getCurrentSelectionRange() ?? null,
          );
        }
        notifySelectionStart(e) {
          this.options.onLineSelectionStart?.(e);
        }
        notifySelectionEnd(e) {
          this.options.onLineSelectionEnd?.(e);
        }
        toEventBaseProps(e) {
          return this.mode === `file`
            ? {
                type: `line`,
                lineElement: e.lineElement,
                lineNumber: e.lineNumber,
                numberColumn: e.numberColumn,
                numberElement: e.numberElement,
              }
            : {
                type: `diff-line`,
                annotationSide: e.side,
                lineType: e.lineType,
                lineElement: e.lineElement,
                numberElement: e.numberElement,
                lineNumber: e.lineNumber,
                numberColumn: e.numberColumn,
              };
        }
        toTokenEventBaseProps({
          lineCharEnd: e,
          lineCharStart: t,
          lineNumber: n,
          side: r,
          tokenElement: i,
          tokenText: a,
        }) {
          return this.mode === `file`
            ? {
                type: `token`,
                lineCharEnd: e,
                lineCharStart: t,
                lineNumber: n,
                tokenElement: i,
                tokenText: a,
              }
            : {
                type: `token`,
                lineCharEnd: e,
                lineCharStart: t,
                lineNumber: n,
                side: r,
                tokenElement: i,
                tokenText: a,
              };
        }
        buildSelectedLineRange(e, t) {
          return this.buildSelectionRange(
            e.lineNumber,
            t.lineNumber,
            e.side,
            t.side,
          );
        }
        buildSelectionRange(e, t, n, r) {
          return {
            start: e,
            end: t,
            ...(n == null ? {} : { side: n }),
            ...(n !== r && r != null ? { endSide: r } : {}),
          };
        }
        resolvePointerTarget(e) {
          let t = !1,
            n,
            r,
            i,
            a,
            o,
            s,
            c,
            l,
            u,
            d;
          for (let f of e) {
            if (!(f instanceof HTMLElement)) continue;
            if (d == null && f.hasAttribute(`data-merge-conflict-action`)) {
              let e = f.getAttribute(`data-merge-conflict-action`) ?? void 0,
                t =
                  f.getAttribute(`data-merge-conflict-conflict-index`) ??
                  void 0,
                n = t == null ? NaN : Number.parseInt(t, 10);
              Vi(e) &&
                Number.isFinite(n) &&
                (d = {
                  kind: `merge-conflict-action`,
                  resolution: e,
                  conflictIndex: n,
                });
            }
            if (s == null && f.hasAttribute(`data-char`)) {
              s = f;
              let e = f.getAttribute(`data-char`);
              if (e != null) {
                let t = Number.parseInt(e, 10);
                if (!Number.isNaN(t)) {
                  let e = f.textContent ?? ``,
                    n = t + e.length;
                  (e.trim() !== `` ||
                    this.options.enableTokenInteractionsOnWhitespace === !0) &&
                    (c = {
                      tokenElement: s,
                      lineCharStart: t,
                      lineCharEnd: n,
                      tokenText: e,
                    });
                  continue;
                }
              }
            }
            let e =
              o == null
                ? (f.getAttribute(`data-column-number`) ?? void 0)
                : void 0;
            if (e != null) {
              ((o = f),
                (u = Number.parseInt(e, 10)),
                (t = !0),
                (n = Yi(f)),
                (a = f.getAttribute(`data-line-index`) ?? void 0));
              continue;
            }
            let p =
              i == null ? (f.getAttribute(`data-line`) ?? void 0) : void 0;
            if (p != null) {
              ((i = f),
                (u = Number.parseInt(p, 10)),
                (n = Yi(f)),
                (a = f.getAttribute(`data-line-index`) ?? void 0));
              continue;
            }
            if (
              l == null &&
              (f.hasAttribute(`data-expand-button`) ||
                f.hasAttribute(`data-unmodified-lines`))
            ) {
              l = {
                hunkIndex: void 0,
                direction: f.hasAttribute(`data-expand-up`)
                  ? `up`
                  : f.hasAttribute(`data-expand-down`)
                    ? `down`
                    : `both`,
                all: f.hasAttribute(`data-expand-all-button`),
              };
              continue;
            }
            let m =
              l == null
                ? void 0
                : (f.getAttribute(`data-expand-index`) ?? void 0);
            if (l != null && m != null) {
              let e = Number.parseInt(m, 10);
              Number.isNaN(e) || (l.hunkIndex = e);
              continue;
            }
            if (r == null && f.hasAttribute(`data-code`)) {
              r = f;
              break;
            }
          }
          if (d != null) return d;
          if (l?.hunkIndex != null)
            return {
              type: `line-info`,
              hunkIndex: l.hunkIndex,
              direction: l.direction,
              all: l.all,
            };
          if (
            ((i ??=
              a == null
                ? void 0
                : Hi(r, `[data-line][data-line-index="${a}"]`)),
            (o ??=
              a == null
                ? void 0
                : Hi(r, `[data-column-number][data-line-index="${a}"]`)),
            r == null ||
              i == null ||
              o == null ||
              n == null ||
              u == null ||
              Number.isNaN(u))
          )
            return;
          let f = this.parseLineIndex(i, this.isSplitDiff());
          return c == null
            ? this.mode === `file`
              ? {
                  kind: `line`,
                  lineType: n,
                  lineElement: i,
                  lineNumber: u,
                  numberColumn: t,
                  numberElement: o,
                  side: void 0,
                  splitLineIndex: f,
                }
              : {
                  kind: `line`,
                  lineType: n,
                  lineElement: i,
                  lineNumber: u,
                  numberColumn: t,
                  numberElement: o,
                  side: Ji(n, r),
                  splitLineIndex: f,
                }
            : this.mode === `file`
              ? {
                  kind: `token`,
                  lineType: n,
                  lineElement: i,
                  lineNumber: u,
                  numberColumn: t,
                  numberElement: o,
                  side: void 0,
                  splitLineIndex: f,
                  ...c,
                }
              : {
                  kind: `token`,
                  lineType: n,
                  lineElement: i,
                  lineNumber: u,
                  numberColumn: t,
                  numberElement: o,
                  side: Ji(n, r),
                  splitLineIndex: f,
                  ...c,
                };
        }
        isSplitDiff() {
          return this.pre?.getAttribute(`data-diff-type`) === `split`;
        }
        parseLineIndex(e, t) {
          let n = (e.getAttribute(`data-line-index`) ?? ``)
            .split(`,`)
            .map((e) => Number.parseInt(e, 10))
            .filter((e) => !Number.isNaN(e));
          if (t && n.length === 2) return n[1];
          if (!t) return n[0];
        }
      }));
  });
function ea(e) {
  let t = Math.max(Math.floor(e), 0);
  return t === 0 ? `auto` : t;
}
function ta(e) {
  return Math.max(Math.ceil(e), 0);
}
function na(e) {
  e.codeElement.isConnected &&
    (e.codeElement.style.removeProperty(`--diffs-column-content-width`),
    e.codeElement.style.removeProperty(`--diffs-column-number-width`),
    e.codeElement.style.removeProperty(`--diffs-column-width`));
}
function ra(e) {
  (e.column1.container.isConnected &&
    e.column1.container.style.removeProperty(`--diffs-annotation-min-height`),
    e.column2.container.isConnected &&
      e.column2.container.style.removeProperty(
        `--diffs-annotation-min-height`,
      ));
}
var ia,
  aa = e(() => {
    ia = class e {
      static resizeObserver;
      static managersByElement = new Map();
      static getResizeObserver() {
        let t =
          e.resizeObserver ?? new ResizeObserver(e.handleSharedResizeEntries);
        return ((e.resizeObserver = t), t);
      }
      static handleSharedResizeEntries(t) {
        let n = new Map();
        for (let r of t) {
          let t = e.managersByElement.get(r.target);
          if (t == null) continue;
          let i = n.get(t);
          i == null ? n.set(t, [r]) : i.push(r);
        }
        for (let [e, t] of n) e.handleResizeEntries(t);
      }
      observedNodes = new Map();
      setup(e, t) {
        let n = new Set(),
          r = 0,
          i = new Map(this.observedNodes);
        this.observedNodes.clear();
        for (let t of e.children) {
          if (r === 2) break;
          let e = (() => {
            if (t instanceof HTMLElement && t.tagName === `CODE`) return t;
          })();
          if (e == null) continue;
          r++;
          let n = i.get(e);
          if (n != null && n.type !== `code`)
            throw Error(
              `ResizeManager.setup: somehow a code node is being used for an annotation, should be impossible`,
            );
          let a = e.firstElementChild;
          (a instanceof HTMLElement || (a = null),
            n == null
              ? ((n = {
                  type: `code`,
                  codeElement: e,
                  numberElement: a,
                  codeWidth: `auto`,
                  numberWidth: 0,
                }),
                this.observedNodes.set(e, n),
                this.observe(e),
                a != null && (this.observedNodes.set(a, n), this.observe(a)))
              : (this.observedNodes.set(e, n),
                i.delete(e),
                n.numberElement === a
                  ? n.numberElement == null
                    ? (n.numberWidth = 0)
                    : (i.delete(n.numberElement),
                      this.observedNodes.set(n.numberElement, n))
                  : (n.numberElement != null &&
                      (this.unobserve(n.numberElement),
                      i.delete(n.numberElement)),
                    a != null &&
                      (this.observe(a),
                      i.delete(a),
                      this.observedNodes.set(a, n)),
                    (n.numberElement = a),
                    (n.numberWidth = 0))));
        }
        if (r > 1 && !t) {
          let t = e.querySelectorAll(`[data-line-annotation*=","]`),
            r = new Map();
          for (let e of t) {
            if (!(e instanceof HTMLElement)) continue;
            let t = e.getAttribute(`data-line-annotation`) ?? ``;
            if (!/^-?\d+,-?\d+$/.test(t)) {
              console.error(
                `DiffFileRenderer.setupResizeObserver: Invalid element or annotation`,
                { lineAnnotation: t, element: e },
              );
              continue;
            }
            let n = r.get(t);
            (n ?? ((n = []), r.set(t, n)), n.push(e));
          }
          for (let [e, t] of r) {
            if (t.length !== 2) {
              console.error(
                `DiffFileRenderer.setupResizeObserver: Bad Pair`,
                e,
                t,
              );
              continue;
            }
            let [r, a] = t,
              o = r.firstElementChild,
              s = a.firstElementChild;
            if (
              !(r instanceof HTMLElement) ||
              !(a instanceof HTMLElement) ||
              !(o instanceof HTMLElement) ||
              !(s instanceof HTMLElement)
            )
              continue;
            let c = i.get(o);
            if (c != null) {
              (this.observedNodes.set(o, c),
                this.observedNodes.set(s, c),
                i.delete(o),
                i.delete(s));
              continue;
            }
            let l = o.getBoundingClientRect().height,
              u = s.getBoundingClientRect().height;
            ((c = {
              type: `annotations`,
              column1: { container: r, child: o, childHeight: l },
              column2: { container: a, child: s, childHeight: u },
              currentHeight: `auto`,
            }),
              n.add({
                child1: o,
                child2: s,
                item: c,
                newHeight: Math.max(l, u),
              }));
          }
          for (let e of n)
            (this.applyNewHeight(e.item, e.newHeight),
              this.observedNodes.set(e.child1, e.item),
              this.observedNodes.set(e.child2, e.item),
              this.observe(e.child1),
              this.observe(e.child2));
          n.clear();
        }
        for (let [e, t] of i)
          (this.unobserve(e), t.type === `code` ? na(t) : ra(t));
        i.clear();
      }
      cleanUp() {
        for (let e of this.observedNodes.keys()) this.unobserve(e);
        this.observedNodes.clear();
      }
      observe(t) {
        let { managersByElement: n } = e,
          r = n.get(t);
        if (r !== this) {
          if (r != null && r !== this)
            throw Error(
              `ResizeManager.observe: element is already owned by another ResizeManager`,
            );
          (n.set(t, this), e.getResizeObserver().observe(t));
        }
      }
      unobserve(t) {
        let { managersByElement: n, resizeObserver: r } = e,
          i = n.get(t);
        if (i != null) {
          if (i !== this)
            throw Error(
              `ResizeManager.unobserve: element is owned by another ResizeManager`,
            );
          (n.delete(t),
            r?.unobserve(t),
            r != null &&
              n.size === 0 &&
              (r.disconnect(), (e.resizeObserver = void 0)));
        }
      }
      handleResizeEntries(e) {
        let t = new Map(),
          n = new Set();
        for (let r of e) {
          let { target: e, borderBoxSize: i, contentBoxSize: a } = r;
          if (!(e instanceof HTMLElement)) {
            console.error(
              `ResizeManager.handleResizeEntries: Invalid element for ResizeObserver`,
              r,
            );
            continue;
          }
          let o = this.observedNodes.get(e);
          if (o == null) {
            console.error(
              `ResizeManager.handleResizeEntries: Not a valid observed node`,
              r,
            );
            continue;
          }
          if (o.type === `annotations`) {
            let t = (() => {
              if (e === o.column1.child) return o.column1;
              if (e === o.column2.child) return o.column2;
            })();
            if (t == null) {
              console.error(
                `ResizeManager.handleResizeEntries: Couldn't find a column for`,
                { item: o, target: e },
              );
              continue;
            }
            ((t.childHeight = i[0].blockSize), n.add(o));
          } else if (o.type === `code`) {
            let n = t.get(o) ?? {},
              r = a[0].inlineSize;
            (e === o.codeElement
              ? (n.codeInlineSize = r)
              : e === o.numberElement && (n.numberInlineSize = r),
              t.set(o, n));
          }
        }
        (this.applyAnnotationUpdates(n),
          n.clear(),
          this.applyColumnUpdates(t),
          t.clear());
      }
      applyAnnotationUpdates(e) {
        for (let t of e)
          this.applyNewHeight(
            t,
            Math.max(t.column1.childHeight, t.column2.childHeight),
          );
      }
      applyColumnUpdates = (e) => {
        for (let [t, n] of e) {
          let e = n.codeInlineSize == null ? t.codeWidth : ea(n.codeInlineSize),
            r =
              n.numberInlineSize == null
                ? t.numberWidth
                : ta(n.numberInlineSize),
            i = e !== t.codeWidth,
            a = r !== t.numberWidth;
          if (
            !(!i && !a) &&
            ((t.codeWidth = e),
            (t.numberWidth = r),
            i &&
              t.codeElement.style.setProperty(
                `--diffs-column-width`,
                `${typeof e == `number` ? `${e}px` : `auto`}`,
              ),
            a &&
              t.codeElement.style.setProperty(
                `--diffs-column-number-width`,
                `${r === 0 ? `auto` : `${r}px`}`,
              ),
            i || (a && e !== `auto`))
          ) {
            let n = typeof e == `number` ? Math.max(e - r, 0) : 0;
            t.codeElement.style.setProperty(
              `--diffs-column-content-width`,
              `${n > 0 ? `${n}px` : `auto`}`,
            );
          }
        }
      };
      applyNewHeight(e, t) {
        t !== e.currentHeight &&
          ((e.currentHeight = Math.max(t, 0)),
          e.column1.container.style.setProperty(
            `--diffs-annotation-min-height`,
            `${e.currentHeight}px`,
          ),
          e.column2.container.style.setProperty(
            `--diffs-annotation-min-height`,
            `${e.currentHeight}px`,
          ));
      }
    };
  }),
  oa,
  sa,
  ca,
  la,
  ua = e(() => {
    ((oa = new Map()), (sa = new Map()), (ca = new Map()), (la = new Set()));
  });
function da(e) {
  for (let t of Array.isArray(e) ? e : [e])
    if (!(t === `text` || t === `ansi`) && !la.has(t)) return !1;
  return !0;
}
var fa = e(() => {
  ua();
});
function pa(e, t) {
  e = Array.isArray(e) ? e : [e];
  for (let n of e) {
    if (la.has(n.name)) continue;
    let e = oa.get(n.name);
    (e ?? ((e = n), oa.set(n.name, e)),
      la.add(e.name),
      t.loadLanguageSync(e.data));
  }
}
var ma = e(() => {
  ua();
});
function ha() {
  return (
    typeof WorkerGlobalScope < `u` &&
    typeof self < `u` &&
    self instanceof WorkerGlobalScope
  );
}
var ga = e(() => {}),
  G,
  _a = e(() => {
    G = class extends Error {
      constructor(e) {
        (super(e), (this.name = `ShikiError`));
      }
    };
  });
function va(e) {
  return ya(e);
}
function ya(e) {
  return Array.isArray(e)
    ? ba(e)
    : e instanceof RegExp
      ? e
      : typeof e == `object`
        ? xa(e)
        : e;
}
function ba(e) {
  let t = [];
  for (let n = 0, r = e.length; n < r; n++) t[n] = ya(e[n]);
  return t;
}
function xa(e) {
  let t = {};
  for (let n in e) t[n] = ya(e[n]);
  return t;
}
function Sa(e, ...t) {
  return (
    t.forEach((t) => {
      for (let n in t) e[n] = t[n];
    }),
    e
  );
}
function Ca(e) {
  let t = ~e.lastIndexOf(`/`) || ~e.lastIndexOf(`\\`);
  return t === 0
    ? e
    : ~t === e.length - 1
      ? Ca(e.substring(0, e.length - 1))
      : e.substr(~t + 1);
}
function wa(e, t) {
  return e < t ? -1 : +(e > t);
}
function Ta(e, t) {
  if (e === null && t === null) return 0;
  if (!e) return -1;
  if (!t) return 1;
  let n = e.length,
    r = t.length;
  if (n === r) {
    for (let r = 0; r < n; r++) {
      let n = wa(e[r], t[r]);
      if (n !== 0) return n;
    }
    return 0;
  }
  return n - r;
}
function Ea(e) {
  return !!(
    /^#[0-9a-f]{6}$/i.test(e) ||
    /^#[0-9a-f]{8}$/i.test(e) ||
    /^#[0-9a-f]{3}$/i.test(e) ||
    /^#[0-9a-f]{4}$/i.test(e)
  );
}
function Da(e) {
  return e.replace(/[\-\\\{\}\*\+\?\|\^\$\.\,\[\]\(\)\#\s]/g, `\\$&`);
}
function Oa(e, t) {
  if (t.length === 0) return !0;
  for (let n = 0; n < t.length; n++) {
    let r = t[n],
      i = !1;
    if (r === `>`) {
      if (n === t.length - 1) return !1;
      ((r = t[++n]), (i = !0));
    }
    for (; e && !ka(e.scopeName, r);) {
      if (i) return !1;
      e = e.parent;
    }
    if (!e) return !1;
    e = e.parent;
  }
  return !0;
}
function ka(e, t) {
  return t === e || (e.startsWith(t) && e[t.length] === `.`);
}
function Aa(e) {
  if (!e || !e.settings || !Array.isArray(e.settings)) return [];
  let t = e.settings,
    n = [],
    r = 0;
  for (let e = 0, i = t.length; e < i; e++) {
    let i = t[e];
    if (!i.settings) continue;
    let a;
    if (typeof i.scope == `string`) {
      let e = i.scope;
      ((e = e.replace(/^[,]+/, ``)),
        (e = e.replace(/[,]+$/, ``)),
        (a = e.split(`,`)));
    } else a = Array.isArray(i.scope) ? i.scope : [``];
    let o = -1;
    if (typeof i.settings.fontStyle == `string`) {
      o = 0;
      let e = i.settings.fontStyle.split(` `);
      for (let t = 0, n = e.length; t < n; t++)
        switch (e[t]) {
          case `italic`:
            o |= 1;
            break;
          case `bold`:
            o |= 2;
            break;
          case `underline`:
            o |= 4;
            break;
          case `strikethrough`:
            o |= 8;
            break;
        }
    }
    let s = null;
    typeof i.settings.foreground == `string` &&
      Ea(i.settings.foreground) &&
      (s = i.settings.foreground);
    let c = null;
    typeof i.settings.background == `string` &&
      Ea(i.settings.background) &&
      (c = i.settings.background);
    for (let t = 0, i = a.length; t < i; t++) {
      let i = a[t].trim().split(` `),
        l = i[i.length - 1],
        u = null;
      (i.length > 1 && ((u = i.slice(0, i.length - 1)), u.reverse()),
        (n[r++] = new fo(l, u, e, o, s, c)));
    }
  }
  return n;
}
function ja(e, t) {
  e.sort((e, t) => {
    let n = wa(e.scope, t.scope);
    return n !== 0 || ((n = Ta(e.parentScopes, t.parentScopes)), n !== 0)
      ? n
      : e.index - t.index;
  });
  let n = 0,
    r = `#000000`,
    i = `#ffffff`;
  for (; e.length >= 1 && e[0].scope === ``;) {
    let t = e.shift();
    (t.fontStyle !== -1 && (n = t.fontStyle),
      t.foreground !== null && (r = t.foreground),
      t.background !== null && (i = t.background));
  }
  let a = new po(t),
    o = new uo(n, a.getId(r), a.getId(i)),
    s = new go(new ho(0, null, -1, 0, 0), []);
  for (let t = 0, n = e.length; t < n; t++) {
    let n = e[t];
    s.insert(
      0,
      n.scope,
      n.parentScopes,
      n.fontStyle,
      a.getId(n.foreground),
      a.getId(n.background),
    );
  }
  return new co(a, o, s);
}
function Ma(e) {
  return e;
}
function Na(e) {
  return e;
}
function Pa(e, t) {
  let n = [],
    r = Ia(e),
    i = r.next();
  for (; i !== null;) {
    let e = 0;
    if (i.length === 2 && i.charAt(1) === `:`) {
      switch (i.charAt(0)) {
        case `R`:
          e = 1;
          break;
        case `L`:
          e = -1;
          break;
        default:
          console.log(`Unknown priority ${i} in scope selector`);
      }
      i = r.next();
    }
    let t = o();
    if ((n.push({ matcher: t, priority: e }), i !== `,`)) break;
    i = r.next();
  }
  return n;
  function a() {
    if (i === `-`) {
      i = r.next();
      let e = a();
      return (t) => !!e && !e(t);
    }
    if (i === `(`) {
      i = r.next();
      let e = s();
      return (i === `)` && (i = r.next()), e);
    }
    if (Fa(i)) {
      let e = [];
      do (e.push(i), (i = r.next()));
      while (Fa(i));
      return (n) => t(e, n);
    }
    return null;
  }
  function o() {
    let e = [],
      t = a();
    for (; t;) (e.push(t), (t = a()));
    return (t) => e.every((e) => e(t));
  }
  function s() {
    let e = [],
      t = o();
    for (; t && (e.push(t), i === `|` || i === `,`);) {
      do i = r.next();
      while (i === `|` || i === `,`);
      t = o();
    }
    return (t) => e.some((e) => e(t));
  }
}
function Fa(e) {
  return !!e && !!e.match(/[\w\.:]+/);
}
function Ia(e) {
  let t = /([LR]:|[\w\.:][\w\.:\-]*|[\,\|\-\(\)])/g,
    n = t.exec(e);
  return {
    next: () => {
      if (!n) return null;
      let r = n[0];
      return ((n = t.exec(e)), r);
    },
  };
}
function La(e) {
  typeof e.dispose == `function` && e.dispose();
}
function Ra(e, t, n, r) {
  let i = n.lookup(e.scopeName);
  if (!i) {
    if (e.scopeName === t) throw Error(`No grammar provided for <${t}>`);
    return;
  }
  let a = n.lookup(t);
  e instanceof vo
    ? Ba({ baseGrammar: a, selfGrammar: i }, r)
    : za(
        e.ruleName,
        { baseGrammar: a, selfGrammar: i, repository: i.repository },
        r,
      );
  let o = n.injections(e.scopeName);
  if (o) for (let e of o) r.add(new vo(e));
}
function za(e, t, n) {
  if (t.repository && t.repository[e]) {
    let r = t.repository[e];
    Va([r], t, n);
  }
}
function Ba(e, t) {
  (e.selfGrammar.patterns &&
    Array.isArray(e.selfGrammar.patterns) &&
    Va(
      e.selfGrammar.patterns,
      { ...e, repository: e.selfGrammar.repository },
      t,
    ),
    e.selfGrammar.injections &&
      Va(
        Object.values(e.selfGrammar.injections),
        { ...e, repository: e.selfGrammar.repository },
        t,
      ));
}
function Va(e, t, n) {
  for (let r of e) {
    if (n.visitedRule.has(r)) continue;
    n.visitedRule.add(r);
    let e = r.repository ? Sa({}, t.repository, r.repository) : t.repository;
    Array.isArray(r.patterns) && Va(r.patterns, { ...t, repository: e }, n);
    let i = r.include;
    if (!i) continue;
    let a = Ha(i);
    switch (a.kind) {
      case 0:
        Ba({ ...t, selfGrammar: t.baseGrammar }, n);
        break;
      case 1:
        Ba(t, n);
        break;
      case 2:
        za(a.ruleName, { ...t, repository: e }, n);
        break;
      case 3:
      case 4:
        let r =
          a.scopeName === t.selfGrammar.scopeName
            ? t.selfGrammar
            : a.scopeName === t.baseGrammar.scopeName
              ? t.baseGrammar
              : void 0;
        if (r) {
          let i = { baseGrammar: t.baseGrammar, selfGrammar: r, repository: e };
          a.kind === 4 ? za(a.ruleName, i, n) : Ba(i, n);
        } else
          a.kind === 4
            ? n.add(new yo(a.scopeName, a.ruleName))
            : n.add(new vo(a.scopeName));
        break;
    }
  }
}
function Ha(e) {
  if (e === `$base`) return new So();
  if (e === `$self`) return new Co();
  let t = e.indexOf(`#`);
  return t === -1
    ? new To(e)
    : t === 0
      ? new wo(e.substring(1))
      : new Eo(e.substring(0, t), e.substring(t + 1));
}
function Ua(e) {
  return e;
}
function Wa(e) {
  return e;
}
function Ga(e, t, n, r, i, a, o, s) {
  let c = t.content.length,
    l = !1,
    u = -1;
  if (o) {
    let o = Ka(e, t, n, r, i, a);
    ((i = o.stack),
      (r = o.linePos),
      (n = o.isFirstLine),
      (u = o.anchorPosition));
  }
  let d = Date.now();
  for (; !l;) {
    if (s !== 0 && Date.now() - d > s) return new Go(i, !0);
    f();
  }
  return new Go(i, !1);
  function f() {
    let o = qa(e, t, n, r, i, u);
    if (!o) {
      (a.produce(i, c), (l = !0));
      return;
    }
    let s = o.captureIndices,
      d = o.matchedRuleId,
      f = s && s.length > 0 ? s[0].end > r : !1;
    if (d === ko) {
      let o = i.getRule(e);
      (a.produce(i, s[0].start),
        (i = i.withContentNameScopesList(i.nameScopesList)),
        $a(e, t, n, i, a, o.endCaptures, s),
        a.produce(i, s[0].end));
      let d = i;
      if (
        ((i = i.parent), (u = d.getAnchorPos()), !f && d.getEnterPos() === r)
      ) {
        ((i = d), a.produce(i, c), (l = !0));
        return;
      }
    } else {
      let o = e.getRule(d);
      a.produce(i, s[0].start);
      let p = i,
        m = o.getName(t.content, s),
        h = i.contentNameScopesList.pushAttributed(m, e);
      if (
        ((i = i.push(d, r, u, s[0].end === c, null, h, h)), o instanceof Fo)
      ) {
        let r = o;
        ($a(e, t, n, i, a, r.beginCaptures, s),
          a.produce(i, s[0].end),
          (u = s[0].end));
        let d = r.getContentName(t.content, s),
          m = h.pushAttributed(d, e);
        if (
          ((i = i.withContentNameScopesList(m)),
          r.endHasBackReferences &&
            (i = i.withEndRule(
              r.getEndWithResolvedBackReferences(t.content, s),
            )),
          !f && p.hasSameRuleAs(i))
        ) {
          ((i = i.pop()), a.produce(i, c), (l = !0));
          return;
        }
      } else if (o instanceof Io) {
        let r = o;
        ($a(e, t, n, i, a, r.beginCaptures, s),
          a.produce(i, s[0].end),
          (u = s[0].end));
        let d = r.getContentName(t.content, s),
          m = h.pushAttributed(d, e);
        if (
          ((i = i.withContentNameScopesList(m)),
          r.whileHasBackReferences &&
            (i = i.withEndRule(
              r.getWhileWithResolvedBackReferences(t.content, s),
            )),
          !f && p.hasSameRuleAs(i))
        ) {
          ((i = i.pop()), a.produce(i, c), (l = !0));
          return;
        }
      } else if (
        ($a(e, t, n, i, a, o.captures, s),
        a.produce(i, s[0].end),
        (i = i.pop()),
        !f)
      ) {
        ((i = i.safePop()), a.produce(i, c), (l = !0));
        return;
      }
    }
    s[0].end > r && ((r = s[0].end), (n = !1));
  }
}
function Ka(e, t, n, r, i, a) {
  let o = i.beginRuleCapturedEOL ? 0 : -1,
    s = [];
  for (let t = i; t; t = t.pop()) {
    let n = t.getRule(e);
    n instanceof Io && s.push({ rule: n, stack: t });
  }
  for (let c = s.pop(); c; c = s.pop()) {
    let { ruleScanner: s, findOptions: l } = Za(
        c.rule,
        e,
        c.stack.endRule,
        n,
        r === o,
      ),
      u = s.findNextMatchSync(t, r, l);
    if (u) {
      if (u.ruleId !== Ao) {
        i = c.stack.pop();
        break;
      }
      u.captureIndices &&
        u.captureIndices.length &&
        (a.produce(c.stack, u.captureIndices[0].start),
        $a(e, t, n, c.stack, a, c.rule.whileCaptures, u.captureIndices),
        a.produce(c.stack, u.captureIndices[0].end),
        (o = u.captureIndices[0].end),
        u.captureIndices[0].end > r &&
          ((r = u.captureIndices[0].end), (n = !1)));
    } else {
      i = c.stack.pop();
      break;
    }
  }
  return { stack: i, linePos: r, anchorPosition: o, isFirstLine: n };
}
function qa(e, t, n, r, i, a) {
  let o = Ja(e, t, n, r, i, a),
    s = e.getInjections();
  if (s.length === 0) return o;
  let c = Ya(s, e, t, n, r, i, a);
  if (!c) return o;
  if (!o) return c;
  let l = o.captureIndices[0].start,
    u = c.captureIndices[0].start;
  return u < l || (c.priorityMatch && u === l) ? c : o;
}
function Ja(e, t, n, r, i, a) {
  let { ruleScanner: o, findOptions: s } = Xa(
      i.getRule(e),
      e,
      i.endRule,
      n,
      r === a,
    ),
    c = o.findNextMatchSync(t, r, s);
  return c
    ? { captureIndices: c.captureIndices, matchedRuleId: c.ruleId }
    : null;
}
function Ya(e, t, n, r, i, a, o) {
  let s = Number.MAX_VALUE,
    c = null,
    l,
    u = 0,
    d = a.contentNameScopesList.getScopeNames();
  for (let a = 0, f = e.length; a < f; a++) {
    let f = e[a];
    if (!f.matcher(d)) continue;
    let { ruleScanner: p, findOptions: m } = Xa(
        t.getRule(f.ruleId),
        t,
        null,
        r,
        i === o,
      ),
      h = p.findNextMatchSync(n, i, m);
    if (!h) continue;
    let g = h.captureIndices[0].start;
    if (
      !(g >= s) &&
      ((s = g),
      (c = h.captureIndices),
      (l = h.ruleId),
      (u = f.priority),
      s === i)
    )
      break;
  }
  return c
    ? { priorityMatch: u === -1, captureIndices: c, matchedRuleId: l }
    : null;
}
function Xa(e, t, n, r, i) {
  return Wo
    ? { ruleScanner: e.compile(t, n), findOptions: Qa(r, i) }
    : { ruleScanner: e.compileAG(t, n, r, i), findOptions: 0 };
}
function Za(e, t, n, r, i) {
  return Wo
    ? { ruleScanner: e.compileWhile(t, n), findOptions: Qa(r, i) }
    : { ruleScanner: e.compileWhileAG(t, n, r, i), findOptions: 0 };
}
function Qa(e, t) {
  let n = 0;
  return (e || (n |= 1), t || (n |= 4), n);
}
function $a(e, t, n, r, i, a, o) {
  if (a.length === 0) return;
  let s = t.content,
    c = Math.min(a.length, o.length),
    l = [],
    u = o[0].end;
  for (let t = 0; t < c; t++) {
    let c = a[t];
    if (c === null) continue;
    let d = o[t];
    if (d.length === 0) continue;
    if (d.start > u) break;
    for (; l.length > 0 && l[l.length - 1].endPos <= d.start;)
      (i.produceFromScopes(l[l.length - 1].scopes, l[l.length - 1].endPos),
        l.pop());
    if (
      (l.length > 0
        ? i.produceFromScopes(l[l.length - 1].scopes, d.start)
        : i.produce(r, d.start),
      c.retokenizeCapturedWithRuleId)
    ) {
      let t = c.getName(s, o),
        a = r.contentNameScopesList.pushAttributed(t, e),
        l = c.getContentName(s, o),
        u = a.pushAttributed(l, e),
        f = r.push(c.retokenizeCapturedWithRuleId, d.start, -1, !1, null, a, u),
        p = e.createOnigString(s.substring(0, d.end));
      (Ga(e, p, n && d.start === 0, d.start, f, i, !1, 0), La(p));
      continue;
    }
    let f = c.getName(s, o);
    if (f !== null) {
      let t = (
        l.length > 0 ? l[l.length - 1].scopes : r.contentNameScopesList
      ).pushAttributed(f, e);
      l.push(new Ko(t, d.end));
    }
  }
  for (; l.length > 0;)
    (i.produceFromScopes(l[l.length - 1].scopes, l[l.length - 1].endPos),
      l.pop());
}
function eo(e, t, n, r, i, a, o, s) {
  return new qo(e, t, n, r, i, a, o, s);
}
function to(e, t, n, r, i) {
  let a = Pa(t, no),
    o = Lo.getCompiledRuleId(n, r, i.repository);
  for (let n of a)
    e.push({
      debugSelector: t,
      matcher: n.matcher,
      ruleId: o,
      grammar: i,
      priority: n.priority,
    });
}
function no(e, t) {
  if (t.length < e.length) return !1;
  let n = 0;
  return e.every((e) => {
    for (let r = n; r < t.length; r++)
      if (ro(t[r], e)) return ((n = r + 1), !0);
    return !1;
  });
}
function ro(e, t) {
  if (!e) return !1;
  if (e === t) return !0;
  let n = t.length;
  return e.length > n && e.substr(0, n) === t && e[n] === `.`;
}
function io(e, t) {
  return (
    (e = va(e)),
    (e.repository = e.repository || {}),
    (e.repository.$self = {
      $vscodeTextmateLocation: e.$vscodeTextmateLocation,
      patterns: e.patterns,
      name: e.scopeName,
    }),
    (e.repository.$base = t || e.repository.$self),
    e
  );
}
var ao,
  oo,
  so,
  co,
  lo,
  uo,
  fo,
  K,
  po,
  mo,
  ho,
  go,
  _o,
  vo,
  yo,
  bo,
  xo,
  So,
  Co,
  wo,
  To,
  Eo,
  Do,
  Oo,
  ko,
  Ao,
  jo,
  Mo,
  No,
  Po,
  Fo,
  Io,
  Lo,
  Ro,
  zo,
  Bo,
  Vo,
  Ho,
  Uo,
  Wo,
  Go,
  Ko,
  qo,
  Jo,
  Yo,
  Xo,
  Zo,
  Qo,
  $o,
  es,
  ts = e(() => {
    ((ao = /\$(\d+)|\${(\d+):\/(downcase|upcase)}/g),
      (oo = class {
        static hasCaptures(e) {
          return e === null ? !1 : ((ao.lastIndex = 0), ao.test(e));
        }
        static replaceCaptures(e, t, n) {
          return e.replace(ao, (e, r, i, a) => {
            let o = n[parseInt(r || i, 10)];
            if (o) {
              let e = t.substring(o.start, o.end);
              for (; e[0] === `.`;) e = e.substring(1);
              switch (a) {
                case `downcase`:
                  return e.toLowerCase();
                case `upcase`:
                  return e.toUpperCase();
                default:
                  return e;
              }
            } else return e;
          });
        }
      }),
      (so = class {
        constructor(e) {
          this.fn = e;
        }
        cache = new Map();
        get(e) {
          if (this.cache.has(e)) return this.cache.get(e);
          let t = this.fn(e);
          return (this.cache.set(e, t), t);
        }
      }),
      (co = class {
        constructor(e, t, n) {
          ((this._colorMap = e), (this._defaults = t), (this._root = n));
        }
        static createFromRawTheme(e, t) {
          return this.createFromParsedTheme(Aa(e), t);
        }
        static createFromParsedTheme(e, t) {
          return ja(e, t);
        }
        _cachedMatchRoot = new so((e) => this._root.match(e));
        getColorMap() {
          return this._colorMap.getColorMap();
        }
        getDefaults() {
          return this._defaults;
        }
        match(e) {
          if (e === null) return this._defaults;
          let t = e.scopeName,
            n = this._cachedMatchRoot
              .get(t)
              .find((t) => Oa(e.parent, t.parentScopes));
          return n ? new uo(n.fontStyle, n.foreground, n.background) : null;
        }
      }),
      (lo = class e {
        constructor(e, t) {
          ((this.parent = e), (this.scopeName = t));
        }
        static push(t, n) {
          for (let r of n) t = new e(t, r);
          return t;
        }
        static from(...t) {
          let n = null;
          for (let r = 0; r < t.length; r++) n = new e(n, t[r]);
          return n;
        }
        push(t) {
          return new e(this, t);
        }
        getSegments() {
          let e = this,
            t = [];
          for (; e;) (t.push(e.scopeName), (e = e.parent));
          return (t.reverse(), t);
        }
        toString() {
          return this.getSegments().join(` `);
        }
        extends(e) {
          return this === e
            ? !0
            : this.parent === null
              ? !1
              : this.parent.extends(e);
        }
        getExtensionIfDefined(e) {
          let t = [],
            n = this;
          for (; n && n !== e;) (t.push(n.scopeName), (n = n.parent));
          return n === e ? t.reverse() : void 0;
        }
      }),
      (uo = class {
        constructor(e, t, n) {
          ((this.fontStyle = e),
            (this.foregroundId = t),
            (this.backgroundId = n));
        }
      }),
      (fo = class {
        constructor(e, t, n, r, i, a) {
          ((this.scope = e),
            (this.parentScopes = t),
            (this.index = n),
            (this.fontStyle = r),
            (this.foreground = i),
            (this.background = a));
        }
      }),
      (K = ((e) => (
        (e[(e.NotSet = -1)] = `NotSet`),
        (e[(e.None = 0)] = `None`),
        (e[(e.Italic = 1)] = `Italic`),
        (e[(e.Bold = 2)] = `Bold`),
        (e[(e.Underline = 4)] = `Underline`),
        (e[(e.Strikethrough = 8)] = `Strikethrough`),
        e
      ))(K || {})),
      (po = class {
        _isFrozen;
        _lastColorId;
        _id2color;
        _color2id;
        constructor(e) {
          if (
            ((this._lastColorId = 0),
            (this._id2color = []),
            (this._color2id = Object.create(null)),
            Array.isArray(e))
          ) {
            this._isFrozen = !0;
            for (let t = 0, n = e.length; t < n; t++)
              ((this._color2id[e[t]] = t), (this._id2color[t] = e[t]));
          } else this._isFrozen = !1;
        }
        getId(e) {
          if (e === null) return 0;
          e = e.toUpperCase();
          let t = this._color2id[e];
          if (t) return t;
          if (this._isFrozen) throw Error(`Missing color in color map - ${e}`);
          return (
            (t = ++this._lastColorId),
            (this._color2id[e] = t),
            (this._id2color[t] = e),
            t
          );
        }
        getColorMap() {
          return this._id2color.slice(0);
        }
      }),
      (mo = Object.freeze([])),
      (ho = class e {
        scopeDepth;
        parentScopes;
        fontStyle;
        foreground;
        background;
        constructor(e, t, n, r, i) {
          ((this.scopeDepth = e),
            (this.parentScopes = t || mo),
            (this.fontStyle = n),
            (this.foreground = r),
            (this.background = i));
        }
        clone() {
          return new e(
            this.scopeDepth,
            this.parentScopes,
            this.fontStyle,
            this.foreground,
            this.background,
          );
        }
        static cloneArr(e) {
          let t = [];
          for (let n = 0, r = e.length; n < r; n++) t[n] = e[n].clone();
          return t;
        }
        acceptOverwrite(e, t, n, r) {
          (this.scopeDepth > e
            ? console.log(`how did this happen?`)
            : (this.scopeDepth = e),
            t !== -1 && (this.fontStyle = t),
            n !== 0 && (this.foreground = n),
            r !== 0 && (this.background = r));
        }
      }),
      (go = class e {
        constructor(e, t = [], n = {}) {
          ((this._mainRule = e),
            (this._children = n),
            (this._rulesWithParentScopes = t));
        }
        _rulesWithParentScopes;
        static _cmpBySpecificity(e, t) {
          if (e.scopeDepth !== t.scopeDepth) return t.scopeDepth - e.scopeDepth;
          let n = 0,
            r = 0;
          for (
            ;
            e.parentScopes[n] === `>` && n++,
              t.parentScopes[r] === `>` && r++,
              !(n >= e.parentScopes.length || r >= t.parentScopes.length);
          ) {
            let i = t.parentScopes[r].length - e.parentScopes[n].length;
            if (i !== 0) return i;
            (n++, r++);
          }
          return t.parentScopes.length - e.parentScopes.length;
        }
        match(t) {
          if (t !== ``) {
            let e = t.indexOf(`.`),
              n,
              r;
            if (
              (e === -1
                ? ((n = t), (r = ``))
                : ((n = t.substring(0, e)), (r = t.substring(e + 1))),
              this._children.hasOwnProperty(n))
            )
              return this._children[n].match(r);
          }
          let n = this._rulesWithParentScopes.concat(this._mainRule);
          return (n.sort(e._cmpBySpecificity), n);
        }
        insert(t, n, r, i, a, o) {
          if (n === ``) {
            this._doInsertHere(t, r, i, a, o);
            return;
          }
          let s = n.indexOf(`.`),
            c,
            l;
          s === -1
            ? ((c = n), (l = ``))
            : ((c = n.substring(0, s)), (l = n.substring(s + 1)));
          let u;
          (this._children.hasOwnProperty(c)
            ? (u = this._children[c])
            : ((u = new e(
                this._mainRule.clone(),
                ho.cloneArr(this._rulesWithParentScopes),
              )),
              (this._children[c] = u)),
            u.insert(t + 1, l, r, i, a, o));
        }
        _doInsertHere(e, t, n, r, i) {
          if (t === null) {
            this._mainRule.acceptOverwrite(e, n, r, i);
            return;
          }
          for (let a = 0, o = this._rulesWithParentScopes.length; a < o; a++) {
            let o = this._rulesWithParentScopes[a];
            if (Ta(o.parentScopes, t) === 0) {
              o.acceptOverwrite(e, n, r, i);
              return;
            }
          }
          (n === -1 && (n = this._mainRule.fontStyle),
            r === 0 && (r = this._mainRule.foreground),
            i === 0 && (i = this._mainRule.background),
            this._rulesWithParentScopes.push(new ho(e, t, n, r, i)));
        }
      }),
      (_o = class e {
        static toBinaryStr(e) {
          return e.toString(2).padStart(32, `0`);
        }
        static print(t) {
          let n = e.getLanguageId(t),
            r = e.getTokenType(t),
            i = e.getFontStyle(t),
            a = e.getForeground(t),
            o = e.getBackground(t);
          console.log({
            languageId: n,
            tokenType: r,
            fontStyle: i,
            foreground: a,
            background: o,
          });
        }
        static getLanguageId(e) {
          return (e & 255) >>> 0;
        }
        static getTokenType(e) {
          return (e & 768) >>> 8;
        }
        static containsBalancedBrackets(e) {
          return (e & 1024) != 0;
        }
        static getFontStyle(e) {
          return (e & 30720) >>> 11;
        }
        static getForeground(e) {
          return (e & 16744448) >>> 15;
        }
        static getBackground(e) {
          return (e & 4278190080) >>> 24;
        }
        static set(t, n, r, i, a, o, s) {
          let c = e.getLanguageId(t),
            l = e.getTokenType(t),
            u = +!!e.containsBalancedBrackets(t),
            d = e.getFontStyle(t),
            f = e.getForeground(t),
            p = e.getBackground(t);
          return (
            n !== 0 && (c = n),
            r !== 8 && (l = Na(r)),
            i !== null && (u = +!!i),
            a !== -1 && (d = a),
            o !== 0 && (f = o),
            s !== 0 && (p = s),
            ((c << 0) |
              (l << 8) |
              (u << 10) |
              (d << 11) |
              (f << 15) |
              (p << 24)) >>>
              0
          );
        }
      }),
      (vo = class {
        constructor(e) {
          this.scopeName = e;
        }
        toKey() {
          return this.scopeName;
        }
      }),
      (yo = class {
        constructor(e, t) {
          ((this.scopeName = e), (this.ruleName = t));
        }
        toKey() {
          return `${this.scopeName}#${this.ruleName}`;
        }
      }),
      (bo = class {
        _references = [];
        _seenReferenceKeys = new Set();
        get references() {
          return this._references;
        }
        visitedRule = new Set();
        add(e) {
          let t = e.toKey();
          this._seenReferenceKeys.has(t) ||
            (this._seenReferenceKeys.add(t), this._references.push(e));
        }
      }),
      (xo = class {
        constructor(e, t) {
          ((this.repo = e),
            (this.initialScopeName = t),
            this.seenFullScopeRequests.add(this.initialScopeName),
            (this.Q = [new vo(this.initialScopeName)]));
        }
        seenFullScopeRequests = new Set();
        seenPartialScopeRequests = new Set();
        Q;
        processQueue() {
          let e = this.Q;
          this.Q = [];
          let t = new bo();
          for (let n of e) Ra(n, this.initialScopeName, this.repo, t);
          for (let e of t.references)
            if (e instanceof vo) {
              if (this.seenFullScopeRequests.has(e.scopeName)) continue;
              (this.seenFullScopeRequests.add(e.scopeName), this.Q.push(e));
            } else {
              if (
                this.seenFullScopeRequests.has(e.scopeName) ||
                this.seenPartialScopeRequests.has(e.toKey())
              )
                continue;
              (this.seenPartialScopeRequests.add(e.toKey()), this.Q.push(e));
            }
        }
      }),
      (So = class {
        kind = 0;
      }),
      (Co = class {
        kind = 1;
      }),
      (wo = class {
        constructor(e) {
          this.ruleName = e;
        }
        kind = 2;
      }),
      (To = class {
        constructor(e) {
          this.scopeName = e;
        }
        kind = 3;
      }),
      (Eo = class {
        constructor(e, t) {
          ((this.scopeName = e), (this.ruleName = t));
        }
        kind = 4;
      }),
      (Do = /\\(\d+)/),
      (Oo = /\\(\d+)/g),
      (ko = -1),
      (Ao = -2),
      (jo = class {
        $location;
        id;
        _nameIsCapturing;
        _name;
        _contentNameIsCapturing;
        _contentName;
        constructor(e, t, n, r) {
          ((this.$location = e),
            (this.id = t),
            (this._name = n || null),
            (this._nameIsCapturing = oo.hasCaptures(this._name)),
            (this._contentName = r || null),
            (this._contentNameIsCapturing = oo.hasCaptures(this._contentName)));
        }
        get debugName() {
          let e = this.$location
            ? `${Ca(this.$location.filename)}:${this.$location.line}`
            : `unknown`;
          return `${this.constructor.name}#${this.id} @ ${e}`;
        }
        getName(e, t) {
          return !this._nameIsCapturing ||
            this._name === null ||
            e === null ||
            t === null
            ? this._name
            : oo.replaceCaptures(this._name, e, t);
        }
        getContentName(e, t) {
          return !this._contentNameIsCapturing || this._contentName === null
            ? this._contentName
            : oo.replaceCaptures(this._contentName, e, t);
        }
      }),
      (Mo = class extends jo {
        retokenizeCapturedWithRuleId;
        constructor(e, t, n, r, i) {
          (super(e, t, n, r), (this.retokenizeCapturedWithRuleId = i));
        }
        dispose() {}
        collectPatterns(e, t) {
          throw Error(`Not supported!`);
        }
        compile(e, t) {
          throw Error(`Not supported!`);
        }
        compileAG(e, t, n, r) {
          throw Error(`Not supported!`);
        }
      }),
      (No = class extends jo {
        _match;
        captures;
        _cachedCompiledPatterns;
        constructor(e, t, n, r, i) {
          (super(e, t, n, null),
            (this._match = new Ro(r, this.id)),
            (this.captures = i),
            (this._cachedCompiledPatterns = null));
        }
        dispose() {
          this._cachedCompiledPatterns &&=
            (this._cachedCompiledPatterns.dispose(), null);
        }
        get debugMatchRegExp() {
          return `${this._match.source}`;
        }
        collectPatterns(e, t) {
          t.push(this._match);
        }
        compile(e, t) {
          return this._getCachedCompiledPatterns(e).compile(e);
        }
        compileAG(e, t, n, r) {
          return this._getCachedCompiledPatterns(e).compileAG(e, n, r);
        }
        _getCachedCompiledPatterns(e) {
          return (
            this._cachedCompiledPatterns ||
              ((this._cachedCompiledPatterns = new zo()),
              this.collectPatterns(e, this._cachedCompiledPatterns)),
            this._cachedCompiledPatterns
          );
        }
      }),
      (Po = class extends jo {
        hasMissingPatterns;
        patterns;
        _cachedCompiledPatterns;
        constructor(e, t, n, r, i) {
          (super(e, t, n, r),
            (this.patterns = i.patterns),
            (this.hasMissingPatterns = i.hasMissingPatterns),
            (this._cachedCompiledPatterns = null));
        }
        dispose() {
          this._cachedCompiledPatterns &&=
            (this._cachedCompiledPatterns.dispose(), null);
        }
        collectPatterns(e, t) {
          for (let n of this.patterns) e.getRule(n).collectPatterns(e, t);
        }
        compile(e, t) {
          return this._getCachedCompiledPatterns(e).compile(e);
        }
        compileAG(e, t, n, r) {
          return this._getCachedCompiledPatterns(e).compileAG(e, n, r);
        }
        _getCachedCompiledPatterns(e) {
          return (
            this._cachedCompiledPatterns ||
              ((this._cachedCompiledPatterns = new zo()),
              this.collectPatterns(e, this._cachedCompiledPatterns)),
            this._cachedCompiledPatterns
          );
        }
      }),
      (Fo = class extends jo {
        _begin;
        beginCaptures;
        _end;
        endHasBackReferences;
        endCaptures;
        applyEndPatternLast;
        hasMissingPatterns;
        patterns;
        _cachedCompiledPatterns;
        constructor(e, t, n, r, i, a, o, s, c, l) {
          (super(e, t, n, r),
            (this._begin = new Ro(i, this.id)),
            (this.beginCaptures = a),
            (this._end = new Ro(o || `￿`, -1)),
            (this.endHasBackReferences = this._end.hasBackReferences),
            (this.endCaptures = s),
            (this.applyEndPatternLast = c || !1),
            (this.patterns = l.patterns),
            (this.hasMissingPatterns = l.hasMissingPatterns),
            (this._cachedCompiledPatterns = null));
        }
        dispose() {
          this._cachedCompiledPatterns &&=
            (this._cachedCompiledPatterns.dispose(), null);
        }
        get debugBeginRegExp() {
          return `${this._begin.source}`;
        }
        get debugEndRegExp() {
          return `${this._end.source}`;
        }
        getEndWithResolvedBackReferences(e, t) {
          return this._end.resolveBackReferences(e, t);
        }
        collectPatterns(e, t) {
          t.push(this._begin);
        }
        compile(e, t) {
          return this._getCachedCompiledPatterns(e, t).compile(e);
        }
        compileAG(e, t, n, r) {
          return this._getCachedCompiledPatterns(e, t).compileAG(e, n, r);
        }
        _getCachedCompiledPatterns(e, t) {
          if (!this._cachedCompiledPatterns) {
            this._cachedCompiledPatterns = new zo();
            for (let t of this.patterns)
              e.getRule(t).collectPatterns(e, this._cachedCompiledPatterns);
            this.applyEndPatternLast
              ? this._cachedCompiledPatterns.push(
                  this._end.hasBackReferences ? this._end.clone() : this._end,
                )
              : this._cachedCompiledPatterns.unshift(
                  this._end.hasBackReferences ? this._end.clone() : this._end,
                );
          }
          return (
            this._end.hasBackReferences &&
              (this.applyEndPatternLast
                ? this._cachedCompiledPatterns.setSource(
                    this._cachedCompiledPatterns.length() - 1,
                    t,
                  )
                : this._cachedCompiledPatterns.setSource(0, t)),
            this._cachedCompiledPatterns
          );
        }
      }),
      (Io = class extends jo {
        _begin;
        beginCaptures;
        whileCaptures;
        _while;
        whileHasBackReferences;
        hasMissingPatterns;
        patterns;
        _cachedCompiledPatterns;
        _cachedCompiledWhilePatterns;
        constructor(e, t, n, r, i, a, o, s, c) {
          (super(e, t, n, r),
            (this._begin = new Ro(i, this.id)),
            (this.beginCaptures = a),
            (this.whileCaptures = s),
            (this._while = new Ro(o, Ao)),
            (this.whileHasBackReferences = this._while.hasBackReferences),
            (this.patterns = c.patterns),
            (this.hasMissingPatterns = c.hasMissingPatterns),
            (this._cachedCompiledPatterns = null),
            (this._cachedCompiledWhilePatterns = null));
        }
        dispose() {
          ((this._cachedCompiledPatterns &&=
            (this._cachedCompiledPatterns.dispose(), null)),
            (this._cachedCompiledWhilePatterns &&=
              (this._cachedCompiledWhilePatterns.dispose(), null)));
        }
        get debugBeginRegExp() {
          return `${this._begin.source}`;
        }
        get debugWhileRegExp() {
          return `${this._while.source}`;
        }
        getWhileWithResolvedBackReferences(e, t) {
          return this._while.resolveBackReferences(e, t);
        }
        collectPatterns(e, t) {
          t.push(this._begin);
        }
        compile(e, t) {
          return this._getCachedCompiledPatterns(e).compile(e);
        }
        compileAG(e, t, n, r) {
          return this._getCachedCompiledPatterns(e).compileAG(e, n, r);
        }
        _getCachedCompiledPatterns(e) {
          if (!this._cachedCompiledPatterns) {
            this._cachedCompiledPatterns = new zo();
            for (let t of this.patterns)
              e.getRule(t).collectPatterns(e, this._cachedCompiledPatterns);
          }
          return this._cachedCompiledPatterns;
        }
        compileWhile(e, t) {
          return this._getCachedCompiledWhilePatterns(e, t).compile(e);
        }
        compileWhileAG(e, t, n, r) {
          return this._getCachedCompiledWhilePatterns(e, t).compileAG(e, n, r);
        }
        _getCachedCompiledWhilePatterns(e, t) {
          return (
            this._cachedCompiledWhilePatterns ||
              ((this._cachedCompiledWhilePatterns = new zo()),
              this._cachedCompiledWhilePatterns.push(
                this._while.hasBackReferences
                  ? this._while.clone()
                  : this._while,
              )),
            this._while.hasBackReferences &&
              this._cachedCompiledWhilePatterns.setSource(0, t || `￿`),
            this._cachedCompiledWhilePatterns
          );
        }
      }),
      (Lo = class e {
        static createCaptureRule(e, t, n, r, i) {
          return e.registerRule((e) => new Mo(t, e, n, r, i));
        }
        static getCompiledRuleId(t, n, r) {
          return (
            t.id ||
              n.registerRule((i) => {
                if (((t.id = i), t.match))
                  return new No(
                    t.$vscodeTextmateLocation,
                    t.id,
                    t.name,
                    t.match,
                    e._compileCaptures(t.captures, n, r),
                  );
                if (t.begin === void 0) {
                  t.repository && (r = Sa({}, r, t.repository));
                  let i = t.patterns;
                  return (
                    i === void 0 && t.include && (i = [{ include: t.include }]),
                    new Po(
                      t.$vscodeTextmateLocation,
                      t.id,
                      t.name,
                      t.contentName,
                      e._compilePatterns(i, n, r),
                    )
                  );
                }
                return t.while
                  ? new Io(
                      t.$vscodeTextmateLocation,
                      t.id,
                      t.name,
                      t.contentName,
                      t.begin,
                      e._compileCaptures(t.beginCaptures || t.captures, n, r),
                      t.while,
                      e._compileCaptures(t.whileCaptures || t.captures, n, r),
                      e._compilePatterns(t.patterns, n, r),
                    )
                  : new Fo(
                      t.$vscodeTextmateLocation,
                      t.id,
                      t.name,
                      t.contentName,
                      t.begin,
                      e._compileCaptures(t.beginCaptures || t.captures, n, r),
                      t.end,
                      e._compileCaptures(t.endCaptures || t.captures, n, r),
                      t.applyEndPatternLast,
                      e._compilePatterns(t.patterns, n, r),
                    );
              }),
            t.id
          );
        }
        static _compileCaptures(t, n, r) {
          let i = [];
          if (t) {
            let a = 0;
            for (let e in t) {
              if (e === `$vscodeTextmateLocation`) continue;
              let t = parseInt(e, 10);
              t > a && (a = t);
            }
            for (let e = 0; e <= a; e++) i[e] = null;
            for (let a in t) {
              if (a === `$vscodeTextmateLocation`) continue;
              let o = parseInt(a, 10),
                s = 0;
              (t[a].patterns && (s = e.getCompiledRuleId(t[a], n, r)),
                (i[o] = e.createCaptureRule(
                  n,
                  t[a].$vscodeTextmateLocation,
                  t[a].name,
                  t[a].contentName,
                  s,
                )));
            }
          }
          return i;
        }
        static _compilePatterns(t, n, r) {
          let i = [];
          if (t)
            for (let a = 0, o = t.length; a < o; a++) {
              let o = t[a],
                s = -1;
              if (o.include) {
                let t = Ha(o.include);
                switch (t.kind) {
                  case 0:
                  case 1:
                    s = e.getCompiledRuleId(r[o.include], n, r);
                    break;
                  case 2:
                    let i = r[t.ruleName];
                    i && (s = e.getCompiledRuleId(i, n, r));
                    break;
                  case 3:
                  case 4:
                    let a = t.scopeName,
                      c = t.kind === 4 ? t.ruleName : null,
                      l = n.getExternalGrammar(a, r);
                    if (l)
                      if (c) {
                        let t = l.repository[c];
                        t && (s = e.getCompiledRuleId(t, n, l.repository));
                      } else
                        s = e.getCompiledRuleId(
                          l.repository.$self,
                          n,
                          l.repository,
                        );
                    break;
                }
              } else s = e.getCompiledRuleId(o, n, r);
              if (s !== -1) {
                let e = n.getRule(s),
                  t = !1;
                if (
                  ((e instanceof Po || e instanceof Fo || e instanceof Io) &&
                    e.hasMissingPatterns &&
                    e.patterns.length === 0 &&
                    (t = !0),
                  t)
                )
                  continue;
                i.push(s);
              }
            }
          return {
            patterns: i,
            hasMissingPatterns: (t ? t.length : 0) !== i.length,
          };
        }
      }),
      (Ro = class e {
        source;
        ruleId;
        hasAnchor;
        hasBackReferences;
        _anchorCache;
        constructor(e, t) {
          if (e && typeof e == `string`) {
            let t = e.length,
              n = 0,
              r = [],
              i = !1;
            for (let a = 0; a < t; a++)
              if (e.charAt(a) === `\\` && a + 1 < t) {
                let t = e.charAt(a + 1);
                (t === `z`
                  ? (r.push(e.substring(n, a)),
                    r.push(`$(?!\\n)(?<!\\n)`),
                    (n = a + 2))
                  : (t === `A` || t === `G`) && (i = !0),
                  a++);
              }
            ((this.hasAnchor = i),
              n === 0
                ? (this.source = e)
                : (r.push(e.substring(n, t)), (this.source = r.join(``))));
          } else ((this.hasAnchor = !1), (this.source = e));
          (this.hasAnchor
            ? (this._anchorCache = this._buildAnchorCache())
            : (this._anchorCache = null),
            (this.ruleId = t),
            typeof this.source == `string`
              ? (this.hasBackReferences = Do.test(this.source))
              : (this.hasBackReferences = !1));
        }
        clone() {
          return new e(this.source, this.ruleId);
        }
        setSource(e) {
          this.source !== e &&
            ((this.source = e),
            this.hasAnchor && (this._anchorCache = this._buildAnchorCache()));
        }
        resolveBackReferences(e, t) {
          if (typeof this.source != `string`)
            throw Error(
              `This method should only be called if the source is a string`,
            );
          let n = t.map((t) => e.substring(t.start, t.end));
          return (
            (Oo.lastIndex = 0),
            this.source.replace(Oo, (e, t) => Da(n[parseInt(t, 10)] || ``))
          );
        }
        _buildAnchorCache() {
          if (typeof this.source != `string`)
            throw Error(
              `This method should only be called if the source is a string`,
            );
          let e = [],
            t = [],
            n = [],
            r = [],
            i,
            a,
            o,
            s;
          for (i = 0, a = this.source.length; i < a; i++)
            ((o = this.source.charAt(i)),
              (e[i] = o),
              (t[i] = o),
              (n[i] = o),
              (r[i] = o),
              o === `\\` &&
                i + 1 < a &&
                ((s = this.source.charAt(i + 1)),
                s === `A`
                  ? ((e[i + 1] = `￿`),
                    (t[i + 1] = `￿`),
                    (n[i + 1] = `A`),
                    (r[i + 1] = `A`))
                  : s === `G`
                    ? ((e[i + 1] = `￿`),
                      (t[i + 1] = `G`),
                      (n[i + 1] = `￿`),
                      (r[i + 1] = `G`))
                    : ((e[i + 1] = s),
                      (t[i + 1] = s),
                      (n[i + 1] = s),
                      (r[i + 1] = s)),
                i++));
          return {
            A0_G0: e.join(``),
            A0_G1: t.join(``),
            A1_G0: n.join(``),
            A1_G1: r.join(``),
          };
        }
        resolveAnchors(e, t) {
          return !this.hasAnchor ||
            !this._anchorCache ||
            typeof this.source != `string`
            ? this.source
            : e
              ? t
                ? this._anchorCache.A1_G1
                : this._anchorCache.A1_G0
              : t
                ? this._anchorCache.A0_G1
                : this._anchorCache.A0_G0;
        }
      }),
      (zo = class {
        _items;
        _hasAnchors;
        _cached;
        _anchorCache;
        constructor() {
          ((this._items = []),
            (this._hasAnchors = !1),
            (this._cached = null),
            (this._anchorCache = {
              A0_G0: null,
              A0_G1: null,
              A1_G0: null,
              A1_G1: null,
            }));
        }
        dispose() {
          this._disposeCaches();
        }
        _disposeCaches() {
          ((this._cached &&= (this._cached.dispose(), null)),
            this._anchorCache.A0_G0 &&
              (this._anchorCache.A0_G0.dispose(),
              (this._anchorCache.A0_G0 = null)),
            this._anchorCache.A0_G1 &&
              (this._anchorCache.A0_G1.dispose(),
              (this._anchorCache.A0_G1 = null)),
            this._anchorCache.A1_G0 &&
              (this._anchorCache.A1_G0.dispose(),
              (this._anchorCache.A1_G0 = null)),
            this._anchorCache.A1_G1 &&
              (this._anchorCache.A1_G1.dispose(),
              (this._anchorCache.A1_G1 = null)));
        }
        push(e) {
          (this._items.push(e),
            (this._hasAnchors = this._hasAnchors || e.hasAnchor));
        }
        unshift(e) {
          (this._items.unshift(e),
            (this._hasAnchors = this._hasAnchors || e.hasAnchor));
        }
        length() {
          return this._items.length;
        }
        setSource(e, t) {
          this._items[e].source !== t &&
            (this._disposeCaches(), this._items[e].setSource(t));
        }
        compile(e) {
          if (!this._cached) {
            let t = this._items.map((e) => e.source);
            this._cached = new Bo(
              e,
              t,
              this._items.map((e) => e.ruleId),
            );
          }
          return this._cached;
        }
        compileAG(e, t, n) {
          return this._hasAnchors
            ? t
              ? n
                ? (this._anchorCache.A1_G1 ||
                    (this._anchorCache.A1_G1 = this._resolveAnchors(e, t, n)),
                  this._anchorCache.A1_G1)
                : (this._anchorCache.A1_G0 ||
                    (this._anchorCache.A1_G0 = this._resolveAnchors(e, t, n)),
                  this._anchorCache.A1_G0)
              : n
                ? (this._anchorCache.A0_G1 ||
                    (this._anchorCache.A0_G1 = this._resolveAnchors(e, t, n)),
                  this._anchorCache.A0_G1)
                : (this._anchorCache.A0_G0 ||
                    (this._anchorCache.A0_G0 = this._resolveAnchors(e, t, n)),
                  this._anchorCache.A0_G0)
            : this.compile(e);
        }
        _resolveAnchors(e, t, n) {
          return new Bo(
            e,
            this._items.map((e) => e.resolveAnchors(t, n)),
            this._items.map((e) => e.ruleId),
          );
        }
      }),
      (Bo = class {
        constructor(e, t, n) {
          ((this.regExps = t),
            (this.rules = n),
            (this.scanner = e.createOnigScanner(t)));
        }
        scanner;
        dispose() {
          typeof this.scanner.dispose == `function` && this.scanner.dispose();
        }
        toString() {
          let e = [];
          for (let t = 0, n = this.rules.length; t < n; t++)
            e.push(`   - ` + this.rules[t] + `: ` + this.regExps[t]);
          return e.join(`
`);
        }
        findNextMatchSync(e, t, n) {
          let r = this.scanner.findNextMatchSync(e, t, n);
          return r
            ? { ruleId: this.rules[r.index], captureIndices: r.captureIndices }
            : null;
        }
      }),
      (Vo = class {
        constructor(e, t) {
          ((this.languageId = e), (this.tokenType = t));
        }
      }),
      (Ho = class e {
        _defaultAttributes;
        _embeddedLanguagesMatcher;
        constructor(e, t) {
          ((this._defaultAttributes = new Vo(e, 8)),
            (this._embeddedLanguagesMatcher = new Uo(Object.entries(t || {}))));
        }
        getDefaultAttributes() {
          return this._defaultAttributes;
        }
        getBasicScopeAttributes(t) {
          return t === null
            ? e._NULL_SCOPE_METADATA
            : this._getBasicScopeAttributes.get(t);
        }
        static _NULL_SCOPE_METADATA = new Vo(0, 0);
        _getBasicScopeAttributes = new so(
          (e) => new Vo(this._scopeToLanguage(e), this._toStandardTokenType(e)),
        );
        _scopeToLanguage(e) {
          return this._embeddedLanguagesMatcher.match(e) || 0;
        }
        _toStandardTokenType(t) {
          let n = t.match(e.STANDARD_TOKEN_TYPE_REGEXP);
          if (!n) return 8;
          switch (n[1]) {
            case `comment`:
              return 1;
            case `string`:
              return 2;
            case `regex`:
              return 3;
            case `meta.embedded`:
              return 0;
          }
          throw Error(`Unexpected match for standard token type!`);
        }
        static STANDARD_TOKEN_TYPE_REGEXP =
          /\b(comment|string|regex|meta\.embedded)\b/;
      }),
      (Uo = class {
        values;
        scopesRegExp;
        constructor(e) {
          if (e.length === 0)
            ((this.values = null), (this.scopesRegExp = null));
          else {
            this.values = new Map(e);
            let t = e.map(([e, t]) => Da(e));
            (t.sort(),
              t.reverse(),
              (this.scopesRegExp = RegExp(`^((${t.join(`)|(`)}))($|\\.)`, ``)));
          }
        }
        match(e) {
          if (!this.scopesRegExp) return;
          let t = e.match(this.scopesRegExp);
          if (t) return this.values.get(t[1]);
        }
      }),
      typeof process < `u` && {}.VSCODE_TEXTMATE_DEBUG,
      (Wo = !1),
      (Go = class {
        constructor(e, t) {
          ((this.stack = e), (this.stoppedEarly = t));
        }
      }),
      (Ko = class {
        scopes;
        endPos;
        constructor(e, t) {
          ((this.scopes = e), (this.endPos = t));
        }
      }),
      (qo = class {
        constructor(e, t, n, r, i, a, o, s) {
          if (
            ((this._rootScopeName = e),
            (this.balancedBracketSelectors = a),
            (this._onigLib = s),
            (this._basicScopeAttributesProvider = new Ho(n, r)),
            (this._rootId = -1),
            (this._lastRuleId = 0),
            (this._ruleId2desc = [null]),
            (this._includedGrammars = {}),
            (this._grammarRepository = o),
            (this._grammar = io(t, null)),
            (this._injections = null),
            (this._tokenTypeMatchers = []),
            i)
          )
            for (let e of Object.keys(i)) {
              let t = Pa(e, no);
              for (let n of t)
                this._tokenTypeMatchers.push({
                  matcher: n.matcher,
                  type: i[e],
                });
            }
        }
        _rootId;
        _lastRuleId;
        _ruleId2desc;
        _includedGrammars;
        _grammarRepository;
        _grammar;
        _injections;
        _basicScopeAttributesProvider;
        _tokenTypeMatchers;
        get themeProvider() {
          return this._grammarRepository;
        }
        dispose() {
          for (let e of this._ruleId2desc) e && e.dispose();
        }
        createOnigScanner(e) {
          return this._onigLib.createOnigScanner(e);
        }
        createOnigString(e) {
          return this._onigLib.createOnigString(e);
        }
        getMetadataForScope(e) {
          return this._basicScopeAttributesProvider.getBasicScopeAttributes(e);
        }
        _collectInjections() {
          let e = {
              lookup: (e) =>
                e === this._rootScopeName
                  ? this._grammar
                  : this.getExternalGrammar(e),
              injections: (e) => this._grammarRepository.injections(e),
            },
            t = [],
            n = this._rootScopeName,
            r = e.lookup(n);
          if (r) {
            let e = r.injections;
            if (e) for (let n in e) to(t, n, e[n], this, r);
            let i = this._grammarRepository.injections(n);
            i &&
              i.forEach((e) => {
                let n = this.getExternalGrammar(e);
                if (n) {
                  let e = n.injectionSelector;
                  e && to(t, e, n, this, n);
                }
              });
          }
          return (t.sort((e, t) => e.priority - t.priority), t);
        }
        getInjections() {
          return (
            this._injections === null &&
              (this._injections = this._collectInjections()),
            this._injections
          );
        }
        registerRule(e) {
          let t = ++this._lastRuleId,
            n = e(Ua(t));
          return ((this._ruleId2desc[t] = n), n);
        }
        getRule(e) {
          return this._ruleId2desc[Wa(e)];
        }
        getExternalGrammar(e, t) {
          if (this._includedGrammars[e]) return this._includedGrammars[e];
          if (this._grammarRepository) {
            let n = this._grammarRepository.lookup(e);
            if (n)
              return (
                (this._includedGrammars[e] = io(n, t && t.$base)),
                this._includedGrammars[e]
              );
          }
        }
        tokenizeLine(e, t, n = 0) {
          let r = this._tokenize(e, t, !1, n);
          return {
            tokens: r.lineTokens.getResult(r.ruleStack, r.lineLength),
            ruleStack: r.ruleStack,
            stoppedEarly: r.stoppedEarly,
          };
        }
        tokenizeLine2(e, t, n = 0) {
          let r = this._tokenize(e, t, !0, n);
          return {
            tokens: r.lineTokens.getBinaryResult(r.ruleStack, r.lineLength),
            ruleStack: r.ruleStack,
            stoppedEarly: r.stoppedEarly,
          };
        }
        _tokenize(e, t, n, r) {
          this._rootId === -1 &&
            ((this._rootId = Lo.getCompiledRuleId(
              this._grammar.repository.$self,
              this,
              this._grammar.repository,
            )),
            this.getInjections());
          let i;
          if (!t || t === Yo.NULL) {
            i = !0;
            let e = this._basicScopeAttributesProvider.getDefaultAttributes(),
              n = this.themeProvider.getDefaults(),
              r = _o.set(
                0,
                e.languageId,
                e.tokenType,
                null,
                n.fontStyle,
                n.foregroundId,
                n.backgroundId,
              ),
              a = this.getRule(this._rootId).getName(null, null),
              o;
            ((o = a
              ? Jo.createRootAndLookUpScopeName(a, r, this)
              : Jo.createRoot(`unknown`, r)),
              (t = new Yo(null, this._rootId, -1, -1, !1, null, o, o)));
          } else ((i = !1), t.reset());
          e += `
`;
          let a = this.createOnigString(e),
            o = a.content.length,
            s = new Zo(
              n,
              e,
              this._tokenTypeMatchers,
              this.balancedBracketSelectors,
            ),
            c = Ga(this, a, i, 0, t, s, !0, r);
          return (
            La(a),
            {
              lineLength: o,
              lineTokens: s,
              ruleStack: c.stack,
              stoppedEarly: c.stoppedEarly,
            }
          );
        }
      }),
      (Jo = class e {
        constructor(e, t, n) {
          ((this.parent = e), (this.scopePath = t), (this.tokenAttributes = n));
        }
        static fromExtension(t, n) {
          let r = t,
            i = t?.scopePath ?? null;
          for (let t of n)
            ((i = lo.push(i, t.scopeNames)),
              (r = new e(r, i, t.encodedTokenAttributes)));
          return r;
        }
        static createRoot(t, n) {
          return new e(null, new lo(null, t), n);
        }
        static createRootAndLookUpScopeName(t, n, r) {
          let i = r.getMetadataForScope(t),
            a = new lo(null, t),
            o = r.themeProvider.themeMatch(a),
            s = e.mergeAttributes(n, i, o);
          return new e(null, a, s);
        }
        get scopeName() {
          return this.scopePath.scopeName;
        }
        toString() {
          return this.getScopeNames().join(` `);
        }
        equals(t) {
          return e.equals(this, t);
        }
        static equals(e, t) {
          do {
            if (e === t || (!e && !t)) return !0;
            if (
              !e ||
              !t ||
              e.scopeName !== t.scopeName ||
              e.tokenAttributes !== t.tokenAttributes
            )
              return !1;
            ((e = e.parent), (t = t.parent));
          } while (!0);
        }
        static mergeAttributes(e, t, n) {
          let r = -1,
            i = 0,
            a = 0;
          return (
            n !== null &&
              ((r = n.fontStyle), (i = n.foregroundId), (a = n.backgroundId)),
            _o.set(e, t.languageId, t.tokenType, null, r, i, a)
          );
        }
        pushAttributed(t, n) {
          if (t === null) return this;
          if (t.indexOf(` `) === -1) return e._pushAttributed(this, t, n);
          let r = t.split(/ /g),
            i = this;
          for (let t of r) i = e._pushAttributed(i, t, n);
          return i;
        }
        static _pushAttributed(t, n, r) {
          let i = r.getMetadataForScope(n),
            a = t.scopePath.push(n),
            o = r.themeProvider.themeMatch(a),
            s = e.mergeAttributes(t.tokenAttributes, i, o);
          return new e(t, a, s);
        }
        getScopeNames() {
          return this.scopePath.getSegments();
        }
        getExtensionIfDefined(e) {
          let t = [],
            n = this;
          for (; n && n !== e;)
            (t.push({
              encodedTokenAttributes: n.tokenAttributes,
              scopeNames: n.scopePath.getExtensionIfDefined(
                n.parent?.scopePath ?? null,
              ),
            }),
              (n = n.parent));
          return n === e ? t.reverse() : void 0;
        }
      }),
      (Yo = class e {
        constructor(e, t, n, r, i, a, o, s) {
          ((this.parent = e),
            (this.ruleId = t),
            (this.beginRuleCapturedEOL = i),
            (this.endRule = a),
            (this.nameScopesList = o),
            (this.contentNameScopesList = s),
            (this.depth = this.parent ? this.parent.depth + 1 : 1),
            (this._enterPos = n),
            (this._anchorPos = r));
        }
        _stackElementBrand = void 0;
        static NULL = new e(null, 0, 0, 0, !1, null, null, null);
        _enterPos;
        _anchorPos;
        depth;
        equals(t) {
          return t === null ? !1 : e._equals(this, t);
        }
        static _equals(e, t) {
          return e === t
            ? !0
            : this._structuralEquals(e, t)
              ? Jo.equals(e.contentNameScopesList, t.contentNameScopesList)
              : !1;
        }
        static _structuralEquals(e, t) {
          do {
            if (e === t || (!e && !t)) return !0;
            if (
              !e ||
              !t ||
              e.depth !== t.depth ||
              e.ruleId !== t.ruleId ||
              e.endRule !== t.endRule
            )
              return !1;
            ((e = e.parent), (t = t.parent));
          } while (!0);
        }
        clone() {
          return this;
        }
        static _reset(e) {
          for (; e;) ((e._enterPos = -1), (e._anchorPos = -1), (e = e.parent));
        }
        reset() {
          e._reset(this);
        }
        pop() {
          return this.parent;
        }
        safePop() {
          return this.parent ? this.parent : this;
        }
        push(t, n, r, i, a, o, s) {
          return new e(this, t, n, r, i, a, o, s);
        }
        getEnterPos() {
          return this._enterPos;
        }
        getAnchorPos() {
          return this._anchorPos;
        }
        getRule(e) {
          return e.getRule(this.ruleId);
        }
        toString() {
          let e = [];
          return (this._writeString(e, 0), `[` + e.join(`,`) + `]`);
        }
        _writeString(e, t) {
          return (
            this.parent && (t = this.parent._writeString(e, t)),
            (e[t++] =
              `(${this.ruleId}, ${this.nameScopesList?.toString()}, ${this.contentNameScopesList?.toString()})`),
            t
          );
        }
        withContentNameScopesList(e) {
          return this.contentNameScopesList === e
            ? this
            : this.parent.push(
                this.ruleId,
                this._enterPos,
                this._anchorPos,
                this.beginRuleCapturedEOL,
                this.endRule,
                this.nameScopesList,
                e,
              );
        }
        withEndRule(t) {
          return this.endRule === t
            ? this
            : new e(
                this.parent,
                this.ruleId,
                this._enterPos,
                this._anchorPos,
                this.beginRuleCapturedEOL,
                t,
                this.nameScopesList,
                this.contentNameScopesList,
              );
        }
        hasSameRuleAs(e) {
          let t = this;
          for (; t && t._enterPos === e._enterPos;) {
            if (t.ruleId === e.ruleId) return !0;
            t = t.parent;
          }
          return !1;
        }
        toStateStackFrame() {
          return {
            ruleId: Wa(this.ruleId),
            beginRuleCapturedEOL: this.beginRuleCapturedEOL,
            endRule: this.endRule,
            nameScopesList:
              this.nameScopesList?.getExtensionIfDefined(
                this.parent?.nameScopesList ?? null,
              ) ?? [],
            contentNameScopesList:
              this.contentNameScopesList?.getExtensionIfDefined(
                this.nameScopesList,
              ) ?? [],
          };
        }
        static pushFrame(t, n) {
          let r = Jo.fromExtension(t?.nameScopesList ?? null, n.nameScopesList);
          return new e(
            t,
            Ua(n.ruleId),
            n.enterPos ?? -1,
            n.anchorPos ?? -1,
            n.beginRuleCapturedEOL,
            n.endRule,
            r,
            Jo.fromExtension(r, n.contentNameScopesList),
          );
        }
      }),
      (Xo = class {
        balancedBracketScopes;
        unbalancedBracketScopes;
        allowAny = !1;
        constructor(e, t) {
          ((this.balancedBracketScopes = e.flatMap((e) =>
            e === `*`
              ? ((this.allowAny = !0), [])
              : Pa(e, no).map((e) => e.matcher),
          )),
            (this.unbalancedBracketScopes = t.flatMap((e) =>
              Pa(e, no).map((e) => e.matcher),
            )));
        }
        get matchesAlways() {
          return this.allowAny && this.unbalancedBracketScopes.length === 0;
        }
        get matchesNever() {
          return this.balancedBracketScopes.length === 0 && !this.allowAny;
        }
        match(e) {
          for (let t of this.unbalancedBracketScopes) if (t(e)) return !1;
          for (let t of this.balancedBracketScopes) if (t(e)) return !0;
          return this.allowAny;
        }
      }),
      (Zo = class {
        constructor(e, t, n, r) {
          ((this.balancedBracketSelectors = r),
            (this._emitBinaryTokens = e),
            (this._tokenTypeOverrides = n),
            (this._lineText = null),
            (this._tokens = []),
            (this._binaryTokens = []),
            (this._lastTokenEndIndex = 0));
        }
        _emitBinaryTokens;
        _lineText;
        _tokens;
        _binaryTokens;
        _lastTokenEndIndex;
        _tokenTypeOverrides;
        produce(e, t) {
          this.produceFromScopes(e.contentNameScopesList, t);
        }
        produceFromScopes(e, t) {
          if (this._lastTokenEndIndex >= t) return;
          if (this._emitBinaryTokens) {
            let n = e?.tokenAttributes ?? 0,
              r = !1;
            if (
              (this.balancedBracketSelectors?.matchesAlways && (r = !0),
              this._tokenTypeOverrides.length > 0 ||
                (this.balancedBracketSelectors &&
                  !this.balancedBracketSelectors.matchesAlways &&
                  !this.balancedBracketSelectors.matchesNever))
            ) {
              let t = e?.getScopeNames() ?? [];
              for (let e of this._tokenTypeOverrides)
                e.matcher(t) && (n = _o.set(n, 0, Ma(e.type), null, -1, 0, 0));
              this.balancedBracketSelectors &&
                (r = this.balancedBracketSelectors.match(t));
            }
            if (
              (r && (n = _o.set(n, 0, 8, r, -1, 0, 0)),
              this._binaryTokens.length > 0 &&
                this._binaryTokens[this._binaryTokens.length - 1] === n)
            ) {
              this._lastTokenEndIndex = t;
              return;
            }
            (this._binaryTokens.push(this._lastTokenEndIndex),
              this._binaryTokens.push(n),
              (this._lastTokenEndIndex = t));
            return;
          }
          let n = e?.getScopeNames() ?? [];
          (this._tokens.push({
            startIndex: this._lastTokenEndIndex,
            endIndex: t,
            scopes: n,
          }),
            (this._lastTokenEndIndex = t));
        }
        getResult(e, t) {
          return (
            this._tokens.length > 0 &&
              this._tokens[this._tokens.length - 1].startIndex === t - 1 &&
              this._tokens.pop(),
            this._tokens.length === 0 &&
              ((this._lastTokenEndIndex = -1),
              this.produce(e, t),
              (this._tokens[this._tokens.length - 1].startIndex = 0)),
            this._tokens
          );
        }
        getBinaryResult(e, t) {
          (this._binaryTokens.length > 0 &&
            this._binaryTokens[this._binaryTokens.length - 2] === t - 1 &&
            (this._binaryTokens.pop(), this._binaryTokens.pop()),
            this._binaryTokens.length === 0 &&
              ((this._lastTokenEndIndex = -1),
              this.produce(e, t),
              (this._binaryTokens[this._binaryTokens.length - 2] = 0)));
          let n = new Uint32Array(this._binaryTokens.length);
          for (let e = 0, t = this._binaryTokens.length; e < t; e++)
            n[e] = this._binaryTokens[e];
          return n;
        }
      }),
      (Qo = class {
        constructor(e, t) {
          ((this._onigLib = t), (this._theme = e));
        }
        _grammars = new Map();
        _rawGrammars = new Map();
        _injectionGrammars = new Map();
        _theme;
        dispose() {
          for (let e of this._grammars.values()) e.dispose();
        }
        setTheme(e) {
          this._theme = e;
        }
        getColorMap() {
          return this._theme.getColorMap();
        }
        addGrammar(e, t) {
          (this._rawGrammars.set(e.scopeName, e),
            t && this._injectionGrammars.set(e.scopeName, t));
        }
        lookup(e) {
          return this._rawGrammars.get(e);
        }
        injections(e) {
          return this._injectionGrammars.get(e);
        }
        getDefaults() {
          return this._theme.getDefaults();
        }
        themeMatch(e) {
          return this._theme.match(e);
        }
        grammarForScopeName(e, t, n, r, i) {
          if (!this._grammars.has(e)) {
            let a = this._rawGrammars.get(e);
            if (!a) return null;
            this._grammars.set(e, eo(e, a, t, n, r, i, this, this._onigLib));
          }
          return this._grammars.get(e);
        }
      }),
      ($o = class {
        _options;
        _syncRegistry;
        _ensureGrammarCache;
        constructor(e) {
          ((this._options = e),
            (this._syncRegistry = new Qo(
              co.createFromRawTheme(e.theme, e.colorMap),
              e.onigLib,
            )),
            (this._ensureGrammarCache = new Map()));
        }
        dispose() {
          this._syncRegistry.dispose();
        }
        setTheme(e, t) {
          this._syncRegistry.setTheme(co.createFromRawTheme(e, t));
        }
        getColorMap() {
          return this._syncRegistry.getColorMap();
        }
        loadGrammarWithEmbeddedLanguages(e, t, n) {
          return this.loadGrammarWithConfiguration(e, t, {
            embeddedLanguages: n,
          });
        }
        loadGrammarWithConfiguration(e, t, n) {
          return this._loadGrammar(
            e,
            t,
            n.embeddedLanguages,
            n.tokenTypes,
            new Xo(
              n.balancedBracketSelectors || [],
              n.unbalancedBracketSelectors || [],
            ),
          );
        }
        loadGrammar(e) {
          return this._loadGrammar(e, 0, null, null, null);
        }
        _loadGrammar(e, t, n, r, i) {
          let a = new xo(this._syncRegistry, e);
          for (; a.Q.length > 0;)
            (a.Q.map((e) => this._loadSingleGrammar(e.scopeName)),
              a.processQueue());
          return this._grammarForScopeName(e, t, n, r, i);
        }
        _loadSingleGrammar(e) {
          this._ensureGrammarCache.has(e) ||
            (this._doLoadSingleGrammar(e), this._ensureGrammarCache.set(e, !0));
        }
        _doLoadSingleGrammar(e) {
          let t = this._options.loadGrammar(e);
          if (t) {
            let n =
              typeof this._options.getInjections == `function`
                ? this._options.getInjections(e)
                : void 0;
            this._syncRegistry.addGrammar(t, n);
          }
        }
        addGrammar(e, t = [], n = 0, r = null) {
          return (
            this._syncRegistry.addGrammar(e, t),
            this._grammarForScopeName(e.scopeName, n, r)
          );
        }
        _grammarForScopeName(e, t = 0, n = null, r = null, i = null) {
          return this._syncRegistry.grammarForScopeName(e, t, n, r, i);
        }
      }),
      (es = Yo.NULL));
  });
function ns(e, t) {
  let n = typeof e == `string` ? {} : { ...e.colorReplacements },
    r = typeof e == `string` ? e : e.name;
  for (let [e, i] of Object.entries(t?.colorReplacements || {}))
    typeof i == `string` ? (n[e] = i) : e === r && Object.assign(n, i);
  return n;
}
function rs(e, t) {
  return e && (t?.[e?.toLowerCase()] || e);
}
function is(e) {
  return Array.isArray(e) ? e : [e];
}
async function as(e) {
  return Promise.resolve(typeof e == `function` ? e() : e).then(
    (e) => e.default || e,
  );
}
function os(e) {
  return !e || [`plaintext`, `txt`, `text`, `plain`].includes(e);
}
function ss(e) {
  return e === `ansi` || os(e);
}
function cs(e) {
  return e === `none`;
}
function ls(e) {
  return cs(e);
}
function us(e, t) {
  if (!t) return e;
  ((e.properties ||= {}),
    (e.properties.class ||= []),
    typeof e.properties.class == `string` &&
      (e.properties.class = e.properties.class.split(/\s+/g)),
    Array.isArray(e.properties.class) || (e.properties.class = []));
  let n = Array.isArray(t) ? t : t.split(/\s+/g);
  for (let t of n)
    t && !e.properties.class.includes(t) && e.properties.class.push(t);
  return e;
}
function ds(e, t = !1) {
  if (e.length === 0) return [[``, 0]];
  let n = e.split(/(\r?\n)/g),
    r = 0,
    i = [];
  for (let e = 0; e < n.length; e += 2) {
    let a = t ? n[e] + (n[e + 1] || ``) : n[e];
    (i.push([a, r]), (r += n[e].length), (r += n[e + 1]?.length || 0));
  }
  return i;
}
function fs(e) {
  let t = ds(e, !0).map(([e]) => e);
  function n(n) {
    if (n === e.length)
      return { line: t.length - 1, character: t[t.length - 1].length };
    let r = n,
      i = 0;
    for (let e of t) {
      if (r < e.length) break;
      ((r -= e.length), i++);
    }
    return { line: i, character: r };
  }
  function r(e, n) {
    let r = 0;
    for (let n = 0; n < e; n++) r += t[n].length;
    return ((r += n), r);
  }
  return { lines: t, indexToPos: n, posToIndex: r };
}
function ps(e, t, n) {
  let r = new Set();
  for (let t of e.matchAll(/:?lang=["']([^"']+)["']/g)) {
    let e = t[1].toLowerCase().trim();
    e && r.add(e);
  }
  for (let t of e.matchAll(/(?:```|~~~)([\w-]+)/g)) {
    let e = t[1].toLowerCase().trim();
    e && r.add(e);
  }
  for (let t of e.matchAll(/\\begin\{([\w-]+)\}/g)) {
    let e = t[1].toLowerCase().trim();
    e && r.add(e);
  }
  for (let t of e.matchAll(/<script\s+(?:type|lang)=["']([^"']+)["']/gi)) {
    let e = t[1].toLowerCase().trim(),
      n = e.includes(`/`) ? e.split(`/`).pop() : e;
    n && r.add(n);
  }
  if (!n) return Array.from(r);
  let i = n.getBundledLanguages();
  return Array.from(r).filter((e) => e && i[e]);
}
function ms(e, t) {
  let n = 0,
    r = [];
  for (let i of t)
    (i > n &&
      r.push({ ...e, content: e.content.slice(n, i), offset: e.offset + n }),
      (n = i));
  return (
    n < e.content.length &&
      r.push({ ...e, content: e.content.slice(n), offset: e.offset + n }),
    r
  );
}
function hs(e, t) {
  let n = Array.from(t instanceof Set ? t : new Set(t)).sort((e, t) => e - t);
  return n.length
    ? e.map((e) =>
        e.flatMap((e) => {
          let t = n
            .filter((t) => e.offset < t && t < e.offset + e.content.length)
            .map((t) => t - e.offset)
            .sort((e, t) => e - t);
          return t.length ? ms(e, t) : e;
        }),
      )
    : e;
}
function gs(e, t, n, r, i = `css-vars`) {
  let a = { content: e.content, explanation: e.explanation, offset: e.offset },
    o = t.map((t) => _s(e.variants[t])),
    s = new Set(o.flatMap((e) => Object.keys(e))),
    c = {},
    l = (e, r) => {
      let i = r === `color` ? `` : r === `background-color` ? `-bg` : `-${r}`;
      return n + t[e] + (r === `color` ? `` : i);
    };
  return (
    o.forEach((e, n) => {
      for (let a of s) {
        let s = e[a] || `inherit`;
        if (n === 0 && r && fc.includes(a))
          if (r === dc && o.length > 1) {
            let e = t.findIndex((e) => e === `light`),
              r = t.findIndex((e) => e === `dark`);
            if (e === -1 || r === -1)
              throw new G(
                'When using `defaultColor: "light-dark()"`, you must provide both `light` and `dark` themes',
              );
            let u = o[e][a] || `inherit`,
              d = o[r][a] || `inherit`;
            ((c[a] = `light-dark(${u}, ${d})`),
              i === `css-vars` && (c[l(n, a)] = s));
          } else c[a] = s;
        else i === `css-vars` && (c[l(n, a)] = s);
      }
    }),
    (a.htmlStyle = c),
    a
  );
}
function _s(e) {
  let t = {};
  if (
    (e.color && (t.color = e.color),
    e.bgColor && (t[`background-color`] = e.bgColor),
    e.fontStyle)
  ) {
    (e.fontStyle & K.Italic && (t[`font-style`] = `italic`),
      e.fontStyle & K.Bold && (t[`font-weight`] = `bold`));
    let n = [];
    (e.fontStyle & K.Underline && n.push(`underline`),
      e.fontStyle & K.Strikethrough && n.push(`line-through`),
      n.length && (t[`text-decoration`] = n.join(` `)));
  }
  return t;
}
function vs(e) {
  return typeof e == `string`
    ? e
    : Object.entries(e)
        .map(([e, t]) => `${e}:${t}`)
        .join(`;`);
}
function ys(e, t) {
  pc.set(e, t);
}
function bs(e) {
  return pc.get(e);
}
function xs(e) {
  let t = [],
    n = new Set();
  function r(e) {
    if (n.has(e)) return;
    n.add(e);
    let i = e?.nameScopesList?.scopeName;
    (i && t.push(i), e.parent && r(e.parent));
  }
  return (r(e), t);
}
function Ss(e, t) {
  if (!(e instanceof mc)) throw new G(`Invalid grammar state`);
  return e.getInternalStack(t);
}
function Cs() {
  let e = new WeakMap();
  function t(t) {
    if (!e.has(t.meta)) {
      let n = function (e) {
          if (typeof e == `number`) {
            if (e < 0 || e > t.source.length)
              throw new G(
                `Invalid decoration offset: ${e}. Code length: ${t.source.length}`,
              );
            return { ...r.indexToPos(e), offset: e };
          } else {
            let t = r.lines[e.line];
            if (t === void 0)
              throw new G(
                `Invalid decoration position ${JSON.stringify(e)}. Lines length: ${r.lines.length}`,
              );
            let n = e.character;
            if ((n < 0 && (n = t.length + n), n < 0 || n > t.length))
              throw new G(
                `Invalid decoration position ${JSON.stringify(e)}. Line ${e.line} length: ${t.length}`,
              );
            return { ...e, character: n, offset: r.posToIndex(e.line, n) };
          }
        },
        r = fs(t.source),
        i = (t.options.decorations || []).map((e) => ({
          ...e,
          start: n(e.start),
          end: n(e.end),
        }));
      (ws(i),
        e.set(t.meta, { decorations: i, converter: r, source: t.source }));
    }
    return e.get(t.meta);
  }
  return {
    name: `shiki:decorations`,
    tokens(e) {
      if (this.options.decorations?.length)
        return hs(
          e,
          t(this).decorations.flatMap((e) => [e.start.offset, e.end.offset]),
        );
    },
    code(e) {
      if (!this.options.decorations?.length) return;
      let n = t(this),
        r = Array.from(e.children).filter(
          (e) => e.type === `element` && e.tagName === `span`,
        );
      if (r.length !== n.converter.lines.length)
        throw new G(
          `Number of lines in code element (${r.length}) does not match the number of lines in the source (${n.converter.lines.length}). Failed to apply decorations.`,
        );
      function i(e, t, n, i) {
        let a = r[e],
          s = ``,
          c = -1,
          l = -1;
        if (
          (t === 0 && (c = 0),
          n === 0 && (l = 0),
          n === 1 / 0 && (l = a.children.length),
          c === -1 || l === -1)
        )
          for (let e = 0; e < a.children.length; e++)
            ((s += Ts(a.children[e])),
              c === -1 && s.length === t && (c = e + 1),
              l === -1 && s.length === n && (l = e + 1));
        if (c === -1)
          throw new G(
            `Failed to find start index for decoration ${JSON.stringify(i.start)}`,
          );
        if (l === -1)
          throw new G(
            `Failed to find end index for decoration ${JSON.stringify(i.end)}`,
          );
        let u = a.children.slice(c, l);
        if (!i.alwaysWrap && u.length === a.children.length) o(a, i, `line`);
        else if (!i.alwaysWrap && u.length === 1 && u[0].type === `element`)
          o(u[0], i, `token`);
        else {
          let e = {
            type: `element`,
            tagName: `span`,
            properties: {},
            children: u,
          };
          (o(e, i, `wrapper`), a.children.splice(c, u.length, e));
        }
      }
      function a(e, t) {
        r[e] = o(r[e], t, `line`);
      }
      function o(e, t, n) {
        let r = t.properties || {},
          i = t.transform || ((e) => e);
        return (
          (e.tagName = t.tagName || `span`),
          (e.properties = { ...e.properties, ...r, class: e.properties.class }),
          t.properties?.class && us(e, t.properties.class),
          (e = i(e, n) || e),
          e
        );
      }
      let s = [],
        c = n.decorations.sort(
          (e, t) =>
            t.start.offset - e.start.offset || e.end.offset - t.end.offset,
        );
      for (let e of c) {
        let { start: t, end: n } = e;
        if (t.line === n.line) i(t.line, t.character, n.character, e);
        else if (t.line < n.line) {
          i(t.line, t.character, 1 / 0, e);
          for (let r = t.line + 1; r < n.line; r++) s.unshift(() => a(r, e));
          i(n.line, 0, n.character, e);
        }
      }
      s.forEach((e) => e());
    },
  };
}
function ws(e) {
  for (let t = 0; t < e.length; t++) {
    let n = e[t];
    if (n.start.offset > n.end.offset)
      throw new G(
        `Invalid decoration range: ${JSON.stringify(n.start)} - ${JSON.stringify(n.end)}`,
      );
    for (let r = t + 1; r < e.length; r++) {
      let t = e[r],
        i = n.start.offset <= t.start.offset && t.start.offset < n.end.offset,
        a = n.start.offset < t.end.offset && t.end.offset <= n.end.offset,
        o = t.start.offset <= n.start.offset && n.start.offset < t.end.offset,
        s = t.start.offset < n.end.offset && n.end.offset <= t.end.offset;
      if (i || a || o || s) {
        if (
          (i && a) ||
          (o && s) ||
          (o && n.start.offset === n.end.offset) ||
          (a && t.start.offset === t.end.offset)
        )
          continue;
        throw new G(
          `Decorations ${JSON.stringify(n.start)} and ${JSON.stringify(t.start)} intersect.`,
        );
      }
    }
  }
}
function Ts(e) {
  return e.type === `text`
    ? e.value
    : e.type === `element`
      ? e.children.map(Ts).join(``)
      : ``;
}
function Es(e) {
  let t = Ds(e.transformers || []);
  return [...t.pre, ...t.normal, ...t.post, ...hc];
}
function Ds(e) {
  let t = [],
    n = [],
    r = [];
  for (let i of e)
    switch (i.enforce) {
      case `pre`:
        t.push(i);
        break;
      case `post`:
        n.push(i);
        break;
      default:
        r.push(i);
    }
  return { pre: t, post: n, normal: r };
}
function Os(e, t) {
  let n = e.indexOf(`\x1B`, t);
  if (n !== -1 && e[n + 1] === `[`) {
    let t = e.indexOf(`m`, n);
    if (t !== -1)
      return {
        sequence: e.substring(n + 2, t).split(`;`),
        startPosition: n,
        position: t + 1,
      };
  }
  return { position: e.length };
}
function ks(e) {
  let t = e.shift();
  if (t === `2`) {
    let t = e.splice(0, 3).map((e) => Number.parseInt(e));
    return t.length !== 3 || t.some((e) => Number.isNaN(e))
      ? void 0
      : { type: `rgb`, rgb: t };
  } else if (t === `5`) {
    let t = e.shift();
    if (t) return { type: `table`, index: Number(t) };
  }
}
function As(e) {
  let t = [];
  for (; e.length > 0;) {
    let n = e.shift();
    if (!n) continue;
    let r = Number.parseInt(n);
    if (!Number.isNaN(r))
      if (r === 0) t.push({ type: `resetAll` });
      else if (r <= 9) _c[r] && t.push({ type: `setDecoration`, value: _c[r] });
      else if (r <= 29) {
        let e = _c[r - 20];
        e &&
          (t.push({ type: `resetDecoration`, value: e }),
          e === `dim` && t.push({ type: `resetDecoration`, value: `bold` }));
      } else if (r <= 37)
        t.push({
          type: `setForegroundColor`,
          value: { type: `named`, name: gc[r - 30] },
        });
      else if (r === 38) {
        let n = ks(e);
        n && t.push({ type: `setForegroundColor`, value: n });
      } else if (r === 39) t.push({ type: `resetForegroundColor` });
      else if (r <= 47)
        t.push({
          type: `setBackgroundColor`,
          value: { type: `named`, name: gc[r - 40] },
        });
      else if (r === 48) {
        let n = ks(e);
        n && t.push({ type: `setBackgroundColor`, value: n });
      } else
        r === 49
          ? t.push({ type: `resetBackgroundColor` })
          : r === 53
            ? t.push({ type: `setDecoration`, value: `overline` })
            : r === 55
              ? t.push({ type: `resetDecoration`, value: `overline` })
              : r >= 90 && r <= 97
                ? t.push({
                    type: `setForegroundColor`,
                    value: { type: `named`, name: gc[r - 90 + 8] },
                  })
                : r >= 100 &&
                  r <= 107 &&
                  t.push({
                    type: `setBackgroundColor`,
                    value: { type: `named`, name: gc[r - 100 + 8] },
                  });
  }
  return t;
}
function js() {
  let e = null,
    t = null,
    n = new Set();
  return {
    parse(r) {
      let i = [],
        a = 0;
      do {
        let o = Os(r, a),
          s = o.sequence ? r.substring(a, o.startPosition) : r.substring(a);
        if (
          (s.length > 0 &&
            i.push({
              value: s,
              foreground: e,
              background: t,
              decorations: new Set(n),
            }),
          o.sequence)
        ) {
          let r = As(o.sequence);
          for (let i of r)
            i.type === `resetAll`
              ? ((e = null), (t = null), n.clear())
              : i.type === `resetForegroundColor`
                ? (e = null)
                : i.type === `resetBackgroundColor`
                  ? (t = null)
                  : i.type === `resetDecoration` && n.delete(i.value);
          for (let i of r)
            i.type === `setForegroundColor`
              ? (e = i.value)
              : i.type === `setBackgroundColor`
                ? (t = i.value)
                : i.type === `setDecoration` && n.add(i.value);
        }
        a = o.position;
      } while (a < r.length);
      return i;
    },
  };
}
function Ms(e = vc) {
  function t(t) {
    return e[t];
  }
  function n(e) {
    return `#${e.map((e) => Math.max(0, Math.min(e, 255)).toString(16).padStart(2, `0`)).join(``)}`;
  }
  let r;
  function i() {
    if (r) return r;
    r = [];
    for (let e = 0; e < gc.length; e++) r.push(t(gc[e]));
    let e = [0, 95, 135, 175, 215, 255];
    for (let t = 0; t < 6; t++)
      for (let i = 0; i < 6; i++)
        for (let a = 0; a < 6; a++) r.push(n([e[t], e[i], e[a]]));
    let i = 8;
    for (let e = 0; e < 24; e++, i += 10) r.push(n([i, i, i]));
    return r;
  }
  function a(e) {
    return i()[e];
  }
  function o(e) {
    switch (e.type) {
      case `named`:
        return t(e.name);
      case `rgb`:
        return n(e.rgb);
      case `table`:
        return a(e.index);
    }
  }
  return { value: o };
}
function Ns(e, t, n) {
  let r = ns(e, n),
    i = ds(t),
    a = Ms(
      Object.fromEntries(
        gc.map((t) => {
          let n = `terminal.ansi${t[0].toUpperCase()}${t.substring(1)}`;
          return [t, e.colors?.[n] || yc[t]];
        }),
      ),
    ),
    o = js();
  return i.map((t) =>
    o.parse(t[0]).map((n) => {
      let i, o;
      (n.decorations.has(`reverse`)
        ? ((i = n.background ? a.value(n.background) : e.bg),
          (o = n.foreground ? a.value(n.foreground) : e.fg))
        : ((i = n.foreground ? a.value(n.foreground) : e.fg),
          (o = n.background ? a.value(n.background) : void 0)),
        (i = rs(i, r)),
        (o = rs(o, r)),
        n.decorations.has(`dim`) && (i = Ps(i)));
      let s = K.None;
      return (
        n.decorations.has(`bold`) && (s |= K.Bold),
        n.decorations.has(`italic`) && (s |= K.Italic),
        n.decorations.has(`underline`) && (s |= K.Underline),
        n.decorations.has(`strikethrough`) && (s |= K.Strikethrough),
        { content: n.value, offset: t[1], color: i, bgColor: o, fontStyle: s }
      );
    }),
  );
}
function Ps(e) {
  let t = e.match(/#([0-9a-f]{3,8})/i);
  if (t) {
    let e = t[1];
    if (e.length === 8) {
      let t = Math.round(Number.parseInt(e.slice(6, 8), 16) / 2)
        .toString(16)
        .padStart(2, `0`);
      return `#${e.slice(0, 6)}${t}`;
    } else if (e.length === 6) return `#${e}80`;
    else if (e.length === 4) {
      let t = e[0],
        n = e[1],
        r = e[2],
        i = e[3];
      return `#${t}${t}${n}${n}${r}${r}${Math.round(
        Number.parseInt(`${i}${i}`, 16) / 2,
      )
        .toString(16)
        .padStart(2, `0`)}`;
    } else if (e.length === 3) {
      let t = e[0],
        n = e[1],
        r = e[2];
      return `#${t}${t}${n}${n}${r}${r}80`;
    }
  }
  let n = e.match(/var\((--[\w-]+-ansi-[\w-]+)\)/);
  return n ? `var(${n[1]}-dim)` : e;
}
function Fs(e, t, n = {}) {
  let { theme: r = e.getLoadedThemes()[0] } = n,
    i = e.resolveLangAlias(n.lang || `text`);
  if (os(i) || cs(r))
    return ds(t).map((e) => [{ content: e[0], offset: e[1] }]);
  let { theme: a, colorMap: o } = e.setTheme(r);
  if (i === `ansi`) return Ns(a, t, n);
  let s = e.getLanguage(n.lang || `text`);
  if (n.grammarState) {
    if (n.grammarState.lang !== s.name)
      throw new G(
        `Grammar state language "${n.grammarState.lang}" does not match highlight language "${s.name}"`,
      );
    if (!n.grammarState.themes.includes(a.name))
      throw new G(
        `Grammar state themes "${n.grammarState.themes}" do not contain highlight theme "${a.name}"`,
      );
  }
  return Ls(t, s, a, o, n);
}
function Is(...e) {
  if (e.length === 2) return bs(e[1]);
  let [t, n, r = {}] = e,
    { lang: i = `text`, theme: a = t.getLoadedThemes()[0] } = r;
  if (os(i) || cs(a)) throw new G(`Plain language does not have grammar state`);
  if (i === `ansi`) throw new G(`ANSI language does not have grammar state`);
  let { theme: o, colorMap: s } = t.setTheme(a),
    c = t.getLanguage(i);
  return new mc(Rs(n, c, o, s, r).stateStack, c.name, o.name);
}
function Ls(e, t, n, r, i) {
  let a = Rs(e, t, n, r, i),
    o = new mc(a.stateStack, t.name, n.name);
  return (ys(a.tokens, o), a.tokens);
}
function Rs(e, t, n, r, i) {
  let a = ns(n, i),
    { tokenizeMaxLineLength: o = 0, tokenizeTimeLimit: s = 500 } = i,
    c = ds(e),
    l = i.grammarState
      ? (Ss(i.grammarState, n.name) ?? es)
      : i.grammarContextCode == null
        ? es
        : Rs(i.grammarContextCode, t, n, r, {
            ...i,
            grammarState: void 0,
            grammarContextCode: void 0,
          }).stateStack,
    u = [],
    d = [];
  for (let e = 0, f = c.length; e < f; e++) {
    let [f, p] = c[e];
    if (f === ``) {
      ((u = []), d.push([]));
      continue;
    }
    if (o > 0 && f.length >= o) {
      ((u = []), d.push([{ content: f, offset: p, color: ``, fontStyle: 0 }]));
      continue;
    }
    let m, h, g;
    i.includeExplanation &&
      ((m = t.tokenizeLine(f, l, s)), (h = m.tokens), (g = 0));
    let _ = t.tokenizeLine2(f, l, s),
      v = _.tokens.length / 2;
    for (let e = 0; e < v; e++) {
      let t = _.tokens[2 * e],
        o = e + 1 < v ? _.tokens[2 * e + 2] : f.length;
      if (t === o) continue;
      let s = _.tokens[2 * e + 1],
        c = rs(r[_o.getForeground(s)], a),
        l = _o.getFontStyle(s),
        d = {
          content: f.substring(t, o),
          offset: p + t,
          color: c,
          fontStyle: l,
        };
      if (i.includeExplanation) {
        let e = [];
        if (i.includeExplanation !== `scopeName`)
          for (let t of n.settings) {
            let n;
            switch (typeof t.scope) {
              case `string`:
                n = t.scope.split(/,/).map((e) => e.trim());
                break;
              case `object`:
                n = t.scope;
                break;
              default:
                continue;
            }
            e.push({ settings: t, selectors: n.map((e) => e.split(/ /)) });
          }
        d.explanation = [];
        let r = 0;
        for (; t + r < o;) {
          let t = h[g],
            n = f.substring(t.startIndex, t.endIndex);
          ((r += n.length),
            d.explanation.push({
              content: n,
              scopes:
                i.includeExplanation === `scopeName`
                  ? zs(t.scopes)
                  : Bs(e, t.scopes),
            }),
            (g += 1));
        }
      }
      u.push(d);
    }
    (d.push(u), (u = []), (l = _.ruleStack));
  }
  return { tokens: d, stateStack: l };
}
function zs(e) {
  return e.map((e) => ({ scopeName: e }));
}
function Bs(e, t) {
  let n = [];
  for (let r = 0, i = t.length; r < i; r++) {
    let i = t[r];
    n[r] = { scopeName: i, themeMatches: Us(e, i, t.slice(0, r)) };
  }
  return n;
}
function Vs(e, t) {
  return e === t || (t.substring(0, e.length) === e && t[e.length] === `.`);
}
function Hs(e, t, n) {
  if (!Vs(e[e.length - 1], t)) return !1;
  let r = e.length - 2,
    i = n.length - 1;
  for (; r >= 0 && i >= 0;) (Vs(e[r], n[i]) && --r, --i);
  return r === -1;
}
function Us(e, t, n) {
  let r = [];
  for (let { selectors: i, settings: a } of e)
    for (let e of i)
      if (Hs(e, t, n)) {
        r.push(a);
        break;
      }
  return r;
}
function Ws(e, t, n) {
  let r = Object.entries(n.themes)
      .filter((e) => e[1])
      .map((e) => ({ color: e[0], theme: e[1] })),
    i = r.map((r) => {
      let i = Fs(e, t, { ...n, theme: r.theme });
      return {
        tokens: i,
        state: bs(i),
        theme: typeof r.theme == `string` ? r.theme : r.theme.name,
      };
    }),
    a = Gs(...i.map((e) => e.tokens)),
    o = a[0].map((e, t) =>
      e.map((e, i) => {
        let o = { content: e.content, variants: {}, offset: e.offset };
        return (
          `includeExplanation` in n &&
            n.includeExplanation &&
            (o.explanation = e.explanation),
          a.forEach((e, n) => {
            let { content: a, explanation: s, offset: c, ...l } = e[t][i];
            o.variants[r[n].color] = l;
          }),
          o
        );
      }),
    ),
    s = i[0].state
      ? new mc(
          Object.fromEntries(
            i.map((e) => [e.theme, e.state?.getInternalStack(e.theme)]),
          ),
          i[0].state.lang,
        )
      : void 0;
  return (s && ys(o, s), o);
}
function Gs(...e) {
  let t = e.map(() => []),
    n = e.length;
  for (let r = 0; r < e[0].length; r++) {
    let i = e.map((e) => e[r]),
      a = t.map(() => []);
    t.forEach((e, t) => e.push(a[t]));
    let o = i.map(() => 0),
      s = i.map((e) => e[0]);
    for (; s.every((e) => e);) {
      let e = Math.min(...s.map((e) => e.content.length));
      for (let t = 0; t < n; t++) {
        let n = s[t];
        n.content.length === e
          ? (a[t].push(n), (o[t] += 1), (s[t] = i[t][o[t]]))
          : (a[t].push({ ...n, content: n.content.slice(0, e) }),
            (s[t] = {
              ...n,
              content: n.content.slice(e),
              offset: n.offset + e,
            }));
      }
    }
  }
  return t;
}
function Ks(e, t, n) {
  let r, i, a, o, s, c;
  if (`themes` in n) {
    let {
        defaultColor: l = `light`,
        cssVariablePrefix: u = `--shiki-`,
        colorsRendering: d = `css-vars`,
      } = n,
      f = Object.entries(n.themes)
        .filter((e) => e[1])
        .map((e) => ({ color: e[0], theme: e[1] }))
        .sort((e, t) => (e.color === l ? -1 : +(t.color === l)));
    if (f.length === 0) throw new G("`themes` option must not be empty");
    let p = Ws(e, t, n);
    if (((c = bs(p)), l && dc !== l && !f.find((e) => e.color === l)))
      throw new G(
        `\`themes\` option must contain the defaultColor key \`${l}\``,
      );
    let m = f.map((t) => e.getTheme(t.theme)),
      h = f.map((e) => e.color);
    ((a = p.map((e) => e.map((e) => gs(e, h, u, l, d)))), c && ys(a, c));
    let g = f.map((e) => ns(e.theme, n));
    ((i = qs(f, m, g, u, l, `fg`, d)),
      (r = qs(f, m, g, u, l, `bg`, d)),
      (o = `shiki-themes ${m.map((e) => e.name).join(` `)}`),
      (s = l ? void 0 : [i, r].join(`;`)));
  } else if (`theme` in n) {
    let s = ns(n.theme, n);
    a = Fs(e, t, n);
    let l = e.getTheme(n.theme);
    ((r = rs(l.bg, s)), (i = rs(l.fg, s)), (o = l.name), (c = bs(a)));
  } else
    throw new G("Invalid options, either `theme` or `themes` must be provided");
  return {
    tokens: a,
    fg: i,
    bg: r,
    themeName: o,
    rootStyle: s,
    grammarState: c,
  };
}
function qs(e, t, n, r, i, a, o) {
  return e
    .map((s, c) => {
      let l = rs(t[c][a], n[c]) || `inherit`,
        u = `${r + s.color}${a === `bg` ? `-bg` : ``}:${l}`;
      if (c === 0 && i) {
        if (i === dc && e.length > 1) {
          let r = e.findIndex((e) => e.color === `light`),
            i = e.findIndex((e) => e.color === `dark`);
          if (r === -1 || i === -1)
            throw new G(
              'When using `defaultColor: "light-dark()"`, you must provide both `light` and `dark` themes',
            );
          return `light-dark(${rs(t[r][a], n[r]) || `inherit`}, ${rs(t[i][a], n[i]) || `inherit`});${u}`;
        }
        return l;
      }
      return o === `css-vars` ? u : null;
    })
    .filter((e) => !!e)
    .join(`;`);
}
function Js(
  e,
  t,
  n,
  r = {
    meta: {},
    options: n,
    codeToHast: (t, n) => Js(e, t, n),
    codeToTokens: (t, n) => Ks(e, t, n),
  },
) {
  let i = t;
  for (let e of Es(n)) i = e.preprocess?.call(r, i, n) || i;
  let {
      tokens: a,
      fg: o,
      bg: s,
      themeName: c,
      rootStyle: l,
      grammarState: u,
    } = Ks(e, i, n),
    { mergeWhitespaces: d = !0, mergeSameStyleTokens: f = !1 } = n;
  (d === !0 ? (a = Xs(a)) : d === `never` && (a = Zs(a)), f && (a = Qs(a)));
  let p = {
    ...r,
    get source() {
      return i;
    },
  };
  for (let e of Es(n)) a = e.tokens?.call(p, a) || a;
  return Ys(
    a,
    {
      ...n,
      fg: o,
      bg: s,
      themeName: c,
      rootStyle: n.rootStyle === !1 ? !1 : (n.rootStyle ?? l),
    },
    p,
    u,
  );
}
function Ys(e, t, n, r = bs(e)) {
  let i = Es(t),
    a = [],
    o = { type: `root`, children: [] },
    { structure: s = `classic`, tabindex: c = `0` } = t,
    l = { class: `shiki ${t.themeName || ``}` };
  (t.rootStyle !== !1 &&
    (t.rootStyle == null
      ? (l.style = `background-color:${t.bg};color:${t.fg}`)
      : (l.style = t.rootStyle)),
    c !== !1 && c != null && (l.tabindex = c.toString()));
  for (let [e, n] of Object.entries(t.meta || {}))
    e.startsWith(`_`) || (l[e] = n);
  let u = { type: `element`, tagName: `pre`, properties: l, children: [] },
    d = { type: `element`, tagName: `code`, properties: {}, children: a },
    f = [],
    p = {
      ...n,
      structure: s,
      addClassToHast: us,
      get source() {
        return n.source;
      },
      get tokens() {
        return e;
      },
      get options() {
        return t;
      },
      get root() {
        return o;
      },
      get pre() {
        return u;
      },
      get code() {
        return d;
      },
      get lines() {
        return f;
      },
    };
  if (
    (e.forEach((e, t) => {
      t &&
        (s === `inline`
          ? o.children.push({
              type: `element`,
              tagName: `br`,
              properties: {},
              children: [],
            })
          : s === `classic` &&
            a.push({
              type: `text`,
              value: `
`,
            }));
      let n = {
          type: `element`,
          tagName: `span`,
          properties: { class: `line` },
          children: [],
        },
        r = 0;
      for (let a of e) {
        let e = {
            type: `element`,
            tagName: `span`,
            properties: { ...a.htmlAttrs },
            children: [{ type: `text`, value: a.content }],
          },
          c = vs(a.htmlStyle || _s(a));
        c && (e.properties.style = c);
        for (let o of i) e = o?.span?.call(p, e, t + 1, r, n, a) || e;
        (s === `inline`
          ? o.children.push(e)
          : s === `classic` && n.children.push(e),
          (r += a.content.length));
      }
      if (s === `classic`) {
        for (let e of i) n = e?.line?.call(p, n, t + 1) || n;
        (f.push(n), a.push(n));
      } else s === `inline` && f.push(n);
    }),
    s === `classic`)
  ) {
    for (let e of i) d = e?.code?.call(p, d) || d;
    u.children.push(d);
    for (let e of i) u = e?.pre?.call(p, u) || u;
    o.children.push(u);
  } else if (s === `inline`) {
    let e = [],
      t = {
        type: `element`,
        tagName: `span`,
        properties: { class: `line` },
        children: [],
      };
    for (let n of o.children)
      n.type === `element` && n.tagName === `br`
        ? (e.push(t),
          (t = {
            type: `element`,
            tagName: `span`,
            properties: { class: `line` },
            children: [],
          }))
        : (n.type === `element` || n.type === `text`) && t.children.push(n);
    e.push(t);
    let n = { type: `element`, tagName: `code`, properties: {}, children: e };
    for (let e of i) n = e?.code?.call(p, n) || n;
    o.children = [];
    for (let e = 0; e < n.children.length; e++) {
      e > 0 &&
        o.children.push({
          type: `element`,
          tagName: `br`,
          properties: {},
          children: [],
        });
      let t = n.children[e];
      t.type === `element` && o.children.push(...t.children);
    }
  }
  let m = o;
  for (let e of i) m = e?.root?.call(p, m) || m;
  return (r && ys(m, r), m);
}
function Xs(e) {
  return e.map((e) => {
    let t = [],
      n = ``,
      r;
    return (
      e.forEach((i, a) => {
        let o = !(
          i.fontStyle &&
          (i.fontStyle & K.Underline || i.fontStyle & K.Strikethrough)
        );
        o && i.content.match(/^\s+$/) && e[a + 1]
          ? (r === void 0 && (r = i.offset), (n += i.content))
          : n
            ? (o
                ? t.push({ ...i, offset: r, content: n + i.content })
                : t.push({ content: n, offset: r }, i),
              (r = void 0),
              (n = ``))
            : t.push(i);
      }),
      t
    );
  });
}
function Zs(e) {
  return e.map((e) =>
    e.flatMap((e) => {
      if (e.content.match(/^\s+$/)) return e;
      let t = e.content.match(/^(\s*)(.*?)(\s*)$/);
      if (!t) return e;
      let [, n, r, i] = t;
      if (!n && !i) return e;
      let a = [{ ...e, offset: e.offset + n.length, content: r }];
      return (
        n && a.unshift({ content: n, offset: e.offset }),
        i && a.push({ content: i, offset: e.offset + n.length + r.length }),
        a
      );
    }),
  );
}
function Qs(e) {
  return e.map((e) => {
    let t = [];
    for (let n of e) {
      if (t.length === 0) {
        t.push({ ...n });
        continue;
      }
      let e = t[t.length - 1],
        r = vs(e.htmlStyle || _s(e)),
        i = vs(n.htmlStyle || _s(n)),
        a =
          e.fontStyle &&
          (e.fontStyle & K.Underline || e.fontStyle & K.Strikethrough),
        o =
          n.fontStyle &&
          (n.fontStyle & K.Underline || n.fontStyle & K.Strikethrough);
      !a && !o && r === i ? (e.content += n.content) : t.push({ ...n });
    }
    return t;
  });
}
function $s(e, t, n) {
  let r = {
      meta: {},
      options: n,
      codeToHast: (t, n) => Js(e, t, n),
      codeToTokens: (t, n) => Ks(e, t, n),
    },
    i = bc(Js(e, t, n, r));
  for (let e of Es(n)) i = e.postprocess?.call(r, i, n) || i;
  return i;
}
function ec(e) {
  if (e?.[Cc]) return e;
  let t = { ...e };
  (t.tokenColors &&
    !t.settings &&
    ((t.settings = t.tokenColors), delete t.tokenColors),
    (t.type ||= `dark`),
    (t.colorReplacements = { ...t.colorReplacements }),
    (t.settings ||= []));
  let { bg: n, fg: r } = t;
  if (!n || !r) {
    let e = t.settings ? t.settings.find((e) => !e.name && !e.scope) : void 0;
    (e?.settings?.foreground && (r = e.settings.foreground),
      e?.settings?.background && (n = e.settings.background),
      !r &&
        t?.colors?.[`editor.foreground`] &&
        (r = t.colors[`editor.foreground`]),
      !n &&
        t?.colors?.[`editor.background`] &&
        (n = t.colors[`editor.background`]),
      (r ||= t.type === `light` ? xc.light : xc.dark),
      (n ||= t.type === `light` ? Sc.light : Sc.dark),
      (t.fg = r),
      (t.bg = n));
  }
  (t.settings[0] && t.settings[0].settings && !t.settings[0].scope) ||
    t.settings.unshift({ settings: { foreground: t.fg, background: t.bg } });
  let i = 0,
    a = new Map();
  function o(e) {
    if (a.has(e)) return a.get(e);
    i += 1;
    let n = `#${i.toString(16).padStart(8, `0`).toLowerCase()}`;
    return t.colorReplacements?.[`#${n}`] ? o(e) : (a.set(e, n), n);
  }
  t.settings = t.settings.map((e) => {
    let n = e.settings?.foreground && !e.settings.foreground.startsWith(`#`),
      r = e.settings?.background && !e.settings.background.startsWith(`#`);
    if (!n && !r) return e;
    let i = { ...e, settings: { ...e.settings } };
    if (n) {
      let n = o(e.settings.foreground);
      ((t.colorReplacements[n] = e.settings.foreground),
        (i.settings.foreground = n));
    }
    if (r) {
      let n = o(e.settings.background);
      ((t.colorReplacements[n] = e.settings.background),
        (i.settings.background = n));
    }
    return i;
  });
  for (let e of Object.keys(t.colors || {}))
    if (
      (e === `editor.foreground` ||
        e === `editor.background` ||
        e.startsWith(`terminal.ansi`)) &&
      !t.colors[e]?.startsWith(`#`)
    ) {
      let n = o(t.colors[e]);
      ((t.colorReplacements[n] = t.colors[e]), (t.colors[e] = n));
    }
  return (
    Object.defineProperty(t, Cc, { enumerable: !1, writable: !1, value: !0 }),
    t
  );
}
async function tc(e) {
  return Array.from(
    new Set(
      (
        await Promise.all(
          e
            .filter((e) => !ss(e))
            .map(
              async (e) =>
                await as(e).then((e) => (Array.isArray(e) ? e : [e])),
            ),
        )
      ).flat(),
    ),
  );
}
async function nc(e) {
  return (
    await Promise.all(e.map(async (e) => (ls(e) ? null : ec(await as(e)))))
  ).filter((e) => !!e);
}
function rc(e, t = 3) {
  if (wc && !(typeof wc == `number` && t > wc)) {
    if (Tc) throw Error(`[SHIKI DEPRECATE]: ${e}`);
    console.trace(`[SHIKI DEPRECATE]: ${e}`);
  }
}
function ic(e, t) {
  if (!t) return e;
  if (t[e]) {
    let n = new Set([e]);
    for (; t[e];) {
      if (((e = t[e]), n.has(e)))
        throw new Ec(
          `Circular alias \`${Array.from(n).join(` -> `)} -> ${e}\``,
        );
      n.add(e);
    }
  }
  return e;
}
function ac(e) {
  ((kc += 1),
    e.warnings !== !1 &&
      kc >= 10 &&
      kc % 10 == 0 &&
      console.warn(
        `[Shiki] ${kc} instances have been created. Shiki is supposed to be used as a singleton, consider refactoring your code to cache your highlighter instance; Or call \`highlighter.dispose()\` to release unused instances.`,
      ));
  let t = !1;
  if (!e.engine)
    throw new Ec("`engine` option is required for synchronous mode");
  let n = (e.langs || []).flat(1),
    r = (e.themes || []).flat(1).map(ec),
    i = new Oc(e.engine, n),
    a = new Dc(i, r, n, e.langAlias),
    o;
  function s(t) {
    return ic(t, e.langAlias);
  }
  function c(e) {
    _();
    let t = a.getGrammar(typeof e == `string` ? e : e.name);
    if (!t)
      throw new Ec(
        `Language \`${e}\` not found, you may need to load it first`,
      );
    return t;
  }
  function l(e) {
    if (e === `none`)
      return { bg: ``, fg: ``, name: `none`, settings: [], type: `dark` };
    _();
    let t = a.getTheme(e);
    if (!t)
      throw new Ec(`Theme \`${e}\` not found, you may need to load it first`);
    return t;
  }
  function u(e) {
    _();
    let t = l(e);
    return (
      o !== e && (a.setTheme(t), (o = e)),
      { theme: t, colorMap: a.getColorMap() }
    );
  }
  function d() {
    return (_(), a.getLoadedThemes());
  }
  function f() {
    return (_(), a.getLoadedLanguages());
  }
  function p(...e) {
    (_(), a.loadLanguages(e.flat(1)));
  }
  async function m(...e) {
    return p(await tc(e));
  }
  function h(...e) {
    _();
    for (let t of e.flat(1)) a.loadTheme(t);
  }
  async function g(...e) {
    return (_(), h(await nc(e)));
  }
  function _() {
    if (t) throw new Ec(`Shiki instance has been disposed`);
  }
  function v() {
    t || ((t = !0), a.dispose(), --kc);
  }
  return {
    setTheme: u,
    getTheme: l,
    getLanguage: c,
    getLoadedThemes: d,
    getLoadedLanguages: f,
    resolveLangAlias: s,
    loadLanguage: m,
    loadLanguageSync: p,
    loadTheme: g,
    loadThemeSync: h,
    dispose: v,
    [Symbol.dispose]: v,
  };
}
async function oc(e) {
  e.engine ||
    rc(
      "`engine` option is required. Use `createOnigurumaEngine` or `createJavaScriptRegexEngine` to create an engine.",
    );
  let [t, n, r] = await Promise.all([
    nc(e.themes || []),
    tc(e.langs || []),
    e.engine,
  ]);
  return ac({ ...e, themes: t, langs: n, engine: r });
}
async function sc(e) {
  let t = await oc(e);
  return {
    getLastGrammarState: (...e) => Is(t, ...e),
    codeToTokensBase: (e, n) => Fs(t, e, n),
    codeToTokensWithThemes: (e, n) => Ws(t, e, n),
    codeToTokens: (e, n) => Ks(t, e, n),
    codeToHast: (e, n) => Js(t, e, n),
    codeToHtml: (e, n) => $s(t, e, n),
    getBundledLanguages: () => ({}),
    getBundledThemes: () => ({}),
    ...t,
    getInternalContext: () => t,
  };
}
function cc(e) {
  let t = e.langs,
    n = e.themes,
    r = e.engine;
  async function i(e) {
    function i(n) {
      if (typeof n == `string`) {
        if (((n = e.langAlias?.[n] || n), ss(n))) return [];
        let r = t[n];
        if (!r)
          throw new G(
            `Language \`${n}\` is not included in this bundle. You may want to load it from external source.`,
          );
        return r;
      }
      return n;
    }
    function a(e) {
      if (ls(e)) return `none`;
      if (typeof e == `string`) {
        let t = n[e];
        if (!t)
          throw new G(
            `Theme \`${e}\` is not included in this bundle. You may want to load it from external source.`,
          );
        return t;
      }
      return e;
    }
    let o = (e.themes ?? []).map((e) => a(e)),
      s = (e.langs ?? []).map((e) => i(e)),
      c = await sc({ engine: e.engine ?? r(), ...e, themes: o, langs: s });
    return {
      ...c,
      loadLanguage(...e) {
        return c.loadLanguage(...e.map(i));
      },
      loadTheme(...e) {
        return c.loadTheme(...e.map(a));
      },
      getBundledLanguages() {
        return t;
      },
      getBundledThemes() {
        return n;
      },
    };
  }
  return i;
}
function lc(e) {
  let t;
  async function n(n = {}) {
    if (t) {
      let e = await t;
      return (
        await Promise.all([
          e.loadTheme(...(n.themes || [])),
          e.loadLanguage(...(n.langs || [])),
        ]),
        e
      );
    } else {
      t = e({ ...n, themes: [], langs: [] });
      let r = await t;
      return (
        await Promise.all([
          r.loadTheme(...(n.themes || [])),
          r.loadLanguage(...(n.langs || [])),
        ]),
        r
      );
    }
  }
  return n;
}
function uc(e, t) {
  let n = lc(e);
  async function r(e, r) {
    let i = await n({
        langs: [r.lang],
        themes: `theme` in r ? [r.theme] : Object.values(r.themes),
      }),
      a = await t?.guessEmbeddedLanguages?.(e, r.lang, i);
    return (a && (await i.loadLanguage(...a)), i);
  }
  return {
    getSingletonHighlighter(e) {
      return n(e);
    },
    async codeToHtml(e, t) {
      return (await r(e, t)).codeToHtml(e, t);
    },
    async codeToHast(e, t) {
      return (await r(e, t)).codeToHast(e, t);
    },
    async codeToTokens(e, t) {
      return (await r(e, t)).codeToTokens(e, t);
    },
    async codeToTokensBase(e, t) {
      return (await r(e, t)).codeToTokensBase(e, t);
    },
    async codeToTokensWithThemes(e, t) {
      return (await r(e, t)).codeToTokensWithThemes(e, t);
    },
    async getLastGrammarState(e, t) {
      return (
        await n({ langs: [t.lang], themes: [t.theme] })
      ).getLastGrammarState(e, t);
    },
  };
}
var dc,
  fc,
  pc,
  mc,
  hc,
  gc,
  _c,
  vc,
  yc,
  bc,
  xc,
  Sc,
  Cc,
  wc,
  Tc,
  Ec,
  Dc,
  Oc,
  kc,
  Ac = e(() => {
    (_a(),
      _a(),
      ts(),
      Ni(),
      (dc = `light-dark()`),
      (fc = [`color`, `background-color`]),
      (pc = new WeakMap()),
      (mc = class e {
        _stacks = {};
        lang;
        get themes() {
          return Object.keys(this._stacks);
        }
        get theme() {
          return this.themes[0];
        }
        get _stack() {
          return this._stacks[this.theme];
        }
        static initial(t, n) {
          return new e(Object.fromEntries(is(n).map((e) => [e, es])), t);
        }
        constructor(...e) {
          if (e.length === 2) {
            let [t, n] = e;
            ((this.lang = n), (this._stacks = t));
          } else {
            let [t, n, r] = e;
            ((this.lang = n), (this._stacks = { [r]: t }));
          }
        }
        getInternalStack(e = this.theme) {
          return this._stacks[e];
        }
        getScopes(e = this.theme) {
          return xs(this._stacks[e]);
        }
        toJSON() {
          return {
            lang: this.lang,
            theme: this.theme,
            themes: this.themes,
            scopes: this.getScopes(),
          };
        }
      }),
      (hc = [Cs()]),
      (gc = [
        `black`,
        `red`,
        `green`,
        `yellow`,
        `blue`,
        `magenta`,
        `cyan`,
        `white`,
        `brightBlack`,
        `brightRed`,
        `brightGreen`,
        `brightYellow`,
        `brightBlue`,
        `brightMagenta`,
        `brightCyan`,
        `brightWhite`,
      ]),
      (_c = {
        1: `bold`,
        2: `dim`,
        3: `italic`,
        4: `underline`,
        7: `reverse`,
        8: `hidden`,
        9: `strikethrough`,
      }),
      (vc = {
        black: `#000000`,
        red: `#bb0000`,
        green: `#00bb00`,
        yellow: `#bbbb00`,
        blue: `#0000bb`,
        magenta: `#ff00ff`,
        cyan: `#00bbbb`,
        white: `#eeeeee`,
        brightBlack: `#555555`,
        brightRed: `#ff5555`,
        brightGreen: `#00ff00`,
        brightYellow: `#ffff55`,
        brightBlue: `#5555ff`,
        brightMagenta: `#ff55ff`,
        brightCyan: `#55ffff`,
        brightWhite: `#ffffff`,
      }),
      (yc = {
        black: `#000000`,
        red: `#cd3131`,
        green: `#0DBC79`,
        yellow: `#E5E510`,
        blue: `#2472C8`,
        magenta: `#BC3FBC`,
        cyan: `#11A8CD`,
        white: `#E5E5E5`,
        brightBlack: `#666666`,
        brightRed: `#F14C4C`,
        brightGreen: `#23D18B`,
        brightYellow: `#F5F543`,
        brightBlue: `#3B8EEA`,
        brightMagenta: `#D670D6`,
        brightCyan: `#29B8DB`,
        brightWhite: `#FFFFFF`,
      }),
      (bc = Ei),
      (xc = { light: `#333333`, dark: `#bbbbbb` }),
      (Sc = { light: `#fffffe`, dark: `#1e1e1e` }),
      (Cc = `__shiki_resolved`),
      (wc = 3),
      (Tc = !1),
      (Ec = class extends Error {
        constructor(e) {
          (super(e), (this.name = `ShikiError`));
        }
      }),
      (Dc = class extends $o {
        constructor(e, t, n, r = {}) {
          (super(e),
            (this._resolver = e),
            (this._themes = t),
            (this._langs = n),
            (this._alias = r),
            this._themes.map((e) => this.loadTheme(e)),
            this.loadLanguages(this._langs));
        }
        _resolvedThemes = new Map();
        _resolvedGrammars = new Map();
        _langMap = new Map();
        _langGraph = new Map();
        _textmateThemeCache = new WeakMap();
        _loadedThemesCache = null;
        _loadedLanguagesCache = null;
        getTheme(e) {
          return typeof e == `string`
            ? this._resolvedThemes.get(e)
            : this.loadTheme(e);
        }
        loadTheme(e) {
          let t = ec(e);
          return (
            t.name &&
              (this._resolvedThemes.set(t.name, t),
              (this._loadedThemesCache = null)),
            t
          );
        }
        getLoadedThemes() {
          return (
            (this._loadedThemesCache ||= [...this._resolvedThemes.keys()]),
            this._loadedThemesCache
          );
        }
        setTheme(e) {
          let t = this._textmateThemeCache.get(e);
          (t ||
            ((t = co.createFromRawTheme(e)),
            this._textmateThemeCache.set(e, t)),
            this._syncRegistry.setTheme(t));
        }
        getGrammar(e) {
          return ((e = ic(e, this._alias)), this._resolvedGrammars.get(e));
        }
        loadLanguage(e) {
          if (this.getGrammar(e.name)) return;
          let t = new Set(
            [...this._langMap.values()].filter((t) =>
              t.embeddedLangsLazy?.includes(e.name),
            ),
          );
          this._resolver.addLanguage(e);
          let n = {
            balancedBracketSelectors: e.balancedBracketSelectors || [`*`],
            unbalancedBracketSelectors: e.unbalancedBracketSelectors || [],
          };
          this._syncRegistry._rawGrammars.set(e.scopeName, e);
          let r = this.loadGrammarWithConfiguration(e.scopeName, 1, n);
          if (
            ((r.name = e.name),
            this._resolvedGrammars.set(e.name, r),
            e.aliases &&
              e.aliases.forEach((t) => {
                this._alias[t] = e.name;
              }),
            (this._loadedLanguagesCache = null),
            t.size)
          )
            for (let e of t)
              (this._resolvedGrammars.delete(e.name),
                (this._loadedLanguagesCache = null),
                this._syncRegistry?._injectionGrammars?.delete(e.scopeName),
                this._syncRegistry?._grammars?.delete(e.scopeName),
                this.loadLanguage(this._langMap.get(e.name)));
        }
        dispose() {
          (super.dispose(),
            this._resolvedThemes.clear(),
            this._resolvedGrammars.clear(),
            this._langMap.clear(),
            this._langGraph.clear(),
            (this._loadedThemesCache = null));
        }
        loadLanguages(e) {
          for (let t of e) this.resolveEmbeddedLanguages(t);
          let t = Array.from(this._langGraph.entries()),
            n = t.filter(([e, t]) => !t);
          if (n.length) {
            let e = t
              .filter(([e, t]) =>
                t
                  ? (t.embeddedLanguages || t.embeddedLangs)?.some((e) =>
                      n.map(([e]) => e).includes(e),
                    )
                  : !1,
              )
              .filter((e) => !n.includes(e));
            throw new Ec(
              `Missing languages ${n.map(([e]) => `\`${e}\``).join(`, `)}, required by ${e.map(([e]) => `\`${e}\``).join(`, `)}`,
            );
          }
          for (let [e, n] of t) this._resolver.addLanguage(n);
          for (let [e, n] of t) this.loadLanguage(n);
        }
        getLoadedLanguages() {
          return (
            (this._loadedLanguagesCache ||= [
              ...new Set([
                ...this._resolvedGrammars.keys(),
                ...Object.keys(this._alias),
              ]),
            ]),
            this._loadedLanguagesCache
          );
        }
        resolveEmbeddedLanguages(e) {
          (this._langMap.set(e.name, e), this._langGraph.set(e.name, e));
          let t = e.embeddedLanguages ?? e.embeddedLangs;
          if (t) for (let e of t) this._langGraph.set(e, this._langMap.get(e));
        }
      }),
      (Oc = class {
        _langs = new Map();
        _scopeToLang = new Map();
        _injections = new Map();
        _onigLib;
        constructor(e, t) {
          ((this._onigLib = {
            createOnigScanner: (t) => e.createScanner(t),
            createOnigString: (t) => e.createString(t),
          }),
            t.forEach((e) => this.addLanguage(e)));
        }
        get onigLib() {
          return this._onigLib;
        }
        getLangRegistration(e) {
          return this._langs.get(e);
        }
        loadGrammar(e) {
          return this._scopeToLang.get(e);
        }
        addLanguage(e) {
          (this._langs.set(e.name, e),
            e.aliases &&
              e.aliases.forEach((t) => {
                this._langs.set(t, e);
              }),
            this._scopeToLang.set(e.scopeName, e),
            e.injectTo &&
              e.injectTo.forEach((t) => {
                (this._injections.get(t) || this._injections.set(t, []),
                  this._injections.get(t).push(e.scopeName));
              }));
        }
        getInjections(e) {
          let t = e.split(`.`),
            n = [];
          for (let e = 1; e <= t.length; e++) {
            let r = t.slice(0, e).join(`.`);
            n = [...n, ...(this._injections.get(r) || [])];
          }
          return n;
        }
      }),
      (kc = 0));
  }),
  jc,
  Mc,
  Nc,
  Pc,
  Fc = e(() => {
    (d(),
      (jc = [
        {
          id: `abap`,
          name: `ABAP`,
          import: () =>
            u(
              () => import(`./abap-DxuQO2b8.js`),
              __vite__mapDeps([44, 1]),
              import.meta.url,
            ),
        },
        {
          id: `actionscript-3`,
          name: `ActionScript`,
          import: () =>
            u(
              () => import(`./actionscript-3-CieH__f6.js`),
              __vite__mapDeps([45, 1]),
              import.meta.url,
            ),
        },
        {
          id: `ada`,
          name: `Ada`,
          import: () =>
            u(
              () => import(`./ada-CS8346Dn.js`),
              __vite__mapDeps([46, 1]),
              import.meta.url,
            ),
        },
        {
          id: `angular-html`,
          name: `Angular HTML`,
          import: () =>
            u(
              () => import(`./angular-html-UdoV1con.js`),
              __vite__mapDeps([47, 48, 1, 49, 50, 51]),
              import.meta.url,
            ),
        },
        {
          id: `angular-ts`,
          name: `Angular TypeScript`,
          import: () =>
            u(
              () => import(`./angular-ts-vZLvkGuL.js`),
              __vite__mapDeps([52, 1, 48, 49, 50, 51, 53]),
              import.meta.url,
            ),
        },
        {
          id: `apache`,
          name: `Apache Conf`,
          import: () =>
            u(
              () => import(`./apache-Dlp9vCqD.js`),
              __vite__mapDeps([54, 1]),
              import.meta.url,
            ),
        },
        {
          id: `apex`,
          name: `Apex`,
          import: () =>
            u(
              () => import(`./apex-Cx5f9vbJ.js`),
              __vite__mapDeps([55, 1]),
              import.meta.url,
            ),
        },
        {
          id: `apl`,
          name: `APL`,
          import: () =>
            u(
              () => import(`./apl-CyU4rL0N.js`),
              __vite__mapDeps([56, 1, 50, 51, 49, 57, 58, 59]),
              import.meta.url,
            ),
        },
        {
          id: `applescript`,
          name: `AppleScript`,
          import: () =>
            u(
              () => import(`./applescript-TGhSYpQm.js`),
              __vite__mapDeps([60, 1]),
              import.meta.url,
            ),
        },
        {
          id: `ara`,
          name: `Ara`,
          import: () =>
            u(
              () => import(`./ara-C4TgWDTj.js`),
              __vite__mapDeps([61, 1]),
              import.meta.url,
            ),
        },
        {
          id: `asciidoc`,
          name: `AsciiDoc`,
          aliases: [`adoc`],
          import: () =>
            u(
              () => import(`./asciidoc-BFA4dLe-.js`),
              __vite__mapDeps([62, 1]),
              import.meta.url,
            ),
        },
        {
          id: `asm`,
          name: `Assembly`,
          import: () =>
            u(
              () => import(`./asm-CXw8Dlb9.js`),
              __vite__mapDeps([63, 1]),
              import.meta.url,
            ),
        },
        {
          id: `astro`,
          name: `Astro`,
          import: () =>
            u(
              () => import(`./astro-W4hA_sUN.js`),
              __vite__mapDeps([64, 1, 50, 51, 59, 65, 66, 67]),
              import.meta.url,
            ),
        },
        {
          id: `awk`,
          name: `AWK`,
          import: () =>
            u(
              () => import(`./awk-jhlAG1FL.js`),
              __vite__mapDeps([68, 1]),
              import.meta.url,
            ),
        },
        {
          id: `ballerina`,
          name: `Ballerina`,
          import: () =>
            u(
              () => import(`./ballerina-DZdMimCC.js`),
              __vite__mapDeps([69, 1]),
              import.meta.url,
            ),
        },
        {
          id: `bat`,
          name: `Batch File`,
          aliases: [`batch`],
          import: () =>
            u(
              () => import(`./bat-ClNum6z5.js`),
              __vite__mapDeps([70, 1]),
              import.meta.url,
            ),
        },
        {
          id: `beancount`,
          name: `Beancount`,
          import: () =>
            u(
              () => import(`./beancount-Bti-eV3T.js`),
              __vite__mapDeps([71, 1]),
              import.meta.url,
            ),
        },
        {
          id: `berry`,
          name: `Berry`,
          aliases: [`be`],
          import: () =>
            u(
              () => import(`./berry-C6Oy-9vm.js`),
              __vite__mapDeps([72, 1]),
              import.meta.url,
            ),
        },
        {
          id: `bibtex`,
          name: `BibTeX`,
          import: () =>
            u(
              () => import(`./bibtex-D2Zuw43D.js`),
              __vite__mapDeps([73, 1]),
              import.meta.url,
            ),
        },
        {
          id: `bicep`,
          name: `Bicep`,
          import: () =>
            u(
              () => import(`./bicep-BV5aqUmf.js`),
              __vite__mapDeps([74, 1]),
              import.meta.url,
            ),
        },
        {
          id: `blade`,
          name: `Blade`,
          import: () =>
            u(
              () => import(`./blade-BkFEOJDj.js`),
              __vite__mapDeps([75, 1, 50, 51, 49, 57, 58, 59, 76, 77]),
              import.meta.url,
            ),
        },
        {
          id: `bsl`,
          name: `1C (Enterprise)`,
          aliases: [`1c`],
          import: () =>
            u(
              () => import(`./bsl-D2t9I2_S.js`),
              __vite__mapDeps([78, 1, 79]),
              import.meta.url,
            ),
        },
        {
          id: `c`,
          name: `C`,
          import: () =>
            u(
              () => import(`./c-BmyAN9uB.js`),
              __vite__mapDeps([80, 81, 1]),
              import.meta.url,
            ),
        },
        {
          id: `cadence`,
          name: `Cadence`,
          aliases: [`cdc`],
          import: () =>
            u(
              () => import(`./cadence-ZZQD7dZZ.js`),
              __vite__mapDeps([82, 1]),
              import.meta.url,
            ),
        },
        {
          id: `cairo`,
          name: `Cairo`,
          import: () =>
            u(
              () => import(`./cairo-BGnxcrn3.js`),
              __vite__mapDeps([83, 1, 84]),
              import.meta.url,
            ),
        },
        {
          id: `clarity`,
          name: `Clarity`,
          import: () =>
            u(
              () => import(`./clarity-Jx84T4hq.js`),
              __vite__mapDeps([85, 1]),
              import.meta.url,
            ),
        },
        {
          id: `clojure`,
          name: `Clojure`,
          aliases: [`clj`],
          import: () =>
            u(
              () => import(`./clojure-CJrKn15m.js`),
              __vite__mapDeps([86, 1]),
              import.meta.url,
            ),
        },
        {
          id: `cmake`,
          name: `CMake`,
          import: () =>
            u(
              () => import(`./cmake-KgGHQiyL.js`),
              __vite__mapDeps([87, 88, 1]),
              import.meta.url,
            ),
        },
        {
          id: `cobol`,
          name: `COBOL`,
          import: () =>
            u(
              () => import(`./cobol-DMB6nn2Q.js`),
              __vite__mapDeps([89, 1, 49, 50, 51, 58]),
              import.meta.url,
            ),
        },
        {
          id: `codeowners`,
          name: `CODEOWNERS`,
          import: () =>
            u(
              () => import(`./codeowners-DdstKaNr.js`),
              __vite__mapDeps([90, 1]),
              import.meta.url,
            ),
        },
        {
          id: `codeql`,
          name: `CodeQL`,
          aliases: [`ql`],
          import: () =>
            u(
              () => import(`./codeql-DelfTVmv.js`),
              __vite__mapDeps([91, 1]),
              import.meta.url,
            ),
        },
        {
          id: `coffee`,
          name: `CoffeeScript`,
          aliases: [`coffeescript`],
          import: () =>
            u(
              () => import(`./coffee-B5pcpOkO.js`),
              __vite__mapDeps([92, 1, 50]),
              import.meta.url,
            ),
        },
        {
          id: `common-lisp`,
          name: `Common Lisp`,
          aliases: [`lisp`],
          import: () =>
            u(
              () => import(`./common-lisp-BQFvTA-U.js`),
              __vite__mapDeps([93, 1]),
              import.meta.url,
            ),
        },
        {
          id: `coq`,
          name: `Coq`,
          import: () =>
            u(
              () => import(`./coq-C127Ok58.js`),
              __vite__mapDeps([94, 1]),
              import.meta.url,
            ),
        },
        {
          id: `cpp`,
          name: `C++`,
          aliases: [`c++`],
          import: () =>
            u(
              () => import(`./cpp-FyRC_YXp.js`),
              __vite__mapDeps([95, 96, 1, 77, 97, 98, 81]),
              import.meta.url,
            ),
        },
        {
          id: `crystal`,
          name: `Crystal`,
          import: () =>
            u(
              () => import(`./crystal-D0O7CT8b.js`),
              __vite__mapDeps([99, 1, 50, 51, 49, 77, 81, 100]),
              import.meta.url,
            ),
        },
        {
          id: `csharp`,
          name: `C#`,
          aliases: [`c#`, `cs`],
          import: () =>
            u(
              () => import(`./csharp-dWOu_Z9v.js`),
              __vite__mapDeps([101, 102, 1]),
              import.meta.url,
            ),
        },
        {
          id: `css`,
          name: `CSS`,
          import: () =>
            u(
              () => import(`./css-V-D1sJxY.js`),
              __vite__mapDeps([103, 51, 1]),
              import.meta.url,
            ),
        },
        {
          id: `csv`,
          name: `CSV`,
          import: () =>
            u(
              () => import(`./csv-C2W0euoY.js`),
              __vite__mapDeps([104, 105, 1]),
              import.meta.url,
            ),
        },
        {
          id: `cue`,
          name: `CUE`,
          import: () =>
            u(
              () => import(`./cue-DAU6h1eV.js`),
              __vite__mapDeps([106, 1]),
              import.meta.url,
            ),
        },
        {
          id: `cypher`,
          name: `Cypher`,
          aliases: [`cql`],
          import: () =>
            u(
              () => import(`./cypher-BgkdepPV.js`),
              __vite__mapDeps([107, 1]),
              import.meta.url,
            ),
        },
        {
          id: `d`,
          name: `D`,
          import: () =>
            u(
              () => import(`./d-979M2VZt.js`),
              __vite__mapDeps([108, 1]),
              import.meta.url,
            ),
        },
        {
          id: `dart`,
          name: `Dart`,
          import: () =>
            u(
              () => import(`./dart-D_H5G3KO.js`),
              __vite__mapDeps([109, 1]),
              import.meta.url,
            ),
        },
        {
          id: `dax`,
          name: `DAX`,
          import: () =>
            u(
              () => import(`./dax-CtLPlU6I.js`),
              __vite__mapDeps([110, 1]),
              import.meta.url,
            ),
        },
        {
          id: `desktop`,
          name: `Desktop`,
          import: () =>
            u(
              () => import(`./desktop-DCkZF_fi.js`),
              __vite__mapDeps([111, 1]),
              import.meta.url,
            ),
        },
        {
          id: `diff`,
          name: `Diff`,
          import: () =>
            u(
              () => import(`./diff-BzFlkiCH.js`),
              __vite__mapDeps([112, 113, 1]),
              import.meta.url,
            ),
        },
        {
          id: `docker`,
          name: `Dockerfile`,
          aliases: [`dockerfile`],
          import: () =>
            u(
              () => import(`./docker-B6hc43zL.js`),
              __vite__mapDeps([114, 1]),
              import.meta.url,
            ),
        },
        {
          id: `dotenv`,
          name: `dotEnv`,
          import: () =>
            u(
              () => import(`./dotenv-DCAY4dW3.js`),
              __vite__mapDeps([115, 1]),
              import.meta.url,
            ),
        },
        {
          id: `dream-maker`,
          name: `Dream Maker`,
          import: () =>
            u(
              () => import(`./dream-maker-BDscK0je.js`),
              __vite__mapDeps([116, 1]),
              import.meta.url,
            ),
        },
        {
          id: `edge`,
          name: `Edge`,
          import: () =>
            u(
              () => import(`./edge-CWv-0KHn.js`),
              __vite__mapDeps([117, 1, 49, 50, 51, 65, 76]),
              import.meta.url,
            ),
        },
        {
          id: `elixir`,
          name: `Elixir`,
          import: () =>
            u(
              () => import(`./elixir-z9SSFN0q.js`),
              __vite__mapDeps([118, 1, 49, 50, 51]),
              import.meta.url,
            ),
        },
        {
          id: `elm`,
          name: `Elm`,
          import: () =>
            u(
              () => import(`./elm-Dm6xryLg.js`),
              __vite__mapDeps([119, 1, 98, 81]),
              import.meta.url,
            ),
        },
        {
          id: `emacs-lisp`,
          name: `Emacs Lisp`,
          aliases: [`elisp`],
          import: () =>
            u(
              () => import(`./emacs-lisp-08grgfz1.js`),
              __vite__mapDeps([120, 1]),
              import.meta.url,
            ),
        },
        {
          id: `erb`,
          name: `ERB`,
          import: () =>
            u(
              () => import(`./erb-3scvucIY.js`),
              __vite__mapDeps([
                121, 1, 49, 50, 51, 122, 57, 58, 77, 81, 96, 97, 98, 100, 123,
                124, 65, 67, 125, 126, 127,
              ]),
              import.meta.url,
            ),
        },
        {
          id: `erlang`,
          name: `Erlang`,
          aliases: [`erl`],
          import: () =>
            u(
              () => import(`./erlang-Buwg4T7D.js`),
              __vite__mapDeps([128, 1, 129]),
              import.meta.url,
            ),
        },
        {
          id: `fennel`,
          name: `Fennel`,
          import: () =>
            u(
              () => import(`./fennel-CfcAYwTx.js`),
              __vite__mapDeps([130, 1]),
              import.meta.url,
            ),
        },
        {
          id: `fish`,
          name: `Fish`,
          import: () =>
            u(
              () => import(`./fish-B1FCMeeT.js`),
              __vite__mapDeps([131, 1]),
              import.meta.url,
            ),
        },
        {
          id: `fluent`,
          name: `Fluent`,
          aliases: [`ftl`],
          import: () =>
            u(
              () => import(`./fluent-BYDic30O.js`),
              __vite__mapDeps([132, 1]),
              import.meta.url,
            ),
        },
        {
          id: `fortran-fixed-form`,
          name: `Fortran (Fixed Form)`,
          aliases: [`f`, `for`, `f77`],
          import: () =>
            u(
              () => import(`./fortran-fixed-form-BbbChIYF.js`),
              __vite__mapDeps([133, 1, 134]),
              import.meta.url,
            ),
        },
        {
          id: `fortran-free-form`,
          name: `Fortran (Free Form)`,
          aliases: [`f90`, `f95`, `f03`, `f08`, `f18`],
          import: () =>
            u(
              () => import(`./fortran-free-form-CqBp4yPH.js`),
              __vite__mapDeps([135, 134, 1]),
              import.meta.url,
            ),
        },
        {
          id: `fsharp`,
          name: `F#`,
          aliases: [`f#`, `fs`],
          import: () =>
            u(
              () => import(`./fsharp-C5yVDSse.js`),
              __vite__mapDeps([136, 1, 129]),
              import.meta.url,
            ),
        },
        {
          id: `gdresource`,
          name: `GDResource`,
          import: () =>
            u(
              () => import(`./gdresource-DnXtqM4p.js`),
              __vite__mapDeps([137, 1, 138, 139]),
              import.meta.url,
            ),
        },
        {
          id: `gdscript`,
          name: `GDScript`,
          import: () =>
            u(
              () => import(`./gdscript-CWUNS79S.js`),
              __vite__mapDeps([140, 139, 1]),
              import.meta.url,
            ),
        },
        {
          id: `gdshader`,
          name: `GDShader`,
          import: () =>
            u(
              () => import(`./gdshader-LwbvDd7e.js`),
              __vite__mapDeps([141, 138, 1]),
              import.meta.url,
            ),
        },
        {
          id: `genie`,
          name: `Genie`,
          import: () =>
            u(
              () => import(`./genie-BZs60kAO.js`),
              __vite__mapDeps([142, 1]),
              import.meta.url,
            ),
        },
        {
          id: `gherkin`,
          name: `Gherkin`,
          import: () =>
            u(
              () => import(`./gherkin-HraT_eBV.js`),
              __vite__mapDeps([143, 1]),
              import.meta.url,
            ),
        },
        {
          id: `git-commit`,
          name: `Git Commit Message`,
          import: () =>
            u(
              () => import(`./git-commit-DNx_Ajsr.js`),
              __vite__mapDeps([144, 1, 113]),
              import.meta.url,
            ),
        },
        {
          id: `git-rebase`,
          name: `Git Rebase Message`,
          import: () =>
            u(
              () => import(`./git-rebase-Cs3wX5ZZ.js`),
              __vite__mapDeps([145, 1, 100]),
              import.meta.url,
            ),
        },
        {
          id: `gleam`,
          name: `Gleam`,
          import: () =>
            u(
              () => import(`./gleam-DrYb8_r9.js`),
              __vite__mapDeps([146, 1]),
              import.meta.url,
            ),
        },
        {
          id: `glimmer-js`,
          name: `Glimmer JS`,
          aliases: [`gjs`],
          import: () =>
            u(
              () => import(`./glimmer-js-BM4JRk62.js`),
              __vite__mapDeps([147, 1, 50, 51, 49, 65]),
              import.meta.url,
            ),
        },
        {
          id: `glimmer-ts`,
          name: `Glimmer TS`,
          aliases: [`gts`],
          import: () =>
            u(
              () => import(`./glimmer-ts-jgKbaQ7P.js`),
              __vite__mapDeps([148, 1, 50, 51, 49, 65]),
              import.meta.url,
            ),
        },
        {
          id: `glsl`,
          name: `GLSL`,
          import: () =>
            u(
              () => import(`./glsl-C2DGYntX.js`),
              __vite__mapDeps([149, 98, 1, 81]),
              import.meta.url,
            ),
        },
        {
          id: `gnuplot`,
          name: `Gnuplot`,
          import: () =>
            u(
              () => import(`./gnuplot-CY0pRbZS.js`),
              __vite__mapDeps([150, 1]),
              import.meta.url,
            ),
        },
        {
          id: `go`,
          name: `Go`,
          import: () =>
            u(
              () => import(`./go-Cq6UZW8L.js`),
              __vite__mapDeps([151, 152, 1]),
              import.meta.url,
            ),
        },
        {
          id: `graphql`,
          name: `GraphQL`,
          aliases: [`gql`],
          import: () =>
            u(
              () => import(`./graphql-BZFKNm7t.js`),
              __vite__mapDeps([153, 124, 1, 50, 65, 67, 125]),
              import.meta.url,
            ),
        },
        {
          id: `groovy`,
          name: `Groovy`,
          import: () =>
            u(
              () => import(`./groovy-91J0aXvO.js`),
              __vite__mapDeps([154, 1]),
              import.meta.url,
            ),
        },
        {
          id: `hack`,
          name: `Hack`,
          import: () =>
            u(
              () => import(`./hack-j5PJB0dN.js`),
              __vite__mapDeps([155, 1, 49, 50, 51, 77]),
              import.meta.url,
            ),
        },
        {
          id: `haml`,
          name: `Ruby Haml`,
          import: () =>
            u(
              () => import(`./haml-4JVWm-3n.js`),
              __vite__mapDeps([156, 123, 1, 50, 51]),
              import.meta.url,
            ),
        },
        {
          id: `handlebars`,
          name: `Handlebars`,
          aliases: [`hbs`],
          import: () =>
            u(
              () => import(`./handlebars-DhrgIUMA.js`),
              __vite__mapDeps([157, 1, 50, 51, 49, 127]),
              import.meta.url,
            ),
        },
        {
          id: `haskell`,
          name: `Haskell`,
          aliases: [`hs`],
          import: () =>
            u(
              () => import(`./haskell-CkTiDu-A.js`),
              __vite__mapDeps([158, 1]),
              import.meta.url,
            ),
        },
        {
          id: `haxe`,
          name: `Haxe`,
          import: () =>
            u(
              () => import(`./haxe-CEcCCwTA.js`),
              __vite__mapDeps([159, 160, 1]),
              import.meta.url,
            ),
        },
        {
          id: `hcl`,
          name: `HashiCorp HCL`,
          import: () =>
            u(
              () => import(`./hcl-CRYOyEwg.js`),
              __vite__mapDeps([161, 1]),
              import.meta.url,
            ),
        },
        {
          id: `hjson`,
          name: `Hjson`,
          import: () =>
            u(
              () => import(`./hjson-CrPQ-eVJ.js`),
              __vite__mapDeps([162, 1]),
              import.meta.url,
            ),
        },
        {
          id: `hlsl`,
          name: `HLSL`,
          import: () =>
            u(
              () => import(`./hlsl-DCUHJD93.js`),
              __vite__mapDeps([163, 164, 1]),
              import.meta.url,
            ),
        },
        {
          id: `html`,
          name: `HTML`,
          import: () =>
            u(
              () => import(`./html-U70y7bkx.js`),
              __vite__mapDeps([165, 49, 1, 50, 51]),
              import.meta.url,
            ),
        },
        {
          id: `html-derivative`,
          name: `HTML (Derivative)`,
          import: () =>
            u(
              () => import(`./html-derivative-DIA0fB0y.js`),
              __vite__mapDeps([166, 76, 1, 49, 50, 51]),
              import.meta.url,
            ),
        },
        {
          id: `http`,
          name: `HTTP`,
          import: () =>
            u(
              () => import(`./http-oJLH9DwK.js`),
              __vite__mapDeps([167, 1, 57, 58, 59, 100, 124, 50, 65, 67, 125]),
              import.meta.url,
            ),
        },
        {
          id: `hurl`,
          name: `Hurl`,
          import: () =>
            u(
              () => import(`./hurl-CnznnXyE.js`),
              __vite__mapDeps([168, 1, 57, 58, 105, 124, 50, 65, 67, 125]),
              import.meta.url,
            ),
        },
        {
          id: `hxml`,
          name: `HXML`,
          import: () =>
            u(
              () => import(`./hxml-DQCR-9Aw.js`),
              __vite__mapDeps([169, 1, 160]),
              import.meta.url,
            ),
        },
        {
          id: `hy`,
          name: `Hy`,
          import: () =>
            u(
              () => import(`./hy-DZzqSxRj.js`),
              __vite__mapDeps([170, 1]),
              import.meta.url,
            ),
        },
        {
          id: `imba`,
          name: `Imba`,
          import: () =>
            u(
              () => import(`./imba-Vbfs4RA8.js`),
              __vite__mapDeps([171, 1]),
              import.meta.url,
            ),
        },
        {
          id: `ini`,
          name: `INI`,
          aliases: [`properties`],
          import: () =>
            u(
              () => import(`./ini-Dtvfewn7.js`),
              __vite__mapDeps([172, 1]),
              import.meta.url,
            ),
        },
        {
          id: `java`,
          name: `Java`,
          import: () =>
            u(
              () => import(`./java-D9QdBNwZ.js`),
              __vite__mapDeps([173, 58, 1]),
              import.meta.url,
            ),
        },
        {
          id: `javascript`,
          name: `JavaScript`,
          aliases: [`js`, `cjs`, `mjs`],
          import: () =>
            u(
              () => import(`./javascript-DvJiI4BN.js`),
              __vite__mapDeps([174, 50, 1]),
              import.meta.url,
            ),
        },
        {
          id: `jinja`,
          name: `Jinja`,
          import: () =>
            u(
              () => import(`./jinja-ClX0KWKp.js`),
              __vite__mapDeps([175, 1, 49, 50, 51]),
              import.meta.url,
            ),
        },
        {
          id: `jison`,
          name: `Jison`,
          import: () =>
            u(
              () => import(`./jison-CI3yLa7S.js`),
              __vite__mapDeps([176, 1, 50]),
              import.meta.url,
            ),
        },
        {
          id: `json`,
          name: `JSON`,
          import: () =>
            u(
              () => import(`./json-mzGplh77.js`),
              __vite__mapDeps([177, 59, 1]),
              import.meta.url,
            ),
        },
        {
          id: `json5`,
          name: `JSON5`,
          import: () =>
            u(
              () => import(`./json5-DnR-vtQX.js`),
              __vite__mapDeps([178, 1]),
              import.meta.url,
            ),
        },
        {
          id: `jsonc`,
          name: `JSON with Comments`,
          import: () =>
            u(
              () => import(`./jsonc-ucncoNT8.js`),
              __vite__mapDeps([179, 1]),
              import.meta.url,
            ),
        },
        {
          id: `jsonl`,
          name: `JSON Lines`,
          import: () =>
            u(
              () => import(`./jsonl-BAZgty2a.js`),
              __vite__mapDeps([180, 1]),
              import.meta.url,
            ),
        },
        {
          id: `jsonnet`,
          name: `Jsonnet`,
          import: () =>
            u(
              () => import(`./jsonnet-JwJoQ3yG.js`),
              __vite__mapDeps([181, 1]),
              import.meta.url,
            ),
        },
        {
          id: `jssm`,
          name: `JSSM`,
          aliases: [`fsl`],
          import: () =>
            u(
              () => import(`./jssm-90TpF08_.js`),
              __vite__mapDeps([182, 1]),
              import.meta.url,
            ),
        },
        {
          id: `jsx`,
          name: `JSX`,
          import: () =>
            u(
              () => import(`./jsx-CsrZUv5w.js`),
              __vite__mapDeps([183, 125, 1]),
              import.meta.url,
            ),
        },
        {
          id: `julia`,
          name: `Julia`,
          aliases: [`jl`],
          import: () =>
            u(
              () => import(`./julia-DQDw9j-A.js`),
              __vite__mapDeps([184, 1, 50, 77, 84, 96, 97, 98, 81, 185]),
              import.meta.url,
            ),
        },
        {
          id: `kdl`,
          name: `KDL`,
          import: () =>
            u(
              () => import(`./kdl-DFPXMwwh.js`),
              __vite__mapDeps([186, 1]),
              import.meta.url,
            ),
        },
        {
          id: `kotlin`,
          name: `Kotlin`,
          aliases: [`kt`, `kts`],
          import: () =>
            u(
              () => import(`./kotlin-DaCiEX6O.js`),
              __vite__mapDeps([187, 1]),
              import.meta.url,
            ),
        },
        {
          id: `kusto`,
          name: `Kusto`,
          aliases: [`kql`],
          import: () =>
            u(
              () => import(`./kusto-C0SYUvTL.js`),
              __vite__mapDeps([188, 1]),
              import.meta.url,
            ),
        },
        {
          id: `latex`,
          name: `LaTeX`,
          import: () =>
            u(
              () => import(`./latex-CunzpGV0.js`),
              __vite__mapDeps([189, 1, 190, 185]),
              import.meta.url,
            ),
        },
        {
          id: `lean`,
          name: `Lean 4`,
          aliases: [`lean4`],
          import: () =>
            u(
              () => import(`./lean-C10ROJ-Q.js`),
              __vite__mapDeps([191, 1]),
              import.meta.url,
            ),
        },
        {
          id: `less`,
          name: `Less`,
          import: () =>
            u(
              () => import(`./less-CWKu4QOZ.js`),
              __vite__mapDeps([192, 193, 1]),
              import.meta.url,
            ),
        },
        {
          id: `liquid`,
          name: `Liquid`,
          import: () =>
            u(
              () => import(`./liquid-CqLRGdm8.js`),
              __vite__mapDeps([194, 1, 50, 51, 49, 59]),
              import.meta.url,
            ),
        },
        {
          id: `llvm`,
          name: `LLVM IR`,
          import: () =>
            u(
              () => import(`./llvm--vqeDrbk.js`),
              __vite__mapDeps([195, 1]),
              import.meta.url,
            ),
        },
        {
          id: `log`,
          name: `Log file`,
          import: () =>
            u(
              () => import(`./log-lPfpuUBt.js`),
              __vite__mapDeps([196, 1]),
              import.meta.url,
            ),
        },
        {
          id: `logo`,
          name: `Logo`,
          import: () =>
            u(
              () => import(`./logo-CNBzc-Lh.js`),
              __vite__mapDeps([197, 1]),
              import.meta.url,
            ),
        },
        {
          id: `lua`,
          name: `Lua`,
          import: () =>
            u(
              () => import(`./lua-BfJX8OWW.js`),
              __vite__mapDeps([198, 126, 1, 81]),
              import.meta.url,
            ),
        },
        {
          id: `luau`,
          name: `Luau`,
          import: () =>
            u(
              () => import(`./luau-BxUOJZlq.js`),
              __vite__mapDeps([199, 1]),
              import.meta.url,
            ),
        },
        {
          id: `make`,
          name: `Makefile`,
          aliases: [`makefile`],
          import: () =>
            u(
              () => import(`./make-DrMRNqKV.js`),
              __vite__mapDeps([200, 1]),
              import.meta.url,
            ),
        },
        {
          id: `markdown`,
          name: `Markdown`,
          aliases: [`md`],
          import: () =>
            u(
              () => import(`./markdown-D8taA4DT.js`),
              __vite__mapDeps([201, 129, 1]),
              import.meta.url,
            ),
        },
        {
          id: `marko`,
          name: `Marko`,
          import: () =>
            u(
              () => import(`./marko-DUunA5fr.js`),
              __vite__mapDeps([202, 1, 51, 53, 65, 193]),
              import.meta.url,
            ),
        },
        {
          id: `matlab`,
          name: `MATLAB`,
          import: () =>
            u(
              () => import(`./matlab-Cs6W9s25.js`),
              __vite__mapDeps([203, 1]),
              import.meta.url,
            ),
        },
        {
          id: `mdc`,
          name: `MDC`,
          import: () =>
            u(
              () => import(`./mdc-BvBFWg9R.js`),
              __vite__mapDeps([204, 1, 76, 49, 50, 51, 127, 129]),
              import.meta.url,
            ),
        },
        {
          id: `mdx`,
          name: `MDX`,
          import: () =>
            u(
              () => import(`./mdx-Cmvb6FXn.js`),
              __vite__mapDeps([205, 1]),
              import.meta.url,
            ),
        },
        {
          id: `mermaid`,
          name: `Mermaid`,
          aliases: [`mmd`],
          import: () =>
            u(
              () => import(`./mermaid-CPoqeGkL.js`),
              __vite__mapDeps([206, 1]),
              import.meta.url,
            ),
        },
        {
          id: `mipsasm`,
          name: `MIPS Assembly`,
          aliases: [`mips`],
          import: () =>
            u(
              () => import(`./mipsasm-ChwQ2pdz.js`),
              __vite__mapDeps([207, 1]),
              import.meta.url,
            ),
        },
        {
          id: `mojo`,
          name: `Mojo`,
          import: () =>
            u(
              () => import(`./mojo-CL4ckX0C.js`),
              __vite__mapDeps([208, 1]),
              import.meta.url,
            ),
        },
        {
          id: `move`,
          name: `Move`,
          import: () =>
            u(
              () => import(`./move-3VCSOm88.js`),
              __vite__mapDeps([209, 1]),
              import.meta.url,
            ),
        },
        {
          id: `narrat`,
          name: `Narrat Language`,
          aliases: [`nar`],
          import: () =>
            u(
              () => import(`./narrat-CWyCaX3m.js`),
              __vite__mapDeps([210, 1]),
              import.meta.url,
            ),
        },
        {
          id: `nextflow`,
          name: `Nextflow`,
          aliases: [`nf`],
          import: () =>
            u(
              () => import(`./nextflow-CW2eZB5d.js`),
              __vite__mapDeps([211, 1]),
              import.meta.url,
            ),
        },
        {
          id: `nginx`,
          name: `Nginx`,
          import: () =>
            u(
              () => import(`./nginx-CGuqvTXw.js`),
              __vite__mapDeps([212, 1, 126, 81]),
              import.meta.url,
            ),
        },
        {
          id: `nim`,
          name: `Nim`,
          import: () =>
            u(
              () => import(`./nim-Bx41sJJC.js`),
              __vite__mapDeps([213, 1, 50, 51, 49, 57, 58, 81, 98, 129]),
              import.meta.url,
            ),
        },
        {
          id: `nix`,
          name: `Nix`,
          import: () =>
            u(
              () => import(`./nix-COLYZJH5.js`),
              __vite__mapDeps([214, 1]),
              import.meta.url,
            ),
        },
        {
          id: `nushell`,
          name: `nushell`,
          aliases: [`nu`],
          import: () =>
            u(
              () => import(`./nushell-B5b4kHlf.js`),
              __vite__mapDeps([215, 1]),
              import.meta.url,
            ),
        },
        {
          id: `objective-c`,
          name: `Objective-C`,
          aliases: [`objc`],
          import: () =>
            u(
              () => import(`./objective-c-D4Gn-j4f.js`),
              __vite__mapDeps([216, 1]),
              import.meta.url,
            ),
        },
        {
          id: `objective-cpp`,
          name: `Objective-C++`,
          import: () =>
            u(
              () => import(`./objective-cpp-DCnZ80Ja.js`),
              __vite__mapDeps([217, 1]),
              import.meta.url,
            ),
        },
        {
          id: `ocaml`,
          name: `OCaml`,
          import: () =>
            u(
              () => import(`./ocaml-DSoT0tsl.js`),
              __vite__mapDeps([218, 1]),
              import.meta.url,
            ),
        },
        {
          id: `openscad`,
          name: `OpenSCAD`,
          aliases: [`scad`],
          import: () =>
            u(
              () => import(`./openscad-sTPJecY0.js`),
              __vite__mapDeps([219, 1]),
              import.meta.url,
            ),
        },
        {
          id: `pascal`,
          name: `Pascal`,
          import: () =>
            u(
              () => import(`./pascal-BfFCEeSO.js`),
              __vite__mapDeps([220, 1]),
              import.meta.url,
            ),
        },
        {
          id: `perl`,
          name: `Perl`,
          import: () =>
            u(
              () => import(`./perl-DRNGkJyZ.js`),
              __vite__mapDeps([221, 1, 50, 51, 49, 57, 58, 77]),
              import.meta.url,
            ),
        },
        {
          id: `php`,
          name: `PHP`,
          import: () =>
            u(
              () => import(`./php-u3PWnPKC.js`),
              __vite__mapDeps([222, 223, 1, 50, 51, 49, 57, 58, 59, 77]),
              import.meta.url,
            ),
        },
        {
          id: `pkl`,
          name: `Pkl`,
          import: () =>
            u(
              () => import(`./pkl-CrvJXQTz.js`),
              __vite__mapDeps([224, 1]),
              import.meta.url,
            ),
        },
        {
          id: `plsql`,
          name: `PL/SQL`,
          import: () =>
            u(
              () => import(`./plsql-CM0MXXr-.js`),
              __vite__mapDeps([225, 1]),
              import.meta.url,
            ),
        },
        {
          id: `po`,
          name: `Gettext PO`,
          aliases: [`pot`, `potx`],
          import: () =>
            u(
              () => import(`./po-CgJm0ldQ.js`),
              __vite__mapDeps([226, 1]),
              import.meta.url,
            ),
        },
        {
          id: `polar`,
          name: `Polar`,
          import: () =>
            u(
              () => import(`./polar-BMtpq-uK.js`),
              __vite__mapDeps([227, 1]),
              import.meta.url,
            ),
        },
        {
          id: `postcss`,
          name: `PostCSS`,
          import: () =>
            u(
              () => import(`./postcss-CuYKKrDz.js`),
              __vite__mapDeps([228, 66, 1]),
              import.meta.url,
            ),
        },
        {
          id: `powerquery`,
          name: `PowerQuery`,
          import: () =>
            u(
              () => import(`./powerquery-GgO_-WMD.js`),
              __vite__mapDeps([229, 1]),
              import.meta.url,
            ),
        },
        {
          id: `powershell`,
          name: `PowerShell`,
          aliases: [`ps`, `ps1`],
          import: () =>
            u(
              () => import(`./powershell-0yM8JrML.js`),
              __vite__mapDeps([230, 1]),
              import.meta.url,
            ),
        },
        {
          id: `prisma`,
          name: `Prisma`,
          import: () =>
            u(
              () => import(`./prisma-7Yu0zXx7.js`),
              __vite__mapDeps([231, 1]),
              import.meta.url,
            ),
        },
        {
          id: `prolog`,
          name: `Prolog`,
          import: () =>
            u(
              () => import(`./prolog-qL3LRv5Y.js`),
              __vite__mapDeps([232, 1]),
              import.meta.url,
            ),
        },
        {
          id: `proto`,
          name: `Protocol Buffer 3`,
          aliases: [`protobuf`],
          import: () =>
            u(
              () => import(`./proto-BYEI3gE7.js`),
              __vite__mapDeps([233, 1]),
              import.meta.url,
            ),
        },
        {
          id: `pug`,
          name: `Pug`,
          aliases: [`jade`],
          import: () =>
            u(
              () => import(`./pug-BNlsll0M.js`),
              __vite__mapDeps([234, 1, 50, 51, 49]),
              import.meta.url,
            ),
        },
        {
          id: `puppet`,
          name: `Puppet`,
          import: () =>
            u(
              () => import(`./puppet-CpAVMDpy.js`),
              __vite__mapDeps([235, 1]),
              import.meta.url,
            ),
        },
        {
          id: `purescript`,
          name: `PureScript`,
          import: () =>
            u(
              () => import(`./purescript-DDMKqqLy.js`),
              __vite__mapDeps([236, 1]),
              import.meta.url,
            ),
        },
        {
          id: `python`,
          name: `Python`,
          aliases: [`py`],
          import: () =>
            u(
              () => import(`./python-CMSGC7OP.js`),
              __vite__mapDeps([237, 84, 1]),
              import.meta.url,
            ),
        },
        {
          id: `qml`,
          name: `QML`,
          import: () =>
            u(
              () => import(`./qml-Cyv_rvGe.js`),
              __vite__mapDeps([238, 1, 50]),
              import.meta.url,
            ),
        },
        {
          id: `qmldir`,
          name: `QML Directory`,
          import: () =>
            u(
              () => import(`./qmldir-CfVhRUnj.js`),
              __vite__mapDeps([239, 1]),
              import.meta.url,
            ),
        },
        {
          id: `qss`,
          name: `Qt Style Sheets`,
          import: () =>
            u(
              () => import(`./qss-BuA5M-EL.js`),
              __vite__mapDeps([240, 1]),
              import.meta.url,
            ),
        },
        {
          id: `r`,
          name: `R`,
          import: () =>
            u(
              () => import(`./r-DD3XXSfB.js`),
              __vite__mapDeps([241, 185, 1]),
              import.meta.url,
            ),
        },
        {
          id: `racket`,
          name: `Racket`,
          import: () =>
            u(
              () => import(`./racket-y96I3IL-.js`),
              __vite__mapDeps([242, 1]),
              import.meta.url,
            ),
        },
        {
          id: `raku`,
          name: `Raku`,
          aliases: [`perl6`],
          import: () =>
            u(
              () => import(`./raku-FGrSszDT.js`),
              __vite__mapDeps([243, 1]),
              import.meta.url,
            ),
        },
        {
          id: `razor`,
          name: `ASP.NET Razor`,
          import: () =>
            u(
              () => import(`./razor-D88oIyzu.js`),
              __vite__mapDeps([244, 1, 49, 50, 51, 102]),
              import.meta.url,
            ),
        },
        {
          id: `reg`,
          name: `Windows Registry Script`,
          import: () =>
            u(
              () => import(`./reg-oQghgDHR.js`),
              __vite__mapDeps([245, 1]),
              import.meta.url,
            ),
        },
        {
          id: `regexp`,
          name: `RegExp`,
          aliases: [`regex`],
          import: () =>
            u(
              () => import(`./regexp-BYAVqG0d.js`),
              __vite__mapDeps([246, 97, 1]),
              import.meta.url,
            ),
        },
        {
          id: `rel`,
          name: `Rel`,
          import: () =>
            u(
              () => import(`./rel-DWKNrgZc.js`),
              __vite__mapDeps([247, 1]),
              import.meta.url,
            ),
        },
        {
          id: `riscv`,
          name: `RISC-V`,
          import: () =>
            u(
              () => import(`./riscv-DTL-geNI.js`),
              __vite__mapDeps([248, 1]),
              import.meta.url,
            ),
        },
        {
          id: `rosmsg`,
          name: `ROS Interface`,
          import: () =>
            u(
              () => import(`./rosmsg-DXrlBQzq.js`),
              __vite__mapDeps([249, 1]),
              import.meta.url,
            ),
        },
        {
          id: `rst`,
          name: `reStructuredText`,
          import: () =>
            u(
              () => import(`./rst-Dw_wtorp.js`),
              __vite__mapDeps([
                250, 1, 50, 76, 49, 51, 84, 88, 96, 77, 97, 98, 81, 100, 127,
                122, 57, 58, 123, 124, 65, 67, 125, 126,
              ]),
              import.meta.url,
            ),
        },
        {
          id: `ruby`,
          name: `Ruby`,
          aliases: [`rb`],
          import: () =>
            u(
              () => import(`./ruby-Bgo5X_b6.js`),
              __vite__mapDeps([
                251, 122, 1, 50, 51, 49, 57, 58, 77, 81, 96, 97, 98, 100, 123,
                124, 65, 67, 125, 126, 127,
              ]),
              import.meta.url,
            ),
        },
        {
          id: `rust`,
          name: `Rust`,
          aliases: [`rs`],
          import: () =>
            u(
              () => import(`./rust-BT01z89n.js`),
              __vite__mapDeps([252, 1]),
              import.meta.url,
            ),
        },
        {
          id: `sas`,
          name: `SAS`,
          import: () =>
            u(
              () => import(`./sas-UJ1zf83f.js`),
              __vite__mapDeps([253, 1, 77]),
              import.meta.url,
            ),
        },
        {
          id: `sass`,
          name: `Sass`,
          import: () =>
            u(
              () => import(`./sass-BmBNR1GI.js`),
              __vite__mapDeps([254, 1]),
              import.meta.url,
            ),
        },
        {
          id: `scala`,
          name: `Scala`,
          import: () =>
            u(
              () => import(`./scala-DCUSuZ-K.js`),
              __vite__mapDeps([255, 1]),
              import.meta.url,
            ),
        },
        {
          id: `scheme`,
          name: `Scheme`,
          import: () =>
            u(
              () => import(`./scheme-CYTG-RX4.js`),
              __vite__mapDeps([256, 1]),
              import.meta.url,
            ),
        },
        {
          id: `scss`,
          name: `SCSS`,
          import: () =>
            u(
              () => import(`./scss-DFvoAVuL.js`),
              __vite__mapDeps([257, 53, 1, 51]),
              import.meta.url,
            ),
        },
        {
          id: `sdbl`,
          name: `1C (Query)`,
          aliases: [`1c-query`],
          import: () =>
            u(
              () => import(`./sdbl-CsXK_PRI.js`),
              __vite__mapDeps([258, 79, 1]),
              import.meta.url,
            ),
        },
        {
          id: `shaderlab`,
          name: `ShaderLab`,
          aliases: [`shader`],
          import: () =>
            u(
              () => import(`./shaderlab-DpWszKJf.js`),
              __vite__mapDeps([259, 1, 164]),
              import.meta.url,
            ),
        },
        {
          id: `shellscript`,
          name: `Shell`,
          aliases: [`bash`, `sh`, `shell`, `zsh`],
          import: () =>
            u(
              () => import(`./shellscript-D55t4LDA.js`),
              __vite__mapDeps([260, 100, 1]),
              import.meta.url,
            ),
        },
        {
          id: `shellsession`,
          name: `Shell Session`,
          aliases: [`console`],
          import: () =>
            u(
              () => import(`./shellsession-BBePPcZ9.js`),
              __vite__mapDeps([261, 1, 100]),
              import.meta.url,
            ),
        },
        {
          id: `smalltalk`,
          name: `Smalltalk`,
          import: () =>
            u(
              () => import(`./smalltalk-CL5G0l5h.js`),
              __vite__mapDeps([262, 1]),
              import.meta.url,
            ),
        },
        {
          id: `solidity`,
          name: `Solidity`,
          import: () =>
            u(
              () => import(`./solidity-D76fNJ2_.js`),
              __vite__mapDeps([263, 1]),
              import.meta.url,
            ),
        },
        {
          id: `soy`,
          name: `Closure Templates`,
          aliases: [`closure-templates`],
          import: () =>
            u(
              () => import(`./soy-BGNSUI2T.js`),
              __vite__mapDeps([264, 1, 49, 50, 51]),
              import.meta.url,
            ),
        },
        {
          id: `sparql`,
          name: `SPARQL`,
          import: () =>
            u(
              () => import(`./sparql-D92gkpiV.js`),
              __vite__mapDeps([265, 1, 266]),
              import.meta.url,
            ),
        },
        {
          id: `splunk`,
          name: `Splunk Query Language`,
          aliases: [`spl`],
          import: () =>
            u(
              () => import(`./splunk-mrXB0zn8.js`),
              __vite__mapDeps([267, 1]),
              import.meta.url,
            ),
        },
        {
          id: `sql`,
          name: `SQL`,
          import: () =>
            u(
              () => import(`./sql-DD1EB_Zf.js`),
              __vite__mapDeps([268, 77, 1]),
              import.meta.url,
            ),
        },
        {
          id: `ssh-config`,
          name: `SSH Config`,
          import: () =>
            u(
              () => import(`./ssh-config-COCT1jnt.js`),
              __vite__mapDeps([269, 1]),
              import.meta.url,
            ),
        },
        {
          id: `stata`,
          name: `Stata`,
          import: () =>
            u(
              () => import(`./stata-Dd8qx77H.js`),
              __vite__mapDeps([270, 1, 77]),
              import.meta.url,
            ),
        },
        {
          id: `stylus`,
          name: `Stylus`,
          aliases: [`styl`],
          import: () =>
            u(
              () => import(`./stylus-B_7mYYre.js`),
              __vite__mapDeps([271, 272, 1]),
              import.meta.url,
            ),
        },
        {
          id: `svelte`,
          name: `Svelte`,
          import: () =>
            u(
              () => import(`./svelte-T8pEnV9v.js`),
              __vite__mapDeps([273, 1, 50, 51, 65, 66]),
              import.meta.url,
            ),
        },
        {
          id: `swift`,
          name: `Swift`,
          import: () =>
            u(
              () => import(`./swift-Cp25HjPd.js`),
              __vite__mapDeps([274, 1]),
              import.meta.url,
            ),
        },
        {
          id: `system-verilog`,
          name: `SystemVerilog`,
          import: () =>
            u(
              () => import(`./system-verilog-b-Yh1wgm.js`),
              __vite__mapDeps([275, 1]),
              import.meta.url,
            ),
        },
        {
          id: `systemd`,
          name: `Systemd Units`,
          import: () =>
            u(
              () => import(`./systemd-C4zIfg6u.js`),
              __vite__mapDeps([276, 1]),
              import.meta.url,
            ),
        },
        {
          id: `talonscript`,
          name: `TalonScript`,
          aliases: [`talon`],
          import: () =>
            u(
              () => import(`./talonscript-tJHY__4w.js`),
              __vite__mapDeps([277, 1]),
              import.meta.url,
            ),
        },
        {
          id: `tasl`,
          name: `Tasl`,
          import: () =>
            u(
              () => import(`./tasl-IFhozMeK.js`),
              __vite__mapDeps([278, 1]),
              import.meta.url,
            ),
        },
        {
          id: `tcl`,
          name: `Tcl`,
          import: () =>
            u(
              () => import(`./tcl-DAr4ELhm.js`),
              __vite__mapDeps([279, 1]),
              import.meta.url,
            ),
        },
        {
          id: `templ`,
          name: `Templ`,
          import: () =>
            u(
              () => import(`./templ-CSSxJxfL.js`),
              __vite__mapDeps([280, 1, 50, 51, 152]),
              import.meta.url,
            ),
        },
        {
          id: `terraform`,
          name: `Terraform`,
          aliases: [`tf`, `tfvars`],
          import: () =>
            u(
              () => import(`./terraform-hw6CZhLp.js`),
              __vite__mapDeps([281, 1]),
              import.meta.url,
            ),
        },
        {
          id: `tex`,
          name: `TeX`,
          import: () =>
            u(
              () => import(`./tex-1KfC2u42.js`),
              __vite__mapDeps([282, 190, 1, 185]),
              import.meta.url,
            ),
        },
        {
          id: `toml`,
          name: `TOML`,
          import: () =>
            u(
              () => import(`./toml-CWhaK_1f.js`),
              __vite__mapDeps([283, 1]),
              import.meta.url,
            ),
        },
        {
          id: `ts-tags`,
          name: `TypeScript with Tags`,
          aliases: [`lit`],
          import: () =>
            u(
              () => import(`./ts-tags-C_MJ9MMp.js`),
              __vite__mapDeps([284, 1, 50, 51, 49, 57, 58, 65, 77, 98, 81]),
              import.meta.url,
            ),
        },
        {
          id: `tsv`,
          name: `TSV`,
          import: () =>
            u(
              () => import(`./tsv-3sTulsqv.js`),
              __vite__mapDeps([285, 1]),
              import.meta.url,
            ),
        },
        {
          id: `tsx`,
          name: `TSX`,
          import: () =>
            u(
              () => import(`./tsx-CXV4yfgF.js`),
              __vite__mapDeps([286, 67, 1]),
              import.meta.url,
            ),
        },
        {
          id: `turtle`,
          name: `Turtle`,
          import: () =>
            u(
              () => import(`./turtle-nsQACJ8b.js`),
              __vite__mapDeps([287, 266, 1]),
              import.meta.url,
            ),
        },
        {
          id: `twig`,
          name: `Twig`,
          import: () =>
            u(
              () => import(`./twig-ChlV5HWI.js`),
              __vite__mapDeps([
                288, 1, 50, 51, 53, 84, 122, 49, 57, 58, 77, 81, 96, 97, 98,
                100, 123, 124, 65, 67, 125, 126, 127, 223, 59,
              ]),
              import.meta.url,
            ),
        },
        {
          id: `typescript`,
          name: `TypeScript`,
          aliases: [`ts`, `cts`, `mts`],
          import: () =>
            u(
              () => import(`./typescript-Buwqmzid.js`),
              __vite__mapDeps([289, 65, 1]),
              import.meta.url,
            ),
        },
        {
          id: `typespec`,
          name: `TypeSpec`,
          aliases: [`tsp`],
          import: () =>
            u(
              () => import(`./typespec-DwbBEZXS.js`),
              __vite__mapDeps([290, 1]),
              import.meta.url,
            ),
        },
        {
          id: `typst`,
          name: `Typst`,
          aliases: [`typ`],
          import: () =>
            u(
              () => import(`./typst-CQZAUkim.js`),
              __vite__mapDeps([291, 1]),
              import.meta.url,
            ),
        },
        {
          id: `v`,
          name: `V`,
          import: () =>
            u(
              () => import(`./v-BRalsuVD.js`),
              __vite__mapDeps([292, 1]),
              import.meta.url,
            ),
        },
        {
          id: `vala`,
          name: `Vala`,
          import: () =>
            u(
              () => import(`./vala-UwwN6RG6.js`),
              __vite__mapDeps([293, 1]),
              import.meta.url,
            ),
        },
        {
          id: `vb`,
          name: `Visual Basic`,
          aliases: [`cmd`],
          import: () =>
            u(
              () => import(`./vb-Dbm737n_.js`),
              __vite__mapDeps([294, 1]),
              import.meta.url,
            ),
        },
        {
          id: `verilog`,
          name: `Verilog`,
          import: () =>
            u(
              () => import(`./verilog-C0XdXfNK.js`),
              __vite__mapDeps([295, 1]),
              import.meta.url,
            ),
        },
        {
          id: `vhdl`,
          name: `VHDL`,
          import: () =>
            u(
              () => import(`./vhdl-53H-MRF4.js`),
              __vite__mapDeps([296, 1]),
              import.meta.url,
            ),
        },
        {
          id: `viml`,
          name: `Vim Script`,
          aliases: [`vim`, `vimscript`],
          import: () =>
            u(
              () => import(`./viml-Duu1KGxi.js`),
              __vite__mapDeps([297, 1]),
              import.meta.url,
            ),
        },
        {
          id: `vue`,
          name: `Vue`,
          import: () =>
            u(
              () => import(`./vue-CBIoSZEr.js`),
              __vite__mapDeps([298, 299, 1, 50, 51, 49, 59, 65, 76]),
              import.meta.url,
            ),
        },
        {
          id: `vue-html`,
          name: `Vue HTML`,
          import: () =>
            u(
              () => import(`./vue-html-3rupQSQT.js`),
              __vite__mapDeps([300, 1, 50, 299, 51, 49, 59, 65, 76]),
              import.meta.url,
            ),
        },
        {
          id: `vue-vine`,
          name: `Vue Vine`,
          import: () =>
            u(
              () => import(`./vue-vine-CvDrzVSo.js`),
              __vite__mapDeps([
                301, 1, 50, 51, 53, 66, 193, 272, 299, 49, 59, 65, 76,
              ]),
              import.meta.url,
            ),
        },
        {
          id: `vyper`,
          name: `Vyper`,
          aliases: [`vy`],
          import: () =>
            u(
              () => import(`./vyper-DOfDsHjD.js`),
              __vite__mapDeps([302, 1]),
              import.meta.url,
            ),
        },
        {
          id: `wasm`,
          name: `WebAssembly`,
          import: () =>
            u(
              () => import(`./wasm-B5C64WKa.js`),
              __vite__mapDeps([303, 1]),
              import.meta.url,
            ),
        },
        {
          id: `wenyan`,
          name: `Wenyan`,
          aliases: [`文言`],
          import: () =>
            u(
              () => import(`./wenyan-ClaHfQiT.js`),
              __vite__mapDeps([304, 1]),
              import.meta.url,
            ),
        },
        {
          id: `wgsl`,
          name: `WGSL`,
          import: () =>
            u(
              () => import(`./wgsl-Bxe-ns-a.js`),
              __vite__mapDeps([305, 1]),
              import.meta.url,
            ),
        },
        {
          id: `wikitext`,
          name: `Wikitext`,
          aliases: [`mediawiki`, `wiki`],
          import: () =>
            u(
              () => import(`./wikitext-CLwu6_Yj.js`),
              __vite__mapDeps([306, 1]),
              import.meta.url,
            ),
        },
        {
          id: `wit`,
          name: `WebAssembly Interface Types`,
          import: () =>
            u(
              () => import(`./wit-DIUDPGBY.js`),
              __vite__mapDeps([307, 1]),
              import.meta.url,
            ),
        },
        {
          id: `wolfram`,
          name: `Wolfram`,
          aliases: [`wl`],
          import: () =>
            u(
              () => import(`./wolfram-CZyRGOns.js`),
              __vite__mapDeps([308, 1]),
              import.meta.url,
            ),
        },
        {
          id: `xml`,
          name: `XML`,
          import: () =>
            u(
              () => import(`./xml-BpVTI6cz.js`),
              __vite__mapDeps([309, 57, 1, 58]),
              import.meta.url,
            ),
        },
        {
          id: `xsl`,
          name: `XSL`,
          import: () =>
            u(
              () => import(`./xsl-Ddphdz43.js`),
              __vite__mapDeps([310, 1, 57, 58]),
              import.meta.url,
            ),
        },
        {
          id: `yaml`,
          name: `YAML`,
          aliases: [`yml`],
          import: () =>
            u(
              () => import(`./yaml-T0Eno5aE.js`),
              __vite__mapDeps([311, 127, 1]),
              import.meta.url,
            ),
        },
        {
          id: `zenscript`,
          name: `ZenScript`,
          import: () =>
            u(
              () => import(`./zenscript-C6hv8CXE.js`),
              __vite__mapDeps([312, 1]),
              import.meta.url,
            ),
        },
        {
          id: `zig`,
          name: `Zig`,
          import: () =>
            u(
              () => import(`./zig-CAEu7uPI.js`),
              __vite__mapDeps([313, 1]),
              import.meta.url,
            ),
        },
      ]),
      (Mc = Object.fromEntries(jc.map((e) => [e.id, e.import]))),
      (Nc = Object.fromEntries(
        jc.flatMap((e) => e.aliases?.map((t) => [t, e.import]) || []),
      )),
      (Pc = { ...Mc, ...Nc }));
  }),
  Ic,
  Lc,
  Rc = e(() => {
    (d(),
      (Ic = [
        {
          id: `andromeeda`,
          displayName: `Andromeeda`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./andromeeda-Cc0sG9OA.js`),
              __vite__mapDeps([314, 1]),
              import.meta.url,
            ),
        },
        {
          id: `aurora-x`,
          displayName: `Aurora X`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./aurora-x-DUM8kaRP.js`),
              __vite__mapDeps([315, 1]),
              import.meta.url,
            ),
        },
        {
          id: `ayu-dark`,
          displayName: `Ayu Dark`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./ayu-dark-DDRREaEs.js`),
              __vite__mapDeps([316, 1]),
              import.meta.url,
            ),
        },
        {
          id: `catppuccin-frappe`,
          displayName: `Catppuccin Frappé`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./catppuccin-frappe-DZfuiHN5.js`),
              __vite__mapDeps([317, 1]),
              import.meta.url,
            ),
        },
        {
          id: `catppuccin-latte`,
          displayName: `Catppuccin Latte`,
          type: `light`,
          import: () =>
            u(
              () => import(`./catppuccin-latte-CffoFhl6.js`),
              __vite__mapDeps([318, 1]),
              import.meta.url,
            ),
        },
        {
          id: `catppuccin-macchiato`,
          displayName: `Catppuccin Macchiato`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./catppuccin-macchiato-C9UmGfOh.js`),
              __vite__mapDeps([319, 1]),
              import.meta.url,
            ),
        },
        {
          id: `catppuccin-mocha`,
          displayName: `Catppuccin Mocha`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./catppuccin-mocha-BbX4ZWN8.js`),
              __vite__mapDeps([320, 1]),
              import.meta.url,
            ),
        },
        {
          id: `dark-plus`,
          displayName: `Dark Plus`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./dark-plus-DqYPLMqm.js`),
              __vite__mapDeps([321, 1]),
              import.meta.url,
            ),
        },
        {
          id: `dracula`,
          displayName: `Dracula Theme`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./dracula-Dli1Qj-E.js`),
              __vite__mapDeps([322, 1]),
              import.meta.url,
            ),
        },
        {
          id: `dracula-soft`,
          displayName: `Dracula Theme Soft`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./dracula-soft-CJCKwMWA.js`),
              __vite__mapDeps([323, 1]),
              import.meta.url,
            ),
        },
        {
          id: `everforest-dark`,
          displayName: `Everforest Dark`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./everforest-dark-C3_GWKG6.js`),
              __vite__mapDeps([324, 1]),
              import.meta.url,
            ),
        },
        {
          id: `everforest-light`,
          displayName: `Everforest Light`,
          type: `light`,
          import: () =>
            u(
              () => import(`./everforest-light-DEr4yUvA.js`),
              __vite__mapDeps([325, 1]),
              import.meta.url,
            ),
        },
        {
          id: `github-dark`,
          displayName: `GitHub Dark`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./github-dark-jYxIfJAK.js`),
              __vite__mapDeps([326, 1]),
              import.meta.url,
            ),
        },
        {
          id: `github-dark-default`,
          displayName: `GitHub Dark Default`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./github-dark-default-CWz2Ao5C.js`),
              __vite__mapDeps([327, 1]),
              import.meta.url,
            ),
        },
        {
          id: `github-dark-dimmed`,
          displayName: `GitHub Dark Dimmed`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./github-dark-dimmed-BYVv3soK.js`),
              __vite__mapDeps([328, 1]),
              import.meta.url,
            ),
        },
        {
          id: `github-dark-high-contrast`,
          displayName: `GitHub Dark High Contrast`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./github-dark-high-contrast-CFUFE7p6.js`),
              __vite__mapDeps([329, 1]),
              import.meta.url,
            ),
        },
        {
          id: `github-light`,
          displayName: `GitHub Light`,
          type: `light`,
          import: () =>
            u(
              () => import(`./github-light-BHPhwNco.js`),
              __vite__mapDeps([330, 1]),
              import.meta.url,
            ),
        },
        {
          id: `github-light-default`,
          displayName: `GitHub Light Default`,
          type: `light`,
          import: () =>
            u(
              () => import(`./github-light-default-C11W-_SM.js`),
              __vite__mapDeps([331, 1]),
              import.meta.url,
            ),
        },
        {
          id: `github-light-high-contrast`,
          displayName: `GitHub Light High Contrast`,
          type: `light`,
          import: () =>
            u(
              () => import(`./github-light-high-contrast-D1nUAVED.js`),
              __vite__mapDeps([332, 1]),
              import.meta.url,
            ),
        },
        {
          id: `gruvbox-dark-hard`,
          displayName: `Gruvbox Dark Hard`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./gruvbox-dark-hard-gf8b3rIv.js`),
              __vite__mapDeps([333, 1]),
              import.meta.url,
            ),
        },
        {
          id: `gruvbox-dark-medium`,
          displayName: `Gruvbox Dark Medium`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./gruvbox-dark-medium-CAOl-TJK.js`),
              __vite__mapDeps([334, 1]),
              import.meta.url,
            ),
        },
        {
          id: `gruvbox-dark-soft`,
          displayName: `Gruvbox Dark Soft`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./gruvbox-dark-soft-D1_RRDnf.js`),
              __vite__mapDeps([335, 1]),
              import.meta.url,
            ),
        },
        {
          id: `gruvbox-light-hard`,
          displayName: `Gruvbox Light Hard`,
          type: `light`,
          import: () =>
            u(
              () => import(`./gruvbox-light-hard-_Jf3GyJv.js`),
              __vite__mapDeps([336, 1]),
              import.meta.url,
            ),
        },
        {
          id: `gruvbox-light-medium`,
          displayName: `Gruvbox Light Medium`,
          type: `light`,
          import: () =>
            u(
              () => import(`./gruvbox-light-medium-BL2GKYVe.js`),
              __vite__mapDeps([337, 1]),
              import.meta.url,
            ),
        },
        {
          id: `gruvbox-light-soft`,
          displayName: `Gruvbox Light Soft`,
          type: `light`,
          import: () =>
            u(
              () => import(`./gruvbox-light-soft-4hjdjbPW.js`),
              __vite__mapDeps([338, 1]),
              import.meta.url,
            ),
        },
        {
          id: `houston`,
          displayName: `Houston`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./houston-DNjEfTXn.js`),
              __vite__mapDeps([339, 1]),
              import.meta.url,
            ),
        },
        {
          id: `kanagawa-dragon`,
          displayName: `Kanagawa Dragon`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./kanagawa-dragon-Xy--2nev.js`),
              __vite__mapDeps([340, 1]),
              import.meta.url,
            ),
        },
        {
          id: `kanagawa-lotus`,
          displayName: `Kanagawa Lotus`,
          type: `light`,
          import: () =>
            u(
              () => import(`./kanagawa-lotus-BqZTu85J.js`),
              __vite__mapDeps([341, 1]),
              import.meta.url,
            ),
        },
        {
          id: `kanagawa-wave`,
          displayName: `Kanagawa Wave`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./kanagawa-wave-D8AtmMaZ.js`),
              __vite__mapDeps([342, 1]),
              import.meta.url,
            ),
        },
        {
          id: `laserwave`,
          displayName: `LaserWave`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./laserwave-CEBVhjHt.js`),
              __vite__mapDeps([343, 1]),
              import.meta.url,
            ),
        },
        {
          id: `light-plus`,
          displayName: `Light Plus`,
          type: `light`,
          import: () =>
            u(
              () => import(`./light-plus-BBLgA5ch.js`),
              __vite__mapDeps([344, 1]),
              import.meta.url,
            ),
        },
        {
          id: `material-theme`,
          displayName: `Material Theme`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./material-theme-B4fAqikf.js`),
              __vite__mapDeps([345, 1]),
              import.meta.url,
            ),
        },
        {
          id: `material-theme-darker`,
          displayName: `Material Theme Darker`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./material-theme-darker-ClTJk-QW.js`),
              __vite__mapDeps([346, 1]),
              import.meta.url,
            ),
        },
        {
          id: `material-theme-lighter`,
          displayName: `Material Theme Lighter`,
          type: `light`,
          import: () =>
            u(
              () => import(`./material-theme-lighter-DYH9t9Wb.js`),
              __vite__mapDeps([347, 1]),
              import.meta.url,
            ),
        },
        {
          id: `material-theme-ocean`,
          displayName: `Material Theme Ocean`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./material-theme-ocean-Dq627U5V.js`),
              __vite__mapDeps([348, 1]),
              import.meta.url,
            ),
        },
        {
          id: `material-theme-palenight`,
          displayName: `Material Theme Palenight`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./material-theme-palenight-BP4N-rZS.js`),
              __vite__mapDeps([349, 1]),
              import.meta.url,
            ),
        },
        {
          id: `min-dark`,
          displayName: `Min Dark`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./min-dark-NsHtCGvn.js`),
              __vite__mapDeps([350, 1]),
              import.meta.url,
            ),
        },
        {
          id: `min-light`,
          displayName: `Min Light`,
          type: `light`,
          import: () =>
            u(
              () => import(`./min-light-2O6cMpAZ.js`),
              __vite__mapDeps([351, 1]),
              import.meta.url,
            ),
        },
        {
          id: `monokai`,
          displayName: `Monokai`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./monokai-CKPe4T2y.js`),
              __vite__mapDeps([352, 1]),
              import.meta.url,
            ),
        },
        {
          id: `night-owl`,
          displayName: `Night Owl`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./night-owl-BeiaVnS1.js`),
              __vite__mapDeps([353, 1]),
              import.meta.url,
            ),
        },
        {
          id: `nord`,
          displayName: `Nord`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./nord-BwfDtjt_.js`),
              __vite__mapDeps([354, 1]),
              import.meta.url,
            ),
        },
        {
          id: `one-dark-pro`,
          displayName: `One Dark Pro`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./one-dark-pro-DUxcR8F2.js`),
              __vite__mapDeps([355, 1]),
              import.meta.url,
            ),
        },
        {
          id: `one-light`,
          displayName: `One Light`,
          type: `light`,
          import: () =>
            u(
              () => import(`./one-light-BAzbWKLN.js`),
              __vite__mapDeps([356, 1]),
              import.meta.url,
            ),
        },
        {
          id: `plastic`,
          displayName: `Plastic`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./plastic-Bmm031RA.js`),
              __vite__mapDeps([357, 1]),
              import.meta.url,
            ),
        },
        {
          id: `poimandres`,
          displayName: `Poimandres`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./poimandres-CjJE8fFa.js`),
              __vite__mapDeps([358, 1]),
              import.meta.url,
            ),
        },
        {
          id: `red`,
          displayName: `Red`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./red-JZJ4ESLC.js`),
              __vite__mapDeps([359, 1]),
              import.meta.url,
            ),
        },
        {
          id: `rose-pine`,
          displayName: `Rosé Pine`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./rose-pine-s8Ev0U3t.js`),
              __vite__mapDeps([360, 1]),
              import.meta.url,
            ),
        },
        {
          id: `rose-pine-dawn`,
          displayName: `Rosé Pine Dawn`,
          type: `light`,
          import: () =>
            u(
              () => import(`./rose-pine-dawn-CB-gBMzR.js`),
              __vite__mapDeps([361, 1]),
              import.meta.url,
            ),
        },
        {
          id: `rose-pine-moon`,
          displayName: `Rosé Pine Moon`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./rose-pine-moon-CSDlPKbG.js`),
              __vite__mapDeps([362, 1]),
              import.meta.url,
            ),
        },
        {
          id: `slack-dark`,
          displayName: `Slack Dark`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./slack-dark-x1wBI9DE.js`),
              __vite__mapDeps([363, 1]),
              import.meta.url,
            ),
        },
        {
          id: `slack-ochin`,
          displayName: `Slack Ochin`,
          type: `light`,
          import: () =>
            u(
              () => import(`./slack-ochin-BeFBHAe0.js`),
              __vite__mapDeps([364, 1]),
              import.meta.url,
            ),
        },
        {
          id: `snazzy-light`,
          displayName: `Snazzy Light`,
          type: `light`,
          import: () =>
            u(
              () => import(`./snazzy-light-B9GZiL8C.js`),
              __vite__mapDeps([365, 1]),
              import.meta.url,
            ),
        },
        {
          id: `solarized-dark`,
          displayName: `Solarized Dark`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./solarized-dark-BlXYcUpI.js`),
              __vite__mapDeps([366, 1]),
              import.meta.url,
            ),
        },
        {
          id: `solarized-light`,
          displayName: `Solarized Light`,
          type: `light`,
          import: () =>
            u(
              () => import(`./solarized-light-CKahZpeh.js`),
              __vite__mapDeps([367, 1]),
              import.meta.url,
            ),
        },
        {
          id: `synthwave-84`,
          displayName: `Synthwave '84`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./synthwave-84-D33cPQ9Z.js`),
              __vite__mapDeps([368, 1]),
              import.meta.url,
            ),
        },
        {
          id: `tokyo-night`,
          displayName: `Tokyo Night`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./tokyo-night-DxpII2Zf.js`),
              __vite__mapDeps([369, 1]),
              import.meta.url,
            ),
        },
        {
          id: `vesper`,
          displayName: `Vesper`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./vesper-SMH2DGX_.js`),
              __vite__mapDeps([370, 1]),
              import.meta.url,
            ),
        },
        {
          id: `vitesse-black`,
          displayName: `Vitesse Black`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./vitesse-black-Ce7yGFhR.js`),
              __vite__mapDeps([371, 1]),
              import.meta.url,
            ),
        },
        {
          id: `vitesse-dark`,
          displayName: `Vitesse Dark`,
          type: `dark`,
          import: () =>
            u(
              () => import(`./vitesse-dark-DSf3AXh4.js`),
              __vite__mapDeps([372, 1]),
              import.meta.url,
            ),
        },
        {
          id: `vitesse-light`,
          displayName: `Vitesse Light`,
          type: `light`,
          import: () =>
            u(
              () => import(`./vitesse-light-nf2LzGA0.js`),
              __vite__mapDeps([373, 1]),
              import.meta.url,
            ),
        },
      ]),
      (Lc = Object.fromEntries(Ic.map((e) => [e.id, e.import]))));
  });
function zc() {
  return 2147483648;
}
function Bc() {
  return typeof performance < `u` ? performance.now() : Date.now();
}
async function Vc(e) {
  let t,
    n,
    r = {};
  function i(e) {
    ((n = e), (r.HEAPU8 = new Uint8Array(e)), (r.HEAPU32 = new Uint32Array(e)));
  }
  function a(e, t, n) {
    r.HEAPU8.copyWithin(e, t, t + n);
  }
  function o(e) {
    try {
      return (t.grow((e - n.byteLength + 65535) >>> 16), i(t.buffer), 1);
    } catch {}
  }
  function s(e) {
    let t = r.HEAPU8.length;
    e >>>= 0;
    let n = zc();
    if (e > n) return !1;
    for (let r = 1; r <= 4; r *= 2) {
      let i = t * (1 + 0.2 / r);
      if (
        ((i = Math.min(i, e + 100663296)),
        o(Math.min(n, el(Math.max(e, i), 65536))))
      )
        return !0;
    }
    return !1;
  }
  let c = typeof TextDecoder < `u` ? new TextDecoder(`utf8`) : void 0;
  function l(e, t, n = 1024) {
    let r = t + n,
      i = t;
    for (; e[i] && !(i >= r);) ++i;
    if (i - t > 16 && e.buffer && c) return c.decode(e.subarray(t, i));
    let a = ``;
    for (; t < i;) {
      let n = e[t++];
      if (!(n & 128)) {
        a += String.fromCharCode(n);
        continue;
      }
      let r = e[t++] & 63;
      if ((n & 224) == 192) {
        a += String.fromCharCode(((n & 31) << 6) | r);
        continue;
      }
      let i = e[t++] & 63;
      if (
        ((n =
          (n & 240) == 224
            ? ((n & 15) << 12) | (r << 6) | i
            : ((n & 7) << 18) | (r << 12) | (i << 6) | (e[t++] & 63)),
        n < 65536)
      )
        a += String.fromCharCode(n);
      else {
        let e = n - 65536;
        a += String.fromCharCode(55296 | (e >> 10), 56320 | (e & 1023));
      }
    }
    return a;
  }
  function u(e, t) {
    return e ? l(r.HEAPU8, e, t) : ``;
  }
  let d = {
    emscripten_get_now: Bc,
    emscripten_memcpy_big: a,
    emscripten_resize_heap: s,
    fd_write: () => 0,
  };
  async function f() {
    let n = await e({ env: d, wasi_snapshot_preview1: d });
    ((t = n.memory), i(t.buffer), Object.assign(r, n), (r.UTF8ToString = u));
  }
  return (await f(), r);
}
function Hc(e) {
  throw new $c(e.UTF8ToString(e.getLastOnigError()));
}
function Uc(e) {
  return typeof e.instantiator == `function`;
}
function Wc(e) {
  return typeof e.default == `function`;
}
function Gc(e) {
  return e.data !== void 0;
}
function Kc(e) {
  return typeof Response < `u` && e instanceof Response;
}
function qc(e) {
  return (
    (typeof ArrayBuffer < `u` &&
      (e instanceof ArrayBuffer || ArrayBuffer.isView(e))) ||
    (typeof Buffer < `u` && Buffer.isBuffer?.(e)) ||
    (typeof SharedArrayBuffer < `u` && e instanceof SharedArrayBuffer) ||
    (typeof Uint32Array < `u` && e instanceof Uint32Array)
  );
}
function Jc(e) {
  if (sl) return sl;
  async function t() {
    J = await Vc(async (t) => {
      let n = e;
      return (
        (n = await n),
        typeof n == `function` && (n = await n(t)),
        typeof n == `function` && (n = await n(t)),
        Uc(n)
          ? (n = await n.instantiator(t))
          : Wc(n)
            ? (n = await n.default(t))
            : (Gc(n) && (n = n.data),
              Kc(n)
                ? (n =
                    typeof WebAssembly.instantiateStreaming == `function`
                      ? await Xc(n)(t)
                      : await Zc(n)(t))
                : qc(n) || n instanceof WebAssembly.Module
                  ? (n = await Yc(n)(t))
                  : `default` in n &&
                    n.default instanceof WebAssembly.Module &&
                    (n = await Yc(n.default)(t))),
        `instance` in n && (n = n.instance),
        `exports` in n && (n = n.exports),
        n
      );
    });
  }
  return ((sl = t()), sl);
}
function Yc(e) {
  return (t) => WebAssembly.instantiate(e, t);
}
function Xc(e) {
  return (t) => WebAssembly.instantiateStreaming(e, t);
}
function Zc(e) {
  return async (t) => {
    let n = await e.arrayBuffer();
    return WebAssembly.instantiate(n, t);
  };
}
async function Qc(e) {
  return (
    e && (await Jc(e)),
    {
      createScanner(e) {
        return new ol(e.map((e) => (typeof e == `string` ? e : e.source)));
      },
      createString(e) {
        return new al(e);
      },
    }
  );
}
var $c,
  el,
  tl,
  nl,
  q,
  J,
  rl,
  il,
  al,
  ol,
  sl,
  cl = e(() => {
    (($c = class extends Error {
      constructor(e) {
        (super(e), (this.name = `ShikiError`));
      }
    }),
      (el = (e, t) => e + ((t - (e % t)) % t)),
      (tl = Object.defineProperty),
      (nl = (e, t, n) =>
        t in e
          ? tl(e, t, {
              enumerable: !0,
              configurable: !0,
              writable: !0,
              value: n,
            })
          : (e[t] = n)),
      (q = (e, t, n) => nl(e, typeof t == `symbol` ? t : t + ``, n)),
      (J = null),
      (rl = class e {
        constructor(t) {
          (q(this, `utf16Length`),
            q(this, `utf8Length`),
            q(this, `utf16Value`),
            q(this, `utf8Value`),
            q(this, `utf16OffsetToUtf8`),
            q(this, `utf8OffsetToUtf16`));
          let n = t.length,
            r = e._utf8ByteLength(t),
            i = r !== n,
            a = i ? new Uint32Array(n + 1) : null;
          i && (a[n] = r);
          let o = i ? new Uint32Array(r + 1) : null;
          i && (o[r] = n);
          let s = new Uint8Array(r),
            c = 0;
          for (let e = 0; e < n; e++) {
            let r = t.charCodeAt(e),
              l = r,
              u = !1;
            if (r >= 55296 && r <= 56319 && e + 1 < n) {
              let n = t.charCodeAt(e + 1);
              n >= 56320 &&
                n <= 57343 &&
                ((l = (((r - 55296) << 10) + 65536) | (n - 56320)), (u = !0));
            }
            (i &&
              ((a[e] = c),
              u && (a[e + 1] = c),
              l <= 127
                ? (o[c + 0] = e)
                : l <= 2047
                  ? ((o[c + 0] = e), (o[c + 1] = e))
                  : l <= 65535
                    ? ((o[c + 0] = e), (o[c + 1] = e), (o[c + 2] = e))
                    : ((o[c + 0] = e),
                      (o[c + 1] = e),
                      (o[c + 2] = e),
                      (o[c + 3] = e))),
              l <= 127
                ? (s[c++] = l)
                : l <= 2047
                  ? ((s[c++] = 192 | ((l & 1984) >>> 6)),
                    (s[c++] = 128 | ((l & 63) >>> 0)))
                  : l <= 65535
                    ? ((s[c++] = 224 | ((l & 61440) >>> 12)),
                      (s[c++] = 128 | ((l & 4032) >>> 6)),
                      (s[c++] = 128 | ((l & 63) >>> 0)))
                    : ((s[c++] = 240 | ((l & 1835008) >>> 18)),
                      (s[c++] = 128 | ((l & 258048) >>> 12)),
                      (s[c++] = 128 | ((l & 4032) >>> 6)),
                      (s[c++] = 128 | ((l & 63) >>> 0))),
              u && e++);
          }
          ((this.utf16Length = n),
            (this.utf8Length = r),
            (this.utf16Value = t),
            (this.utf8Value = s),
            (this.utf16OffsetToUtf8 = a),
            (this.utf8OffsetToUtf16 = o));
        }
        static _utf8ByteLength(e) {
          let t = 0;
          for (let n = 0, r = e.length; n < r; n++) {
            let i = e.charCodeAt(n),
              a = i,
              o = !1;
            if (i >= 55296 && i <= 56319 && n + 1 < r) {
              let t = e.charCodeAt(n + 1);
              t >= 56320 &&
                t <= 57343 &&
                ((a = (((i - 55296) << 10) + 65536) | (t - 56320)), (o = !0));
            }
            (a <= 127
              ? (t += 1)
              : a <= 2047
                ? (t += 2)
                : a <= 65535
                  ? (t += 3)
                  : (t += 4),
              o && n++);
          }
          return t;
        }
        createString(e) {
          let t = e.omalloc(this.utf8Length);
          return (e.HEAPU8.set(this.utf8Value, t), t);
        }
      }),
      (il = class e {
        constructor(t) {
          if (
            (q(this, `id`, ++e.LAST_ID),
            q(this, `_onigBinding`),
            q(this, `content`),
            q(this, `utf16Length`),
            q(this, `utf8Length`),
            q(this, `utf16OffsetToUtf8`),
            q(this, `utf8OffsetToUtf16`),
            q(this, `ptr`),
            !J)
          )
            throw new $c(`Must invoke loadWasm first.`);
          ((this._onigBinding = J), (this.content = t));
          let n = new rl(t);
          ((this.utf16Length = n.utf16Length),
            (this.utf8Length = n.utf8Length),
            (this.utf16OffsetToUtf8 = n.utf16OffsetToUtf8),
            (this.utf8OffsetToUtf16 = n.utf8OffsetToUtf16),
            this.utf8Length < 1e4 && !e._sharedPtrInUse
              ? ((e._sharedPtr ||= J.omalloc(1e4)),
                (e._sharedPtrInUse = !0),
                J.HEAPU8.set(n.utf8Value, e._sharedPtr),
                (this.ptr = e._sharedPtr))
              : (this.ptr = n.createString(J)));
        }
        convertUtf8OffsetToUtf16(e) {
          return this.utf8OffsetToUtf16
            ? e < 0
              ? 0
              : e > this.utf8Length
                ? this.utf16Length
                : this.utf8OffsetToUtf16[e]
            : e;
        }
        convertUtf16OffsetToUtf8(e) {
          return this.utf16OffsetToUtf8
            ? e < 0
              ? 0
              : e > this.utf16Length
                ? this.utf8Length
                : this.utf16OffsetToUtf8[e]
            : e;
        }
        dispose() {
          this.ptr === e._sharedPtr
            ? (e._sharedPtrInUse = !1)
            : this._onigBinding.ofree(this.ptr);
        }
      }),
      q(il, `LAST_ID`, 0),
      q(il, `_sharedPtr`, 0),
      q(il, `_sharedPtrInUse`, !1),
      (al = il),
      (ol = class {
        constructor(e) {
          if ((q(this, `_onigBinding`), q(this, `_ptr`), !J))
            throw new $c(`Must invoke loadWasm first.`);
          let t = [],
            n = [];
          for (let r = 0, i = e.length; r < i; r++) {
            let i = new rl(e[r]);
            ((t[r] = i.createString(J)), (n[r] = i.utf8Length));
          }
          let r = J.omalloc(4 * e.length);
          J.HEAPU32.set(t, r / 4);
          let i = J.omalloc(4 * e.length);
          J.HEAPU32.set(n, i / 4);
          let a = J.createOnigScanner(r, i, e.length);
          for (let n = 0, r = e.length; n < r; n++) J.ofree(t[n]);
          (J.ofree(i),
            J.ofree(r),
            a === 0 && Hc(J),
            (this._onigBinding = J),
            (this._ptr = a));
        }
        dispose() {
          this._onigBinding.freeOnigScanner(this._ptr);
        }
        findNextMatchSync(e, t, n) {
          let r = 0;
          if ((typeof n == `number` && (r = n), typeof e == `string`)) {
            e = new al(e);
            let n = this._findNextMatchSync(e, t, !1, r);
            return (e.dispose(), n);
          }
          return this._findNextMatchSync(e, t, !1, r);
        }
        _findNextMatchSync(e, t, n, r) {
          let i = this._onigBinding,
            a = i.findNextOnigScannerMatch(
              this._ptr,
              e.id,
              e.ptr,
              e.utf8Length,
              e.convertUtf16OffsetToUtf8(t),
              r,
            );
          if (a === 0) return null;
          let o = i.HEAPU32,
            s = a / 4,
            c = o[s++],
            l = o[s++],
            u = [];
          for (let t = 0; t < l; t++) {
            let n = e.convertUtf8OffsetToUtf16(o[s++]),
              r = e.convertUtf8OffsetToUtf16(o[s++]);
            u[t] = { start: n, end: r, length: r - n };
          }
          return { index: c, captureIndices: u };
        }
      }));
  }),
  ll,
  ul,
  dl,
  fl,
  pl,
  ml,
  hl,
  gl,
  _l = e(() => {
    (Ac(),
      Ac(),
      Fc(),
      Rc(),
      cl(),
      d(),
      (ll = cc({
        langs: Pc,
        themes: Lc,
        engine: () =>
          Qc(
            u(
              () => import(`./wasm-CAhMFZE6.js`),
              __vite__mapDeps([374, 1]),
              import.meta.url,
            ),
          ),
      })),
      ({
        codeToHtml: ul,
        codeToHast: dl,
        codeToTokens: fl,
        codeToTokensBase: pl,
        codeToTokensWithThemes: ml,
        getSingletonHighlighter: hl,
        getLastGrammarState: gl,
      } = uc(ll, { guessEmbeddedLanguages: ps })));
  });
function vl(e) {
  if ([...e].length !== 1)
    throw Error(`Expected "${e}" to be a single code point`);
  return e.codePointAt(0);
}
function yl(e, t, n) {
  return (e.has(t) || e.set(t, n), e.get(t));
}
function bl(e, t) {
  if (e == null) throw Error(t ?? `Value expected`);
  return e;
}
var xl,
  Y,
  Sl = e(() => {
    ((xl = new Set([
      `alnum`,
      `alpha`,
      `ascii`,
      `blank`,
      `cntrl`,
      `digit`,
      `graph`,
      `lower`,
      `print`,
      `punct`,
      `space`,
      `upper`,
      `word`,
      `xdigit`,
    ])),
      (Y = String.raw));
  });
function Cl(e, t = {}) {
  let n = {
    flags: ``,
    ...t,
    rules: { captureGroup: !1, singleline: !1, ...t.rules },
  };
  if (typeof e != `string`) throw Error(`String expected as pattern`);
  let r = Zl(n.flags),
    i = [r.extended],
    a = {
      captureGroup: n.rules.captureGroup,
      getCurrentModX() {
        return i.at(-1);
      },
      numOpenGroups: 0,
      popModX() {
        i.pop();
      },
      pushModX(e) {
        i.push(e);
      },
      replaceCurrentModX(e) {
        i[i.length - 1] = e;
      },
      singleline: n.rules.singleline,
    },
    o = [],
    s;
  for (iu.lastIndex = 0; (s = iu.exec(e));) {
    let t = wl(a, e, s[0], iu.lastIndex);
    (t.tokens ? o.push(...t.tokens) : t.token && o.push(t.token),
      t.lastIndex !== void 0 && (iu.lastIndex = t.lastIndex));
  }
  let c = [],
    l = 0;
  (o
    .filter((e) => e.type === `GroupOpen`)
    .forEach((e) => {
      e.kind === `capturing` ? (e.number = ++l) : e.raw === `(` && c.push(e);
    }),
    l ||
      c.forEach((e, t) => {
        ((e.kind = `capturing`), (e.number = t + 1));
      }));
  let u = l || c.length;
  return {
    tokens: o.map((e) => (e.type === `EscapedNumber` ? $l(e, u) : e)).flat(),
    flags: r,
  };
}
function wl(e, t, n, r) {
  let [i, a] = n;
  if (n === `[` || n === `[^`) {
    let e = Tl(t, n, r);
    return { tokens: e.tokens, lastIndex: e.lastIndex };
  }
  if (i === `\\`) {
    if (`AbBGyYzZ`.includes(a)) return { token: kl(n, n) };
    if (/^\\g[<']/.test(n)) {
      if (!/^\\g(?:<[^>]+>|'[^']+')$/.test(n))
        throw Error(`Invalid group name "${n}"`);
      return { token: Ul(n) };
    }
    if (/^\\k[<']/.test(n)) {
      if (!/^\\k(?:<[^>]+>|'[^']+')$/.test(n))
        throw Error(`Invalid group name "${n}"`);
      return { token: Al(n) };
    }
    if (a === `K`) return { token: Ll(`keep`, n) };
    if (a === `N` || a === `R`)
      return { token: Il(`newline`, n, { negate: a === `N` }) };
    if (a === `O`) return { token: Il(`any`, n) };
    if (a === `X`) return { token: Il(`text_segment`, n) };
    let e = Dl(n, { inCharClass: !1 });
    return Array.isArray(e) ? { tokens: e } : { token: e };
  }
  if (i === `(`) {
    if (a === `*`) return { token: Kl(n) };
    if (n === `(?{`) throw Error(`Unsupported callout "${n}"`);
    if (n.startsWith(`(?#`)) {
      if (t[r] !== `)`) throw Error(`Unclosed comment group "(?#"`);
      return { lastIndex: r + 1 };
    }
    if (/^\(\?[-imx]+[:)]$/.test(n)) return { token: Gl(n, e) };
    if (
      (e.pushModX(e.getCurrentModX()),
      e.numOpenGroups++,
      (n === `(` && !e.captureGroup) || n === `(?:`)
    )
      return { token: Bl(`group`, n) };
    if (n === `(?>`) return { token: Bl(`atomic`, n) };
    if (n === `(?=` || n === `(?!` || n === `(?<=` || n === `(?<!`)
      return {
        token: Bl(n[2] === `<` ? `lookbehind` : `lookahead`, n, {
          negate: n.endsWith(`!`),
        }),
      };
    if (
      (n === `(` && e.captureGroup) ||
      (n.startsWith(`(?<`) && n.endsWith(`>`)) ||
      (n.startsWith(`(?'`) && n.endsWith(`'`))
    )
      return {
        token: Bl(`capturing`, n, {
          ...(n !== `(` && { name: n.slice(3, -1) }),
        }),
      };
    if (n.startsWith(`(?~`)) {
      if (n === `(?~|`) throw Error(`Unsupported absence function kind "${n}"`);
      return { token: Bl(`absence_repeater`, n) };
    }
    throw Error(
      n === `(?(`
        ? `Unsupported conditional "${n}"`
        : `Invalid or unsupported group option "${n}"`,
    );
  }
  if (n === `)`) {
    if ((e.popModX(), e.numOpenGroups--, e.numOpenGroups < 0))
      throw Error(`Unmatched ")"`);
    return { token: zl(n) };
  }
  if (e.getCurrentModX()) {
    if (n === `#`) {
      let e = t.indexOf(
        `
`,
        r,
      );
      return { lastIndex: e === -1 ? t.length : e };
    }
    if (/^\s$/.test(n)) {
      let e = /\s+/y;
      return ((e.lastIndex = r), { lastIndex: e.exec(t) ? e.lastIndex : r });
    }
  }
  return n === `.`
    ? { token: Il(`dot`, n) }
    : n === `^` || n === `$`
      ? { token: kl(e.singleline ? { "^": Y`\A`, $: Y`\Z` }[n] : n, n) }
      : n === `|`
        ? { token: Ol(n) }
        : ru.test(n)
          ? { tokens: eu(n) }
          : { token: jl(vl(n), n) };
}
function Tl(e, t, n) {
  let r = [Fl(t[1] === `^`, t)],
    i = 1,
    a;
  for (au.lastIndex = n; (a = au.exec(e));) {
    let e = a[0];
    if (e[0] === `[` && e[1] !== `:`) (i++, r.push(Fl(e[1] === `^`, e)));
    else if (e === `]`) {
      if (r.at(-1).type === `CharacterClassOpen`) r.push(jl(93, e));
      else if ((i--, r.push(Ml(e)), !i)) break;
    } else {
      let t = El(e);
      Array.isArray(t) ? r.push(...t) : r.push(t);
    }
  }
  return { tokens: r, lastIndex: au.lastIndex || e.length };
}
function El(e) {
  if (e[0] === `\\`) return Dl(e, { inCharClass: !0 });
  if (e[0] === `[`) {
    let t = /\[:(?<negate>\^?)(?<name>[a-z]+):\]/.exec(e);
    if (!t || !xl.has(t.groups.name)) throw Error(`Invalid POSIX class "${e}"`);
    return Il(`posix`, e, { value: t.groups.name, negate: !!t.groups.negate });
  }
  return e === `-` ? Nl(e) : e === `&&` ? Pl(e) : jl(vl(e), e);
}
function Dl(e, { inCharClass: t }) {
  let n = e[1];
  if (n === `c` || n === `C`) return Wl(e);
  if (`dDhHsSwW`.includes(n)) return Jl(e);
  if (e.startsWith(Y`\o{`))
    throw Error(`Incomplete, invalid, or unsupported octal code point "${e}"`);
  if (/^\\[pP]\{/.test(e)) {
    if (e.length === 3)
      throw Error(`Incomplete or invalid Unicode property "${e}"`);
    return Yl(e);
  }
  if (/^\\x[89A-Fa-f]\p{AHex}/u.test(e))
    try {
      let t = e
          .split(/\\x/)
          .slice(1)
          .map((e) => parseInt(e, 16)),
        n = new TextDecoder(`utf-8`, { ignoreBOM: !0, fatal: !0 }).decode(
          new Uint8Array(t),
        ),
        r = new TextEncoder();
      return [...n].map((e) => {
        let t = [...r.encode(e)].map((e) => `\\x${e.toString(16)}`).join(``);
        return jl(vl(e), t);
      });
    } catch {
      throw Error(`Multibyte code "${e}" incomplete or invalid in Oniguruma`);
    }
  if (n === `u` || n === `x`) return jl(Ql(e), e);
  if (su.has(n)) return jl(su.get(n), e);
  if (/\d/.test(n)) return Rl(t, e);
  if (e === `\\`) throw Error(Y`Incomplete escape "\"`);
  if (n === `M`) throw Error(`Unsupported meta "${e}"`);
  if ([...e].length === 2) return jl(e.codePointAt(1), e);
  throw Error(`Unexpected escape "${e}"`);
}
function Ol(e) {
  return { type: `Alternator`, raw: e };
}
function kl(e, t) {
  return { type: `Assertion`, kind: e, raw: t };
}
function Al(e) {
  return { type: `Backreference`, raw: e };
}
function jl(e, t) {
  return { type: `Character`, value: e, raw: t };
}
function Ml(e) {
  return { type: `CharacterClassClose`, raw: e };
}
function Nl(e) {
  return { type: `CharacterClassHyphen`, raw: e };
}
function Pl(e) {
  return { type: `CharacterClassIntersector`, raw: e };
}
function Fl(e, t) {
  return { type: `CharacterClassOpen`, negate: e, raw: t };
}
function Il(e, t, n = {}) {
  return { type: `CharacterSet`, kind: e, ...n, raw: t };
}
function Ll(e, t, n = {}) {
  return e === `keep`
    ? { type: `Directive`, kind: e, raw: t }
    : { type: `Directive`, kind: e, flags: bl(n.flags), raw: t };
}
function Rl(e, t) {
  return { type: `EscapedNumber`, inCharClass: e, raw: t };
}
function zl(e) {
  return { type: `GroupClose`, raw: e };
}
function Bl(e, t, n = {}) {
  return { type: `GroupOpen`, kind: e, ...n, raw: t };
}
function Vl(e, t, n, r) {
  return { type: `NamedCallout`, kind: e, tag: t, arguments: n, raw: r };
}
function Hl(e, t, n, r) {
  return { type: `Quantifier`, kind: e, min: t, max: n, raw: r };
}
function Ul(e) {
  return { type: `Subroutine`, raw: e };
}
function Wl(e) {
  let t = e[1] === `c` ? e[2] : e[3];
  if (!t || !/[A-Za-z]/.test(t))
    throw Error(`Unsupported control character "${e}"`);
  return jl(vl(t.toUpperCase()) - 64, e);
}
function Gl(e, t) {
  let { on: n, off: r } = /^\(\?(?<on>[imx]*)(?:-(?<off>[-imx]*))?/.exec(
    e,
  ).groups;
  r ??= ``;
  let i = (t.getCurrentModX() || n.includes(`x`)) && !r.includes(`x`),
    a = Xl(n),
    o = Xl(r),
    s = {};
  if ((a && (s.enable = a), o && (s.disable = o), e.endsWith(`)`)))
    return (t.replaceCurrentModX(i), Ll(`flags`, e, { flags: s }));
  if (e.endsWith(`:`))
    return (
      t.pushModX(i),
      t.numOpenGroups++,
      Bl(`group`, e, { ...((a || o) && { flags: s }) })
    );
  throw Error(`Unexpected flag modifier "${e}"`);
}
function Kl(e) {
  let t =
    /\(\*(?<name>[A-Za-z_]\w*)?(?:\[(?<tag>(?:[A-Za-z_]\w*)?)\])?(?:\{(?<args>[^}]*)\})?\)/.exec(
      e,
    );
  if (!t) throw Error(`Incomplete or invalid named callout "${e}"`);
  let { name: n, tag: r, args: i } = t.groups;
  if (!n) throw Error(`Invalid named callout "${e}"`);
  if (r === ``)
    throw Error(`Named callout tag with empty value not allowed "${e}"`);
  let a = i
      ? i
          .split(`,`)
          .filter((e) => e !== ``)
          .map((e) => (/^[+-]?\d+$/.test(e) ? +e : e))
      : [],
    [o, s, c] = a,
    l = ou.has(n) ? n.toLowerCase() : `custom`;
  switch (l) {
    case `fail`:
    case `mismatch`:
    case `skip`:
      if (a.length > 0)
        throw Error(`Named callout arguments not allowed "${a}"`);
      break;
    case `error`:
      if (a.length > 1)
        throw Error(`Named callout allows only one argument "${a}"`);
      if (typeof o == `string`)
        throw Error(`Named callout argument must be a number "${o}"`);
      break;
    case `max`:
      if (!a.length || a.length > 2)
        throw Error(`Named callout must have one or two arguments "${a}"`);
      if (typeof o == `string` && !/^[A-Za-z_]\w*$/.test(o))
        throw Error(
          `Named callout argument one must be a tag or number "${o}"`,
        );
      if (a.length === 2 && (typeof s == `number` || !/^[<>X]$/.test(s)))
        throw Error(
          `Named callout optional argument two must be '<', '>', or 'X' "${s}"`,
        );
      break;
    case `count`:
    case `total_count`:
      if (a.length > 1)
        throw Error(`Named callout allows only one argument "${a}"`);
      if (a.length === 1 && (typeof o == `number` || !/^[<>X]$/.test(o)))
        throw Error(
          `Named callout optional argument must be '<', '>', or 'X' "${o}"`,
        );
      break;
    case `cmp`:
      if (a.length !== 3)
        throw Error(`Named callout must have three arguments "${a}"`);
      if (typeof o == `string` && !/^[A-Za-z_]\w*$/.test(o))
        throw Error(
          `Named callout argument one must be a tag or number "${o}"`,
        );
      if (typeof s == `number` || !/^(?:[<>!=]=|[<>])$/.test(s))
        throw Error(
          `Named callout argument two must be '==', '!=', '>', '<', '>=', or '<=' "${s}"`,
        );
      if (typeof c == `string` && !/^[A-Za-z_]\w*$/.test(c))
        throw Error(
          `Named callout argument three must be a tag or number "${c}"`,
        );
      break;
    case `custom`:
      throw Error(`Undefined callout name "${n}"`);
    default:
      throw Error(`Unexpected named callout kind "${l}"`);
  }
  return Vl(l, r ?? null, i?.split(`,`) ?? null, e);
}
function ql(e) {
  let t = null,
    n,
    r;
  if (e[0] === `{`) {
    let { minStr: i, maxStr: a } = /^\{(?<minStr>\d*)(?:,(?<maxStr>\d*))?/.exec(
        e,
      ).groups,
      o = 1e5;
    if (+i > o || (a && +a > o))
      throw Error(`Quantifier value unsupported in Oniguruma`);
    if (
      ((n = +i),
      (r = a === void 0 ? +i : a === `` ? 1 / 0 : +a),
      n > r && ((t = `possessive`), ([n, r] = [r, n])),
      e.endsWith(`?`))
    ) {
      if (t === `possessive`)
        throw Error(
          `Unsupported possessive interval quantifier chain with "?"`,
        );
      t = `lazy`;
    } else t ||= `greedy`;
  } else
    ((n = +(e[0] === `+`)),
      (r = e[0] === `?` ? 1 : 1 / 0),
      (t = e[1] === `+` ? `possessive` : e[1] === `?` ? `lazy` : `greedy`));
  return Hl(t, n, r, e);
}
function Jl(e) {
  let t = e[1].toLowerCase();
  return Il({ d: `digit`, h: `hex`, s: `space`, w: `word` }[t], e, {
    negate: e[1] !== t,
  });
}
function Yl(e) {
  let {
    p: t,
    neg: n,
    value: r,
  } = /^\\(?<p>[pP])\{(?<neg>\^?)(?<value>[^}]+)/.exec(e).groups;
  return Il(`property`, e, {
    value: r,
    negate: (t === `P` && !n) || (t === `p` && !!n),
  });
}
function Xl(e) {
  let t = {};
  return (
    e.includes(`i`) && (t.ignoreCase = !0),
    e.includes(`m`) && (t.dotAll = !0),
    e.includes(`x`) && (t.extended = !0),
    Object.keys(t).length ? t : null
  );
}
function Zl(e) {
  let t = {
    ignoreCase: !1,
    dotAll: !1,
    extended: !1,
    digitIsAscii: !1,
    posixIsAscii: !1,
    spaceIsAscii: !1,
    wordIsAscii: !1,
    textSegmentMode: null,
  };
  for (let n = 0; n < e.length; n++) {
    let r = e[n];
    if (!`imxDPSWy`.includes(r)) throw Error(`Invalid flag "${r}"`);
    if (r === `y`) {
      if (!/^y{[gw]}/.test(e.slice(n)))
        throw Error(`Invalid or unspecified flag "y" mode`);
      ((t.textSegmentMode = e[n + 2] === `g` ? `grapheme` : `word`), (n += 3));
      continue;
    }
    t[
      {
        i: `ignoreCase`,
        m: `dotAll`,
        x: `extended`,
        D: `digitIsAscii`,
        P: `posixIsAscii`,
        S: `spaceIsAscii`,
        W: `wordIsAscii`,
      }[r]
    ] = !0;
  }
  return t;
}
function Ql(e) {
  if (/^(?:\\u(?!\p{AHex}{4})|\\x(?!\p{AHex}{1,2}|\{\p{AHex}{1,8}\}))/u.test(e))
    throw Error(`Incomplete or invalid escape "${e}"`);
  let t =
    e[2] === `{`
      ? /^\\x\{\s*(?<hex>\p{AHex}+)/u.exec(e).groups.hex
      : e.slice(2);
  return parseInt(t, 16);
}
function $l(e, t) {
  let { raw: n, inCharClass: r } = e,
    i = n.slice(1);
  if (!r && ((i !== `0` && i.length === 1) || (i[0] !== `0` && +i <= t)))
    return [Al(n)];
  let a = [],
    o = i.match(/^[0-7]+|\d/g);
  for (let e = 0; e < o.length; e++) {
    let t = o[e],
      r;
    if (e === 0 && t !== `8` && t !== `9`) {
      if (((r = parseInt(t, 8)), r > 127))
        throw Error(Y`Octal encoded byte above 177 unsupported "${n}"`);
    } else r = vl(t);
    a.push(jl(r, (e === 0 ? `\\` : ``) + t));
  }
  return a;
}
function eu(e) {
  let t = [],
    n = new RegExp(ru, `gy`),
    r;
  for (; (r = n.exec(e));) {
    let e = r[0];
    if (e[0] === `{`) {
      let r = /^\{(?<min>\d+),(?<max>\d+)\}\??$/.exec(e);
      if (r) {
        let { min: i, max: a } = r.groups;
        if (+i > +a && e.endsWith(`?`)) {
          (n.lastIndex--, t.push(ql(e.slice(0, -1))));
          continue;
        }
      }
    }
    t.push(ql(e));
  }
  return t;
}
var tu,
  nu,
  ru,
  iu,
  au,
  ou,
  su,
  cu = e(() => {
    (Sl(),
      (tu = Y`\[\^?`),
      (nu = `c.? | C(?:-.?)?|${Y`[pP]\{(?:\^?[-\x20_]*[A-Za-z][-\x20\w]*\})?`}|${Y`x[89A-Fa-f]\p{AHex}(?:\\x[89A-Fa-f]\p{AHex})*`}|${Y`u(?:\p{AHex}{4})? | x\{[^\}]*\}? | x\p{AHex}{0,2}`}|${Y`o\{[^\}]*\}?`}|${Y`\d{1,3}`}`),
      (ru = /[?*+][?+]?|\{(?:\d+(?:,\d*)?|,\d+)\}\??/),
      (iu = new RegExp(
        Y`
  \\ (?:
    ${nu}
    | [gk]<[^>]*>?
    | [gk]'[^']*'?
    | .
  )
  | \( (?:
    \? (?:
      [:=!>({]
      | <[=!]
      | <[^>]*>
      | '[^']*'
      | ~\|?
      | #(?:[^)\\]|\\.?)*
      | [^:)]*[:)]
    )?
    | \*[^\)]*\)?
  )?
  | (?:${ru.source})+
  | ${tu}
  | .
`.replace(/\s+/g, ``),
        `gsu`,
      )),
      (au = new RegExp(
        Y`
  \\ (?:
    ${nu}
    | .
  )
  | \[:(?:\^?\p{Alpha}+|\^):\]
  | ${tu}
  | &&
  | .
`.replace(/\s+/g, ``),
        `gsu`,
      )),
      (ou = new Set([
        `COUNT`,
        `CMP`,
        `ERROR`,
        `FAIL`,
        `MAX`,
        `MISMATCH`,
        `SKIP`,
        `TOTAL_COUNT`,
      ])),
      (su = new Map([
        [`a`, 7],
        [`b`, 8],
        [`e`, 27],
        [`f`, 12],
        [`n`, 10],
        [`r`, 13],
        [`t`, 9],
        [`v`, 11],
      ])));
  });
function lu(e, t) {
  if (!Array.isArray(e.body)) throw Error(`Expected node with body array`);
  if (e.body.length !== 1) return !1;
  let n = e.body[0];
  return !t || Object.keys(t).every((e) => t[e] === n[e]);
}
function uu(e) {
  return du.has(e.type);
}
var du,
  fu = e(() => {
    du = new Set([
      `AbsenceFunction`,
      `Backreference`,
      `CapturingGroup`,
      `Character`,
      `CharacterClass`,
      `CharacterSet`,
      `Group`,
      `Quantifier`,
      `Subroutine`,
    ]);
  });
function pu(e, t = {}) {
  let n = {
      flags: ``,
      normalizeUnknownPropertyNames: !1,
      skipBackrefValidation: !1,
      skipLookbehindValidation: !1,
      skipPropertyNameValidation: !1,
      unicodePropertyMap: null,
      ...t,
      rules: { captureGroup: !1, singleline: !1, ...t.rules },
    },
    r = Cl(e, {
      flags: n.flags,
      rules: {
        captureGroup: n.rules.captureGroup,
        singleline: n.rules.singleline,
      },
    }),
    i = (e, t) => {
      let n = r.tokens[a.nextIndex];
      switch (((a.parent = e), a.nextIndex++, n.type)) {
        case `Alternator`:
          return Cu();
        case `Assertion`:
          return mu(n);
        case `Backreference`:
          return hu(n, a);
        case `Character`:
          return Du(n.value, { useLastValid: !!t.isCheckingRangeEnd });
        case `CharacterClassHyphen`:
          return gu(n, a, t);
        case `CharacterClassOpen`:
          return _u(n, a, t);
        case `CharacterSet`:
          return vu(n, a);
        case `Directive`:
          return ju(n.kind, { flags: n.flags });
        case `GroupOpen`:
          return yu(n, a, t);
        case `NamedCallout`:
          return Fu(n.kind, n.tag, n.arguments);
        case `Quantifier`:
          return bu(n, a);
        case `Subroutine`:
          return xu(n, a);
        default:
          throw Error(`Unexpected token type "${n.type}"`);
      }
    },
    a = {
      capturingGroups: [],
      hasNumberedRef: !1,
      namedGroupsByName: new Map(),
      nextIndex: 0,
      normalizeUnknownPropertyNames: n.normalizeUnknownPropertyNames,
      parent: null,
      skipBackrefValidation: n.skipBackrefValidation,
      skipLookbehindValidation: n.skipLookbehindValidation,
      skipPropertyNameValidation: n.skipPropertyNameValidation,
      subroutines: [],
      tokens: r.tokens,
      unicodePropertyMap: n.unicodePropertyMap,
      walk: i,
    },
    o = Ru(Mu(r.flags)),
    s = o.body[0];
  for (; a.nextIndex < r.tokens.length;) {
    let e = i(s, {});
    e.type === `Alternative` ? (o.body.push(e), (s = e)) : s.body.push(e);
  }
  let {
    capturingGroups: c,
    hasNumberedRef: l,
    namedGroupsByName: u,
    subroutines: d,
  } = a;
  if (l && u.size && !n.rules.captureGroup)
    throw Error(
      `Numbered backref/subroutine not allowed when using named capture`,
    );
  for (let { ref: e } of d)
    if (typeof e == `number`) {
      if (e > c.length)
        throw Error(`Subroutine uses a group number that's not defined`);
      e && (c[e - 1].isSubroutined = !0);
    } else if (u.has(e)) {
      if (u.get(e).length > 1)
        throw Error(Y`Subroutine uses a duplicate group name "\g<${e}>"`);
      u.get(e)[0].isSubroutined = !0;
    } else
      throw Error(
        Y`Subroutine uses a group name that's not defined "\g<${e}>"`,
      );
  return o;
}
function mu({ kind: e }) {
  return wu(
    bl(
      {
        "^": `line_start`,
        $: `line_end`,
        "\\A": `string_start`,
        "\\b": `word_boundary`,
        "\\B": `word_boundary`,
        "\\G": `search_start`,
        "\\y": `text_segment_boundary`,
        "\\Y": `text_segment_boundary`,
        "\\z": `string_end`,
        "\\Z": `string_end_newline`,
      }[e],
      `Unexpected assertion kind "${e}"`,
    ),
    { negate: e === Y`\B` || e === Y`\Y` },
  );
}
function hu({ raw: e }, t) {
  let n = /^\\k[<']/.test(e),
    r = n ? e.slice(3, -1) : e.slice(1),
    i = (n, r = !1) => {
      let i = t.capturingGroups.length,
        a = !1;
      if (n > i)
        if (t.skipBackrefValidation) a = !0;
        else
          throw Error(`Not enough capturing groups defined to the left "${e}"`);
      return ((t.hasNumberedRef = !0), Tu(r ? i + 1 - n : n, { orphan: a }));
    };
  if (n) {
    let n = /^(?<sign>-?)0*(?<num>[1-9]\d*)$/.exec(r);
    if (n) return i(+n.groups.num, !!n.groups.sign);
    if (/[-+]/.test(r)) throw Error(`Invalid backref name "${e}"`);
    if (!t.namedGroupsByName.has(r))
      throw Error(`Group name not defined to the left "${e}"`);
    return Tu(r);
  }
  return i(+r);
}
function gu(e, t, n) {
  let { tokens: r, walk: i } = t,
    a = t.parent,
    o = a.body.at(-1),
    s = r[t.nextIndex];
  if (
    !n.isCheckingRangeEnd &&
    o &&
    o.type !== `CharacterClass` &&
    o.type !== `CharacterClassRange` &&
    s &&
    s.type !== `CharacterClassOpen` &&
    s.type !== `CharacterClassClose` &&
    s.type !== `CharacterClassIntersector`
  ) {
    let e = i(a, { ...n, isCheckingRangeEnd: !0 });
    if (o.type === `Character` && e.type === `Character`)
      return (a.body.pop(), ku(o, e));
    throw Error(`Invalid character class range`);
  }
  return Du(vl(`-`));
}
function _u({ negate: e }, t, n) {
  let { tokens: r, walk: i } = t,
    a = r[t.nextIndex],
    o = [Ou()],
    s = Yu(a);
  for (; s.type !== `CharacterClassClose`;) {
    if (s.type === `CharacterClassIntersector`) (o.push(Ou()), t.nextIndex++);
    else {
      let e = o.at(-1);
      e.body.push(i(e, n));
    }
    s = Yu(r[t.nextIndex], a);
  }
  let c = Ou({ negate: e });
  return (
    o.length === 1
      ? (c.body = o[0].body)
      : ((c.kind = `intersection`),
        (c.body = o.map((e) => (e.body.length === 1 ? e.body[0] : e)))),
    t.nextIndex++,
    c
  );
}
function vu({ kind: e, negate: t, value: n }, r) {
  let {
    normalizeUnknownPropertyNames: i,
    skipPropertyNameValidation: a,
    unicodePropertyMap: o,
  } = r;
  if (e === `property`) {
    let r = Ju(n);
    if (xl.has(r) && !o?.has(r)) ((e = `posix`), (n = r));
    else
      return Bu(n, {
        negate: t,
        normalizeUnknownPropertyNames: i,
        skipPropertyNameValidation: a,
        unicodePropertyMap: o,
      });
  }
  return e === `posix` ? Iu(n, { negate: t }) : Au(e, { negate: t });
}
function yu(e, t, n) {
  let {
      tokens: r,
      capturingGroups: i,
      namedGroupsByName: a,
      skipLookbehindValidation: o,
      walk: s,
    } = t,
    c = Vu(e),
    l = c.type === `AbsenceFunction`,
    u = Gu(c),
    d = u && c.negate;
  if (
    (c.type === `CapturingGroup` &&
      (i.push(c), c.name && yl(a, c.name, []).push(c)),
    l && n.isInAbsenceFunction)
  )
    throw Error(`Nested absence function not supported by Oniguruma`);
  let f = Xu(r[t.nextIndex]);
  for (; f.type !== `GroupClose`;) {
    if (f.type === `Alternator`) (c.body.push(Cu()), t.nextIndex++);
    else {
      let e = c.body.at(-1),
        t = s(e, {
          ...n,
          isInAbsenceFunction: n.isInAbsenceFunction || l,
          isInLookbehind: n.isInLookbehind || u,
          isInNegLookbehind: n.isInNegLookbehind || d,
        });
      if ((e.body.push(t), (u || n.isInLookbehind) && !o)) {
        let e = `Lookbehind includes a pattern not allowed by Oniguruma`;
        if (d || n.isInNegLookbehind) {
          if (Wu(t) || t.type === `CapturingGroup`) throw Error(e);
        } else if (Wu(t) || (Gu(t) && t.negate)) throw Error(e);
      }
    }
    f = Xu(r[t.nextIndex]);
  }
  return (t.nextIndex++, c);
}
function bu({ kind: e, min: t, max: n }, r) {
  let i = r.parent,
    a = i.body.at(-1);
  if (!a || !uu(a)) throw Error(`Quantifier requires a repeatable token`);
  let o = Lu(e, t, n, a);
  return (i.body.pop(), o);
}
function xu({ raw: e }, t) {
  let { capturingGroups: n, subroutines: r } = t,
    i = e.slice(3, -1),
    a = /^(?<sign>[-+]?)0*(?<num>[1-9]\d*)$/.exec(i);
  if (a) {
    let e = +a.groups.num,
      r = n.length;
    if (
      ((t.hasNumberedRef = !0),
      (i = { "": e, "+": r + e, "-": r + 1 - e }[a.groups.sign]),
      i < 1)
    )
      throw Error(`Invalid subroutine number`);
  } else i === `0` && (i = 0);
  let o = zu(i);
  return (r.push(o), o);
}
function Su(e, t) {
  if (e !== `repeater`) throw Error(`Unexpected absence function kind "${e}"`);
  return { type: `AbsenceFunction`, kind: e, body: Hu(t?.body) };
}
function Cu(e) {
  return { type: `Alternative`, body: Uu(e?.body) };
}
function wu(e, t) {
  let n = { type: `Assertion`, kind: e };
  return (
    (e === `word_boundary` || e === `text_segment_boundary`) &&
      (n.negate = !!t?.negate),
    n
  );
}
function Tu(e, t) {
  let n = !!t?.orphan;
  return { type: `Backreference`, ref: e, ...(n && { orphan: n }) };
}
function Eu(e, t) {
  let n = { name: void 0, isSubroutined: !1, ...t };
  if (n.name !== void 0 && !Ku(n.name))
    throw Error(`Group name "${n.name}" invalid in Oniguruma`);
  return {
    type: `CapturingGroup`,
    number: e,
    ...(n.name && { name: n.name }),
    ...(n.isSubroutined && { isSubroutined: n.isSubroutined }),
    body: Hu(t?.body),
  };
}
function Du(e, t) {
  let n = { useLastValid: !1, ...t };
  if (e > 1114111) {
    let t = e.toString(16);
    if (n.useLastValid) e = 1114111;
    else
      throw Error(
        e > 1310719
          ? `Invalid code point out of range "\\x{${t}}"`
          : `Invalid code point out of range in JS "\\x{${t}}"`,
      );
  }
  return { type: `Character`, value: e };
}
function Ou(e) {
  let t = { kind: `union`, negate: !1, ...e };
  return {
    type: `CharacterClass`,
    kind: t.kind,
    negate: t.negate,
    body: Uu(e?.body),
  };
}
function ku(e, t) {
  if (t.value < e.value) throw Error(`Character class range out of order`);
  return { type: `CharacterClassRange`, min: e, max: t };
}
function Au(e, t) {
  let n = !!t?.negate,
    r = { type: `CharacterSet`, kind: e };
  return (
    (e === `digit` ||
      e === `hex` ||
      e === `newline` ||
      e === `space` ||
      e === `word`) &&
      (r.negate = n),
    (e === `text_segment` || (e === `newline` && !n)) &&
      (r.variableLength = !0),
    r
  );
}
function ju(e, t = {}) {
  if (e === `keep`) return { type: `Directive`, kind: e };
  if (e === `flags`) return { type: `Directive`, kind: e, flags: bl(t.flags) };
  throw Error(`Unexpected directive kind "${e}"`);
}
function Mu(e) {
  return { type: `Flags`, ...e };
}
function Nu(e) {
  let t = e?.atomic,
    n = e?.flags;
  if (t && n) throw Error(`Atomic group cannot have flags`);
  return {
    type: `Group`,
    ...(t && { atomic: t }),
    ...(n && { flags: n }),
    body: Hu(e?.body),
  };
}
function Pu(e) {
  let t = { behind: !1, negate: !1, ...e };
  return {
    type: `LookaroundAssertion`,
    kind: t.behind ? `lookbehind` : `lookahead`,
    negate: t.negate,
    body: Hu(e?.body),
  };
}
function Fu(e, t, n) {
  return { type: `NamedCallout`, kind: e, tag: t, arguments: n };
}
function Iu(e, t) {
  let n = !!t?.negate;
  if (!xl.has(e)) throw Error(`Invalid POSIX class "${e}"`);
  return { type: `CharacterSet`, kind: `posix`, value: e, negate: n };
}
function Lu(e, t, n, r) {
  if (t > n) throw Error(`Invalid reversed quantifier range`);
  return { type: `Quantifier`, kind: e, min: t, max: n, body: r };
}
function Ru(e, t) {
  return { type: `Regex`, body: Hu(t?.body), flags: e };
}
function zu(e) {
  return { type: `Subroutine`, ref: e };
}
function Bu(e, t) {
  let n = {
      negate: !1,
      normalizeUnknownPropertyNames: !1,
      skipPropertyNameValidation: !1,
      unicodePropertyMap: null,
      ...t,
    },
    r = n.unicodePropertyMap?.get(Ju(e));
  if (!r) {
    if (n.normalizeUnknownPropertyNames) r = qu(e);
    else if (n.unicodePropertyMap && !n.skipPropertyNameValidation)
      throw Error(Y`Invalid Unicode property "\p{${e}}"`);
  }
  return {
    type: `CharacterSet`,
    kind: `property`,
    value: r ?? e,
    negate: n.negate,
  };
}
function Vu({ flags: e, kind: t, name: n, negate: r, number: i }) {
  switch (t) {
    case `absence_repeater`:
      return Su(`repeater`);
    case `atomic`:
      return Nu({ atomic: !0 });
    case `capturing`:
      return Eu(i, { name: n });
    case `group`:
      return Nu({ flags: e });
    case `lookahead`:
    case `lookbehind`:
      return Pu({ behind: t === `lookbehind`, negate: r });
    default:
      throw Error(`Unexpected group kind "${t}"`);
  }
}
function Hu(e) {
  if (e === void 0) e = [Cu()];
  else if (
    !Array.isArray(e) ||
    !e.length ||
    !e.every((e) => e.type === `Alternative`)
  )
    throw Error(
      `Invalid body; expected array of one or more Alternative nodes`,
    );
  return e;
}
function Uu(e) {
  if (e === void 0) e = [];
  else if (!Array.isArray(e) || !e.every((e) => !!e.type))
    throw Error(`Invalid body; expected array of nodes`);
  return e;
}
function Wu(e) {
  return e.type === `LookaroundAssertion` && e.kind === `lookahead`;
}
function Gu(e) {
  return e.type === `LookaroundAssertion` && e.kind === `lookbehind`;
}
function Ku(e) {
  return /^[\p{Alpha}\p{Pc}][^)]*$/u.test(e);
}
function qu(e) {
  return e
    .trim()
    .replace(/[- _]+/g, `_`)
    .replace(/[A-Z][a-z]+(?=[A-Z])/g, `$&_`)
    .replace(
      /[A-Za-z]+/g,
      (e) => e[0].toUpperCase() + e.slice(1).toLowerCase(),
    );
}
function Ju(e) {
  return e.replace(/[- _]+/g, ``).toLowerCase();
}
function Yu(e, t) {
  return bl(
    e,
    `${t?.type === `Character` && t.value === 93 ? `Empty` : `Unclosed`} character class`,
  );
}
function Xu(e) {
  return bl(e, `Unclosed group`);
}
var Zu = e(() => {
  (cu(), Sl(), fu());
});
function Qu(e, t, n = null) {
  function r(e, t) {
    for (let n = 0; n < e.length; n++) {
      let r = i(e[n], t, n, e);
      n = Math.max(-1, n + r);
    }
  }
  function i(a, o = null, s = null, c = null) {
    let l = 0,
      u = !1,
      d = {
        node: a,
        parent: o,
        key: s,
        container: c,
        root: e,
        remove() {
          ($u(c).splice(Math.max(0, ed(s) + l), 1), l--, (u = !0));
        },
        removeAllNextSiblings() {
          return $u(c).splice(ed(s) + 1);
        },
        removeAllPrevSiblings() {
          let e = ed(s) + l;
          return ((l -= e), $u(c).splice(0, Math.max(0, e)));
        },
        replaceWith(e, t = {}) {
          let n = !!t.traverse;
          (c
            ? (c[Math.max(0, ed(s) + l)] = e)
            : (bl(o, `Can't replace root node`)[s] = e),
            n && i(e, o, s, c),
            (u = !0));
        },
        replaceWithMultiple(e, t = {}) {
          let n = !!t.traverse;
          if (
            ($u(c).splice(Math.max(0, ed(s) + l), 1, ...e),
            (l += e.length - 1),
            n)
          ) {
            let t = 0;
            for (let n = 0; n < e.length; n++)
              t += i(e[n], o, ed(s) + n + t, c);
          }
          u = !0;
        },
        skip() {
          u = !0;
        },
      },
      { type: f } = a,
      p = t[`*`],
      m = t[f],
      h = typeof p == `function` ? p : p?.enter,
      g = typeof m == `function` ? m : m?.enter;
    if ((h?.(d, n), g?.(d, n), !u))
      switch (f) {
        case `AbsenceFunction`:
        case `CapturingGroup`:
        case `Group`:
          r(a.body, a);
          break;
        case `Alternative`:
        case `CharacterClass`:
          r(a.body, a);
          break;
        case `Assertion`:
        case `Backreference`:
        case `Character`:
        case `CharacterSet`:
        case `Directive`:
        case `Flags`:
        case `NamedCallout`:
        case `Subroutine`:
          break;
        case `CharacterClassRange`:
          (i(a.min, a, `min`), i(a.max, a, `max`));
          break;
        case `LookaroundAssertion`:
          r(a.body, a);
          break;
        case `Quantifier`:
          i(a.body, a, `body`);
          break;
        case `Regex`:
          (r(a.body, a), i(a.flags, a, `flags`));
          break;
        default:
          throw Error(`Unexpected node type "${f}"`);
      }
    return (m?.exit?.(d, n), p?.exit?.(d, n), l);
  }
  return (i(e), e);
}
function $u(e) {
  if (!Array.isArray(e)) throw Error(`Container expected`);
  return e;
}
function ed(e) {
  if (typeof e != `number`) throw Error(`Numeric key expected`);
  return e;
}
var td = e(() => {
  Sl();
});
function nd(e, t) {
  for (let n = 0; n < e.length; n++) e[n] >= t && e[n]++;
}
function rd(e, t, n, r) {
  return e.slice(0, t) + r + e.slice(t + n.length);
}
var id,
  ad = e(() => {
    id = String.raw`\(\?(?:[:=!>A-Za-z\-]|<[=!]|\(DEFINE\))`;
  });
function od(e, t, n, r) {
  let i = new RegExp(String.raw`${t}|(?<$skip>\[\^?|\\?.)`, `gsu`),
    a = [!1],
    o = 0,
    s = ``;
  for (let t of e.matchAll(i)) {
    let {
      0: e,
      groups: { $skip: i },
    } = t;
    if (!i && (!r || (r === dd.DEFAULT) == !o)) {
      n instanceof Function
        ? (s += n(t, {
            context: o ? dd.CHAR_CLASS : dd.DEFAULT,
            negated: a[a.length - 1],
          }))
        : (s += n);
      continue;
    }
    (e[0] === `[`
      ? (o++, a.push(e[1] === `^`))
      : e === `]` && o && (o--, a.pop()),
      (s += e));
  }
  return s;
}
function sd(e, t, n, r) {
  od(e, t, n, r);
}
function cd(e, t, n = 0, r) {
  if (!new RegExp(t, `su`).test(e)) return null;
  let i = RegExp(`${t}|(?<$skip>\\\\?.)`, `gsu`);
  i.lastIndex = n;
  let a = 0,
    o;
  for (; (o = i.exec(e));) {
    let {
      0: e,
      groups: { $skip: t },
    } = o;
    if (!t && (!r || (r === dd.DEFAULT) == !a)) return o;
    (e === `[` ? a++ : e === `]` && a && a--,
      i.lastIndex == o.index && i.lastIndex++);
  }
  return null;
}
function ld(e, t, n) {
  return !!cd(e, t, 0, n);
}
function ud(e, t) {
  let n = /\\?./gsu;
  n.lastIndex = t;
  let r = e.length,
    i = 0,
    a = 1,
    o;
  for (; (o = n.exec(e));) {
    let [e] = o;
    if (e === `[`) i++;
    else if (i) e === `]` && i--;
    else if (e === `(`) a++;
    else if (e === `)` && (a--, !a)) {
      r = o.index;
      break;
    }
  }
  return e.slice(t, r);
}
var dd,
  fd = e(() => {
    dd = Object.freeze({ DEFAULT: `DEFAULT`, CHAR_CLASS: `CHAR_CLASS` });
  });
function pd(e, t) {
  let n = t?.hiddenCaptures ?? [],
    r = t?.captureTransfers ?? new Map();
  if (!/\(\?>/.test(e))
    return { pattern: e, captureTransfers: r, hiddenCaptures: n };
  let i = [0],
    a = [],
    o = 0,
    s = 0,
    c = NaN,
    l;
  do {
    l = !1;
    let t = 0,
      u = 0,
      d = !1,
      f;
    for (hd.lastIndex = Number.isNaN(c) ? 0 : c + 7; (f = hd.exec(e));) {
      let {
        0: p,
        index: m,
        groups: { capturingStart: h, noncapturingStart: g },
      } = f;
      if (p === `[`) t++;
      else if (t) p === `]` && t--;
      else if (p === `(?>` && !d) ((c = m), (d = !0));
      else if (d && g) u++;
      else if (h) d ? u++ : (o++, i.push(o + s));
      else if (p === `)` && d) {
        if (!u) {
          s++;
          let t = o + s;
          if (
            ((e = `${e.slice(0, c)}(?:(?=(${e.slice(c + 3, m)}))<$$${t}>)${e.slice(m + 1)}`),
            (l = !0),
            a.push(t),
            nd(n, t),
            r.size)
          ) {
            let e = new Map();
            (r.forEach((n, r) => {
              e.set(
                r >= t ? r + 1 : r,
                n.map((e) => (e >= t ? e + 1 : e)),
              );
            }),
              (r = e));
          }
          break;
        }
        u--;
      }
    }
  } while (l);
  return (
    n.push(...a),
    (e = od(
      e,
      String.raw`\\(?<backrefNum>[1-9]\d*)|<\$\$(?<wrappedBackrefNum>\d+)>`,
      ({ 0: e, groups: { backrefNum: t, wrappedBackrefNum: n } }) => {
        if (t) {
          let n = +t;
          if (n > i.length - 1)
            throw Error(`Backref "${e}" greater than number of captures`);
          return `\\${i[n]}`;
        }
        return `\\${n}`;
      },
      dd.DEFAULT,
    )),
    { pattern: e, captureTransfers: r, hiddenCaptures: n }
  );
}
function md(e) {
  if (!RegExp(`${gd}\\+`).test(e)) return { pattern: e };
  let t = [],
    n = null,
    r = null,
    i = ``,
    a = 0,
    o;
  for (_d.lastIndex = 0; (o = _d.exec(e));) {
    let {
      0: s,
      index: c,
      groups: { qBase: l, qMod: u, invalidQ: d },
    } = o;
    if (s === `[`) (a || (r = c), a++);
    else if (s === `]`) a ? a-- : (r = null);
    else if (!a)
      if (u === `+` && i && !i.startsWith(`(`)) {
        if (d) throw Error(`Invalid quantifier "${s}"`);
        let t = -1;
        if (/^\{\d+\}$/.test(l)) e = rd(e, c + l.length, u, ``);
        else {
          if (i === `)` || i === `]`) {
            let t = i === `)` ? n : r;
            if (t === null) throw Error(`Invalid unmatched "${i}"`);
            e = `${e.slice(0, t)}(?>${e.slice(t, c)}${l})${e.slice(c + s.length)}`;
          } else
            e = `${e.slice(0, c - i.length)}(?>${i}${l})${e.slice(c + s.length)}`;
          t += 4;
        }
        _d.lastIndex += t;
      } else
        s[0] === `(` ? t.push(c) : s === `)` && (n = t.length ? t.pop() : null);
    i = s;
  }
  return { pattern: e };
}
var hd,
  gd,
  _d,
  vd = e(() => {
    (ad(),
      fd(),
      (hd = new RegExp(
        String.raw`(?<noncapturingStart>${id})|(?<capturingStart>\((?:\?<[^>]+>)?)|\\?.`,
        `gsu`,
      )),
      (gd = String.raw`(?:[?*+]|\{\d+(?:,\d*)?\})`),
      (_d = new RegExp(
        String.raw`
\\(?: \d+
  | c[A-Za-z]
  | [gk]<[^>]+>
  | [pPu]\{[^\}]+\}
  | u[A-Fa-f\d]{4}
  | x[A-Fa-f\d]{2}
  )
| \((?: \? (?: [:=!>]
  | <(?:[=!]|[^>]+>)
  | [A-Za-z\-]+:
  | \(DEFINE\)
  ))?
| (?<qBase>${gd})(?<qMod>[?+]?)(?<invalidQ>[?*+\{]?)
| \\?.
`.replace(/\s+/g, ``),
        `gsu`,
      )));
  }),
  yd = e(() => {
    vd();
  });
function bd(e, t) {
  let { hiddenCaptures: n, mode: r } = {
      hiddenCaptures: [],
      mode: `plugin`,
      ...t,
    },
    i = t?.captureTransfers ?? new Map();
  if (!new RegExp(Dd, `su`).test(e))
    return { pattern: e, captureTransfers: i, hiddenCaptures: n };
  if (r === `plugin` && ld(e, Ed`\(\?\(DEFINE\)`, dd.DEFAULT))
    throw Error(`DEFINE groups cannot be used with recursion`);
  let a = [],
    o = ld(e, Ed`\\[1-9]`, dd.DEFAULT),
    s = new Map(),
    c = [],
    l = !1,
    u = 0,
    d = 0,
    f;
  for (Ad.lastIndex = 0; (f = Ad.exec(e));) {
    let {
      0: t,
      groups: { captureName: p, rDepth: m, gRNameOrNum: h, gRDepth: g },
    } = f;
    if (t === `[`) u++;
    else if (u) t === `]` && u--;
    else if (m) {
      if ((xd(m), l)) throw Error(jd);
      if (o)
        throw Error(
          `${r === `external` ? `Backrefs` : `Numbered backrefs`} cannot be used with global recursion`,
        );
      let t = e.slice(0, f.index),
        s = e.slice(Ad.lastIndex);
      if (ld(s, Dd, dd.DEFAULT)) throw Error(jd);
      let c = m - 1;
      ((e = Sd(t, s, c, !1, n, a, d)), (i = Td(i, t, c, a.length, 0, d)));
      break;
    } else if (h) {
      xd(g);
      let u = !1;
      for (let e of c)
        if (e.name === h || e.num === +h) {
          if (((u = !0), e.hasRecursedWithin)) throw Error(jd);
          break;
        }
      if (!u)
        throw Error(
          Ed`Recursive \g cannot be used outside the referenced group "${r === `external` ? h : Ed`\g<${h}&R=${g}>`}"`,
        );
      let p = s.get(h),
        m = ud(e, p);
      if (o && ld(m, Ed`${Od}|\((?!\?)`, dd.DEFAULT))
        throw Error(
          `${r === `external` ? `Backrefs` : `Numbered backrefs`} cannot be used with recursion of capturing groups`,
        );
      let _ = e.slice(p, f.index),
        v = m.slice(_.length + t.length),
        y = a.length,
        b = g - 1,
        x = Sd(_, v, b, !0, n, a, d);
      ((i = Td(i, _, b, a.length - y, y, d)),
        (e = `${e.slice(0, p)}${x}${e.slice(p + m.length)}`),
        (Ad.lastIndex += x.length - t.length - _.length - v.length),
        c.forEach((e) => (e.hasRecursedWithin = !0)),
        (l = !0));
    } else if (p)
      (d++,
        s.set(String(d), Ad.lastIndex),
        s.set(p, Ad.lastIndex),
        c.push({ num: d, name: p }));
    else if (t[0] === `(`) {
      let e = t === `(`;
      (e && (d++, s.set(String(d), Ad.lastIndex)), c.push(e ? { num: d } : {}));
    } else t === `)` && c.pop();
  }
  return (n.push(...a), { pattern: e, captureTransfers: i, hiddenCaptures: n });
}
function xd(e) {
  let t = `Max depth must be integer between 2 and 100; used ${e}`;
  if (!/^[1-9]\d*$/.test(e) || ((e = +e), e < 2 || e > 100)) throw Error(t);
}
function Sd(e, t, n, r, i, a, o) {
  let s = new Set();
  r &&
    sd(
      e + t,
      Od,
      ({ groups: { captureName: e } }) => {
        s.add(e);
      },
      dd.DEFAULT,
    );
  let c = [n, r ? s : null, i, a, o];
  return `${e}${Cd(`(?:${e}`, `forward`, ...c)}(?:)${Cd(`${t})`, `backward`, ...c)}${t}`;
}
function Cd(e, t, n, r, i, a, o) {
  let s = (e) => (t === `forward` ? e + 2 : n - e + 2 - 1),
    c = ``;
  for (let t = 0; t < n; t++) {
    let n = s(t);
    c += od(
      e,
      Ed`${kd}|\\k<(?<backref>[^>]+)>`,
      ({ 0: e, groups: { captureName: t, unnamed: s, backref: c } }) => {
        if (c && r && !r.has(c)) return e;
        let l = `_$${n}`;
        if (s || t) {
          let n = o + a.length + 1;
          return (a.push(n), wd(i, n), s ? e : `(?<${t}${l}>`);
        }
        return Ed`\k<${c}${l}>`;
      },
      dd.DEFAULT,
    );
  }
  return c;
}
function wd(e, t) {
  for (let n = 0; n < e.length; n++) e[n] >= t && e[n]++;
}
function Td(e, t, n, r, i, a) {
  if (e.size && r) {
    let o = 0;
    sd(t, kd, () => o++, dd.DEFAULT);
    let s = a - o + i,
      c = new Map();
    return (
      e.forEach((e, t) => {
        let i = (r - o * n) / n,
          a = o * n,
          l = t > s + o ? t + r : t,
          u = [];
        for (let t of e)
          if (t <= s) u.push(t);
          else if (t > s + o + i) u.push(t + r);
          else if (t <= s + o) for (let e = 0; e <= n; e++) u.push(t + o * e);
          else for (let e = 0; e <= n; e++) u.push(t + a + i * e);
        c.set(l, u);
      }),
      c
    );
  }
  return e;
}
var Ed,
  Dd,
  Od,
  kd,
  Ad,
  jd,
  Md = e(() => {
    (fd(),
      (Ed = String.raw),
      (Dd = Ed`\(\?R=(?<rDepth>[^\)]+)\)|${Ed`\\g<(?<gRNameOrNum>[^>&]+)&R=(?<gRDepth>[^>]+)>`}`),
      (Od = Ed`\(\?<(?![=!])(?<captureName>[^>]+)>`),
      (kd = Ed`${Od}|(?<unnamed>\()(?!\?)`),
      (Ad = new RegExp(Ed`${Od}|${Dd}|\(\?|\\?.`, `gsu`)),
      (jd = `Cannot use multiple overlapping recursions`));
  });
function Nd(e, { enable: t, disable: n }) {
  return {
    dotAll: !n?.dotAll && !!(t?.dotAll || e.dotAll),
    ignoreCase: !n?.ignoreCase && !!(t?.ignoreCase || e.ignoreCase),
  };
}
function Pd(e, t, n) {
  return (e.has(t) || e.set(t, n), e.get(t));
}
function Fd(e, t) {
  return Tf[e] >= Tf[t];
}
function Id(e, t) {
  if (e == null) throw Error(t ?? `Value expected`);
  return e;
}
function Ld(e = {}) {
  if ({}.toString.call(e) !== `[object Object]`)
    throw Error(`Unexpected options`);
  if (e.target !== void 0 && !Ef[e.target])
    throw Error(`Unexpected target "${e.target}"`);
  let t = {
    accuracy: `default`,
    avoidSubclass: !1,
    flags: ``,
    global: !1,
    hasIndices: !1,
    lazyCompileLength: 1 / 0,
    target: `auto`,
    verbose: !1,
    ...e,
    rules: {
      allowOrphanBackrefs: !1,
      asciiWordBoundaries: !1,
      captureGroup: !1,
      recursionLimit: 20,
      singleline: !1,
      ...e.rules,
    },
  };
  return (
    t.target === `auto` &&
      (t.target = wf.flagGroups
        ? `ES2025`
        : wf.unicodeSets
          ? `ES2024`
          : `ES2018`),
    t
  );
}
function Rd(e) {
  if (Of.has(e)) return [e];
  let t = new Set(),
    n = e.toLowerCase(),
    r = n.toUpperCase(),
    i = Nf.get(n),
    a = jf.get(n),
    o = Mf.get(n);
  return (
    [...r].length === 1 && t.add(r),
    o && t.add(o),
    i && t.add(i),
    t.add(n),
    a && t.add(a),
    [...t]
  );
}
function zd(e, t) {
  let n = [];
  for (let r = e; r <= t; r++) n.push(r);
  return n;
}
function Bd(e) {
  let t = Z(e);
  return [t.toLowerCase(), t];
}
function Vd(e, t) {
  return zd(e, t).map((e) => Bd(e));
}
function Hd(e, t) {
  let n = {
    accuracy: `default`,
    asciiWordBoundaries: !1,
    avoidSubclass: !1,
    bestEffortTarget: `ES2025`,
    ...t,
  };
  Ud(e);
  let r = {
    accuracy: n.accuracy,
    asciiWordBoundaries: n.asciiWordBoundaries,
    avoidSubclass: n.avoidSubclass,
    flagDirectivesByAlt: new Map(),
    jsGroupNameMap: new Map(),
    minTargetEs2024: Fd(n.bestEffortTarget, `ES2024`),
    passedLookbehind: !1,
    strategy: null,
    subroutineRefMap: new Map(),
    supportedGNodes: new Set(),
    digitIsAscii: e.flags.digitIsAscii,
    spaceIsAscii: e.flags.spaceIsAscii,
    wordIsAscii: e.flags.wordIsAscii,
  };
  Qu(e, If, r);
  let i = { dotAll: e.flags.dotAll, ignoreCase: e.flags.ignoreCase },
    a = {
      currentFlags: i,
      prevFlags: null,
      globalFlags: i,
      groupOriginByCopy: new Map(),
      groupsByName: new Map(),
      multiplexCapturesToLeftByRef: new Map(),
      openRefs: new Map(),
      reffedNodesByReferencer: new Map(),
      subroutineRefMap: r.subroutineRefMap,
    };
  return (
    Qu(e, Lf, a),
    Qu(e, Rf, {
      groupsByName: a.groupsByName,
      highestOrphanBackref: 0,
      numCapturesToLeft: 0,
      reffedNodesByReferencer: a.reffedNodesByReferencer,
    }),
    (e._originMap = a.groupOriginByCopy),
    (e._strategy = r.strategy),
    e
  );
}
function Ud(e) {
  Qu(e, {
    "*"({ node: e, parent: t }) {
      e.parent = t;
    },
  });
}
function Wd(e, t) {
  return e.dotAll === t.dotAll && e.ignoreCase === t.ignoreCase;
}
function Gd(e, t) {
  let n = t;
  do {
    if (n.type === `Regex`) return !1;
    if (n.type === `Alternative`) continue;
    if (n === e) return !1;
    let t = Qd(n.parent);
    for (let r of t) {
      if (r === n) break;
      if (r === e || ef(r, e)) return !0;
    }
  } while ((n = n.parent));
  throw Error(`Unexpected path`);
}
function Kd(e, t, n, r) {
  let i = Array.isArray(e) ? [] : {};
  for (let [a, o] of Object.entries(e))
    a === `parent`
      ? (i.parent = Array.isArray(n) ? r : n)
      : o && typeof o == `object`
        ? (i[a] = Kd(o, t, i, n))
        : (a === `type` && o === `CapturingGroup` && t.set(i, t.get(e) ?? e),
          (i[a] = o));
  return i;
}
function qd(e) {
  let t = zu(e);
  return ((t.isRecursive = !0), t);
}
function Jd(e, t) {
  let n = [];
  for (; (e = e.parent);) (!t || t(e)) && n.push(e);
  return n;
}
function Yd(e, t) {
  if (t.has(e)) return t.get(e);
  let n = `$${t.size}_${e.replace(/^[^$_\p{IDS}]|[^$\u200C\u200D\p{IDC}]/gu, `_`)}`;
  return (t.set(e, n), n);
}
function Xd(e) {
  let t = [`dotAll`, `ignoreCase`],
    n = { enable: {}, disable: {} };
  return (
    e.forEach(({ flags: e }) => {
      t.forEach((t) => {
        (e.enable?.[t] && (delete n.disable[t], (n.enable[t] = !0)),
          e.disable?.[t] && (n.disable[t] = !0));
      });
    }),
    Object.keys(n.enable).length || delete n.enable,
    Object.keys(n.disable).length || delete n.disable,
    n.enable || n.disable ? n : null
  );
}
function Zd({ dotAll: e, ignoreCase: t }) {
  let n = {};
  return (
    (e || t) &&
      ((n.enable = {}),
      e && (n.enable.dotAll = !0),
      t && (n.enable.ignoreCase = !0)),
    (!e || !t) &&
      ((n.disable = {}),
      !e && (n.disable.dotAll = !0),
      !t && (n.disable.ignoreCase = !0)),
    n
  );
}
function Qd(e) {
  if (!e) throw Error(`Node expected`);
  let { body: t } = e;
  return Array.isArray(t) ? t : t ? [t] : null;
}
function $d(e) {
  let t = e.find(
    (e) => e.kind === `search_start` || rf(e, { negate: !1 }) || !tf(e),
  );
  if (!t) return null;
  if (t.kind === `search_start`) return t;
  if (t.type === `LookaroundAssertion`) return t.body[0].body[0];
  if (t.type === `CapturingGroup` || t.type === `Group`) {
    let e = [];
    for (let n of t.body) {
      let t = $d(n.body);
      if (!t) return null;
      Array.isArray(t) ? e.push(...t) : e.push(t);
    }
    return e;
  }
  return null;
}
function ef(e, t) {
  let n = Qd(e) ?? [];
  for (let e of n) if (e === t || ef(e, t)) return !0;
  return !1;
}
function tf({ type: e }) {
  return e === `Assertion` || e === `Directive` || e === `LookaroundAssertion`;
}
function nf(e) {
  let t = [`Character`, `CharacterClass`, `CharacterSet`];
  return (
    t.includes(e.type) ||
    (e.type === `Quantifier` && e.min && t.includes(e.body.type))
  );
}
function rf(e, t) {
  let n = { negate: null, ...t };
  return (
    e.type === `LookaroundAssertion` &&
    (n.negate === null || e.negate === n.negate) &&
    e.body.length === 1 &&
    lu(e.body[0], { type: `Assertion`, kind: `search_start` })
  );
}
function af(e) {
  return /^[$_\p{IDS}][$\u200C\u200D\p{IDC}]*$/u.test(e);
}
function of(e, t) {
  let n = pu(e, { ...t, unicodePropertyMap: Af }).body;
  return n.length > 1 || n[0].body.length > 1 ? Nu({ body: n }) : n[0].body[0];
}
function sf(e, t) {
  return ((e.negate = t), e);
}
function cf(e, t) {
  return ((e.parent = t), e);
}
function X(e, t) {
  return (Ud(e), (e.parent = t), e);
}
function lf(e, t) {
  let n = Ld(t),
    r = Fd(n.target, `ES2024`),
    i = Fd(n.target, `ES2025`),
    a = n.rules.recursionLimit;
  if (!Number.isInteger(a) || a < 2 || a > 20)
    throw Error(`Invalid recursionLimit; use 2-20`);
  let o = null,
    s = null;
  if (!i) {
    let t = [e.flags.ignoreCase];
    Qu(e, zf, {
      getCurrentModI: () => t.at(-1),
      popModI() {
        t.pop();
      },
      pushModI(e) {
        t.push(e);
      },
      setHasCasedChar() {
        t.at(-1) ? (o = !0) : (s = !0);
      },
    });
  }
  let c = {
      dotAll: e.flags.dotAll,
      ignoreCase: !!((e.flags.ignoreCase || o) && !s),
    },
    l = e,
    u = {
      accuracy: n.accuracy,
      appliedGlobalFlags: c,
      captureMap: new Map(),
      currentFlags: { dotAll: e.flags.dotAll, ignoreCase: e.flags.ignoreCase },
      inCharClass: !1,
      lastNode: l,
      originMap: e._originMap,
      recursionLimit: a,
      useAppliedIgnoreCase: !!(!i && o && s),
      useFlagMods: i,
      useFlagV: r,
      verbose: n.verbose,
    };
  function d(e) {
    return (
      (u.lastNode = l),
      (l = e),
      Id(Bf[e.type], `Unexpected node type "${e.type}"`)(e, u, d)
    );
  }
  let f = {
    pattern: e.body.map(d).join(`|`),
    flags: d(e.flags),
    options: { ...e.options },
  };
  return (
    r ||
      (delete f.options.force.v,
      (f.options.disable.v = !0),
      (f.options.unicodeSetsPlugin = null)),
    (f._captureTransfers = new Map()),
    (f._hiddenCaptures = []),
    u.captureMap.forEach((e, t) => {
      (e.hidden && f._hiddenCaptures.push(t),
        e.transferTo && Pd(f._captureTransfers, e.transferTo, []).push(t));
    }),
    f
  );
}
function uf(e) {
  return Gf.test(e);
}
function df(e, t) {
  let n = !!t?.firstOnly,
    r = e.min.value,
    i = e.max.value,
    a = [];
  if ((r < 65 && (i === 65535 || i >= 131071)) || (r === 65536 && i >= 131071))
    return a;
  for (let e = r; e <= i; e++) {
    let t = Z(e);
    if (!uf(t)) continue;
    let o = Rd(t).filter((e) => {
      let t = e.codePointAt(0);
      return t < r || t > i;
    });
    if (o.length && (a.push(...o), n)) break;
  }
  return a;
}
function ff(e, { escDigit: t, inCharClass: n, useFlagV: r }) {
  if (Wf.has(e)) return Wf.get(e);
  if (e < 32 || (e > 126 && e < 160) || e > 262143 || (t && _f(e)))
    return e > 255
      ? `\\u{${e.toString(16).toUpperCase()}}`
      : `\\x${e.toString(16).toUpperCase().padStart(2, `0`)}`;
  let i = n ? (r ? Uf : Hf) : Vf,
    a = Z(e);
  return (i.has(a) ? `\\` : ``) + a;
}
function pf(e) {
  let t = e.map((e) => e.codePointAt(0)).sort((e, t) => e - t),
    n = [],
    r = null;
  for (let e = 0; e < t.length; e++)
    t[e + 1] === t[e] + 1
      ? (r ??= t[e])
      : r === null
        ? n.push(t[e])
        : (n.push([r, t[e]]), (r = null));
  return n;
}
function mf(e, t, n) {
  if (e) return `>`;
  let r = ``;
  if (t && n) {
    let { enable: e, disable: n } = t;
    r =
      (e?.ignoreCase ? `i` : ``) +
      (e?.dotAll ? `s` : ``) +
      (n ? `-` : ``) +
      (n?.ignoreCase ? `i` : ``) +
      (n?.dotAll ? `s` : ``);
  }
  return `${r}:`;
}
function hf({ kind: e, max: t, min: n }) {
  let r;
  return (
    (r =
      !n && t === 1
        ? `?`
        : !n && t === 1 / 0
          ? `*`
          : n === 1 && t === 1 / 0
            ? `+`
            : n === t
              ? `{${n}}`
              : `{${n},${t === 1 / 0 ? `` : t}}`),
    r + { greedy: ``, lazy: `?`, possessive: `+` }[e]
  );
}
function gf({ type: e }) {
  return e === `CapturingGroup` || e === `Group` || e === `LookaroundAssertion`;
}
function _f(e) {
  return e > 47 && e < 58;
}
function vf({ type: e, value: t }) {
  return e === `Character` && t === 45;
}
function yf(e, t, n, r) {
  if (((e.index += t), (e.input = n), r)) {
    let n = e.indices;
    for (let e = 0; e < n.length; e++) {
      let r = n[e];
      r && (n[e] = [r[0] + t, r[1] + t]);
    }
    let r = n.groups;
    r &&
      Object.keys(r).forEach((e) => {
        let n = r[e];
        n && (r[e] = [n[0] + t, n[1] + t]);
      });
  }
}
function bf(e, t) {
  let n = new Map();
  for (let t of e) n.set(t, { hidden: !0 });
  for (let [e, r] of t) for (let t of r) Pd(n, t, {}).transferTo = e;
  return n;
}
function xf(e) {
  let t = /(?<capture>\((?:\?<(?![=!])(?<name>[^>]+)>|(?!\?)))|\\?./gsu,
    n = new Map(),
    r = 0,
    i = 0,
    a;
  for (; (a = t.exec(e));) {
    let {
      0: e,
      groups: { capture: t, name: o },
    } = a;
    e === `[` ? r++ : r ? e === `]` && r-- : t && (i++, o && n.set(i, o));
  }
  return n;
}
function Sf(e, t) {
  let n = Cf(e, t);
  return n.options
    ? new Kf(n.pattern, n.flags, n.options)
    : new RegExp(n.pattern, n.flags);
}
function Cf(e, t) {
  let n = Ld(t),
    r = Hd(
      pu(e, {
        flags: n.flags,
        normalizeUnknownPropertyNames: !0,
        rules: {
          captureGroup: n.rules.captureGroup,
          singleline: n.rules.singleline,
        },
        skipBackrefValidation: n.rules.allowOrphanBackrefs,
        unicodePropertyMap: Af,
      }),
      {
        accuracy: n.accuracy,
        asciiWordBoundaries: n.rules.asciiWordBoundaries,
        avoidSubclass: n.avoidSubclass,
        bestEffortTarget: n.target,
      },
    ),
    i = lf(r, n),
    a = bd(i.pattern, {
      captureTransfers: i._captureTransfers,
      hiddenCaptures: i._hiddenCaptures,
      mode: `external`,
    }),
    o = pd(md(a.pattern).pattern, {
      captureTransfers: a.captureTransfers,
      hiddenCaptures: a.hiddenCaptures,
    }),
    s = {
      pattern: o.pattern,
      flags: `${n.hasIndices ? `d` : ``}${n.global ? `g` : ``}${i.flags}${i.options.disable.v ? `u` : `v`}`,
    };
  if (n.avoidSubclass) {
    if (n.lazyCompileLength !== 1 / 0)
      throw Error(`Lazy compilation requires subclass`);
  } else {
    let e = o.hiddenCaptures.sort((e, t) => e - t),
      t = Array.from(o.captureTransfers),
      i = r._strategy,
      a = s.pattern.length >= n.lazyCompileLength;
    (e.length || t.length || i || a) &&
      (s.options = {
        ...(e.length && { hiddenCaptures: e }),
        ...(t.length && { transfers: t }),
        ...(i && { strategy: i }),
        ...(a && { lazyCompile: a }),
      });
  }
  return s;
}
var Z,
  Q,
  wf,
  Tf,
  Ef,
  Df,
  Of,
  kf,
  Af,
  jf,
  Mf,
  Nf,
  Pf,
  Ff,
  If,
  Lf,
  Rf,
  zf,
  Bf,
  Vf,
  Hf,
  Uf,
  Wf,
  Gf,
  Kf,
  qf = e(() => {
    (Zu(),
      td(),
      yd(),
      Md(),
      (Z = String.fromCodePoint),
      (Q = String.raw),
      (wf = { flagGroups: !0, unicodeSets: !0 }),
      (wf.bugFlagVLiteralHyphenIsRange = wf.unicodeSets
        ? (() => {
            try {
              new RegExp(Q`[\d\-a]`, `v`);
            } catch {
              return !0;
            }
            return !1;
          })()
        : !1),
      (wf.bugNestedClassIgnoresNegation =
        wf.unicodeSets && RegExp(`[[^a]]`, `v`).test(`a`)),
      (Tf = { ES2025: 2025, ES2024: 2024, ES2018: 2018 }),
      (Ef = {
        auto: `auto`,
        ES2025: `ES2025`,
        ES2024: `ES2024`,
        ES2018: `ES2018`,
      }),
      (Df = `[	-\r ]`),
      (Of = new Set([Z(304), Z(305)])),
      (kf = Q`[\p{L}\p{M}\p{N}\p{Pc}]`),
      (Af = new Map(
        `C Other
Cc Control cntrl
Cf Format
Cn Unassigned
Co Private_Use
Cs Surrogate
L Letter
LC Cased_Letter
Ll Lowercase_Letter
Lm Modifier_Letter
Lo Other_Letter
Lt Titlecase_Letter
Lu Uppercase_Letter
M Mark Combining_Mark
Mc Spacing_Mark
Me Enclosing_Mark
Mn Nonspacing_Mark
N Number
Nd Decimal_Number digit
Nl Letter_Number
No Other_Number
P Punctuation punct
Pc Connector_Punctuation
Pd Dash_Punctuation
Pe Close_Punctuation
Pf Final_Punctuation
Pi Initial_Punctuation
Po Other_Punctuation
Ps Open_Punctuation
S Symbol
Sc Currency_Symbol
Sk Modifier_Symbol
Sm Math_Symbol
So Other_Symbol
Z Separator
Zl Line_Separator
Zp Paragraph_Separator
Zs Space_Separator
ASCII
ASCII_Hex_Digit AHex
Alphabetic Alpha
Any
Assigned
Bidi_Control Bidi_C
Bidi_Mirrored Bidi_M
Case_Ignorable CI
Cased
Changes_When_Casefolded CWCF
Changes_When_Casemapped CWCM
Changes_When_Lowercased CWL
Changes_When_NFKC_Casefolded CWKCF
Changes_When_Titlecased CWT
Changes_When_Uppercased CWU
Dash
Default_Ignorable_Code_Point DI
Deprecated Dep
Diacritic Dia
Emoji
Emoji_Component EComp
Emoji_Modifier EMod
Emoji_Modifier_Base EBase
Emoji_Presentation EPres
Extended_Pictographic ExtPict
Extender Ext
Grapheme_Base Gr_Base
Grapheme_Extend Gr_Ext
Hex_Digit Hex
IDS_Binary_Operator IDSB
IDS_Trinary_Operator IDST
ID_Continue IDC
ID_Start IDS
Ideographic Ideo
Join_Control Join_C
Logical_Order_Exception LOE
Lowercase Lower
Math
Noncharacter_Code_Point NChar
Pattern_Syntax Pat_Syn
Pattern_White_Space Pat_WS
Quotation_Mark QMark
Radical
Regional_Indicator RI
Sentence_Terminal STerm
Soft_Dotted SD
Terminal_Punctuation Term
Unified_Ideograph UIdeo
Uppercase Upper
Variation_Selector VS
White_Space space
XID_Continue XIDC
XID_Start XIDS`
          .split(/\s/)
          .map((e) => [Ju(e), e]),
      )),
      (jf = new Map([
        [`s`, Z(383)],
        [Z(383), `s`],
      ])),
      (Mf = new Map([
        [Z(223), Z(7838)],
        [Z(107), Z(8490)],
        [Z(229), Z(8491)],
        [Z(969), Z(8486)],
      ])),
      (Nf = new Map([
        Bd(453),
        Bd(456),
        Bd(459),
        Bd(498),
        ...Vd(8072, 8079),
        ...Vd(8088, 8095),
        ...Vd(8104, 8111),
        Bd(8124),
        Bd(8140),
        Bd(8188),
      ])),
      (Pf = new Map([
        [`alnum`, Q`[\p{Alpha}\p{Nd}]`],
        [`alpha`, Q`\p{Alpha}`],
        [`ascii`, Q`\p{ASCII}`],
        [`blank`, Q`[\p{Zs}\t]`],
        [`cntrl`, Q`\p{Cc}`],
        [`digit`, Q`\p{Nd}`],
        [`graph`, Q`[\P{space}&&\P{Cc}&&\P{Cn}&&\P{Cs}]`],
        [`lower`, Q`\p{Lower}`],
        [`print`, Q`[[\P{space}&&\P{Cc}&&\P{Cn}&&\P{Cs}]\p{Zs}]`],
        [`punct`, Q`[\p{P}\p{S}]`],
        [`space`, Q`\p{space}`],
        [`upper`, Q`\p{Upper}`],
        [`word`, Q`[\p{Alpha}\p{M}\p{Nd}\p{Pc}]`],
        [`xdigit`, Q`\p{AHex}`],
      ])),
      (Ff = new Set([
        `Lower`,
        `Lowercase`,
        `Upper`,
        `Uppercase`,
        `Ll`,
        `Lowercase_Letter`,
        `Lt`,
        `Titlecase_Letter`,
        `Lu`,
        `Uppercase_Letter`,
      ])),
      (If = {
        AbsenceFunction({ node: e, parent: t, replaceWith: n }) {
          let { body: r, kind: i } = e;
          if (i === `repeater`) {
            let e = Nu();
            e.body[0].body.push(Pu({ negate: !0, body: r }), Bu(`Any`));
            let i = Nu();
            (i.body[0].body.push(Lu(`greedy`, 0, 1 / 0, e)),
              n(X(i, t), { traverse: !0 }));
          } else throw Error(`Unsupported absence function "(?~|"`);
        },
        Alternative: {
          enter({ node: e, parent: t, key: n }, { flagDirectivesByAlt: r }) {
            let i = e.body.filter((e) => e.kind === `flags`);
            for (let e = n + 1; e < t.body.length; e++) {
              let n = t.body[e];
              Pd(r, n, []).push(...i);
            }
          },
          exit({ node: e }, { flagDirectivesByAlt: t }) {
            if (t.get(e)?.length) {
              let n = Xd(t.get(e));
              if (n) {
                let t = Nu({ flags: n });
                ((t.body[0].body = e.body), (e.body = [X(t, e)]));
              }
            }
          },
        },
        Assertion(
          {
            node: e,
            parent: t,
            key: n,
            container: r,
            root: i,
            remove: a,
            replaceWith: o,
          },
          s,
        ) {
          let { kind: c, negate: l } = e,
            {
              asciiWordBoundaries: u,
              avoidSubclass: d,
              supportedGNodes: f,
              wordIsAscii: p,
            } = s;
          if (c === `text_segment_boundary`)
            throw Error(
              `Unsupported text segment boundary "\\${l ? `Y` : `y`}"`,
            );
          if (c === `line_end`)
            o(
              X(
                Pu({
                  body: [
                    Cu({ body: [wu(`string_end`)] }),
                    Cu({ body: [Du(10)] }),
                  ],
                }),
                t,
              ),
            );
          else if (c === `line_start`)
            o(X(of(Q`(?<=\A|\n(?!\z))`, { skipLookbehindValidation: !0 }), t));
          else if (c === `search_start`)
            if (f.has(e)) ((i.flags.sticky = !0), a());
            else {
              let e = r[n - 1];
              if (e && nf(e)) o(X(Pu({ negate: !0 }), t));
              else if (d)
                throw Error(Q`Uses "\G" in a way that requires a subclass`);
              else (o(cf(wu(`string_start`), t)), (s.strategy = `clip_search`));
            }
          else if (!(c === `string_end` || c === `string_start`))
            if (c === `string_end_newline`) o(X(of(Q`(?=\n?\z)`), t));
            else if (c === `word_boundary`) {
              if (!p && !u) {
                let e = `(?:(?<=${kf})(?!${kf})|(?<!${kf})(?=${kf}))`,
                  n = `(?:(?<=${kf})(?=${kf})|(?<!${kf})(?!${kf}))`;
                o(X(of(l ? n : e), t));
              }
            } else throw Error(`Unexpected assertion kind "${c}"`);
        },
        Backreference({ node: e }, { jsGroupNameMap: t }) {
          let { ref: n } = e;
          typeof n == `string` && !af(n) && ((n = Yd(n, t)), (e.ref = n));
        },
        CapturingGroup(
          { node: e },
          { jsGroupNameMap: t, subroutineRefMap: n },
        ) {
          let { name: r } = e;
          (r && !af(r) && ((r = Yd(r, t)), (e.name = r)),
            n.set(e.number, e),
            r && n.set(r, e));
        },
        CharacterClassRange({ node: e, parent: t, replaceWith: n }) {
          t.kind === `intersection` &&
            n(X(Ou({ body: [e] }), t), { traverse: !0 });
        },
        CharacterSet(
          { node: e, parent: t, replaceWith: n },
          {
            accuracy: r,
            minTargetEs2024: i,
            digitIsAscii: a,
            spaceIsAscii: o,
            wordIsAscii: s,
          },
        ) {
          let { kind: c, negate: l, value: u } = e;
          if (a && (c === `digit` || u === `digit`)) {
            n(cf(Au(`digit`, { negate: l }), t));
            return;
          }
          if (o && (c === `space` || u === `space`)) {
            n(X(sf(of(Df), l), t));
            return;
          }
          if (s && (c === `word` || u === `word`)) {
            n(cf(Au(`word`, { negate: l }), t));
            return;
          }
          if (c === `any`) n(cf(Bu(`Any`), t));
          else if (c === `digit`) n(cf(Bu(`Nd`, { negate: l }), t));
          else if (c !== `dot`)
            if (c === `text_segment`) {
              if (r === `strict`)
                throw Error(Q`Use of "\X" requires non-strict accuracy`);
              let e = `\\p{Emoji}(?:\\p{EMod}|\\uFE0F\\u20E3?|[\\x{E0020}-\\x{E007E}]+\\x{E007F})?`,
                a = Q`\p{RI}{2}|${e}(?:\u200D${e})*`;
              n(
                X(
                  of(Q`(?>\r\n|${i ? Q`\p{RGI_Emoji}` : a}|\P{M}\p{M}*)`, {
                    skipPropertyNameValidation: !0,
                  }),
                  t,
                ),
              );
            } else if (c === `hex`) n(cf(Bu(`AHex`, { negate: l }), t));
            else if (c === `newline`)
              n(
                X(
                  of(
                    l
                      ? `[^
]`
                      : `(?>\r
?|[
\v\f\u2028\u2029])`,
                  ),
                  t,
                ),
              );
            else if (c === `posix`)
              if (!i && (u === `graph` || u === `print`)) {
                if (r === `strict`)
                  throw Error(
                    `POSIX class "${u}" requires min target ES2024 or non-strict accuracy`,
                  );
                let e = { graph: `!-~`, print: ` -~` }[u];
                (l &&
                  (e = `\0-${Z(e.codePointAt(0) - 1)}${Z(e.codePointAt(2) + 1)}-\u{10FFFF}`),
                  n(X(of(`[${e}]`), t)));
              } else n(X(sf(of(Pf.get(u)), l), t));
            else if (c === `property`) Af.has(Ju(u)) || (e.key = `sc`);
            else if (c === `space`) n(cf(Bu(`space`, { negate: l }), t));
            else if (c === `word`) n(X(sf(of(kf), l), t));
            else throw Error(`Unexpected character set kind "${c}"`);
        },
        Directive({
          node: e,
          parent: t,
          root: n,
          remove: r,
          replaceWith: i,
          removeAllPrevSiblings: a,
          removeAllNextSiblings: o,
        }) {
          let { kind: s, flags: c } = e;
          if (s === `flags`)
            if (!c.enable && !c.disable) r();
            else {
              let e = Nu({ flags: c });
              ((e.body[0].body = o()), i(X(e, t), { traverse: !0 }));
            }
          else if (s === `keep`) {
            let e = n.body[0],
              r =
                n.body.length === 1 &&
                lu(e, { type: `Group` }) &&
                e.body[0].body.length === 1
                  ? e.body[0]
                  : n;
            if (t.parent !== r || r.body.length > 1)
              throw Error(Q`Uses "\K" in a way that's unsupported`);
            let o = Pu({ behind: !0 });
            ((o.body[0].body = a()), i(X(o, t)));
          } else throw Error(`Unexpected directive kind "${s}"`);
        },
        Flags({ node: e, parent: t }) {
          if (e.posixIsAscii) throw Error(`Unsupported flag "P"`);
          if (e.textSegmentMode === `word`)
            throw Error(`Unsupported flag "y{w}"`);
          ([
            `digitIsAscii`,
            `extended`,
            `posixIsAscii`,
            `spaceIsAscii`,
            `wordIsAscii`,
            `textSegmentMode`,
          ].forEach((t) => delete e[t]),
            Object.assign(e, {
              global: !1,
              hasIndices: !1,
              multiline: !1,
              sticky: e.sticky ?? !1,
            }),
            (t.options = { disable: { x: !0, n: !0 }, force: { v: !0 } }));
        },
        Group({ node: e }) {
          if (!e.flags) return;
          let { enable: t, disable: n } = e.flags;
          (t?.extended && delete t.extended,
            n?.extended && delete n.extended,
            t?.dotAll && n?.dotAll && delete t.dotAll,
            t?.ignoreCase && n?.ignoreCase && delete t.ignoreCase,
            t && !Object.keys(t).length && delete e.flags.enable,
            n && !Object.keys(n).length && delete e.flags.disable,
            !e.flags.enable && !e.flags.disable && delete e.flags);
        },
        LookaroundAssertion({ node: e }, t) {
          let { kind: n } = e;
          n === `lookbehind` && (t.passedLookbehind = !0);
        },
        NamedCallout({ node: e, parent: t, replaceWith: n }) {
          let { kind: r } = e;
          if (r === `fail`) n(X(Pu({ negate: !0 }), t));
          else throw Error(`Unsupported named callout "(*${r.toUpperCase()}"`);
        },
        Quantifier({ node: e }) {
          if (e.body.type === `Quantifier`) {
            let t = Nu();
            (t.body[0].body.push(e.body), (e.body = X(t, e)));
          }
        },
        Regex: {
          enter({ node: e }, { supportedGNodes: t }) {
            let n = [],
              r = !1,
              i = !1;
            for (let t of e.body)
              if (t.body.length === 1 && t.body[0].kind === `search_start`)
                t.body.pop();
              else {
                let e = $d(t.body);
                e
                  ? ((r = !0), Array.isArray(e) ? n.push(...e) : n.push(e))
                  : (i = !0);
              }
            r && !i && n.forEach((e) => t.add(e));
          },
          exit(e, { accuracy: t, passedLookbehind: n, strategy: r }) {
            if (t === `strict` && n && r)
              throw Error(
                Q`Uses "\G" in a way that requires non-strict accuracy`,
              );
          },
        },
        Subroutine({ node: e }, { jsGroupNameMap: t }) {
          let { ref: n } = e;
          typeof n == `string` && !af(n) && ((n = Yd(n, t)), (e.ref = n));
        },
      }),
      (Lf = {
        Backreference(
          { node: e },
          { multiplexCapturesToLeftByRef: t, reffedNodesByReferencer: n },
        ) {
          let { orphan: r, ref: i } = e;
          r || n.set(e, [...t.get(i).map(({ node: e }) => e)]);
        },
        CapturingGroup: {
          enter(
            { node: e, parent: t, replaceWith: n, skip: r },
            {
              groupOriginByCopy: i,
              groupsByName: a,
              multiplexCapturesToLeftByRef: o,
              openRefs: s,
              reffedNodesByReferencer: c,
            },
          ) {
            let l = i.get(e);
            if (l && s.has(e.number)) {
              let r = cf(qd(e.number), t);
              (c.set(r, s.get(e.number)), n(r));
              return;
            }
            (s.set(e.number, e),
              o.set(e.number, []),
              e.name && Pd(o, e.name, []));
            let u = o.get(e.name ?? e.number);
            for (let t = 0; t < u.length; t++) {
              let n = u[t];
              if (l === n.node || (l && l === n.origin) || e === n.origin) {
                u.splice(t, 1);
                break;
              }
            }
            if (
              (o.get(e.number).push({ node: e, origin: l }),
              e.name && o.get(e.name).push({ node: e, origin: l }),
              e.name)
            ) {
              let t = Pd(a, e.name, new Map()),
                n = !1;
              if (l) n = !0;
              else
                for (let e of t.values())
                  if (!e.hasDuplicateNameToRemove) {
                    n = !0;
                    break;
                  }
              a.get(e.name).set(e, { node: e, hasDuplicateNameToRemove: n });
            }
          },
          exit({ node: e }, { openRefs: t }) {
            t.delete(e.number);
          },
        },
        Group: {
          enter({ node: e }, t) {
            ((t.prevFlags = t.currentFlags),
              e.flags && (t.currentFlags = Nd(t.currentFlags, e.flags)));
          },
          exit(e, t) {
            t.currentFlags = t.prevFlags;
          },
        },
        Subroutine({ node: e, parent: t, replaceWith: n }, r) {
          let { isRecursive: i, ref: a } = e;
          if (i) {
            let n = t;
            for (
              ;
              (n = n.parent) &&
              !(
                n.type === `CapturingGroup` &&
                (n.name === a || n.number === a)
              );
            );
            r.reffedNodesByReferencer.set(e, n);
            return;
          }
          let o = r.subroutineRefMap.get(a),
            s = a === 0,
            c = s ? qd(0) : Kd(o, r.groupOriginByCopy, null),
            l = c;
          if (!s) {
            let e = Xd(Jd(o, (e) => e.type === `Group` && !!e.flags)),
              t = e ? Nd(r.globalFlags, e) : r.globalFlags;
            Wd(t, r.currentFlags) ||
              ((l = Nu({ flags: Zd(t) })), l.body[0].body.push(c));
          }
          n(X(l, t), { traverse: !s });
        },
      }),
      (Rf = {
        Backreference({ node: e, parent: t, replaceWith: n }, r) {
          if (e.orphan) {
            r.highestOrphanBackref = Math.max(r.highestOrphanBackref, e.ref);
            return;
          }
          let i = r.reffedNodesByReferencer.get(e).filter((t) => Gd(t, e));
          i.length
            ? i.length > 1
              ? n(
                  X(
                    Nu({
                      atomic: !0,
                      body: i
                        .reverse()
                        .map((e) => Cu({ body: [Tu(e.number)] })),
                    }),
                    t,
                  ),
                )
              : (e.ref = i[0].number)
            : n(X(Pu({ negate: !0 }), t));
        },
        CapturingGroup({ node: e }, t) {
          ((e.number = ++t.numCapturesToLeft),
            e.name &&
              t.groupsByName.get(e.name).get(e).hasDuplicateNameToRemove &&
              delete e.name);
        },
        Regex: {
          exit({ node: e }, t) {
            let n = Math.max(t.highestOrphanBackref - t.numCapturesToLeft, 0);
            for (let t = 0; t < n; t++) {
              let t = Eu();
              e.body.at(-1).body.push(t);
            }
          },
        },
        Subroutine({ node: e }, t) {
          !e.isRecursive ||
            e.ref === 0 ||
            (e.ref = t.reffedNodesByReferencer.get(e).number);
        },
      }),
      (zf = {
        "*": {
          enter({ node: e }, t) {
            if (gf(e)) {
              let n = t.getCurrentModI();
              t.pushModI(
                e.flags ? Nd({ ignoreCase: n }, e.flags).ignoreCase : n,
              );
            }
          },
          exit({ node: e }, t) {
            gf(e) && t.popModI();
          },
        },
        Backreference(e, t) {
          t.setHasCasedChar();
        },
        Character({ node: e }, t) {
          uf(Z(e.value)) && t.setHasCasedChar();
        },
        CharacterClassRange({ node: e, skip: t }, n) {
          (t(), df(e, { firstOnly: !0 }).length && n.setHasCasedChar());
        },
        CharacterSet({ node: e }, t) {
          e.kind === `property` && Ff.has(e.value) && t.setHasCasedChar();
        },
      }),
      (Bf = {
        Alternative({ body: e }, t, n) {
          return e.map(n).join(``);
        },
        Assertion({ kind: e, negate: t }) {
          if (e === `string_end`) return `$`;
          if (e === `string_start`) return `^`;
          if (e === `word_boundary`) return t ? Q`\B` : Q`\b`;
          throw Error(`Unexpected assertion kind "${e}"`);
        },
        Backreference({ ref: e }, t) {
          if (typeof e != `number`)
            throw Error(`Unexpected named backref in transformed AST`);
          if (
            !t.useFlagMods &&
            t.accuracy === `strict` &&
            t.currentFlags.ignoreCase &&
            !t.captureMap.get(e).ignoreCase
          )
            throw Error(
              `Use of case-insensitive backref to case-sensitive group requires target ES2025 or non-strict accuracy`,
            );
          return `\\` + e;
        },
        CapturingGroup(e, t, n) {
          let { body: r, name: i, number: a } = e,
            o = { ignoreCase: t.currentFlags.ignoreCase },
            s = t.originMap.get(e);
          return (
            s && ((o.hidden = !0), a > s.number && (o.transferTo = s.number)),
            t.captureMap.set(a, o),
            `(${i ? `?<${i}>` : ``}${r.map(n).join(`|`)})`
          );
        },
        Character({ value: e }, t) {
          let n = Z(e),
            r = ff(e, {
              escDigit: t.lastNode.type === `Backreference`,
              inCharClass: t.inCharClass,
              useFlagV: t.useFlagV,
            });
          if (r !== n) return r;
          if (t.useAppliedIgnoreCase && t.currentFlags.ignoreCase && uf(n)) {
            let e = Rd(n);
            return t.inCharClass
              ? e.join(``)
              : e.length > 1
                ? `[${e.join(``)}]`
                : e[0];
          }
          return n;
        },
        CharacterClass(e, t, n) {
          let { kind: r, negate: i, parent: a } = e,
            { body: o } = e;
          if (r === `intersection` && !t.useFlagV)
            throw Error(
              `Use of character class intersection requires min target ES2024`,
            );
          wf.bugFlagVLiteralHyphenIsRange &&
            t.useFlagV &&
            o.some(vf) &&
            (o = [Du(45), ...o.filter((e) => !vf(e))]);
          let s = () =>
            `[${i ? `^` : ``}${o.map(n).join(r === `intersection` ? `&&` : ``)}]`;
          if (!t.inCharClass) {
            if ((!t.useFlagV || wf.bugNestedClassIgnoresNegation) && !i) {
              let t = o.filter(
                (e) =>
                  e.type === `CharacterClass` && e.kind === `union` && e.negate,
              );
              if (t.length) {
                let r = Nu(),
                  i = r.body[0];
                return (
                  (r.parent = a),
                  (i.parent = r),
                  (o = o.filter((e) => !t.includes(e))),
                  (e.body = o),
                  o.length ? ((e.parent = i), i.body.push(e)) : r.body.pop(),
                  t.forEach((e) => {
                    let t = Cu({ body: [e] });
                    ((e.parent = t), (t.parent = r), r.body.push(t));
                  }),
                  n(r)
                );
              }
            }
            t.inCharClass = !0;
            let r = s();
            return ((t.inCharClass = !1), r);
          }
          let c = o[0];
          if (
            r === `union` &&
            !i &&
            c &&
            (((!t.useFlagV || !t.verbose) &&
              a.kind === `union` &&
              !(wf.bugFlagVLiteralHyphenIsRange && t.useFlagV)) ||
              (!t.verbose &&
                a.kind === `intersection` &&
                o.length === 1 &&
                c.type !== `CharacterClassRange`))
          )
            return o.map(n).join(``);
          if (!t.useFlagV && a.type === `CharacterClass`)
            throw Error(
              `Uses nested character class in a way that requires min target ES2024`,
            );
          return s();
        },
        CharacterClassRange(e, t) {
          let n = e.min.value,
            r = e.max.value,
            i = { escDigit: !1, inCharClass: !0, useFlagV: t.useFlagV },
            a = ff(n, i),
            o = ff(r, i),
            s = new Set();
          return (
            t.useAppliedIgnoreCase &&
              t.currentFlags.ignoreCase &&
              pf(df(e)).forEach((e) => {
                s.add(
                  Array.isArray(e) ? `${ff(e[0], i)}-${ff(e[1], i)}` : ff(e, i),
                );
              }),
            `${a}-${o}${[...s].join(``)}`
          );
        },
        CharacterSet({ kind: e, negate: t, value: n, key: r }, i) {
          if (e === `dot`)
            return i.currentFlags.dotAll
              ? i.appliedGlobalFlags.dotAll || i.useFlagMods
                ? `.`
                : `[^]`
              : Q`[^\n]`;
          if (e === `digit`) return t ? Q`\D` : Q`\d`;
          if (e === `property`) {
            if (
              i.useAppliedIgnoreCase &&
              i.currentFlags.ignoreCase &&
              Ff.has(n)
            )
              throw Error(
                `Unicode property "${n}" can't be case-insensitive when other chars have specific case`,
              );
            return `${t ? Q`\P` : Q`\p`}{${r ? `${r}=` : ``}${n}}`;
          }
          if (e === `word`) return t ? Q`\W` : Q`\w`;
          throw Error(`Unexpected character set kind "${e}"`);
        },
        Flags(e, t) {
          return (
            (t.appliedGlobalFlags.ignoreCase ? `i` : ``) +
            (e.dotAll ? `s` : ``) +
            (e.sticky ? `y` : ``)
          );
        },
        Group({ atomic: e, body: t, flags: n, parent: r }, i, a) {
          let o = i.currentFlags;
          n && (i.currentFlags = Nd(o, n));
          let s = t.map(a).join(`|`),
            c =
              !i.verbose &&
              t.length === 1 &&
              r.type !== `Quantifier` &&
              !e &&
              (!i.useFlagMods || !n)
                ? s
                : `(?${mf(e, n, i.useFlagMods)}${s})`;
          return ((i.currentFlags = o), c);
        },
        LookaroundAssertion({ body: e, kind: t, negate: n }, r, i) {
          return `(?${`${t === `lookahead` ? `` : `<`}${n ? `!` : `=`}`}${e.map(i).join(`|`)})`;
        },
        Quantifier(e, t, n) {
          return n(e.body) + hf(e);
        },
        Subroutine({ isRecursive: e, ref: t }, n) {
          if (!e)
            throw Error(
              `Unexpected non-recursive subroutine in transformed AST`,
            );
          let r = n.recursionLimit;
          return t === 0 ? `(?R=${r})` : Q`\g<${t}&R=${r}>`;
        },
      }),
      (Vf = new Set([
        `$`,
        `(`,
        `)`,
        `*`,
        `+`,
        `.`,
        `?`,
        `[`,
        `\\`,
        `]`,
        `^`,
        `{`,
        `|`,
        `}`,
      ])),
      (Hf = new Set([`-`, `\\`, `]`, `^`, `[`])),
      (Uf = new Set("()-/[\\]^{|}!#$%&*+,.:;<=>?@`~".split(``))),
      (Wf = new Map([
        [9, Q`\t`],
        [10, Q`\n`],
        [11, Q`\v`],
        [12, Q`\f`],
        [13, Q`\r`],
        [8232, Q`\u2028`],
        [8233, Q`\u2029`],
        [65279, Q`\uFEFF`],
      ])),
      (Gf = /^\p{Cased}$/u),
      (Kf = class e extends RegExp {
        #e = new Map();
        #t = null;
        #n;
        #r = null;
        #i = null;
        rawOptions = {};
        get source() {
          return this.#n || `(?:)`;
        }
        constructor(t, n, r) {
          let i = !!r?.lazyCompile;
          if (t instanceof RegExp) {
            if (r) throw Error(`Cannot provide options when copying a regexp`);
            let i = t;
            (super(i, n),
              (this.#n = i.source),
              i instanceof e &&
                ((this.#e = i.#e),
                (this.#r = i.#r),
                (this.#i = i.#i),
                (this.rawOptions = i.rawOptions)));
          } else {
            let e = { hiddenCaptures: [], strategy: null, transfers: [], ...r };
            (super(i ? `` : t, n),
              (this.#n = t),
              (this.#e = bf(e.hiddenCaptures, e.transfers)),
              (this.#i = e.strategy),
              (this.rawOptions = r ?? {}));
          }
          i || (this.#t = this);
        }
        exec(t) {
          if (!this.#t) {
            let { lazyCompile: t, ...n } = this.rawOptions;
            this.#t = new e(this.#n, this.flags, n);
          }
          let n = this.global || this.sticky,
            r = this.lastIndex;
          if (this.#i === `clip_search` && n && r) {
            this.lastIndex = 0;
            let e = this.#a(t.slice(r));
            return (
              e && (yf(e, r, t, this.hasIndices), (this.lastIndex += r)),
              e
            );
          }
          return this.#a(t);
        }
        #a(e) {
          this.#t.lastIndex = this.lastIndex;
          let t = super.exec.call(this.#t, e);
          if (((this.lastIndex = this.#t.lastIndex), !t || !this.#e.size))
            return t;
          let n = [...t];
          t.length = 1;
          let r;
          this.hasIndices && ((r = [...t.indices]), (t.indices.length = 1));
          let i = [0];
          for (let e = 1; e < n.length; e++) {
            let { hidden: a, transferTo: o } = this.#e.get(e) ?? {};
            if (
              (a
                ? i.push(null)
                : (i.push(t.length),
                  t.push(n[e]),
                  this.hasIndices && t.indices.push(r[e])),
              o && n[e] !== void 0)
            ) {
              let a = i[o];
              if (!a) throw Error(`Invalid capture transfer to "${a}"`);
              if (
                ((t[a] = n[e]),
                this.hasIndices && (t.indices[a] = r[e]),
                t.groups)
              ) {
                this.#r ||= xf(this.source);
                let i = this.#r.get(o);
                i &&
                  ((t.groups[i] = n[e]),
                  this.hasIndices && (t.indices.groups[i] = r[e]));
              }
            }
          }
          return t;
        }
      }));
  }),
  Jf,
  Yf,
  Xf = e(() => {
    ((Jf = 4294967295),
      (Yf = class {
        constructor(e, t = {}) {
          ((this.patterns = e), (this.options = t));
          let { forgiving: n = !1, cache: r, regexConstructor: i } = t;
          if (!i) throw Error("Option `regexConstructor` is not provided");
          this.regexps = e.map((e) => {
            if (typeof e != `string`) return e;
            let t = r?.get(e);
            if (t) {
              if (t instanceof RegExp) return t;
              if (n) return null;
              throw t;
            }
            try {
              let t = i(e);
              return (r?.set(e, t), t);
            } catch (t) {
              if ((r?.set(e, t), n)) return null;
              throw t;
            }
          });
        }
        regexps;
        findNextMatchSync(e, t, n) {
          let r = typeof e == `string` ? e : e.content,
            i = [];
          function a(e, t, n = 0) {
            return {
              index: e,
              captureIndices: t.indices.map((e) =>
                e == null
                  ? { start: Jf, end: Jf, length: 0 }
                  : { start: e[0] + n, end: e[1] + n, length: e[1] - e[0] },
              ),
            };
          }
          for (let e = 0; e < this.regexps.length; e++) {
            let n = this.regexps[e];
            if (n)
              try {
                n.lastIndex = t;
                let o = n.exec(r);
                if (!o) continue;
                if (o.index === t) return a(e, o, 0);
                i.push([e, o, 0]);
              } catch (e) {
                if (this.options.forgiving) continue;
                throw e;
              }
          }
          if (i.length) {
            let e = Math.min(...i.map((e) => e[1].index));
            for (let [t, n, r] of i) if (n.index === e) return a(t, n, r);
          }
          return null;
        }
      }));
  });
function Zf(e, t) {
  return Sf(e, {
    global: !0,
    hasIndices: !0,
    lazyCompileLength: 3e3,
    rules: {
      allowOrphanBackrefs: !0,
      asciiWordBoundaries: !0,
      captureGroup: !0,
      recursionLimit: 5,
      singleline: !0,
    },
    ...t,
  });
}
function Qf(e = {}) {
  let t = Object.assign({ target: `auto`, cache: new Map() }, e);
  return (
    (t.regexConstructor ||= (e) => Zf(e, { target: t.target })),
    {
      createScanner(e) {
        return new Yf(e, t);
      },
      createString(e) {
        return { content: e };
      },
    }
  );
}
var $f = e(() => {
    (qf(), Xf());
  }),
  ep = e(() => {
    ($f(), Xf());
  }),
  tp = e(() => {
    (_l(), ep(), cl(), Ac(), Fc(), Rc());
  });
async function np(e) {
  if (ha())
    throw Error(
      `resolveLanguage("${e}") cannot be called from a worker context. Languages must be pre-resolved on the main thread and passed to the worker via the resolvedLanguages parameter.`,
    );
  let t = sa.get(e);
  if (t != null) return t;
  try {
    let t = ca.get(e);
    if (
      (t == null && Object.prototype.hasOwnProperty.call(Pc, e) && (t = Pc[e]),
      t == null)
    )
      throw Error(
        `resolveLanguage: "${e}" not found in bundled or custom languages`,
      );
    let n = t().then(({ default: t }) => {
      let n = { name: e, data: t };
      return (oa.has(e) || oa.set(e, n), n);
    });
    return (sa.set(e, n), await n);
  } finally {
    sa.delete(e);
  }
}
var rp = e(() => {
  (ua(), ga(), tp());
});
function ip(e) {
  return oa.get(e) ?? np(e);
}
var ap = e(() => {
    (ua(), rp());
  }),
  op,
  sp = e(() => {
    op = new Set();
  });
function cp(e) {
  let t = [],
    n = new Set();
  for (let r of lp(e.themes)) {
    let e = fp(r) ? r.getThemes() : [r];
    for (let r of e) {
      if (n.has(r.name))
        throw Error(`Theme collection already contains theme "${r.name}"`);
      (n.add(r.name), t.push(r));
    }
  }
  let r = Object.freeze([...t]),
    i = Object.freeze(r.filter((e) => e.colorScheme === `light`)),
    a = Object.freeze(r.filter((e) => e.colorScheme === `dark`)),
    o = new Map(r.map((e) => [e.name, e])),
    s = Object.freeze(r.map((e) => e.name)),
    c = Object.freeze(i.map((e) => e.name)),
    l = Object.freeze(a.map((e) => e.name));
  function u(e) {
    if (e == null) return r;
    let { colorScheme: t, collection: n } = e;
    return n == null
      ? t === `light`
        ? i
        : t === `dark`
          ? a
          : r
      : r.filter((e) =>
          e.collection === n ? t == null || e.colorScheme === t : !1,
        );
  }
  return {
    getTheme(e) {
      return o.get(e);
    },
    getThemes(e) {
      return u(e);
    },
    getThemeNames(e) {
      return e?.collection == null
        ? e?.colorScheme === `light`
          ? c
          : e?.colorScheme === `dark`
            ? l
            : s
        : u(e).map((e) => e.name);
    },
    hasTheme(e) {
      return o.has(e);
    },
    orderBy(e) {
      return cp({
        themes: r
          .map((e, t) => ({ descriptor: e, index: t }))
          .sort((t, n) => {
            let r = e(t.descriptor, n.descriptor);
            return r === 0 ? t.index - n.index : r;
          })
          .map((e) => e.descriptor),
      });
    },
    pick(e) {
      let t = [],
        n = new Set();
      for (let r of e) {
        if (n.has(r))
          throw Error(`Theme collection pick already includes theme "${r}"`);
        n.add(r);
        let e = o.get(r);
        if (e == null)
          throw Error(`Theme collection does not contain theme "${r}"`);
        t.push(e);
      }
      return cp({ themes: t });
    },
    registerInto(e) {
      for (let t of r) e.registerThemeIfAbsent(t.name, t.load);
    },
  };
}
function lp(e) {
  return up(e) ? [e] : e;
}
function up(e) {
  return fp(e) || dp(e);
}
function dp(e) {
  return typeof e.name == `string` && typeof e.load == `function`;
}
function fp(e) {
  return typeof e.getThemes == `function`;
}
var pp = e(() => {});
function mp(e) {
  return typeof e == `object` && e && `default` in e ? e.default : e;
}
var hp = e(() => {});
function gp() {
  let e = new Map(),
    t = new Map(),
    n = new Map(),
    r = 0;
  function i(t, n) {
    if (e.has(t)) throw new _p(t);
    e.set(t, n);
  }
  function a(t, n) {
    return e.has(t) ? !1 : (e.set(t, n), !0);
  }
  function o(t) {
    return e.has(t);
  }
  function s(i) {
    let a = t.get(i);
    if (a !== void 0) return Promise.resolve(a);
    let o = n.get(i);
    if (o !== void 0) return o;
    let s = e.get(i);
    if (s === void 0) return Promise.reject(new vp(i));
    let c = r,
      l = s()
        .then((e) => {
          let a = mp(e);
          return (c === r && t.set(i, a), n.get(i) === l && n.delete(i), a);
        })
        .catch((e) => {
          throw (n.get(i) === l && n.delete(i), e);
        });
    return (n.set(i, l), l);
  }
  function c(e) {
    return Promise.all(e.map((e) => s(e)));
  }
  function l(e, n) {
    t.set(e, n);
  }
  function u(e) {
    for (let [t, n] of e) l(t, n);
  }
  function d(e) {
    return t.get(e);
  }
  function f(e) {
    let n = [];
    for (let r of e) {
      let e = t.get(r);
      if (e === void 0) throw new yp(r);
      n.push(e);
    }
    return n;
  }
  function p(e) {
    return t.has(e);
  }
  function m(e) {
    for (let n of e) if (!t.has(n)) return !1;
    return !0;
  }
  function h(e) {
    let n = t.get(e);
    return n === void 0 ? s(e) : n;
  }
  function g() {
    (r++, t.clear(), n.clear());
  }
  return {
    clearResolvedThemes: g,
    getResolvedOrResolveTheme: h,
    getResolvedTheme: d,
    getResolvedThemes: f,
    hasRegisteredTheme: o,
    hasResolvedTheme: p,
    hasResolvedThemes: m,
    registerTheme: i,
    registerThemeIfAbsent: a,
    resolveTheme: s,
    resolveThemes: c,
    seedResolvedTheme: l,
    seedResolvedThemes: u,
  };
}
var _p,
  vp,
  yp,
  bp = e(() => {
    (hp(),
      (_p = class extends Error {
        constructor(e) {
          (super(`Theme "${e}" is already registered`),
            (this.name = `DuplicateThemeError`));
        }
      }),
      (vp = class extends Error {
        constructor(e) {
          (super(`No loader registered for theme "${e}"`),
            (this.name = `UnregisteredThemeError`));
        }
      }),
      (yp = class extends Error {
        constructor(e) {
          (super(`Theme "${e}" has not been resolved`),
            (this.name = `UnresolvedThemeError`));
        }
      }));
  }),
  xp = e(() => {
    bp();
  }),
  Sp,
  Cp = e(() => {
    (xp(), (Sp = gp()));
  });
function wp(e, t) {
  e = Array.isArray(e) ? e : [e];
  for (let n of e) {
    let e;
    if (typeof n == `string`) {
      if (((e = Sp.getResolvedTheme(n)), e == null))
        throw Error(
          `loadResolvedThemes: ${n} is not resolved, you must resolve it before calling loadResolvedThemes`,
        );
    } else
      ((e = n),
        (n = n.name),
        Sp.getResolvedTheme(n) ?? Sp.seedResolvedTheme(n, e));
    op.has(n) || (op.add(n), t.loadThemeSync(e));
  }
}
var Tp = e(() => {
    (sp(), Cp());
  }),
  Ep = e(() => {
    Ac();
  });
function Dp({
  name: e,
  load: t,
  colorScheme: n,
  collection: r,
  displayName: i,
}) {
  return {
    name: e,
    colorScheme: n,
    collection: r,
    displayName: i,
    load: Op(t),
  };
}
function Op(e) {
  return async () => ec(mp(await e()));
}
var kp = e(() => {
  (hp(), Ep());
});
function Ap(e) {
  return Ip.has(e) ? `light` : `dark`;
}
function jp(e) {
  return Dp({
    name: e,
    collection: Mp,
    colorScheme: Ap(e),
    displayName: Lp[e],
    load: Rp[e],
  });
}
var Mp,
  Np,
  Pp,
  Fp,
  Ip,
  Lp,
  Rp,
  zp,
  Bp = e(() => {
    (pp(),
      kp(),
      d(),
      (Mp = `pierre`),
      (Np = [
        `pierre-dark`,
        `pierre-dark-soft`,
        `pierre-dark-vibrant`,
        `pierre-dark-protanopia-deuteranopia`,
        `pierre-dark-tritanopia`,
      ]),
      (Pp = [
        `pierre-light`,
        `pierre-light-soft`,
        `pierre-light-vibrant`,
        `pierre-light-protanopia-deuteranopia`,
        `pierre-light-tritanopia`,
      ]),
      (Fp = [...Pp, ...Np]),
      (Ip = new Set(Pp)),
      (Lp = {
        "pierre-dark": `Pierre Dark`,
        "pierre-dark-soft": `Pierre Dark Soft`,
        "pierre-dark-vibrant": `Pierre Dark Vibrant`,
        "pierre-dark-protanopia-deuteranopia": `Pierre Dark Protanopia & Deuteranopia`,
        "pierre-dark-tritanopia": `Pierre Dark Tritanopia`,
        "pierre-light": `Pierre Light`,
        "pierre-light-soft": `Pierre Light Soft`,
        "pierre-light-vibrant": `Pierre Light Vibrant`,
        "pierre-light-protanopia-deuteranopia": `Pierre Light Protanopia & Deuteranopia`,
        "pierre-light-tritanopia": `Pierre Light Tritanopia`,
      }),
      (Rp = {
        "pierre-dark": () =>
          u(
            () => import(`./pierre-dark-D1f1axGh.js`),
            __vite__mapDeps([375, 1]),
            import.meta.url,
          ),
        "pierre-dark-soft": () =>
          u(
            () => import(`./pierre-dark-soft-CK0OEJfm.js`),
            __vite__mapDeps([376, 1]),
            import.meta.url,
          ),
        "pierre-dark-vibrant": () =>
          u(
            () => import(`./pierre-dark-vibrant-JtxWxkjr.js`),
            __vite__mapDeps([377, 1]),
            import.meta.url,
          ),
        "pierre-dark-protanopia-deuteranopia": () =>
          u(
            () => import(`./pierre-dark-protanopia-deuteranopia-io05zghc.js`),
            __vite__mapDeps([378, 1]),
            import.meta.url,
          ),
        "pierre-dark-tritanopia": () =>
          u(
            () => import(`./pierre-dark-tritanopia-7Blk6S2W.js`),
            __vite__mapDeps([379, 1]),
            import.meta.url,
          ),
        "pierre-light": () =>
          u(
            () => import(`./pierre-light-DEv95pIt.js`),
            __vite__mapDeps([380, 1]),
            import.meta.url,
          ),
        "pierre-light-soft": () =>
          u(
            () => import(`./pierre-light-soft-TPLnnocw.js`),
            __vite__mapDeps([381, 1]),
            import.meta.url,
          ),
        "pierre-light-vibrant": () =>
          u(
            () => import(`./pierre-light-vibrant-Fd0fBJFl.js`),
            __vite__mapDeps([382, 1]),
            import.meta.url,
          ),
        "pierre-light-protanopia-deuteranopia": () =>
          u(
            () => import(`./pierre-light-protanopia-deuteranopia-BXyOu9LV.js`),
            __vite__mapDeps([383, 1]),
            import.meta.url,
          ),
        "pierre-light-tritanopia": () =>
          u(
            () => import(`./pierre-light-tritanopia-B0Toikcn.js`),
            __vite__mapDeps([384, 1]),
            import.meta.url,
          ),
      }),
      (zp = cp({ themes: Fp.map((e) => jp(e)) })));
  });
function Vp(e) {
  return Kp.has(e) ? `light` : `dark`;
}
function Hp(e) {
  return Dp({ name: e, collection: Up, colorScheme: Vp(e), load: qp[e] });
}
var Up,
  Wp,
  Gp,
  Kp,
  qp,
  Jp,
  Yp = e(() => {
    (pp(),
      kp(),
      d(),
      (Up = `shiki`),
      (Wp = [
        `ayu-light`,
        `catppuccin-latte`,
        `everforest-light`,
        `github-light`,
        `github-light-default`,
        `github-light-high-contrast`,
        `gruvbox-light-hard`,
        `gruvbox-light-medium`,
        `gruvbox-light-soft`,
        `horizon-bright`,
        `kanagawa-lotus`,
        `light-plus`,
        `material-theme-lighter`,
        `min-light`,
        `night-owl-light`,
        `one-light`,
        `rose-pine-dawn`,
        `slack-ochin`,
        `snazzy-light`,
        `solarized-light`,
        `vitesse-light`,
      ]),
      (Gp =
        `andromeeda.aurora-x.ayu-dark.ayu-mirage.catppuccin-frappe.catppuccin-macchiato.catppuccin-mocha.dark-plus.dracula.dracula-soft.everforest-dark.github-dark.github-dark-default.github-dark-dimmed.github-dark-high-contrast.gruvbox-dark-hard.gruvbox-dark-medium.gruvbox-dark-soft.horizon.houston.kanagawa-dragon.kanagawa-wave.laserwave.material-theme.material-theme-darker.material-theme-ocean.material-theme-palenight.min-dark.monokai.night-owl.nord.one-dark-pro.plastic.poimandres.red.rose-pine.rose-pine-moon.slack-dark.solarized-dark.synthwave-84.tokyo-night.vesper.vitesse-black.vitesse-dark`.split(
          `.`,
        )),
      (Kp = new Set(Wp)),
      (qp = {
        andromeeda: () =>
          u(
            () => import(`./andromeeda-BjHkedMI.js`),
            __vite__mapDeps([385, 1]),
            import.meta.url,
          ),
        "aurora-x": () =>
          u(
            () => import(`./aurora-x-CDuvSF5C.js`),
            __vite__mapDeps([386, 1]),
            import.meta.url,
          ),
        "ayu-dark": () =>
          u(
            () => import(`./ayu-dark-D4crUA5F.js`),
            __vite__mapDeps([0, 1]),
            import.meta.url,
          ),
        "ayu-light": () =>
          u(
            () => import(`./ayu-light-DFsrtA8V.js`),
            __vite__mapDeps([387, 1]),
            import.meta.url,
          ),
        "ayu-mirage": () =>
          u(
            () => import(`./ayu-mirage-Dkr7cBIo.js`),
            __vite__mapDeps([388, 1]),
            import.meta.url,
          ),
        "catppuccin-frappe": () =>
          u(
            () => import(`./catppuccin-frappe-DSdDdD8r.js`),
            __vite__mapDeps([389, 1]),
            import.meta.url,
          ),
        "catppuccin-latte": () =>
          u(
            () => import(`./catppuccin-latte-zOcFwNaN.js`),
            __vite__mapDeps([2, 1]),
            import.meta.url,
          ),
        "catppuccin-macchiato": () =>
          u(
            () => import(`./catppuccin-macchiato-BCZfgPsl.js`),
            __vite__mapDeps([390, 1]),
            import.meta.url,
          ),
        "catppuccin-mocha": () =>
          u(
            () => import(`./catppuccin-mocha-Cjutq3dD.js`),
            __vite__mapDeps([3, 1]),
            import.meta.url,
          ),
        "dark-plus": () =>
          u(
            () => import(`./dark-plus-BcK_Lxq4.js`),
            __vite__mapDeps([4, 1]),
            import.meta.url,
          ),
        dracula: () =>
          u(
            () => import(`./dracula-CkmxuQCA.js`),
            __vite__mapDeps([5, 1]),
            import.meta.url,
          ),
        "dracula-soft": () =>
          u(
            () => import(`./dracula-soft-CsaHCQck.js`),
            __vite__mapDeps([391, 1]),
            import.meta.url,
          ),
        "everforest-dark": () =>
          u(
            () => import(`./everforest-dark-DNwyXI-O.js`),
            __vite__mapDeps([6, 1]),
            import.meta.url,
          ),
        "everforest-light": () =>
          u(
            () => import(`./everforest-light-BgZujp1P.js`),
            __vite__mapDeps([7, 1]),
            import.meta.url,
          ),
        "github-dark": () =>
          u(
            () => import(`./github-dark-DzYeK2Us.js`),
            __vite__mapDeps([392, 1]),
            import.meta.url,
          ),
        "github-dark-default": () =>
          u(
            () => import(`./github-dark-default-B3U1wxKH.js`),
            __vite__mapDeps([8, 1]),
            import.meta.url,
          ),
        "github-dark-dimmed": () =>
          u(
            () => import(`./github-dark-dimmed-CTa0Kgn1.js`),
            __vite__mapDeps([393, 1]),
            import.meta.url,
          ),
        "github-dark-high-contrast": () =>
          u(
            () => import(`./github-dark-high-contrast-C9braW9T.js`),
            __vite__mapDeps([394, 1]),
            import.meta.url,
          ),
        "github-light": () =>
          u(
            () => import(`./github-light-Bp5ibGIl.js`),
            __vite__mapDeps([395, 1]),
            import.meta.url,
          ),
        "github-light-default": () =>
          u(
            () => import(`./github-light-default-xyCLROFf.js`),
            __vite__mapDeps([9, 1]),
            import.meta.url,
          ),
        "github-light-high-contrast": () =>
          u(
            () => import(`./github-light-high-contrast-B3zN_Jiy.js`),
            __vite__mapDeps([396, 1]),
            import.meta.url,
          ),
        "gruvbox-dark-hard": () =>
          u(
            () => import(`./gruvbox-dark-hard-CVhtCRxN.js`),
            __vite__mapDeps([397, 1]),
            import.meta.url,
          ),
        "gruvbox-dark-medium": () =>
          u(
            () => import(`./gruvbox-dark-medium-DzojUsna.js`),
            __vite__mapDeps([10, 1]),
            import.meta.url,
          ),
        "gruvbox-dark-soft": () =>
          u(
            () => import(`./gruvbox-dark-soft-BoMFM66L.js`),
            __vite__mapDeps([398, 1]),
            import.meta.url,
          ),
        "gruvbox-light-hard": () =>
          u(
            () => import(`./gruvbox-light-hard-yIR7_agU.js`),
            __vite__mapDeps([399, 1]),
            import.meta.url,
          ),
        "gruvbox-light-medium": () =>
          u(
            () => import(`./gruvbox-light-medium-DLCqPW_M.js`),
            __vite__mapDeps([11, 1]),
            import.meta.url,
          ),
        "gruvbox-light-soft": () =>
          u(
            () => import(`./gruvbox-light-soft-D_Lt_2kp.js`),
            __vite__mapDeps([400, 1]),
            import.meta.url,
          ),
        horizon: () =>
          u(
            () => import(`./horizon-BI4McSDO.js`),
            __vite__mapDeps([401, 1]),
            import.meta.url,
          ),
        "horizon-bright": () =>
          u(
            () => import(`./horizon-bright-B-BSNqvV.js`),
            __vite__mapDeps([402, 1]),
            import.meta.url,
          ),
        houston: () =>
          u(
            () => import(`./houston-C60H8_Lv.js`),
            __vite__mapDeps([403, 1]),
            import.meta.url,
          ),
        "kanagawa-dragon": () =>
          u(
            () => import(`./kanagawa-dragon-CNKo0Sep.js`),
            __vite__mapDeps([404, 1]),
            import.meta.url,
          ),
        "kanagawa-lotus": () =>
          u(
            () => import(`./kanagawa-lotus-B1CXMJRL.js`),
            __vite__mapDeps([405, 1]),
            import.meta.url,
          ),
        "kanagawa-wave": () =>
          u(
            () => import(`./kanagawa-wave-BlH46o84.js`),
            __vite__mapDeps([406, 1]),
            import.meta.url,
          ),
        laserwave: () =>
          u(
            () => import(`./laserwave-Bt9rc1h_.js`),
            __vite__mapDeps([407, 1]),
            import.meta.url,
          ),
        "light-plus": () =>
          u(
            () => import(`./light-plus-HJjakdiH.js`),
            __vite__mapDeps([12, 1]),
            import.meta.url,
          ),
        "material-theme": () =>
          u(
            () => import(`./material-theme-BeT-D04D.js`),
            __vite__mapDeps([408, 1]),
            import.meta.url,
          ),
        "material-theme-darker": () =>
          u(
            () => import(`./material-theme-darker-CPmP8lV-.js`),
            __vite__mapDeps([13, 1]),
            import.meta.url,
          ),
        "material-theme-lighter": () =>
          u(
            () => import(`./material-theme-lighter-rGvkHBph.js`),
            __vite__mapDeps([409, 1]),
            import.meta.url,
          ),
        "material-theme-ocean": () =>
          u(
            () => import(`./material-theme-ocean-BcMG6ksT.js`),
            __vite__mapDeps([410, 1]),
            import.meta.url,
          ),
        "material-theme-palenight": () =>
          u(
            () => import(`./material-theme-palenight-DSd1sHBQ.js`),
            __vite__mapDeps([411, 1]),
            import.meta.url,
          ),
        "min-dark": () =>
          u(
            () => import(`./min-dark-Cj3j3fK8.js`),
            __vite__mapDeps([412, 1]),
            import.meta.url,
          ),
        "min-light": () =>
          u(
            () => import(`./min-light-BejKsFnZ.js`),
            __vite__mapDeps([413, 1]),
            import.meta.url,
          ),
        monokai: () =>
          u(
            () => import(`./monokai-DpEr2NbN.js`),
            __vite__mapDeps([14, 1]),
            import.meta.url,
          ),
        "night-owl": () =>
          u(
            () => import(`./night-owl-BcTDS1K4.js`),
            __vite__mapDeps([15, 1]),
            import.meta.url,
          ),
        "night-owl-light": () =>
          u(
            () => import(`./night-owl-light-D0d-QV9l.js`),
            __vite__mapDeps([414, 1]),
            import.meta.url,
          ),
        nord: () =>
          u(
            () => import(`./nord-C0MzpZym.js`),
            __vite__mapDeps([16, 1]),
            import.meta.url,
          ),
        "one-dark-pro": () =>
          u(
            () => import(`./one-dark-pro-FzVQBXsn.js`),
            __vite__mapDeps([17, 1]),
            import.meta.url,
          ),
        "one-light": () =>
          u(
            () => import(`./one-light-DHYBffPd.js`),
            __vite__mapDeps([18, 1]),
            import.meta.url,
          ),
        plastic: () =>
          u(
            () => import(`./plastic-qd5GJRza.js`),
            __vite__mapDeps([415, 1]),
            import.meta.url,
          ),
        poimandres: () =>
          u(
            () => import(`./poimandres-DsFFYPV2.js`),
            __vite__mapDeps([416, 1]),
            import.meta.url,
          ),
        red: () =>
          u(
            () => import(`./red-BpkevTrS.js`),
            __vite__mapDeps([417, 1]),
            import.meta.url,
          ),
        "rose-pine": () =>
          u(
            () => import(`./rose-pine-CZuangkl.js`),
            __vite__mapDeps([418, 1]),
            import.meta.url,
          ),
        "rose-pine-dawn": () =>
          u(
            () => import(`./rose-pine-dawn-B9YdL2fA.js`),
            __vite__mapDeps([19, 1]),
            import.meta.url,
          ),
        "rose-pine-moon": () =>
          u(
            () => import(`./rose-pine-moon-CUQmuvpy.js`),
            __vite__mapDeps([20, 1]),
            import.meta.url,
          ),
        "slack-dark": () =>
          u(
            () => import(`./slack-dark-C2q7QxSE.js`),
            __vite__mapDeps([419, 1]),
            import.meta.url,
          ),
        "slack-ochin": () =>
          u(
            () => import(`./slack-ochin-D26VOPqZ.js`),
            __vite__mapDeps([420, 1]),
            import.meta.url,
          ),
        "snazzy-light": () =>
          u(
            () => import(`./snazzy-light-BgO3kAsz.js`),
            __vite__mapDeps([421, 1]),
            import.meta.url,
          ),
        "solarized-dark": () =>
          u(
            () => import(`./solarized-dark-C1VH3Rq7.js`),
            __vite__mapDeps([21, 1]),
            import.meta.url,
          ),
        "solarized-light": () =>
          u(
            () => import(`./solarized-light-Ccw5Eg4G.js`),
            __vite__mapDeps([22, 1]),
            import.meta.url,
          ),
        "synthwave-84": () =>
          u(
            () => import(`./synthwave-84-CbTL_48L.js`),
            __vite__mapDeps([422, 1]),
            import.meta.url,
          ),
        "tokyo-night": () =>
          u(
            () => import(`./tokyo-night-BQQ8aTW5.js`),
            __vite__mapDeps([23, 1]),
            import.meta.url,
          ),
        vesper: () =>
          u(
            () => import(`./vesper-DZQUcIy4.js`),
            __vite__mapDeps([423, 1]),
            import.meta.url,
          ),
        "vitesse-black": () =>
          u(
            () => import(`./vitesse-black-BsJLk9y4.js`),
            __vite__mapDeps([424, 1]),
            import.meta.url,
          ),
        "vitesse-dark": () =>
          u(
            () => import(`./vitesse-dark-Cogd6TIZ.js`),
            __vite__mapDeps([425, 1]),
            import.meta.url,
          ),
        "vitesse-light": () =>
          u(
            () => import(`./vitesse-light-DeTfENL4.js`),
            __vite__mapDeps([426, 1]),
            import.meta.url,
          ),
      }),
      (Jp = cp({
        themes: Object.freeze([
          ...Wp.map((e) => Hp(e)),
          ...Gp.map((e) => Hp(e)),
        ]),
      })));
  }),
  Xp = e(() => {
    (pp(), kp(), Bp(), Yp(), cp({ themes: [zp, Jp] }));
  });
function Zp(e) {
  if (ha())
    throw Error(
      `Theme "${e}" cannot be resolved from a worker context. Themes must be pre-resolved on the main thread and passed to the worker via the resolvedLanguages parameter.`,
    );
  if (Sp.hasRegisteredTheme(e)) return;
  let t = Jp.getTheme(e);
  if (t != null) {
    Sp.registerThemeIfAbsent(t.name, t.load);
    return;
  }
  throw Error(`No valid theme loader registered for "${e}"`);
}
function Qp(e, t) {
  if (t.name !== e)
    throw Error(
      `resolvedTheme: themeName: ${e} does not match theme.name: ${t.name}`,
    );
}
var $p = e(() => {
  (ga(), Cp(), Xp());
});
async function em(e) {
  Zp(e);
  let t = await Sp.resolveTheme(e);
  return (Qp(e, t), t);
}
var tm = e(() => {
  (Cp(), $p());
});
function nm(e) {
  return Sp.getResolvedTheme(e) ?? em(e);
}
var rm = e(() => {
  (Cp(), tm());
});
async function im({
  themes: e,
  langs: t,
  preferredHighlighter: n = `shiki-js`,
}) {
  sm ??= ll({
    themes: [],
    langs: [`text`],
    engine:
      n === `shiki-wasm`
        ? Qc(
            u(
              () => import(`./wasm-CAhMFZE6.js`),
              __vite__mapDeps([374, 1]),
              import.meta.url,
            ),
          )
        : Qf(),
  });
  let r = om(sm) ? await sm : sm;
  sm = r;
  let i = [];
  for (let e of t) {
    if (e === `text` || e === `ansi`) continue;
    let t = ip(e);
    `then` in t ? i.push(t) : pa(t, r);
  }
  let a = [];
  for (let t of e) {
    let e = nm(t);
    `then` in e ? a.push(e) : wp(e, sm);
  }
  return (
    (i.length > 0 || a.length > 0) &&
      (await Promise.all([
        Promise.all(i).then((e) => {
          pa(e, r);
        }),
        Promise.all(a).then((e) => {
          wp(e, r);
        }),
      ])),
    r
  );
}
function am() {
  if (sm != null && !(`then` in sm)) return sm;
}
function om(e = sm) {
  return e != null && `then` in e;
}
var sm,
  cm = e(() => {
    (ma(), ap(), Cp(), Tp(), rm(), tp(), Xp(), d());
    for (let e of zp.getThemes()) Sp.registerThemeIfAbsent(e.name, e.load);
  });
function lm(e = P) {
  let t = [];
  return (
    typeof e == `string` ? t.push(e) : (t.push(e.dark), t.push(e.light)),
    t
  );
}
var um = e(() => {
  F();
});
function dm(e) {
  for (let t of lm(e)) if (!op.has(t)) return !1;
  return !0;
}
var fm = e(() => {
  (sp(), um());
});
function pm(e) {
  return Sp.hasResolvedThemes(e);
}
var mm = e(() => {
  Cp();
});
function hm(e, t) {
  return (
    en(e.theme, t.theme) &&
    e.useTokenTransformer === t.useTokenTransformer &&
    e.tokenizeMaxLineLength === t.tokenizeMaxLineLength
  );
}
var gm = e(() => {
  tn();
});
function _m(e, t) {
  return e == null || t == null
    ? e === t
    : e.startingLine === t.startingLine &&
        e.totalLines === t.totalLines &&
        e.bufferBefore === t.bufferBefore &&
        e.bufferAfter === t.bufferAfter;
}
var vm = e(() => {});
function ym(e) {
  return I({
    tagName: `div`,
    children: [
      I({
        tagName: `div`,
        children: e.annotations?.map((e) =>
          I({ tagName: `slot`, properties: { name: e } }),
        ),
        properties: { "data-annotation-content": `` },
      }),
    ],
    properties: { "data-line-annotation": `${e.hunkIndex},${e.lineIndex}` },
  });
}
var bm = e(() => {
  L();
});
function xm(e) {
  switch (e) {
    case `file`:
      return `diffs-icon-file-code`;
    case `change`:
      return `diffs-icon-symbol-modified`;
    case `new`:
      return `diffs-icon-symbol-added`;
    case `deleted`:
      return `diffs-icon-symbol-deleted`;
    case `rename-pure`:
    case `rename-changed`:
      return `diffs-icon-symbol-moved`;
  }
}
var Sm = e(() => {});
function Cm({ fileOrDiff: e, mode: t, stickyHeader: n }) {
  let r = `type` in e ? e : void 0,
    i = {
      "data-diffs-header": t,
      "data-change-type": r?.type,
      "data-sticky": n ? `` : void 0,
    };
  return I({
    tagName: `div`,
    children: [
      t === `custom`
        ? I({ tagName: `slot`, properties: { name: Nt } })
        : wm({
            name: e.name,
            prevName: `prevName` in e ? e.prevName : void 0,
            iconType: r?.type ?? `file`,
          }),
      ...(t === `custom` ? [] : [Tm(r)]),
    ],
    properties: i,
  });
}
function wm({ name: e, prevName: t, iconType: n }) {
  let r = [
    I({ tagName: `slot`, properties: { name: At } }),
    hn({ name: xm(n), properties: { "data-change-icon": n } }),
  ];
  return (
    t != null &&
      (r.push(
        I({
          tagName: `div`,
          children: [I({ tagName: `bdi`, children: [mn(t)] })],
          properties: { "data-prev-name": `` },
        }),
      ),
      r.push(
        hn({
          name: `diffs-icon-arrow-right-short`,
          properties: { "data-rename-icon": `` },
        }),
      )),
    r.push(
      I({
        tagName: `div`,
        children: [I({ tagName: `bdi`, children: [mn(e)] })],
        properties: { "data-title": `` },
      }),
    ),
    r.push(I({ tagName: `slot`, properties: { name: jt } })),
    I({
      tagName: `div`,
      children: r,
      properties: { "data-header-content": `` },
    })
  );
}
function Tm(e) {
  let t = [];
  if (e != null) {
    let n = 0,
      r = 0;
    for (let t of e.hunks) ((n += t.additionLines), (r += t.deletionLines));
    ((r > 0 || n === 0) &&
      t.push(
        I({
          tagName: `span`,
          children: [mn(`-${r}`)],
          properties: { "data-deletions-count": `` },
        }),
      ),
      (n > 0 || r === 0) &&
        t.push(
          I({
            tagName: `span`,
            children: [mn(`+${n}`)],
            properties: { "data-additions-count": `` },
          }),
        ));
  }
  return (
    t.push(I({ tagName: `slot`, properties: { name: Mt } })),
    I({ tagName: `div`, children: t, properties: { "data-metadata": `` } })
  );
}
var Em = e(() => {
  (F(), L(), Sm());
});
function Dm(e) {
  return I({ tagName: `pre`, properties: Om(e) });
}
function Om({
  diffIndicators: e,
  disableBackground: t,
  disableLineNumbers: n,
  overflow: r,
  split: i,
  totalLines: a,
  type: o,
  customProperties: s,
}) {
  return {
    ...s,
    "data-diff": o === `diff` ? `` : void 0,
    "data-file": o === `file` ? `` : void 0,
    "data-diff-type": o === `diff` ? (i ? `split` : `single`) : void 0,
    "data-overflow": r,
    "data-disable-line-numbers": n ? `` : void 0,
    "data-background": t ? void 0 : ``,
    "data-indicators": e === `bars` || e === `classic` ? e : void 0,
    style: `--diffs-min-number-column-width-default:${`${a}`.length}ch;`,
  };
}
var km = e(() => {
  L();
});
function Am(e) {
  if (Nm.has(e)) return Nm.get(e) ?? `text`;
  if (Fm[e] != null) return Fm[e];
  let t = e.match(/\.([^/\\]+\.[^/\\]+)$/);
  if (t != null) {
    if (Nm.has(t[1])) return Nm.get(t[1]) ?? `text`;
    if (Fm[t[1]] != null) return Fm[t[1]] ?? `text`;
  }
  let n = e.match(/\.([^.]+)$/)?.[1] ?? ``;
  return Nm.has(n) ? (Nm.get(n) ?? `text`) : (Fm[n] ?? `text`);
}
function jm() {
  return Pm;
}
function Mm() {
  return Object.fromEntries(Nm);
}
var Nm,
  Pm,
  Fm,
  Im = e(() => {
    ((Nm = new Map()),
      (Pm = 0),
      (Fm = {
        "1c": `1c`,
        abap: `abap`,
        as: `actionscript-3`,
        ada: `ada`,
        adb: `ada`,
        ads: `ada`,
        adoc: `asciidoc`,
        asciidoc: `asciidoc`,
        "component.html": `angular-html`,
        "component.ts": `angular-ts`,
        conf: `nginx`,
        htaccess: `apache`,
        cls: `tex`,
        trigger: `apex`,
        apl: `apl`,
        applescript: `applescript`,
        scpt: `applescript`,
        ara: `ara`,
        asm: `asm`,
        s: `riscv`,
        astro: `astro`,
        awk: `awk`,
        bal: `ballerina`,
        sh: `zsh`,
        bash: `zsh`,
        bat: `cmd`,
        cmd: `cmd`,
        be: `berry`,
        beancount: `beancount`,
        bib: `bibtex`,
        bicep: `bicep`,
        "blade.php": `blade`,
        bsl: `bsl`,
        c: `c`,
        h: `objective-cpp`,
        cs: `csharp`,
        cpp: `cpp`,
        hpp: `cpp`,
        cc: `cpp`,
        cxx: `cpp`,
        hh: `cpp`,
        cdc: `cdc`,
        cairo: `cairo`,
        clar: `clarity`,
        clj: `clojure`,
        cljs: `clojure`,
        cljc: `clojure`,
        soy: `soy`,
        cmake: `cmake`,
        "CMakeLists.txt": `cmake`,
        cob: `cobol`,
        cbl: `cobol`,
        cobol: `cobol`,
        CODEOWNERS: `codeowners`,
        ql: `ql`,
        coffee: `coffeescript`,
        lisp: `lisp`,
        cl: `lisp`,
        lsp: `lisp`,
        log: `log`,
        v: `verilog`,
        cql: `cql`,
        cr: `crystal`,
        css: `css`,
        csv: `csv`,
        cue: `cue`,
        cypher: `cypher`,
        cyp: `cypher`,
        d: `d`,
        dart: `dart`,
        dax: `dax`,
        desktop: `desktop`,
        diff: `diff`,
        patch: `diff`,
        Dockerfile: `dockerfile`,
        dockerfile: `dockerfile`,
        env: `dotenv`,
        dm: `dream-maker`,
        edge: `edge`,
        el: `emacs-lisp`,
        ex: `elixir`,
        exs: `elixir`,
        elm: `elm`,
        erb: `erb`,
        erl: `erlang`,
        hrl: `erlang`,
        f: `fortran-fixed-form`,
        for: `fortran-fixed-form`,
        fs: `fsharp`,
        fsi: `fsharp`,
        fsx: `fsharp`,
        f03: `f03`,
        f08: `f08`,
        f18: `f18`,
        f77: `f77`,
        f90: `fortran-free-form`,
        f95: `fortran-free-form`,
        fnl: `fennel`,
        fish: `fish`,
        ftl: `ftl`,
        tres: `gdresource`,
        res: `gdresource`,
        gd: `gdscript`,
        gdshader: `gdshader`,
        gs: `genie`,
        feature: `gherkin`,
        COMMIT_EDITMSG: `git-commit`,
        "git-rebase-todo": `git-rebase`,
        gjs: `glimmer-js`,
        gleam: `gleam`,
        gts: `glimmer-ts`,
        glsl: `glsl`,
        vert: `glsl`,
        frag: `glsl`,
        shader: `shaderlab`,
        gp: `gnuplot`,
        plt: `gnuplot`,
        gnuplot: `gnuplot`,
        go: `go`,
        graphql: `graphql`,
        gql: `graphql`,
        groovy: `groovy`,
        gvy: `groovy`,
        hack: `hack`,
        haml: `haml`,
        hbs: `handlebars`,
        handlebars: `handlebars`,
        hs: `haskell`,
        lhs: `haskell`,
        hx: `haxe`,
        hcl: `hcl`,
        hjson: `hjson`,
        hlsl: `hlsl`,
        fx: `hlsl`,
        html: `html`,
        htm: `html`,
        http: `http`,
        rest: `http`,
        hxml: `hxml`,
        hy: `hy`,
        imba: `imba`,
        ini: `ini`,
        cfg: `ini`,
        jade: `pug`,
        pug: `pug`,
        java: `java`,
        js: `javascript`,
        mjs: `javascript`,
        cjs: `javascript`,
        jinja: `jinja`,
        jinja2: `jinja`,
        j2: `jinja`,
        jison: `jison`,
        jl: `julia`,
        json: `json`,
        json5: `json5`,
        jsonc: `jsonc`,
        jsonl: `jsonl`,
        jsonnet: `jsonnet`,
        libsonnet: `jsonnet`,
        jssm: `jssm`,
        jsx: `jsx`,
        kt: `kotlin`,
        kts: `kts`,
        kql: `kusto`,
        tex: `tex`,
        ltx: `tex`,
        lean: `lean4`,
        less: `less`,
        liquid: `liquid`,
        lit: `lit`,
        ll: `llvm`,
        logo: `logo`,
        lua: `lua`,
        luau: `luau`,
        Makefile: `makefile`,
        mk: `makefile`,
        makefile: `makefile`,
        md: `markdown`,
        markdown: `markdown`,
        marko: `marko`,
        m: `wolfram`,
        mat: `matlab`,
        mdc: `mdc`,
        mdx: `mdx`,
        wiki: `wikitext`,
        mediawiki: `wikitext`,
        mmd: `mermaid`,
        mermaid: `mermaid`,
        mips: `mipsasm`,
        mojo: `mojo`,
        "🔥": `mojo`,
        move: `move`,
        nar: `narrat`,
        nf: `nextflow`,
        nim: `nim`,
        nims: `nim`,
        nimble: `nim`,
        nix: `nix`,
        nu: `nushell`,
        mm: `objective-cpp`,
        ml: `ocaml`,
        mli: `ocaml`,
        mll: `ocaml`,
        mly: `ocaml`,
        pas: `pascal`,
        p: `pascal`,
        pl: `prolog`,
        pm: `perl`,
        t: `perl`,
        raku: `raku`,
        p6: `raku`,
        pl6: `raku`,
        php: `php`,
        phtml: `php`,
        pls: `plsql`,
        sql: `sql`,
        po: `po`,
        polar: `polar`,
        pcss: `postcss`,
        pot: `pot`,
        potx: `potx`,
        pq: `powerquery`,
        pqm: `powerquery`,
        ps1: `powershell`,
        psm1: `powershell`,
        psd1: `powershell`,
        prisma: `prisma`,
        pro: `prolog`,
        P: `prolog`,
        properties: `properties`,
        proto: `protobuf`,
        pp: `puppet`,
        purs: `purescript`,
        py: `python`,
        pyw: `python`,
        pyi: `python`,
        qml: `qml`,
        qmldir: `qmldir`,
        qss: `qss`,
        r: `r`,
        R: `r`,
        rkt: `racket`,
        rktl: `racket`,
        razor: `razor`,
        cshtml: `razor`,
        rb: `ruby`,
        rbw: `ruby`,
        reg: `reg`,
        regex: `regexp`,
        rel: `rel`,
        rs: `rust`,
        rst: `rst`,
        rake: `ruby`,
        gemspec: `ruby`,
        jbuilder: `ruby`,
        builder: `ruby`,
        rabl: `ruby`,
        arb: `ruby`,
        ru: `ruby`,
        podspec: `ruby`,
        Gemfile: `ruby`,
        Rakefile: `ruby`,
        Guardfile: `ruby`,
        Capfile: `ruby`,
        Berksfile: `ruby`,
        Brewfile: `ruby`,
        Vagrantfile: `ruby`,
        Thorfile: `ruby`,
        Appraisals: `ruby`,
        Dangerfile: `ruby`,
        sas: `sas`,
        sass: `sass`,
        scala: `scala`,
        sc: `scala`,
        scm: `scheme`,
        ss: `scheme`,
        sld: `scheme`,
        scss: `scss`,
        sdbl: `sdbl`,
        shadergraph: `shader`,
        st: `smalltalk`,
        sol: `solidity`,
        sparql: `sparql`,
        rq: `sparql`,
        spl: `splunk`,
        config: `ssh-config`,
        do: `stata`,
        ado: `stata`,
        dta: `stata`,
        styl: `stylus`,
        stylus: `stylus`,
        svelte: `svelte`,
        swift: `swift`,
        sv: `system-verilog`,
        svh: `system-verilog`,
        service: `systemd`,
        socket: `systemd`,
        device: `systemd`,
        timer: `systemd`,
        talon: `talonscript`,
        tasl: `tasl`,
        tcl: `tcl`,
        templ: `templ`,
        tf: `tf`,
        tfvars: `tfvars`,
        toml: `toml`,
        ts: `typescript`,
        mts: `typescript`,
        cts: `typescript`,
        tsp: `typespec`,
        tsv: `tsv`,
        tsx: `tsx`,
        ttl: `turtle`,
        twig: `twig`,
        typ: `typst`,
        vv: `v`,
        vala: `vala`,
        vapi: `vala`,
        vb: `vb`,
        vbs: `vb`,
        bas: `vb`,
        vh: `verilog`,
        vhd: `vhdl`,
        vhdl: `vhdl`,
        vim: `vimscript`,
        vue: `vue`,
        "vine.ts": `vue-vine`,
        vy: `vyper`,
        wasm: `wasm`,
        wat: `wasm`,
        wy: `文言`,
        wgsl: `wgsl`,
        wit: `wit`,
        wl: `wolfram`,
        nb: `wolfram`,
        xml: `xml`,
        xsl: `xsl`,
        xslt: `xsl`,
        yaml: `yaml`,
        yml: `yml`,
        zs: `zenscript`,
        zig: `zig`,
        zsh: `zsh`,
        sty: `tex`,
      }));
  });
function Lm(e, { theme: t, preferredHighlighter: n = `shiki-js` }) {
  return { langs: [e ?? `text`], themes: lm(t), preferredHighlighter: n };
}
var Rm = e(() => {
  um();
});
function zm(e) {
  return `annotation-${`side` in e ? `${e.side}-` : ``}${e.lineNumber}`;
}
var Bm = e(() => {});
function Vm(e, t, n) {
  let r = typeof n.lineInfo == `function` ? n.lineInfo(t) : n.lineInfo[t - 1];
  if (r == null) {
    let r = `processLine: line ${t}, contains no state.lineInfo`;
    throw (console.error(r, { node: e, line: t, state: n }), Error(r));
  }
  return (
    (e.tagName = `div`),
    (e.properties[`data-line`] = r.lineNumber),
    (e.properties[`data-alt-line`] = r.altLineNumber),
    (e.properties[`data-line-type`] = r.type),
    (e.properties[`data-line-index`] = r.lineIndex),
    e.children.length === 0 &&
      e.children.push(
        mn(`
`),
      ),
    e
  );
}
var Hm = e(() => {
  L();
});
function Um(e) {
  let t = Wm(e);
  if (t != null) return t;
  let n = qm,
    r = [],
    i = [],
    a,
    o = () => {
      if (i.length === 0 || a == null) {
        ((i = []), (a = void 0));
        return;
      }
      if (i.length === 1) {
        let e = i[0];
        if (e?.type === `element`) {
          Km(e, a);
          for (let t of e.children) Gm(t);
        } else Gm(e);
        (r.push(e), (i = []), (a = void 0));
        return;
      }
      for (let e of i) Gm(e);
      (r.push(
        I({ tagName: `span`, properties: { "data-char": a }, children: i }),
      ),
        (i = []),
        (a = void 0));
    },
    s = (e) => {
      if (e !== qm) {
        if (e === Jm) {
          n = Jm;
          return;
        }
        if (n === qm) {
          n = e;
          return;
        }
        n !== e && (n = Jm);
      }
    };
  for (let t of e.children) {
    let e = t.type === `element` ? Um(t) : qm;
    if ((s(e), typeof e != `number`)) {
      (o(), r.push(t));
      continue;
    }
    (a != null && a !== e && o(), (a ??= e), i.push(t));
  }
  return (o(), (e.children = r), n);
}
function Wm(e) {
  let t = e.properties[`data-char`];
  if (typeof t == `number`) return t;
}
function Gm(e) {
  if (e.type === `element`) {
    e.properties[`data-char`] = void 0;
    for (let t of e.children) Gm(t);
  }
}
function Km(e, t) {
  e.properties[`data-char`] = t;
}
var qm,
  Jm,
  Ym = e(() => {
    (L(), (qm = Symbol(`no-token`)), (Jm = Symbol(`multiple-tokens`)));
  });
function Xm(e = {}) {
  let {
      classPrefix: t = `__shiki_`,
      classSuffix: n = ``,
      classReplacer: r = (e) => e,
    } = e,
    i = new Map();
  function a(e) {
    return Object.entries(e)
      .map(([e, t]) => `${e}:${t}`)
      .join(`;`);
  }
  function o(e) {
    let o = typeof e == `string` ? e : a(e),
      s = t + Zm(o) + n;
    return (
      (s = r(s)),
      i.has(s) || i.set(s, typeof e == `string` ? e : { ...e }),
      s
    );
  }
  return {
    name: `@shikijs/transformers:style-to-class`,
    pre(e) {
      if (!e.properties.style) return;
      let t = o(e.properties.style);
      (delete e.properties.style, this.addClassToHast(e, t));
    },
    tokens(e) {
      for (let t of e)
        for (let e of t) {
          if (!e.htmlStyle) continue;
          let t = o(e.htmlStyle);
          ((e.htmlStyle = {}),
            (e.htmlAttrs ||= {}),
            e.htmlAttrs.class
              ? (e.htmlAttrs.class += ` ${t}`)
              : (e.htmlAttrs.class = t));
        }
    },
    getClassRegistry() {
      return i;
    },
    getCSS() {
      let e = ``;
      for (let [t, n] of i.entries())
        e += `.${t}{${typeof n == `string` ? n : a(n)}}`;
      return e;
    },
    clearRegistry() {
      i.clear();
    },
  };
}
function Zm(e, t = 0) {
  let n = 3735928559 ^ t,
    r = 1103547991 ^ t;
  for (let t = 0, i; t < e.length; t++)
    ((i = e.charCodeAt(t)),
      (n = Math.imul(n ^ i, 2654435761)),
      (r = Math.imul(r ^ i, 1597334677)));
  return (
    (n = Math.imul(n ^ (n >>> 16), 2246822507)),
    (n ^= Math.imul(r ^ (r >>> 13), 3266489909)),
    (r = Math.imul(r ^ (r >>> 16), 2246822507)),
    (r ^= Math.imul(n ^ (n >>> 13), 3266489909)),
    (4294967296 * (2097151 & r) + (n >>> 0)).toString(36).slice(0, 6)
  );
}
var Qm = e(() => {});
function $m(e = !1, t = !1) {
  let n = { lineInfo: [] },
    r = [
      {
        line(e) {
          return (delete e.properties.class, e);
        },
        pre(t) {
          let r = gn(t),
            i = [];
          if (r != null) {
            let t = 1;
            for (let a of r.children)
              a.type === `element` && (e && Um(a), i.push(Vm(a, t, n)), t++);
            r.children = i;
          }
          return t;
        },
        ...(e
          ? {
              tokens(e) {
                for (let t of e) {
                  let e = 0;
                  for (let n of t) {
                    let t = n;
                    ((t.__lineChar ??= e), (e += n.content.length));
                  }
                }
              },
              preprocess(e, t) {
                t.mergeWhitespaces = `never`;
              },
              span(e, t, n, r, i) {
                if (i?.offset != null && i.content != null) {
                  let t = i.__lineChar;
                  return (t != null && (e.properties[`data-char`] = t), e);
                }
                return e;
              },
            }
          : null),
      },
    ];
  return (
    t && r.push(th, eh),
    e &&
      r.push({
        line: (e) => (
          e.type === `element` &&
            e.children.length === 0 &&
            e.children.push({
              type: `element`,
              tagName: `br`,
              properties: {},
              children: [],
            }),
          e
        ),
      }),
    { state: n, transformers: r, toClass: eh }
  );
}
var eh,
  th,
  nh = e(() => {
    (L(),
      Hm(),
      Ym(),
      Qm(),
      (eh = Xm({ classPrefix: `hl-` })),
      (th = {
        name: `token-style-normalizer`,
        tokens(e) {
          for (let t of e)
            for (let e of t) {
              if (e.htmlStyle != null) continue;
              let t = {};
              (e.color != null && (t.color = e.color),
                e.bgColor != null && (t[`background-color`] = e.bgColor),
                e.fontStyle != null &&
                  e.fontStyle !== 0 &&
                  (e.fontStyle & 1 && (t[`font-style`] = `italic`),
                  e.fontStyle & 2 && (t[`font-weight`] = `bold`),
                  e.fontStyle & 4 && (t[`text-decoration`] = `underline`)),
                Object.keys(t).length > 0 && (e.htmlStyle = t));
            }
        },
      }));
  });
function rh(e) {
  return `--${e === `token` ? `diffs-token` : `diffs`}-`;
}
var ih = e(() => {});
function ah(e) {
  let t = e.indexOf(`(`);
  if (t <= 0 || !e.endsWith(`)`)) return;
  let n = e.slice(0, t).trim();
  if (!/^(?:rgb|rgba|hsl|hsla|hwb|lab|lch|oklab|oklch|color)$/i.test(n)) return;
  let r = e.slice(t + 1, -1).trim();
  if (r.length === 0) return;
  let i = r.lastIndexOf(`/`);
  if (i !== -1) return r.slice(i + 1).trim();
  if (/^(?:rgba|hsla)$/i.test(n)) {
    let e = r.split(`,`);
    if (e.length === 4) return e[3]?.trim();
  }
}
function oh(e) {
  let t = /^#([0-9a-f]{3}|[0-9a-f]{6}|[0-9a-f]{8})\b/i.exec(e.trim());
  if (t == null) return null;
  let n = t[1],
    r,
    i = 1;
  return (
    n.length === 3
      ? (r = n
          .split(``)
          .map((e) => e + e)
          .join(``))
      : n.length === 6
        ? (r = n)
        : ((r = n.slice(0, 6)), (i = parseInt(n.slice(6, 8), 16) / 255)),
    [
      parseInt(r.slice(0, 2), 16),
      parseInt(r.slice(2, 4), 16),
      parseInt(r.slice(4, 6), 16),
      i,
    ]
  );
}
function sh(e) {
  if (e == null) return null;
  let t = oh(e);
  if (t == null) return null;
  let n = t[0] / 255,
    r = t[1] / 255,
    i = t[2] / 255,
    a = (e) => (e <= 0.03928 ? e / 12.92 : ((e + 0.055) / 1.055) ** 2.4);
  return 0.2126 * a(n) + 0.7152 * a(r) + 0.0722 * a(i);
}
function ch(e) {
  if (e == null) return !1;
  let t = e.trim().toLowerCase();
  if (t === `transparent` || uh.test(t)) return !0;
  let n = ah(t);
  return n != null && dh.test(n);
}
function lh(e, t, n) {
  if (t == null || n == null) return !1;
  let r = sh(e),
    i = sh(t),
    a = sh(n);
  return r == null || i == null || a == null
    ? !1
    : Math.abs(r - a) < Math.abs(r - i);
}
var uh,
  dh,
  fh = e(() => {
    ((uh = /^#(?:[0-9a-f]{3}0|[0-9a-f]{6}00)$/i), (dh = /^0(?:\.0+)?%?$/));
  });
function ph(e) {
  let t = _h.get(e);
  if (t != null) return t;
  let n = e.colors ?? {},
    r = { ...n },
    i = n[`editor.background`] ?? e.bg,
    a = n[`editor.foreground`] ?? e.fg,
    o = n[`sideBar.background`] ?? i,
    s = n[`sideBar.foreground`] ?? a;
  (mh(r, `editor.background`, i),
    mh(r, `editor.foreground`, a),
    mh(r, `sideBar.background`, o),
    mh(r, `sideBar.foreground`, s),
    mh(r, `input.background`, n[`input.background`] ?? o),
    mh(
      r,
      `sideBarSectionHeader.foreground`,
      n[`sideBarSectionHeader.foreground`] ?? s,
    ),
    mh(
      r,
      `list.activeSelectionForeground`,
      n[`list.activeSelectionForeground`] ?? s,
    ),
    mh(
      r,
      `gitDecoration.addedResourceForeground`,
      hh(
        n[`gitDecoration.addedResourceForeground`],
        n[`terminal.ansiGreen`],
        n[`editorGutter.addedBackground`],
      ),
    ),
    mh(
      r,
      `gitDecoration.modifiedResourceForeground`,
      hh(
        n[`gitDecoration.modifiedResourceForeground`],
        n[`terminal.ansiBlue`],
        n[`editorGutter.modifiedBackground`],
      ),
    ),
    mh(
      r,
      `gitDecoration.deletedResourceForeground`,
      hh(
        n[`gitDecoration.deletedResourceForeground`],
        n[`terminal.ansiRed`],
        n[`editorGutter.deletedBackground`],
      ),
    ));
  let c =
    (ch(n[`list.focusOutline`]) ? void 0 : n[`list.focusOutline`]) ??
    (ch(n.focusBorder) ? void 0 : n.focusBorder);
  c == null ? delete r[`list.focusOutline`] : (r[`list.focusOutline`] = c);
  let l = n[`list.hoverBackground`];
  l != null && (gh(l, o) || lh(l, o, s)) && delete r[`list.hoverBackground`];
  let u = Object.freeze({ ...e, colors: Object.freeze(r) });
  return (_h.set(e, u), u);
}
function mh(e, t, n) {
  n != null && n !== `` && (e[t] = n);
}
function hh(...e) {
  for (let t of e) if (t != null && t !== ``) return t;
}
function gh(e, t) {
  return t != null && e.toLowerCase() === t.toLowerCase();
}
var _h,
  vh = e(() => {
    (fh(), (_h = new WeakMap()));
  }),
  yh = e(() => {
    vh();
  });
function bh({ theme: e = P, highlighter: t, prefix: n }) {
  let r = ``;
  if (typeof e == `string`) {
    let i = t.getTheme(e),
      a = ph(i);
    ((r += `color:${a.fg};`),
      (r += `background-color:${a.bg};`),
      (r += `${rh(`global`)}fg:${a.fg};`),
      (r += `${rh(`global`)}bg:${a.bg};`),
      (r += xh(i, n)));
  } else {
    let n = t.getTheme(e.dark),
      i = ph(n);
    ((r += `${rh(`global`)}dark:${i.fg};`),
      (r += `${rh(`global`)}dark-bg:${i.bg};`),
      (r += xh(n, `dark`)),
      (n = t.getTheme(e.light)),
      (i = ph(n)),
      (r += `${rh(`global`)}light:${i.fg};`),
      (r += `${rh(`global`)}light-bg:${i.bg};`),
      (r += xh(n, `light`)));
  }
  return r;
}
function xh(e, t) {
  t = t == null ? `` : `${t}-`;
  let n = ``,
    r =
      e.colors?.[`gitDecoration.addedResourceForeground`] ??
      e.colors?.[`terminal.ansiGreen`];
  r != null && (n += `${rh(`global`)}${t}addition-color:${r};`);
  let i =
    e.colors?.[`gitDecoration.deletedResourceForeground`] ??
    e.colors?.[`terminal.ansiRed`];
  i != null && (n += `${rh(`global`)}${t}deletion-color:${i};`);
  let a =
    e.colors?.[`gitDecoration.modifiedResourceForeground`] ??
    e.colors?.[`terminal.ansiBlue`];
  return (a != null && (n += `${rh(`global`)}${t}modified-color:${a};`), n);
}
var Sh = e(() => {
  (F(), ih(), yh());
});
function Ch(e) {
  let t = e.children[0];
  for (; t != null;) {
    if (t.type === `element` && t.tagName === `code`) return t.children;
    t = `children` in t ? t.children[0] : null;
  }
  throw (console.error(e), Error(`getLineNodes: Unable to find children`));
}
var wh = e(() => {});
function Th(e) {
  let t = [0];
  for (let n = 0; n < e.length; n++) {
    let r = e.charCodeAt(n);
    (r === Oh || r === kh) &&
      (r === kh && n + 1 < e.length && e.charCodeAt(n + 1) === Oh && n++,
      t.push(n + 1));
  }
  return t;
}
function Eh(e) {
  let t = 0;
  for (let n = 0; n < e.length; n++) {
    let r = e.charCodeAt(n);
    (r === Oh || r === kh) &&
      (r === kh && n + 1 < e.length && e.charCodeAt(n + 1) === Oh && n++, t++);
  }
  return t;
}
function Dh(e) {
  let t = Th(e);
  return Array.from({ length: t.length }, (n, r) => {
    let i = t[r],
      a = t[r + 1] ?? e.length;
    return e.slice(i, a);
  });
}
var Oh,
  kh,
  Ah = e(() => {
    ((Oh = 10), (kh = 13));
  });
function jh(
  e,
  t,
  { theme: n = P, tokenizeMaxLineLength: r, useTokenTransformer: i },
  { forcePlainText: a, startingLine: o, totalLines: s, lines: c } = Nh,
) {
  a ? ((o ??= 0), (s ??= 1 / 0)) : ((o = 0), (s = 1 / 0));
  let l = o > 0 || s < 1 / 0,
    { state: u, transformers: d } = $m(i),
    f = a ? `text` : (e.lang ?? Am(e.name)),
    p = typeof n == `string` ? t.getTheme(n).type : void 0,
    m = bh({ theme: n, highlighter: t });
  u.lineInfo = (e) => ({
    type: `context`,
    lineIndex: e - 1 + o,
    lineNumber: e + o,
  });
  let h =
      typeof n == `string`
        ? {
            lang: f,
            theme: n,
            transformers: d,
            defaultColor: !1,
            cssVariablePrefix: rh(`token`),
            tokenizeMaxLineLength: r,
            tokenizeTimeLimit: 0,
          }
        : {
            lang: f,
            themes: n,
            transformers: d,
            defaultColor: !1,
            cssVariablePrefix: rh(`token`),
            tokenizeMaxLineLength: r,
            tokenizeTimeLimit: 0,
          },
    g = Ch(t.codeToHast(l ? Mh(c ?? Dh(e.contents), o, s) : e.contents, h)),
    _ = l ? Array(o) : g;
  return (l && _.push(...g), { code: _, themeStyles: m, baseThemeType: p });
}
function Mh(e, t, n) {
  if (e.length === 0) return ``;
  let r = Math.min(t + n, e.length);
  return e.slice(t, r).join(``);
}
var Nh,
  Ph = e(() => {
    (F(), Ah(), Im(), nh(), ih(), Sh(), wh(), (Nh = { forcePlainText: !1 }));
  });
function Fh(e) {
  return e?.some((e) => e.lineNumber === 0) ?? !1;
}
function Ih(e) {
  let t = e[0];
  return t != null && t.length > 0 ? t : void 0;
}
function Lh(e) {
  return e.startingLine === 0 && e.totalLines > 0;
}
var Rh,
  zh = e(() => {
    Rh = `-1,-1`;
  });
function Bh(e, t) {
  return I({
    tagName: `div`,
    children: e,
    properties: { "data-content": ``, style: `grid-row: span ${t}` },
  });
}
var Vh = e(() => {
  L();
});
function Hh(e) {
  return (e.lang ?? Am(e.name)) === `text`;
}
var Uh = e(() => {
  Im();
});
function Wh(e) {
  return (
    e.useTokenTransformer === !0 ||
    e.onTokenClick != null ||
    e.onTokenEnter != null ||
    e.onTokenLeave != null
  );
}
var Gh = e(() => {});
function Kh(e, t) {
  return e > t;
}
var qh,
  Jh,
  Yh = e(() => {
    (F(),
      pn(),
      zh(),
      L(),
      fa(),
      cm(),
      um(),
      fm(),
      mm(),
      gm(),
      vm(),
      Ah(),
      bm(),
      Vh(),
      Em(),
      km(),
      Im(),
      Rm(),
      Bm(),
      Uh(),
      Ph(),
      Gh(),
      Ni(),
      (qh = -1),
      (Jh = class {
        options;
        onRenderUpdate;
        workerManager;
        __id = `file-renderer:${++qh}`;
        highlighter;
        renderCache;
        computedLang = `text`;
        lineAnnotations = {};
        lineCache;
        textDoucmentCache = new WeakMap();
        constructor(e = { theme: P }, t, n) {
          ((this.options = e),
            (this.onRenderUpdate = t),
            (this.workerManager = n),
            n?.isWorkingPool() !== !0 &&
              (this.highlighter = dm(e.theme ?? P) ? am() : void 0));
        }
        setOptions(e) {
          this.options = e;
        }
        mergeOptions(e) {
          this.options = { ...this.options, ...e };
        }
        setLineAnnotations(e) {
          this.lineAnnotations = {};
          for (let t of e) {
            let e = this.lineAnnotations[t.lineNumber] ?? [];
            ((this.lineAnnotations[t.lineNumber] = e), e.push(t));
          }
        }
        cleanUp() {
          (this.recycle(),
            (this.workerManager = void 0),
            (this.onRenderUpdate = void 0));
        }
        recycle() {
          (this.clearRenderCache(),
            (this.highlighter = void 0),
            this.workerManager?.cleanUpTasks(this),
            (this.lineCache = void 0));
        }
        clearRenderCache() {
          let e = this.renderCache;
          ((this.renderCache = void 0),
            e != null &&
              e.isDirty === !0 &&
              e.file.cacheKey != null &&
              this.workerManager?.evictFileFromCache(e.file.cacheKey));
        }
        hydrate(e) {
          let { options: t } = this.getRenderOptions(e),
            n = Kh(
              this.getOrCreateLineCache(e).length,
              this.getTokenizeMaxLength(),
            ),
            r = this.workerManager?.getFileResultCache(e);
          (r != null && !hm(t, r.options) && (r = void 0),
            (this.renderCache ??= {
              file: e,
              options: t,
              highlighted: !n && !Hh(e),
              result: n ? void 0 : r?.result,
              renderRange: void 0,
            }),
            this.workerManager?.isWorkingPool() === !0
              ? this.renderCache.result == null &&
                !n &&
                this.workerManager.highlightFileAST(this, e)
              : (this.highlighter ??
                ((this.computedLang = e.lang ?? Am(e.name)),
                this.initializeHighlighter())));
        }
        getRenderOptions(e) {
          let t = (() => {
              if (this.workerManager?.isWorkingPool() === !0)
                return this.workerManager.getFileRenderOptions();
              let { theme: e = P, tokenizeMaxLineLength: t = 1e3 } =
                this.options;
              return {
                theme: e,
                useTokenTransformer: Wh(this.options),
                tokenizeMaxLineLength: t,
              };
            })(),
            { renderCache: n } = this;
          return n?.result == null || !fn(e, n.file) || !hm(t, n.options)
            ? { options: t, forceHighlight: !0 }
            : { options: t, forceHighlight: !1 };
        }
        getOrCreateLineCache(e) {
          if (e.cacheKey == null)
            return ((this.lineCache = void 0), Dh(e.contents));
          let { lineCache: t } = this;
          return (
            (t == null || t.cacheKey !== e.cacheKey) &&
              (t = { cacheKey: e.cacheKey, lines: Dh(e.contents) }),
            (this.lineCache = t),
            t.lines
          );
        }
        getLineCount(e) {
          return (
            this.textDoucmentCache.get(e)?.lineCount ??
            this.getOrCreateLineCache(e).length
          );
        }
        updateRenderCache(e, t) {
          if (this.renderCache == null) return;
          let { result: n } = this.renderCache;
          if (n != null) {
            for (let [r, i] of e)
              n.code[r] = {
                type: `element`,
                tagName: `div`,
                properties: {
                  "data-line": r + 1,
                  "data-line-type": `context`,
                  "data-line-index": r,
                },
                children: i.map(([e, n, r]) =>
                  e === 0 && n === ``
                    ? r === ``
                      ? {
                          type: `element`,
                          tagName: `br`,
                          properties: {},
                          children: [],
                        }
                      : { type: `text`, value: r }
                    : {
                        type: `element`,
                        tagName: `span`,
                        properties: {
                          "data-char": e,
                          style: `--diffs-token-${t}:${n};`,
                        },
                        children: [{ type: `text`, value: r }],
                      },
                ),
              };
            ((n.baseThemeType = t), (this.renderCache.isDirty = !0));
          }
        }
        applyDocumentChange(e) {
          if (this.renderCache == null) return;
          let { file: t, result: n } = this.renderCache;
          if (n != null && n.code.length !== e.lineCount) {
            n.code.length = Math.min(n.code.length, e.lineCount);
            for (let t = n.code.length; t < e.lineCount; t++)
              n.code.push({
                type: `element`,
                tagName: `div`,
                properties: {
                  "data-line": t + 1,
                  "data-line-type": `context`,
                  "data-line-index": t,
                },
                children: [
                  {
                    type: `element`,
                    tagName: `span`,
                    properties: { "data-char": 0 },
                    children: [{ type: `text`, value: e.getLineText(t) }],
                  },
                ],
              });
            this.renderCache.isDirty = !0;
          }
          this.textDoucmentCache.set(t, e);
        }
        renderFile(e = this.renderCache?.file, t = Wt) {
          if (e == null) return;
          let { options: n, forceHighlight: r } = this.getRenderOptions(e),
            i = this.getMatchingWorkerResultCache(e, n);
          (i != null &&
            !this.hasHighlightedRenderCache(e, n) &&
            ((this.renderCache = {
              file: e,
              highlighted: !0,
              renderRange: void 0,
              ...i,
            }),
            (r = !1)),
            (this.renderCache ??= {
              file: e,
              highlighted: !1,
              options: n,
              result: void 0,
              renderRange: void 0,
            }));
          let a = this.getOrCreateLineCache(e),
            o = e.contents.length > 0,
            s = !o || Hh(e) || Kh(a.length, this.getTokenizeMaxLength()),
            c = !fn(e, this.renderCache.file),
            l = !_m(this.renderCache.renderRange, t);
          if (this.workerManager?.isWorkingPool() === !0)
            ((s ||
              this.renderCache.result == null ||
              (!this.renderCache.highlighted && (c || l))) &&
              ((this.renderCache.file = e),
              (this.renderCache.options = n),
              (this.renderCache.highlighted = !1),
              (this.renderCache.result == null || c || l || r) &&
                (this.renderCache.result = this.workerManager.getPlainFileAST(
                  e,
                  t.startingLine,
                  t.totalLines,
                  a,
                )),
              (this.renderCache.renderRange = t)),
              !s &&
                o &&
                (!this.renderCache.highlighted || r) &&
                this.workerManager.highlightFileAST(this, e));
          else {
            this.computedLang = e.lang ?? Am(e.name);
            let t = this.highlighter != null && dm(n.theme),
              i = this.highlighter != null && da(this.computedLang),
              a = !s && i;
            if (
              this.highlighter != null &&
              t &&
              (r ||
                s ||
                (!this.renderCache.highlighted && a) ||
                this.renderCache.result == null)
            ) {
              let { result: t, options: n } = this.renderFileWithHighlighter(
                e,
                this.highlighter,
                s || !i,
              );
              this.renderCache = {
                file: e,
                options: n,
                highlighted: a,
                result: t,
                renderRange: void 0,
              };
            }
            (!t || (!s && !i)) &&
              this.asyncHighlight(e).then(({ result: t, options: n }) => {
                (this.renderCache != null &&
                  (this.renderCache.highlighted = !1),
                  this.onHighlightSuccess(e, t, n, !s));
              });
          }
          return this.renderCache.result == null
            ? void 0
            : this.processFileResult(
                this.renderCache.file,
                t,
                this.renderCache.result,
              );
        }
        async asyncRender(e, t = Wt) {
          let { result: n } = await this.asyncHighlight(e);
          return this.processFileResult(e, t, n);
        }
        async asyncHighlight(e) {
          let t = Kh(
            this.getOrCreateLineCache(e).length,
            this.getTokenizeMaxLength(),
          );
          this.computedLang = t ? `text` : (e.lang ?? Am(e.name));
          let n = this.highlighter != null && pm(lm(this.options.theme)),
            r = t || (this.highlighter != null && da(this.computedLang));
          return (
            (this.highlighter == null || !n || !r) &&
              (this.highlighter = await this.initializeHighlighter()),
            this.renderFileWithHighlighter(e, this.highlighter, t)
          );
        }
        renderFileWithHighlighter(e, t, n = !1) {
          let { options: r } = this.getRenderOptions(e);
          return { result: jh(e, t, r, { forcePlainText: n }), options: r };
        }
        processFileResult(e, t, { code: n, themeStyles: r, baseThemeType: i }) {
          let a = this.getLineCount(e),
            { disableFileHeader: o = !1 } = this.options,
            s = [],
            c = _n(),
            l = Math.min(t.startingLine + t.totalLines, a),
            u = 0,
            d = Lh(t) ? Ih(this.lineAnnotations) : void 0;
          d != null &&
            (c.children.push(yn(`context`, `annotation`, 1)),
            s.push(
              ym({
                type: `annotation`,
                hunkIndex: -1,
                lineIndex: -1,
                annotations: d.map((e) => zm(e)),
              }),
            ),
            u++);
          for (let r = t.startingLine; r < l; r++) {
            let t = r + 1,
              i = n[r];
            if (i == null) {
              let n = `FileRenderer.processFileResult: Line doesnt exist`;
              throw (
                console.error(n, { name: e.name, lineIndex: r, lineNumber: t }),
                Error(n)
              );
            }
            (c.children.push(vn(`context`, t, `${r}`)), s.push(i), u++);
            let a = this.lineAnnotations[t];
            a != null &&
              (c.children.push(yn(`context`, `annotation`, 1)),
              s.push(
                ym({
                  type: `annotation`,
                  hunkIndex: 0,
                  lineIndex: t,
                  annotations: a.map((e) => zm(e)),
                }),
              ),
              u++);
          }
          return (
            (c.properties.style = `grid-row: span ${u}`),
            {
              gutterAST: c.children ?? [],
              contentAST: s,
              preAST: this.createPreElement(a),
              headerAST: o ? void 0 : this.renderHeader(e),
              totalLines: a,
              rowCount: u,
              themeStyles: r,
              baseThemeType: i,
              bufferBefore: t.bufferBefore,
              bufferAfter: t.bufferAfter,
              css: ``,
            }
          );
        }
        renderHeader(e) {
          let { headerRenderMode: t = `default`, stickyHeader: n = !1 } =
            this.options;
          return Cm({ fileOrDiff: e, mode: t, stickyHeader: n });
        }
        renderFullHTML(e) {
          return Ei(this.renderFullAST(e));
        }
        renderFullAST(e, t = []) {
          return (
            t.push(
              I({
                tagName: `code`,
                children: this.renderCodeAST(e),
                properties: { "data-code": `` },
              }),
            ),
            { ...e.preAST, children: t }
          );
        }
        renderCodeAST(e) {
          let t = _n();
          return (
            (t.children = e.gutterAST),
            (t.properties.style = `grid-row: span ${e.rowCount}`),
            [t, Bh(e.contentAST, e.rowCount)]
          );
        }
        renderPartialHTML(e, t = !1) {
          return Ei(
            t
              ? I({
                  tagName: `code`,
                  children: e,
                  properties: { "data-code": `` },
                })
              : e,
          );
        }
        async initializeHighlighter() {
          return (
            (this.highlighter = await im(Lm(this.computedLang, this.options))),
            this.highlighter
          );
        }
        onHighlightSuccess(e, t, n, r = !0) {
          if (this.renderCache == null) return;
          let i =
            !fn(e, this.renderCache.file) ||
            !this.renderCache.highlighted ||
            !hm(n, this.renderCache.options);
          ((this.renderCache = {
            file: e,
            options: n,
            highlighted: r,
            result: t,
            renderRange: void 0,
          }),
            i && this.onRenderUpdate?.());
        }
        getMatchingWorkerResultCache(e, t) {
          let n = this.workerManager?.getFileResultCache(e);
          if (!(n == null || !hm(t, n.options))) return n;
        }
        hasHighlightedRenderCache(e, t) {
          let { renderCache: n } = this;
          return (
            n?.result != null &&
            n.highlighted &&
            fn(e, n.file) &&
            hm(t, n.options)
          );
        }
        onHighlightError(e) {
          console.error(e);
        }
        getTokenizeMaxLength() {
          return this.options.tokenizeMaxLength ?? 1e5;
        }
        createPreElement(e) {
          let { disableLineNumbers: t = !1, overflow: n = `scroll` } =
            this.options;
          return Dm({
            type: `file`,
            diffIndicators: `none`,
            disableBackground: !0,
            disableLineNumbers: t,
            overflow: n,
            split: !1,
            totalLines: e,
          });
        }
      }));
  }),
  Xh,
  Zh = e(() => {
    Xh = `<svg data-icon-sprite aria-hidden="true" width="0" height="0">
  <symbol id="diffs-icon-arrow-right-short" viewBox="0 0 16 16">
    <path d="M8.47 4.22a.75.75 0 0 0 0 1.06l1.97 1.97H3.75a.75.75 0 0 0 0 1.5h6.69l-1.97 1.97a.75.75 0 1 0 1.06 1.06l3.25-3.25a.75.75 0 0 0 0-1.06L9.53 4.22a.75.75 0 0 0-1.06 0"/>
  </symbol>
  <symbol id="diffs-icon-brand-github" viewBox="0 0 16 16">
    <path d="M8 0c4.42 0 8 3.58 8 8a8.01 8.01 0 0 1-5.45 7.59c-.4.08-.55-.17-.55-.38 0-.27.01-1.13.01-2.2 0-.75-.25-1.23-.54-1.48 1.78-.2 3.65-.88 3.65-3.95 0-.88-.31-1.59-.82-2.15.08-.2.36-1.02-.08-2.12 0 0-.67-.22-2.2.82-.64-.18-1.32-.27-2-.27s-1.36.09-2 .27c-1.53-1.03-2.2-.82-2.2-.82-.44 1.1-.16 1.92-.08 2.12-.51.56-.82 1.28-.82 2.15 0 3.06 1.86 3.75 3.64 3.95-.23.2-.44.55-.51 1.07-.46.21-1.61.55-2.33-.66-.15-.24-.6-.83-1.23-.82-.67.01-.27.38.01.53.34.19.73.9.82 1.13.16.45.68 1.31 2.69.94 0 .67.01 1.3.01 1.49 0 .21-.15.45-.55.38A7.995 7.995 0 0 1 0 8c0-4.42 3.58-8 8-8"/>
  </symbol>
  <symbol id="diffs-icon-chevron" viewBox="0 0 16 16">
    <path d="M1.47 4.47a.75.75 0 0 1 1.06 0L8 9.94l5.47-5.47a.75.75 0 1 1 1.06 1.06l-6 6a.75.75 0 0 1-1.06 0l-6-6a.75.75 0 0 1 0-1.06"/>
  </symbol>
  <symbol id="diffs-icon-chevrons-narrow" viewBox="0 0 10 16">
    <path d="M4.47 2.22a.75.75 0 0 1 1.06 0l3.25 3.25a.75.75 0 0 1-1.06 1.06L5 3.81 2.28 6.53a.75.75 0 0 1-1.06-1.06zM1.22 9.47a.75.75 0 0 1 1.06 0L5 12.19l2.72-2.72a.75.75 0 0 1 1.06 1.06l-3.25 3.25a.75.75 0 0 1-1.06 0l-3.25-3.25a.75.75 0 0 1 0-1.06"/>
  </symbol>
  <symbol id="diffs-icon-diff-split" viewBox="0 0 16 16">
    <path d="M14 0H8.5v16H14a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2m-1.5 6.5v1h1a.5.5 0 0 1 0 1h-1v1a.5.5 0 0 1-1 0v-1h-1a.5.5 0 0 1 0-1h1v-1a.5.5 0 0 1 1 0"/><path d="M2 0a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h5.5V0zm.5 7.5h3a.5.5 0 0 1 0 1h-3a.5.5 0 0 1 0-1" opacity=".3"/>
  </symbol>
  <symbol id="diffs-icon-diff-unified" viewBox="0 0 16 16">
    <path fill-rule="evenodd" d="M16 14a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2V8.5h16zm-8-4a.5.5 0 0 0-.5.5v1h-1a.5.5 0 0 0 0 1h1v1a.5.5 0 0 0 1 0v-1h1a.5.5 0 0 0 0-1h-1v-1A.5.5 0 0 0 8 10" clip-rule="evenodd"/><path fill-rule="evenodd" d="M14 0a2 2 0 0 1 2 2v5.5H0V2a2 2 0 0 1 2-2zM6.5 3.5a.5.5 0 0 0 0 1h3a.5.5 0 0 0 0-1z" clip-rule="evenodd" opacity=".4"/>
  </symbol>
  <symbol id="diffs-icon-expand" viewBox="0 0 16 16">
    <path d="M3.47 5.47a.75.75 0 0 1 1.06 0L8 8.94l3.47-3.47a.75.75 0 1 1 1.06 1.06l-4 4a.75.75 0 0 1-1.06 0l-4-4a.75.75 0 0 1 0-1.06"/>
  </symbol>
  <symbol id="diffs-icon-expand-all" viewBox="0 0 16 16">
    <path d="M11.47 9.47a.75.75 0 1 1 1.06 1.06l-4 4a.75.75 0 0 1-1.06 0l-4-4a.75.75 0 1 1 1.06-1.06L8 12.94zM7.526 1.418a.75.75 0 0 1 1.004.052l4 4a.75.75 0 1 1-1.06 1.06L8 3.06 4.53 6.53a.75.75 0 1 1-1.06-1.06l4-4z"/>
  </symbol>
  <symbol id="diffs-icon-file-code" viewBox="0 0 16 16">
    <path d="M10.75 0c.199 0 .39.08.53.22l3.5 3.5c.14.14.22.331.22.53v9A2.75 2.75 0 0 1 12.25 16h-8.5A2.75 2.75 0 0 1 1 13.25V2.75A2.75 2.75 0 0 1 3.75 0zm-7 1.5c-.69 0-1.25.56-1.25 1.25v10.5c0 .69.56 1.25 1.25 1.25h8.5c.69 0 1.25-.56 1.25-1.25V5h-1.25A2.25 2.25 0 0 1 10 2.75V1.5z"/><path d="M7.248 6.19a.75.75 0 0 1 .063 1.058L5.753 9l1.558 1.752a.75.75 0 0 1-1.122.996l-2-2.25a.75.75 0 0 1 0-.996l2-2.25a.75.75 0 0 1 1.06-.063M8.69 7.248a.75.75 0 1 1 1.12-.996l2 2.25a.75.75 0 0 1 0 .996l-2 2.25a.75.75 0 1 1-1.12-.996L10.245 9z"/>
  </symbol>
  <symbol id="diffs-icon-plus" viewBox="0 0 16 16">
    <path d="M8 3a.75.75 0 0 1 .75.75v3.5h3.5a.75.75 0 0 1 0 1.5h-3.5v3.5a.75.75 0 0 1-1.5 0v-3.5h-3.5a.75.75 0 0 1 0-1.5h3.5v-3.5A.75.75 0 0 1 8 3"/>
  </symbol>
  <symbol id="diffs-icon-symbol-added" viewBox="0 0 16 16">
    <path d="M8 4a.75.75 0 0 1 .75.75v2.5h2.5a.75.75 0 0 1 0 1.5h-2.5v2.5a.75.75 0 0 1-1.5 0v-2.5h-2.5a.75.75 0 0 1 0-1.5h2.5v-2.5A.75.75 0 0 1 8 4"/><path d="M1.788 4.296c.196-.88.478-1.381.802-1.706s.826-.606 1.706-.802C5.194 1.588 6.387 1.5 8 1.5s2.806.088 3.704.288c.88.196 1.381.478 1.706.802s.607.826.802 1.706c.2.898.288 2.091.288 3.704s-.088 2.806-.288 3.704c-.195.88-.478 1.381-.802 1.706s-.826.607-1.706.802c-.898.2-2.091.288-3.704.288s-2.806-.088-3.704-.288c-.88-.195-1.381-.478-1.706-.802s-.606-.826-.802-1.706C1.588 10.806 1.5 9.613 1.5 8s.088-2.806.288-3.704M8 0C1.412 0 0 1.412 0 8s1.412 8 8 8 8-1.412 8-8-1.412-8-8-8"/>
  </symbol>
  <symbol id="diffs-icon-symbol-deleted" viewBox="0 0 16 16">
    <path d="M4 8a.75.75 0 0 1 .75-.75h6.5a.75.75 0 0 1 0 1.5h-6.5A.75.75 0 0 1 4 8"/><path d="M1.788 4.296c.196-.88.478-1.381.802-1.706s.826-.606 1.706-.802C5.194 1.588 6.387 1.5 8 1.5s2.806.088 3.704.288c.88.196 1.381.478 1.706.802s.607.826.802 1.706c.2.898.288 2.091.288 3.704s-.088 2.806-.288 3.704c-.195.88-.478 1.381-.802 1.706s-.826.607-1.706.802c-.898.2-2.091.288-3.704.288s-2.806-.088-3.704-.288c-.88-.195-1.381-.478-1.706-.802s-.606-.826-.802-1.706C1.588 10.806 1.5 9.613 1.5 8s.088-2.806.288-3.704M8 0C1.412 0 0 1.412 0 8s1.412 8 8 8 8-1.412 8-8-1.412-8-8-8"/>
  </symbol>
  <symbol id="diffs-icon-symbol-diffstat" viewBox="0 0 16 16">
    <path d="M1.788 4.296c.196-.88.478-1.381.802-1.706s.826-.606 1.706-.802C5.194 1.588 6.387 1.5 8 1.5s2.806.088 3.704.288c.88.196 1.381.478 1.706.802s.607.826.802 1.706c.2.898.288 2.091.288 3.704s-.088 2.806-.288 3.704c-.195.88-.478 1.381-.802 1.706s-.826.607-1.706.802c-.898.2-2.091.288-3.704.288s-2.806-.088-3.704-.288c-.88-.195-1.381-.478-1.706-.802s-.606-.826-.802-1.706C1.588 10.806 1.5 9.613 1.5 8s.088-2.806.288-3.704M8 0C1.412 0 0 1.412 0 8s1.412 8 8 8 8-1.412 8-8-1.412-8-8-8"/><path d="M8.75 4.296a.75.75 0 0 0-1.5 0V6.25h-2a.75.75 0 0 0 0 1.5h2v1.5h1.5v-1.5h2a.75.75 0 0 0 0-1.5h-2zM5.25 10a.75.75 0 0 0 0 1.5h5.5a.75.75 0 0 0 0-1.5z"/>
  </symbol>
  <symbol id="diffs-icon-symbol-ignored" viewBox="0 0 16 16">
    <path d="M1.5 8c0 1.613.088 2.806.288 3.704.196.88.478 1.381.802 1.706s.826.607 1.706.802c.898.2 2.091.288 3.704.288s2.806-.088 3.704-.288c.88-.195 1.381-.478 1.706-.802s.607-.826.802-1.706c.2-.898.288-2.091.288-3.704s-.088-2.806-.288-3.704c-.195-.88-.478-1.381-.802-1.706s-.826-.606-1.706-.802C10.806 1.588 9.613 1.5 8 1.5s-2.806.088-3.704.288c-.88.196-1.381.478-1.706.802s-.606.826-.802 1.706C1.588 5.194 1.5 6.387 1.5 8M0 8c0-6.588 1.412-8 8-8s8 1.412 8 8-1.412 8-8 8-8-1.412-8-8m11.53-2.47a.75.75 0 0 0-1.06-1.06l-6 6a.75.75 0 1 0 1.06 1.06z"/>
  </symbol>
  <symbol id="diffs-icon-symbol-modified" viewBox="0 0 16 16">
    <path d="M1.5 8c0 1.613.088 2.806.288 3.704.196.88.478 1.381.802 1.706s.826.607 1.706.802c.898.2 2.091.288 3.704.288s2.806-.088 3.704-.288c.88-.195 1.381-.478 1.706-.802s.607-.826.802-1.706c.2-.898.288-2.091.288-3.704s-.088-2.806-.288-3.704c-.195-.88-.478-1.381-.802-1.706s-.826-.606-1.706-.802C10.806 1.588 9.613 1.5 8 1.5s-2.806.088-3.704.288c-.88.196-1.381.478-1.706.802s-.606.826-.802 1.706C1.588 5.194 1.5 6.387 1.5 8M0 8c0-6.588 1.412-8 8-8s8 1.412 8 8-1.412 8-8 8-8-1.412-8-8m8 3a3 3 0 1 0 0-6 3 3 0 0 0 0 6"/>
  </symbol>
  <symbol id="diffs-icon-symbol-moved" viewBox="0 0 16 16">
    <path d="M1.788 4.296c.196-.88.478-1.381.802-1.706s.826-.606 1.706-.802C5.194 1.588 6.387 1.5 8 1.5s2.806.088 3.704.288c.88.196 1.381.478 1.706.802s.607.826.802 1.706c.2.898.288 2.091.288 3.704s-.088 2.806-.288 3.704c-.195.88-.478 1.381-.802 1.706s-.826.607-1.706.802c-.898.2-2.091.288-3.704.288s-2.806-.088-3.704-.288c-.88-.195-1.381-.478-1.706-.802s-.606-.826-.802-1.706C1.588 10.806 1.5 9.613 1.5 8s.088-2.806.288-3.704M8 0C1.412 0 0 1.412 0 8s1.412 8 8 8 8-1.412 8-8-1.412-8-8-8"/><path d="M8.495 4.695a.75.75 0 0 0-.05 1.06L10.486 8l-2.041 2.246a.75.75 0 0 0 1.11 1.008l2.5-2.75a.75.75 0 0 0 0-1.008l-2.5-2.75a.75.75 0 0 0-1.06-.051m-4 0a.75.75 0 0 0-.05 1.06l2.044 2.248-1.796 1.995a.75.75 0 0 0 1.114 1.004l2.25-2.5a.75.75 0 0 0-.002-1.007l-2.5-2.75a.75.75 0 0 0-1.06-.05"/>
  </symbol>
  <symbol id="diffs-icon-symbol-ref" viewBox="0 0 16 16">
    <path d="M1.5 8c0 1.613.088 2.806.288 3.704.196.88.478 1.381.802 1.706.286.286.71.54 1.41.73V1.86c-.7.19-1.124.444-1.41.73-.324.325-.606.826-.802 1.706C1.588 5.194 1.5 6.387 1.5 8m4 6.397c.697.07 1.522.103 2.5.103 1.613 0 2.806-.088 3.704-.288.88-.195 1.381-.478 1.706-.802s.607-.826.802-1.706c.2-.898.288-2.091.288-3.704s-.088-2.806-.288-3.704c-.195-.88-.478-1.381-.802-1.706s-.826-.606-1.706-.802C10.806 1.588 9.613 1.5 8 1.5c-.978 0-1.803.033-2.5.103zM0 8c0-6.588 1.412-8 8-8s8 1.412 8 8-1.412 8-8 8-8-1.412-8-8m7-2a1 1 0 0 1 1-1h3a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H8a1 1 0 0 1-1-1z"/>
  </symbol>
</svg>`;
  });
function Qh(e, t) {
  return e.lineNumber === t.lineNumber && e.metadata === t.metadata;
}
var $h = e(() => {});
function eg(e, t) {
  return e == null || t == null
    ? e === t
    : tg(e.customProperties, t.customProperties) &&
        e.type === t.type &&
        e.diffIndicators === t.diffIndicators &&
        e.disableBackground === t.disableBackground &&
        e.disableLineNumbers === t.disableLineNumbers &&
        e.overflow === t.overflow &&
        e.split === t.split &&
        e.totalLines === t.totalLines;
}
function tg(e = ng, t = ng) {
  if (e === t) return !0;
  let n = Object.keys(e),
    r = Object.keys(t);
  if (n.length !== r.length) return !1;
  for (let r of n) if (e[r] !== t[r]) return !1;
  return !0;
}
var ng,
  rg = e(() => {
    ng = {};
  });
function ig(e) {
  let t = document.createElement(`div`);
  return (
    (t.dataset.annotationSlot = ``),
    (t.slot = e),
    (t.style.whiteSpace = `normal`),
    t
  );
}
var ag = e(() => {});
function og() {
  let e = document.createElement(`div`);
  return (
    (e.slot = `gutter-utility-slot`),
    (e.style.position = `absolute`),
    (e.style.top = `0`),
    (e.style.bottom = `0`),
    (e.style.textAlign = `center`),
    (e.style.whiteSpace = `normal`),
    (e.style.touchAction = `none`),
    e
  );
}
var sg = e(() => {});
function cg() {
  let e = document.createElement(`style`);
  return (e.setAttribute(Ft, ``), e);
}
var lg = e(() => {
    F();
  }),
  ug,
  dg = e(() => {
    ug = `@layer base {
  :host {
    --diffs-font-fallback: "SF Mono", Monaco, Consolas, "Ubuntu Mono", "Liberation Mono",
      "Courier New", monospace;
    --diffs-header-font-fallback: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue",
      "Noto Sans", "Liberation Sans", Arial, sans-serif;
    --diffs-mixer: light-dark(#000, #fff);
    --diffs-gap-fallback: 8px;
    --diffs-scrollbar-gutter-fallback: 6px;
    --diffs-scrollbar-gutter: var(--diffs-scrollbar-gutter-override, var(--diffs-scrollbar-gutter-measured, var(--diffs-scrollbar-gutter-fallback)));
    --diffs-added-light: #0dbe4e;
    --diffs-added-dark: #5ecc71;
    --diffs-modified-light: #009fff;
    --diffs-modified-dark: #69b1ff;
    --diffs-deleted-light: #ff2e3f;
    --diffs-deleted-dark: #ff6762;
    --diffs-warning-light: #d5a910;
    --diffs-warning-dark: #ffd452;
    color-scheme: light dark;
    font-family: var(--diffs-header-font-family, var(--diffs-header-font-fallback));
    font-size: var(--diffs-font-size, 13px);
    line-height: var(--diffs-line-height, 20px);
    font-feature-settings: var(--diffs-font-features);
    --diffs-bg: light-dark(var(--diffs-light-bg, #fff), var(--diffs-dark-bg, #000));
    --diffs-bg-buffer: var(--diffs-bg-buffer-override, light-dark(color-mix(in lab, var(--diffs-bg) 92%, var(--diffs-mixer)), color-mix(in lab, var(--diffs-bg) 92%, var(--diffs-mixer))));
    --diffs-bg-context: var(--diffs-bg-context-override, light-dark(color-mix(in lab, var(--diffs-bg) 98.5%, var(--diffs-mixer)), color-mix(in lab, var(--diffs-bg) 92.5%, var(--diffs-mixer))));
    --diffs-bg-context-gutter: var(--diffs-bg-context-gutter-override, light-dark(color-mix(in lab, var(--diffs-bg-context) 90%, var(--diffs-bg)), color-mix(in lab, var(--diffs-bg-context) 45%, var(--diffs-bg))));
    --diffs-bg-separator: var(--diffs-bg-separator-override, light-dark(color-mix(in lab, var(--diffs-bg) 96%, var(--diffs-mixer)), color-mix(in lab, var(--diffs-bg) 85%, var(--diffs-mixer))));
    --diffs-fg: light-dark(var(--diffs-light, #000), var(--diffs-dark, #fff));
    --diffs-fg-number: var(--diffs-fg-number-override, light-dark(color-mix(in lab, var(--diffs-fg) 65%, var(--diffs-bg)), color-mix(in lab, var(--diffs-fg) 65%, var(--diffs-bg))));
    --diffs-fg-conflict-marker: var(--diffs-fg-conflict-marker-override, var(--diffs-fg-number));
    --diffs-deletion-base: var(--diffs-deletion-color-override, light-dark(var(--diffs-light-deletion-color, var(--diffs-deletion-color, var(--diffs-deleted-light))), var(--diffs-dark-deletion-color, var(--diffs-deletion-color, var(--diffs-deleted-dark)))));
    --diffs-addition-base: var(--diffs-addition-color-override, light-dark(var(--diffs-light-addition-color, var(--diffs-addition-color, var(--diffs-added-light))), var(--diffs-dark-addition-color, var(--diffs-addition-color, var(--diffs-added-dark)))));
    --diffs-modified-base: var(--diffs-modified-color-override, light-dark(var(--diffs-light-modified-color, var(--diffs-modified-color, var(--diffs-modified-light))), var(--diffs-dark-modified-color, var(--diffs-modified-color, var(--diffs-modified-dark)))));
    --diffs-bg-deletion: var(--diffs-bg-deletion-override, light-dark(color-mix(in lab, var(--diffs-bg) 88%, var(--diffs-deletion-base)), color-mix(in lab, var(--diffs-bg) 80%, var(--diffs-deletion-base))));
    --diffs-bg-deletion-emphasis: var(--diffs-bg-deletion-emphasis-override, light-dark(rgb(from var(--diffs-deletion-base) r g b / .15), rgb(from var(--diffs-deletion-base) r g b / .2)));
    --diffs-bg-addition: var(--diffs-bg-addition-override, light-dark(color-mix(in lab, var(--diffs-bg) 88%, var(--diffs-addition-base)), color-mix(in lab, var(--diffs-bg) 80%, var(--diffs-addition-base))));
    --diffs-bg-addition-emphasis: var(--diffs-bg-addition-emphasis-override, light-dark(rgb(from var(--diffs-addition-base) r g b / .15), rgb(from var(--diffs-addition-base) r g b / .2)));
    --diffs-selection-base: var(--diffs-modified-base);
    --diffs-selection-number-fg: light-dark(color-mix(in lab, var(--diffs-selection-base) 65%, var(--diffs-mixer)), color-mix(in lab, var(--diffs-selection-base) 75%, var(--diffs-mixer)));
    background-color: var(--diffs-bg);
    color: var(--diffs-fg);
    display: block;
  }

  pre, code, [data-error-wrapper] {
    isolation: isolate;
    font-family: var(--diffs-font-family, var(--diffs-font-fallback));
    outline: none;
    margin: 0;
    padding: 0;
    display: block;
  }

  pre, code {
    background-color: var(--diffs-bg);
  }

  code {
    contain: content;
  }

  input, button {
    font-family: inherit;
    font-size: inherit;
    line-height: inherit;
  }

  *, :before, :after {
    box-sizing: border-box;
  }

  [data-icon-sprite] {
    display: none;
  }

  [data-diffs-header], [data-separator] {
    font-family: var(--diffs-header-font-family, var(--diffs-header-font-fallback));
  }

  [data-diffs-header][data-sticky] {
    z-index: 1;
    background-color: var(--diffs-bg);
    position: sticky;
    top: 0;
  }

  [data-file-info] {
    color: var(--fg);
    background-color: color-mix(in lab, var(--bg) 98%, var(--fg));
    border-block: 1px solid color-mix(in lab, var(--bg) 95%, var(--fg));
    padding: 10px;
    font-weight: 700;
  }

  [data-diff], [data-file] {
    --diffs-grid-number-column-width: minmax(min-content, max-content);
    --diffs-code-grid: var(--diffs-grid-number-column-width) 1fr;

    &[data-dehydrated] {
      --diffs-code-grid: var(--diffs-grid-number-column-width) minmax(0, 1fr);
    }

    &:hover [data-code]::-webkit-scrollbar-thumb {
      background-color: var(--diffs-bg-context);
    }
  }

  @supports (-webkit-touch-callout: none) {
    :host {
      --diffs-scrollbar-gutter-fallback: 0px;
    }
  }

  [data-line] span {
    color: light-dark(var(--diffs-token-light, var(--diffs-light)), var(--diffs-token-dark, var(--diffs-dark)));
    background-color: light-dark(var(--diffs-token-light-bg, inherit), var(--diffs-token-dark-bg, inherit));
    font-weight: light-dark(var(--diffs-token-light-font-weight, inherit), var(--diffs-token-dark-font-weight, inherit));
    font-style: light-dark(var(--diffs-token-light-font-style, inherit), var(--diffs-token-dark-font-style, inherit));
    text-decoration: light-dark(var(--diffs-token-light-text-decoration, inherit), var(--diffs-token-dark-text-decoration, inherit));
  }

  [data-line], [data-gutter-buffer], [data-column-number], [data-line-annotation], [data-no-newline], [data-merge-conflict], [data-merge-conflict-actions], [data-editor-overlay] {
    --diffs-computed-decoration-bg: var(--diffs-bg);
    --diffs-computed-diff-line-bg: var(--diffs-bg);
    --diffs-computed-selected-line-bg: var(--diffs-bg);
    color: var(--diffs-fg);
    background-color: var(--diffs-line-bg, var(--diffs-bg));

    @media (pointer: fine) {
      &:where([data-hovered]) {
        --diffs-computed-hovered-line-bg: light-dark(color-mix(in lab,
            var(--diffs-computed-selected-line-bg) 97%,
            var(--diffs-bg-hover-override, var(--diffs-mixer))), color-mix(in lab,
            var(--diffs-computed-selected-line-bg) 91%,
            var(--diffs-bg-hover-override, var(--diffs-mixer))));
        --diffs-line-bg: var(--diffs-computed-hovered-line-bg, inherit);
      }
    }
  }

  [data-line], [data-no-newline] {
    &[data-decoration-bg] {
      --mix-deco-light: 92%;
      --mix-deco-dark: 85%;

      &[data-decoration-bg-depth="2"] {
        --mix-deco-light: 88%;
        --mix-deco-dark: 80%;
      }

      &[data-decoration-bg-depth="3"] {
        --mix-deco-light: 85%;
        --mix-deco-dark: 78%;
      }

      @media (pointer: fine) {
        &[data-hovered]:not([data-selected-line]) {
          --mix-deco-light: 85%;
          --mix-deco-dark: 85%;
        }

        &[data-hovered]:not([data-selected-line])[data-decoration-bg-depth="2"] {
          --mix-deco-light: 83%;
          --mix-deco-dark: 83%;
        }

        &[data-hovered]:not([data-selected-line])[data-decoration-bg-depth="3"] {
          --mix-deco-light: 81%;
          --mix-deco-dark: 81%;
        }
      }

      --diffs-computed-decoration-bg: light-dark(color-mix(in lab,
          var(--diffs-bg) var(--mix-deco-light),
          var(--diffs-decoration-bg)), color-mix(in lab,
          var(--diffs-bg) var(--mix-deco-dark),
          var(--diffs-decoration-bg)));
      --diffs-computed-diff-line-bg: var(--diffs-computed-decoration-bg);
      --diffs-computed-selected-line-bg: var(--diffs-computed-decoration-bg);
      --diffs-line-bg: var(--diffs-computed-decoration-bg);
    }
  }

  [data-line-annotation], [data-gutter-buffer="annotation"] {
    --diffs-annotation-bg: var(--diffs-bg-context);
    --diffs-computed-decoration-bg: var(--diffs-annotation-bg);
    --diffs-computed-diff-line-bg: var(--diffs-annotation-bg);
    --diffs-computed-selected-line-bg: var(--diffs-annotation-bg);
    --diffs-line-bg: var(--diffs-annotation-bg);
  }

  [data-merge-conflict-actions], [data-gutter-buffer="merge-conflict-action"], [data-gutter-buffer="merge-conflict-marker-base"], [data-gutter-buffer="merge-conflict-marker-separator"], [data-merge-conflict="marker-base"], [data-merge-conflict="marker-separator"] {
    --diffs-computed-decoration-bg: var(--diffs-bg-context);
    --diffs-computed-diff-line-bg: var(--diffs-bg-context);
    --diffs-computed-selected-line-bg: var(--diffs-bg-context);
    --diffs-line-bg: var(--diffs-bg-context);
  }

  [data-gutter-buffer="merge-conflict-marker-start"], [data-merge-conflict="marker-start"] {
    --diffs-computed-decoration-bg: light-dark(color-mix(in lab,
        var(--diffs-bg) 78%,
        var(--conflict-bg-current-header-override, var(--diffs-addition-base))), color-mix(in lab,
        var(--diffs-bg) 68%,
        var(--conflict-bg-current-header-override, var(--diffs-addition-base))));
    --diffs-computed-diff-line-bg: var(--diffs-computed-decoration-bg);
    --diffs-computed-selected-line-bg: var(--diffs-computed-decoration-bg);
    --diffs-line-bg: var(--diffs-computed-decoration-bg);
  }

  [data-gutter-buffer="merge-conflict-marker-end"], [data-merge-conflict="marker-end"] {
    --diffs-computed-decoration-bg: light-dark(color-mix(in lab,
        var(--diffs-bg) 78%,
        var(--conflict-bg-incoming-header-override, var(--diffs-modified-base))), color-mix(in lab,
        var(--diffs-bg) 68%,
        var(--conflict-bg-incoming-header-override, var(--diffs-modified-base))));
    --diffs-computed-diff-line-bg: var(--diffs-computed-decoration-bg);
    --diffs-computed-selected-line-bg: var(--diffs-computed-decoration-bg);
    --diffs-line-bg: var(--diffs-computed-decoration-bg);
  }

  [data-has-merge-conflict] [data-line-annotation], [data-has-merge-conflict] [data-gutter-buffer="annotation"] {
    --diffs-computed-decoration-bg: var(--diffs-bg);
    --diffs-computed-diff-line-bg: var(--diffs-bg);
    --diffs-computed-selected-line-bg: var(--diffs-bg);
    --diffs-line-bg: var(--diffs-bg);
  }

  :where([data-background]) {
    & [data-gutter-buffer], & [data-column-number] {
      --mix-light: 91%;
      --mix-dark: 85%;
    }

    & [data-line], & [data-no-newline] {
      --mix-light: 88%;
      --mix-dark: 80%;
    }

    & [data-gutter-buffer], & [data-column-number], & [data-line], & [data-no-newline] {
      --diffs-diff-line-mix-target: var(--diffs-bg);

      &[data-line-type="change-deletion"] {
        --diffs-diff-line-mix-target: var(--diffs-bg-deletion-override, var(--diffs-deletion-base));

        @media (pointer: fine) {
          &[data-hovered] {
            --mix-light: 80%;
            --mix-dark: 75%;
          }
        }

        &:where([data-gutter-buffer], [data-column-number]) {
          color: var(--diffs-fg-number-deletion-override, var(--diffs-deletion-base));
          --diffs-diff-line-mix-target: var(--diffs-bg-deletion-number-override, var(--diffs-deletion-base));
        }

        --diffs-computed-diff-line-bg: light-dark(color-mix(in lab,
            var(--diffs-computed-decoration-bg) var(--mix-light),
            var(--diffs-diff-line-mix-target)), color-mix(in lab,
            var(--diffs-computed-decoration-bg) var(--mix-dark),
            var(--diffs-diff-line-mix-target)));
        --diffs-computed-selected-line-bg: var(--diffs-computed-diff-line-bg);
        --diffs-line-bg: var(--diffs-computed-diff-line-bg, inherit);
      }

      &[data-line-type="change-addition"] {
        --diffs-diff-line-mix-target: var(--diffs-bg-addition-override, var(--diffs-addition-base));

        @media (pointer: fine) {
          &[data-hovered] {
            --mix-light: 80%;
            --mix-dark: 70%;
          }
        }

        &:where([data-gutter-buffer], [data-column-number]) {
          color: var(--diffs-fg-number-addition-override, var(--diffs-addition-base));
          --diffs-diff-line-mix-target: var(--diffs-bg-addition-number-override, var(--diffs-addition-base));
        }

        --diffs-computed-diff-line-bg: light-dark(color-mix(in lab,
            var(--diffs-computed-decoration-bg) var(--mix-light),
            var(--diffs-diff-line-mix-target)), color-mix(in lab,
            var(--diffs-computed-decoration-bg) var(--mix-dark),
            var(--diffs-diff-line-mix-target)));
        --diffs-computed-selected-line-bg: var(--diffs-computed-diff-line-bg);
        --diffs-line-bg: var(--diffs-computed-diff-line-bg, inherit);
      }

      &[data-merge-conflict="current"] {
        --diffs-diff-line-mix-target: var(--conflict-bg-current-override, var(--diffs-addition-base));

        &:where([data-gutter-buffer], [data-column-number]) {
          color: var(--diffs-fg-number-addition-override, var(--diffs-addition-base));
          --diffs-diff-line-mix-target: var(--conflict-bg-current-number-override, var(--diffs-addition-base));
        }

        @media (pointer: fine) {
          &[data-hovered] {
            --mix-light: 80%;
            --mix-dark: 70%;
          }
        }

        --diffs-computed-diff-line-bg: light-dark(color-mix(in lab,
            var(--diffs-computed-decoration-bg) var(--mix-light),
            var(--diffs-diff-line-mix-target)), color-mix(in lab,
            var(--diffs-computed-decoration-bg) var(--mix-dark),
            var(--diffs-diff-line-mix-target)));
        --diffs-computed-selected-line-bg: var(--diffs-computed-diff-line-bg);
        --diffs-line-bg: var(--diffs-computed-diff-line-bg, inherit);
      }

      &[data-merge-conflict="incoming"] {
        --diffs-diff-line-mix-target: var(--conflict-bg-incoming-override, var(--diffs-modified-base));

        &:where([data-gutter-buffer], [data-column-number]) {
          color: var(--diffs-modified-base);
          --diffs-diff-line-mix-target: var(--conflict-bg-incoming-number-override, var(--diffs-modified-base));
        }

        @media (pointer: fine) {
          &[data-hovered] {
            --mix-light: 80%;
            --mix-dark: 70%;
          }
        }

        --diffs-computed-diff-line-bg: light-dark(color-mix(in lab,
            var(--diffs-computed-decoration-bg) var(--mix-light),
            var(--diffs-diff-line-mix-target)), color-mix(in lab,
            var(--diffs-computed-decoration-bg) var(--mix-dark),
            var(--diffs-diff-line-mix-target)));
        --diffs-computed-selected-line-bg: var(--diffs-computed-diff-line-bg);
        --diffs-line-bg: var(--diffs-computed-diff-line-bg, inherit);
      }
    }
  }

  [data-gutter-buffer], [data-column-number], [data-line], [data-line-annotation], [data-merge-conflict], [data-merge-conflict-actions], [data-no-newline], [data-editor-overlay] {
    --diffs-selection-mix-target: var(--diffs-bg-selection-override, var(--diffs-selection-base));

    &:where([data-editor-overlay]), &:where([data-line], [data-line-annotation], [data-merge-conflict], [data-merge-conflict-actions], [data-no-newline])[data-selected-line] {
      --mix-selection-light: 82%;
      --mix-selection-dark: 75%;

      @media (pointer: fine) {
        &[data-hovered]:not([data-merge-conflict], [data-line-type="change-addition"], [data-line-type="change-deletion"]) {
          --mix-selection-light: 75%;
          --mix-selection-dark: 70%;
        }
      }
    }

    &:where([data-gutter-buffer], [data-column-number])[data-selected-line] {
      --mix-selection-light: 75%;
      --mix-selection-dark: 60%;
      --diffs-selection-mix-target: var(--diffs-bg-selection-number-override, var(--diffs-selection-base));

      @media (pointer: fine) {
        &[data-hovered]:not([data-merge-conflict], [data-line-type="change-addition"], [data-line-type="change-deletion"]) {
          --mix-selection-light: 70%;
          --mix-selection-dark: 55%;
        }
      }
    }

    &:where([data-editor-overlay]), &[data-selected-line] {
      --diffs-computed-selected-line-bg: light-dark(color-mix(in lab,
          var(--diffs-computed-diff-line-bg) var(--mix-selection-light),
          var(--diffs-selection-mix-target)), color-mix(in lab,
          var(--diffs-computed-diff-line-bg) var(--mix-selection-dark),
          var(--diffs-selection-mix-target)));
      --diffs-line-bg: var(--diffs-computed-selected-line-bg, inherit);
    }
  }

  [data-gutter-buffer], [data-column-number] {
    &[data-selected-line] {
      color: var(--diffs-selection-number-fg);
    }
  }

  [data-no-newline] {
    user-select: none;

    & span {
      opacity: .6;
    }
  }

  [data-diff-type="split"][data-overflow="scroll"] {
    grid-template-columns: 1fr 1fr;
    display: grid;

    & [data-additions] {
      border-left: 1px solid var(--diffs-bg);
    }

    & [data-deletions] {
      border-right: 1px solid var(--diffs-bg);
    }
  }

  [data-code] {
    grid-auto-flow: dense;
    grid-template-columns: var(--diffs-code-grid);
    overflow: var(--diffs-overflow-override, scroll) clip;
    overscroll-behavior-x: none;
    tab-size: var(--diffs-tab-size, 2);
    padding-top: var(--diffs-gap-block, var(--diffs-gap-fallback));
    padding-bottom: max(0px,
      calc(var(--diffs-gap-block, var(--diffs-gap-fallback)) -
          var(--diffs-scrollbar-gutter)));
    scrollbar-gutter: stable;
    align-self: flex-start;
    display: grid;
  }

  [data-diffs-scrollbar-measure] {
    opacity: 0;
    pointer-events: none;
    scrollbar-gutter: auto;
    grid-template-columns: none;
    width: 100px;
    height: 100px;
    padding: 0;
    position: absolute;
    top: -200px;
    left: -200px;
  }

  [data-container-size] {
    container-type: inline-size;
  }

  [data-code]::-webkit-scrollbar {
    width: 0;
    height: var(--diffs-scrollbar-gutter);
  }

  [data-code]::-webkit-scrollbar-track {
    background: none;
  }

  [data-code]::-webkit-scrollbar-thumb {
    background-color: #0000;
    background-clip: content-box;
    border: 1px solid #0000;
    border-radius: 3px;
  }

  [data-code]::-webkit-scrollbar-corner {
    background-color: #0000;
  }

  @supports ((-moz-appearance: none)) {
    [data-code] {
      scrollbar-width: thin;
      scrollbar-color: var(--diffs-bg-context) transparent;
      padding-bottom: var(--diffs-gap-block, var(--diffs-gap-fallback));
    }
  }

  [data-diffs-header] ~ [data-diff], [data-diffs-header] ~ [data-file] {
    & [data-code], &[data-overflow="wrap"] {
      padding-top: 0;
    }
  }

  [data-gutter] {
    grid-template-rows: subgrid;
    grid-template-columns: subgrid;
    z-index: 3;
    background-color: var(--diffs-bg);
    grid-column: 1;
    display: grid;
    position: relative;

    & [data-gutter-buffer], & [data-column-number] {
      border-right: var(--diffs-gap-style, 2px solid var(--diffs-bg));
    }
  }

  [data-content] {
    grid-template-rows: subgrid;
    grid-template-columns: subgrid;
    background-color: var(--diffs-bg);
    grid-column: 2;
    min-width: 0;
    display: grid;
  }

  [data-diff-type="split"][data-overflow="wrap"] {
    grid-auto-flow: dense;
    grid-template-columns: repeat(2, var(--diffs-code-grid));
    padding-block: var(--diffs-gap-block, var(--diffs-gap-fallback));
    display: grid;

    & [data-deletions] {
      display: contents;

      & [data-gutter] {
        grid-column: 1;
      }

      & [data-content] {
        border-right: 1px solid var(--diffs-bg);
        grid-column: 2;
      }
    }

    & [data-additions] {
      display: contents;

      & [data-gutter] {
        border-left: 1px solid var(--diffs-bg);
        grid-column: 3;
      }

      & [data-content] {
        grid-column: 4;
      }
    }
  }

  [data-overflow="scroll"] [data-gutter] {
    position: sticky;
    left: 0;
  }

  [data-interactive-lines] [data-line] {
    cursor: pointer;
  }

  [data-interactive-line-numbers] [data-column-number] {
    cursor: pointer;
    touch-action: none;
  }

  [data-content-buffer], [data-gutter-buffer] {
    user-select: none;
    min-height: 1lh;
    position: relative;
  }

  [data-gutter-buffer] {
    padding-left: 2ch;
    padding-right: 1ch;

    &:before {
      content: "";
      min-width: var(--diffs-min-number-column-width, var(--diffs-min-number-column-width-default, 3ch));
      display: block;
    }
  }

  [data-gutter-buffer="annotation"] {
    --diffs-annotation-bg: var(--diffs-bg-context-gutter);
    min-height: 0;
  }

  [data-gutter-buffer="buffer"] {
    --diffs-line-bg: var(--diffs-bg-context-gutter);
  }

  [data-content-buffer] {
    background-position: 5px 0;
    background-size: 8px 8px;
    background-origin: border-box;
    background-image: repeating-linear-gradient(-45deg,
      transparent,
      transparent calc(3px * 1.414),
      var(--diffs-bg-buffer) calc(3px * 1.414),
      var(--diffs-bg-buffer) calc(4px * 1.414));
    grid-column: 1;
  }

  [data-separator] {
    box-sizing: content-box;
    background-color: var(--diffs-bg);
  }

  [data-separator="simple"] {
    min-height: 4px;
  }

  [data-separator="line-info"], [data-separator="line-info-basic"], [data-separator="metadata"], [data-separator="simple"] {
    background-color: var(--diffs-bg-separator);
  }

  [data-separator="line-info"], [data-separator="line-info-basic"], [data-separator="metadata"] {
    height: 32px;
    position: relative;
  }

  [data-separator-wrapper] {
    user-select: none;
    fill: currentColor;
    background-color: var(--diffs-bg);
    align-items: center;
    height: 100%;
    display: flex;
    position: absolute;
    inset-inline: 0;
  }

  [data-content] [data-separator-wrapper] {
    display: none;
  }

  [data-separator="metadata"] [data-separator-wrapper] {
    background-color: var(--diffs-bg-separator);
    height: 100%;
    color: var(--diffs-fg-number);
    white-space: nowrap;
    text-overflow: ellipsis;
    min-width: min-content;
    padding-inline: 1ch;
    inset-inline: 100% auto;
    overflow: hidden;
  }

  [data-separator="line-info"] {
    margin-block: var(--diffs-gap-block, var(--diffs-gap-fallback));

    & [data-separator-wrapper] {
      min-width: 16px;
    }
  }

  [data-separator="line-info-basic"], [data-separator="metadata"] {
    margin-block: 0;
  }

  [data-separator="line-info"][data-separator-first] {
    margin-top: 0;
  }

  [data-separator="line-info"][data-separator-last] {
    margin-bottom: 0;
  }

  [data-expand-index] [data-separator-wrapper] {
    grid-template-columns: 32px auto;
    display: grid;
  }

  [data-expand-index] [data-separator-wrapper][data-separator-multi-button] {
    grid-template-columns: 32px 32px auto;
  }

  [data-expand-button], [data-separator-content] {
    background-color: var(--diffs-bg-separator);
    flex: none;
    align-items: center;
    display: flex;
  }

  [data-expand-index] [data-separator-content]:hover {
    cursor: pointer;
    text-decoration: underline;
  }

  [data-expand-button] {
    cursor: pointer;
    min-width: 32px;
    color: var(--diffs-fg-number);
    border-right: 2px solid var(--diffs-bg);
    flex-shrink: 0;
    justify-content: center;
    align-self: stretch;

    &:hover {
      color: var(--diffs-fg);
    }

    &[data-expand-all-button] {
      display: none;
    }
  }

  [data-expand-down] [data-icon] {
    transform: scaleY(-1);
  }

  [data-separator-content] {
    height: 100%;
    color: var(--diffs-fg-number);
    flex: auto;
    justify-content: flex-start;
    padding: 0 1ch;
    overflow: hidden;
  }

  [data-separator="line-info"], [data-separator="line-info-basic"] {
    & [data-separator-content] {
      user-select: none;
      height: 100%;
      overflow: clip;
    }
  }

  [data-unmodified-lines] {
    text-overflow: ellipsis;
    white-space: nowrap;
    flex: 0 auto;
    min-width: 0;
    display: block;
    overflow: hidden;
  }

  @supports (width: 1cqi) {
    [data-unified] {
      & [data-separator="line-info"] [data-separator-wrapper] {
        padding-inline: var(--diffs-gap-inline, var(--diffs-gap-fallback));
        width: 100cqi;

        & [data-separator-content] {
          border-radius: 6px;
        }
      }

      & [data-separator="line-info"][data-expand-index] [data-separator-wrapper] [data-separator-content] {
        border-top-left-radius: unset;
        border-bottom-left-radius: unset;
      }
    }

    [data-gutter] {
      & [data-separator="line-info"] [data-separator-wrapper] {
        padding-left: var(--diffs-gap-inline, var(--diffs-gap-fallback));
      }

      & [data-separator="line-info"] [data-separator-content] {
        border-top-left-radius: 6px;
        border-bottom-left-radius: 6px;
      }

      & [data-separator="line-info"][data-expand-index] [data-separator-content] {
        border-top-left-radius: unset;
        border-bottom-left-radius: unset;
      }
    }

    [data-additions] {
      & [data-content] [data-separator="line-info"] {
        background-color: var(--diffs-bg);

        & [data-separator-wrapper] {
          display: none;
        }
      }

      & [data-gutter] [data-separator="line-info"] [data-separator-wrapper] {
        background-color: var(--diffs-bg-separator);
        border-top-right-radius: 6px;
        border-bottom-right-radius: 6px;
        height: 100%;
        display: block;

        & [data-separator-content], & [data-expand-button] {
          display: none;
        }
      }
    }

    [data-overflow="scroll"] [data-additions] [data-gutter] [data-separator="line-info"] [data-separator-wrapper] {
      width: calc(100cqi - var(--diffs-gap-inline, var(--diffs-gap-fallback)));
    }

    [data-overflow="wrap"] [data-additions] [data-content] [data-separator="line-info"] [data-separator-wrapper] {
      background-color: var(--diffs-bg-separator);
      height: 100%;
      margin-right: var(--diffs-gap-inline, var(--diffs-gap-fallback));
      border-top-right-radius: 6px;
      border-bottom-right-radius: 6px;
      display: block;

      & [data-separator-content], & [data-expand-button] {
        display: none;
      }
    }

    [data-separator="line-info"] [data-separator-wrapper] {
      & [data-expand-both], & [data-expand-down], & [data-expand-up] {
        border-top-left-radius: 6px;
        border-bottom-left-radius: 6px;
      }
    }

    @media (pointer: fine) {
      [data-separator="line-info"] [data-separator-wrapper] {
        &[data-separator-multi-button] {
          & [data-expand-up] {
            border-top-left-radius: 6px;
            border-bottom-left-radius: unset;
          }

          & [data-expand-down] {
            border-bottom-left-radius: 6px;
            border-top-left-radius: unset;
          }
        }
      }
    }
  }

  @media (pointer: coarse) {
    [data-separator="line-info-basic"] [data-separator-wrapper][data-separator-multi-button] {
      grid-template-columns: 34px 34px auto;

      & [data-separator-content] {
        grid-column: unset;
        grid-row: unset;
      }
    }

    @supports (width: 1cqi) {
      [data-separator="line-info"] [data-separator-wrapper] {
        & [data-expand-both], & [data-expand-down], & [data-expand-up] {
          border-top-left-radius: 6px;
          border-bottom-left-radius: 6px;
        }

        &[data-separator-multi-button] {
          & [data-expand-up] {
            border-top-left-radius: 6px;
            border-bottom-left-radius: 6px;
          }

          & [data-expand-down] {
            border-bottom-left-radius: unset;
            border-top-left-radius: unset;
          }
        }
      }
    }
  }

  @media (pointer: fine) {
    [data-separator-wrapper][data-separator-multi-button] {
      grid-template-rows: 50% 50%;
      display: grid;

      & [data-separator-content] {
        grid-area: 1 / 2 / -1;
        min-width: min-content;
      }

      & [data-expand-button] {
        grid-column: 1;
      }
    }

    [data-separator="line-info"] [data-separator-wrapper], [data-separator="line-info"] [data-separator-wrapper][data-separator-multi-button] {
      grid-template-columns: 34px auto;
    }

    [data-separator="line-info-basic"][data-expand-index] [data-separator-wrapper] {
      grid-template-columns: 100% auto;
    }

    [data-separator="line-info"], [data-separator="line-info-basic"] {
      & [data-separator-multi-button] {
        & [data-expand-up] {
          border-bottom: 1px solid var(--diffs-bg);
          border-right: 2px solid var(--diffs-bg);
        }

        & [data-expand-down] {
          border-top: 1px solid var(--diffs-bg);
          border-right: 2px solid var(--diffs-bg);
        }
      }
    }
  }

  [data-additions] [data-gutter] [data-separator-wrapper], [data-additions] [data-separator="line-info-basic"] [data-separator-wrapper], [data-content] [data-separator-wrapper] {
    display: none;
  }

  [data-line-annotation] {
    min-height: var(--diffs-annotation-min-height, 0);
    z-index: 2;
  }

  [data-merge-conflict-actions] {
    z-index: 2;
  }

  [data-separator="custom"] {
    grid-template-columns: subgrid;
    display: grid;
  }

  [data-line], [data-column-number], [data-no-newline] {
    padding-inline: 1ch;
    position: relative;
  }

  [data-indicators="classic"] [data-line] {
    padding-inline-start: 2ch;
  }

  [data-indicators="classic"] {
    & [data-line-type="change-addition"], & [data-line-type="change-deletion"] {
      &[data-no-newline], &[data-line] {
        &:before {
          user-select: none;
          width: 1ch;
          height: 1lh;
          display: inline-block;
          position: absolute;
          top: 0;
          left: 0;
        }
      }
    }

    & [data-line-type="change-addition"] {
      &[data-line], &[data-no-newline] {
        &:before {
          content: "+";
          color: var(--diffs-addition-base);
        }
      }
    }

    & [data-line-type="change-deletion"] {
      &[data-line], &[data-no-newline] {
        &:before {
          content: "-";
          color: var(--diffs-deletion-base);
        }
      }
    }
  }

  [data-indicators="bars"] {
    & [data-line-type="change-deletion"], & [data-line-type="change-addition"] {
      &[data-column-number] {
        &:before {
          content: "";
          user-select: none;
          contain: strict;
          width: 4px;
          height: 100%;
          display: block;
          position: absolute;
          top: 0;
          left: 0;
        }
      }
    }

    & [data-line-type="change-deletion"] {
      &[data-column-number] {
        &:before {
          background-image: linear-gradient(0deg,
            var(--diffs-bg-deletion) 50%,
            var(--diffs-deletion-base) 50%);
          background-repeat: repeat;
          background-size: 2px 2px;
          background-size: calc(1lh / round(1lh / 2px))
            calc(1lh / round(1lh / 2px));
        }
      }
    }

    & [data-line-type="change-addition"] {
      &[data-column-number] {
        &:before {
          background-color: var(--diffs-addition-base);
        }
      }
    }
  }

  [data-overflow="wrap"] {
    & [data-line], & [data-annotation-content] {
      white-space: pre-wrap;
      word-break: break-word;
    }
  }

  [data-overflow="scroll"] [data-line] {
    white-space: pre;
    min-height: 1lh;
  }

  [data-column-number] {
    box-sizing: content-box;
    text-align: right;
    user-select: none;
    color: var(--diffs-fg-number);
    padding-left: 2ch;
  }

  [data-line-number-content] {
    min-width: var(--diffs-min-number-column-width, var(--diffs-min-number-column-width-default, 3ch));
    z-index: 1;
    display: inline-block;
    position: relative;
  }

  [data-disable-line-numbers] {
    & [data-gutter-buffer], & [data-column-number] {
      min-width: 4px;
      padding: 0;

      &:before {
        min-width: 0;
      }
    }

    & [data-line-number-content] {
      display: none;
    }

    & [data-gutter-utility-slot] {
      right: unset;
      justify-content: flex-start;
      left: 0;
    }

    &[data-indicators="bars"] [data-gutter-utility-slot] {
      left: 6px;
    }
  }

  [data-file][data-disable-line-numbers] {
    & [data-gutter-buffer], & [data-column-number] {
      border-right: 0;
      min-width: 0;
    }
  }

  [data-diff-span] {
    box-decoration-break: clone;
    border-radius: 3px;
  }

  [data-line-type="change-addition"] [data-diff-span] {
    background-color: var(--diffs-bg-addition-emphasis);
  }

  [data-line-type="change-deletion"] [data-diff-span] {
    background-color: var(--diffs-bg-deletion-emphasis);
  }

  [data-merge-conflict="marker-start"], [data-merge-conflict="marker-base"], [data-merge-conflict="marker-separator"], [data-merge-conflict="marker-end"] {
    color: var(--diffs-fg);
    padding-left: 1ch;
  }

  [data-merge-conflict="marker-start"], [data-merge-conflict="marker-end"] {
    align-items: center;
    display: flex;

    &:after {
      color: var(--diffs-fg-conflict-marker);
      font-size: .75rem;
      font-style: normal;
      line-height: 1.25rem;
      font-family: var(--diffs-header-font-family, var(--diffs-header-font-fallback));
      padding-left: 1ch;
    }
  }

  [data-merge-conflict="marker-start"]:after {
    content: "(Current Change)";
  }

  [data-merge-conflict="marker-end"]:after {
    content: "(Incoming Change)";
  }

  [data-merge-conflict-actions-content] {
    min-height: 1.75rem;
    font-family: var(--diffs-header-font-family, var(--diffs-header-font-fallback));
    color: var(--diffs-fg);
    align-items: center;
    gap: .25rem;
    padding-inline: .5rem;
    font-size: .75rem;
    line-height: 1.2;
    display: flex;
  }

  [data-merge-conflict-action] {
    appearance: none;
    color: var(--diffs-fg-number);
    font: inherit;
    cursor: pointer;
    background: none;
    border: 0;
    padding: 0;
    font-style: normal;
  }

  [data-merge-conflict-action]:hover {
    color: var(--diffs-fg);
  }

  [data-merge-conflict-action="current"]:hover {
    color: var(--diffs-addition-base);
  }

  [data-merge-conflict-action="incoming"]:hover {
    color: var(--diffs-modified-base);
  }

  [data-merge-conflict-action-separator] {
    color: var(--diffs-fg-number);
    opacity: .6;
    user-select: none;
  }

  [data-diffs-header="default"] {
    background-color: var(--diffs-bg);
    justify-content: space-between;
    align-items: center;
    gap: var(--diffs-gap-inline, var(--diffs-gap-fallback));
    min-height: calc(1lh + (var(--diffs-gap-block, var(--diffs-gap-fallback)) * 3));
    z-index: 2;
    flex-direction: row;
    padding-inline: 16px;
    display: flex;
    position: relative;
    top: 0;
  }

  [data-header-content] {
    align-items: center;
    gap: var(--diffs-gap-inline, var(--diffs-gap-fallback));
    white-space: nowrap;
    flex-direction: row;
    min-width: 0;
    display: flex;
  }

  [data-header-content] [data-prev-name], [data-header-content] [data-title] {
    text-overflow: ellipsis;
    white-space: nowrap;
    direction: rtl;
    min-width: 0;
    overflow: hidden;
  }

  [data-prev-name] {
    opacity: .7;
  }

  [data-rename-icon] {
    fill: currentColor;
    flex-grow: 0;
    flex-shrink: 0;
  }

  [data-diffs-header="default"] [data-metadata] {
    white-space: nowrap;
    align-items: center;
    gap: 1ch;
    display: flex;
  }

  [data-diffs-header="default"] [data-additions-count] {
    font-family: var(--diffs-font-family, var(--diffs-font-fallback));
    color: var(--diffs-addition-base);
  }

  [data-diffs-header="default"] [data-deletions-count] {
    font-family: var(--diffs-font-family, var(--diffs-font-fallback));
    color: var(--diffs-deletion-base);
  }

  [data-change-icon] {
    fill: currentColor;
    flex-shrink: 0;
  }

  [data-change-icon="change"], [data-change-icon="rename-pure"], [data-change-icon="rename-changed"] {
    color: var(--diffs-modified-base);
  }

  [data-change-icon="new"] {
    color: var(--diffs-addition-base);
  }

  [data-change-icon="deleted"] {
    color: var(--diffs-deletion-base);
  }

  [data-change-icon="file"] {
    opacity: .6;
  }

  [data-annotation-content] {
    z-index: 2;
    isolation: isolate;
    align-self: flex-start;
    min-width: 0;
    display: flow-root;
    position: relative;
  }

  [data-overflow="scroll"] [data-annotation-content], [data-overflow="scroll"] [data-merge-conflict-actions-content] {
    width: var(--diffs-column-content-width, auto);
    left: var(--diffs-column-number-width, 0);
    position: sticky;
  }

  [data-annotation-slot] {
    text-wrap-mode: wrap;
    word-break: normal;
    white-space-collapse: collapse;
  }

  [data-gutter-utility-slot] {
    touch-action: none;
    justify-content: flex-end;
    display: flex;
    position: absolute;
    top: 0;
    bottom: 0;
    right: 0;
  }

  [data-utility-button] {
    appearance: none;
    cursor: pointer;
    width: 1lh;
    height: 1lh;
    font-size: var(--diffs-font-size, 13px);
    line-height: var(--diffs-line-height, 20px);
    background-color: var(--diffs-modified-base);
    color: var(--diffs-bg);
    fill: currentColor;
    z-index: 4;
    touch-action: none;
    border: none;
    border-radius: 4px;
    justify-content: center;
    align-items: center;
    margin-right: calc(-1lh + 1ch);
    padding: 0;
    display: flex;
    position: relative;

    &:before {
      content: "";
      display: block;
      position: absolute;
      inset: 0 0 0 -4px;
    }
  }

  [data-decoration-bar-stack] {
    pointer-events: none;
    isolation: isolate;
    z-index: 1;
    background-color: var(--diffs-decoration-bar-color, transparent);
    box-sizing: content-box;
    border-left: 2px solid var(--diffs-bg);
    border-right: 2px solid var(--diffs-bg);
    width: 6px;
    position: absolute;
    top: 0;
    bottom: 0;
    right: -2px;

    [data-decoration-bar-depth="1"] & {
      background-color: color-mix(in lab,
        var(--diffs-bg) 20%,
        var(--diffs-decoration-bar-color, transparent));
    }

    [data-decoration-bar-depth="2"] & {
      background-color: color-mix(in lab,
        var(--diffs-bg) 45%,
        var(--diffs-decoration-bar-color, transparent));
    }

    [data-decoration-bar-depth="3"] & {
      background-color: color-mix(in lab,
        var(--diffs-bg) 65%,
        var(--diffs-decoration-bar-color, transparent));
    }

    [data-decoration-bar-start] & {
      border-top-left-radius: 5px;
      border-top-right-radius: 5px;
    }

    [data-decoration-bar-end] & {
      z-index: 3;
      border-bottom-right-radius: 5px;
      border-bottom-left-radius: 5px;
    }
  }

  [data-placeholder] {
    contain: strict;
  }

  [data-error-wrapper] {
    padding: var(--diffs-gap-block, var(--diffs-gap-fallback))
      var(--diffs-gap-inline, var(--diffs-gap-fallback));
    scrollbar-width: none;
    max-height: 400px;
    overflow: auto;

    & [data-error-message] {
      color: var(--diffs-deletion-base);
      font-size: 18px;
      font-weight: bold;
    }

    & [data-error-stack] {
      color: var(--diffs-fg-number);
    }
  }
}

@layer theme, rendered, unsafe;
`;
  });
function fg(e) {
  if (mg != null) return mg;
  let t = e.host;
  if (typeof HTMLElement < `u` && t instanceof HTMLElement && !t.isConnected)
    return;
  let n = document.createElement(`div`);
  (n.setAttribute(`data-code`, ``), n.setAttribute(It, `true`));
  let r = document.createElement(`div`);
  return (
    (r.style.position = `relative`),
    (r.style.width = `200%`),
    (r.style.height = `200%`),
    n.appendChild(r),
    e.appendChild(n),
    (mg = Math.max(n.offsetHeight - n.clientHeight, 0)),
    n.remove(),
    mg
  );
}
function pg(e) {
  return `${Lt}: ${e == null ? `var(--diffs-scrollbar-gutter-fallback)` : `${e}px`};`;
}
var mg,
  hg = e(() => {
    F();
  });
function gg(e) {
  return `${bg}
@layer unsafe {
  ${e}
}`;
}
function _g(e, t = `system`, n) {
  return `${bg}
@layer rendered {
  :host {${
    t === `system`
      ? ``
      : `
  color-scheme: ${t};`
  }
  ${pg(n)}
  ${e}
  }
}`;
}
function vg(e, t) {
  let n = pg(t);
  return e.replace(xg, n);
}
function yg(e) {
  return e.replace(/[.*+?^${}()|[\]\\]/g, `\\$&`);
}
var bg,
  xg,
  Sg = e(() => {
    (F(),
      hg(),
      (bg = `@layer base, theme, rendered, unsafe;`),
      (xg = RegExp(`${yg(Lt)}\\s*:\\s*[^;]+;`)));
  });
function Cg({
  code: e,
  pre: t,
  columnType: n,
  rowSpan: r,
  containerSize: i = !1,
} = {}) {
  return (
    e ??
      ((e = document.createElement(`code`)),
      e.setAttribute(`data-code`, ``),
      n != null && e.setAttribute(`data-${n}`, ``),
      t?.appendChild(e)),
    r == null
      ? e.style.removeProperty(`grid-row`)
      : e.style.setProperty(`grid-row`, `span ${r}`),
    i
      ? e.setAttribute(`data-container-size`, ``)
      : e.removeAttribute(`data-container-size`),
    e
  );
}
var wg = e(() => {});
function Tg(e, t) {
  if (t == null) return;
  let n = e.shadowRoot ?? e.attachShadow({ mode: `open` });
  n.innerHTML === `` && (n.innerHTML = t);
}
var Eg = e(() => {});
function Dg(
  e,
  {
    type: t,
    diffIndicators: n,
    disableBackground: r,
    disableLineNumbers: i,
    overflow: a,
    split: o,
    totalLines: s,
    customProperties: c,
  },
) {
  if (c != null)
    for (let t in c) {
      let n = c[t];
      n != null && e.setAttribute(t, `${n}`);
    }
  switch (
    (t === `diff`
      ? (e.setAttribute(`data-diff`, ``), e.removeAttribute(`data-file`))
      : (e.setAttribute(`data-file`, ``), e.removeAttribute(`data-diff`)),
    n)
  ) {
    case `bars`:
    case `classic`:
      e.setAttribute(`data-indicators`, n);
      break;
    case `none`:
      e.removeAttribute(`data-indicators`);
      break;
  }
  return (
    i
      ? e.setAttribute(`data-disable-line-numbers`, ``)
      : e.removeAttribute(`data-disable-line-numbers`),
    r
      ? e.removeAttribute(`data-background`)
      : e.setAttribute(`data-background`, ``),
    t === `diff`
      ? e.setAttribute(`data-diff-type`, o ? `split` : `single`)
      : e.removeAttribute(`data-diff-type`),
    e.setAttribute(`data-overflow`, a),
    e.style.setProperty(
      `--diffs-min-number-column-width-default`,
      `${`${s}`.length}ch`,
    ),
    e
  );
}
var Og = e(() => {});
function kg(e) {
  if (typeof HTMLStyleElement < `u` && e instanceof HTMLStyleElement) return !0;
  let t = e.tagName ?? e.nodeName;
  return typeof t == `string` && t.toLowerCase() === `style`;
}
var Ag = e(() => {});
function jg(e) {
  return {
    theme: e?.theme,
    disableLineNumbers: e?.disableLineNumbers,
    overflow: e?.overflow,
    themeType: e?.themeType,
    collapsed: e?.collapsed,
    disableFileHeader: e?.disableFileHeader,
    disableVirtualizationBuffers: e?.disableVirtualizationBuffers,
    stickyHeader: e?.stickyHeader,
    preferredHighlighter: e?.preferredHighlighter,
    useCSSClasses: e?.useCSSClasses,
    useTokenTransformer: e?.useTokenTransformer,
    tokenizeMaxLineLength: e?.tokenizeMaxLineLength,
    tokenizeMaxLength: e?.tokenizeMaxLength,
    unsafeCSS: e?.unsafeCSS,
    headerRenderMode: e?.renderCustomHeader == null ? `default` : `custom`,
  };
}
var Mg = e(() => {});
function Ng({ shadowRoot: e, currentNode: t, themeCSS: n }) {
  if (n.trim() === ``) {
    t?.remove();
    return;
  }
  return (
    (t ??= Pg()),
    (t.textContent = n),
    t.parentNode !== e && e.appendChild(t),
    t
  );
}
function Pg() {
  let e = document.createElement(`style`);
  return (e.setAttribute(Pt, ``), e);
}
var Fg = e(() => {
    F();
  }),
  Ig = e(() => {
    if (
      (F(),
      dg(),
      hg(),
      typeof HTMLElement < `u` && customElements.get(`diffs-container`) == null)
    ) {
      let e;
      class t extends HTMLElement {
        constructor() {
          if ((super(), this.shadowRoot != null)) return;
          let t = this.attachShadow({ mode: `open` });
          (e ?? ((e = new CSSStyleSheet()), e.replaceSync(ug)),
            (t.adoptedStyleSheets = [e]));
        }
        connectedCallback() {
          fg(this.shadowRoot ?? this.attachShadow({ mode: `open` }));
        }
      }
      customElements.define(xt, t);
    }
  });
function Lg(e, t, n = !1) {
  return !n && e == null && t != null;
}
function Rg(e, t, n = !1) {
  return e == null && t != null && !n;
}
var zg,
  Bg,
  Vg,
  Hg = e(() => {
    (F(),
      tn(),
      Ag(),
      pn(),
      $i(),
      aa(),
      vm(),
      Bm(),
      Uh(),
      Yh(),
      Zh(),
      $h(),
      rg(),
      ag(),
      sg(),
      lg(),
      hg(),
      Sg(),
      Mg(),
      wg(),
      Fg(),
      Eg(),
      Og(),
      Ig(),
      Ni(),
      (zg = [``]),
      (Bg = -1),
      (Vg = class {
        options;
        workerManager;
        isContainerManaged;
        static LoadedCustomComponent = !0;
        __id = `file:${++Bg}`;
        type = `file`;
        fileContainer;
        spriteSVG;
        pre;
        code;
        bufferBefore;
        bufferAfter;
        themeCSSStyle;
        appliedThemeCSS;
        hasAdoptedThemeCSS = !1;
        unsafeCSSStyle;
        appliedUnsafeCSS;
        gutterUtilityContent;
        errorWrapper;
        placeHolder;
        lastRenderedHeaderHTML;
        cachedHeaderHTML;
        appliedPreAttributes;
        lastRowCount;
        mounted = !1;
        headerElement;
        headerCustom;
        headerPrefix;
        headerFilenameSuffix;
        headerMetadata;
        fileRenderer;
        resizeManager;
        interactionManager;
        annotationCache = new Map();
        lineAnnotations = [];
        managersDirty = !1;
        file;
        renderRange;
        enabled = !0;
        editor;
        constructor(e = { theme: P }, t, n = !1) {
          ((this.options = e),
            (this.workerManager = t),
            (this.isContainerManaged = n),
            (this.fileRenderer = new Jh(
              e,
              this.handleHighlightRender,
              this.workerManager,
            )),
            (this.resizeManager = new ia()),
            (this.interactionManager = new Qi(`file`, Pi(e))),
            this.workerManager?.subscribeToThemeChanges(this));
        }
        handleHighlightRender = () => {
          this.rerender();
        };
        rerender() {
          !this.enabled ||
            this.file == null ||
            this.render({
              file: this.file,
              forceRender: !0,
              renderRange: this.renderRange,
            });
        }
        onThemeChange() {
          (this.fileRenderer.clearRenderCache(), this.rerender());
        }
        setOptions(e) {
          e != null &&
            ((this.options = e),
            (this.cachedHeaderHTML = void 0),
            this.syncInteractionOptions());
        }
        syncInteractionOptions() {
          this.interactionManager.setOptions(Pi(this.options));
        }
        mergeOptions(e) {
          this.options = { ...this.options, ...e };
        }
        setThemeType(e) {
          (this.options.themeType ?? `system`) !== e &&
            (this.mergeOptions({ themeType: e }),
            this.applyCachedThemeState(e));
        }
        applyCachedThemeState(e) {
          if (
            typeof this.options.theme == `string` ||
            this.fileContainer == null ||
            this.appliedThemeCSS == null
          )
            return !1;
          let t = this.appliedThemeCSS.baseThemeType ?? e;
          return this.appliedThemeCSS.themeType === t
            ? !1
            : (this.applyThemeState(
                this.fileContainer,
                this.appliedThemeCSS.themeStyles,
                e,
                this.appliedThemeCSS.baseThemeType,
              ),
              !0);
        }
        hasThemeChanged() {
          return (
            this.appliedThemeCSS != null &&
            !en(this.appliedThemeCSS.theme, this.options.theme ?? P)
          );
        }
        getHoveredLine = () => this.interactionManager.getHoveredLine();
        setLineAnnotations(e) {
          this.lineAnnotations = e;
        }
        setSelectedLines(e, t) {
          this.interactionManager.setSelection(e, t);
        }
        flushManagers() {
          if (!this.managersDirty || this.pre == null) {
            this.managersDirty = !1;
            return;
          }
          let { overflow: e = `scroll` } = this.options;
          (this.interactionManager.setup(this.pre),
            this.resizeManager.setup(this.pre, e === `wrap`),
            (this.managersDirty = !1));
        }
        cleanUp(e = !1) {
          (this.emitPostRender(!0),
            this.resizeManager.cleanUp(),
            this.interactionManager.cleanUp(),
            (this.managersDirty = !1),
            this.workerManager?.unsubscribeToThemeChanges(this),
            (this.renderRange = void 0),
            this.isContainerManaged || this.fileContainer?.remove(),
            (this.fileContainer = void 0),
            (this.mounted = !1),
            e || (this.lineAnnotations = []),
            this.annotationCache.clear(),
            (this.pre = void 0),
            this.bufferBefore?.remove(),
            (this.bufferBefore = void 0),
            this.bufferAfter?.remove(),
            (this.bufferAfter = void 0),
            (this.appliedPreAttributes = void 0),
            (this.lastRowCount = void 0),
            (this.headerElement = void 0),
            (this.headerPrefix = void 0),
            (this.headerFilenameSuffix = void 0),
            (this.headerMetadata = void 0),
            (this.headerCustom = void 0),
            (this.lastRenderedHeaderHTML = void 0),
            e || (this.cachedHeaderHTML = void 0),
            (this.errorWrapper = void 0),
            (this.themeCSSStyle = void 0),
            (this.appliedThemeCSS = void 0),
            (this.hasAdoptedThemeCSS = !1),
            (this.unsafeCSSStyle = void 0),
            (this.appliedUnsafeCSS = void 0),
            (this.placeHolder = void 0),
            (this.unsafeCSSStyle = void 0),
            e
              ? this.fileRenderer.recycle()
              : (this.fileRenderer.cleanUp(),
                (this.workerManager = void 0),
                (this.file = void 0)),
            (this.enabled = !1),
            this.editor?.cleanUp(),
            (this.editor = void 0));
        }
        virtualizedSetup() {
          ((this.enabled = !0),
            this.workerManager?.subscribeToThemeChanges(this));
        }
        hydrate(e) {
          let {
            fileContainer: t,
            prerenderedHTML: n,
            preventEmit: r = !1,
            file: i,
            lineAnnotations: a,
          } = e;
          if (!this.enabled)
            throw Error(
              `File.hydrate: attempting to call hydrate after cleaned up`,
            );
          if (this.fileContainer != null)
            throw Error(
              `File.hydrate: hydrate can only be called before the instance has rendered or hydrated`,
            );
          (this.hydrateElements(t, n),
            Lg(this.pre, i, this.options.collapsed) ||
            Rg(this.headerElement, i, this.options.disableFileHeader)
              ? this.render({ ...e, preventEmit: !0 })
              : this.hydrationSetup({ file: i, lineAnnotations: a }),
            r || this.emitPostRender());
        }
        hydrateElements(e, t) {
          (this.fileContainer !== e && this.emitPostRender(!0), Tg(e, t));
          for (let t of Array.from(e.shadowRoot?.children ?? [])) {
            if (t instanceof SVGElement) {
              this.spriteSVG = t;
              continue;
            }
            if (t instanceof HTMLElement) {
              if (t instanceof HTMLPreElement) {
                ((this.pre = t), (this.appliedPreAttributes = void 0));
                continue;
              }
              if (
                t instanceof HTMLStyleElement &&
                t.hasAttribute(`data-theme-css`)
              ) {
                this.themeCSSStyle = t;
                continue;
              }
              if (
                t instanceof HTMLStyleElement &&
                t.hasAttribute(`data-unsafe-css`)
              ) {
                ((this.unsafeCSSStyle = t),
                  (this.appliedUnsafeCSS = t.textContent));
                continue;
              }
              if (`diffsHeader` in t.dataset) {
                ((this.headerElement = t),
                  (this.lastRenderedHeaderHTML = void 0));
                continue;
              }
            }
          }
          (this.pre != null &&
            (this.syncCodeNodeFromPre(this.pre),
            this.pre.removeAttribute(`data-dehydrated`)),
            (this.fileContainer = e),
            this.hydrateMeasuredScrollbar());
        }
        hydrationSetup({ file: e, lineAnnotations: t }) {
          ((this.lineAnnotations = t ?? this.lineAnnotations),
            (this.file = e),
            this.fileRenderer.setOptions(jg(this.options)),
            this.syncInteractionOptions(),
            this.pre != null &&
              (this.fileRenderer.hydrate(e),
              this.renderAnnotations(),
              this.renderGutterUtility(),
              this.injectUnsafeCSS(),
              (this.managersDirty = !0),
              this.flushManagers()));
        }
        getOrCreateLineCache(e = this.file) {
          return e == null ? zg : this.fileRenderer.getOrCreateLineCache(e);
        }
        updateBuffers(e) {
          this.pre != null && this.applyBuffers(this.pre, e);
        }
        syncRenderViewToEditor() {
          let e = this.editor,
            t = this.fileContainer,
            n = this.file;
          e != null &&
            t != null &&
            n != null &&
            this.fileRenderer.initializeHighlighter().then((r) => {
              e.__syncRenderView(
                r,
                t,
                n,
                this.lineAnnotations,
                this.renderRange,
              );
            });
        }
        attachEditor(e) {
          return (
            this.editor?.cleanUp(),
            (this.editor = e),
            this.interactionManager.setEditorAttached(!0),
            this.syncRenderViewToEditor(),
            () => {
              ((this.editor = void 0),
                this.interactionManager.setEditorAttached(!1));
            }
          );
        }
        applyDocumentChange(e, t) {
          (this.fileRenderer.applyDocumentChange(e),
            t != null &&
              t !== this.lineAnnotations &&
              this.file != null &&
              (this.setLineAnnotations(t),
              this.fileRenderer.setLineAnnotations(this.lineAnnotations),
              this.renderAnnotations()));
        }
        updateRenderCache(e, t) {
          this.fileRenderer.updateRenderCache(e, t);
        }
        render({
          file: e,
          fileContainer: t,
          forceRender: n = !1,
          preventEmit: r = !1,
          containerWrapper: i,
          deferManagers: a = !1,
          lineAnnotations: o,
          renderRange: s,
        }) {
          if (!this.enabled)
            throw Error(
              `File.render: attempting to call render after cleaned up`,
            );
          (e.cacheKey === void 0 && (e.cacheKey = e.name),
            this.editor?.__postponeBgTokenizeToNextFrame());
          let { collapsed: c = !1, themeType: l = `system` } = this.options,
            u = c ? void 0 : s,
            d = this.renderRange,
            f = this.hasThemeChanged(),
            p =
              o != null && (o.length > 0 || this.lineAnnotations.length > 0)
                ? o !== this.lineAnnotations
                : !1,
            m = !fn(this.file, e);
          if (!c && !n && _m(u, this.renderRange) && !m && !p && !f)
            return this.applyCachedThemeState(l);
          ((this.renderRange = u),
            m && (this.cachedHeaderHTML = void 0),
            (this.file = e),
            this.fileRenderer.setOptions(jg(this.options)),
            this.syncInteractionOptions(),
            o != null && this.setLineAnnotations(o),
            this.fileRenderer.setLineAnnotations(this.lineAnnotations));
          let { disableErrorHandling: h = !1, disableFileHeader: g = !1 } =
            this.options;
          if (
            (g &&
              (this.headerElement != null &&
                (this.headerElement.remove(),
                (this.headerElement = void 0),
                (this.lastRenderedHeaderHTML = void 0)),
              this.clearHeaderSlots()),
            (t = this.getOrCreateFileContainerNode(t, i)),
            this.applyCachedThemeState(l),
            c)
          ) {
            (this.removeRenderedCode(), this.clearAuxiliaryNodes());
            try {
              let n = this.fileRenderer.renderFile(e, Gt);
              (n != null &&
                this.applyThemeState(t, n.themeStyles, l, n.baseThemeType),
                n?.headerAST != null && this.applyHeaderToDOM(n.headerAST, t),
                this.injectUnsafeCSS());
            } catch (e) {
              if (h) throw e;
              (console.error(e),
                e instanceof Error && this.applyErrorToDOM(e, t));
            }
            return (r || this.emitPostRender(), !0);
          }
          try {
            let r = this.getOrCreatePreNode(t);
            if (
              !this.canPartiallyRender(n, p, m || f) ||
              !this.applyPartialRender(d, u)
            ) {
              let n = this.fileRenderer.renderFile(e, u);
              if (n == null)
                return (
                  this.workerManager?.isInitialized() === !1 &&
                    this.workerManager.initialize().then(() => this.rerender()),
                  !1
                );
              (this.applyThemeState(t, n.themeStyles, l, n.baseThemeType),
                n.headerAST != null && this.applyHeaderToDOM(n.headerAST, t),
                this.applyFullRender(n, r));
            }
            (this.applyBuffers(r, u),
              this.injectUnsafeCSS(),
              (this.managersDirty = !0),
              a || this.flushManagers(),
              this.renderAnnotations(),
              this.renderGutterUtility(),
              this.editor != null && this.syncRenderViewToEditor());
          } catch (e) {
            if (h) throw e;
            (console.error(e),
              e instanceof Error && this.applyErrorToDOM(e, t));
          }
          return (r || this.emitPostRender(), !0);
        }
        emitPostRender(e = !1) {
          let {
            fileContainer: t,
            options: { onPostRender: n },
          } = this;
          if (e) {
            if (!this.mounted || ((this.mounted = !1), t == null)) return;
            n?.(t, this, `unmount`);
            return;
          }
          if (t == null) return;
          let r = this.mounted ? `update` : `mount`;
          ((this.mounted = !0), n?.(t, this, r));
        }
        removeRenderedCode() {
          (this.resizeManager.cleanUp(),
            this.interactionManager.cleanUp(),
            this.bufferBefore?.remove(),
            (this.bufferBefore = void 0),
            this.bufferAfter?.remove(),
            (this.bufferAfter = void 0),
            this.code?.remove(),
            (this.code = void 0),
            this.pre?.remove(),
            (this.pre = void 0),
            (this.appliedPreAttributes = void 0),
            (this.lastRowCount = void 0));
        }
        clearAuxiliaryNodes() {
          for (let { element: e } of this.annotationCache.values()) e.remove();
          (this.annotationCache.clear(),
            this.gutterUtilityContent?.remove(),
            (this.gutterUtilityContent = void 0));
        }
        canPartiallyRender(e, t, n) {
          return !(e || t || n);
        }
        renderPlaceholder(e) {
          if (this.fileContainer == null) return !1;
          if (
            (this.emitPostRender(!0),
            this.cleanChildNodes(),
            this.placeHolder == null)
          ) {
            let e =
              this.fileContainer.shadowRoot ??
              this.fileContainer.attachShadow({ mode: `open` });
            ((this.placeHolder = document.createElement(`div`)),
              (this.placeHolder.dataset.placeholder = ``),
              e.appendChild(this.placeHolder));
          }
          return (this.placeHolder.style.setProperty(`height`, `${e}px`), !0);
        }
        primeHighlightCache() {
          let { file: e, workerManager: t } = this;
          e == null ||
            e.cacheKey == null ||
            t == null ||
            Hh(e) ||
            this.fileRenderer.getOrCreateLineCache(e).length >
              (this.options.tokenizeMaxLength ?? 1e5) ||
            t.primeFileHighlightCache(e).catch(() => void 0);
        }
        cleanChildNodes() {
          (this.resizeManager.cleanUp(),
            this.interactionManager.cleanUp(),
            this.bufferAfter?.remove(),
            this.bufferBefore?.remove(),
            this.code?.remove(),
            this.errorWrapper?.remove(),
            this.headerElement?.remove(),
            this.gutterUtilityContent?.remove(),
            this.headerPrefix?.remove(),
            this.headerFilenameSuffix?.remove(),
            this.headerMetadata?.remove(),
            this.headerCustom?.remove(),
            this.pre?.remove(),
            this.spriteSVG?.remove(),
            this.themeCSSStyle?.remove(),
            this.unsafeCSSStyle?.remove(),
            (this.bufferAfter = void 0),
            (this.bufferBefore = void 0),
            (this.code = void 0),
            (this.errorWrapper = void 0),
            (this.headerElement = void 0),
            (this.gutterUtilityContent = void 0),
            (this.headerPrefix = void 0),
            (this.headerFilenameSuffix = void 0),
            (this.headerMetadata = void 0),
            (this.headerCustom = void 0),
            (this.pre = void 0),
            (this.spriteSVG = void 0),
            (this.themeCSSStyle = void 0),
            (this.appliedThemeCSS = void 0),
            (this.hasAdoptedThemeCSS = !1),
            (this.unsafeCSSStyle = void 0),
            (this.appliedUnsafeCSS = void 0),
            (this.lastRenderedHeaderHTML = void 0),
            (this.lastRowCount = void 0),
            (this.mounted = !1));
        }
        renderAnnotations() {
          if (this.isContainerManaged || this.fileContainer == null) {
            for (let { element: e } of this.annotationCache.values())
              e.remove();
            this.annotationCache.clear();
            return;
          }
          let e = new Map(this.annotationCache),
            { renderAnnotation: t } = this.options;
          if (t != null && this.lineAnnotations.length > 0)
            for (let [n, r] of this.lineAnnotations.entries()) {
              let i = `${n}-${zm(r)}`,
                a = this.annotationCache.get(i);
              if (a == null || !Qh(r, a.annotation)) {
                a?.element.remove();
                let e = t(r);
                if (e == null) continue;
                ((a = { element: ig(zm(r)), annotation: r }),
                  a.element.appendChild(e),
                  this.fileContainer.appendChild(a.element),
                  this.annotationCache.set(i, a));
              }
              e.delete(i);
            }
          for (let [t, { element: n }] of e.entries())
            (this.annotationCache.delete(t), n.remove());
        }
        renderGutterUtility() {
          let { renderGutterUtility: e } = this.options;
          if (this.fileContainer == null || e == null) {
            (this.gutterUtilityContent?.remove(),
              (this.gutterUtilityContent = void 0));
            return;
          }
          let t = e(this.interactionManager.getHoveredLine);
          if (t != null && this.gutterUtilityContent != null) return;
          if (t == null) {
            (this.gutterUtilityContent?.remove(),
              (this.gutterUtilityContent = void 0));
            return;
          }
          let n = og();
          (n.appendChild(t),
            this.fileContainer.appendChild(n),
            (this.gutterUtilityContent = n));
        }
        injectUnsafeCSS() {
          let { unsafeCSS: e } = this.options,
            t = this.fileContainer?.shadowRoot;
          if (t != null) {
            if (e == null || e === ``) {
              (this.unsafeCSSStyle != null &&
                (this.unsafeCSSStyle.remove(), (this.unsafeCSSStyle = void 0)),
                (this.appliedUnsafeCSS = void 0));
              return;
            }
            (this.unsafeCSSStyle?.parentNode === t &&
              this.appliedUnsafeCSS === e) ||
              ((this.unsafeCSSStyle ??= cg()),
              this.unsafeCSSStyle.parentNode !== t &&
                t.appendChild(this.unsafeCSSStyle),
              (this.unsafeCSSStyle.textContent = gg(e)),
              (this.appliedUnsafeCSS = e));
          }
        }
        applyThemeState(e, t, n, r) {
          let i = e.shadowRoot ?? e.attachShadow({ mode: `open` }),
            a = r ?? n,
            o = this.options.theme ?? P,
            s = typeof o == `string` ? o : { ...o },
            c = fg(i);
          if (
            this.themeCSSStyle?.parentNode === i &&
            this.appliedThemeCSS?.themeStyles === t &&
            this.appliedThemeCSS.themeType === a &&
            this.appliedThemeCSS.scrollbarGutter === c
          ) {
            this.appliedThemeCSS.theme = s;
            return;
          }
          if (this.hasAdoptedThemeCSS && this.themeCSSStyle?.parentNode === i) {
            ((this.hasAdoptedThemeCSS = !1),
              (this.appliedThemeCSS = {
                theme: s,
                themeStyles: t,
                themeType: a,
                baseThemeType: r,
                scrollbarGutter: c,
              }));
            return;
          }
          ((this.themeCSSStyle = Ng({
            shadowRoot: i,
            currentNode: this.themeCSSStyle,
            themeCSS: _g(t, a, c),
          })),
            (this.appliedThemeCSS =
              this.themeCSSStyle == null
                ? void 0
                : {
                    theme: s,
                    themeStyles: t,
                    themeType: a,
                    baseThemeType: r,
                    scrollbarGutter: c,
                  }));
        }
        hydrateMeasuredScrollbar() {
          let e = this.fileContainer?.shadowRoot;
          e == null ||
            this.themeCSSStyle == null ||
            (this.themeCSSStyle.textContent = vg(
              this.themeCSSStyle.textContent ?? ``,
              fg(e),
            ));
        }
        applyFullRender(e, t) {
          (this.cleanupErrorWrapper(),
            this.applyPreNodeAttributes(t, e),
            (this.code = Cg({ code: this.code })));
          let n = this.fileRenderer.renderCodeAST(e);
          if (this.code.childElementCount >= 2)
            for (let e = 0; e < 2; e++) {
              let t = this.code.children[e],
                r = n[e];
              ((t.innerHTML = Ei(r.children)),
                (t.style.cssText = r.properties.style));
            }
          else this.code.innerHTML = Ei(n);
          (t.contains(this.code) || t.replaceChildren(this.code),
            (this.lastRowCount = e.rowCount));
        }
        applyPartialRender(e, t) {
          if (e == null || t == null) return !1;
          let { file: n, code: r } = this,
            i = r == null ? void 0 : this.getColumns(r);
          if (n == null || r == null || i == null) return !1;
          let a = e.startingLine,
            o = t.startingLine,
            s = e.totalLines === 1 / 0 ? 1 / 0 : a + e.totalLines,
            c = t.totalLines === 1 / 0 ? 1 / 0 : o + t.totalLines,
            l = Math.max(a, o),
            u = Math.min(s, c);
          if (
            u <= l ||
            !this.trimDOMToOverlap(i.gutter, l, u) ||
            !this.trimDOMToOverlap(i.content, l, u)
          )
            return !1;
          let { length: d } = i.content.children,
            f = (e, t) => {
              if (!(t <= 0))
                return this.fileRenderer.renderFile(n, {
                  startingLine: e,
                  totalLines: t,
                  bufferBefore: 0,
                  bufferAfter: 0,
                });
            },
            p = o < l ? f(o, l - o) : void 0;
          if (p === void 0 && o < l) return !1;
          let m = c === 1 / 0 ? 1 / 0 : Math.max(0, c - u),
            h = c > u ? f(u, m) : void 0;
          return h === void 0 && c > u
            ? !1
            : (this.cleanupErrorWrapper(),
              p != null &&
                (i.gutter.insertAdjacentHTML(
                  `afterbegin`,
                  this.fileRenderer.renderPartialHTML(p.gutterAST),
                ),
                i.content.insertAdjacentHTML(
                  `afterbegin`,
                  this.fileRenderer.renderPartialHTML(p.contentAST),
                ),
                (d += p.rowCount)),
              h != null &&
                (i.gutter.insertAdjacentHTML(
                  `beforeend`,
                  this.fileRenderer.renderPartialHTML(h.gutterAST),
                ),
                i.content.insertAdjacentHTML(
                  `beforeend`,
                  this.fileRenderer.renderPartialHTML(h.contentAST),
                ),
                (d += h.rowCount)),
              this.lastRowCount !== d &&
                (i.gutter.style.setProperty(`grid-row`, `span ${d}`),
                i.content.style.setProperty(`grid-row`, `span ${d}`),
                (this.lastRowCount = d)),
              !0);
        }
        getColumns(e) {
          let t = e.children[0],
            n = e.children[1];
          if (!(
            !(t instanceof HTMLElement) ||
            !(n instanceof HTMLElement) ||
            t.dataset.gutter == null ||
            n.dataset.content == null
          ))
            return { gutter: t, content: n };
        }
        trimDOMToOverlap(e, t, n) {
          let r = this.getDOMBoundaryIndices(e, [t, n]),
            i = r.get(t) ?? e.children.length,
            a = r.get(n) ?? e.children.length;
          if (i > a) return !1;
          for (let t = e.children.length - 1; t >= a; --t)
            e.children[t]?.remove();
          for (let t = i - 1; t >= 0; --t) e.children[t]?.remove();
          return !0;
        }
        getDOMBoundaryIndices(e, t) {
          let n = [...new Set(t)].sort((e, t) => e - t),
            r = new Map();
          if (n.length === 0) return r;
          let i = 0,
            a = n[i],
            { children: o } = e;
          a === 0 && (r.set(0, 0), (i += 1), (a = n[i]));
          for (let e = 0; e < o.length; e += 1) {
            let t = o[e];
            if (!(t instanceof HTMLElement)) continue;
            let s = this.getLineIndexFromDOMNode(t);
            if (s != null) {
              for (; a != null && s >= a;) (r.set(a, e), (i += 1), (a = n[i]));
              if (i >= n.length) break;
            }
          }
          for (let e of n) r.has(e) || r.set(e, o.length);
          return r;
        }
        getLineIndexFromDOMNode(e) {
          let t = e.dataset.lineIndex;
          if (t == null) return;
          let n = Number(t);
          return Number.isNaN(n) ? void 0 : n;
        }
        applyBuffers(e, t) {
          if (t == null || this.shouldDisableVirtualizationBuffers()) {
            (this.bufferBefore != null &&
              (this.bufferBefore.remove(), (this.bufferBefore = void 0)),
              this.bufferAfter != null &&
                (this.bufferAfter.remove(), (this.bufferAfter = void 0)));
            return;
          }
          (t.bufferBefore > 0
            ? (this.bufferBefore ??
                ((this.bufferBefore = document.createElement(`div`)),
                (this.bufferBefore.dataset.virtualizerBuffer = `before`),
                e.before(this.bufferBefore)),
              this.bufferBefore.style.setProperty(
                `height`,
                `${t.bufferBefore}px`,
              ),
              this.bufferBefore.style.setProperty(`contain`, `strict`))
            : this.bufferBefore != null &&
              (this.bufferBefore.remove(), (this.bufferBefore = void 0)),
            t.bufferAfter > 0
              ? (this.bufferAfter ??
                  ((this.bufferAfter = document.createElement(`div`)),
                  (this.bufferAfter.dataset.virtualizerBuffer = `after`),
                  e.after(this.bufferAfter)),
                this.bufferAfter.style.setProperty(
                  `height`,
                  `${t.bufferAfter}px`,
                ),
                this.bufferAfter.style.setProperty(`contain`, `strict`))
              : this.bufferAfter != null &&
                (this.bufferAfter.remove(), (this.bufferAfter = void 0)));
        }
        shouldDisableVirtualizationBuffers() {
          return this.options.disableVirtualizationBuffers ?? !1;
        }
        applyHeaderToDOM(e, t) {
          let { file: n } = this;
          if (n == null) return;
          (this.cleanupErrorWrapper(),
            this.placeHolder?.remove(),
            (this.placeHolder = void 0));
          let r = this.cachedHeaderHTML ?? Ei(e);
          if (
            ((this.cachedHeaderHTML = r), r !== this.lastRenderedHeaderHTML)
          ) {
            let e = document.createElement(`div`);
            e.innerHTML = r;
            let n = e.firstElementChild;
            if (!(n instanceof HTMLElement)) return;
            (this.headerElement == null
              ? t.shadowRoot?.prepend(n)
              : t.shadowRoot?.replaceChild(n, this.headerElement),
              (this.headerElement = n),
              (this.lastRenderedHeaderHTML = r));
          }
          if (this.isContainerManaged) return;
          let {
            renderHeaderPrefix: i,
            renderHeaderFilenameSuffix: a,
            renderCustomHeader: o,
            renderHeaderMetadata: s,
          } = this.options;
          if (o != null) {
            let e = o(n) ?? void 0;
            ((this.headerCustom = this.upsertHeaderSlotElement(
              t,
              this.headerCustom,
              Nt,
              e,
            )),
              this.headerPrefix?.remove(),
              this.headerFilenameSuffix?.remove(),
              this.headerMetadata?.remove(),
              (this.headerPrefix = void 0),
              (this.headerFilenameSuffix = void 0),
              (this.headerMetadata = void 0));
          } else {
            let e = i?.(n) ?? void 0,
              r = a?.(n) ?? void 0,
              o = s?.(n) ?? void 0;
            ((this.headerPrefix = this.upsertHeaderSlotElement(
              t,
              this.headerPrefix,
              At,
              e,
            )),
              (this.headerFilenameSuffix = this.upsertHeaderSlotElement(
                t,
                this.headerFilenameSuffix,
                jt,
                r,
              )),
              (this.headerMetadata = this.upsertHeaderSlotElement(
                t,
                this.headerMetadata,
                Mt,
                o,
              )),
              this.headerCustom?.remove(),
              (this.headerCustom = void 0));
          }
        }
        clearHeaderSlots() {
          (this.headerPrefix?.remove(),
            this.headerFilenameSuffix?.remove(),
            this.headerMetadata?.remove(),
            this.headerCustom?.remove(),
            (this.headerPrefix = void 0),
            (this.headerFilenameSuffix = void 0),
            (this.headerMetadata = void 0),
            (this.headerCustom = void 0));
        }
        upsertHeaderSlotElement(e, t, n, r) {
          if (r == null) {
            t?.remove();
            return;
          }
          let i = t ?? this.createHeaderSlotElement(n);
          return (
            t ?? e.appendChild(i),
            this.replaceHeaderSlotContent(i, r),
            i
          );
        }
        replaceHeaderSlotContent(e, t) {
          (e.replaceChildren(),
            t instanceof Element ? e.appendChild(t) : (e.innerText = `${t}`));
        }
        createHeaderSlotElement(e) {
          let t = document.createElement(`div`);
          return ((t.slot = e), t);
        }
        getOrCreateFileContainerNode(e, t) {
          let { fileContainer: n } = this,
            r = e ?? n ?? document.createElement(`diffs-container`),
            i = n !== r;
          return (
            i && this.emitPostRender(!0),
            (this.fileContainer = r),
            n != null &&
              i &&
              ((this.lastRenderedHeaderHTML = void 0),
              (this.headerElement = void 0)),
            t != null &&
              this.fileContainer.parentNode !== t &&
              t.appendChild(this.fileContainer),
            i && this.adoptReusableShellElements(this.fileContainer),
            this.ensureSpriteSVG(this.fileContainer),
            this.fileContainer
          );
        }
        adoptReusableShellElements(e) {
          let { shadowRoot: t } = e;
          if (t != null)
            for (let e of t.children)
              e instanceof SVGElement
                ? (this.spriteSVG ??= e)
                : kg(e) && e.hasAttribute(`data-theme-css`)
                  ? ((this.themeCSSStyle ??= e), (this.hasAdoptedThemeCSS = !0))
                  : kg(e) &&
                    e.hasAttribute(`data-unsafe-css`) &&
                    ((this.unsafeCSSStyle ??= e),
                    (this.appliedUnsafeCSS ??=
                      this.options.unsafeCSS ?? void 0));
        }
        ensureSpriteSVG(e) {
          let t = e.shadowRoot ?? e.attachShadow({ mode: `open` });
          if (this.spriteSVG == null) {
            let e = document.createElement(`div`);
            e.innerHTML = Xh;
            let t = e.firstChild;
            t instanceof SVGElement && (this.spriteSVG = t);
          }
          this.spriteSVG != null &&
            this.spriteSVG.parentNode !== t &&
            t.appendChild(this.spriteSVG);
        }
        getOrCreatePreNode(e) {
          let t = e.shadowRoot ?? e.attachShadow({ mode: `open` });
          return (
            this.pre == null
              ? ((this.pre = document.createElement(`pre`)),
                (this.appliedPreAttributes = void 0),
                (this.code = void 0),
                t.appendChild(this.pre))
              : this.pre.parentNode !== t &&
                (e.shadowRoot?.appendChild(this.pre),
                (this.appliedPreAttributes = void 0)),
            this.placeHolder?.remove(),
            (this.placeHolder = void 0),
            this.pre
          );
        }
        syncCodeNodeFromPre(e) {
          this.code = void 0;
          for (let t of Array.from(e.children))
            if (t instanceof HTMLElement && t.hasAttribute(`data-code`)) {
              this.code = t;
              return;
            }
        }
        applyPreNodeAttributes(e, { totalLines: t }) {
          let { overflow: n = `scroll`, disableLineNumbers: r = !1 } =
              this.options,
            i = {
              type: `file`,
              split: !1,
              overflow: n,
              disableLineNumbers: r,
              diffIndicators: `none`,
              disableBackground: !0,
              totalLines: t,
            };
          eg(i, this.appliedPreAttributes) ||
            (Dg(e, i), (this.appliedPreAttributes = i));
        }
        applyErrorToDOM(e, t) {
          (this.cleanupErrorWrapper(),
            this.pre?.remove(),
            (this.pre = void 0),
            (this.appliedPreAttributes = void 0));
          let n = t.shadowRoot ?? t.attachShadow({ mode: `open` });
          ((this.errorWrapper ??= document.createElement(`div`)),
            (this.errorWrapper.dataset.errorWrapper = ``),
            (this.errorWrapper.textContent = ``),
            n.appendChild(this.errorWrapper));
          let r = document.createElement(`div`);
          ((r.dataset.errorMessage = ``),
            (r.innerText = e.message),
            this.errorWrapper.appendChild(r));
          let i = document.createElement(`pre`);
          ((i.dataset.errorStack = ``),
            (i.innerText = e.stack ?? `No Error Stack`),
            this.errorWrapper.appendChild(i));
        }
        cleanupErrorWrapper() {
          (this.errorWrapper?.remove(), (this.errorWrapper = void 0));
        }
      }));
  });
function Ug(e) {
  return { ...zt, ...e };
}
function Wg(e, t) {
  let n = Gg(e, t);
  return t ? n : e.diffHeaderHeight + n;
}
function Gg(e, t) {
  return e.paddingTop ?? (t ? e.spacing : 0);
}
function Kg(e) {
  return e.paddingBottom ?? e.spacing;
}
function qg(e) {
  switch (e) {
    case `simple`:
      return 4;
    case `metadata`:
    case `line-info`:
    case `line-info-basic`:
    case `custom`:
      return 32;
  }
}
var Jg = e(() => {
  F();
});
function Yg(e, t) {
  return (
    (e.overflow ?? `scroll`) !== (t.overflow ?? `scroll`) ||
    (e.collapsed ?? !1) !== (t.collapsed ?? !1) ||
    (e.disableLineNumbers ?? !1) !== (t.disableLineNumbers ?? !1) ||
    (e.disableFileHeader ?? !1) !== (t.disableFileHeader ?? !1) ||
    e.unsafeCSS !== t.unsafeCSS
  );
}
function Xg(e) {
  let t;
  for (let n of e.children)
    n instanceof HTMLElement &&
      n.dataset.lineAnnotation === Rh &&
      (t = Math.max(t ?? 0, n.getBoundingClientRect().height));
  return t;
}
var Zg,
  Qg,
  $g,
  e_ = e(() => {
    (F(),
      $t(),
      an(),
      pn(),
      Jg(),
      zh(),
      Hg(),
      (Zg = 5e3),
      (Qg = -1),
      ($g = class extends Vg {
        virtualizer;
        metrics;
        __id = `virtualized-file:${++Qg}`;
        top;
        height = 0;
        cache = {
          heights: new Map(),
          checkpoints: [],
          fileAnnotationHeight: 0,
        };
        isVisible = !1;
        isSetup = !1;
        layoutDirty = !0;
        forceRenderOverride;
        currentCollapsed;
        constructor(e, t, n = zt, r, i = !1) {
          (super(e, r, i), (this.virtualizer = t), (this.metrics = n));
        }
        setMetrics(e, t = !1) {
          (!t && Qt(this.metrics, e)) ||
            ((this.metrics = e), this.resetLayoutCache());
        }
        setLineAnnotations(e) {
          this.syncLineAnnotations(e) && this.resetLayoutCache();
        }
        syncLineAnnotations(e) {
          return e == null ||
            e === this.lineAnnotations ||
            (e.length === 0 && this.lineAnnotations.length === 0)
            ? !1
            : (super.setLineAnnotations(e), !0);
        }
        hasLineAnnotations() {
          return this.lineAnnotations.some((e) => e.lineNumber > 0);
        }
        getLineHeight(e, t = !1) {
          let n = this.cache.heights.get(e);
          if (n != null) return n;
          let r = t ? 2 : 1;
          return this.metrics.lineHeight * r;
        }
        setOptions(e) {
          if (this.isAdvancedMode())
            throw Error(
              `VirtualizedFile.setOptions cannot be used inside CodeView. Update CodeView options instead.`,
            );
          if (e == null) return;
          let { options: t } = this,
            n = !nn(t, e),
            r = Yg(t, e);
          (super.setOptions(e),
            r && this.resetLayoutCache(!0),
            n && (this.forceRenderOverride = !0),
            n && this.virtualizer.instanceChanged(this, r));
        }
        setThemeType(e) {
          if (this.isAdvancedMode())
            throw Error(
              `VirtualizedFile.setThemeType cannot be used inside CodeView. Update CodeView options instead.`,
            );
          super.setThemeType(e);
        }
        resetLayoutCache(e = !1, t = !0) {
          ((this.layoutDirty = !0),
            (this.cache.fileAnnotationHeight = 0),
            this.cache.heights.size > 0 && this.cache.heights.clear(),
            this.cache.checkpoints.length > 0 &&
              (this.cache.checkpoints.length = 0),
            this.renderRange != null && t && (this.renderRange = void 0),
            e && this.isSimpleMode() && this.computeApproximateSize());
        }
        reconcileHeights() {
          let e = !1;
          if (this.fileContainer == null || this.file == null)
            return (this.height !== 0 && (e = !0), (this.height = 0), e);
          let { overflow: t = `scroll` } = this.options;
          if (
            ((this.top = this.getVirtualizedTop()),
            (t === `scroll` &&
              this.lineAnnotations.length === 0 &&
              !this.isResizeDebuggingEnabled()) ||
              this.code == null)
          )
            return e;
          let n = this.code.children[1];
          if (!(n instanceof HTMLElement)) return e;
          let r = Fh(this.lineAnnotations);
          if (this.renderRange != null && r && Lh(this.renderRange)) {
            let t = Xg(n) ?? 0;
            t !== this.cache.fileAnnotationHeight &&
              ((this.cache.fileAnnotationHeight = t), (e = !0));
          } else
            !r &&
              this.cache.fileAnnotationHeight !== 0 &&
              ((this.cache.fileAnnotationHeight = 0), (e = !0));
          for (let t of n.children) {
            if (!(t instanceof HTMLElement)) continue;
            let n = t.dataset.lineIndex;
            if (n == null) continue;
            let r = Number(n),
              i = t.getBoundingClientRect().height,
              a = !1;
            t.nextElementSibling instanceof HTMLElement &&
              (`lineAnnotation` in t.nextElementSibling.dataset ||
                `noNewline` in t.nextElementSibling.dataset) &&
              (`noNewline` in t.nextElementSibling.dataset && (a = !0),
              (i += t.nextElementSibling.getBoundingClientRect().height));
            let o = this.getLineHeight(r, a);
            i !== o &&
              ((e = !0),
              i === this.metrics.lineHeight * (a ? 2 : 1)
                ? this.cache.heights.delete(r)
                : this.cache.heights.set(r, i));
          }
          return (
            (e || this.isResizeDebuggingEnabled()) &&
              this.computeApproximateSize(!0),
            e
          );
        }
        onRender = (e) =>
          this.fileContainer == null || this.file == null
            ? !1
            : (e && (this.top = this.getVirtualizedTop()),
              this.render({ file: this.file }));
        prepareCodeViewItem(e, t, n, r) {
          let i = this.syncLineAnnotations(r),
            a = n?.resetFileLayoutCache === !0 || i;
          n?.metrics != null && ((this.metrics = n.metrics), (a = !0));
          let { collapsed: o = !1 } = this.options;
          return (
            this.currentCollapsed !== o &&
              ((this.currentCollapsed = o), (a = !0)),
            a && this.resetLayoutCache(),
            this.file !== e && (this.layoutDirty = !0),
            (this.file = e),
            (this.top = t),
            this.computeApproximateSize(),
            this.height
          );
        }
        getLinePosition(e) {
          if (this.file == null || e < 1) return;
          let { disableFileHeader: t = !1, collapsed: n = !1 } = this.options,
            r = this.fileRenderer.getLineCount(this.file) - 1,
            i = Wg(this.metrics, t);
          if (n || r < 0) return { top: i, height: 0 };
          let a = Math.min(Math.max(e - 1, 0), r),
            { overflow: o = `scroll` } = this.options,
            { lineHeight: s } = this.metrics;
          if (
            ((i += this.cache.fileAnnotationHeight),
            o === `scroll` && !this.hasLineAnnotations())
          )
            return { top: i + a * s, height: s };
          let c = this.getLayoutCheckpointBeforeLineIndex(a);
          i = c?.top ?? i;
          for (let e = c?.lineIndex ?? 0; e < a; e++)
            i += this.getLineHeight(e, !1);
          return { top: i, height: this.getLineHeight(a, !1) };
        }
        getNumericScrollAnchor(e) {
          if (this.file == null || this.renderRange == null) return;
          let {
            disableFileHeader: t = !1,
            collapsed: n = !1,
            overflow: r = `scroll`,
          } = this.options;
          if (n || this.renderRange.totalLines <= 0) return;
          let i = this.fileRenderer.getLineCount(this.file) - 1;
          if (i < 0) return;
          let a = Wg(this.metrics, t),
            o = Math.min(this.renderRange.startingLine, i),
            s = Math.min(o + this.renderRange.totalLines - 1, i);
          if (s < o) return;
          let { fileAnnotationHeight: c } = this.cache;
          if (r === `scroll` && !this.hasLineAnnotations()) {
            let { lineHeight: t } = this.metrics,
              n = a + (o === 0 ? c : this.renderRange.bufferBefore),
              r = o + Math.max(Math.ceil((e - n) / t), 0);
            return r > s ? void 0 : { lineNumber: r + 1, top: a + c + r * t };
          }
          let l = a + (o === 0 ? c : this.renderRange.bufferBefore);
          for (let t = o; t <= s; t++) {
            if (l >= e) return { lineNumber: t + 1, top: l };
            l += this.getLineHeight(t);
          }
        }
        getVirtualizedHeight() {
          return this.height;
        }
        getAdvancedStickySpecs(e) {
          if (this.top == null || this.file == null) return;
          if (this.options.collapsed === !0)
            return { topOffset: this.top, height: this.height };
          let t =
            e == null
              ? this.renderRange
              : this.computeRenderRangeFromWindow(this.file, this.top, e);
          if (t == null) return;
          let { bufferBefore: n, bufferAfter: r, totalLines: i } = t,
            a = 0;
          if (i === 0) {
            let t = e ?? this.virtualizer.getWindowSpecs();
            this.top < t.top && (a = r);
          }
          return { topOffset: this.top + n + a, height: this.height - (n + r) };
        }
        cleanUp(e = !1) {
          (this.fileContainer != null &&
            this.isSimpleMode() &&
            this.getSimpleVirtualizer()?.disconnect(this.fileContainer),
            e || this.resetLayoutCache(),
            (this.isSetup = !1),
            super.cleanUp(e));
        }
        computeApproximateSize(e = !1) {
          let t = this.isResizeDebuggingEnabled();
          if (!e && !this.layoutDirty && !t) return;
          let n = this.height === 0;
          if (
            ((this.height = 0),
            (this.cache.checkpoints = []),
            this.file == null)
          ) {
            this.layoutDirty = !1;
            return;
          }
          let {
              disableFileHeader: r = !1,
              collapsed: i = !1,
              overflow: a = `scroll`,
            } = this.options,
            { lineHeight: o } = this.metrics,
            s = this.fileRenderer.getLineCount(this.file),
            c = Wg(this.metrics, r),
            l = Kg(this.metrics);
          if (((this.height += c), i)) {
            this.layoutDirty = !1;
            return;
          }
          if (
            ((this.height += this.cache.fileAnnotationHeight),
            a === `scroll` && !this.hasLineAnnotations())
          )
            this.height += s * o;
          else
            for (let e = 0; e < s; e++)
              (this.addLayoutCheckpoint(e, this.height),
                (this.height += this.getLineHeight(e, !1)));
          if (
            (s > 0 && (this.height += l), this.fileContainer != null && t && !n)
          ) {
            let e = this.fileContainer.getBoundingClientRect();
            e.height === this.height
              ? console.log(
                  `VirtualizedFile.computeApproximateSize: computed height IS CORRECT`,
                )
              : console.log(
                  `VirtualizedFile.computeApproximateSize: computed height doesnt match`,
                  {
                    name: this.file.name,
                    elementHeight: e.height,
                    computedHeight: this.height,
                  },
                );
          }
          this.layoutDirty = !1;
        }
        setVisibility(e) {
          this.isAdvancedMode() ||
            this.fileContainer == null ||
            (e && !this.isVisible
              ? ((this.top = this.getVirtualizedTop()), (this.isVisible = !0))
              : !e &&
                this.isVisible &&
                ((this.isVisible = !1), this.rerender()));
        }
        rerender() {
          !this.enabled ||
            this.file == null ||
            ((this.forceRenderOverride = !0),
            this.virtualizer.instanceChanged(this, !1));
        }
        applyDocumentChange(e, t, n = !1) {
          let r = this.renderRange;
          if (
            (super.applyDocumentChange(e, t),
            this.getSimpleVirtualizer()?.markDOMDirty(),
            this.resetLayoutCache(this.isSimpleMode(), !1),
            n && r !== void 0 && this.file !== void 0)
          ) {
            let e = this.virtualizer.getWindowSpecs(),
              t = this.computeRenderRangeFromWindow(
                this.file,
                this.top ?? 0,
                e,
              );
            t.bufferAfter !== r.bufferAfter && this.updateBuffers(t);
          }
        }
        render({
          fileContainer: e,
          file: t,
          forceRender: n = !1,
          lineAnnotations: r,
          ...i
        }) {
          let a = this.file == null || !fn(this.file, t),
            { forceRenderOverride: o, isSetup: s } = this;
          this.forceRenderOverride = void 0;
          let c = this.syncLineAnnotations(r);
          if (
            (c && this.resetLayoutCache(),
            (this.file = t),
            (e = this.getOrCreateFileContainerNode(e)),
            this.file == null)
          )
            return (
              console.error(
                `VirtualizedFile.render: attempting to virtually render when we dont have file`,
              ),
              !1
            );
          if (s)
            ((this.top ??= this.getVirtualizedTop()),
              a &&
                this.isSimpleMode() &&
                (this.getSimpleVirtualizer()?.markDOMDirty(),
                this.resetLayoutCache(!0)));
          else {
            this.computeApproximateSize();
            let t = this.getSimpleVirtualizer();
            if (
              ((this.top ??= this.getVirtualizedTop()), this.isAdvancedMode())
            )
              this.isVisible = !0;
            else {
              if (t == null)
                throw Error(
                  `VirtualizedFile.render: simple virtualizer is not available`,
                );
              (t.connect(e, this),
                (this.isVisible = t.isInstanceVisible(
                  this.top ?? 0,
                  this.height,
                )));
            }
            this.isSetup = !0;
          }
          if (!this.isVisible && this.isSimpleMode())
            return this.renderPlaceholder(this.height);
          let l = this.virtualizer.getWindowSpecs(),
            u = this.top ?? 0,
            d = this.computeRenderRangeFromWindow(this.file, u, l);
          return super.render({
            file: this.file,
            fileContainer: e,
            renderRange: d,
            lineAnnotations: r,
            forceRender: (o ?? n) || c,
            ...i,
          });
        }
        syncVirtualizedTop() {
          this.top = this.getVirtualizedTop();
        }
        shouldDisableVirtualizationBuffers() {
          return (
            this.isAdvancedMode() || super.shouldDisableVirtualizationBuffers()
          );
        }
        isSimpleMode() {
          return this.virtualizer.type === `simple`;
        }
        isAdvancedMode() {
          return this.virtualizer.type === `advanced`;
        }
        addLayoutCheckpoint(e, t) {
          e % Zg === 0 && this.cache.checkpoints.push({ lineIndex: e, top: t });
        }
        getLayoutCheckpointBeforeLineIndex(e) {
          if (e <= 0 || this.cache.checkpoints.length === 0) return;
          let t = 0,
            n = this.cache.checkpoints.length - 1,
            r;
          for (; t <= n;) {
            let i = (t + n) >> 1,
              a = this.cache.checkpoints[i];
            if (a == null)
              throw Error(`VirtualizedFile: invalid checkpoint index`);
            a.lineIndex <= e ? ((r = a), (t = i + 1)) : (n = i - 1);
          }
          return r;
        }
        getLayoutCheckpointBeforeTop(e, t) {
          let n = 0,
            r = this.cache.checkpoints.length - 1,
            i = -1;
          for (; n <= r;) {
            let t = (n + r) >> 1,
              a = this.cache.checkpoints[t];
            if (a == null)
              throw Error(`VirtualizedFile: invalid checkpoint index`);
            a.top <= e ? ((i = t), (n = t + 1)) : (r = t - 1);
          }
          if (t == null) return i >= 0 ? this.cache.checkpoints[i] : void 0;
          for (let e = i; e >= 0; e--) {
            let n = this.cache.checkpoints[e];
            if (n == null)
              throw Error(`VirtualizedFile: invalid checkpoint index`);
            if (n.lineIndex % t === 0) return n;
          }
        }
        getVirtualizedTop() {
          return this.virtualizer.type === `advanced`
            ? this.virtualizer.getLocalTopForInstance(this)
            : this.fileContainer == null
              ? 0
              : this.virtualizer.getOffsetInScrollContainer(this.fileContainer);
        }
        getSimpleVirtualizer() {
          return this.virtualizer.type === `simple` ? this.virtualizer : void 0;
        }
        isResizeDebuggingEnabled() {
          return this.getSimpleVirtualizer()?.config.resizeDebugging ?? !1;
        }
        computeRenderRangeFromWindow(e, t, { top: n, bottom: r }) {
          let { disableFileHeader: i = !1, overflow: a = `scroll` } =
              this.options,
            { hunkLineCount: o, lineHeight: s } = this.metrics,
            c = this.fileRenderer.getLineCount(e),
            l = this.height,
            u = Wg(this.metrics, i),
            d = c > 0 ? Kg(this.metrics) : 0,
            { fileAnnotationHeight: f } = this.cache,
            p = u + f,
            m = Math.max(0, l - u - f - d),
            h = Fh(this.lineAnnotations),
            g = t + u,
            _ = f > 0 && h && g < r && g + f > n;
          if (t < n - l || t > r)
            return {
              startingLine: 0,
              totalLines: 0,
              bufferBefore: 0,
              bufferAfter: l - u - d,
            };
          if (c <= o)
            return {
              startingLine: 0,
              totalLines: o,
              bufferBefore: 0,
              bufferAfter: 0,
            };
          let v = Math.ceil(Math.max(r - n, 0) / s),
            y = Math.ceil(v / o) * o + o * 2,
            b = y / o,
            x = (n + r) / 2;
          if (a === `scroll` && !this.hasLineAnnotations()) {
            let e = t + p,
              i = e + m;
            if (!_ && !(e < r && i > n))
              return {
                startingLine: 0,
                totalLines: 0,
                bufferBefore: 0,
                bufferAfter: l - u - d,
              };
            let a = Math.floor(_ && x < t + p ? 0 : (x - (t + p)) / s),
              h = Math.floor(a / o) - Math.floor(b / 2),
              g = Math.ceil(c / o),
              v = Math.max(0, Math.min(h, g)) * o,
              S = h < 0 ? y + h * o : y,
              C = v === 0 ? 0 : f + v * s,
              ee = Math.min(S, c - v);
            return {
              startingLine: v,
              totalLines: S,
              bufferBefore: C,
              bufferAfter: Math.max(0, (c - v - ee) * s),
            };
          }
          let S = b,
            C = [],
            ee = this.getLayoutCheckpointBeforeTop(
              Math.max(0, n - t - y * s * 2),
              o,
            ),
            w = t + (ee?.top ?? p),
            T = ee?.lineIndex ?? 0,
            te,
            E,
            D,
            ne = ee?.lineIndex ?? 0;
          for (let e = ne; e < c; e++) {
            let i = T % o === 0,
              a = Math.floor(T / o);
            if (i && ((C[a] = w - (t + p)), D != null)) {
              if (D <= 0) break;
              D--;
            }
            let s = this.getLineHeight(e, !1);
            (w > n - s && w < r && (te ??= a),
              w + s > x && (E ??= a),
              D == null && w >= r && i && (D = S),
              T++,
              (w += s));
          }
          if (te == null)
            if (_) ((te = 0), (E = 0));
            else
              return {
                startingLine: 0,
                totalLines: 0,
                bufferBefore: 0,
                bufferAfter: l - u - d,
              };
          E ??= te;
          let O = Math.round(E - b / 2),
            re = Math.max(0, Math.ceil(c / o) - b),
            ie = Math.max(0, Math.min(O, re)),
            ae = ie * o,
            oe = O < 0 ? y + O * o : y,
            k = C[ie] ?? 0,
            A = ae === 0 ? 0 : f + k,
            j = ie + oe / o,
            se = j < C.length ? m - C[j] : m - (w - t - p);
          return {
            startingLine: ae,
            totalLines: oe,
            bufferBefore: A,
            bufferAfter: Math.max(0, se),
          };
        }
      }));
  });
function t_(e, t) {
  return e === t || (e?.cacheKey != null && e.cacheKey === t?.cacheKey);
}
var n_ = e(() => {});
function r_(e) {
  return {
    ...e,
    hunks: e.hunks.map((e) => ({
      ...e,
      hunkContent: e.hunkContent.map((e) => ({ ...e })),
    })),
    deletionLines: [...e.deletionLines],
    additionLines: [...e.additionLines],
  };
}
var i_ = e(() => {});
function a_(e) {
  return e === `` ? [] : e.split(Tt);
}
var o_ = e(() => {
  F();
});
function s_(e, t, n) {
  let r = e === `clone` ? r_(t) : t;
  if (!r.isPartial) throw Error(`hydratePartialDiff: fileDiff must be partial`);
  switch (r.type) {
    case `change`:
    case `rename-changed`:
      return c_(r, u_(r, n), d_(r, n));
    case `rename-pure`: {
      let e = d_(r, n);
      f_(r, n);
      let t = a_(e.contents);
      return (
        (r.isPartial = !1),
        (r.deletionLines = t),
        (r.additionLines = t),
        m_(r, null, e),
        r
      );
    }
  }
  throw Error(
    `hydratePartialDiff: ${r.type} diffs cannot be hydrated from loaded files`,
  );
}
function c_(e, t, n) {
  let r = a_(t.contents),
    i = a_(n.contents),
    {
      hunks: a,
      splitLineCount: o,
      unifiedLineCount: s,
    } = l_(e.hunks, i.length);
  return (
    (e.hunks = a),
    (e.splitLineCount = o),
    (e.unifiedLineCount = s),
    (e.isPartial = !1),
    (e.deletionLines = r),
    (e.additionLines = i),
    m_(e, t, n),
    e
  );
}
function l_(e, t) {
  let n = 0,
    r = 0,
    i = 0,
    a = [];
  for (let t of e) {
    let e = Math.max(t.additionStart - 1, 0),
      o = Math.max(t.deletionStart - 1, 0),
      s = e,
      c = o,
      l = 0,
      u = 0,
      d = 0,
      f = 0,
      p = [];
    for (let e of t.hunkContent) {
      if (e.type === `context`) {
        (p.push({ ...e, additionLineIndex: s, deletionLineIndex: c }),
          (s += e.lines),
          (c += e.lines),
          (d += e.lines),
          (f += e.lines));
        continue;
      }
      (p.push({ ...e, additionLineIndex: s, deletionLineIndex: c }),
        (s += e.additions),
        (c += e.deletions),
        (l += e.additions),
        (u += e.deletions),
        (d += Math.max(e.additions, e.deletions)),
        (f += e.additions + e.deletions));
    }
    let m = Math.max(t.additionStart - 1 - i, 0);
    (a.push({
      ...t,
      collapsedBefore: m,
      additionLineIndex: e,
      deletionLineIndex: o,
      additionLines: l,
      deletionLines: u,
      hunkContent: p,
      splitLineStart: n + m,
      unifiedLineStart: r + m,
      splitLineCount: d,
      unifiedLineCount: f,
    }),
      (n += m + d),
      (r += m + f),
      (i = t.additionStart + t.additionCount - 1));
  }
  if (a.length > 0) {
    let e = a[a.length - 1],
      i = Math.max(e.additionStart + e.additionCount - 1, 0),
      o = Math.max(t - i, 0);
    ((n += o), (r += o));
  }
  return { hunks: a, splitLineCount: n, unifiedLineCount: r };
}
function u_(e, t) {
  if (t.oldFile == null)
    throw Error(
      `hydratePartialDiff: ${e.type} diff for ${e.name} requires oldFile`,
    );
  return t.oldFile;
}
function d_(e, t) {
  if (t.newFile == null)
    throw Error(
      `hydratePartialDiff: ${e.type} diff for ${e.name} requires newFile`,
    );
  return t.newFile;
}
function f_(e, t) {
  if (t.oldFile !== null)
    throw Error(
      `hydratePartialDiff: ${e.type} diff for ${e.name} requires oldFile to be null`,
    );
}
function p_(e, t, n) {
  return e.cacheKey == null ? h_(t, n) : `${e.cacheKey}:hydrated`;
}
function m_(e, t, n) {
  let r = p_(e, t, n);
  if (r == null) {
    delete e.cacheKey;
    return;
  }
  e.cacheKey = r;
}
function h_(e, t) {
  return e != null && t != null
    ? e.cacheKey != null && t.cacheKey != null
      ? `${e.cacheKey}:${t.cacheKey}`
      : void 0
    : (e?.cacheKey ?? t?.cacheKey);
}
var g_ = e(() => {
  (i_(), o_());
});
function __(e) {
  let t = e.length;
  return (
    e.charCodeAt(t - 1) === 10 && (t--, e.charCodeAt(t - 1) === 13 && t--),
    e.slice(0, t)
  );
}
var v_ = e(() => {});
function y_() {
  w_.length !== C_ && (w_ = new Uint8Array(C_));
}
function $(e) {
  if (e.length === 0) return e;
  if (S_.test(e)) return JSON.parse(JSON.stringify(e));
  let t = e.length * 3;
  w_.length < t && (w_ = new Uint8Array(t));
  let { written: n } = b_.encodeInto(e, w_);
  return x_.decode(w_.subarray(0, n));
}
var b_,
  x_,
  S_,
  C_,
  w_,
  T_ = e(() => {
    ((b_ = new TextEncoder()),
      (x_ = new TextDecoder(`utf-8`, { ignoreBOM: !0 })),
      (S_ = /[\uD800-\uDFFF]/),
      (C_ = 1024),
      (w_ = new Uint8Array(C_)));
  });
function E_(e, t, n) {
  try {
    return D_(e, t, n);
  } finally {
    y_();
  }
}
function D_(e, t, n = !1) {
  let r = G_(e),
    i = r ? P_(e) : F_(e),
    a,
    o = [];
  for (let e of i) {
    if (r && !wt.test(e)) {
      if (a == null) a = $(e);
      else if (n) throw Error(`parsePatchContent: unknown file blob`);
      else console.error(`parsePatchContent: unknown file blob:`, e);
      continue;
    } else if (!r && !I_(e)) {
      if (a == null) a = $(e);
      else if (n) throw Error(`parsePatchContent: unknown file blob`);
      else console.error(`parsePatchContent: unknown file blob:`, e);
      continue;
    }
    let i = k_(e, {
      cacheKey: t == null ? void 0 : `${t}-${o.length}`,
      isGitDiff: r,
      throwOnError: n,
    });
    i != null && o.push(i);
  }
  return { patchMetadata: a, files: o };
}
function O_(e, t) {
  try {
    return k_(e, t);
  } finally {
    y_();
  }
}
function k_(
  e,
  {
    cacheKey: t,
    isGitDiff: n = wt.test(e),
    oldFile: r,
    newFile: i,
    throwOnError: a = !1,
  } = {},
) {
  let o = 0,
    s = K_(e, `@@ `),
    c,
    l = r == null || i == null,
    u = 0,
    d = 0;
  for (let e of s) {
    let s = N_(e),
      f = s[0];
    if (f == null) {
      if (a) throw Error(`parsePatchContent: invalid hunk`);
      console.error(`parsePatchContent: invalid hunk`, e);
      continue;
    }
    let p = H_(f),
      m = 0,
      h = 0;
    if (p == null || c == null) {
      if (c != null) {
        if (a) throw Error(`parsePatchContent: Invalid hunk`);
        console.error(`parsePatchContent: Invalid hunk`, e);
        continue;
      }
      ((c = {
        name: ``,
        type: `change`,
        hunks: [],
        splitLineCount: 0,
        unifiedLineCount: 0,
        isPartial: l,
        additionLines: !l && r != null && i != null ? M_(i.contents) : [],
        deletionLines: !l && r != null && i != null ? M_(r.contents) : [],
        cacheKey: J_(t),
      }),
        c.additionLines.length === 1 &&
          i?.contents === `` &&
          (c.additionLines.length = 0),
        c.deletionLines.length === 1 &&
          r?.contents === `` &&
          (c.deletionLines.length = 0));
      for (let e of s) {
        if (e.startsWith(`diff --git`)) {
          let t = e.trim().match(Ot),
            n = t?.[1] ?? t?.[2],
            r = t?.[3] ?? t?.[4];
          if (n == null || r == null) {
            if (a) throw Error(`parsePatchContent: invalid git diff header`);
            console.error(`parsePatchContent: invalid git diff header`, e);
            continue;
          }
          ((c.name = $(r.trim())), n !== r && (c.prevName = $(n.trim())));
          continue;
        }
        let t =
          e.startsWith(`---`) || e.startsWith(`+++`)
            ? e.match(n ? Dt : Et)
            : null;
        if (t != null) {
          let [, e, n] = t;
          if (e === `---` && n !== `/dev/null`) {
            let e = $(n.trim());
            ((c.prevName = e), (c.name = e));
          } else e === `+++` && n !== `/dev/null` && (c.name = $(n.trim()));
        } else if (n) {
          if (
            (e.startsWith(`new mode `) && (c.mode = $(e.slice(8).trim())),
            e.startsWith(`old mode `) && (c.prevMode = $(e.slice(8).trim())),
            e.startsWith(`new file mode`) &&
              ((c.type = `new`), (c.mode = $(e.slice(13).trim()))),
            e.startsWith(`deleted file mode`) &&
              ((c.type = `deleted`), (c.mode = $(e.slice(17).trim()))),
            e.startsWith(`similarity index`) &&
              (e.startsWith(`similarity index 100%`)
                ? (c.type = `rename-pure`)
                : (c.type = `rename-changed`)),
            e.startsWith(`index `))
          ) {
            let [, t, n, r] = e.trim().match(kt) ?? [];
            (t != null && (c.prevObjectId = $(t)),
              n != null && (c.newObjectId = $(n)),
              r != null && (c.mode = $(r)));
          }
          (e.startsWith(`rename from `) && (c.prevName = $(e.slice(12).trim())),
            e.startsWith(`rename to `) && (c.name = $(e.slice(10).trim())));
        }
      }
      continue;
    }
    let g, _;
    for (
      ;
      s.length > 0 &&
      (s[s.length - 1] ===
        `
` ||
        s[s.length - 1] === `\r` ||
        s[s.length - 1] ===
          `\r
` ||
        s[s.length - 1] === ``);
    )
      s.pop();
    let { additionStart: v, deletionStart: y } = p;
    ((u = l ? u : y - 1), (d = l ? d : v - 1));
    let b = {
        collapsedBefore: 0,
        splitLineCount: 0,
        splitLineStart: 0,
        unifiedLineCount: 0,
        unifiedLineStart: 0,
        additionCount: p.additionCount,
        additionStart: v,
        additionLines: m,
        deletionCount: p.deletionCount,
        deletionStart: y,
        deletionLines: h,
        deletionLineIndex: u,
        additionLineIndex: d,
        hunkContent: [],
        hunkContext: J_(p.hunkContext),
        hunkSpecs: $(f),
        noEOFCRAdditions: !1,
        noEOFCRDeletions: !1,
      },
      x = 0,
      S = 0;
    for (let e = 1; e < s.length; e++) {
      let t = s[e];
      if (x >= b.additionCount && S >= b.deletionCount && !t.startsWith(`\\`)) {
        if (a && B_(t) && !V_(t))
          throw Error(`parsePatchContent: hunk has more lines than expected`);
        break;
      }
      let n = t[0];
      if (n !== `+` && n !== `-` && n !== ` ` && n !== `\\`) {
        if (a) throw Error(`parsePatchContent: invalid hunk line`);
        (console.error(
          `parseLineType: Invalid firstChar: "${n}", full line: "${t}"`,
        ),
          console.error(`processFile: invalid rawLine:`, t));
        continue;
      }
      let r = Y_(n);
      if (r === `addition`) {
        if (a && x >= b.additionCount)
          throw Error(`parsePatchContent: hunk has too many addition lines`);
        let e = X_(t);
        ((g == null || g.type !== `change`) &&
          ((g = Z_(`change`, u, d)), b.hunkContent.push(g)),
          d++,
          x++,
          l && c.additionLines.push(e),
          g.additions++,
          m++,
          (_ = `addition`));
      } else if (r === `deletion`) {
        if (a && S >= b.deletionCount)
          throw Error(`parsePatchContent: hunk has too many deletion lines`);
        let e = X_(t);
        ((g == null || g.type !== `change`) &&
          ((g = Z_(`change`, u, d)), b.hunkContent.push(g)),
          u++,
          S++,
          l && c.deletionLines.push(e),
          g.deletions++,
          h++,
          (_ = `deletion`));
      } else if (r === `context`) {
        if (a && (S >= b.deletionCount || x >= b.additionCount))
          throw Error(`parsePatchContent: hunk has too many context lines`);
        let e = X_(t);
        ((g == null || g.type !== `context`) &&
          ((g = Z_(`context`, u, d)), b.hunkContent.push(g)),
          d++,
          u++,
          x++,
          S++,
          l && (c.deletionLines.push(e), c.additionLines.push(e)),
          g.lines++,
          (_ = `context`));
      } else if (r === `metadata` && g != null) {
        if (
          (g.type === `context`
            ? ((b.noEOFCRAdditions = !0), (b.noEOFCRDeletions = !0))
            : _ === `deletion`
              ? (b.noEOFCRDeletions = !0)
              : _ === `addition` && (b.noEOFCRAdditions = !0),
          l && (_ === `addition` || _ === `context`))
        ) {
          let e = c.additionLines.length - 1;
          e >= 0 && (c.additionLines[e] = __(c.additionLines[e]));
        }
        if (l && (_ === `deletion` || _ === `context`)) {
          let e = c.deletionLines.length - 1;
          e >= 0 && (c.deletionLines[e] = __(c.deletionLines[e]));
        }
      }
    }
    if (a && (x !== b.additionCount || S !== b.deletionCount))
      throw Error(`parsePatchContent: hunk line count mismatch`);
    ((b.additionLines = m),
      (b.deletionLines = h),
      (b.collapsedBefore = Math.max(b.additionStart - 1 - o, 0)),
      c.hunks.push(b),
      (o = b.additionStart + b.additionCount - 1));
    for (let e of b.hunkContent)
      e.type === `context`
        ? ((b.splitLineCount += e.lines), (b.unifiedLineCount += e.lines))
        : ((b.splitLineCount += Math.max(e.additions, e.deletions)),
          (b.unifiedLineCount += e.deletions + e.additions));
    ((b.splitLineStart = c.splitLineCount + b.collapsedBefore),
      (b.unifiedLineStart = c.unifiedLineCount + b.collapsedBefore),
      (c.splitLineCount += b.collapsedBefore + b.splitLineCount),
      (c.unifiedLineCount += b.collapsedBefore + b.unifiedLineCount));
  }
  if (c != null) {
    if (a && l && !n && c.hunks.length === 0)
      throw Error(`parsePatchContent: unified file has no hunks`);
    if (
      c.hunks.length > 0 &&
      !l &&
      c.additionLines.length > 0 &&
      c.deletionLines.length > 0
    ) {
      let e = c.hunks[c.hunks.length - 1],
        t = e.additionStart + e.additionCount - 1,
        n = c.additionLines.length,
        r = Math.max(n - t, 0);
      ((c.splitLineCount += r), (c.unifiedLineCount += r));
    }
    return (
      n ||
        (c.prevName != null && c.name !== c.prevName
          ? c.hunks.length > 0
            ? (c.type = `rename-changed`)
            : (c.type = `rename-pure`)
          : (r == null || r.contents === ``) && i != null && i.contents !== ``
            ? (c.type = `new`)
            : r != null &&
              r.contents !== `` &&
              (i == null || i.contents === ``) &&
              (c.type = `deleted`)),
      c.type !== `rename-pure` &&
        c.type !== `rename-changed` &&
        (c.prevName = void 0),
      c
    );
  }
}
function A_(e, t, n = !1) {
  let r = [],
    i = j_(e) ? e.split(Ct) : [e];
  for (let e of i)
    try {
      r.push(E_(e, t == null ? void 0 : `${t}-${r.length}`, n));
    } catch (e) {
      if (n) throw e;
      console.error(e);
    }
  return r;
}
function j_(e) {
  return (
    e.startsWith(`From `) ||
    e.includes(`
From `)
  );
}
function M_(e) {
  let t = N_(e);
  for (let e = 0; e < t.length; e++) t[e] = $(t[e]);
  return t;
}
function N_(e) {
  if (e.length === 0) return [``];
  let t = [],
    n = 0;
  for (;;) {
    let r = e.indexOf(
      `
`,
      n,
    );
    if (r === -1) break;
    (t.push(e.slice(n, r + 1)), (n = r + 1));
  }
  return (n < e.length && t.push(e.slice(n)), t);
}
function P_(e) {
  return K_(e, `diff --git`);
}
function F_(e) {
  if (e.length === 0) return [``];
  let t = [],
    n = 0,
    r = 0,
    i = 0,
    a = 0,
    o = !1;
  for (; r < e.length;) {
    let s = z_(e, r);
    if (i <= 0 && a <= 0) {
      if (L_(e, r)) {
        (r > n && t.push(e.slice(n, r)), (n = r), (o = !0), (r = z_(e, s)));
        continue;
      }
      if (o && e.startsWith(`@@ -`, r)) {
        let t = H_(e.slice(r, s));
        t != null && ((i = t.deletionCount), (a = t.additionCount));
      }
      r = s;
      continue;
    }
    let c = e[r];
    if (c === `\\`) {
      r = s;
      continue;
    }
    (c === ` `
      ? ((i = Math.max(i - 1, 0)), (a = Math.max(a - 1, 0)))
      : c === `-`
        ? (i = Math.max(i - 1, 0))
        : c === `+` && (a = Math.max(a - 1, 0)),
      (r = s));
  }
  return (t.push(e.slice(n)), t);
}
function I_(e) {
  return L_(e, 0);
}
function L_(e, t) {
  let n = z_(e, t);
  return R_(e, t, `---`) && R_(e, n, `+++`);
}
function R_(e, t, n) {
  if (!e.startsWith(n, t)) return !1;
  let r = e[t + n.length];
  if (r !== ` ` && r !== `	`) return !1;
  for (let r = t + n.length + 1; r < e.length; r++) {
    let t = e[r];
    if (
      t ===
        `
` ||
      t === `\r`
    )
      break;
    if (t !== ` ` && t !== `	`) return !0;
  }
  return !1;
}
function z_(e, t) {
  let n = e.indexOf(
    `
`,
    t,
  );
  return n === -1 ? e.length : n + 1;
}
function B_(e) {
  let t = e[0];
  return t === `+` || t === `-` || t === ` `;
}
function V_(e) {
  if (!e.startsWith(`--`)) return !1;
  for (let t = 2; t < e.length; t++) {
    let n = e[t];
    if (
      n !== ` ` &&
      n !== `	` &&
      n !==
        `
` &&
      n !== `\r`
    )
      return !1;
  }
  return !0;
}
function H_(e) {
  if (!e.startsWith(`@@ -`)) return;
  let t = 4,
    n = U_(e, t);
  if (n == null) return;
  let r = n.value;
  t = n.endIndex;
  let i = 1;
  if (e[t] === `,`) {
    let n = U_(e, t + 1);
    if (n == null) return;
    ((i = n.value), (t = n.endIndex));
  }
  if (e[t] !== ` ` || e[t + 1] !== `+`) return;
  t += 2;
  let a = U_(e, t);
  if (a == null) return;
  let o = a.value;
  t = a.endIndex;
  let s = 1;
  if (e[t] === `,`) {
    let n = U_(e, t + 1);
    if (n == null) return;
    ((s = n.value), (t = n.endIndex));
  }
  if (e[t] !== ` ` || e[t + 1] !== `@` || e[t + 2] !== `@`) return;
  let c,
    l = t + 3;
  return (
    e[l] === ` ` && (c = W_(e.slice(l + 1))),
    {
      additionCount: s,
      additionStart: o,
      deletionCount: i,
      deletionStart: r,
      hunkContext: c,
    }
  );
}
function U_(e, t) {
  let n = t,
    r = 0;
  for (; n < e.length; n++) {
    let t = e.charCodeAt(n) - 48;
    if (t < 0 || t > 9) break;
    r = r * 10 + t;
  }
  if (n !== t) return { value: r, endIndex: n };
}
function W_(e) {
  return e.endsWith(`\r
`)
    ? e.slice(0, -2)
    : e.endsWith(`
`)
      ? e.slice(0, -1)
      : e;
}
function G_(e) {
  return (
    e.startsWith(`diff --git`) ||
    e.includes(`
diff --git`)
  );
}
function K_(e, t) {
  if (e.length === 0) return [``];
  let n = `\n${t}`,
    r = e.startsWith(t) ? 0 : q_(e, n, 0);
  if (r === -1) return [e];
  let i = [];
  r > 0 && i.push(e.slice(0, r));
  let a = r;
  for (;;) {
    let t = q_(e, n, a + 1);
    if (t === -1) break;
    (i.push(e.slice(a, t)), (a = t));
  }
  return (i.push(e.slice(a)), i);
}
function q_(e, t, n) {
  let r = e.indexOf(t, n);
  return r === -1 ? -1 : r + 1;
}
function J_(e) {
  return e == null ? e : $(e);
}
function Y_(e) {
  return e === ` `
    ? `context`
    : e === `\\`
      ? `metadata`
      : e === `+`
        ? `addition`
        : `deletion`;
}
function X_(e) {
  let t = e.slice(1);
  return $(
    t === ``
      ? `
`
      : t,
  );
}
function Z_(e, t, n) {
  return e === `change`
    ? {
        type: `change`,
        additions: 0,
        deletions: 0,
        additionLineIndex: n,
        deletionLineIndex: t,
      }
    : { type: `context`, lines: 0, additionLineIndex: n, deletionLineIndex: t };
}
var Q_ = e(() => {
    (F(), v_(), T_());
  }),
  $_,
  ev = e(() => {
    $_ = class {
      diff(e, t, n = {}) {
        let r;
        typeof n == `function`
          ? ((r = n), (n = {}))
          : `callback` in n && (r = n.callback);
        let i = this.castInput(e, n),
          a = this.castInput(t, n),
          o = this.removeEmpty(this.tokenize(i, n)),
          s = this.removeEmpty(this.tokenize(a, n));
        return this.diffWithOptionsObj(o, s, n, r);
      }
      diffWithOptionsObj(e, t, n, r) {
        let i = (e) => {
            if (((e = this.postProcess(e, n)), r)) {
              setTimeout(function () {
                r(e);
              }, 0);
              return;
            } else return e;
          },
          a = t.length,
          o = e.length,
          s = 1,
          c = a + o;
        n.maxEditLength != null && (c = Math.min(c, n.maxEditLength));
        let l = n.timeout ?? 1 / 0,
          u = Date.now() + l,
          d = [{ oldPos: -1, lastComponent: void 0 }],
          f = this.extractCommon(d[0], t, e, 0, n);
        if (d[0].oldPos + 1 >= o && f + 1 >= a)
          return i(this.buildValues(d[0].lastComponent, t, e));
        let p = -1 / 0,
          m = 1 / 0,
          h = () => {
            for (let r = Math.max(p, -s); r <= Math.min(m, s); r += 2) {
              let s,
                c = d[r - 1],
                l = d[r + 1];
              c && (d[r - 1] = void 0);
              let u = !1;
              if (l) {
                let e = l.oldPos - r;
                u = l && 0 <= e && e < a;
              }
              let h = c && c.oldPos + 1 < o;
              if (!u && !h) {
                d[r] = void 0;
                continue;
              }
              if (
                ((s =
                  !h || (u && c.oldPos < l.oldPos)
                    ? this.addToPath(l, !0, !1, 0, n)
                    : this.addToPath(c, !1, !0, 1, n)),
                (f = this.extractCommon(s, t, e, r, n)),
                s.oldPos + 1 >= o && f + 1 >= a)
              )
                return i(this.buildValues(s.lastComponent, t, e)) || !0;
              ((d[r] = s),
                s.oldPos + 1 >= o && (m = Math.min(m, r - 1)),
                f + 1 >= a && (p = Math.max(p, r + 1)));
            }
            s++;
          };
        if (r)
          (function e() {
            setTimeout(function () {
              if (s > c || Date.now() > u) return r(void 0);
              h() || e();
            }, 0);
          })();
        else
          for (; s <= c && Date.now() <= u;) {
            let e = h();
            if (e) return e;
          }
      }
      addToPath(e, t, n, r, i) {
        let a = e.lastComponent;
        return a && !i.oneChangePerToken && a.added === t && a.removed === n
          ? {
              oldPos: e.oldPos + r,
              lastComponent: {
                count: a.count + 1,
                added: t,
                removed: n,
                previousComponent: a.previousComponent,
              },
            }
          : {
              oldPos: e.oldPos + r,
              lastComponent: {
                count: 1,
                added: t,
                removed: n,
                previousComponent: a,
              },
            };
      }
      extractCommon(e, t, n, r, i) {
        let a = t.length,
          o = n.length,
          s = e.oldPos,
          c = s - r,
          l = 0;
        for (; c + 1 < a && s + 1 < o && this.equals(n[s + 1], t[c + 1], i);)
          (c++,
            s++,
            l++,
            i.oneChangePerToken &&
              (e.lastComponent = {
                count: 1,
                previousComponent: e.lastComponent,
                added: !1,
                removed: !1,
              }));
        return (
          l &&
            !i.oneChangePerToken &&
            (e.lastComponent = {
              count: l,
              previousComponent: e.lastComponent,
              added: !1,
              removed: !1,
            }),
          (e.oldPos = s),
          c
        );
      }
      equals(e, t, n) {
        return n.comparator
          ? n.comparator(e, t)
          : e === t || (!!n.ignoreCase && e.toLowerCase() === t.toLowerCase());
      }
      removeEmpty(e) {
        let t = [];
        for (let n = 0; n < e.length; n++) e[n] && t.push(e[n]);
        return t;
      }
      castInput(e, t) {
        return e;
      }
      tokenize(e, t) {
        return Array.from(e);
      }
      join(e) {
        return e.join(``);
      }
      postProcess(e, t) {
        return e;
      }
      get useLongestToken() {
        return !1;
      }
      buildValues(e, t, n) {
        let r = [],
          i;
        for (; e;)
          (r.push(e),
            (i = e.previousComponent),
            delete e.previousComponent,
            (e = i));
        r.reverse();
        let a = r.length,
          o = 0,
          s = 0,
          c = 0;
        for (; o < a; o++) {
          let e = r[o];
          if (e.removed)
            ((e.value = this.join(n.slice(c, c + e.count))), (c += e.count));
          else {
            if (!e.added && this.useLongestToken) {
              let r = t.slice(s, s + e.count);
              ((r = r.map(function (e, t) {
                let r = n[c + t];
                return r.length > e.length ? r : e;
              })),
                (e.value = this.join(r)));
            } else e.value = this.join(t.slice(s, s + e.count));
            ((s += e.count), e.added || (c += e.count));
          }
        }
        return r;
      }
    };
  });
function tv(e, t, n) {
  return rv.diff(e, t, n);
}
var nv,
  rv,
  iv = e(() => {
    (ev(), (nv = class extends $_ {}), (rv = new nv()));
  });
function av(e, t) {
  let n;
  for (n = 0; n < e.length && n < t.length; n++)
    if (e[n] != t[n]) return e.slice(0, n);
  return e.slice(0, n);
}
function ov(e, t) {
  let n;
  if (!e || !t || e[e.length - 1] != t[t.length - 1]) return ``;
  for (n = 0; n < e.length && n < t.length; n++)
    if (e[e.length - (n + 1)] != t[t.length - (n + 1)]) return e.slice(-n);
  return e.slice(-n);
}
function sv(e, t, n) {
  if (e.slice(0, t.length) != t)
    throw Error(
      `string ${JSON.stringify(e)} doesn't start with prefix ${JSON.stringify(t)}; this is a bug`,
    );
  return n + e.slice(t.length);
}
function cv(e, t, n) {
  if (!t) return e + n;
  if (e.slice(-t.length) != t)
    throw Error(
      `string ${JSON.stringify(e)} doesn't end with suffix ${JSON.stringify(t)}; this is a bug`,
    );
  return e.slice(0, -t.length) + n;
}
function lv(e, t) {
  return sv(e, t, ``);
}
function uv(e, t) {
  return cv(e, t, ``);
}
function dv(e, t) {
  return t.slice(0, fv(e, t));
}
function fv(e, t) {
  let n = 0;
  e.length > t.length && (n = e.length - t.length);
  let r = t.length;
  e.length < t.length && (r = e.length);
  let i = Array(r),
    a = 0;
  i[0] = 0;
  for (let e = 1; e < r; e++) {
    for (t[e] == t[a] ? (i[e] = i[a]) : (i[e] = a); a > 0 && t[e] != t[a];)
      a = i[a];
    t[e] == t[a] && a++;
  }
  a = 0;
  for (let r = n; r < e.length; r++) {
    for (; a > 0 && e[r] != t[a];) a = i[a];
    e[r] == t[a] && a++;
  }
  return a;
}
function pv(e, t) {
  let n = [];
  for (let r of Array.from(t.segment(e))) {
    let e = r.segment;
    n.length && /\s/.test(n[n.length - 1]) && /\s/.test(e)
      ? (n[n.length - 1] += e)
      : n.push(e);
  }
  return n;
}
function mv(e, t) {
  if (t) return gv(e, t)[1];
  let n;
  for (n = e.length - 1; n >= 0 && e[n].match(/\s/); n--);
  return e.substring(n + 1);
}
function hv(e, t) {
  if (t) return gv(e, t)[0];
  let n = e.match(/^\s*/);
  return n ? n[0] : ``;
}
function gv(e, t) {
  if (!t) return [hv(e), mv(e)];
  if (t.resolvedOptions().granularity != `word`)
    throw Error(`The segmenter passed must have a granularity of "word"`);
  let n = pv(e, t),
    r = n[0],
    i = n[n.length - 1];
  return [/\s/.test(r) ? r : ``, /\s/.test(i) ? i : ``];
}
var _v = e(() => {});
function vv(e, t, n, r, i) {
  if (t && n) {
    let [a, o] = gv(t.value, i),
      [s, c] = gv(n.value, i);
    if (e) {
      let r = av(a, s);
      ((e.value = cv(e.value, s, r)),
        (t.value = lv(t.value, r)),
        (n.value = lv(n.value, r)));
    }
    if (r) {
      let e = ov(o, c);
      ((r.value = sv(r.value, c, e)),
        (t.value = uv(t.value, e)),
        (n.value = uv(n.value, e)));
    }
  } else if (n) {
    if (e) {
      let e = hv(n.value, i);
      n.value = n.value.substring(e.length);
    }
    if (r) {
      let e = hv(r.value, i);
      r.value = r.value.substring(e.length);
    }
  } else if (e && r) {
    let n = hv(r.value, i),
      [a, o] = gv(t.value, i),
      s = av(n, a);
    t.value = lv(t.value, s);
    let c = ov(lv(n, s), o);
    ((t.value = uv(t.value, c)),
      (r.value = sv(r.value, n, c)),
      (e.value = cv(e.value, n, n.slice(0, n.length - c.length))));
  } else if (r) {
    let e = hv(r.value, i),
      n = dv(mv(t.value, i), e);
    t.value = uv(t.value, n);
  } else if (e) {
    let n = dv(mv(e.value, i), hv(t.value, i));
    t.value = lv(t.value, n);
  }
}
function yv(e, t, n) {
  return wv.diff(e, t, n);
}
var bv,
  xv,
  Sv,
  Cv,
  wv,
  Tv = e(() => {
    (ev(),
      _v(),
      (bv = `a-zA-Z0-9_\\u{AD}\\u{C0}-\\u{D6}\\u{D8}-\\u{F6}\\u{F8}-\\u{2C6}\\u{2C8}-\\u{2D7}\\u{2DE}-\\u{2FF}\\u{1E00}-\\u{1EFF}`),
      (xv = RegExp(`[${bv}]+|\\s+|[^${bv}]`, `ug`)),
      (Sv = class extends $_ {
        equals(e, t, n) {
          return (
            n.ignoreCase && ((e = e.toLowerCase()), (t = t.toLowerCase())),
            e.trim() === t.trim()
          );
        }
        tokenize(e, t = {}) {
          let n;
          if (t.intlSegmenter) {
            let r = t.intlSegmenter;
            if (r.resolvedOptions().granularity != `word`)
              throw Error(
                `The segmenter passed must have a granularity of "word"`,
              );
            n = pv(e, r);
          } else n = e.match(xv) || [];
          let r = [],
            i = null;
          return (
            n.forEach((e) => {
              (/\s/.test(e)
                ? i == null
                  ? r.push(e)
                  : r.push(r.pop() + e)
                : i != null && /\s/.test(i)
                  ? r[r.length - 1] == i
                    ? r.push(r.pop() + e)
                    : r.push(i + e)
                  : r.push(e),
                (i = e));
            }),
            r
          );
        }
        join(e) {
          return e.map((e, t) => (t == 0 ? e : e.replace(/^\s+/, ``))).join(``);
        }
        postProcess(e, t) {
          if (!e || t.oneChangePerToken) return e;
          let n = null,
            r = null,
            i = null;
          return (
            e.forEach((e) => {
              e.added
                ? (r = e)
                : e.removed
                  ? (i = e)
                  : ((r || i) && vv(n, i, r, e, t.intlSegmenter),
                    (n = e),
                    (r = null),
                    (i = null));
            }),
            (r || i) && vv(n, i, r, null, t.intlSegmenter),
            e
          );
        }
      }),
      new Sv(),
      (Cv = class extends $_ {
        tokenize(e) {
          let t = RegExp(`(\\r?\\n)|[${bv}]+|[^\\S\\n\\r]+|[^${bv}]`, `ug`);
          return e.match(t) || [];
        }
      }),
      (wv = new Cv()));
  });
function Ev(e, t, n) {
  return kv.diff(e, t, n);
}
function Dv(e, t) {
  t.stripTrailingCr &&
    (e = e.replace(
      /\r\n/g,
      `
`,
    ));
  let n = [],
    r = e.split(/(\n|\r\n)/);
  r[r.length - 1] || r.pop();
  for (let e = 0; e < r.length; e++) {
    let i = r[e];
    e % 2 && !t.newlineIsToken ? (n[n.length - 1] += i) : n.push(i);
  }
  return n;
}
var Ov,
  kv,
  Av = e(() => {
    (ev(),
      (Ov = class extends $_ {
        constructor() {
          (super(...arguments), (this.tokenize = Dv));
        }
        equals(e, t, n) {
          return (
            n.ignoreWhitespace
              ? ((!n.newlineIsToken ||
                  !e.includes(`
`)) &&
                  (e = e.trim()),
                (!n.newlineIsToken ||
                  !t.includes(`
`)) &&
                  (t = t.trim()))
              : n.ignoreNewlineAtEof &&
                !n.newlineIsToken &&
                (e.endsWith(`
`) && (e = e.slice(0, -1)),
                t.endsWith(`
`) && (t = t.slice(0, -1))),
            super.equals(e, t, n)
          );
        }
      }),
      (kv = new Ov()));
  });
function jv(e) {
  return e == `.` || e == `!` || e == `?`;
}
var Mv,
  Nv = e(() => {
    (ev(),
      (Mv = class extends $_ {
        tokenize(e) {
          let t = [],
            n = 0;
          for (let r = 0; r < e.length; r++) {
            if (r == e.length - 1) {
              t.push(e.slice(n));
              break;
            }
            if (jv(e[r]) && e[r + 1].match(/\s/)) {
              for (
                t.push(e.slice(n, r + 1)), r = n = r + 1;
                e[r + 1]?.match(/\s/);
              )
                r++;
              (t.push(e.slice(n, r + 1)), (n = r + 1));
            }
          }
          return t;
        }
      }),
      new Mv());
  }),
  Pv,
  Fv = e(() => {
    (ev(),
      (Pv = class extends $_ {
        tokenize(e) {
          return e.split(/([{}:;,]|\s+)/);
        }
      }),
      new Pv());
  });
function Iv(e, t, n, r, i) {
  ((t ||= []), (n ||= []), r && (e = r(i === void 0 ? `` : i, e)));
  let a;
  for (a = 0; a < t.length; a += 1) if (t[a] === e) return n[a];
  let o;
  if (Object.prototype.toString.call(e) === `[object Array]`) {
    for (t.push(e), o = Array(e.length), n.push(o), a = 0; a < e.length; a += 1)
      o[a] = Iv(e[a], t, n, r, String(a));
    return (t.pop(), n.pop(), o);
  }
  if ((e && e.toJSON && (e = e.toJSON()), typeof e == `object` && e)) {
    (t.push(e), (o = {}), n.push(o));
    let i = [],
      s;
    for (s in e) Object.prototype.hasOwnProperty.call(e, s) && i.push(s);
    for (i.sort(), a = 0; a < i.length; a += 1)
      ((s = i[a]), (o[s] = Iv(e[s], t, n, r, s)));
    (t.pop(), n.pop());
  } else o = e;
  return o;
}
var Lv,
  Rv = e(() => {
    (ev(),
      Av(),
      (Lv = class extends $_ {
        constructor() {
          (super(...arguments), (this.tokenize = Dv));
        }
        get useLongestToken() {
          return !0;
        }
        castInput(e, t) {
          let {
            undefinedReplacement: n,
            stringifyReplacer: r = (e, t) => (t === void 0 ? n : t),
          } = t;
          return typeof e == `string`
            ? e
            : JSON.stringify(Iv(e, null, null, r), null, `  `);
        }
        equals(e, t, n) {
          return super.equals(
            e.replace(/,([\r\n])/g, `$1`),
            t.replace(/,([\r\n])/g, `$1`),
            n,
          );
        }
      }),
      new Lv());
  }),
  zv,
  Bv = e(() => {
    (ev(),
      (zv = class extends $_ {
        tokenize(e) {
          return e.slice();
        }
        join(e) {
          return e;
        }
        removeEmpty(e) {
          return e;
        }
      }),
      new zv());
  });
function Vv(e) {
  for (let t = 0; t < e.length; t++)
    if (e[t] < ` ` || e[t] > `~` || e[t] === `"` || e[t] === `\\`) return !0;
  return !1;
}
function Hv(e) {
  if (!Vv(e)) return e;
  let t = `"`,
    n = new TextEncoder().encode(e),
    r = 0;
  for (; r < n.length;) {
    let e = n[r];
    (e === 7
      ? (t += `\\a`)
      : e === 8
        ? (t += `\\b`)
        : e === 9
          ? (t += `\\t`)
          : e === 10
            ? (t += `\\n`)
            : e === 11
              ? (t += `\\v`)
              : e === 12
                ? (t += `\\f`)
                : e === 13
                  ? (t += `\\r`)
                  : e === 34
                    ? (t += `\\"`)
                    : e === 92
                      ? (t += `\\\\`)
                      : e >= 32 && e <= 126
                        ? (t += String.fromCharCode(e))
                        : (t += `\\` + e.toString(8).padStart(3, `0`)),
      r++);
  }
  return ((t += `"`), t);
}
function Uv(e, t, n, r, i, a, o) {
  let s;
  ((s = o ? (typeof o == `function` ? { callback: o } : o) : {}),
    s.context === void 0 && (s.context = 4));
  let c = s.context;
  if (s.newlineIsToken)
    throw Error(
      `newlineIsToken may not be used with patch-generation functions, only with diffing functions`,
    );
  if (s.callback) {
    let { callback: e } = s;
    Ev(
      n,
      r,
      Object.assign(Object.assign({}, s), {
        callback: (t) => {
          let n = l(t);
          e(n);
        },
      }),
    );
  } else return l(Ev(n, r, s));
  function l(n) {
    if (!n) return;
    n.push({ value: ``, lines: [] });
    function r(e) {
      return e.map(function (e) {
        return ` ` + e;
      });
    }
    let o = [],
      s = 0,
      l = 0,
      u = [],
      d = 1,
      f = 1;
    for (let e = 0; e < n.length; e++) {
      let t = n[e],
        i = t.lines || Kv(t.value);
      if (((t.lines = i), t.added || t.removed)) {
        if (!s) {
          let t = n[e - 1];
          ((s = d),
            (l = f),
            t &&
              ((u = c > 0 ? r(t.lines.slice(-c)) : []),
              (s -= u.length),
              (l -= u.length)));
        }
        for (let e of i) u.push((t.added ? `+` : `-`) + e);
        t.added ? (f += i.length) : (d += i.length);
      } else {
        if (s)
          if (i.length <= c * 2 && e < n.length - 2)
            for (let e of r(i)) u.push(e);
          else {
            let e = Math.min(i.length, c);
            for (let t of r(i.slice(0, e))) u.push(t);
            let t = {
              oldStart: s,
              oldLines: d - s + e,
              newStart: l,
              newLines: f - l + e,
              lines: u,
            };
            (o.push(t), (s = 0), (l = 0), (u = []));
          }
        ((d += i.length), (f += i.length));
      }
    }
    for (let e of o)
      for (let t = 0; t < e.lines.length; t++)
        e.lines[t].endsWith(`
`)
          ? (e.lines[t] = e.lines[t].slice(0, -1))
          : (e.lines.splice(t + 1, 0, `\\ No newline at end of file`), t++);
    return {
      oldFileName: e,
      newFileName: t,
      oldHeader: i,
      newHeader: a,
      hunks: o,
    };
  }
}
function Wv(e, t) {
  if (((t ||= qv), Array.isArray(e))) {
    if (e.length > 1 && !t.includeFileHeaders && !e.every((e) => e.isGit))
      throw Error(
        `Cannot omit file headers on a multi-file patch. (The result would be unparseable; how would a tool trying to apply the patch know which changes are to which file?)`,
      );
    return e.map((e) => Wv(e, t)).join(`
`);
  }
  let n = [];
  if (e.isGit) {
    if (((t = qv), !e.oldFileName))
      throw Error(`oldFileName must be specified for Git patches`);
    if (!e.newFileName)
      throw Error(`newFileName must be specified for Git patches`);
    let r = e.oldFileName,
      i = e.newFileName;
    (e.isCreate && r === `/dev/null`
      ? (r = i.replace(/^b\//, `a/`))
      : e.isDelete && i === `/dev/null` && (i = r.replace(/^a\//, `b/`)),
      n.push(`diff --git ` + Hv(r) + ` ` + Hv(i)),
      e.isDelete && n.push(`deleted file mode ` + (e.oldMode ?? `100644`)),
      e.isCreate && n.push(`new file mode ` + (e.newMode ?? `100644`)),
      e.oldMode &&
        e.newMode &&
        !e.isDelete &&
        !e.isCreate &&
        (n.push(`old mode ` + e.oldMode), n.push(`new mode ` + e.newMode)),
      e.isRename &&
        (n.push(`rename from ` + Hv((e.oldFileName ?? ``).replace(/^a\//, ``))),
        n.push(`rename to ` + Hv((e.newFileName ?? ``).replace(/^b\//, ``)))),
      e.isCopy &&
        (n.push(`copy from ` + Hv((e.oldFileName ?? ``).replace(/^a\//, ``))),
        n.push(`copy to ` + Hv((e.newFileName ?? ``).replace(/^b\//, ``)))));
  } else
    (t.includeIndex &&
      e.oldFileName == e.newFileName &&
      e.oldFileName !== void 0 &&
      n.push(`Index: ` + e.oldFileName),
      t.includeUnderline &&
        n.push(
          `===================================================================`,
        ));
  let r = e.hunks.length > 0;
  t.includeFileHeaders &&
    e.oldFileName !== void 0 &&
    e.newFileName !== void 0 &&
    (!e.isGit || r) &&
    (n.push(`--- ` + Hv(e.oldFileName) + (e.oldHeader ? `	` + e.oldHeader : ``)),
    n.push(`+++ ` + Hv(e.newFileName) + (e.newHeader ? `	` + e.newHeader : ``)));
  for (let t = 0; t < e.hunks.length; t++) {
    let r = e.hunks[t],
      i = r.oldLines === 0 ? r.oldStart - 1 : r.oldStart,
      a = r.newLines === 0 ? r.newStart - 1 : r.newStart;
    n.push(`@@ -` + i + `,` + r.oldLines + ` +` + a + `,` + r.newLines + ` @@`);
    for (let e of r.lines) n.push(e);
  }
  return (
    n.join(`
`) +
    `
`
  );
}
function Gv(e, t, n, r, i, a, o) {
  if ((typeof o == `function` && (o = { callback: o }), o?.callback)) {
    let { callback: s } = o;
    Uv(
      e,
      t,
      n,
      r,
      i,
      a,
      Object.assign(Object.assign({}, o), {
        callback: (e) => {
          s(e ? Wv(e, o.headerOptions) : void 0);
        },
      }),
    );
  } else {
    let s = Uv(e, t, n, r, i, a, o);
    return s ? Wv(s, o?.headerOptions) : void 0;
  }
}
function Kv(e) {
  let t = e.endsWith(`
`),
    n = e
      .split(
        `
`,
      )
      .map(
        (e) =>
          e +
          `
`,
      );
  return (t ? n.pop() : n.push(n.pop().slice(0, -1)), n);
}
var qv,
  Jv = e(() => {
    (Av(),
      (qv = {
        includeIndex: !0,
        includeUnderline: !0,
        includeFileHeaders: !0,
      }));
  }),
  Yv = e(() => {
    (iv(), Tv(), Av(), Nv(), Fv(), Rv(), Bv(), Jv());
  });
function Xv(e, t, n, r = !1) {
  if (e === null && t === null)
    throw Error(`parseDiffFromFile: You must pass oldFile, newFile, or both`);
  let i = e ?? Zv(),
    a = t ?? Zv(),
    o = O_(Gv(i.name, a.name, i.contents, a.contents, i.header, a.header, n), {
      cacheKey: (() => {
        let n = e?.cacheKey ?? e?.name,
          r = t?.cacheKey ?? t?.name;
        return n != null && r != null ? n + `:` + r : (n ?? r);
      })(),
      oldFile: i,
      newFile: a,
      throwOnError: r,
    });
  if (o == null)
    throw Error(
      `parseDiffFrom: FileInvalid diff -- probably need to fix something -- if the files are the same maybe?`,
    );
  e === null
    ? ((o.type = `new`), (o.prevName = void 0))
    : t === null && ((o.type = `deleted`), (o.prevName = void 0));
  let s = t?.lang ?? (t === null ? e?.lang : void 0);
  return (s != null && (o.lang = s), o);
}
function Zv() {
  return { name: Qv, contents: `` };
}
var Qv,
  $v = e(() => {
    (Q_(), Yv(), (Qv = `/dev/null`));
  }),
  ey,
  ty = e(() => {
    ey = class {
      isDeletionsScrolling = !1;
      isAdditionsScrolling = !1;
      timeoutId = -1;
      codeDeletions;
      codeAdditions;
      enabled = !1;
      cleanUp() {
        this.enabled &&=
          (this.codeDeletions?.removeEventListener(
            `scroll`,
            this.handleDeletionsScroll,
          ),
          this.codeAdditions?.removeEventListener(
            `scroll`,
            this.handleAdditionsScroll,
          ),
          clearTimeout(this.timeoutId),
          (this.codeDeletions = void 0),
          (this.codeAdditions = void 0),
          !1);
      }
      setup(e, t, n) {
        if (t == null || n == null)
          for (let r of e.children ?? [])
            r instanceof HTMLElement &&
              (`deletions` in r.dataset
                ? (t = r)
                : `additions` in r.dataset && (n = r));
        if (n == null || t == null) {
          this.cleanUp();
          return;
        }
        (this.codeDeletions !== t &&
          (this.codeDeletions?.removeEventListener(
            `scroll`,
            this.handleDeletionsScroll,
          ),
          (this.codeDeletions = t),
          t.addEventListener(`scroll`, this.handleDeletionsScroll, {
            passive: !0,
          })),
          this.codeAdditions !== n &&
            (this.codeAdditions?.removeEventListener(
              `scroll`,
              this.handleAdditionsScroll,
            ),
            (this.codeAdditions = n),
            n.addEventListener(`scroll`, this.handleAdditionsScroll, {
              passive: !0,
            })),
          (this.enabled = !0));
      }
      handleDeletionsScroll = () => {
        this.isAdditionsScrolling ||
          ((this.isDeletionsScrolling = !0),
          clearTimeout(this.timeoutId),
          (this.timeoutId = setTimeout(() => {
            this.isDeletionsScrolling = !1;
          }, 300)),
          this.codeAdditions?.scrollTo({
            left: this.codeDeletions?.scrollLeft,
          }));
      };
      handleAdditionsScroll = () => {
        this.isDeletionsScrolling ||
          ((this.isAdditionsScrolling = !0),
          clearTimeout(this.timeoutId),
          (this.timeoutId = setTimeout(() => {
            this.isAdditionsScrolling = !1;
          }, 300)),
          this.codeDeletions?.scrollTo({
            left: this.codeAdditions?.scrollLeft,
          }));
      };
    };
  });
function ny(e, t) {
  return (
    en(e.theme, t.theme) &&
    e.useTokenTransformer === t.useTokenTransformer &&
    e.tokenizeMaxLineLength === t.tokenizeMaxLineLength &&
    e.lineDiffType === t.lineDiffType &&
    e.maxLineDiffLength === t.maxLineDiffLength
  );
}
var ry = e(() => {
  tn();
});
function iy(e) {
  return I({
    tagName: `div`,
    properties: {
      "data-content-buffer": ``,
      "data-buffer-size": e,
      style: `grid-row: span ${e};min-height:calc(${e} * 1lh)`,
    },
  });
}
var ay = e(() => {
  L();
});
function oy(e) {
  return I({
    tagName: `div`,
    children: [
      I({ tagName: `span`, children: [mn(`No newline at end of file`)] }),
    ],
    properties: {
      "data-no-newline": ``,
      "data-line-type": e,
      "data-column-content": ``,
    },
  });
}
var sy = e(() => {
  L();
});
function cy(e) {
  return I({
    tagName: `div`,
    children: [
      hn({
        name: e === `both` ? `diffs-icon-expand-all` : `diffs-icon-expand`,
        properties: { "data-icon": `` },
      }),
    ],
    properties: {
      role: `button`,
      "data-expand-button": ``,
      "data-expand-both": e === `both` ? `` : void 0,
      "data-expand-up": e === `up` ? `` : void 0,
      "data-expand-down": e === `down` ? `` : void 0,
    },
  });
}
function ly({
  type: e,
  content: t,
  expandIndex: n,
  chunked: r = !1,
  slotName: i,
  isFirstHunk: a,
  isLastHunk: o,
}) {
  let s = 0,
    c = [];
  if (
    (e === `metadata` &&
      t != null &&
      c.push(
        I({
          tagName: `div`,
          children: [mn(t)],
          properties: { "data-separator-wrapper": `` },
        }),
      ),
    (e === `line-info` || e === `line-info-basic`) && t != null)
  ) {
    let e = [];
    (n != null &&
      (r
        ? (a || (e.push(cy(`up`)), s++), o || (e.push(cy(`down`)), s++))
        : (e.push(cy(!a && !o ? `both` : a ? `down` : `up`)), s++)),
      e.push(
        I({
          tagName: `div`,
          children: [
            I({
              tagName: `span`,
              children: [mn(t)],
              properties: { "data-unmodified-lines": `` },
            }),
          ],
          properties: { "data-separator-content": `` },
        }),
      ),
      r &&
        n != null &&
        e.push(
          I({
            tagName: `div`,
            children: [mn(`Expand all`)],
            properties: {
              role: `button`,
              "data-expand-button": ``,
              "data-expand-all-button": ``,
            },
          }),
        ),
      c.push(
        I({
          tagName: `div`,
          children: e,
          properties: {
            "data-separator-wrapper": ``,
            "data-separator-multi-button": s > 1 ? `` : void 0,
          },
        }),
      ));
  }
  return (
    e === `custom` &&
      i != null &&
      c.push(I({ tagName: `slot`, properties: { name: i } })),
    I({
      tagName: `div`,
      children: c,
      properties: {
        "data-separator": c.length === 0 ? `simple` : e,
        "data-expand-index": n,
        "data-separator-first": a ? `` : void 0,
        "data-separator-last": o ? `` : void 0,
      },
    })
  );
}
var uy = e(() => {
  L();
});
function dy(e, t) {
  return `hunk-separator-${e}-${t}`;
}
var fy = e(() => {});
function py(e) {
  let t = e.at(-1);
  return t == null
    ? 0
    : Math.max(
        t.additionStart + t.additionCount,
        t.deletionStart + t.deletionCount,
      );
}
var my = e(() => {});
function hy(e) {
  return (
    e.startingLine === 0 &&
    e.totalLines === 1 / 0 &&
    e.bufferBefore === 0 &&
    e.bufferAfter === 0
  );
}
var gy = e(() => {});
function _y({ line: e, spanStart: t, spanLength: n }) {
  return {
    start: { line: e, character: t },
    end: { line: e, character: t + n },
    properties: { "data-diff-span": `` },
    alwaysWrap: !0,
  };
}
function vy({
  item: e,
  arr: t,
  enableJoin: n,
  isNeutral: r = !1,
  isLastItem: i = !1,
}) {
  let a = t[t.length - 1];
  if (a == null || i || !n) {
    t.push([+!r, e.value]);
    return;
  }
  let o = a[0] === 0;
  if (r === o || (r && e.value.length === 1 && !o)) {
    a[1] += e.value;
    return;
  }
  t.push([+!r, e.value]);
}
var yy = e(() => {
  Yv();
});
function by({
  isPartial: e,
  rangeSize: t,
  expandedHunks: n,
  hunkIndex: r,
  collapsedContextThreshold: i,
}) {
  let a = Math.max(t, 0);
  if (a === 0 || e)
    return {
      fromStart: 0,
      fromEnd: 0,
      rangeSize: a,
      collapsedLines: a,
      renderAll: !1,
    };
  if (n === !0 || a <= i)
    return {
      fromStart: a,
      fromEnd: 0,
      rangeSize: a,
      collapsedLines: 0,
      renderAll: !0,
    };
  let o = n?.get(r),
    s = Math.min(Math.max(o?.fromStart ?? 0, 0), a),
    c = Math.min(Math.max(o?.fromEnd ?? 0, 0), a),
    l = s + c,
    u = l >= a;
  return {
    fromStart: u ? a : s,
    fromEnd: u ? 0 : c,
    rangeSize: a,
    collapsedLines: Math.max(a - l, 0),
    renderAll: u,
  };
}
function xy(e) {
  let t = e.hunks[e.hunks.length - 1];
  if (
    t == null ||
    e.isPartial ||
    e.additionLines.length === 0 ||
    e.deletionLines.length === 0
  )
    return !1;
  let n = e.additionLines.length - (t.additionLineIndex + t.additionCount),
    r = e.deletionLines.length - (t.deletionLineIndex + t.deletionCount);
  return n <= 0 && r <= 0 ? !1 : n !== r;
}
function Sy({ fileDiff: e, errorPrefix: t }) {
  let n = e.hunks[e.hunks.length - 1];
  if (
    n == null ||
    e.isPartial ||
    e.additionLines.length === 0 ||
    e.deletionLines.length === 0
  )
    return 0;
  let r = e.additionLines.length - (n.additionLineIndex + n.additionCount),
    i = e.deletionLines.length - (n.deletionLineIndex + n.deletionCount);
  if (r <= 0 && i <= 0) return 0;
  if (r !== i)
    throw Error(
      `${t}: trailing context mismatch (additions=${r}, deletions=${i}) for ${e.name}`,
    );
  return Math.min(r, i);
}
function Cy({
  fileDiff: e,
  hunkIndex: t,
  expandedHunks: n,
  collapsedContextThreshold: r,
  errorPrefix: i,
}) {
  if (t !== e.hunks.length - 1) return;
  let a = Sy({ fileDiff: e, errorPrefix: i });
  if (a <= 0) return;
  if (n === !0 || a <= r)
    return {
      fromStart: a,
      fromEnd: 0,
      rangeSize: a,
      collapsedLines: 0,
      renderAll: !0,
    };
  let o = n?.get(e.hunks.length),
    s = Math.min(Math.max(o?.fromStart ?? 0, 0), a);
  return {
    fromStart: s,
    fromEnd: 0,
    rangeSize: a,
    collapsedLines: a - s,
    renderAll: s >= a,
  };
}
function wy({ type: e, metrics: t }) {
  return t.hunkSeparatorHeight ?? qg(e);
}
function Ty({ type: e, metrics: t }) {
  return e === `simple` || e === `metadata` || e === `line-info-basic`
    ? 0
    : t.spacing;
}
function Ey({ type: e, hunkIndex: t, hunkSpecs: n }) {
  switch (e) {
    case `simple`:
      return t > 0;
    case `metadata`:
      return n != null;
    case `line-info`:
    case `line-info-basic`:
    case `custom`:
      return !0;
  }
}
function Dy(e) {
  return e !== `simple` && e !== `metadata`;
}
function Oy({ type: e, metrics: t, hunkIndex: n, hunkSpecs: r }) {
  if (!Ey({ type: e, hunkIndex: n, hunkSpecs: r })) return;
  let i = wy({ type: e, metrics: t }),
    a = Ty({ type: e, metrics: t }),
    o = n > 0 ? a : 0,
    s = a;
  return { height: i, gapBefore: o, gapAfter: s, totalHeight: o + i + s };
}
function ky({ type: e, metrics: t }) {
  if (!Dy(e)) return;
  let n = wy({ type: e, metrics: t }),
    r = Ty({ type: e, metrics: t });
  return { height: n, gapBefore: r, gapAfter: 0, totalHeight: r + n };
}
var Ay = e(() => {
  Jg();
});
function jy({
  diff: e,
  diffStyle: t,
  startingLine: n = 0,
  totalLines: r = 1 / 0,
  expandedHunks: i,
  collapsedContextThreshold: a = 1,
  callback: o,
}) {
  let s = My({
      diff: e,
      diffStyle: t,
      startingLine: n,
      expandedHunks: i,
      collapsedContextThreshold: a,
    }),
    c = {
      viewportStart: n,
      viewportEnd: n + r,
      isWindowedHighlight: n > 0 || r < 1 / 0,
      splitCount: s.splitCount,
      unifiedCount: s.unifiedCount,
      finalHunkIndex: e.hunks.length - 1,
      shouldBreak() {
        if (!c.isWindowedHighlight) return !1;
        let e = c.unifiedCount >= n + r,
          i = c.splitCount >= n + r;
        return t === `unified` ? e : (t === `split` || e) && i;
      },
      shouldSkip(e, r) {
        if (!c.isWindowedHighlight) return !1;
        let i = e > 0 && c.unifiedCount + e <= n,
          a = r > 0 && c.splitCount + r <= n;
        return t === `unified` ? i : (t === `split` || i) && a;
      },
      incrementCounts(e, n) {
        ((t === `unified` || t === `both`) && (c.unifiedCount += e),
          (t === `split` || t === `both`) && (c.splitCount += n));
      },
      isInWindow(e, n) {
        if (!c.isWindowedHighlight) return !0;
        let r = c.isInUnifiedWindow(e),
          i = c.isInSplitWindow(n);
        return t === `unified` ? r : t === `split` ? i : r || i;
      },
      isInUnifiedWindow(e) {
        return (
          !c.isWindowedHighlight ||
          (c.unifiedCount >= n - e && c.unifiedCount < n + r)
        );
      },
      isInSplitWindow(e) {
        return (
          !c.isWindowedHighlight ||
          (c.splitCount >= n - e && c.splitCount < n + r)
        );
      },
      emit(e, n = !1) {
        return (
          n ||
            (t === `unified`
              ? c.incrementCounts(1, 0)
              : t === `split`
                ? c.incrementCounts(0, 1)
                : c.incrementCounts(1, 1)),
          o(e) ?? !1
        );
      },
    };
  hunkIterator: for (let n = s.hunkIndex; n < e.hunks.length; n++) {
    let r = e.hunks[n];
    if (r == null) throw Error(`iterateOverDiff: invalid hunk index`);
    if (c.shouldBreak()) break;
    let o = by({
        isPartial: e.isPartial,
        rangeSize: r.collapsedBefore,
        expandedHunks: i,
        hunkIndex: n,
        collapsedContextThreshold: a,
      }),
      s =
        n === c.finalHunkIndex
          ? Cy({
              fileDiff: e,
              hunkIndex: n,
              expandedHunks: i,
              collapsedContextThreshold: a,
              errorPrefix: `iterateOverDiff`,
            })
          : void 0,
      l = o.fromStart + o.fromEnd;
    function u(e, n) {
      return s == null || s.collapsedLines <= 0 || s.fromStart + s.fromEnd > 0
        ? 0
        : t === `unified`
          ? e === r.unifiedLineStart + r.unifiedLineCount - 1
            ? s.collapsedLines
            : 0
          : n === r.splitLineStart + r.splitLineCount - 1
            ? s.collapsedLines
            : 0;
    }
    let d = o.collapsedLines === 0;
    function f() {
      return d ? 0 : ((d = !0), o.collapsedLines);
    }
    if (c.shouldSkip(l, l)) (c.incrementCounts(l, l), f());
    else {
      let e = r.unifiedLineStart - o.rangeSize,
        i = r.splitLineStart - o.rangeSize,
        a = r.deletionLineIndex - o.rangeSize,
        s = r.additionLineIndex - o.rangeSize,
        l = r.deletionStart - o.rangeSize,
        u = r.additionStart - o.rangeSize;
      if (
        Fy(c, o.fromStart, t, (t) =>
          c.emit({
            hunkIndex: n,
            hunk: r,
            collapsedBefore: 0,
            collapsedAfter: 0,
            type: `context-expanded`,
            deletionLine: {
              lineNumber: l + t,
              lineIndex: a + t,
              noEOFCR: !1,
              unifiedLineIndex: e + t,
              splitLineIndex: i + t,
            },
            additionLine: {
              unifiedLineIndex: e + t,
              splitLineIndex: i + t,
              lineIndex: s + t,
              lineNumber: u + t,
              noEOFCR: !1,
            },
          }),
        ) ||
        ((e = r.unifiedLineStart - o.fromEnd),
        (i = r.splitLineStart - o.fromEnd),
        (a = r.deletionLineIndex - o.fromEnd),
        (s = r.additionLineIndex - o.fromEnd),
        (l = r.deletionStart - o.fromEnd),
        (u = r.additionStart - o.fromEnd),
        Fy(
          c,
          o.fromEnd,
          t,
          (t) =>
            c.emit({
              hunkIndex: n,
              hunk: r,
              collapsedBefore: f(),
              collapsedAfter: 0,
              type: `context-expanded`,
              deletionLine: {
                lineNumber: l + t,
                lineIndex: a + t,
                noEOFCR: !1,
                unifiedLineIndex: e + t,
                splitLineIndex: i + t,
              },
              additionLine: {
                unifiedLineIndex: e + t,
                splitLineIndex: i + t,
                lineIndex: s + t,
                lineNumber: u + t,
                noEOFCR: !1,
              },
            }),
          () => {
            f();
          },
        ))
      )
        break hunkIterator;
    }
    let p = r.unifiedLineStart,
      m = r.splitLineStart,
      h = r.deletionLineIndex,
      g = r.additionLineIndex,
      _ = r.deletionStart,
      v = r.additionStart,
      y = r.hunkContent.at(-1);
    for (let e of r.hunkContent) {
      if (c.shouldBreak()) break hunkIterator;
      let i = e === y;
      if (e.type === `context`) {
        if (c.shouldSkip(e.lines, e.lines))
          (c.incrementCounts(e.lines, e.lines), f());
        else if (
          Fy(
            c,
            e.lines,
            t,
            (t) => {
              let a = i && t === e.lines - 1,
                o = p + t,
                s = m + t;
              return c.emit({
                hunkIndex: n,
                hunk: r,
                collapsedBefore: f(),
                collapsedAfter: u(o, s),
                type: `context`,
                deletionLine: {
                  lineNumber: _ + t,
                  lineIndex: h + t,
                  noEOFCR: a && r.noEOFCRDeletions,
                  unifiedLineIndex: o,
                  splitLineIndex: s,
                },
                additionLine: {
                  unifiedLineIndex: o,
                  splitLineIndex: s,
                  lineIndex: g + t,
                  lineNumber: v + t,
                  noEOFCR: a && r.noEOFCRAdditions,
                },
              });
            },
            () => {
              f();
            },
          )
        )
          break hunkIterator;
        ((p += e.lines),
          (m += e.lines),
          (h += e.lines),
          (g += e.lines),
          (_ += e.lines),
          (v += e.lines));
      } else {
        let a = Math.max(e.deletions, e.additions),
          o = e.deletions + e.additions;
        if (!c.shouldSkip(o, a)) {
          let s = Iy(c, e, t);
          (s[0]?.[0] ?? 0) > 0 && f();
          for (let [l, d] of s)
            for (let s = l; s < d; s++) {
              let l = u(
                p + s,
                t === `unified`
                  ? m + (s < e.deletions ? s : s - e.deletions)
                  : m + s,
              );
              if (
                c.emit(
                  Ly({
                    hunkIndex: n,
                    hunk: r,
                    collapsedBefore: f(),
                    collapsedAfter: l,
                    diffStyle: t,
                    index: s,
                    unifiedLineIndex: p,
                    splitLineIndex: m,
                    additionLineIndex: g,
                    deletionLineIndex: h,
                    additionLineNumber: v,
                    deletionLineNumber: _,
                    content: e,
                    isLastContent: i,
                    unifiedCount: o,
                    splitCount: a,
                  }),
                  !0,
                )
              )
                break hunkIterator;
            }
        }
        (f(),
          c.incrementCounts(o, a),
          (p += o),
          (m += a),
          (h += e.deletions),
          (g += e.additions),
          (_ += e.deletions),
          (v += e.additions));
      }
    }
    if (s != null) {
      let { collapsedLines: n, fromStart: r, fromEnd: i } = s,
        a = r + i;
      if (
        Fy(
          c,
          a,
          t,
          (t) => {
            let r = t === a - 1;
            return c.emit({
              hunkIndex: e.hunks.length,
              hunk: void 0,
              collapsedBefore: 0,
              collapsedAfter: r ? n : 0,
              type: `context-expanded`,
              deletionLine: {
                lineNumber: _ + t,
                lineIndex: h + t,
                noEOFCR: !1,
                unifiedLineIndex: p + t,
                splitLineIndex: m + t,
              },
              additionLine: {
                unifiedLineIndex: p + t,
                splitLineIndex: m + t,
                lineIndex: g + t,
                lineNumber: v + t,
                noEOFCR: !1,
              },
            });
          },
          void 0,
          () => c.shouldBreak(),
        )
      )
        break hunkIterator;
    }
  }
}
function My({
  diff: e,
  diffStyle: t,
  startingLine: n,
  expandedHunks: r,
  collapsedContextThreshold: i,
}) {
  if (n <= 0 || t === `both`)
    return { hunkIndex: 0, splitCount: 0, unifiedCount: 0 };
  let a = Ny({ diff: e, expandedHunks: r, collapsedContextThreshold: i }),
    o = 0,
    s = e.hunks.length - 1,
    c = e.hunks.length;
  for (; o <= s;) {
    let e = (o + s) >> 1,
      r = a[e + 1];
    if (r == null) throw Error(`iterateOverDiff: invalid hunk prefix index`);
    (t === `unified` ? r.unifiedCount : r.splitCount) > n
      ? ((c = e), (s = e - 1))
      : (o = e + 1);
  }
  if (c >= e.hunks.length) {
    let t = a[e.hunks.length];
    if (t == null)
      throw Error(`iterateOverDiff: invalid terminal hunk prefix index`);
    return {
      hunkIndex: e.hunks.length,
      splitCount: t.splitCount,
      unifiedCount: t.unifiedCount,
    };
  }
  let l = a[c];
  if (l == null)
    throw Error(`iterateOverDiff: invalid selected hunk prefix index`);
  return {
    hunkIndex: c,
    splitCount: l.splitCount,
    unifiedCount: l.unifiedCount,
  };
}
function Ny({ diff: e, expandedHunks: t, collapsedContextThreshold: n }) {
  let r = 0,
    i = 0,
    a = e.hunks.length - 1,
    o = [{ splitCount: 0, unifiedCount: 0 }];
  for (let s = 0; s < e.hunks.length; s++) {
    let c = e.hunks[s];
    if (c == null) throw Error(`iterateOverDiff: invalid hunk summary index`);
    let l = by({
        isPartial: e.isPartial,
        rangeSize: c.collapsedBefore,
        expandedHunks: t,
        hunkIndex: s,
        collapsedContextThreshold: n,
      }),
      u = l.fromStart + l.fromEnd;
    ((r += u + c.splitLineCount), (i += u + c.unifiedLineCount));
    let d =
      s === a
        ? Cy({
            fileDiff: e,
            hunkIndex: s,
            expandedHunks: t,
            collapsedContextThreshold: n,
            errorPrefix: `iterateOverDiff`,
          })
        : void 0;
    if (d != null) {
      let e = d.fromStart + d.fromEnd;
      ((r += e), (i += e));
    }
    o.push({ splitCount: r, unifiedCount: i });
  }
  return o;
}
function Py(e, t, n) {
  if (!e.isWindowedHighlight || t <= 0) return [0, t];
  let r = [];
  function i(n) {
    let i = Math.max(0, e.viewportStart - n),
      a = Math.min(t, e.viewportEnd - n);
    a > i && r.push([i, a]);
  }
  if (
    (n !== `split` && i(e.unifiedCount),
    n !== `unified` && i(e.splitCount),
    r.length === 0)
  )
    return [0, 0];
  let a = r[0][0],
    o = r[0][1];
  for (let e = 1; e < r.length; e++) {
    let t = r[e];
    ((a = Math.min(a, t[0])), (o = Math.max(o, t[1])));
  }
  return [a, o];
}
function Fy(e, t, n, r, i, a) {
  let [o, s] = Py(e, t, n);
  o > 0 && (e.incrementCounts(o, o), i?.());
  let c = o;
  for (; c < t;) {
    if (a?.() === !0) return !0;
    if (c >= s) {
      e.incrementCounts(t - c, t - c);
      break;
    }
    if (e.isInWindow(0, 0)) {
      if (r(c) === !0) return !0;
    } else e.incrementCounts(1, 1);
    c++;
  }
  return !1;
}
function Iy(e, t, n) {
  if (!e.isWindowedHighlight)
    return [
      [
        0,
        n === `unified`
          ? t.deletions + t.additions
          : Math.max(t.deletions, t.additions),
      ],
    ];
  let r = n !== `split`,
    i = n !== `unified`,
    a = n === `unified` ? `unified` : `split`,
    o = [];
  function s(t, n) {
    if (t + n <= e.viewportStart || t >= e.viewportEnd) return;
    let r = Math.max(0, e.viewportStart - t),
      i = Math.min(n, e.viewportEnd - t);
    return i > r ? [r, i] : void 0;
  }
  function c(e, n) {
    return a === `split`
      ? e
      : n === `additions`
        ? [e[0] + t.deletions, e[1] + t.deletions]
        : e;
  }
  function l(e, t) {
    if (e == null) return;
    let [n, r] = c(e, t);
    r > n && o.push([n, r]);
  }
  if (
    (r &&
      (l(s(e.unifiedCount, t.deletions), `deletions`),
      l(s(e.unifiedCount + t.deletions, t.additions), `additions`)),
    i &&
      (l(s(e.splitCount, t.deletions), `deletions`),
      l(s(e.splitCount, t.additions), `additions`)),
    o.length === 0)
  )
    return o;
  o.sort((e, t) => e[0] - t[0]);
  let u = [o[0]];
  for (let [e, t] of o.slice(1)) {
    let n = u[u.length - 1];
    e <= n[1] ? (n[1] = Math.max(n[1], t)) : u.push([e, t]);
  }
  return u;
}
function Ly({
  hunkIndex: e,
  hunk: t,
  collapsedAfter: n,
  collapsedBefore: r,
  diffStyle: i,
  index: a,
  unifiedLineIndex: o,
  splitLineIndex: s,
  additionLineIndex: c,
  deletionLineIndex: l,
  additionLineNumber: u,
  deletionLineNumber: d,
  content: f,
  isLastContent: p,
  unifiedCount: m,
  splitCount: h,
}) {
  let g = a < f.deletions ? o + a : void 0,
    _ =
      i === `unified`
        ? a >= f.deletions
          ? o + a
          : void 0
        : a < f.additions
          ? o + f.deletions + a
          : void 0,
    v = i === `unified` ? s + (a < f.deletions ? a : a - f.deletions) : s + a,
    y = a < f.deletions ? l + a : void 0,
    b = a < f.deletions ? d + a : void 0,
    x =
      i === `unified`
        ? a >= f.deletions
          ? c + (a - f.deletions)
          : void 0
        : a < f.additions
          ? c + a
          : void 0,
    S =
      i === `unified`
        ? a >= f.deletions
          ? u + (a - f.deletions)
          : void 0
        : a < f.additions
          ? u + a
          : void 0,
    C =
      i === `unified`
        ? p && a === f.deletions - 1 && t.noEOFCRDeletions
        : p && a === h - 1 && t.noEOFCRDeletions,
    ee =
      i === `unified`
        ? p && a === m - 1 && t.noEOFCRAdditions
        : p && a === h - 1 && t.noEOFCRAdditions,
    w =
      y != null && b != null && g != null
        ? {
            lineNumber: b,
            lineIndex: y,
            noEOFCR: C,
            unifiedLineIndex: g,
            splitLineIndex: v,
          }
        : void 0,
    T =
      x != null && S != null && _ != null
        ? {
            unifiedLineIndex: _,
            splitLineIndex: v,
            lineIndex: x,
            lineNumber: S,
            noEOFCR: ee,
          }
        : void 0;
  if (w == null && T != null)
    return {
      type: `change`,
      hunkIndex: e,
      hunk: t,
      collapsedAfter: n,
      collapsedBefore: r,
      deletionLine: void 0,
      additionLine: T,
    };
  if (w != null && T == null)
    return {
      type: `change`,
      hunkIndex: e,
      hunk: t,
      collapsedAfter: n,
      collapsedBefore: r,
      deletionLine: w,
      additionLine: void 0,
    };
  if (w == null || T == null)
    throw Error(`iterateOverDiff: missing change line data`);
  return {
    type: `change`,
    hunkIndex: e,
    hunk: t,
    collapsedAfter: n,
    collapsedBefore: r,
    deletionLine: w,
    additionLine: T,
  };
}
var Ry = e(() => {
  Ay();
});
function zy(
  e,
  t,
  n,
  {
    forcePlainText: r,
    startingLine: i,
    totalLines: a,
    expandedHunks: o,
    collapsedContextThreshold: s = 1,
  } = Uy,
) {
  r ? ((i ??= 0), (a ??= 1 / 0)) : ((i = 0), (a = 1 / 0));
  let c = i > 0 || a < 1 / 0,
    l = typeof n.theme == `string` ? t.getTheme(n.theme).type : void 0,
    u = bh({ theme: n.theme, highlighter: t }),
    d =
      r && !c && (e.unifiedLineCount > 1e3 || e.splitLineCount > 1e3)
        ? `none`
        : n.lineDiffType,
    f = { deletionLines: [], additionLines: [] },
    { maxLineDiffLength: p } = n,
    m = !r && !e.isPartial,
    h = r ? o : void 0,
    g = new Map();
  function _(e) {
    let t = m ? 0 : e,
      n = g.get(t) ?? Vy();
    return (g.set(t, n), n);
  }
  function v(e, t, n, r) {
    if (c) {
      let e = n.at(-1);
      ((e == null || e.targetIndex + e.count !== t) &&
        ((e = { targetIndex: t, originalOffset: r.length, count: 0 }),
        n.push(e)),
        e.count++);
    }
    r.push(e);
  }
  jy({
    diff: e,
    diffStyle: `both`,
    startingLine: i,
    totalLines: a,
    expandedHunks: c ? h : !0,
    collapsedContextThreshold: s,
    callback: ({ hunkIndex: t, additionLine: n, deletionLine: r, type: i }) => {
      let a = _(t),
        o = n == null ? r.splitLineIndex : n.splitLineIndex;
      (i === `change` &&
        n != null &&
        r != null &&
        By({
          additionLine: e.additionLines[n.lineIndex],
          deletionLine: e.deletionLines[r.lineIndex],
          deletionLineIndex: a.deletionContent.length,
          additionLineIndex: a.additionContent.length,
          deletionDecorations: a.deletionDecorations,
          additionDecorations: a.additionDecorations,
          lineDiffType: d,
          maxLineDiffLength: p,
        }),
        r != null &&
          (v(
            e.deletionLines[r.lineIndex],
            r.lineIndex,
            a.deletionSegments,
            a.deletionContent,
          ),
          a.deletionInfo.push({
            type: i === `change` ? `change-deletion` : i,
            lineNumber: r.lineNumber,
            altLineNumber: i === `change` ? void 0 : (n.lineNumber ?? void 0),
            lineIndex: `${r.unifiedLineIndex},${o}`,
          })),
        n != null &&
          (v(
            e.additionLines[n.lineIndex],
            n.lineIndex,
            a.additionSegments,
            a.additionContent,
          ),
          a.additionInfo.push({
            type: i === `change` ? `change-addition` : i,
            lineNumber: n.lineNumber,
            altLineNumber: i === `change` ? void 0 : (r.lineNumber ?? void 0),
            lineIndex: `${n.unifiedLineIndex},${o}`,
          })));
    },
  });
  for (let i of g.values()) {
    if (i.deletionContent.length === 0 && i.additionContent.length === 0)
      continue;
    let a = { name: e.prevName ?? e.name, contents: i.deletionContent.value },
      o = { name: e.name, contents: i.additionContent.value },
      { deletionLines: s, additionLines: c } = Hy({
        deletionFile: a,
        deletionInfo: i.deletionInfo,
        deletionDecorations: i.deletionDecorations,
        additionFile: o,
        additionInfo: i.additionInfo,
        additionDecorations: i.additionDecorations,
        highlighter: t,
        options: n,
        languageOverride: r ? `text` : e.lang,
      });
    if (m) {
      ((f.deletionLines = s), (f.additionLines = c));
      continue;
    }
    if (i.deletionSegments.length > 0)
      for (let e of i.deletionSegments)
        for (let t = 0; t < e.count; t++)
          f.deletionLines[e.targetIndex + t] = s[e.originalOffset + t];
    else f.deletionLines.push(...s);
    if (i.additionSegments.length > 0)
      for (let e of i.additionSegments)
        for (let t = 0; t < e.count; t++)
          f.additionLines[e.targetIndex + t] = c[e.originalOffset + t];
    else f.additionLines.push(...c);
  }
  return { code: f, themeStyles: u, baseThemeType: l };
}
function By({
  deletionLine: e,
  additionLine: t,
  deletionLineIndex: n,
  additionLineIndex: r,
  deletionDecorations: i,
  additionDecorations: a,
  lineDiffType: o,
  maxLineDiffLength: s,
}) {
  if (
    e == null ||
    t == null ||
    o === `none` ||
    ((e = __(e)), (t = __(t)), e.length > s || t.length > s)
  )
    return;
  let c = o === `char` ? tv(e, t) : yv(e, t),
    l = [],
    u = [],
    d = o === `word-alt`,
    f = c.at(-1);
  for (let e of c) {
    let t = e === f;
    !e.added && !e.removed
      ? (vy({ item: e, arr: l, enableJoin: d, isNeutral: !0, isLastItem: t }),
        vy({ item: e, arr: u, enableJoin: d, isNeutral: !0, isLastItem: t }))
      : e.removed
        ? vy({ item: e, arr: l, enableJoin: d, isLastItem: t })
        : vy({ item: e, arr: u, enableJoin: d, isLastItem: t });
  }
  let p = 0;
  for (let e of l)
    (e[0] === 1 &&
      i.push(_y({ line: n, spanStart: p, spanLength: e[1].length })),
      (p += e[1].length));
  p = 0;
  for (let e of u)
    (e[0] === 1 &&
      a.push(_y({ line: r, spanStart: p, spanLength: e[1].length })),
      (p += e[1].length));
}
function Vy() {
  return {
    deletionContent: {
      push(e) {
        ((this.value += e), this.length++);
      },
      value: ``,
      length: 0,
    },
    additionContent: {
      push(e) {
        ((this.value += e), this.length++);
      },
      value: ``,
      length: 0,
    },
    deletionInfo: [],
    additionInfo: [],
    deletionDecorations: [],
    additionDecorations: [],
    deletionSegments: [],
    additionSegments: [],
  };
}
function Hy({
  deletionFile: e,
  additionFile: t,
  deletionInfo: n,
  additionInfo: r,
  highlighter: i,
  deletionDecorations: a,
  additionDecorations: o,
  languageOverride: s,
  options: { theme: c = P, ...l },
}) {
  let u = s ?? Am(e.name),
    d = s ?? Am(t.name),
    { state: f, transformers: p } = $m(l.useTokenTransformer),
    m =
      typeof c == `string`
        ? {
            ...l,
            lang: `text`,
            theme: c,
            transformers: p,
            decorations: void 0,
            defaultColor: !1,
            cssVariablePrefix: rh(`token`),
            tokenizeTimeLimit: 0,
          }
        : {
            ...l,
            lang: `text`,
            themes: c,
            transformers: p,
            decorations: void 0,
            defaultColor: !1,
            cssVariablePrefix: rh(`token`),
            tokenizeTimeLimit: 0,
          };
  return {
    deletionLines:
      e.contents === ``
        ? []
        : ((m.lang = u),
          (f.lineInfo = n),
          (m.decorations = a),
          Ch(i.codeToHast(__(e.contents), m))),
    additionLines:
      t.contents === ``
        ? []
        : ((m.lang = d),
          (m.decorations = o),
          (f.lineInfo = r),
          Ch(i.codeToHast(__(t.contents), m))),
  };
}
var Uy,
  Wy = e(() => {
    (F(),
      Im(),
      nh(),
      ih(),
      Sh(),
      wh(),
      Ry(),
      v_(),
      yy(),
      Yv(),
      (Uy = { forcePlainText: !1 }));
  });
function Gy(e) {
  let t = e.lang ?? Am(e.name),
    n = e.lang ?? (e.prevName == null ? `text` : Am(e.prevName));
  return t === `text` && n === `text`;
}
var Ky = e(() => {
  Im();
});
function qy(e, t) {
  let n = Xv(
    { name: e.prevName ?? e.name, contents: e.deletionLines.join(``) },
    { name: e.name, contents: e.additionLines.join(``), lang: e.lang },
    t,
  );
  return {
    hunks: n.hunks,
    splitLineCount: n.splitLineCount,
    unifiedLineCount: n.unifiedLineCount,
    additionLines: n.additionLines,
    deletionLines: n.deletionLines,
    type: n.type,
  };
}
function Jy(e, t) {
  let n = Math.max(e, 1),
    r = Array.from({ length: n }, (e, t) => `${` `.repeat(t + 1)}\n`).join(``);
  return (
    r === t &&
      (r = Array.from({ length: n }, (e, t) => `\u0000${` `.repeat(t)}\n`).join(
        ``,
      )),
    r
  );
}
function Yy(e) {
  for (let t of e) if (t.trim().length > 0) return !1;
  return !0;
}
function Xy(e, t) {
  return t.length > 0 && t.length < e.deletionLines.length && Yy(t);
}
function Zy(e, t, n) {
  let r = e.deletionLines.join(``),
    i = Jy(t.length, r),
    a = Xv(
      { name: e.prevName ?? e.name, contents: r },
      { name: e.name, contents: i, lang: e.lang },
      n,
    );
  return {
    hunks: a.hunks,
    splitLineCount: a.splitLineCount,
    unifiedLineCount: a.unifiedLineCount,
    additionLines: t,
    deletionLines: a.deletionLines,
    type: a.type,
  };
}
function Qy(e, t) {
  return Zy(e, [``], t);
}
function $y(e, t) {
  return e.additionLines.length === 0
    ? Qy(e, t)
    : Xy(e, e.additionLines)
      ? Zy(e, e.additionLines, t)
      : qy(e, t);
}
function eb(e, t, n) {
  if (e.isPartial || e.deletionLines.length !== e.additionLines.length)
    return tb(e, qy(e, n));
  let r = Array.from(t);
  if (r.length === 0)
    return tb(e, {
      hunks: e.hunks,
      splitLineCount: e.splitLineCount,
      unifiedLineCount: e.unifiedLineCount,
      type: e.type,
    });
  for (let t of r) {
    let r = e.additionLines[t],
      i = e.deletionLines[t];
    if (r == null || i == null || __(r) === __(i)) return tb(e, qy(e, n));
  }
  let i = nb(e, r);
  if (i.size === 0) return tb(e, qy(e, n));
  for (let t of i) if (!ib(e, t, n)) return tb(e, qy(e, n));
  return (
    lb(e),
    xy(e)
      ? tb(e, qy(e, n))
      : tb(e, {
          hunks: e.hunks,
          splitLineCount: e.splitLineCount,
          unifiedLineCount: e.unifiedLineCount,
          type: e.type,
        })
  );
}
function tb(e, t) {
  return (Object.assign(e, t), t);
}
function nb(e, t) {
  let n = new Set();
  for (let r of t) {
    let t = rb(e, r);
    if (t == null) return new Set();
    n.add(t);
  }
  return n;
}
function rb(e, t) {
  for (let [n, r] of e.hunks.entries()) {
    let e = r.additionLineIndex + r.additionCount;
    if (t >= r.additionLineIndex && t < e) return n;
  }
}
function ib(e, t, n) {
  let r = e.hunks[t];
  if (r == null) return !1;
  let i = e.deletionLines.slice(
      r.deletionLineIndex,
      r.deletionLineIndex + r.deletionCount,
    ),
    a = e.additionLines.slice(
      r.additionLineIndex,
      r.additionLineIndex + r.additionCount,
    ),
    o = Xv(
      { name: e.prevName ?? e.name, contents: i.join(``) },
      { name: e.name, contents: a.join(``), lang: e.lang },
      { ...n, context: 0 },
    ),
    s = o.hunks[0];
  return s == null || o.hunks.length !== 1 ? !1 : (ob(r, s), ab(e, t), !0);
}
function ab(e, t) {
  let n = e.hunks[t];
  if (n == null) return;
  if (t !== e.hunks.length - 1) {
    ((n.noEOFCRAdditions = !1), (n.noEOFCRDeletions = !1));
    return;
  }
  let r = e.additionLines.at(-1),
    i = e.deletionLines.at(-1);
  ((n.noEOFCRAdditions =
    r != null &&
    r !== `` &&
    !r.endsWith(`
`)),
    (n.noEOFCRDeletions =
      i != null &&
      i !== `` &&
      !i.endsWith(`
`)));
}
function ob(e, t) {
  let n = e.additionLineIndex,
    r = e.deletionLineIndex;
  ((e.hunkContent = t.hunkContent.map((e) => sb(e, n, r))),
    (e.additionLineIndex = n + t.additionLineIndex),
    (e.additionStart += t.additionLineIndex),
    (e.additionCount = t.additionCount),
    (e.additionLines = t.additionLines),
    t.deletionLineIndex >= 0 &&
      ((e.deletionLineIndex = r + t.deletionLineIndex),
      (e.deletionStart += t.deletionLineIndex)),
    (e.deletionCount = t.deletionCount),
    (e.deletionLines = t.deletionLines),
    (e.noEOFCRAdditions = t.noEOFCRAdditions),
    (e.noEOFCRDeletions = t.noEOFCRDeletions),
    cb(e));
}
function sb(e, t, n) {
  return {
    ...e,
    additionLineIndex: e.additionLineIndex + t,
    deletionLineIndex: e.deletionLineIndex + n,
  };
}
function cb(e) {
  let t = 0,
    n = 0;
  for (let r of e.hunkContent)
    r.type === `context`
      ? ((t += r.lines), (n += r.lines))
      : ((t += Math.max(r.additions, r.deletions)),
        (n += r.additions + r.deletions));
  ((e.splitLineCount = t), (e.unifiedLineCount = n));
}
function lb(e) {
  let t = 0,
    n = 0,
    r = 0;
  for (let i of e.hunks)
    ((i.collapsedBefore = Math.max(i.additionStart - 1 - r, 0)),
      (i.splitLineStart = t + i.collapsedBefore),
      (i.unifiedLineStart = n + i.collapsedBefore),
      cb(i),
      (t += i.collapsedBefore + i.splitLineCount),
      (n += i.collapsedBefore + i.unifiedLineCount),
      (r = i.additionStart + i.additionCount - 1));
  if (e.hunks.length > 0) {
    let r = e.hunks[e.hunks.length - 1],
      i = Math.max(
        e.additionLines.length - (r.additionLineIndex + r.additionCount),
        0,
      );
    ((t += i), (n += i));
  }
  ((e.splitLineCount = t), (e.unifiedLineCount = n));
}
var ub = e(() => {
  (Ay(), v_(), $v());
});
function db(e) {
  return e?.map((e) => zm(e)) ?? [];
}
function fb(e) {
  return `${e} unmodified line${wb.select(e) === `one` ? `` : `s`}`;
}
function pb(e, t) {
  for (let n of e)
    (t.unifiedContentAST.push(n.content),
      t.pushToGutter(`unified`, n.gutter),
      t.incrementRowCount(1));
}
function mb(e, t, n) {
  for (let { deletion: r, addition: i } of e) {
    if (r == null && i == null) continue;
    let e =
      r != null && i != null ? void 0 : r == null ? `deletions` : `additions`;
    ((e == null || n.side !== e) && n.flush(),
      r != null &&
        (t.deletionsContentAST.push(r.content),
        t.pushToGutter(`deletions`, r.gutter)),
      i != null &&
        (t.additionsContentAST.push(i.content),
        t.pushToGutter(`additions`, i.gutter)),
      e != null && ((n.side = e), n.increment()),
      t.incrementRowCount(1));
  }
}
function hb({
  diffStyle: e,
  type: t,
  deletionLine: n,
  additionLine: r,
  unifiedSpan: i,
  deletionSpan: a,
  additionSpan: o,
  createAnnotationElement: s,
  context: c,
}) {
  let l = !1;
  if (e === `unified`) {
    if (
      (r == null
        ? n != null && c.unifiedContentAST.push(n)
        : c.unifiedContentAST.push(r),
      i != null)
    ) {
      let e =
        t === `change`
          ? n == null
            ? `change-addition`
            : `change-deletion`
          : t;
      (c.unifiedContentAST.push(s(i)),
        c.pushToGutter(`unified`, yn(e, `annotation`, 1)),
        (l = !0));
    }
  } else if (e === `split`) {
    if (
      (n != null && c.deletionsContentAST.push(n),
      r != null && c.additionsContentAST.push(r),
      a != null)
    ) {
      let e = t === `change` ? (n == null ? `context` : `change-deletion`) : t;
      (c.deletionsContentAST.push(s(a)),
        c.pushToGutter(`deletions`, yn(e, `annotation`, 1)),
        (l = !0));
    }
    if (o != null) {
      let e = t === `change` ? (r == null ? `context` : `change-addition`) : t;
      (c.additionsContentAST.push(s(o)),
        c.pushToGutter(`additions`, yn(e, `annotation`, 1)),
        (l = !0));
    }
  }
  l && c.incrementRowCount(1);
}
function gb(
  e,
  {
    hunkIndex: t,
    collapsedLines: n,
    rangeSize: r,
    hunkSpecs: i,
    isFirstHunk: a,
    isLastHunk: o,
    isExpandable: s,
  },
  c,
) {
  if (typeof n == `number` && n <= 0) return;
  let l =
    e === `unified`
      ? c.unifiedContentAST
      : e === `deletions`
        ? c.deletionsContentAST
        : c.additionsContentAST;
  if (c.hunkSeparators === `metadata`) {
    i != null &&
      (c.pushToGutter(
        e,
        ly({ type: `metadata`, content: i, isFirstHunk: a, isLastHunk: o }),
      ),
      l.push(
        ly({ type: `metadata`, content: i, isFirstHunk: a, isLastHunk: o }),
      ),
      e !== `additions` && c.incrementRowCount(1));
    return;
  }
  if (c.hunkSeparators === `simple`) {
    t > 0 &&
      (c.pushToGutter(
        e,
        ly({ type: `simple`, isFirstHunk: a, isLastHunk: !1 }),
      ),
      l.push(ly({ type: `simple`, isFirstHunk: a, isLastHunk: !1 })),
      e !== `additions` && c.incrementRowCount(1));
    return;
  }
  let u = dy(e, t),
    d = r > c.expansionLineCount,
    f = s ? t : void 0,
    p =
      typeof n == `number` ? fb(n) : `More unchanged context may be available`;
  (c.pushToGutter(
    e,
    ly({
      type: c.hunkSeparators,
      content: p,
      expandIndex: f,
      chunked: d,
      slotName: u,
      isFirstHunk: a,
      isLastHunk: o,
    }),
  ),
    l.push(
      ly({
        type: c.hunkSeparators,
        content: p,
        expandIndex: f,
        chunked: d,
        slotName: u,
        isFirstHunk: a,
        isLastHunk: o,
      }),
    ),
    e !== `additions` && c.incrementRowCount(1),
    c.hunkData.push({
      slotName: u,
      hunkIndex: t,
      lines: typeof n == `number` ? n : 0,
      lineCountKnown: typeof n == `number`,
      type: e,
      expandable: s ? { up: !a, down: !o, chunked: d } : void 0,
    }));
}
function _b(e, t, n) {
  return e == null || e.type !== `element` || (t == null && n == null)
    ? e
    : { ...e, properties: { ...e.properties, ...t, ...n } };
}
function vb(e, t) {
  return {
    type: `element`,
    tagName: `div`,
    properties: {
      "data-line": e + 1,
      "data-line-index": `${e},${e}`,
      "data-line-type": `context`,
    },
    children: [
      {
        type: `element`,
        tagName: `span`,
        properties: { "data-char": 0 },
        children: [{ type: `text`, value: t.getLineText(e) }],
      },
    ],
  };
}
function yb(e, t) {
  return e.endsWith(`\r
`)
    ? t +
        `\r
`
    : e.endsWith(`\r`)
      ? t + `\r`
      : e.endsWith(`
`)
        ? t +
          `
`
        : t;
}
function bb(e, t) {
  return Math.max(e.additionLines.length, e.deletionLines.length) > t;
}
function xb(e, t) {
  return (
    e.isPartial && t && (e.type === `change` || e.type === `rename-changed`)
  );
}
var Sb,
  Cb,
  wb,
  Tb = e(() => {
    (F(),
      zh(),
      L(),
      fa(),
      cm(),
      fm(),
      vm(),
      bm(),
      Vh(),
      Em(),
      km(),
      Im(),
      Rm(),
      Bm(),
      Gh(),
      n_(),
      Ay(),
      o_(),
      Ry(),
      v_(),
      ry(),
      ay(),
      sy(),
      uy(),
      fy(),
      my(),
      gy(),
      Ky(),
      Wy(),
      ub(),
      Ni(),
      (Sb = -1),
      (Cb = class {
        options;
        onRenderUpdate;
        workerManager;
        __id = `diff-hunks-renderer:${++Sb}`;
        highlighter;
        diff;
        expandedHunks = new Map();
        deletionAnnotations = {};
        additionAnnotations = {};
        computedLang = `text`;
        renderCache;
        constructor(e = { theme: P }, t, n) {
          ((this.options = e),
            (this.onRenderUpdate = t),
            (this.workerManager = n),
            n?.isWorkingPool() !== !0 &&
              (this.highlighter = dm(e.theme ?? P) ? am() : void 0));
        }
        cleanUp() {
          (this.recycle(),
            this.expandedHunks.clear(),
            (this.workerManager = void 0),
            (this.onRenderUpdate = void 0));
        }
        recycle() {
          ((this.highlighter = void 0),
            (this.diff = void 0),
            this.clearRenderCache(),
            (this.additionAnnotations = {}),
            (this.deletionAnnotations = {}),
            this.workerManager?.cleanUpTasks(this));
        }
        getDiffCache() {
          return this.renderCache?.diff ?? this.diff;
        }
        clearRenderCache() {
          let e = this.renderCache;
          ((this.renderCache = void 0),
            e != null &&
              e.isDirty === !0 &&
              e.diff.cacheKey != null &&
              this.workerManager?.evictDiffFromCache(e.diff.cacheKey));
        }
        setOptions(e) {
          this.options = e;
        }
        mergeOptions(e) {
          this.options = { ...this.options, ...e };
        }
        expandHunk(e, t, n = this.getOptionsWithDefaults().expansionLineCount) {
          let r = {
            ...(this.expandedHunks.get(e) ?? { fromStart: 0, fromEnd: 0 }),
          };
          ((t === `up` || t === `both`) && (r.fromStart += n),
            (t === `down` || t === `both`) && (r.fromEnd += n),
            this.renderCache?.highlighted !== !0 && this.clearRenderCache(),
            this.expandedHunks.set(e, r));
        }
        getExpandedHunk(e) {
          return this.expandedHunks.get(e) ?? Ut;
        }
        getExpandedHunksMap() {
          return this.expandedHunks;
        }
        setLineAnnotations(e) {
          ((this.additionAnnotations = {}), (this.deletionAnnotations = {}));
          for (let t of e) {
            let e = (() => {
                switch (t.side) {
                  case `deletions`:
                    return this.deletionAnnotations;
                  case `additions`:
                    return this.additionAnnotations;
                }
              })(),
              n = e[t.lineNumber] ?? [];
            ((e[t.lineNumber] = n), n.push(t));
          }
        }
        updateRenderCache(e, t) {
          if (this.renderCache == null) return;
          let { result: n, diff: r } = this.renderCache;
          if (n == null) return;
          if (r.isPartial)
            throw Error(`Could not update render cache for partial diff`);
          let i = n.code.additionLines,
            a = [];
          for (let [n, o] of e) {
            let e = i[n]?.properties ?? {},
              s = o.map((e) => e[2]).join(``),
              c = n < r.additionLines.length,
              l = c ? (r.additionLines[n] ?? ``) : ``,
              u = __(l);
            (c && ((r.additionLines[n] = yb(l, s)), u !== s && a.push(n)),
              (i[n] = {
                type: `element`,
                tagName: `div`,
                properties: {
                  "data-line": e[`data-line`] ?? n + 1,
                  "data-line-index": e[`data-line-index`] ?? n,
                  "data-line-type": e[`data-line-type`] ?? `context`,
                },
                children: o.map(([e, n, r]) =>
                  e === 0 && n === ``
                    ? r === ``
                      ? {
                          type: `element`,
                          tagName: `br`,
                          properties: {},
                          children: [],
                        }
                      : { type: `text`, value: r }
                    : {
                        type: `element`,
                        tagName: `span`,
                        properties: {
                          "data-char": e,
                          style: `--diffs-token-${t}:${n};`,
                        },
                        children: [{ type: `text`, value: r }],
                      },
                ),
              }));
          }
          (a.length > 0 &&
            Object.assign(r, eb(r, a, this.options.parseDiffOptions)),
            (n.baseThemeType = t),
            (this.renderCache.isDirty = !0));
        }
        applyDocumentChange(e) {
          if (this.renderCache == null) return;
          let { diff: t, result: n } = this.renderCache;
          if (n == null) return;
          let r = e.getText();
          if (r.trim().length === 0) {
            let n = [];
            for (let t = 0; t < e.lineCount; t++) n.push(e.getLineText(t, !0));
            t.additionLines = n;
          } else t.additionLines = a_(r);
          let i = t.additionLines.length,
            a = n.code.additionLines,
            o = a.length;
          i < o && (a.length = i);
          for (let t = o; t < i; t++) a[t] ??= vb(t, e);
          (t.isPartial ||
            (t.additionLines.length <= 1 && t.additionLines.join(``) === ``
              ? (Object.assign(t, Qy(t, this.options.parseDiffOptions)),
                (a[0] = vb(0, e)))
              : Object.assign(t, $y(t, this.options.parseDiffOptions))),
            (this.renderCache.isDirty = !0));
        }
        getUnifiedLineDecoration({ lineType: e }) {
          return {
            gutterLineType: e,
            contentProperties: { "data-line-type": e },
          };
        }
        getSplitLineDecoration({ side: e, type: t }) {
          let n =
            t === `change`
              ? e === `deletions`
                ? `change-deletion`
                : `change-addition`
              : t;
          return {
            gutterLineType: n,
            contentProperties: { "data-line-type": n },
          };
        }
        createAnnotationElement = (e) => ym(e);
        getOptionsWithDefaults() {
          let {
            diffIndicators: e = `bars`,
            diffStyle: t = `split`,
            disableBackground: n = !1,
            disableFileHeader: r = !1,
            disableLineNumbers: i = !1,
            disableVirtualizationBuffers: a = !1,
            collapsed: o = !1,
            expandUnchanged: s = !1,
            collapsedContextThreshold: c = 1,
            expansionLineCount: l = 100,
            hunkSeparators: u = `line-info`,
            lineDiffType: d = `word-alt`,
            maxLineDiffLength: f = 1e3,
            overflow: p = `scroll`,
            stickyHeader: m = !1,
            theme: h = P,
            headerRenderMode: g = `default`,
            tokenizeMaxLineLength: _ = 1e3,
            tokenizeMaxLength: v = Rt,
            useTokenTransformer: y = !1,
            useCSSClasses: b = !1,
          } = this.options;
          return {
            diffIndicators: e,
            diffStyle: t,
            disableBackground: n,
            disableFileHeader: r,
            disableLineNumbers: i,
            disableVirtualizationBuffers: a,
            collapsed: o,
            expandUnchanged: s,
            collapsedContextThreshold: c,
            expansionLineCount: l,
            hunkSeparators: u,
            lineDiffType: d,
            maxLineDiffLength: f,
            overflow: p,
            stickyHeader: m,
            theme: this.workerManager?.getDiffRenderOptions().theme ?? h,
            headerRenderMode: g,
            tokenizeMaxLineLength: _,
            tokenizeMaxLength: v,
            useTokenTransformer: y,
            useCSSClasses: b,
          };
        }
        async initializeHighlighter() {
          return (
            (this.highlighter = await im(Lm(this.computedLang, this.options))),
            this.highlighter
          );
        }
        hydrate(e) {
          if (e == null) return;
          this.diff = e;
          let { options: t } = this.getRenderOptions(e),
            n = bb(e, this.getTokenizeMaxLength()),
            r = this.workerManager?.getDiffResultCache(e);
          (r != null && !ny(t, r.options) && (r = void 0),
            (this.renderCache ??= {
              diff: e,
              highlighted: !n && !Gy(e),
              options: t,
              result: n ? void 0 : r?.result,
              renderRange: void 0,
            }),
            this.workerManager?.isWorkingPool() === !0
              ? this.renderCache.result == null &&
                !n &&
                this.workerManager.highlightDiffAST(this, this.diff)
              : (this.highlighter ??
                ((this.computedLang = e.lang ?? Am(e.name)),
                this.initializeHighlighter())));
        }
        getRenderOptions(e) {
          let t = (() => {
            if (this.workerManager?.isWorkingPool() === !0)
              return this.workerManager.getDiffRenderOptions();
            let {
              theme: e,
              tokenizeMaxLineLength: t,
              lineDiffType: n,
              maxLineDiffLength: r,
            } = this.getOptionsWithDefaults();
            return {
              theme: e,
              useTokenTransformer: Wh(this.options),
              tokenizeMaxLineLength: t,
              lineDiffType: n,
              maxLineDiffLength: r,
            };
          })();
          this.getOptionsWithDefaults();
          let { renderCache: n } = this;
          return n?.result == null || !t_(e, n.diff) || !ny(t, n.options)
            ? { options: t, forceHighlight: !0 }
            : { options: t, forceHighlight: !1 };
        }
        renderDiff(e = this.renderCache?.diff, t = Wt) {
          if (e == null) return;
          let { expandUnchanged: n = !1, collapsedContextThreshold: r } =
              this.getOptionsWithDefaults(),
            { options: i, forceHighlight: a } = this.getRenderOptions(e),
            o = this.getMatchingWorkerResultCache(e, i);
          (o != null &&
            !this.hasHighlightedRenderCache(e, i) &&
            ((this.renderCache = {
              diff: e,
              highlighted: !0,
              renderRange: void 0,
              ...o,
            }),
            (a = !1)),
            (this.renderCache ??= {
              diff: e,
              highlighted: !1,
              options: i,
              result: void 0,
              renderRange: void 0,
            }));
          let s = e.additionLines.length > 0 || e.deletionLines.length > 0,
            c = !s || Gy(e) || bb(e, this.getTokenizeMaxLength()),
            l = !t_(e, this.renderCache.diff),
            u = !_m(this.renderCache.renderRange, t);
          if (this.workerManager?.isWorkingPool() === !0)
            ((c ||
              this.renderCache.result == null ||
              (!this.renderCache.highlighted && (l || u))) &&
              ((this.renderCache.diff = e),
              (this.renderCache.options = i),
              (this.renderCache.highlighted = !1),
              (this.renderCache.result == null || l || u || a) &&
                (this.renderCache.result = this.workerManager.getPlainDiffAST(
                  e,
                  t.startingLine,
                  t.totalLines,
                  hy(t) || n ? !0 : this.expandedHunks,
                  r,
                )),
              (this.renderCache.renderRange = t)),
              !c &&
                s &&
                (!this.renderCache.highlighted || a) &&
                this.workerManager.highlightDiffAST(this, e));
          else {
            this.computedLang = e.lang ?? Am(e.name);
            let t = this.highlighter != null && dm(i.theme),
              n = this.highlighter != null && da(this.computedLang),
              r = !c && n;
            if (
              this.highlighter != null &&
              t &&
              (a ||
                c ||
                (!this.renderCache.highlighted && r) ||
                this.renderCache.result == null)
            ) {
              let { result: t, options: i } = this.renderDiffWithHighlighter(
                e,
                this.highlighter,
                c || !n,
              );
              this.renderCache = {
                diff: e,
                options: i,
                highlighted: r,
                result: t,
                renderRange: void 0,
              };
            }
            (!t || (!c && !n)) &&
              this.asyncHighlight(e).then(({ result: t, options: n }) => {
                (this.renderCache != null &&
                  (this.renderCache.highlighted = !1),
                  this.onHighlightSuccess(e, t, n, !c));
              });
          }
          return this.renderCache.result == null
            ? void 0
            : this.processDiffResult(
                this.renderCache.diff,
                t,
                this.renderCache.result,
              );
        }
        async asyncRender(e, t = Wt) {
          let { result: n } = await this.asyncHighlight(e);
          return this.processDiffResult(e, t, n);
        }
        createPreElement(e, t, n) {
          let {
            diffIndicators: r,
            disableBackground: i,
            disableLineNumbers: a,
            overflow: o,
          } = this.getOptionsWithDefaults();
          return Dm({
            type: `diff`,
            diffIndicators: r,
            disableBackground: i,
            disableLineNumbers: a,
            overflow: o,
            split: e,
            totalLines: t,
            customProperties: n,
          });
        }
        async asyncHighlight(e) {
          let t = bb(e, this.getTokenizeMaxLength());
          this.computedLang = t ? `text` : (e.lang ?? Am(e.name));
          let n = this.highlighter != null && dm(this.options.theme ?? P),
            r = t || (this.highlighter != null && da(this.computedLang));
          return (
            (this.highlighter == null || !n || !r) &&
              (this.highlighter = await this.initializeHighlighter()),
            this.renderDiffWithHighlighter(e, this.highlighter, t)
          );
        }
        renderDiffWithHighlighter(e, t, n = !1) {
          let { options: r } = this.getRenderOptions(e),
            { collapsedContextThreshold: i } = this.getOptionsWithDefaults();
          return {
            result: zy(e, t, r, {
              forcePlainText: n,
              expandedHunks: n ? !0 : void 0,
              collapsedContextThreshold: i,
            }),
            options: r,
          };
        }
        onHighlightSuccess(e, t, n, r = !0) {
          if (this.renderCache == null) return;
          let i =
            !this.renderCache.highlighted ||
            !ny(this.renderCache.options, n) ||
            !t_(this.renderCache.diff, e);
          ((this.renderCache = {
            diff: e,
            options: n,
            highlighted: r,
            result: t,
            renderRange: void 0,
          }),
            i && this.onRenderUpdate?.());
        }
        getMatchingWorkerResultCache(e, t) {
          let n = this.workerManager?.getDiffResultCache(e);
          if (!(n == null || !ny(t, n.options))) return n;
        }
        hasHighlightedRenderCache(e, t) {
          let { renderCache: n } = this;
          return (
            n?.result != null &&
            n.highlighted &&
            t_(e, n.diff) &&
            ny(t, n.options)
          );
        }
        onHighlightError(e) {
          console.error(e);
        }
        getTokenizeMaxLength() {
          return this.options.tokenizeMaxLength ?? 1e5;
        }
        processDiffResult(e, t, { code: n, themeStyles: r, baseThemeType: i }) {
          let {
              diffStyle: a,
              disableFileHeader: o,
              expandUnchanged: s,
              expansionLineCount: c,
              collapsedContextThreshold: l,
              hunkSeparators: u,
            } = this.getOptionsWithDefaults(),
            d = this.renderCache?.isDirty ?? !1;
          this.diff = e;
          let f = a === `unified`,
            p = xb(e, this.options.loadDiffFiles != null),
            m = !e.isPartial || p,
            h = [],
            g = [],
            _ = [],
            v = [],
            { additionLines: y, deletionLines: b } = n,
            x = {
              rowCount: 0,
              hunkSeparators: u,
              additionsContentAST: h,
              deletionsContentAST: g,
              unifiedContentAST: _,
              unifiedGutterAST: _n(),
              deletionsGutterAST: _n(),
              additionsGutterAST: _n(),
              expansionLineCount: c,
              hunkData: v,
              incrementRowCount(e = 1) {
                x.rowCount += e;
              },
              pushToGutter(e, t) {
                switch (e) {
                  case `unified`:
                    x.unifiedGutterAST.children.push(t);
                    break;
                  case `deletions`:
                    x.deletionsGutterAST.children.push(t);
                    break;
                  case `additions`:
                    x.additionsGutterAST.children.push(t);
                    break;
                }
              },
            },
            S = Sy({
              fileDiff: e,
              errorPrefix: `DiffHunksRenderer.processDiffResult`,
            }),
            C = {
              size: 0,
              side: void 0,
              increment() {
                this.size += 1;
              },
              flush() {
                if (a !== `unified`) {
                  if (this.size <= 0 || this.side == null) {
                    ((this.side = void 0), (this.size = 0));
                    return;
                  }
                  (this.side === `additions`
                    ? (x.pushToGutter(
                        `additions`,
                        yn(void 0, `buffer`, this.size),
                      ),
                      h?.push(iy(this.size)))
                    : (x.pushToGutter(
                        `deletions`,
                        yn(void 0, `buffer`, this.size),
                      ),
                      g?.push(iy(this.size))),
                    (this.size = 0),
                    (this.side = void 0));
                }
              },
            },
            ee = (e, t, n, r, i) => {
              x.pushToGutter(e, vn(t, n, r, i));
            };
          function w(e) {
            (C.flush(),
              a === `unified`
                ? gb(`unified`, e, x)
                : (gb(`deletions`, e, x), gb(`additions`, e, x)));
          }
          (this.pushFileLevelAnnotations(e, a, t, x),
            jy({
              diff: e,
              diffStyle: a,
              startingLine: t.startingLine,
              totalLines: t.totalLines,
              expandedHunks: s ? !0 : this.expandedHunks,
              collapsedContextThreshold: l,
              callback: ({
                hunkIndex: t,
                hunk: n,
                collapsedBefore: r,
                collapsedAfter: i,
                additionLine: o,
                deletionLine: s,
                type: c,
              }) => {
                let l = s == null ? o.splitLineIndex : s.splitLineIndex,
                  f = o == null ? s.unifiedLineIndex : o.unifiedLineIndex;
                (a === `split` && c !== `change` && C.flush(),
                  r > 0 &&
                    w({
                      hunkIndex: t,
                      collapsedLines: r,
                      rangeSize: Math.max(n?.collapsedBefore ?? 0, 0),
                      hunkSpecs: n?.hunkSpecs,
                      isFirstHunk: t === 0,
                      isLastHunk: !1,
                      isExpandable: m,
                    }));
                let h = a === `unified` ? f : l,
                  g = {
                    type: c,
                    hunkIndex: t,
                    lineIndex: h,
                    unifiedLineIndex: f,
                    splitLineIndex: l,
                    deletionLine: s,
                    additionLine: o,
                  };
                if (a === `unified`) {
                  let n = this.getUnifiedInjectedRowsForLine?.(g);
                  n?.before != null && pb(n.before, x);
                  let r = s == null ? void 0 : b[s.lineIndex],
                    i = o == null ? void 0 : y[o.lineIndex];
                  if (r == null && i == null) {
                    let t = `DiffHunksRenderer.processDiffResult: deletionLine and additionLine are null, something is wrong`;
                    throw (console.error(t, { file: e.name }), Error(t));
                  }
                  let a =
                      c === `change`
                        ? o == null
                          ? `change-deletion`
                          : `change-addition`
                        : c,
                    u = this.getUnifiedLineDecoration({
                      type: c,
                      lineType: a,
                      additionLineIndex: o?.lineIndex,
                      deletionLineIndex: s?.lineIndex,
                    });
                  (ee(
                    `unified`,
                    u.gutterLineType,
                    o == null ? s.lineNumber : o.lineNumber,
                    `${f},${l}`,
                    u.gutterProperties,
                  ),
                    i == null
                      ? r != null &&
                        (r = _b(
                          r,
                          u.contentProperties,
                          d && s != null
                            ? {
                                "data-line": s.lineNumber,
                                "data-line-index": `${f},${l}`,
                              }
                            : void 0,
                        ))
                      : (i = _b(
                          i,
                          u.contentProperties,
                          d && o != null
                            ? {
                                "data-line": o.lineNumber,
                                "data-line-index": `${f},${l}`,
                              }
                            : void 0,
                        )),
                    hb({
                      diffStyle: `unified`,
                      type: c,
                      deletionLine: r,
                      additionLine: i,
                      unifiedSpan: this.getAnnotations(
                        `unified`,
                        s?.lineNumber,
                        o?.lineNumber,
                        t,
                        h,
                      ),
                      createAnnotationElement: (e) =>
                        this.createAnnotationElement(e),
                      context: x,
                    }),
                    n?.after != null && pb(n.after, x));
                } else {
                  let n = this.getSplitInjectedRowsForLine?.(g);
                  n?.before != null && mb(n.before, x, C);
                  let r = s == null ? void 0 : b[s.lineIndex],
                    i = o == null ? void 0 : y[o.lineIndex],
                    a = this.getSplitLineDecoration({
                      side: `deletions`,
                      type: c,
                      lineIndex: s?.lineIndex,
                    }),
                    u = this.getSplitLineDecoration({
                      side: `additions`,
                      type: c,
                      lineIndex: o?.lineIndex,
                    });
                  if (r == null && i == null) {
                    let t = `DiffHunksRenderer.processDiffResult: deletionLine and additionLine are null, something is wrong`;
                    throw (console.error(t, { file: e.name }), Error(t));
                  }
                  let f = (() => {
                    if (c === `change`) {
                      if (i == null) return `additions`;
                      if (r == null) return `deletions`;
                    }
                  })();
                  if (f != null) {
                    if (C.side != null && C.side !== f)
                      throw Error(
                        `DiffHunksRenderer.processDiffResult: iterateOverDiff, invalid pending splits`,
                      );
                    ((C.side = f), C.increment());
                  }
                  let p = this.getAnnotations(
                    `split`,
                    s?.lineNumber,
                    o?.lineNumber,
                    t,
                    h,
                  );
                  if ((p != null && C.size > 0 && C.flush(), s != null)) {
                    let e = _b(
                      r,
                      a.contentProperties,
                      d
                        ? {
                            "data-line": s.lineNumber,
                            "data-line-index": `${s.unifiedLineIndex},${l}`,
                          }
                        : void 0,
                    );
                    (ee(
                      `deletions`,
                      a.gutterLineType,
                      s.lineNumber,
                      `${s.unifiedLineIndex},${l}`,
                      a.gutterProperties,
                    ),
                      e != null && (r = e));
                  }
                  if (o != null) {
                    let e = _b(
                      i,
                      u.contentProperties,
                      d
                        ? {
                            "data-line": o.lineNumber,
                            "data-line-index": `${o.unifiedLineIndex},${l}`,
                          }
                        : void 0,
                    );
                    (ee(
                      `additions`,
                      u.gutterLineType,
                      o.lineNumber,
                      `${o.unifiedLineIndex},${l}`,
                      u.gutterProperties,
                    ),
                      e != null && (i = e));
                  }
                  (hb({
                    diffStyle: `split`,
                    type: c,
                    additionLine: i,
                    deletionLine: r,
                    ...p,
                    createAnnotationElement: (e) =>
                      this.createAnnotationElement(e),
                    context: x,
                  }),
                    n?.after != null && mb(n.after, x, C));
                }
                let _ =
                    a === `split` &&
                    n != null &&
                    l === n.splitLineStart + n.splitLineCount - 1,
                  v =
                    t === e.hunks.length - 1 &&
                    n != null &&
                    (a === `split`
                      ? l === n.splitLineStart + n.splitLineCount - 1
                      : f === n.unifiedLineStart + n.unifiedLineCount - 1),
                  T = _ ? n.noEOFCRDeletions : !1,
                  te = _ ? n.noEOFCRAdditions : !1,
                  E = (s?.noEOFCR ?? !1) || T,
                  D = (o?.noEOFCR ?? !1) || te;
                if (D || E) {
                  if ((a === `split` && C.flush(), E)) {
                    let e =
                      c === `context` || c === `context-expanded`
                        ? c
                        : `change-deletion`;
                    a === `unified`
                      ? (x.unifiedContentAST.push(oy(e)),
                        x.pushToGutter(`unified`, yn(e, `metadata`, 1)))
                      : (x.deletionsContentAST.push(oy(e)),
                        x.pushToGutter(`deletions`, yn(e, `metadata`, 1)),
                        D ||
                          (x.pushToGutter(`additions`, yn(void 0, `buffer`, 1)),
                          x.additionsContentAST.push(iy(1))));
                  }
                  if (D) {
                    let e =
                      c === `context` || c === `context-expanded`
                        ? c
                        : `change-addition`;
                    a === `unified`
                      ? (x.unifiedContentAST.push(oy(e)),
                        x.pushToGutter(`unified`, yn(e, `metadata`, 1)))
                      : (x.additionsContentAST.push(oy(e)),
                        x.pushToGutter(`additions`, yn(e, `metadata`, 1)),
                        E ||
                          (x.pushToGutter(`deletions`, yn(void 0, `buffer`, 1)),
                          x.deletionsContentAST.push(iy(1))));
                  }
                  x.incrementRowCount(1);
                }
                (u !== `simple` &&
                  u !== `metadata` &&
                  (i > 0 || (v && p)) &&
                  w({
                    hunkIndex: c === `context-expanded` ? t : t + 1,
                    collapsedLines: v && p ? `unknown` : i,
                    rangeSize: S,
                    hunkSpecs: void 0,
                    isFirstHunk: !1,
                    isLastHunk: !0,
                    isExpandable: m,
                  }),
                  x.incrementRowCount(1));
              },
            }),
            a === `split` && C.flush());
          let T = Math.max(
              py(e.hunks),
              e.additionLines.length ?? 0,
              e.deletionLines.length ?? 0,
            ),
            te = t.bufferBefore > 0 || t.bufferAfter > 0,
            E = !f && e.type !== `deleted`,
            D = !f && e.type !== `new`,
            ne = x.rowCount > 0 || te;
          ((h = E && ne ? h : void 0),
            (g = D && ne ? g : void 0),
            (_ = f && ne ? _ : void 0));
          let O = this.createPreElement(g != null && h != null, T);
          return {
            unifiedGutterAST: f && ne ? x.unifiedGutterAST.children : void 0,
            unifiedContentAST: _,
            deletionsGutterAST:
              D && ne ? x.deletionsGutterAST.children : void 0,
            deletionsContentAST: g,
            additionsGutterAST:
              E && ne ? x.additionsGutterAST.children : void 0,
            additionsContentAST: h,
            hunkData: v,
            preNode: O,
            themeStyles: r,
            baseThemeType: i,
            headerElement: o ? void 0 : this.renderHeader(this.diff),
            totalLines: T,
            rowCount: x.rowCount,
            bufferBefore: t.bufferBefore,
            bufferAfter: t.bufferAfter,
            css: ``,
          };
        }
        renderCodeAST(e, t) {
          let n =
              e === `unified`
                ? t.unifiedGutterAST
                : e === `deletions`
                  ? t.deletionsGutterAST
                  : t.additionsGutterAST,
            r =
              e === `unified`
                ? t.unifiedContentAST
                : e === `deletions`
                  ? t.deletionsContentAST
                  : t.additionsContentAST;
          if (n == null || r == null) return;
          let i = _n(n);
          return (
            (i.properties.style = `grid-row: span ${t.rowCount}`),
            [i, Bh(r, t.rowCount)]
          );
        }
        renderFullAST(e, t = []) {
          let n = this.getOptionsWithDefaults().hunkSeparators === `line-info`,
            r = this.renderCodeAST(`unified`, e);
          if (r != null)
            return (
              t.push(
                I({
                  tagName: `code`,
                  children: r,
                  properties: {
                    "data-code": ``,
                    "data-container-size": n ? `` : void 0,
                    "data-unified": ``,
                  },
                }),
              ),
              { ...e.preNode, children: t }
            );
          let i = this.renderCodeAST(`deletions`, e);
          i != null &&
            t.push(
              I({
                tagName: `code`,
                children: i,
                properties: {
                  "data-code": ``,
                  "data-container-size": n ? `` : void 0,
                  "data-deletions": ``,
                },
              }),
            );
          let a = this.renderCodeAST(`additions`, e);
          return (
            a != null &&
              t.push(
                I({
                  tagName: `code`,
                  children: a,
                  properties: {
                    "data-code": ``,
                    "data-container-size": n ? `` : void 0,
                    "data-additions": ``,
                  },
                }),
              ),
            { ...e.preNode, children: t }
          );
        }
        renderFullHTML(e, t = []) {
          return Ei(this.renderFullAST(e, t));
        }
        renderPartialHTML(e, t) {
          return Ei(
            t == null
              ? e
              : I({
                  tagName: `code`,
                  children: e,
                  properties: {
                    "data-code": ``,
                    "data-container-size":
                      this.getOptionsWithDefaults().hunkSeparators ===
                      `line-info`
                        ? ``
                        : void 0,
                    [`data-${t}`]: ``,
                  },
                }),
          );
        }
        pushFileLevelAnnotations(e, t, n, r) {
          if (!Lh(n)) return;
          let i = e.type === `new` ? [] : db(Ih(this.deletionAnnotations)),
            a = e.type === `deleted` ? [] : db(Ih(this.additionAnnotations));
          if (i.length === 0 && a.length === 0) return;
          let { createAnnotationElement: o } = this;
          if (t === `unified`) {
            hb({
              diffStyle: t,
              type: `context`,
              unifiedSpan: {
                type: `annotation`,
                hunkIndex: -1,
                lineIndex: -1,
                annotations: i.concat(a),
              },
              createAnnotationElement: o,
              context: r,
            });
            return;
          }
          hb({
            diffStyle: t,
            type: `context`,
            deletionSpan: {
              type: `annotation`,
              hunkIndex: -1,
              lineIndex: -1,
              annotations: i,
            },
            additionSpan: {
              type: `annotation`,
              hunkIndex: -1,
              lineIndex: -1,
              annotations: a,
            },
            createAnnotationElement: o,
            context: r,
          });
        }
        getAnnotations(e, t, n, r, i) {
          let a = {
            type: `annotation`,
            hunkIndex: r,
            lineIndex: i,
            annotations: [],
          };
          if (t != null)
            for (let e of this.deletionAnnotations[t] ?? [])
              a.annotations.push(zm(e));
          let o = {
            type: `annotation`,
            hunkIndex: r,
            lineIndex: i,
            annotations: [],
          };
          if (n != null)
            for (let t of this.additionAnnotations[n] ?? [])
              (e === `unified` ? a : o).annotations.push(zm(t));
          if (e === `unified`) return a.annotations.length > 0 ? a : void 0;
          if (!(o.annotations.length === 0 && a.annotations.length === 0))
            return { deletionSpan: a, additionSpan: o };
        }
        renderHeader(e) {
          let { headerRenderMode: t, stickyHeader: n } =
            this.getOptionsWithDefaults();
          return Cm({ fileOrDiff: e, mode: t, stickyHeader: n });
        }
      }),
      (wb = new Intl.PluralRules(`en-US`)));
  });
function Eb(e, t) {
  return (
    e.lineNumber === t.lineNumber &&
    e.side === t.side &&
    e.metadata === t.metadata
  );
}
var Db = e(() => {});
function Ob(e, t) {
  return (
    e.slotName === t.slotName &&
    e.hunkIndex === t.hunkIndex &&
    e.lines === t.lines &&
    e.lineCountKnown === t.lineCountKnown &&
    e.type === t.type &&
    e.expandable?.chunked === t.expandable?.chunked &&
    e.expandable?.up === t.expandable?.up &&
    e.expandable?.down === t.expandable?.down
  );
}
var kb = e(() => {});
async function Ab(e, t = 300) {
  let n;
  try {
    await Promise.race([
      e(),
      new Promise((e) => {
        n = setTimeout(e, t);
      }),
    ]);
  } finally {
    n != null && clearTimeout(n);
  }
}
var jb = e(() => {});
function Mb({ oldFile: e, newFile: t }, n) {
  if (!(e === void 0 && t === void 0)) {
    if (e === void 0 || t === void 0)
      throw Error(
        `${n}: Pass null for an intentionally missing oldFile or newFile side`,
      );
    if (e === null) {
      if (t === null)
        throw Error(`${n}: You must pass oldFile, newFile, or both`);
      return { oldFile: e, newFile: t };
    }
    return { oldFile: e, newFile: t };
  }
}
var Nb = e(() => {});
function Pb(e) {
  return {
    theme: e?.theme,
    disableLineNumbers: e?.disableLineNumbers,
    overflow: e?.overflow,
    collapsed: e?.collapsed,
    disableFileHeader: e?.disableFileHeader,
    disableVirtualizationBuffers: e?.disableVirtualizationBuffers,
    stickyHeader: e?.stickyHeader,
    preferredHighlighter: e?.preferredHighlighter,
    useCSSClasses: e?.useCSSClasses,
    useTokenTransformer: e?.useTokenTransformer,
    tokenizeMaxLineLength: e?.tokenizeMaxLineLength,
    tokenizeMaxLength: e?.tokenizeMaxLength,
    diffStyle: e?.diffStyle,
    diffIndicators: e?.diffIndicators,
    disableBackground: e?.disableBackground,
    hunkSeparators:
      typeof e?.hunkSeparators == `function` ? `custom` : e?.hunkSeparators,
    expandUnchanged: e?.expandUnchanged,
    loadDiffFiles: e?.loadDiffFiles,
    collapsedContextThreshold: e?.collapsedContextThreshold,
    lineDiffType: e?.lineDiffType,
    maxLineDiffLength: e?.maxLineDiffLength,
    expansionLineCount: e?.expansionLineCount,
    headerRenderMode: e?.renderCustomHeader == null ? `default` : `custom`,
  };
}
var Fb = e(() => {});
function Ib(e) {
  return (
    e.isPartial &&
    (e.type === `change` ||
      e.type === `rename-changed` ||
      e.type === `rename-pure`)
  );
}
function Lb(e, t) {
  return e == null || t == null ? e == null && t == null : fn(e, t);
}
function Rb({ fileDiff: e, oldFile: t, newFile: n }) {
  return (e != null && e.hunks.length > 0) || t != null || n != null;
}
function zb({ fileDiff: e, oldFile: t, newFile: n }) {
  return e != null || t != null || n != null;
}
function Bb(e, t, n = !1) {
  return !n && e == null && t;
}
function Vb(e, t, n = !1) {
  return e == null && t && !n;
}
function Hb(e) {
  if (!(e == null || e.type !== `element`)) return e.children ?? [];
}
var Ub,
  Wb,
  Gb = e(() => {
    (F(),
      tn(),
      Ag(),
      pn(),
      $i(),
      aa(),
      vm(),
      Bm(),
      Zh(),
      rg(),
      ag(),
      sg(),
      lg(),
      hg(),
      Sg(),
      wg(),
      Fg(),
      Eg(),
      Og(),
      Ig(),
      jb(),
      Nb(),
      g_(),
      $v(),
      ty(),
      Ky(),
      Tb(),
      Db(),
      kb(),
      Fb(),
      Ni(),
      (Ub = -1),
      (Wb = class {
        options;
        workerManager;
        isContainerManaged;
        static LoadedCustomComponent = !0;
        __id = `file-diff:${++Ub}`;
        type = `file-diff`;
        fileContainer;
        spriteSVG;
        pre;
        codeUnified;
        codeDeletions;
        codeAdditions;
        bufferBefore;
        bufferAfter;
        themeCSSStyle;
        appliedThemeCSS;
        hasAdoptedThemeCSS = !1;
        unsafeCSSStyle;
        appliedUnsafeCSS;
        gutterUtilityContent;
        headerElement;
        headerPrefix;
        headerFilenameSuffix;
        headerMetadata;
        headerCustom;
        separatorCache = new Map();
        errorWrapper;
        placeHolder;
        hunksRenderer;
        resizeManager;
        scrollSyncManager;
        interactionManager;
        annotationCache = new Map();
        lineAnnotations = [];
        managersDirty = !1;
        deletionFile;
        additionFile;
        fileDiff;
        renderRange;
        pendingFiles;
        appliedPreAttributes;
        lastRenderedHeaderHTML;
        cachedHeaderHTML;
        lastRowCount;
        mounted = !1;
        enabled = !0;
        editor;
        refreshViewTimeout;
        deferSetSelection;
        constructor(e = { theme: P }, t, n = !1) {
          ((this.options = e),
            (this.workerManager = t),
            (this.isContainerManaged = n),
            (this.hunksRenderer = this.createHunksRenderer(e)),
            (this.resizeManager = new ia()),
            (this.scrollSyncManager = new ey()),
            (this.interactionManager = new Qi(
              `diff`,
              Pi(
                e,
                typeof e.hunkSeparators == `function` ||
                  (e.hunkSeparators ?? `line-info`) === `line-info` ||
                  e.hunkSeparators === `line-info-basic`
                  ? this.handleExpandHunk
                  : void 0,
                this.getLineIndex,
              ),
            )),
            this.workerManager?.subscribeToThemeChanges(this),
            (this.enabled = !0));
        }
        handleHighlightRender = () => {
          this.rerender();
        };
        getHunksRendererOptions(e) {
          return Pb(e);
        }
        createHunksRenderer(e) {
          return new Cb(
            this.getHunksRendererOptions(e),
            this.handleHighlightRender,
            this.workerManager,
          );
        }
        getLineIndex = (e, t = `additions`) => {
          let n = this.hunksRenderer.getDiffCache() ?? this.fileDiff;
          if (n == null) return;
          let r = n.hunks.at(-1),
            i,
            a;
          hunkIterator: for (let o of n.hunks) {
            let n = t === `deletions` ? o.deletionStart : o.additionStart,
              s = t === `deletions` ? o.deletionCount : o.additionCount,
              c = o.splitLineStart,
              l = o.unifiedLineStart;
            if (e < n) {
              let t = n - e;
              ((i = Math.max(l - t, 0)), (a = Math.max(c - t, 0)));
              break hunkIterator;
            }
            if (e >= n + s) {
              if (o === r) {
                let t = e - (n + s);
                ((i = l + o.unifiedLineCount + t),
                  (a = c + o.splitLineCount + t));
                break hunkIterator;
              }
              continue;
            }
            for (let r of o.hunkContent)
              if (r.type === `context`)
                if (e < n + r.lines) {
                  let t = e - n;
                  ((a = c + t), (i = l + t));
                  break hunkIterator;
                } else ((n += r.lines), (c += r.lines), (l += r.lines));
              else {
                let o = t === `deletions` ? r.deletions : r.additions;
                if (e < n + o) {
                  let o = e - n;
                  ((i = l + (t === `additions` ? r.deletions : 0) + o),
                    (a = c + o));
                  break hunkIterator;
                } else
                  ((n += o),
                    (c += Math.max(r.deletions, r.additions)),
                    (l += r.deletions + r.additions));
              }
            break hunkIterator;
          }
          if (!(i == null || a == null)) return [i, a];
        };
        setOptions(e) {
          e != null &&
            ((this.options = e),
            (this.cachedHeaderHTML = void 0),
            this.hunksRenderer.setOptions(this.getHunksRendererOptions(e)),
            this.syncInteractionOptions());
        }
        syncInteractionOptions() {
          this.interactionManager.setOptions(
            Pi(
              this.options,
              typeof this.options.hunkSeparators == `function` ||
                (this.options.hunkSeparators ?? `line-info`) === `line-info` ||
                this.options.hunkSeparators === `line-info-basic`
                ? this.handleExpandHunk
                : void 0,
              this.getLineIndex,
            ),
          );
        }
        mergeOptions(e) {
          this.options = { ...this.options, ...e };
        }
        setThemeType(e) {
          (this.options.themeType ?? `system`) !== e &&
            (this.mergeOptions({ themeType: e }),
            this.applyCachedThemeState(e));
        }
        applyCachedThemeState(e) {
          if (
            typeof this.options.theme == `string` ||
            this.fileContainer == null ||
            this.appliedThemeCSS == null
          )
            return !1;
          let t = this.appliedThemeCSS.baseThemeType ?? e;
          return this.appliedThemeCSS.themeType === t
            ? !1
            : (this.applyThemeState(
                this.fileContainer,
                this.appliedThemeCSS.themeStyles,
                e,
                this.appliedThemeCSS.baseThemeType,
              ),
              !0);
        }
        hasThemeChanged() {
          return (
            this.appliedThemeCSS != null &&
            !en(this.appliedThemeCSS.theme, this.options.theme ?? P)
          );
        }
        getHoveredLine = () => this.interactionManager.getHoveredLine();
        setLineAnnotations(e) {
          this.lineAnnotations = e;
        }
        canPartiallyRender(e, t, n) {
          return !(
            e ||
            t ||
            n ||
            typeof this.options.hunkSeparators == `function`
          );
        }
        setSelectedLines(e, t) {
          this.refreshViewTimeout == null
            ? this.interactionManager.setSelection(e, t)
            : (this.deferSetSelection = [e, t]);
        }
        flushManagers() {
          if (!this.managersDirty || this.pre == null) {
            this.managersDirty = !1;
            return;
          }
          let { diffStyle: e = `split`, overflow: t = `scroll` } = this.options;
          (this.interactionManager.setup(this.pre),
            this.resizeManager.setup(this.pre, t === `wrap`),
            t === `scroll` && e === `split`
              ? this.scrollSyncManager.setup(
                  this.pre,
                  this.codeDeletions,
                  this.codeAdditions,
                )
              : this.scrollSyncManager.cleanUp(),
            (this.managersDirty = !1));
        }
        cleanUp(e = !1) {
          (this.emitPostRender(!0),
            this.resizeManager.cleanUp(),
            this.interactionManager.cleanUp(),
            this.scrollSyncManager.cleanUp(),
            (this.managersDirty = !1),
            this.workerManager?.unsubscribeToThemeChanges(this),
            (this.renderRange = void 0),
            (this.pendingFiles = void 0),
            this.isContainerManaged || this.fileContainer?.remove(),
            (this.fileContainer = void 0),
            (this.mounted = !1),
            e || (this.lineAnnotations = []),
            this.clearAuxiliaryNodes(),
            this.annotationCache.clear(),
            (this.pre = void 0),
            (this.codeUnified = void 0),
            (this.codeDeletions = void 0),
            (this.codeAdditions = void 0),
            this.bufferBefore?.remove(),
            (this.bufferBefore = void 0),
            this.bufferAfter?.remove(),
            (this.bufferAfter = void 0),
            (this.appliedPreAttributes = void 0),
            (this.headerElement = void 0),
            (this.headerPrefix = void 0),
            (this.headerFilenameSuffix = void 0),
            (this.headerMetadata = void 0),
            (this.headerCustom = void 0),
            (this.placeHolder = void 0),
            (this.lastRenderedHeaderHTML = void 0),
            e || (this.cachedHeaderHTML = void 0),
            (this.errorWrapper = void 0),
            (this.spriteSVG = void 0),
            (this.lastRowCount = void 0),
            (this.themeCSSStyle = void 0),
            (this.appliedThemeCSS = void 0),
            (this.hasAdoptedThemeCSS = !1),
            (this.unsafeCSSStyle = void 0),
            (this.appliedUnsafeCSS = void 0),
            e
              ? this.hunksRenderer.recycle()
              : (this.hunksRenderer.cleanUp(),
                (this.workerManager = void 0),
                (this.fileDiff = void 0),
                (this.deletionFile = void 0),
                (this.additionFile = void 0)),
            (this.enabled = !1),
            this.editor?.cleanUp(),
            (this.editor = void 0),
            this.refreshViewTimeout != null &&
              (clearTimeout(this.refreshViewTimeout),
              (this.refreshViewTimeout = void 0)));
        }
        virtualizedSetup() {
          ((this.enabled = !0),
            this.workerManager?.subscribeToThemeChanges(this));
        }
        hydrate({
          fileContainer: e,
          prerenderedHTML: t,
          preventEmit: n = !1,
          lineAnnotations: r,
          fileDiff: i,
          ...a
        }) {
          if (!this.enabled)
            throw Error(
              `FileDiff.hydrate: attempting to call hydrate after cleaned up`,
            );
          if (this.fileContainer != null)
            throw Error(
              `FileDiff.hydrate: hydrate can only be called before the instance has rendered or hydrated`,
            );
          let o = Mb(a, `FileDiff.hydrate`),
            s = o?.oldFile,
            c = o?.newFile;
          (this.hydrateElements(e, t),
            Bb(
              this.pre,
              Rb({ fileDiff: i, oldFile: s, newFile: c }),
              this.options.collapsed,
            ) ||
            Vb(
              this.headerElement,
              zb({ fileDiff: i, oldFile: s, newFile: c }),
              this.options.disableFileHeader,
            )
              ? this.render({
                  ...a,
                  fileContainer: e,
                  lineAnnotations: r,
                  fileDiff: i,
                  preventEmit: !0,
                })
              : this.hydrationSetup({ fileDiff: i, lineAnnotations: r, ...o }),
            n || this.emitPostRender());
        }
        hydrateElements(e, t) {
          (this.fileContainer !== e && this.emitPostRender(!0), Tg(e, t));
          for (let t of e.shadowRoot?.children ?? []) {
            if (t instanceof SVGElement) {
              this.spriteSVG = t;
              continue;
            }
            if (t instanceof HTMLElement) {
              if (t instanceof HTMLPreElement) {
                this.pre = t;
                for (let e of t.children)
                  !(e instanceof HTMLElement) ||
                    e.tagName.toLowerCase() !== `code` ||
                    (`deletions` in e.dataset && (this.codeDeletions = e),
                    `additions` in e.dataset && (this.codeAdditions = e),
                    `unified` in e.dataset && (this.codeUnified = e));
                continue;
              }
              if (`diffsHeader` in t.dataset) {
                this.headerElement = t;
                continue;
              }
              if (
                t instanceof HTMLStyleElement &&
                t.hasAttribute(`data-theme-css`)
              ) {
                this.themeCSSStyle = t;
                continue;
              }
              if (
                t instanceof HTMLStyleElement &&
                t.hasAttribute(`data-unsafe-css`)
              ) {
                ((this.unsafeCSSStyle = t),
                  (this.appliedUnsafeCSS = t.textContent));
                continue;
              }
            }
          }
          (this.pre != null &&
            (this.syncCodeNodesFromPre(this.pre),
            this.pre.removeAttribute(`data-dehydrated`)),
            (this.fileContainer = e),
            this.hydrateMeasuredScrollbar());
        }
        hydrationSetup({
          fileDiff: e,
          oldFile: t,
          newFile: n,
          lineAnnotations: r,
        }) {
          ((this.lineAnnotations = r ?? this.lineAnnotations),
            (this.additionFile = n),
            (this.deletionFile = t),
            (this.fileDiff =
              e ??
              (t !== void 0 && n !== void 0
                ? Xv(t, n, this.options.parseDiffOptions)
                : void 0)),
            this.pre != null &&
              (this.syncInteractionOptions(),
              this.hunksRenderer.hydrate(this.fileDiff),
              this.renderAnnotations(),
              this.renderGutterUtility(),
              this.injectUnsafeCSS(),
              (this.managersDirty = !0),
              this.flushManagers()));
        }
        rerender() {
          !this.enabled ||
            (this.fileDiff == null &&
              this.additionFile == null &&
              this.deletionFile == null) ||
            this.render({ forceRender: !0, renderRange: this.renderRange });
        }
        onThemeChange() {
          (this.hunksRenderer.clearRenderCache(), this.rerender());
        }
        handleExpandHunk = (e, t, n) => {
          this.expandHunk(e, t, n);
        };
        expandHunk = (e, t, n) => {
          (this.hunksRenderer.expandHunk(e, t, n),
            this.loadFilesIfNecessary(),
            this.rerender());
        };
        loadFilesIfNecessary() {
          let {
            fileDiff: e,
            options: { loadDiffFiles: t },
          } = this;
          e == null ||
            t == null ||
            !Ib(e) ||
            this.pendingFiles?.fileDiff === e ||
            (this.pendingFiles = {
              fileDiff: e,
              promise: this.loadFilesForDiff(e, t),
            });
        }
        async loadFilesForDiff(e, t) {
          try {
            let n = await t(e);
            if (!this.enabled || this.fileDiff !== e) return;
            await this.handleFilesLoaded(e, n);
          } catch (e) {
            if (this.options.disableErrorHandling === !0) throw e;
            console.error(e);
          } finally {
            this.pendingFiles?.fileDiff === e && (this.pendingFiles = void 0);
          }
        }
        async handleFilesLoaded(e, t) {
          this.fileDiff !== e ||
            !e.isPartial ||
            (s_(`merge`, e, t),
            this.setHydratedState(t),
            await Ab(() => this.primeHighlightCache(e)),
            !(!this.enabled || this.fileDiff !== e) && this.rerender());
        }
        setHydratedState(e) {
          ((this.deletionFile = e.oldFile),
            (this.additionFile = e.newFile),
            this.workerManager?.cleanUpTasks(this.hunksRenderer),
            this.hunksRenderer.clearRenderCache());
        }
        render({
          fileDiff: e,
          deferManagers: t = !1,
          forceRender: n = !1,
          preventEmit: r = !1,
          lineAnnotations: i,
          fileContainer: a,
          containerWrapper: o,
          renderRange: s,
          ...c
        }) {
          let l = Mb(c, `FileDiff.render`),
            u = l?.oldFile,
            d = l?.newFile;
          if (!this.enabled)
            throw Error(
              `FileDiff.render: attempting to call render after cleaned up`,
            );
          (e != null &&
            e.cacheKey === void 0 &&
            (e.cacheKey =
              e.prevName == null ? e.name : e.prevName + `:` + e.name),
            this.editor?.__postponeBgTokenizeToNextFrame());
          let {
              collapsed: f = !1,
              themeType: p = `system`,
              expandUnchanged: m = !1,
            } = this.options,
            h = f ? void 0 : s,
            g = this.hasThemeChanged(),
            _ = l != null,
            v = _ && (!Lb(u, this.deletionFile) || !Lb(d, this.additionFile)),
            y = e != null && e !== this.fileDiff,
            b =
              i != null && (i.length > 0 || this.lineAnnotations.length > 0)
                ? i !== this.lineAnnotations
                : !1;
          if (
            !f &&
            _m(h, this.renderRange) &&
            !n &&
            !b &&
            !g &&
            ((e != null && e === this.fileDiff) || (e == null && !v))
          )
            return this.applyCachedThemeState(p);
          let x;
          e == null &&
            _ &&
            (v || this.fileDiff == null) &&
            (x = Xv(l.oldFile, l.newFile, this.options.parseDiffOptions));
          let { renderRange: S } = this;
          if (
            ((this.renderRange = h),
            (this.deletionFile = u),
            (this.additionFile = d),
            e == null
              ? x != null && ((y = !0), (this.fileDiff = x))
              : (this.fileDiff = e),
            y && (this.cachedHeaderHTML = void 0),
            i != null && this.setLineAnnotations(i),
            this.fileDiff == null)
          )
            return !1;
          (m && this.loadFilesIfNecessary(),
            this.hunksRenderer.setOptions(
              this.getHunksRendererOptions(this.options),
            ),
            this.syncInteractionOptions(),
            this.hunksRenderer.setLineAnnotations(this.lineAnnotations));
          let { disableErrorHandling: C = !1, disableFileHeader: ee = !1 } =
            this.options;
          if (
            (ee &&
              (this.headerElement != null &&
                (this.headerElement.remove(),
                (this.headerElement = void 0),
                (this.lastRenderedHeaderHTML = void 0)),
              this.clearHeaderSlots()),
            (a = this.getOrCreateFileContainer(a, o)),
            this.applyCachedThemeState(p),
            f)
          ) {
            (this.removeRenderedCode(), this.clearAuxiliaryNodes());
            try {
              let e = this.hunksRenderer.renderDiff(this.fileDiff, Gt);
              (e != null &&
                this.applyThemeState(a, e.themeStyles, p, e.baseThemeType),
                e?.headerElement != null &&
                  this.applyHeaderToDOM(e.headerElement, a),
                this.placeHolder?.remove(),
                (this.placeHolder = void 0),
                this.renderSeparators([]),
                this.injectUnsafeCSS());
            } catch (e) {
              if (C) throw e;
              (console.error(e),
                e instanceof Error && this.applyErrorToDOM(e, a));
            }
            return (r || this.emitPostRender(), !0);
          }
          try {
            let e = this.getOrCreatePreNode(a);
            if (!(
              this.canPartiallyRender(n, b, v || y || g) &&
              this.applyPartialRender({
                previousRenderRange: S,
                renderRange: h,
              })
            )) {
              let t = this.hunksRenderer.renderDiff(this.fileDiff, h);
              if (t == null)
                return (
                  this.placeHolder != null &&
                    (this.placeHolder.dataset.loading = ``),
                  this.workerManager?.isInitialized() === !1 &&
                    this.workerManager.initialize().then(() => this.rerender()),
                  !1
                );
              (this.applyThemeState(a, t.themeStyles, p, t.baseThemeType),
                t.headerElement != null &&
                  this.applyHeaderToDOM(t.headerElement, a),
                t.additionsContentAST != null ||
                t.deletionsContentAST != null ||
                t.unifiedContentAST != null
                  ? this.applyHunksToDOM(e, t)
                  : this.pre != null &&
                    (this.pre.remove(), (this.pre = void 0)),
                this.placeHolder?.remove(),
                (this.placeHolder = void 0),
                this.renderSeparators(t.hunkData));
            }
            (this.applyBuffers(e, h),
              this.injectUnsafeCSS(),
              this.renderAnnotations(),
              this.renderGutterUtility(),
              (this.managersDirty = !0),
              t || this.flushManagers(),
              this.editor != null && this.syncRenderViewToEditor());
          } catch (e) {
            if (C) throw e;
            (console.error(e),
              e instanceof Error && this.applyErrorToDOM(e, a));
          }
          return (r || this.emitPostRender(), !0);
        }
        emitPostRender(e = !1) {
          let {
            fileContainer: t,
            options: { onPostRender: n },
          } = this;
          if (e) {
            if (!this.mounted || ((this.mounted = !1), t == null)) return;
            this.options.onPostRender?.(t, this, `unmount`);
            return;
          }
          if (t == null) return;
          let r = this.mounted ? `update` : `mount`;
          ((this.mounted = !0), n?.(t, this, r));
        }
        syncRenderViewToEditor() {
          let e = this.editor,
            t = this.fileContainer,
            n = this.hunksRenderer.getDiffCache() ?? this.fileDiff;
          e != null &&
            t != null &&
            n != null &&
            !n.isPartial &&
            this.hunksRenderer.initializeHighlighter().then((r) => {
              e.__syncRenderView(
                r,
                t,
                n,
                this.lineAnnotations,
                this.renderRange,
              );
            });
        }
        attachEditor(e) {
          return (
            this.editor?.cleanUp(),
            (this.editor = e),
            this.interactionManager.setEditorAttached(!0),
            this.syncRenderViewToEditor(),
            () => {
              ((this.editor = void 0),
                this.interactionManager.setEditorAttached(!1));
            }
          );
        }
        applyDocumentChange(e, t) {
          this.hunksRenderer.applyDocumentChange(e);
          let n = this.hunksRenderer.getDiffCache();
          if (n != null) {
            let e = this.fileDiff?.cacheKey;
            (e != null && n.cacheKey == null && (n.cacheKey = e),
              (this.fileDiff = n));
          }
          (t !== void 0 &&
            t !== this.lineAnnotations &&
            (this.setLineAnnotations(t),
            this.hunksRenderer.setLineAnnotations(this.lineAnnotations),
            this.renderAnnotations()),
            this.rerender(),
            this.interactionManager.setSelectionDirty());
        }
        updateRenderCache(e, t, n) {
          (this.hunksRenderer.updateRenderCache(e, t),
            n &&
              (this.refreshViewTimeout != null &&
                clearTimeout(this.refreshViewTimeout),
              (this.refreshViewTimeout = setTimeout(() => {
                ((this.refreshViewTimeout = void 0),
                  this.options.diffStyle === `split`
                    ? this.refreshSplitDiffView()
                    : this.refreshUnifiedDiffView(),
                  this.deferSetSelection != null &&
                    (this.interactionManager.setSelection(
                      ...this.deferSetSelection,
                    ),
                    (this.deferSetSelection = void 0)));
              }, 150))));
        }
        removeRenderedCode() {
          (this.resizeManager.cleanUp(),
            this.scrollSyncManager.cleanUp(),
            this.interactionManager.cleanUp(),
            this.bufferBefore?.remove(),
            (this.bufferBefore = void 0),
            this.bufferAfter?.remove(),
            (this.bufferAfter = void 0),
            this.codeUnified?.remove(),
            (this.codeUnified = void 0),
            this.codeDeletions?.remove(),
            (this.codeDeletions = void 0),
            this.codeAdditions?.remove(),
            (this.codeAdditions = void 0),
            this.pre?.remove(),
            (this.pre = void 0),
            (this.appliedPreAttributes = void 0),
            (this.lastRowCount = void 0));
        }
        clearAuxiliaryNodes() {
          for (let { element: e } of this.separatorCache.values()) e.remove();
          this.separatorCache.clear();
          for (let { element: e } of this.annotationCache.values()) e.remove();
          (this.annotationCache.clear(),
            this.gutterUtilityContent?.remove(),
            (this.gutterUtilityContent = void 0));
        }
        renderPlaceholder(e, t = !1) {
          if (this.fileContainer == null) return !1;
          if (
            (this.emitPostRender(!0),
            this.cleanChildNodes(),
            this.placeHolder == null)
          ) {
            let e =
              this.fileContainer.shadowRoot ??
              this.fileContainer.attachShadow({ mode: `open` });
            ((this.placeHolder = document.createElement(`div`)),
              (this.placeHolder.dataset.placeholder = ``),
              e.appendChild(this.placeHolder));
          }
          return (
            this.placeHolder.style.setProperty(`height`, `${e}px`),
            this.placeHolder.toggleAttribute(`data-loading`, t),
            this.injectUnsafeCSS(),
            !0
          );
        }
        async primeHighlightCache(e = this.fileDiff) {
          let { workerManager: t } = this;
          if (
            e == null ||
            t == null ||
            !t.isWorkingPool() ||
            e.cacheKey == null ||
            Gy(e)
          )
            return;
          let n = this.options.tokenizeMaxLength ?? 1e5;
          Math.max(e.additionLines.length, e.deletionLines.length) > n ||
            (await t.primeDiffHighlightCache(e).catch((e) => {
              console.error(e);
            }));
        }
        cleanChildNodes() {
          (this.resizeManager.cleanUp(),
            this.scrollSyncManager.cleanUp(),
            this.interactionManager.cleanUp(),
            this.clearAuxiliaryNodes(),
            this.bufferAfter?.remove(),
            this.bufferBefore?.remove(),
            this.codeAdditions?.remove(),
            this.codeDeletions?.remove(),
            this.codeUnified?.remove(),
            this.errorWrapper?.remove(),
            this.headerElement?.remove(),
            this.headerPrefix?.remove(),
            this.headerFilenameSuffix?.remove(),
            this.headerMetadata?.remove(),
            this.headerCustom?.remove(),
            this.pre?.remove(),
            this.spriteSVG?.remove(),
            this.themeCSSStyle?.remove(),
            this.unsafeCSSStyle?.remove(),
            (this.bufferAfter = void 0),
            (this.bufferBefore = void 0),
            (this.codeAdditions = void 0),
            (this.codeDeletions = void 0),
            (this.codeUnified = void 0),
            (this.errorWrapper = void 0),
            (this.headerElement = void 0),
            (this.headerPrefix = void 0),
            (this.headerFilenameSuffix = void 0),
            (this.headerMetadata = void 0),
            (this.headerCustom = void 0),
            (this.pre = void 0),
            (this.spriteSVG = void 0),
            (this.themeCSSStyle = void 0),
            (this.appliedThemeCSS = void 0),
            (this.hasAdoptedThemeCSS = !1),
            (this.unsafeCSSStyle = void 0),
            (this.appliedUnsafeCSS = void 0),
            (this.lastRenderedHeaderHTML = void 0),
            (this.lastRowCount = void 0),
            (this.mounted = !1));
        }
        renderSeparators(e) {
          let { hunkSeparators: t } = this.options;
          if (
            this.isContainerManaged ||
            this.fileContainer == null ||
            typeof t != `function`
          ) {
            for (let { element: e } of this.separatorCache.values()) e.remove();
            this.separatorCache.clear();
            return;
          }
          let n = new Map(this.separatorCache);
          for (let r of e) {
            let e = r.slotName,
              i = this.separatorCache.get(e);
            if (i == null || !Ob(r, i.hunkData)) {
              i?.element.remove();
              let n = document.createElement(`div`);
              ((n.style.display = `contents`), (n.slot = r.slotName));
              let a = t(r, this);
              (a != null && n.appendChild(a),
                this.fileContainer.appendChild(n),
                (i = { element: n, hunkData: r }),
                this.separatorCache.set(e, i));
            }
            n.delete(e);
          }
          for (let [e, { element: t }] of n.entries())
            (this.separatorCache.delete(e), t.remove());
        }
        renderAnnotations() {
          if (this.isContainerManaged || this.fileContainer == null) {
            for (let { element: e } of this.annotationCache.values())
              e.remove();
            this.annotationCache.clear();
            return;
          }
          let e = new Map(this.annotationCache),
            { renderAnnotation: t } = this.options;
          if (t != null && this.lineAnnotations.length > 0)
            for (let [n, r] of this.lineAnnotations.entries()) {
              let i = `${n}-${zm(r)}`,
                a = this.annotationCache.get(i);
              if (a == null || !Eb(r, a.annotation)) {
                a?.element.remove();
                let e = t(r);
                if (e == null) continue;
                ((a = { element: ig(zm(r)), annotation: r }),
                  a.element.appendChild(e),
                  this.fileContainer.appendChild(a.element),
                  this.annotationCache.set(i, a));
              }
              e.delete(i);
            }
          for (let [t, { element: n }] of e.entries())
            (this.annotationCache.delete(t), n.remove());
        }
        renderGutterUtility() {
          let { renderGutterUtility: e } = this.options;
          if (this.fileContainer == null || e == null) {
            (this.gutterUtilityContent?.remove(),
              (this.gutterUtilityContent = void 0));
            return;
          }
          let t = e(this.interactionManager.getHoveredLine);
          if (t != null && this.gutterUtilityContent != null) return;
          if (t == null) {
            (this.gutterUtilityContent?.remove(),
              (this.gutterUtilityContent = void 0));
            return;
          }
          let n = og();
          (n.appendChild(t),
            this.fileContainer.appendChild(n),
            (this.gutterUtilityContent = n));
        }
        getOrCreateFileContainer(e, t) {
          let { fileContainer: n } = this,
            r = e ?? n ?? document.createElement(`diffs-container`),
            i = n !== r;
          return (
            i && this.emitPostRender(!0),
            (this.fileContainer = r),
            n != null &&
              i &&
              ((this.lastRenderedHeaderHTML = void 0),
              (this.headerElement = void 0)),
            t != null &&
              this.fileContainer.parentNode !== t &&
              t.appendChild(this.fileContainer),
            i && this.adoptReusableShellElements(this.fileContainer),
            this.ensureSpriteSVG(this.fileContainer),
            this.fileContainer
          );
        }
        adoptReusableShellElements(e) {
          let { shadowRoot: t } = e;
          if (t != null)
            for (let e of t.children)
              e instanceof SVGElement
                ? (this.spriteSVG ??= e)
                : kg(e) && e.hasAttribute(`data-theme-css`)
                  ? ((this.themeCSSStyle ??= e), (this.hasAdoptedThemeCSS = !0))
                  : kg(e) &&
                    e.hasAttribute(`data-unsafe-css`) &&
                    ((this.unsafeCSSStyle ??= e),
                    (this.appliedUnsafeCSS ??=
                      this.options.unsafeCSS ?? void 0));
        }
        ensureSpriteSVG(e) {
          let t = e.shadowRoot ?? e.attachShadow({ mode: `open` });
          if (this.spriteSVG == null) {
            let e = document.createElement(`div`);
            e.innerHTML = Xh;
            let t = e.firstChild;
            t instanceof SVGElement && (this.spriteSVG = t);
          }
          this.spriteSVG != null &&
            this.spriteSVG.parentNode !== t &&
            t.appendChild(this.spriteSVG);
        }
        getOrCreatePreNode(e) {
          let t = e.shadowRoot ?? e.attachShadow({ mode: `open` });
          return (
            this.pre == null
              ? ((this.pre = document.createElement(`pre`)),
                (this.appliedPreAttributes = void 0),
                (this.codeUnified = void 0),
                (this.codeDeletions = void 0),
                (this.codeAdditions = void 0),
                t.appendChild(this.pre))
              : this.pre.parentNode !== t &&
                (t.appendChild(this.pre), (this.appliedPreAttributes = void 0)),
            this.pre
          );
        }
        syncCodeNodesFromPre(e) {
          ((this.codeUnified = void 0),
            (this.codeDeletions = void 0),
            (this.codeAdditions = void 0));
          for (let t of Array.from(e.children))
            t instanceof HTMLElement &&
              (t.hasAttribute(`data-unified`)
                ? (this.codeUnified = t)
                : t.hasAttribute(`data-deletions`)
                  ? (this.codeDeletions = t)
                  : t.hasAttribute(`data-additions`) &&
                    (this.codeAdditions = t));
        }
        applyHeaderToDOM(e, t) {
          (this.cleanupErrorWrapper(),
            this.placeHolder?.remove(),
            (this.placeHolder = void 0));
          let { fileDiff: n } = this,
            r = this.cachedHeaderHTML ?? Ei(e);
          if (
            ((this.cachedHeaderHTML = r), r !== this.lastRenderedHeaderHTML)
          ) {
            let e = document.createElement(`div`);
            e.innerHTML = r;
            let n = e.firstElementChild;
            if (!(n instanceof HTMLElement)) return;
            (this.headerElement == null
              ? t.shadowRoot?.prepend(n)
              : t.shadowRoot?.replaceChild(n, this.headerElement),
              (this.headerElement = n),
              (this.lastRenderedHeaderHTML = r));
          }
          if (this.isContainerManaged || n == null) return;
          let {
            renderCustomHeader: i,
            renderHeaderPrefix: a,
            renderHeaderFilenameSuffix: o,
            renderHeaderMetadata: s,
          } = this.options;
          if (i != null) {
            let e = i(n) ?? void 0;
            ((this.headerCustom = this.upsertHeaderSlotElement(
              t,
              this.headerCustom,
              Nt,
              e,
            )),
              this.headerPrefix?.remove(),
              this.headerFilenameSuffix?.remove(),
              this.headerMetadata?.remove(),
              (this.headerPrefix = void 0),
              (this.headerFilenameSuffix = void 0),
              (this.headerMetadata = void 0));
            return;
          }
          let c = a?.(n) ?? void 0,
            l = o?.(n) ?? void 0,
            u = s?.(n) ?? void 0;
          ((this.headerPrefix = this.upsertHeaderSlotElement(
            t,
            this.headerPrefix,
            At,
            c,
          )),
            (this.headerFilenameSuffix = this.upsertHeaderSlotElement(
              t,
              this.headerFilenameSuffix,
              jt,
              l,
            )),
            (this.headerMetadata = this.upsertHeaderSlotElement(
              t,
              this.headerMetadata,
              Mt,
              u,
            )),
            this.headerCustom?.remove(),
            (this.headerCustom = void 0));
        }
        clearHeaderSlots() {
          (this.headerPrefix?.remove(),
            this.headerFilenameSuffix?.remove(),
            this.headerMetadata?.remove(),
            this.headerCustom?.remove(),
            (this.headerPrefix = void 0),
            (this.headerFilenameSuffix = void 0),
            (this.headerMetadata = void 0),
            (this.headerCustom = void 0));
        }
        upsertHeaderSlotElement(e, t, n, r) {
          if (r == null) {
            t?.remove();
            return;
          }
          let i = t ?? this.createHeaderSlotElement(n);
          return (
            t ?? e.appendChild(i),
            this.replaceHeaderSlotContent(i, r),
            i
          );
        }
        replaceHeaderSlotContent(e, t) {
          (e.replaceChildren(),
            t instanceof Element ? e.appendChild(t) : (e.innerText = `${t}`));
        }
        createHeaderSlotElement(e) {
          let t = document.createElement(`div`);
          return ((t.slot = e), t);
        }
        injectUnsafeCSS() {
          let { unsafeCSS: e } = this.options,
            t = this.fileContainer?.shadowRoot;
          if (t != null) {
            if (e == null || e === ``) {
              (this.unsafeCSSStyle != null &&
                (this.unsafeCSSStyle.remove(), (this.unsafeCSSStyle = void 0)),
                (this.appliedUnsafeCSS = void 0));
              return;
            }
            (this.unsafeCSSStyle?.parentNode === t &&
              this.appliedUnsafeCSS === e) ||
              ((this.unsafeCSSStyle ??= cg()),
              this.unsafeCSSStyle.parentNode !== t &&
                t.appendChild(this.unsafeCSSStyle),
              (this.unsafeCSSStyle.textContent = gg(e)),
              (this.appliedUnsafeCSS = e));
          }
        }
        applyThemeState(e, t, n, r) {
          let i = e.shadowRoot ?? e.attachShadow({ mode: `open` }),
            a = r ?? n,
            o = this.options.theme ?? P,
            s = typeof o == `string` ? o : { ...o },
            c = fg(i);
          if (
            this.themeCSSStyle?.parentNode === i &&
            this.appliedThemeCSS?.themeStyles === t &&
            this.appliedThemeCSS.themeType === a &&
            this.appliedThemeCSS.scrollbarGutter === c
          ) {
            this.appliedThemeCSS.theme = s;
            return;
          }
          if (this.hasAdoptedThemeCSS && this.themeCSSStyle?.parentNode === i) {
            ((this.hasAdoptedThemeCSS = !1),
              (this.appliedThemeCSS = {
                theme: s,
                themeStyles: t,
                themeType: a,
                baseThemeType: r,
                scrollbarGutter: c,
              }));
            return;
          }
          ((this.themeCSSStyle = Ng({
            shadowRoot: i,
            currentNode: this.themeCSSStyle,
            themeCSS: _g(t, a, c),
          })),
            (this.appliedThemeCSS =
              this.themeCSSStyle == null
                ? void 0
                : {
                    theme: s,
                    themeStyles: t,
                    themeType: a,
                    baseThemeType: r,
                    scrollbarGutter: c,
                  }));
        }
        hydrateMeasuredScrollbar() {
          let e = this.fileContainer?.shadowRoot;
          e == null ||
            this.themeCSSStyle == null ||
            (this.themeCSSStyle.textContent = vg(
              this.themeCSSStyle.textContent ?? ``,
              fg(e),
            ));
        }
        applyHunksToDOM(e, t) {
          let { overflow: n = `scroll` } = this.options,
            r = (this.options.hunkSeparators ?? `line-info`) === `line-info`,
            i = n === `wrap` ? t.rowCount : void 0;
          (this.cleanupErrorWrapper(), this.applyPreNodeAttributes(e, t));
          let a = !1,
            o = [],
            s = this.hunksRenderer.renderCodeAST(`unified`, t),
            c = this.hunksRenderer.renderCodeAST(`deletions`, t),
            l = this.hunksRenderer.renderCodeAST(`additions`, t);
          (s == null
            ? c != null || l != null
              ? (c == null
                  ? (this.codeDeletions?.remove(),
                    (this.codeDeletions = void 0))
                  : ((a =
                      this.codeDeletions == null || this.codeUnified != null),
                    this.codeUnified?.remove(),
                    (this.codeUnified = void 0),
                    (this.codeDeletions = Cg({
                      code: this.codeDeletions,
                      columnType: `deletions`,
                      rowSpan: i,
                      containerSize: r,
                    })),
                    (this.codeDeletions.innerHTML =
                      this.hunksRenderer.renderPartialHTML(c)),
                    o.push(this.codeDeletions)),
                l == null
                  ? (this.codeAdditions?.remove(),
                    (this.codeAdditions = void 0))
                  : ((a =
                      a ||
                      this.codeAdditions == null ||
                      this.codeUnified != null),
                    this.codeUnified?.remove(),
                    (this.codeUnified = void 0),
                    (this.codeAdditions = Cg({
                      code: this.codeAdditions,
                      columnType: `additions`,
                      rowSpan: i,
                      containerSize: r,
                    })),
                    (this.codeAdditions.innerHTML =
                      this.hunksRenderer.renderPartialHTML(l)),
                    o.push(this.codeAdditions)))
              : (this.codeUnified?.remove(),
                (this.codeUnified = void 0),
                this.codeDeletions?.remove(),
                (this.codeDeletions = void 0),
                this.codeAdditions?.remove(),
                (this.codeAdditions = void 0))
            : ((a =
                this.codeUnified == null ||
                this.codeAdditions != null ||
                this.codeDeletions != null),
              this.codeDeletions?.remove(),
              (this.codeDeletions = void 0),
              this.codeAdditions?.remove(),
              (this.codeAdditions = void 0),
              (this.codeUnified = Cg({
                code: this.codeUnified,
                columnType: `unified`,
                rowSpan: i,
                containerSize: r,
              })),
              (this.codeUnified.innerHTML =
                this.hunksRenderer.renderPartialHTML(s)),
              o.push(this.codeUnified)),
            o.length === 0
              ? (e.textContent = ``)
              : a && e.replaceChildren(...o),
            (this.lastRowCount = t.rowCount));
        }
        applyPartialRender({ previousRenderRange: e, renderRange: t }) {
          let {
            pre: n,
            codeUnified: r,
            codeAdditions: i,
            codeDeletions: a,
            options: { diffStyle: o = `split` },
          } = this;
          if (
            n == null ||
            e == null ||
            t == null ||
            !Number.isFinite(e.totalLines) ||
            !Number.isFinite(t.totalLines) ||
            this.lastRowCount == null
          )
            return !1;
          let s = this.getCodeColumns(o, r, a, i);
          if (s == null) return !1;
          let c = e.startingLine,
            l = t.startingLine,
            u = c + e.totalLines,
            d = l + t.totalLines,
            f = Math.max(c, l),
            p = Math.min(u, d);
          if (p <= f) return !1;
          let m = Math.max(0, f - c),
            h = Math.max(0, u - p),
            g = this.trimColumns({
              columns: s,
              trimStart: m,
              trimEnd: h,
              previousStart: c,
              overlapStart: f,
              overlapEnd: p,
              diffStyle: o,
            });
          if (g < 0)
            throw Error(`applyPartialRender: failed to trim to overlap`);
          if (this.lastRowCount < g)
            throw Error(`applyPartialRender: trimmed beyond DOM row count`);
          let _ = this.lastRowCount - g,
            v = (e, t) => {
              if (!(t <= 0 || this.fileDiff == null))
                return this.hunksRenderer.renderDiff(this.fileDiff, {
                  startingLine: e,
                  totalLines: t,
                  bufferBefore: 0,
                  bufferAfter: 0,
                });
            },
            y = v(l, Math.max(f - l, 0));
          if (y == null && l < f) return !1;
          let b = v(p, Math.max(d - p, 0));
          if (b == null && d > p) return !1;
          let x = (e, t) => {
            if (e != null) {
              if (o === `unified` && !Array.isArray(s))
                this.insertPartialHTML(o, s, e, t);
              else if (o === `split` && Array.isArray(s))
                this.insertPartialHTML(o, s, e, t);
              else
                throw Error(
                  `FileDiff.applyPartialRender.applyChunk: invalid chunk application`,
                );
              _ += e.rowCount;
            }
          };
          return (
            this.cleanupErrorWrapper(),
            x(y, `afterbegin`),
            x(b, `beforeend`),
            this.lastRowCount !== _ &&
              (this.applyRowSpan(o, s, _), (this.lastRowCount = _)),
            !0
          );
        }
        insertPartialHTML(e, t, n, r) {
          if (e === `unified` && !Array.isArray(t)) {
            let e = this.hunksRenderer.renderCodeAST(`unified`, n);
            this.renderPartialColumn(t, e, r);
          } else if (e === `split` && Array.isArray(t)) {
            let e = this.hunksRenderer.renderCodeAST(`deletions`, n),
              i = this.hunksRenderer.renderCodeAST(`additions`, n);
            (this.renderPartialColumn(t[0], e, r),
              this.renderPartialColumn(t[1], i, r));
          } else
            throw Error(
              `FileDiff.insertPartialHTML: Invalid argument composition`,
            );
        }
        refreshSplitDiffView() {
          if (this.options.diffStyle !== `split`) return;
          let e = this.hunksRenderer.renderDiff(
            this.fileDiff,
            this.renderRange,
          );
          if (e == null) return;
          let t = this.getCodeColumns(
            `split`,
            this.codeUnified,
            this.codeDeletions,
            this.codeAdditions,
          );
          if (!Array.isArray(t)) return;
          let n = (t, n) => {
            if (n == null) return;
            let r = this.hunksRenderer.renderCodeAST(t, e),
              i = Hb(r?.[0]),
              a = Hb(r?.[1]);
            for (let [e, t] of [
              [n.gutter, i],
              [n.content, a],
            ])
              if (t != null && e.childElementCount === t.length)
                for (let n = 0; n < t.length; n++) {
                  let r = e.children[n],
                    i = t[n].properties[`data-line-type`];
                  i != null &&
                    r.dataset.lineType !== i &&
                    (r.dataset.lineType = i);
                }
          };
          (n(`deletions`, t[0]), n(`additions`, t[1]));
        }
        refreshUnifiedDiffView() {
          if (this.options.diffStyle !== `unified`) return;
          let e = this.hunksRenderer.renderDiff(
            this.fileDiff,
            this.renderRange,
          );
          if (e == null) return;
          let t = this.getCodeColumns(
            `unified`,
            this.codeUnified,
            this.codeDeletions,
            this.codeAdditions,
          );
          if (t == null || Array.isArray(t)) return;
          let n = this.hunksRenderer.renderCodeAST(`unified`, e),
            r = Hb(n?.[0]),
            i = Hb(n?.[1]);
          for (let [e, n] of [
            [t.gutter, r],
            [t.content, i],
          ])
            n != null && (e.innerHTML = Ei(n));
          (e.rowCount !== this.lastRowCount &&
            (this.applyRowSpan(`unified`, t, e.rowCount),
            (this.lastRowCount = e.rowCount)),
            this.renderSeparators(e.hunkData),
            (this.managersDirty = !0),
            this.flushManagers(),
            this.syncRenderViewToEditor());
        }
        renderPartialColumn(e, t, n) {
          if (e == null || t == null) return;
          let r = Hb(t[0]),
            i = Hb(t[1]);
          if (r == null || i == null)
            throw Error(`FileDiff.insertPartialHTML: Unexpected AST structure`);
          let a = i.at(0);
          n === `beforeend` &&
            a?.type === `element` &&
            typeof a.properties[`data-buffer-size`] == `number` &&
            this.mergeBuffersIfNecessary(
              a.properties[`data-buffer-size`],
              e.content.children[e.content.children.length - 1],
              e.gutter.children[e.gutter.children.length - 1],
              r,
              i,
              !0,
            );
          let o = i.at(-1);
          (n === `afterbegin` &&
            o?.type === `element` &&
            typeof o.properties[`data-buffer-size`] == `number` &&
            this.mergeBuffersIfNecessary(
              o.properties[`data-buffer-size`],
              e.content.children[0],
              e.gutter.children[0],
              r,
              i,
              !1,
            ),
            e.gutter.insertAdjacentHTML(
              n,
              this.hunksRenderer.renderPartialHTML(r),
            ),
            e.content.insertAdjacentHTML(
              n,
              this.hunksRenderer.renderPartialHTML(i),
            ));
        }
        mergeBuffersIfNecessary(e, t, n, r, i, a) {
          if (!(t instanceof HTMLElement) || !(n instanceof HTMLElement))
            return;
          let o = this.getBufferSize(t.dataset);
          o != null &&
            (a ? (r.shift(), i.shift()) : (r.pop(), i.pop()),
            this.updateBufferSize(t, o + e),
            this.updateBufferSize(n, o + e));
        }
        applyRowSpan(e, t, n) {
          let r = (e) => {
            e != null &&
              (e.gutter.style.setProperty(`grid-row`, `span ${n}`),
              e.content.style.setProperty(`grid-row`, `span ${n}`));
          };
          if (e === `unified` && !Array.isArray(t)) r(t);
          else if (e === `split` && Array.isArray(t)) (r(t[0]), r(t[1]));
          else throw Error(`dun fuuuuked up`);
        }
        trimColumnRows(e, t, n) {
          let r = 0,
            i = 0,
            a = 0,
            o = !1,
            s = n >= 0;
          if (e == null) return 0;
          let c = Array.from(e.content.children),
            l = Array.from(e.gutter.children);
          if (c.length !== l.length)
            throw Error(`FileDiff.trimColumnRows: columns do not match`);
          for (; a < c.length && !(t <= 0 && !s && !o);) {
            let e = l[a],
              u = c[a];
            if (
              (a++, !(e instanceof HTMLElement) || !(u instanceof HTMLElement))
            )
              throw (
                console.error({ gutterElement: e, contentElement: u }),
                Error(`FileDiff.trimColumnRows: invalid row elements`)
              );
            if (
              o &&
              ((o = !1),
              (e.dataset.gutterBuffer === `annotation` &&
                `lineAnnotation` in u.dataset) ||
                (e.dataset.gutterBuffer === `metadata` &&
                  `noNewline` in u.dataset))
            ) {
              (e.remove(), u.remove(), i++);
              continue;
            }
            if (`lineIndex` in e.dataset && `lineIndex` in u.dataset) {
              ((t > 0 || (s && r >= n)) &&
                (e.remove(),
                u.remove(),
                t > 0 && (t--, t === 0 && (o = !0)),
                i++),
                r++);
              continue;
            }
            if (`separator` in e.dataset && `separator` in u.dataset) {
              (t > 0 || (s && r >= n)) && (e.remove(), u.remove(), i++);
              continue;
            }
            if (
              e.dataset.gutterBuffer === `annotation` &&
              `lineAnnotation` in u.dataset
            ) {
              (t > 0 || (s && r >= n)) && (e.remove(), u.remove(), i++);
              continue;
            }
            if (
              e.dataset.gutterBuffer === `metadata` &&
              `noNewline` in u.dataset
            ) {
              (t > 0 || (s && r >= n)) && (e.remove(), u.remove(), i++);
              continue;
            }
            if (
              e.dataset.gutterBuffer === `buffer` &&
              `contentBuffer` in u.dataset
            ) {
              let a = this.getBufferSize(u.dataset);
              if (a == null)
                throw Error(`FileDiff.trimColumnRows: invalid element`);
              if (t > 0) {
                let n = Math.min(t, a),
                  r = a - n;
                (r > 0
                  ? (this.updateBufferSize(e, r),
                    this.updateBufferSize(u, r),
                    (i += n))
                  : (e.remove(), u.remove(), (i += a)),
                  (t -= n),
                  t === 0 && r === 0 && (o = !0));
              } else if (s) {
                let t = r,
                  o = r + a - 1;
                if (n <= t) (e.remove(), u.remove(), (i += a));
                else if (n <= o) {
                  let t = o - n + 1,
                    r = a - t;
                  (this.updateBufferSize(e, r),
                    this.updateBufferSize(u, r),
                    (i += t));
                }
              }
              r += a;
              continue;
            }
            throw (
              console.error({ gutterElement: e, contentElement: u }),
              Error(`FileDiff.trimColumnRows: unknown row elements`)
            );
          }
          return i;
        }
        trimColumns({
          columns: e,
          diffStyle: t,
          overlapEnd: n,
          overlapStart: r,
          previousStart: i,
          trimEnd: a,
          trimStart: o,
        }) {
          let s = Math.max(0, r - i),
            c = n - i;
          if (c < 0)
            throw Error(`FileDiff.trimColumns: overlap ends before previous`);
          let l = o > 0,
            u = a > 0;
          if (!l && !u) return 0;
          let d = l ? s : 0,
            f = u ? c : -1;
          if (t === `unified` && !Array.isArray(e))
            return this.trimColumnRows(e, d, f);
          if (t === `split` && Array.isArray(e)) {
            let t = this.trimColumnRows(e[0], d, f),
              n = this.trimColumnRows(e[1], d, f);
            if (e[0] != null && e[1] != null && t !== n)
              throw Error(`FileDiff.trimColumns: split columns out of sync`);
            return e[0] == null ? n : t;
          } else
            throw (
              console.error({ diffStyle: t, columns: e }),
              Error(`FileDiff.trimColumns: Invalid columns for diffType`)
            );
        }
        getBufferSize(e) {
          let t = Number.parseInt(e?.bufferSize ?? ``, 10);
          return Number.isNaN(t) ? void 0 : t;
        }
        updateBufferSize(e, t) {
          ((e.dataset.bufferSize = `${t}`),
            e.style.setProperty(`grid-row`, `span ${t}`),
            e.style.setProperty(`min-height`, `calc(${t} * 1lh)`));
        }
        getCodeColumns(e, t, n, r) {
          function i(e) {
            if (e == null) return;
            let t = e.children[0],
              n = e.children[1];
            if (!(
              !(t instanceof HTMLElement) ||
              !(n instanceof HTMLElement) ||
              t.dataset.gutter == null ||
              n.dataset.content == null
            ))
              return { gutter: t, content: n };
          }
          if (e === `unified`) return i(t);
          {
            let e = i(n),
              t = i(r);
            return e != null || t != null ? [e, t] : void 0;
          }
        }
        updateBuffers(e) {
          this.pre != null && this.applyBuffers(this.pre, e);
        }
        applyBuffers(e, t) {
          if (t == null || this.shouldDisableVirtualizationBuffers()) {
            (this.bufferBefore != null &&
              (this.bufferBefore.remove(), (this.bufferBefore = void 0)),
              this.bufferAfter != null &&
                (this.bufferAfter.remove(), (this.bufferAfter = void 0)));
            return;
          }
          (t.bufferBefore > 0
            ? (this.bufferBefore ??
                ((this.bufferBefore = document.createElement(`div`)),
                (this.bufferBefore.dataset.virtualizerBuffer = `before`),
                e.before(this.bufferBefore)),
              this.bufferBefore.style.setProperty(
                `height`,
                `${t.bufferBefore}px`,
              ),
              this.bufferBefore.style.setProperty(`contain`, `strict`))
            : this.bufferBefore != null &&
              (this.bufferBefore.remove(), (this.bufferBefore = void 0)),
            t.bufferAfter > 0
              ? (this.bufferAfter ??
                  ((this.bufferAfter = document.createElement(`div`)),
                  (this.bufferAfter.dataset.virtualizerBuffer = `after`),
                  e.after(this.bufferAfter)),
                this.bufferAfter.style.setProperty(
                  `height`,
                  `${t.bufferAfter}px`,
                ),
                this.bufferAfter.style.setProperty(`contain`, `strict`))
              : this.bufferAfter != null &&
                (this.bufferAfter.remove(), (this.bufferAfter = void 0)));
        }
        shouldDisableVirtualizationBuffers() {
          return this.options.disableVirtualizationBuffers ?? !1;
        }
        applyPreNodeAttributes(
          e,
          { additionsContentAST: t, deletionsContentAST: n, totalLines: r },
          i,
        ) {
          let {
              diffIndicators: a = `bars`,
              disableBackground: o = !1,
              disableLineNumbers: s = !1,
              overflow: c = `scroll`,
              diffStyle: l = `split`,
            } = this.options,
            u = {
              type: `diff`,
              diffIndicators: a,
              disableBackground: o,
              disableLineNumbers: s,
              overflow: c,
              split: l === `unified` ? !1 : t != null && n != null,
              totalLines: r,
              customProperties: i,
            };
          eg(u, this.appliedPreAttributes) ||
            (Dg(e, u), (this.appliedPreAttributes = u));
        }
        applyErrorToDOM(e, t) {
          (this.cleanupErrorWrapper(),
            this.placeHolder?.remove(),
            (this.placeHolder = void 0),
            this.pre?.remove(),
            (this.pre = void 0),
            (this.appliedPreAttributes = void 0));
          let n = t.shadowRoot ?? t.attachShadow({ mode: `open` });
          ((this.errorWrapper ??= document.createElement(`div`)),
            (this.errorWrapper.dataset.errorWrapper = ``),
            (this.errorWrapper.textContent = ``),
            n.appendChild(this.errorWrapper));
          let r = document.createElement(`div`);
          ((r.dataset.errorMessage = ``),
            (r.innerText = e.message),
            this.errorWrapper.appendChild(r));
          let i = document.createElement(`pre`);
          ((i.dataset.errorStack = ``),
            (i.innerText = e.stack ?? `No Error Stack`),
            this.errorWrapper.appendChild(i));
        }
        cleanupErrorWrapper() {
          (this.errorWrapper?.remove(), (this.errorWrapper = void 0));
        }
      }));
  });
function Kb({
  fileDiff: e,
  metrics: t,
  disableFileHeader: n,
  hunkSeparators: r,
  expandUnchanged: i,
  expandedHunks: a,
  collapsedContextThreshold: o,
  canHydratePartialDiff: s,
}) {
  let c = Wg(t, n),
    l = c,
    u = i ? !0 : a,
    d = e.hunks.length - 1;
  for (let n = 0; n < e.hunks.length; n++) {
    let i = e.hunks[n];
    if (i == null)
      throw Error(`computeEstimatedDiffHeights: invalid hunk index`);
    let a = by({
        isPartial: e.isPartial,
        rangeSize: i.collapsedBefore,
        expandedHunks: u,
        hunkIndex: n,
        collapsedContextThreshold: o,
      }),
      f = (a.fromStart + a.fromEnd) * t.lineHeight;
    if (((c += f), (l += f), a.collapsedLines > 0)) {
      let e =
        Oy({ type: r, metrics: t, hunkIndex: n, hunkSpecs: i.hunkSpecs })
          ?.totalHeight ?? 0;
      ((c += e), (l += e));
    }
    ((c += i.splitLineCount * t.lineHeight),
      (l += i.unifiedLineCount * t.lineHeight));
    let p = qb(i);
    ((c += p.split * t.lineHeight), (l += p.unified * t.lineHeight));
    let m =
      n === d
        ? Cy({
            fileDiff: e,
            hunkIndex: n,
            expandedHunks: u,
            collapsedContextThreshold: o,
            errorPrefix: `computeEstimatedDiffHeights`,
          })
        : void 0;
    if (m != null) {
      let e = (m.fromStart + m.fromEnd) * t.lineHeight;
      if (((c += e), (l += e), m.collapsedLines > 0)) {
        let e = ky({ type: r, metrics: t })?.totalHeight ?? 0;
        ((c += e), (l += e));
      }
    } else if (n === d && e.isPartial && s) {
      let e = ky({ type: r, metrics: t })?.totalHeight ?? 0;
      ((c += e), (l += e));
    }
  }
  if (e.hunks.length > 0) {
    let e = Kg(t);
    ((c += e), (l += e));
  }
  return { splitHeight: c, unifiedHeight: l };
}
function qb(e) {
  if (!e.noEOFCRAdditions && !e.noEOFCRDeletions)
    return { split: 0, unified: 0 };
  let t = e.hunkContent.at(-1);
  if (t == null) return { split: 0, unified: 0 };
  if (t.type === `context`) {
    let e = +(t.lines > 0);
    return { split: e, unified: e };
  }
  return Jb(e, t);
}
function Jb(e, t) {
  let n =
      (t.deletions > 0 && e.noEOFCRDeletions ? 1 : 0) +
      (t.additions > 0 && e.noEOFCRAdditions ? 1 : 0),
    r = t.deletions > 0 && e.noEOFCRDeletions,
    i = t.additions > 0 && e.noEOFCRAdditions;
  return { split: r || i ? 1 : 0, unified: n };
}
var Yb = e(() => {
  (Jg(), Ay());
});
function Xb(e) {
  let t;
  for (let n of e) {
    if (n == null) continue;
    let e = n.children[1];
    if (e instanceof HTMLElement)
      for (let n of e.children)
        n instanceof HTMLElement &&
          n.dataset.lineAnnotation === Rh &&
          (t = Math.max(t ?? 0, n.getBoundingClientRect().height));
  }
  return t;
}
function Zb(e) {
  let t = Array.from(e).sort((e, t) => e[0] - t[0]),
    n = [],
    r = [0],
    i = 0;
  for (let [e, a] of t) (n.push(e), (i += a), r.push(i));
  return { lineIndexes: n, prefixTotals: r };
}
function Qb({ lineIndexes: e, prefixTotals: t }, n, r) {
  if (n >= r || e.length === 0) return 0;
  let i = $b(e, n);
  return (t[$b(e, r)] ?? 0) - (t[i] ?? 0);
}
function $b(e, t) {
  let n = 0,
    r = e.length;
  for (; n < r;) {
    let i = (n + r) >> 1,
      a = e[i];
    if (a == null) throw Error(`VirtualizedFileDiff: invalid prefix index`);
    a < t ? (n = i + 1) : (r = i);
  }
  return n;
}
function ex(e) {
  return Math.ceil(e / lx) * lx;
}
function tx(e, t) {
  let n = 0;
  for (let r of e) r < t && n++;
  return n;
}
function nx({ diffStyle: e, hunk: t, rowCount: n }) {
  if (n <= 0 || (!t.noEOFCRAdditions && !t.noEOFCRDeletions)) return [];
  let r = t.hunkContent.at(-1);
  if (r == null) return [];
  if (r.type === `context`) return [n - 1];
  let i = Math.max(r.deletions, r.additions),
    a = r.deletions + r.additions;
  if (e === `split`)
    return i > 0 && (t.noEOFCRAdditions || t.noEOFCRDeletions) ? [n - 1] : [];
  let o = [],
    s = n - a;
  return (
    r.deletions > 0 && t.noEOFCRDeletions && o.push(s + r.deletions - 1),
    r.additions > 0 && t.noEOFCRAdditions && o.push(n - 1),
    o
  );
}
function rx(e, t) {
  return (
    (e.diffStyle ?? `split`) !== (t.diffStyle ?? `split`) ||
    (e.overflow ?? `scroll`) !== (t.overflow ?? `scroll`) ||
    (e.collapsed ?? !1) !== (t.collapsed ?? !1) ||
    (e.disableLineNumbers ?? !1) !== (t.disableLineNumbers ?? !1) ||
    (e.disableFileHeader ?? !1) !== (t.disableFileHeader ?? !1) ||
    (e.diffIndicators ?? `bars`) !== (t.diffIndicators ?? `bars`) ||
    (e.hunkSeparators ?? `line-info`) !== (t.hunkSeparators ?? `line-info`) ||
    !!e.loadDiffFiles != !!t.loadDiffFiles ||
    (e.expandUnchanged ?? !1) !== (t.expandUnchanged ?? !1) ||
    (e.collapsedContextThreshold ?? 1) !== (t.collapsedContextThreshold ?? 1) ||
    e.unsafeCSS !== t.unsafeCSS
  );
}
function ix(e, t) {
  return (
    (e.disableFileHeader ?? !1) !== (t.disableFileHeader ?? !1) ||
    (e.hunkSeparators ?? `line-info`) !== (t.hunkSeparators ?? `line-info`) ||
    !!e.loadDiffFiles != !!t.loadDiffFiles ||
    (e.expandUnchanged ?? !1) !== (t.expandUnchanged ?? !1) ||
    (e.collapsedContextThreshold ?? 1) !== (t.collapsedContextThreshold ?? 1)
  );
}
function ax(e, t) {
  return (
    e.isPartial && t && (e.type === `change` || e.type === `rename-changed`)
  );
}
function ox(e) {
  return typeof e == `function` ? `custom` : (e ?? `line-info`);
}
function sx(e, t) {
  return e == null || t == null ? e == null && t == null : fn(e, t);
}
function cx(e, t) {
  let [n, r] = e.split(`,`).map(Number);
  return t === `split` ? r : n;
}
var lx,
  ux,
  dx,
  fx = e(() => {
    ($t(),
      an(),
      pn(),
      Jg(),
      zh(),
      n_(),
      jb(),
      Ay(),
      Yb(),
      Nb(),
      g_(),
      Ry(),
      $v(),
      Gb(),
      (lx = 5e3),
      (ux = -1),
      (dx = class extends Wb {
        __id = `little-virtualized-file-diff:${++ux}`;
        top;
        height = 0;
        metrics;
        cache = {
          heightDeltas: new Map(),
          measuredHeightDeltaTotal: 0,
          estimatedSplitHeight: void 0,
          estimatedUnifiedHeight: void 0,
          checkpoints: [],
          totalLines: 0,
          fileAnnotationHeight: 0,
        };
        isVisible = !1;
        isSetup = !1;
        virtualizer;
        layoutDirty = !0;
        forceRenderOverride;
        currentCollapsed;
        pendingHydratedDiff;
        pendingExpansions;
        constructor(e, t, n, r, i = !1) {
          (super(e, r, i), (this.virtualizer = t), (this.metrics = Ug(n)));
        }
        setMetrics(e, t = !1) {
          let n = Ug(e);
          (!t && Qt(this.metrics, n)) ||
            ((this.metrics = n),
            this.resetLayoutCache({ includeEstimatedHeights: !0 }));
        }
        setLineAnnotations(e) {
          this.syncLineAnnotations(e) &&
            this.resetLayoutCache({ includeEstimatedHeights: !1 });
        }
        syncLineAnnotations(e) {
          return e == null ||
            e === this.lineAnnotations ||
            (e.length === 0 && this.lineAnnotations.length === 0)
            ? !1
            : (super.setLineAnnotations(e), !0);
        }
        setFileAnnotationHeight(e) {
          let t = this.cache.fileAnnotationHeight;
          return e === t
            ? !1
            : ((this.cache.fileAnnotationHeight = e),
              (this.cache.measuredHeightDeltaTotal += e - t),
              !0);
        }
        hasFileAnnotations(e = this.fileDiff) {
          return e == null || !Fh(this.lineAnnotations)
            ? !1
            : this.lineAnnotations.some((t) =>
                t.lineNumber === 0
                  ? e.type === `new`
                    ? t.side === `additions`
                    : e.type === `deleted`
                      ? t.side === `deletions`
                      : !0
                  : !1,
              );
        }
        getLineHeight(e, t = !1) {
          return (
            this.getEstimatedLineHeight(t) +
            (this.cache.heightDeltas.get(e) ?? 0)
          );
        }
        getEstimatedLineHeight(e = !1) {
          let t = e ? 2 : 1;
          return this.metrics.lineHeight * t;
        }
        setOptions(e) {
          if (this.isAdvancedMode())
            throw Error(
              `VirtualizedFileDiff.setOptions cannot be used inside CodeView. Update CodeView options instead.`,
            );
          if (e == null) return;
          let { options: t } = this,
            n = !nn(t, e),
            r = n && rx(t, e);
          (super.setOptions(e),
            r &&
              this.resetLayoutCache({
                forceSimpleRecompute: !0,
                includeEstimatedHeights: ix(t, e),
              }),
            n && (this.forceRenderOverride = !0),
            n &&
              this.isSimpleMode() &&
              this.virtualizer.instanceChanged(this, r));
        }
        setThemeType(e) {
          if (this.isAdvancedMode())
            throw Error(
              `VirtualizedFileDiff.setThemeType cannot be used inside CodeView. Update CodeView options instead.`,
            );
          super.setThemeType(e);
        }
        resetLayoutCache({
          forceSimpleRecompute: e = !1,
          includeEstimatedHeights: t = !1,
          resetRenderRange: n = !0,
        } = {}) {
          ((this.layoutDirty = !0),
            (this.cache.fileAnnotationHeight = 0),
            this.cache.heightDeltas.size > 0 && this.cache.heightDeltas.clear(),
            this.cache.measuredHeightDeltaTotal !== 0 &&
              (this.cache.measuredHeightDeltaTotal = 0),
            this.invalidateDerivedLayoutCache(t, n),
            e && this.isSimpleMode() && this.computeApproximateSize());
        }
        invalidateDerivedLayoutCache(e, t = !0) {
          ((this.layoutDirty = !0),
            this.cache.checkpoints.length > 0 &&
              (this.cache.checkpoints.length = 0),
            this.cache.totalLines !== 0 && (this.cache.totalLines = 0),
            e &&
              ((this.cache.estimatedSplitHeight = void 0),
              (this.cache.estimatedUnifiedHeight = void 0)),
            this.renderRange != null && t && (this.renderRange = void 0));
        }
        reconcileHeights() {
          let e = !1,
            { overflow: t = `scroll` } = this.options;
          if (this.fileContainer == null || this.fileDiff == null)
            return (this.height !== 0 && (e = !0), (this.height = 0), e);
          if (
            ((this.top = this.getVirtualizedTop()),
            t === `scroll` &&
              this.lineAnnotations.length === 0 &&
              !this.isResizeDebuggingEnabled())
          )
            return e;
          let n = this.getDiffStyle(),
            r =
              n === `split`
                ? [this.codeDeletions, this.codeAdditions]
                : [this.codeUnified],
            i = this.hasFileAnnotations(this.fileDiff);
          if (this.renderRange != null && i && Lh(this.renderRange)) {
            let t = Xb(r) ?? 0;
            this.setFileAnnotationHeight(t) && (e = !0);
          } else !i && this.setFileAnnotationHeight(0) && (e = !0);
          for (let t of r) {
            if (t == null) continue;
            let r = t.children[1];
            if (r instanceof HTMLElement)
              for (let t of r.children) {
                if (!(t instanceof HTMLElement)) continue;
                let r = t.dataset.lineIndex;
                if (r == null) continue;
                let i = cx(r, n),
                  a = t.getBoundingClientRect().height,
                  o = !1;
                t.nextElementSibling instanceof HTMLElement &&
                  (`lineAnnotation` in t.nextElementSibling.dataset ||
                    `noNewline` in t.nextElementSibling.dataset) &&
                  (`noNewline` in t.nextElementSibling.dataset && (o = !0),
                  (a += t.nextElementSibling.getBoundingClientRect().height));
                let s = this.getEstimatedLineHeight(o),
                  c = this.cache.heightDeltas.get(i) ?? 0,
                  l = a - s;
                l !== c &&
                  ((e = !0),
                  (this.cache.measuredHeightDeltaTotal += l - c),
                  l === 0
                    ? this.cache.heightDeltas.delete(i)
                    : this.cache.heightDeltas.set(i, l));
              }
          }
          return (
            (e || this.isResizeDebuggingEnabled()) &&
              this.computeApproximateSize(!0),
            e
          );
        }
        onRender = (e) =>
          this.fileContainer == null
            ? !1
            : ((e || this.top == null) && (this.top = this.getVirtualizedTop()),
              this.render());
        prepareCodeViewItem(e, t, n, r) {
          let i = !t_(this.fileDiff, e),
            a = this.syncLineAnnotations(r),
            o = n?.resetDiffLayoutCache === !0 || i || a,
            s =
              i ||
              (n?.resetDiffLayoutCache === !0 && n.includeEstimatedDiffHeights);
          n?.metrics != null &&
            ((this.metrics = Ug(n.metrics)), (o = !0), (s = !0));
          let { collapsed: c = !1 } = this.options;
          return (
            this.currentCollapsed !== c &&
              ((this.currentCollapsed = c), (o = !0)),
            o && this.resetLayoutCache({ includeEstimatedHeights: s }),
            (this.fileDiff = e),
            (this.top = t),
            this.computeApproximateSize(),
            this.height
          );
        }
        getLinePosition(e, t = `additions`) {
          if (this.fileDiff == null || e < 1) return;
          let n = this.getLineIndex(e, t);
          if (n == null) return;
          let {
              disableFileHeader: r = !1,
              expandUnchanged: i = !1,
              collapsed: a = !1,
              collapsedContextThreshold: o = 1,
            } = this.options,
            s = this.getDiffStyle(),
            c = this.getHunkSeparatorType(),
            l = s === `split` ? n[1] : n[0];
          this.approximateLayoutCheckpoints();
          let u = Wg(this.metrics, r),
            d = this.getLayoutCheckpointBeforeLineIndex(l),
            f = d?.top ?? u + this.cache.fileAnnotationHeight;
          if (a) return { top: u, height: 0 };
          let p;
          return (
            jy({
              diff: this.fileDiff,
              diffStyle: s,
              startingLine: d?.renderedLineIndex ?? 0,
              expandedHunks: i ? !0 : this.hunksRenderer.getExpandedHunksMap(),
              collapsedContextThreshold: o,
              callback: ({
                hunkIndex: e,
                hunk: t,
                collapsedBefore: n,
                collapsedAfter: r,
                deletionLine: i,
                additionLine: a,
              }) => {
                let o =
                  s === `split`
                    ? (a?.splitLineIndex ?? i?.splitLineIndex)
                    : (a?.unifiedLineIndex ?? i?.unifiedLineIndex);
                if (o == null)
                  throw Error(
                    `VirtualizedFileDiff.getLinePosition: missing line index data`,
                  );
                if (n > 0) {
                  let r = Oy({
                    type: c,
                    metrics: this.metrics,
                    hunkIndex: e,
                    hunkSpecs: t?.hunkSpecs,
                  });
                  if (r != null) {
                    if (((f += r.gapBefore), l >= o - n && l < o))
                      return ((p = { top: f, height: r.height }), !0);
                    f += r.height + r.gapAfter;
                  }
                }
                let u = this.getLineHeight(
                  o,
                  (a?.noEOFCR ?? !1) || (i?.noEOFCR ?? !1),
                );
                if (o === l) return ((p = { top: f, height: u }), !0);
                if (((f += u), r > 0)) {
                  let e = ky({ type: c, metrics: this.metrics });
                  if (e != null) {
                    if (l > o && l <= o + r)
                      return (
                        (p = { top: f + e.gapBefore, height: e.height }),
                        !0
                      );
                    f += e.totalHeight;
                  }
                }
                return !1;
              },
            }),
            p
          );
        }
        getNumericScrollAnchor(e) {
          if (this.fileDiff == null) return;
          let {
            disableFileHeader: t = !1,
            expandUnchanged: n = !1,
            collapsed: r = !1,
            collapsedContextThreshold: i = 1,
          } = this.options;
          if (r) return;
          let a = this.getDiffStyle(),
            o = this.getHunkSeparatorType();
          this.approximateLayoutCheckpoints();
          let s = this.getLayoutCheckpointBeforeTop(e),
            c = s?.top ?? Wg(this.metrics, t) + this.cache.fileAnnotationHeight,
            l;
          return (
            jy({
              diff: this.fileDiff,
              diffStyle: a,
              startingLine: s?.renderedLineIndex ?? 0,
              expandedHunks: n ? !0 : this.hunksRenderer.getExpandedHunksMap(),
              collapsedContextThreshold: i,
              callback: ({
                hunkIndex: t,
                hunk: n,
                collapsedBefore: r,
                collapsedAfter: i,
                deletionLine: s,
                additionLine: u,
              }) => {
                let d =
                  a === `split`
                    ? (u?.splitLineIndex ?? s?.splitLineIndex)
                    : (u?.unifiedLineIndex ?? s?.unifiedLineIndex);
                if (d == null)
                  throw Error(
                    `VirtualizedFileDiff.getNumericScrollAnchor: missing line index data`,
                  );
                if (r > 0) {
                  let e = Oy({
                    type: o,
                    metrics: this.metrics,
                    hunkIndex: t,
                    hunkSpecs: n?.hunkSpecs,
                  });
                  e != null && (c += e.totalHeight);
                }
                if (
                  c >= e &&
                  (s == null
                    ? u != null &&
                      (l = {
                        lineNumber: u.lineNumber,
                        side: `additions`,
                        top: c,
                      })
                    : (l = {
                        lineNumber: s.lineNumber,
                        side: `deletions`,
                        top: c,
                      }),
                  l != null)
                )
                  return !0;
                let f = this.getLineHeight(
                  d,
                  (u?.noEOFCR ?? !1) || (s?.noEOFCR ?? !1),
                );
                if (((c += f), i > 0)) {
                  let e = ky({ type: o, metrics: this.metrics });
                  e != null && (c += e.totalHeight);
                }
                return !1;
              },
            }),
            l
          );
        }
        getVirtualizedHeight() {
          return this.height;
        }
        getAdvancedStickySpecs(e) {
          if (this.top == null || this.fileDiff == null) return;
          if (this.options.collapsed === !0)
            return { topOffset: this.top, height: this.height };
          let t =
            e == null
              ? this.renderRange
              : this.computeRenderRangeFromWindow(this.fileDiff, this.top, e);
          if (t == null) return;
          let { bufferBefore: n, bufferAfter: r, totalLines: i } = t,
            a = 0;
          if (i === 0) {
            let t = e ?? this.virtualizer.getWindowSpecs();
            this.top < t.top && (a = r);
          }
          return { topOffset: this.top + n + a, height: this.height - (n + r) };
        }
        cleanUp(e = !1) {
          (this.fileContainer != null &&
            this.isSimpleMode() &&
            this.getSimpleVirtualizer()?.disconnect(this.fileContainer),
            e ||
              (this.resetLayoutCache({ includeEstimatedHeights: !0 }),
              (this.pendingExpansions = void 0),
              (this.pendingHydratedDiff = void 0)),
            (this.isSetup = !1),
            super.cleanUp(e));
        }
        expandHunk = (e, t, n) => {
          this.fileDiff != null &&
            (this.isAdvancedMode()
              ? ((this.pendingExpansions ??= []),
                this.pendingExpansions.push({
                  hunkIndex: e,
                  direction: t,
                  expansionLineCountOverride: n,
                }))
              : (this.hunksRenderer.expandHunk(e, t, n),
                this.resetLayoutCache({ includeEstimatedHeights: !0 }),
                this.computeApproximateSize()),
            this.loadFilesIfNecessary(),
            (this.forceRenderOverride = !0),
            this.virtualizer.instanceChanged(this, !0));
        };
        async handleFilesLoaded(e, t) {
          if (!(this.fileDiff !== e || !e.isPartial)) {
            if (this.isAdvancedMode()) {
              let n = s_(`clone`, e, t);
              if (
                (await Ab(() => this.primeHighlightCache(n)),
                !this.enabled || this.fileDiff !== e)
              )
                return;
              this.pendingHydratedDiff = {
                expectedDiff: e,
                nextDiff: n,
                files: t,
              };
            } else {
              if (
                (s_(`merge`, e, t),
                this.setHydratedState(t),
                await Ab(() => this.primeHighlightCache(e)),
                !this.enabled || this.fileDiff !== e)
              )
                return;
              (this.resetLayoutCache({ includeEstimatedHeights: !0 }),
                this.computeApproximateSize());
            }
            ((this.forceRenderOverride = !0),
              this.virtualizer.instanceChanged(this, !0));
          }
        }
        consumeCodeViewLayoutChanges(e) {
          let t = !1,
            n,
            { pendingExpansions: r, pendingHydratedDiff: i } = this;
          if (r != null) {
            this.pendingExpansions = void 0;
            for (let e of r)
              (this.hunksRenderer.expandHunk(
                e.hunkIndex,
                e.direction,
                e.expansionLineCountOverride,
              ),
                (t = !0));
          }
          return (
            i != null &&
              ((this.pendingHydratedDiff = void 0),
              i.expectedDiff === e &&
                (this.setHydratedState(i.files), (n = i.nextDiff))),
            n == null
              ? t &&
                ((this.forceRenderOverride = !0),
                this.invalidateDerivedLayoutCache(!0))
              : ((this.forceRenderOverride = !0),
                this.resetLayoutCache({ includeEstimatedHeights: !0 })),
            n
          );
        }
        loadFilesIfNecessary() {
          if (this.pendingHydratedDiff != null) {
            if (this.pendingHydratedDiff.expectedDiff === this.fileDiff) return;
            this.pendingHydratedDiff = void 0;
          }
          super.loadFilesIfNecessary();
        }
        setVisibility(e) {
          if (!(this.isAdvancedMode() || this.fileContainer == null)) {
            if (((this.renderRange = void 0), this.top == null)) {
              this.isVisible = e;
              return;
            }
            e && !this.isVisible
              ? ((this.top = this.getVirtualizedTop()), (this.isVisible = !0))
              : !e &&
                this.isVisible &&
                ((this.isVisible = !1), this.rerender());
          }
        }
        rerender() {
          !this.enabled ||
            (this.fileDiff == null &&
              this.additionFile == null &&
              this.deletionFile == null) ||
            ((this.forceRenderOverride = !0),
            this.virtualizer.instanceChanged(this, !1));
        }
        applyDocumentChange(e, t, n = !1) {
          let r = this.renderRange;
          if (
            (super.applyDocumentChange(e, t),
            this.getSimpleVirtualizer()?.markDOMDirty(),
            this.resetLayoutCache({
              forceSimpleRecompute: this.isSimpleMode(),
              includeEstimatedHeights: !0,
              resetRenderRange: !1,
            }),
            this.isSimpleMode() || this.computeApproximateSize(!0),
            n && r !== void 0 && this.fileDiff !== void 0)
          ) {
            let e = this.virtualizer.getWindowSpecs(),
              t = this.computeRenderRangeFromWindow(
                this.fileDiff,
                this.top ?? 0,
                e,
              );
            t.bufferAfter !== r.bufferAfter && this.updateBuffers(t);
          }
        }
        computeApproximateSize(e = !1, t = this.fileDiff) {
          let n = this.isResizeDebuggingEnabled();
          if (!e && !this.layoutDirty && !n) return;
          let r = this.height === 0;
          if (
            ((this.height = 0),
            (this.cache.checkpoints = []),
            (this.cache.totalLines = 0),
            t == null)
          ) {
            this.layoutDirty = !1;
            return;
          }
          let { disableFileHeader: i = !1, collapsed: a = !1 } = this.options,
            o = Wg(this.metrics, i);
          if (((this.height += o), a)) {
            this.layoutDirty = !1;
            return;
          }
          ((this.height =
            this.getActiveEstimatedHeight(t) +
            this.cache.measuredHeightDeltaTotal),
            n && !r && this.validateComputedHeight(t),
            (this.layoutDirty = !1));
        }
        getActiveEstimatedHeight(e = this.fileDiff) {
          this.ensureEstimatedDiffHeights(e);
          let t =
            this.getDiffStyle() === `split`
              ? this.cache.estimatedSplitHeight
              : this.cache.estimatedUnifiedHeight;
          if (t == null)
            throw Error(
              `VirtualizedFileDiff.getActiveEstimatedHeight: missing estimated height`,
            );
          return t;
        }
        ensureEstimatedDiffHeights(e = this.fileDiff) {
          if (e == null) {
            ((this.cache.estimatedSplitHeight = void 0),
              (this.cache.estimatedUnifiedHeight = void 0));
            return;
          }
          if (
            this.cache.estimatedSplitHeight != null &&
            this.cache.estimatedUnifiedHeight != null
          )
            return;
          let {
              disableFileHeader: t = !1,
              expandUnchanged: n = !1,
              collapsedContextThreshold: r = 1,
            } = this.options,
            { splitHeight: i, unifiedHeight: a } = Kb({
              fileDiff: e,
              metrics: this.metrics,
              disableFileHeader: t,
              hunkSeparators: this.getHunkSeparatorType(),
              expandUnchanged: n,
              expandedHunks: this.hunksRenderer.getExpandedHunksMap(),
              collapsedContextThreshold: r,
              canHydratePartialDiff: ax(e, this.options.loadDiffFiles != null),
            });
          ((this.cache.estimatedSplitHeight = i),
            (this.cache.estimatedUnifiedHeight = a));
        }
        validateComputedHeight(e = this.fileDiff) {
          if (this.fileContainer == null || e == null) return;
          let t = this.fileContainer.getBoundingClientRect();
          t.height === this.height
            ? console.log(
                `VirtualizedFileDiff.computeApproximateSize: computed height IS CORRECT`,
              )
            : console.log(
                `VirtualizedFileDiff.computeApproximateSize: computed height doesnt match`,
                {
                  name: e.name,
                  elementHeight: t.height,
                  computedHeight: this.height,
                },
              );
        }
        render({
          fileContainer: e,
          fileDiff: t,
          forceRender: n = !1,
          lineAnnotations: r,
          ...i
        } = {}) {
          let a = Mb(i, `VirtualizedFileDiff.render`),
            o = a != null,
            s = a?.oldFile,
            c = a?.newFile,
            l = o && (!sx(s, this.deletionFile) || !sx(c, this.additionFile)),
            u = t ?? this.fileDiff;
          t == null &&
            o &&
            (l || this.fileDiff == null) &&
            (u = Xv(a.oldFile, a.newFile, this.options.parseDiffOptions));
          let { forceRenderOverride: d, isSetup: f } = this;
          this.forceRenderOverride = void 0;
          let p = this.syncLineAnnotations(r);
          p && this.resetLayoutCache({ includeEstimatedHeights: !1 });
          let m = t != null && t !== this.fileDiff,
            h = u != null && !t_(this.fileDiff, u),
            g = m || l;
          if (
            (h && this.resetLayoutCache({ includeEstimatedHeights: !0 }),
            (e = this.getOrCreateFileContainer(e)),
            u == null)
          )
            return (
              console.error(
                `VirtualizedFileDiff.render: attempting to virtually render when we dont have the correct data`,
              ),
              !1
            );
          h &&
            f &&
            (this.computeApproximateSize(!1, u),
            this.isSimpleMode() && this.virtualizer.instanceChanged(this, !0));
          let _ = !1;
          if (f)
            this.top == null && this.isSimpleMode()
              ? (_ = !0)
              : (this.top ??= this.getVirtualizedTop());
          else {
            this.computeApproximateSize(!1, u);
            let t = this.getSimpleVirtualizer();
            if (this.isAdvancedMode())
              ((this.top ??= this.getVirtualizedTop()), (this.isVisible = !0));
            else {
              if (t == null)
                throw Error(
                  `VirtualizedFileDiff.render: simple virtualizer is not available`,
                );
              (t.connect(e, this), (_ = !0));
            }
            this.isSetup = !0;
          }
          if (this.isSimpleMode() && (_ || (!this.isVisible && (!g || !f))))
            return (
              (this.fileDiff = u),
              a != null && ((this.deletionFile = s), (this.additionFile = c)),
              h && (this.cachedHeaderHTML = void 0),
              this.renderPlaceholder(this.height, _)
            );
          let v = this.virtualizer.getWindowSpecs(),
            y = this.top ?? 0,
            b = this.computeRenderRangeFromWindow(u, y, v);
          return super.render({
            fileDiff: u,
            fileContainer: e,
            renderRange: b,
            lineAnnotations: r,
            forceRender: (d ?? n) || p || h,
            ...a,
            ...i,
          });
        }
        syncVirtualizedTop() {
          this.top = this.getVirtualizedTop();
        }
        shouldDisableVirtualizationBuffers() {
          return (
            this.isAdvancedMode() || super.shouldDisableVirtualizationBuffers()
          );
        }
        isSimpleMode() {
          return this.virtualizer.type === `simple`;
        }
        isAdvancedMode() {
          return this.virtualizer.type === `advanced`;
        }
        getVirtualizedTop() {
          return this.virtualizer.type === `advanced`
            ? this.virtualizer.getLocalTopForInstance(this)
            : this.fileContainer == null
              ? 0
              : this.virtualizer.getOffsetInScrollContainer(this.fileContainer);
        }
        getSimpleVirtualizer() {
          return this.virtualizer.type === `simple` ? this.virtualizer : void 0;
        }
        isResizeDebuggingEnabled() {
          return this.getSimpleVirtualizer()?.config.resizeDebugging ?? !1;
        }
        getDiffStyle() {
          return this.options.diffStyle ?? `split`;
        }
        getHunkSeparatorType() {
          return ox(this.options.hunkSeparators);
        }
        approximateLayoutCheckpoints(e = this.fileDiff) {
          if (
            (!this.layoutDirty && this.cache.checkpoints.length > 0) ||
            e == null ||
            e.hunks.length === 0 ||
            this.options.collapsed === !0
          )
            return;
          let {
              disableFileHeader: t = !1,
              expandUnchanged: n = !1,
              collapsedContextThreshold: r = 1,
            } = this.options,
            i = e.hunks.length - 1,
            a = ax(e, this.options.loadDiffFiles != null),
            o = this.getDiffStyle(),
            s = this.getHunkSeparatorType(),
            c = n ? !0 : this.hunksRenderer.getExpandedHunksMap(),
            l = Zb(this.cache.heightDeltas),
            u = Wg(this.metrics, t) + this.cache.fileAnnotationHeight,
            d = 0,
            f = ({
              rowCount: e,
              startLineIndex: t,
              preSeparatorHeight: n = 0,
              postSeparatorHeight: r = 0,
              metadataOffsets: i = [],
            }) => {
              if (e <= 0) return;
              let a = d,
                o = d + e,
                s = ex(a);
              for (; s < o;) {
                let e = s - a,
                  r =
                    u +
                    (e > 0 ? n : 0) +
                    e * this.metrics.lineHeight +
                    tx(i, e) * this.metrics.lineHeight +
                    Qb(l, t, t + e);
                (this.cache.checkpoints.push({
                  renderedLineIndex: s,
                  lineIndex: t + e,
                  top: r,
                }),
                  (s += lx));
              }
              ((u +=
                n +
                e * this.metrics.lineHeight +
                i.length * this.metrics.lineHeight +
                Qb(l, t, t + e) +
                r),
                (d = o));
            };
          for (let t = 0; t < e.hunks.length; t++) {
            let n = e.hunks[t];
            if (n == null)
              throw Error(
                `VirtualizedFileDiff.approximateLayoutCheckpoints: invalid hunk index`,
              );
            let l = by({
                isPartial: e.isPartial,
                rangeSize: n.collapsedBefore,
                expandedHunks: c,
                hunkIndex: t,
                collapsedContextThreshold: r,
              }),
              u =
                l.collapsedLines > 0
                  ? (Oy({
                      type: s,
                      metrics: this.metrics,
                      hunkIndex: t,
                      hunkSpecs: n.hunkSpecs,
                    })?.totalHeight ?? 0)
                  : 0;
            f({
              rowCount: l.fromStart,
              startLineIndex:
                (o === `split` ? n.splitLineStart : n.unifiedLineStart) -
                l.rangeSize,
            });
            let d = u;
            (f({
              rowCount: l.fromEnd,
              startLineIndex:
                (o === `split` ? n.splitLineStart : n.unifiedLineStart) -
                l.fromEnd,
              preSeparatorHeight: d,
            }),
              l.fromEnd > 0 && (d = 0));
            let p =
                t === i
                  ? Cy({
                      fileDiff: e,
                      hunkIndex: t,
                      expandedHunks: c,
                      collapsedContextThreshold: r,
                      errorPrefix: `VirtualizedFileDiff`,
                    })
                  : void 0,
              m =
                (p != null && p.collapsedLines > 0) || (t === i && a)
                  ? (ky({ type: s, metrics: this.metrics })?.totalHeight ?? 0)
                  : 0,
              h = p == null ? 0 : p.fromStart + p.fromEnd,
              g = o === `split` ? n.splitLineCount : n.unifiedLineCount,
              _ = o === `split` ? n.splitLineStart : n.unifiedLineStart;
            (f({
              rowCount: g,
              startLineIndex: _,
              preSeparatorHeight: d,
              postSeparatorHeight: h === 0 ? m : 0,
              metadataOffsets: nx({ diffStyle: o, hunk: n, rowCount: g }),
            }),
              p != null &&
                h > 0 &&
                f({
                  rowCount: h,
                  startLineIndex: _ + g,
                  postSeparatorHeight: m,
                }));
          }
          this.cache.totalLines = d;
        }
        getLayoutCheckpointBeforeLineIndex(e) {
          if (e <= 0 || this.cache.checkpoints.length === 0) return;
          let t = 0,
            n = this.cache.checkpoints.length - 1,
            r;
          for (; t <= n;) {
            let i = (t + n) >> 1,
              a = this.cache.checkpoints[i];
            if (a == null)
              throw Error(`VirtualizedFileDiff: invalid checkpoint index`);
            a.lineIndex <= e ? ((r = a), (t = i + 1)) : (n = i - 1);
          }
          return r;
        }
        getLayoutCheckpointBeforeTop(e, t) {
          let n = 0,
            r = this.cache.checkpoints.length - 1,
            i = -1;
          for (; n <= r;) {
            let t = (n + r) >> 1,
              a = this.cache.checkpoints[t];
            if (a == null)
              throw Error(`VirtualizedFileDiff: invalid checkpoint index`);
            a.top <= e ? ((i = t), (n = t + 1)) : (r = t - 1);
          }
          if (t == null) return i >= 0 ? this.cache.checkpoints[i] : void 0;
          for (let e = i; e >= 0; e--) {
            let n = this.cache.checkpoints[e];
            if (n == null)
              throw Error(`VirtualizedFileDiff: invalid checkpoint index`);
            if (n.renderedLineIndex % t === 0) return n;
          }
        }
        getExpandedLineCount(e, t) {
          let n = 0;
          if (e.isPartial) {
            for (let r of e.hunks)
              n += t === `split` ? r.splitLineCount : r.unifiedLineCount;
            return n;
          }
          let { expandUnchanged: r = !1, collapsedContextThreshold: i = 1 } =
              this.options,
            a = r ? !0 : this.hunksRenderer.getExpandedHunksMap();
          for (let [r, o] of e.hunks.entries()) {
            let s = t === `split` ? o.splitLineCount : o.unifiedLineCount;
            n += s;
            let c = Math.max(o.collapsedBefore, 0),
              {
                fromStart: l,
                fromEnd: u,
                renderAll: d,
              } = by({
                isPartial: e.isPartial,
                rangeSize: c,
                expandedHunks: a,
                hunkIndex: r,
                collapsedContextThreshold: i,
              });
            c > 0 && (n += d ? c : l + u);
          }
          let o = Cy({
            fileDiff: e,
            hunkIndex: e.hunks.length - 1,
            expandedHunks: a,
            collapsedContextThreshold: i,
            errorPrefix: `VirtualizedFileDiff`,
          });
          return (o != null && (n += o.fromStart + o.fromEnd), n);
        }
        getLayoutLineCount(e, t) {
          let n = this.getExpandedLineCount(e, t),
            r = t === `split` ? e.splitLineCount : e.unifiedLineCount;
          return Math.max(
            n,
            r,
            e.additionLines.length,
            e.deletionLines.length,
            this.cache.totalLines,
          );
        }
        computeRenderRangeFromWindow(e, t, { top: n, bottom: r }) {
          let {
              disableFileHeader: i = !1,
              expandUnchanged: a = !1,
              collapsedContextThreshold: o = 1,
            } = this.options,
            { hunkLineCount: s, lineHeight: c } = this.metrics,
            l = this.getDiffStyle(),
            u = this.getHunkSeparatorType(),
            d = ax(e, this.options.loadDiffFiles != null),
            f = this.height,
            p = this.getLayoutLineCount(e, l),
            m = Wg(this.metrics, i),
            h = e.hunks.length > 0 ? Kg(this.metrics) : 0,
            { fileAnnotationHeight: g } = this.cache,
            _ = m + g,
            v = Math.max(0, f - m - g - h),
            y = this.hasFileAnnotations(e),
            b = t + m,
            x = g > 0 && y && b < r && b + g > n;
          if (t < n - f || t > r)
            return {
              startingLine: 0,
              totalLines: 0,
              bufferBefore: 0,
              bufferAfter: f - m - h,
            };
          if (p <= s || e.hunks.length === 0)
            return {
              startingLine: 0,
              totalLines: s,
              bufferBefore: 0,
              bufferAfter: 0,
            };
          (this.approximateLayoutCheckpoints(e),
            (p = this.getLayoutLineCount(e, l)));
          let S = Math.ceil(Math.max(r - n, 0) / c),
            C = Math.ceil(S / s) * s + s,
            ee = C / s,
            w = ee,
            T = [],
            te = (n + r) / 2,
            E = this.getLayoutCheckpointBeforeTop(
              Math.max(0, n - t - C * c * 2),
              s,
            ),
            D = t + (E?.top ?? _),
            ne = E?.renderedLineIndex ?? 0,
            O,
            re,
            ie;
          if (
            (jy({
              diff: e,
              diffStyle: l,
              startingLine: E?.renderedLineIndex ?? 0,
              expandedHunks: a ? !0 : this.hunksRenderer.getExpandedHunksMap(),
              collapsedContextThreshold: o,
              callback: ({
                hunkIndex: i,
                hunk: a,
                collapsedBefore: o,
                collapsedAfter: c,
                deletionLine: f,
                additionLine: p,
              }) => {
                let m = p == null ? f.splitLineIndex : p.splitLineIndex,
                  h = p == null ? f.unifiedLineIndex : p.unifiedLineIndex,
                  g = (p?.noEOFCR ?? !1) || (f?.noEOFCR ?? !1),
                  v =
                    i === e.hunks.length - 1 &&
                    a != null &&
                    (l === `split`
                      ? m === a.splitLineStart + a.splitLineCount - 1
                      : h === a.unifiedLineStart + a.unifiedLineCount - 1),
                  y =
                    (o > 0
                      ? Oy({
                          type: u,
                          metrics: this.metrics,
                          hunkIndex: i,
                          hunkSpecs: a?.hunkSpecs,
                        })
                      : void 0
                    )?.totalHeight ?? 0;
                D += y;
                let b = ne % s === 0,
                  x = Math.floor(ne / s);
                if (b && ((T[x] = D - (t + _ + y)), ie != null)) {
                  if (ie <= 0) return !0;
                  ie--;
                }
                let S = this.getLineHeight(l === `split` ? m : h, g);
                if (
                  (D > n - S && D < r && (O ??= x),
                  re == null && D + S > te && (re = x),
                  ie == null && D >= r && b && (ie = w),
                  ne++,
                  (D += S),
                  c > 0 || (v && d))
                ) {
                  let e = ky({ type: u, metrics: this.metrics });
                  e != null &&
                    (D < r && D + e.totalHeight > n && (O ??= x),
                    re == null && D + e.totalHeight > te && (re = x),
                    (D += e.totalHeight));
                }
                return !1;
              },
            }),
            O == null)
          )
            if (x) ((O = 0), (re = 0));
            else
              return {
                startingLine: 0,
                totalLines: 0,
                bufferBefore: 0,
                bufferAfter: f - m - h,
              };
          re ??= O;
          let ae = Math.round(re - ee / 2),
            oe = Math.max(0, Math.ceil(p / s) - ee),
            k = Math.max(0, Math.min(ae, oe)),
            A = k * s,
            j = ae < 0 ? C + ae * s : C,
            se = T[k] ?? 0,
            ce = A === 0 ? 0 : g + se,
            le = k + j / s,
            ue = le < T.length ? v - T[le] : v - (D - t - _);
          return {
            startingLine: A,
            totalLines: j,
            bufferBefore: ce,
            bufferAfter: Math.max(0, ue),
          };
        }
      }));
  });
function px(e) {
  let t = window.devicePixelRatio ?? 1;
  return Math.round(e * t) / t;
}
var mx = e(() => {});
function hx(e, t) {
  Object.defineProperty(e, Px, { configurable: !1, enumerable: !1, value: t });
}
function gx(e) {
  return e[Px];
}
function _x(e, t, n) {
  Object.defineProperty(e, t, {
    configurable: !1,
    enumerable: !0,
    get() {
      return n(this);
    },
  });
}
function vx(e) {
  return (
    e.instance.cleanUp(!0),
    e.type === `diff`
      ? e.instance.prepareCodeViewItem(
          e.item.fileDiff,
          e.top,
          void 0,
          e.item.annotations ?? [],
        )
      : e.instance.prepareCodeViewItem(
          e.item.file,
          e.top,
          void 0,
          e.item.annotations ?? [],
        )
  );
}
function yx(e, t) {
  return (
    !en(e.theme ?? P, t.theme ?? P) ||
    (e.themeType ?? `system`) !== (t.themeType ?? `system`) ||
    e.unsafeCSS !== t.unsafeCSS
  );
}
function bx(e, t) {
  return (
    (e.overflow ?? `scroll`) !== (t.overflow ?? `scroll`) ||
    (e.disableLineNumbers ?? !1) !== (t.disableLineNumbers ?? !1) ||
    (e.disableFileHeader ?? !1) !== (t.disableFileHeader ?? !1) ||
    e.unsafeCSS !== t.unsafeCSS ||
    (e.diffStyle ?? `split`) !== (t.diffStyle ?? `split`) ||
    (e.diffIndicators ?? `bars`) !== (t.diffIndicators ?? `bars`) ||
    (e.hunkSeparators ?? `line-info`) !== (t.hunkSeparators ?? `line-info`) ||
    (e.expandUnchanged ?? !1) !== (t.expandUnchanged ?? !1) ||
    (e.collapsedContextThreshold ?? 1) !== (t.collapsedContextThreshold ?? 1)
  );
}
function xx(e, t) {
  return (
    (e.disableFileHeader ?? !1) !== (t.disableFileHeader ?? !1) ||
    (e.hunkSeparators ?? `line-info`) !== (t.hunkSeparators ?? `line-info`) ||
    (e.expandUnchanged ?? !1) !== (t.expandUnchanged ?? !1) ||
    (e.collapsedContextThreshold ?? 1) !== (t.collapsedContextThreshold ?? 1)
  );
}
function Sx(e) {
  return e instanceof SVGElement
    ? !0
    : kg(e) &&
        (e.hasAttribute(`data-core-css`) ||
          e.hasAttribute(`data-theme-css`) ||
          e.hasAttribute(`data-unsafe-css`));
}
function Cx(e) {
  let t = wx(e.start, e.side),
    n = wx(e.end, e.endSide ?? e.side);
  return t === n ? t : `${t}-${n}`;
}
function wx(e, t) {
  return t == null ? `${e}` : `${t === `deletions` ? `D` : `A`}${e}`;
}
function Tx(e, t, n = !1) {
  return e.type === `diff`
    ? e.instance.render({
        deferManagers: !0,
        fileContainer: t,
        fileDiff: e.item.fileDiff,
        forceRender: n,
        lineAnnotations: e.item.annotations ?? [],
      })
    : e.instance.render({
        deferManagers: !0,
        fileContainer: t,
        file: e.item.file,
        forceRender: n,
        lineAnnotations: e.item.annotations ?? [],
      });
}
function Ex(e, t, n) {
  if (n == null) {
    e.firstChild !== t && e.prepend(t);
    return;
  }
  n.nextSibling !== t && n.after(t);
}
function Dx(e) {
  return (e.annotations?.length ?? 0) > 0;
}
function Ox(
  e,
  { hasHeaderRenderers: t, hasAnnotationRenderer: n, hasGutterRenderer: r },
) {
  if (e.length === 0) return;
  if (t || r) return e;
  if (!n) return;
  let i = [];
  for (let t of e) Dx(t.item) && i.push(t);
  return i.length > 0 ? i : void 0;
}
function kx(e, t) {
  if (e == null || t == null) return e === t;
  if (e.length !== t.length) return !1;
  for (let n = 0; n < e.length; n++) {
    let r = e[n],
      i = t[n];
    if (
      r == null ||
      i == null ||
      r.id !== i.id ||
      r.type !== i.type ||
      r.element !== i.element ||
      r.version !== i.version
    )
      return !1;
  }
  return !0;
}
var Ax,
  jx,
  Mx,
  Nx,
  Px,
  Fx,
  Ix,
  Lx,
  Rx,
  zx,
  Bx,
  Vx,
  Hx,
  Ux,
  Wx = e(() => {
    (F(),
      Zt(),
      $t(),
      tn(),
      an(),
      sn(),
      ln(),
      Ag(),
      dn(),
      mx(),
      e_(),
      fx(),
      (Ax =
        `theme.disableLineNumbers.overflow.themeType.disableFileHeader.disableVirtualizationBuffers.preferredHighlighter.useCSSClasses.useTokenTransformer.tokenizeMaxLineLength.tokenizeMaxLength.unsafeCSS.diffStyle.diffIndicators.disableBackground.expandUnchanged.loadDiffFiles.collapsedContextThreshold.lineDiffType.maxLineDiffLength.expansionLineCount.lineHoverHighlight.enableTokenInteractionsOnWhitespace.enableGutterUtility.__debugPointerEvents.enableLineSelection.controlledSelection.disableErrorHandling`.split(
          `.`,
        )),
      (jx = [
        `theme`,
        `disableLineNumbers`,
        `overflow`,
        `themeType`,
        `disableFileHeader`,
        `disableVirtualizationBuffers`,
        `preferredHighlighter`,
        `useCSSClasses`,
        `useTokenTransformer`,
        `tokenizeMaxLineLength`,
        `tokenizeMaxLength`,
        `unsafeCSS`,
        `lineHoverHighlight`,
        `enableTokenInteractionsOnWhitespace`,
        `enableGutterUtility`,
        `__debugPointerEvents`,
        `enableLineSelection`,
        `controlledSelection`,
        `disableErrorHandling`,
      ]),
      (Mx = [
        `renderCustomHeader`,
        `renderHeaderPrefix`,
        `renderHeaderFilenameSuffix`,
        `renderHeaderMetadata`,
        `renderAnnotation`,
        `renderGutterUtility`,
        `onPostRender`,
        `onGutterUtilityClick`,
        `onLineClick`,
        `onLineNumberClick`,
        `onLineEnter`,
        `onLineLeave`,
        `onTokenClick`,
        `onTokenEnter`,
        `onTokenLeave`,
      ]),
      (Nx = [
        `onLineSelected`,
        `onLineSelectionStart`,
        `onLineSelectionChange`,
        `onLineSelectionEnd`,
      ]),
      (Px = Symbol(`CodeView.itemOptionsState`)),
      (Fx = 120),
      (Ix = `--diffs-overflow-override`),
      (Lx = 12e6),
      (Rx = 1e6),
      (zx = 2e6),
      (Bx = Lx - zx),
      (Vx = Lx - Rx),
      (Hx = (() => {
        let { navigator: e } = globalThis,
          t = e.userAgent,
          n = /iP(?:hone|ad|od)/.test(t),
          r = e.platform === `MacIntel` && e.maxTouchPoints > 1;
        return (
          (n || r) &&
          /AppleWebKit/.test(t) &&
          /Safari/.test(t) &&
          !/(CriOS|FxiOS|EdgiOS|OPiOS)/.test(t)
        );
      })()),
      (Ux = class e {
        static __STOP = !1;
        static __lastScrollPosition = 0;
        type = `advanced`;
        config = {
          overscrollSize: 200,
          intersectionObserverMargin: 0,
          resizeDebugging: !1,
        };
        items = [];
        idToItem = new Map();
        selectedLines = null;
        instanceToItem = new Map();
        layoutDirtyIndex;
        pendingLayoutReset;
        renderOptionsRevision = 0;
        slotCoordinator;
        slotSnapshot;
        scrollListeners = new Set();
        scrollHeight = 0;
        containerHeight = -1;
        scrollTop = 0;
        scrollPageOffset = 0;
        scrollDirty = !0;
        scrollInteractionFixTimer;
        pointerEventsDisabled = !1;
        codeOverflowFix = !1;
        height = 0;
        heightDirty = !0;
        windowSpecs = { top: 0, bottom: 0 };
        renderState = {
          scrollTop: -1,
          firstIndex: -1,
          lastIndex: -1,
          stickyHeight: 0,
          stickyTop: -1,
          stickyBottom: -1,
        };
        itemMetricsCache = Bt;
        fileOptionsPrototype;
        diffOptionsPrototype;
        pendingScrollTarget;
        pendingLayoutAnchor;
        shouldFixContainerFocus = !1;
        scrollAnimation;
        root;
        resizeObserver;
        container = document.createElement(`div`);
        stickyContainer = document.createElement(`div`);
        stickyOffset = document.createElement(`div`);
        elementPool = [];
        elementPoolVersion = 0;
        elementPoolTracker = new WeakMap();
        pendingElementPool = [];
        options;
        workerManager;
        isReadySubscription;
        isContainerManaged;
        constructor(e = { theme: P }, t, n = !1) {
          ((this.options = e),
            this.computeMetricsCache(e.itemMetrics),
            (this.fileOptionsPrototype = this.createFileOptionsPrototype()),
            (this.diffOptionsPrototype = this.createDiffOptionsPrototype()),
            (this.workerManager = t),
            (this.isContainerManaged = n),
            (this.stickyOffset.style.contain = `layout size`),
            (this.stickyContainer.style.position = `sticky`),
            (this.stickyContainer.style.width = `100%`),
            (this.stickyContainer.style.contain = `layout style inline-size`),
            (this.stickyContainer.style.isolation = `isolate`),
            (this.stickyContainer.style.display = `flex`),
            (this.stickyContainer.style.flexDirection = `column`));
        }
        getLayout() {
          return this.options.layout ?? Vt;
        }
        computeMetricsCache(e) {
          return (
            (this.itemMetricsCache = {
              hunkLineCount: e?.hunkLineCount ?? Bt.hunkLineCount,
              lineHeight: e?.lineHeight ?? Bt.lineHeight,
              diffHeaderHeight: e?.diffHeaderHeight ?? Bt.diffHeaderHeight,
              hunkSeparatorHeight: e?.hunkSeparatorHeight,
              spacing: e?.spacing ?? Bt.spacing,
              paddingTop: e?.paddingTop,
              paddingBottom: e?.paddingBottom,
            }),
            this.itemMetricsCache
          );
        }
        getSmoothScrollSettings() {
          return this.options.smoothScrollSettings ?? Ht;
        }
        shouldDisablePointerEvents() {
          return this.options.pointerEventsOnScroll !== !0;
        }
        shouldValidateItemHeights() {
          return St && this.options.__devOnlyValidateItemHeights === !0;
        }
        validateRenderedItemHeight(e) {
          if (!this.shouldValidateItemHeights() || e.element == null) return;
          let t = e.instance.getAdvancedStickySpecs();
          if (t == null) return;
          let n = t.height,
            r = e.element.getBoundingClientRect().height;
          n !== r &&
            console.error(
              `CodeView: reconciled item height does not match DOM height`,
              {
                id: e.item.id,
                type: e.type,
                index: e.index,
                version: e.version,
                expectedHeight: n,
                actualHeight: r,
                delta: r - n,
                stickyTopOffset: t.topOffset,
                virtualizedHeight: e.instance.getVirtualizedHeight(),
                top: e.top,
                scrollTop: this.getScrollTop(),
                windowSpecs: { ...this.windowSpecs },
                element: e.element,
                instance: e.instance,
              },
            );
        }
        validateStickyContainerHeight() {
          if (!this.shouldValidateItemHeights()) return;
          let {
            firstIndex: e,
            lastIndex: t,
            stickyHeight: n,
            stickyTop: r,
            stickyBottom: i,
          } = this.renderState;
          if (e === -1 || t === -1) return;
          let a = this.stickyContainer.getBoundingClientRect().height;
          Math.abs(a - n) < 1 ||
            console.error(
              `CodeView: sticky container height does not match computed layout`,
              {
                computedStickyHeight: n,
                actualStickyHeight: a,
                delta: a - n,
                stickyTop: r,
                stickyBottom: i,
                firstIndex: e,
                lastIndex: t,
                firstStickySpecs:
                  this.items[e]?.instance.getAdvancedStickySpecs(),
                lastStickySpecs:
                  this.items[t]?.instance.getAdvancedStickySpecs(),
                scrollTop: this.getScrollTop(),
                scrollPageOffset: this.scrollPageOffset,
                windowSpecs: { ...this.windowSpecs },
                stickyContainer: this.stickyContainer,
              },
            );
        }
        clearScrollInteractionTimer() {
          this.scrollInteractionFixTimer != null &&
            (clearTimeout(this.scrollInteractionFixTimer),
            (this.scrollInteractionFixTimer = void 0));
        }
        suspendScrollInteractions() {
          (this.clearScrollInteractionTimer(),
            this.shouldDisablePointerEvents() &&
              !this.pointerEventsDisabled &&
              ((this.stickyContainer.style.pointerEvents = `none`),
              (this.pointerEventsDisabled = !0)),
            Hx &&
              !this.codeOverflowFix &&
              (this.stickyContainer.style.setProperty(Ix, `hidden`),
              (this.codeOverflowFix = !0)),
            (this.scrollInteractionFixTimer = setTimeout(
              this.restoreScrollInteractions,
              Fx,
            )));
        }
        restoreScrollInteractions = () => {
          (this.clearScrollInteractionTimer(),
            (this.pointerEventsDisabled &&=
              (this.stickyContainer.style.removeProperty(`pointer-events`),
              !1)),
            (this.codeOverflowFix &&=
              (this.stickyContainer.style.setProperty(Ix, `auto`), !1)));
        };
        syncLayout() {
          let { gap: e, paddingBottom: t, paddingTop: n } = this.getLayout();
          ((this.stickyContainer.style.gap = `${e}px`),
            this.container?.style.setProperty(`margin-top`, `${n}px`),
            this.container?.style.setProperty(`margin-bottom`, `${t}px`));
        }
        setup(t) {
          if (this.root != null) throw Error(`CodeView.setup: already setup`);
          (this.workerManager?.subscribeToThemeChanges(this),
            (this.root = t),
            (this.root.style.overflowAnchor = `none`),
            this.root.hasAttribute(`tabindex`) || (this.root.tabIndex = -1),
            (this.container ??= document.createElement(`div`)),
            (this.container.style.contain = `layout style`),
            this.syncLayout(),
            this.container.appendChild(this.stickyOffset),
            this.container.appendChild(this.stickyContainer),
            this.root.appendChild(this.container),
            (this.scrollDirty = !0),
            (this.heightDirty = !0),
            (this.resizeObserver = new ResizeObserver(this.handleResize)),
            this.resizeObserver.observe(this.stickyContainer),
            this.root.addEventListener(`scroll`, this.handleScroll, {
              passive: !0,
            }),
            this.root.addEventListener(`wheel`, this.clearPendingScroll, {
              passive: !0,
            }),
            this.root.addEventListener(`touchstart`, this.clearPendingScroll, {
              passive: !0,
            }),
            this.root.addEventListener(`pointerdown`, this.clearPendingScroll, {
              passive: !0,
            }),
            this.root.addEventListener(`keydown`, this.clearPendingScroll, {
              passive: !0,
            }),
            this.resizeObserver.observe(this.root),
            this.render(!0),
            (window.__INSTANCE = this),
            (window.__TOGGLE = () => {
              e.__STOP
                ? ((e.__STOP = !1),
                  this.scrollTo({
                    type: `position`,
                    position: e.__lastScrollPosition,
                    behavior: `instant`,
                  }))
                : ((e.__lastScrollPosition = this.getScrollTop()),
                  (e.__STOP = !0));
            }));
        }
        reset() {
          (this.clearReadySubscription(),
            this.restoreScrollInteractions(),
            this.cleanAllRenderedItems(),
            (this.selectedLines = null),
            (this.items.length = 0),
            this.idToItem.clear(),
            this.instanceToItem.clear(),
            (this.layoutDirtyIndex = void 0),
            (this.pendingLayoutReset = void 0),
            (this.stickyContainer.textContent = ``),
            (this.stickyOffset.style.height = ``),
            this.container?.style.removeProperty(`height`),
            (this.containerHeight = -1),
            (this.windowSpecs = { top: 0, bottom: 0 }),
            (this.pendingLayoutAnchor = void 0),
            (this.shouldFixContainerFocus = !1),
            (this.height = 0),
            (this.scrollTop = 0),
            (this.scrollPageOffset = 0),
            (this.scrollHeight = 0),
            (this.scrollDirty = !0),
            (this.heightDirty = !0),
            this.resetRenderState(),
            this.isContainerManaged || this.flushSlotCoordinator());
        }
        cleanUp() {
          (this.reset(),
            this.clearElementPool(),
            this.restoreScrollInteractions(),
            this.workerManager?.unsubscribeToThemeChanges(this),
            this.resizeObserver?.disconnect(),
            (this.resizeObserver = void 0),
            this.root?.removeEventListener(`scroll`, this.handleScroll),
            this.root?.removeEventListener(`wheel`, this.clearPendingScroll),
            this.root?.removeEventListener(
              `touchstart`,
              this.clearPendingScroll,
            ),
            this.root?.removeEventListener(
              `pointerdown`,
              this.clearPendingScroll,
            ),
            this.root?.removeEventListener(`keydown`, this.clearPendingScroll),
            this.root?.style.removeProperty(`overflow-anchor`),
            this.container?.remove(),
            this.stickyOffset.remove(),
            this.stickyContainer.remove(),
            (this.stickyContainer.textContent = ``),
            (this.root = void 0),
            (this.container = void 0));
        }
        cleanAllRenderedItems() {
          if (this.renderState.firstIndex !== -1)
            for (
              let e = this.renderState.firstIndex;
              e <= this.renderState.lastIndex;
              e++
            ) {
              let t = this.items[e];
              if (t == null)
                throw Error(
                  `CodeView.cleanAllRenderedItems: Item does not exist at index: ${e}`,
                );
              this.releaseRenderedItem(t);
            }
        }
        primeScrollTarget(e) {
          e.type !== `position` &&
            this.idToItem.get(e.id)?.instance.primeHighlightCache();
        }
        getElementPoolLimit() {
          let e = this.getHeight() + this.config.overscrollSize * 2,
            { diffHeaderHeight: t } = this.itemMetricsCache;
          return (
            Math.max(8, Math.ceil(e / Math.max(t, 10)) + 1) *
            (this.isContainerManaged ? 2 : 1)
          );
        }
        acquireElement() {
          this.promotePendingPooledElements();
          let e = this.elementPool.pop();
          for (; e != null && !this.isElementPoolGenerationCurrent(e);)
            e = this.elementPool.pop();
          return (
            (e ??= document.createElement(xt)),
            this.markElementPoolGenerationCurrent(e),
            e
          );
        }
        releaseRenderedItem(e) {
          let { element: t } = e;
          (t != null &&
            this.renderedItemOwnsFocus(t) &&
            (this.shouldFixContainerFocus = !0),
            e.instance.cleanUp(!0),
            (e.element = void 0),
            t != null &&
              (t.remove(), this.cleanElement(t), this.queueElementForPool(t)));
        }
        renderedItemOwnsFocus(e) {
          let { activeElement: t } = document;
          return (
            t === e || e.contains(t) || e.shadowRoot?.activeElement != null
          );
        }
        fixContainerFocus() {
          this.shouldFixContainerFocus &&
            ((this.shouldFixContainerFocus = !1),
            this.root?.focus({ preventScroll: !0 }));
        }
        cleanElement(e) {
          let { shadowRoot: t } = e;
          if (t != null)
            for (let e of Array.from(t.children)) Sx(e) || e.remove();
          this.isContainerManaged || e.replaceChildren();
        }
        queueElementForPool(e) {
          let t = this.getElementPoolLimit();
          !this.isElementPoolGenerationCurrent(e) ||
            this.getElementPoolSize() >= t ||
            (this.isElementClean(e)
              ? this.elementPool.push(e)
              : this.pendingElementPool.push(e));
        }
        promotePendingPooledElements() {
          if (this.pendingElementPool.length === 0) return;
          let { pendingElementPool: e } = this;
          this.pendingElementPool = [];
          let t = this.getElementPoolLimit();
          for (let n of e)
            this.isElementPoolGenerationCurrent(n) &&
            this.isElementClean(n) &&
            this.elementPool.length < t
              ? this.elementPool.push(n)
              : this.isElementPoolGenerationCurrent(n) &&
                this.getElementPoolSize() < t &&
                this.pendingElementPool.push(n);
        }
        isElementClean(e) {
          return e.childNodes.length === 0;
        }
        getElementPoolSize() {
          return this.elementPool.length + this.pendingElementPool.length;
        }
        clearElementPool() {
          ((this.elementPool.length = 0), (this.pendingElementPool.length = 0));
        }
        invalidateElementPool() {
          (this.elementPoolVersion++, this.clearElementPool());
        }
        markElementPoolGenerationCurrent(e) {
          this.elementPoolTracker.set(e, this.elementPoolVersion);
        }
        isElementPoolGenerationCurrent(e) {
          return this.elementPoolTracker.get(e) === this.elementPoolVersion;
        }
        resolveEffectiveScrollBehavior(e, t) {
          return un()
            ? `instant`
            : e.behavior === `smooth-auto`
              ? Math.abs(t - this.getScrollTop()) <= this.getHeight() * 10
                ? `smooth`
                : `instant`
              : (e.behavior ?? `instant`);
        }
        scrollTo(e) {
          if (this.root == null) return;
          let t = this.normalizeScrollTarget(e);
          if (t == null) return;
          let n = this.resolveScrollTargetTop(t);
          n != null &&
            (this.primeScrollTarget(t),
            this.resolveEffectiveScrollBehavior(t, n) === `smooth`
              ? (this.scrollAnimation ??= {
                  position: this.getScrollTop(),
                  velocity: 0,
                  lastTimestamp: performance.now(),
                })
              : (this.scrollAnimation = void 0),
            this.suspendScrollInteractions(),
            (this.pendingLayoutAnchor = void 0),
            (this.pendingScrollTarget = t),
            this.render());
        }
        setSelectedLines(e, t) {
          this.applySelectedLines(e, t);
        }
        getSelectedLines() {
          return this.selectedLines;
        }
        clearSelectedLines(e) {
          this.applySelectedLines(null, e);
        }
        getItem(e) {
          return this.idToItem.get(e)?.item;
        }
        updateItem(e) {
          let t = this.idToItem.get(e.id);
          return t == null
            ? (console.error(`CodeView.updateItem: unknown item id "${e.id}"`),
              !1)
            : this.syncItemRecord(t, e)
              ? (this.markItemLayoutDirty(t),
                (this.scrollDirty = !0),
                this.render(),
                this.syncSelection(),
                !0)
              : !1;
        }
        updateItemId(e, t) {
          if (e === t) return !0;
          let n = this.idToItem.get(e);
          return n == null
            ? (console.error(`CodeView.updateItemId: unknown item id "${e}"`),
              !1)
            : this.idToItem.has(t)
              ? (console.error(
                  `CodeView.updateItemId: duplicate item id "${t}"`,
                ),
                !1)
              : (this.idToItem.delete(e),
                (n.item.id = t),
                this.idToItem.set(t, n),
                this.updateItemOptionsId(n.instance.options, t),
                this.selectedLines?.id === e &&
                  ((this.selectedLines = { ...this.selectedLines, id: t }),
                  this.options.onSelectedLinesChange?.(this.selectedLines)),
                this.renamePendingScrollTarget(e, t),
                this.renamePendingLayoutAnchor(e, t),
                this.render(),
                !0);
        }
        addItem(e) {
          (this.addItems([e]), this.syncSelection());
        }
        addItems(e) {
          (this.appendItemsInternal(e), this.syncSelection());
        }
        setItems(e) {
          (e.length === 0
            ? this.reset()
            : this.items.length === 0
              ? this.appendItemsInternal(e)
              : this.tryAppendItems(e) || this.reconcileItems(e),
            this.syncSelection());
        }
        appendItemsInternal(e, t = !0) {
          if (e.length === 0) return;
          let n = this.getLayout(),
            r = this.items.length === 0 ? 0 : this.scrollHeight + n.gap,
            i = r;
          for (let t = 0; t < e.length; t++) {
            let i = e[t];
            if (i == null)
              throw Error(`CodeView.appendItemsInternal: missing input item`);
            if (this.idToItem.has(i.id))
              throw Error(`CodeView.addItem: duplicate id "${i.id}"`);
            let a = this.createItem(i, this.items.length, r);
            (this.items.push(a),
              this.idToItem.set(a.item.id, a),
              this.instanceToItem.set(a.instance, a),
              (a.height = vx(a)),
              (r += a.height + n.gap));
          }
          ((this.scrollHeight = r - n.gap),
            (this.scrollDirty = !0),
            t &&
              (this.canSkipRenderForAppend(i)
                ? this.syncContainerHeight()
                : this.render()));
        }
        canSkipRenderForAppend(e) {
          return (
            this.container != null &&
            this.renderState.firstIndex !== -1 &&
            this.pendingScrollTarget == null &&
            this.scrollAnimation == null &&
            this.layoutDirtyIndex == null &&
            e > this.windowSpecs.bottom
          );
        }
        onThemeChange() {
          this.invalidateElementPool();
        }
        setOptions(e) {
          if (e == null) return;
          this.capturePendingLayoutAnchor();
          let { options: t } = this,
            n = this.getLayout(),
            { itemMetricsCache: r } = this;
          (yx(t, e) && this.invalidateElementPool(), (this.options = e));
          let i = this.computeMetricsCache(e.itemMetrics),
            a = !Qt(r, i),
            o = !Qt(n, this.getLayout());
          o && this.syncLayout();
          let s = a || bx(t, e);
          if (s) {
            let n = this.pendingLayoutReset;
            this.pendingLayoutReset = {
              metrics: a ? i : n?.metrics,
              resetFileLayoutCache: !0,
              resetDiffLayoutCache: !0,
              includeEstimatedDiffHeights:
                n?.includeEstimatedDiffHeights === !0 || a || xx(t, e),
            };
          }
          ((o || s) &&
            (this.markLayoutDirtyFromIndex(0), (this.scrollDirty = !0)),
            nn(t, e) || this.renderOptionsRevision++,
            !this.isContainerManaged && this.items.length > 0 && this.render());
        }
        capturePendingLayoutAnchor() {
          this.root == null ||
            this.items.length === 0 ||
            this.pendingScrollTarget != null ||
            (this.pendingLayoutAnchor = this.getScrollAnchor(
              this.getScrollTop(),
            ));
        }
        render(t = !1) {
          e.__STOP ||
            (t
              ? (qt(this.computeRenderRangeAndEmit),
                this.computeRenderRangeAndEmit())
              : Kt(this.computeRenderRangeAndEmit));
        }
        isReady() {
          let { workerManager: e } = this;
          return e == null || e.isInitialized() || e.getStats().workersFailed
            ? (this.clearReadySubscription(), !0)
            : ((this.isReadySubscription ??= e.subscribeToStatChanges((e) => {
                (e.managerState !== `initialized` && !e.workersFailed) ||
                  (this.clearReadySubscription(), this.render(!0));
              })),
              e.getStats().managerState === `waiting` &&
                e.initialize().catch(() => {}),
              !1);
        }
        clearReadySubscription() {
          this.isReadySubscription != null &&
            (this.isReadySubscription(), (this.isReadySubscription = void 0));
        }
        instanceChanged(e, t) {
          let n = this.instanceToItem.get(e);
          if (n == null)
            throw Error(
              `CodeView.instanceChanged: An instance has changed that is not registered`,
            );
          (t && this.markItemLayoutDirty(n), this.render());
        }
        getWindowSpecs() {
          return this.windowSpecs;
        }
        getContainerElement() {
          return this.root;
        }
        getRenderedItems() {
          let { firstIndex: e, lastIndex: t } = this.renderState;
          if (e === -1 || t === -1 || t < e) return [];
          let n = [];
          for (let r = e; r <= t; r++) {
            let e = this.items[r];
            e?.element != null &&
              (e.type === `diff`
                ? n.push({
                    id: e.item.id,
                    type: `diff`,
                    item: e.item,
                    version: e.version,
                    element: e.element,
                    instance: e.instance,
                  })
                : n.push({
                    id: e.item.id,
                    type: `file`,
                    item: e.item,
                    version: e.version,
                    element: e.element,
                    instance: e.instance,
                  }));
          }
          return n;
        }
        setSlotCoordinator(e) {
          return e === this.slotCoordinator
            ? !1
            : ((this.slotCoordinator = e), (this.slotSnapshot = void 0), !0);
        }
        getSlotSnapshot(e) {
          return Ox(this.getRenderedItems(), e);
        }
        subscribeToScroll(e) {
          return (
            this.scrollListeners.add(e),
            () => {
              this.scrollListeners.delete(e);
            }
          );
        }
        getLocalTopForInstance(e) {
          let t = this.instanceToItem.get(e);
          if (t == null)
            throw Error(
              `CodeView.getLocalTopForInstance: unknown virtualized instance`,
            );
          return t.top;
        }
        getTopForItem(e) {
          let t = this.idToItem.get(e);
          if (t != null) return t.top + this.getLayout().paddingTop;
        }
        createItem(e, t, n) {
          let { itemMetricsCache: r } = this;
          if (e.type === `diff`) {
            let i = new dx(
              this.createDiffOptions(e.id),
              this,
              r,
              this.workerManager,
              this.isContainerManaged,
            );
            return {
              type: `diff`,
              item: e,
              version: e.version,
              index: t,
              top: n,
              height: 0,
              element: void 0,
              renderedOptionsRevision: this.renderOptionsRevision,
              instance: i,
            };
          }
          let i = new $g(
            this.createFileOptions(e.id),
            this,
            r,
            this.workerManager,
            this.isContainerManaged,
          );
          return {
            type: `file`,
            item: e,
            version: e.version,
            index: t,
            top: n,
            height: 0,
            element: void 0,
            renderedOptionsRevision: this.renderOptionsRevision,
            instance: i,
          };
        }
        applySelectedLines(e, t) {
          let { selectedLines: n } = this;
          (e == null && n == null) ||
            (e != null && n?.id === e.id && on(n.range, e.range)) ||
            (n != null &&
              n.id !== e?.id &&
              this.idToItem
                .get(n.id)
                ?.instance.setSelectedLines(null, { notify: !1 }),
            (this.selectedLines = e),
            this.idToItem
              .get(e?.id ?? ``)
              ?.instance.setSelectedLines(e?.range ?? null, t));
        }
        syncSelection() {
          if (this.selectedLines == null) return;
          let e = this.idToItem.get(this.selectedLines.id);
          if (e == null) {
            this.selectedLines = null;
            return;
          }
          e.instance.setSelectedLines(this.selectedLines.range, { notify: !1 });
        }
        renamePendingScrollTarget(e, t) {
          let { pendingScrollTarget: n } = this;
          n == null ||
            n.type === `position` ||
            n.id !== e ||
            (this.pendingScrollTarget = { ...n, id: t });
        }
        renamePendingLayoutAnchor(e, t) {
          this.pendingLayoutAnchor?.id === e &&
            (this.pendingLayoutAnchor.id = t);
        }
        createFileOptionsPrototype() {
          let e = {};
          for (let t of jx) _x(e, t, () => this.options[t]);
          (_x(e, `stickyHeader`, () => this.options.stickyHeaders),
            _x(e, `collapsed`, (e) => {
              let t = gx(e);
              if (t != null)
                return this.getItemOptions(t, `file`)?.item.collapsed;
            }));
          for (let t of Mx) this.defineItemSharedCallback(e, `file`, t);
          for (let t of Nx) this.defineItemSelectionCallback(e, `file`, t);
          return e;
        }
        createDiffOptionsPrototype() {
          let e = {};
          for (let t of Ax) _x(e, t, () => this.options[t]);
          (_x(e, `stickyHeader`, () => this.options.stickyHeaders),
            _x(e, `hunkSeparators`, () => this.options.hunkSeparators),
            _x(e, `collapsed`, (e) => {
              let t = gx(e);
              if (t != null)
                return this.getItemOptions(t, `diff`)?.item.collapsed;
            }));
          for (let t of Mx) this.defineItemSharedCallback(e, `diff`, t);
          for (let t of Nx) this.defineItemSelectionCallback(e, `diff`, t);
          return e;
        }
        createFileOptions(e) {
          let t = Object.create(this.fileOptionsPrototype);
          return (hx(t, { id: e }), t);
        }
        createDiffOptions(e) {
          let t = Object.create(this.diffOptionsPrototype);
          return (hx(t, { id: e }), t);
        }
        updateItemOptionsId(e, t) {
          let n = gx(e);
          if (n == null)
            throw Error(`CodeView.updateItemOptionsId: No valid state`);
          n.id = t;
        }
        getItemOptions(e, t) {
          let n = this.idToItem.get(e.id);
          if (!(n == null || n.type !== t)) return n;
        }
        defineItemSharedCallback(e, t, n) {
          _x(e, n, (e) => {
            if (this.options[n] == null) return;
            let r = gx(e);
            if (r == null) return;
            let i = (r.callbackCache ??= {}),
              a = i[n];
            return (
              a ??
                ((a = (...e) => {
                  let i = this.getItemOptions(r, t);
                  if (i == null) return;
                  let a = this.options[n];
                  return a?.(...e, i);
                }),
                (i[n] = a)),
              a
            );
          });
        }
        defineItemSelectionCallback(e, t, n) {
          _x(e, n, (e) => {
            if (this.options.enableLineSelection !== !0) return;
            let r = gx(e);
            if (r == null) return;
            let i = (r.callbackCache ??= {}),
              a = i[n];
            return (
              a ??
                ((a = (e) => {
                  let i = this.getItemOptions(r, t);
                  if (i == null) return;
                  let a = e == null ? null : { id: i.item.id, range: e };
                  (this.options.controlledSelection !== !0 &&
                    (e != null || this.selectedLines?.id === i.item.id) &&
                    this.applySelectedLines(a, { notify: !1 }),
                    this.options.onSelectedLinesChange?.(a));
                  let o = this.options[n];
                  return o?.(e, i);
                }),
                (i[n] = a)),
              a
            );
          });
        }
        markLayoutDirtyFromIndex(e) {
          this.layoutDirtyIndex = Math.min(this.layoutDirtyIndex ?? e, e);
        }
        markItemLayoutDirty(e) {
          if (this.items[e.index] !== e)
            throw Error(
              `CodeView.markItemLayoutDirty: unknown item id "${e.item.id}"`,
            );
          this.markLayoutDirtyFromIndex(e.index);
        }
        tryAppendItems(e) {
          if (e.length <= this.items.length) return !1;
          for (let t = 0; t < this.items.length; t++) {
            let n = this.items[t];
            if (n == null)
              throw Error(`CodeView.tryAppendItems: missing existing item`);
            let r = e[t];
            if (r == null || n.item.id !== r.id || n.type !== r.type) return !1;
          }
          for (let t = 0; t < this.items.length; t++) {
            let n = this.items[t];
            if (n == null)
              throw Error(`CodeView.tryAppendItems: missing existing item`);
            let r = e[t];
            if (r == null)
              throw Error(
                `CodeView.tryAppendItems: append candidate missing prefix item`,
              );
            this.syncItemRecord(n, r) && this.markLayoutDirtyFromIndex(t);
          }
          return (
            this.appendItemsInternal(e.slice(this.items.length), !1),
            (this.scrollDirty = !0),
            this.render(),
            !0
          );
        }
        reconcileItems(e) {
          let { items: t, idToItem: n } = this,
            r = new Set(t),
            i = [],
            a = new Map(),
            o = new Map(),
            s;
          for (let c = 0; c < e.length; c++) {
            let l = e[c];
            if (l == null)
              throw Error(`CodeView.reconcileItems: missing input item`);
            if (a.has(l.id))
              throw Error(`CodeView.setItems: duplicate id "${l.id}"`);
            let u = n.get(l.id),
              d = u != null && u.type === l.type ? u : this.createItem(l, c, 0);
            ((d.index = c),
              u != null && u.type === l.type
                ? (r.delete(u),
                  this.syncItemRecord(d, l) && (s = Math.min(s ?? c, c)))
                : (s = Math.min(s ?? c, c)),
              t[c] !== d && (s = Math.min(s ?? c, c)),
              i.push(d),
              a.set(l.id, d),
              o.set(d.instance, d));
          }
          for (let e = 0; e < t.length; e++) {
            let n = t[e];
            if (n == null || !r.has(n)) continue;
            this.releaseRenderedItem(n);
            let a = Math.max(i.length - 1, 0);
            s = Math.min(s ?? a, a);
          }
          s != null &&
            ((this.items = i),
            (this.idToItem = a),
            (this.instanceToItem = o),
            this.renderState.firstIndex >= i.length
              ? this.resetRenderState()
              : this.renderState.lastIndex >= i.length &&
                (this.renderState.lastIndex = i.length - 1),
            this.markLayoutDirtyFromIndex(s),
            (this.scrollDirty = !0),
            this.render());
        }
        syncItemRecord(e, t) {
          if (e.type !== t.type)
            throw Error(
              `CodeView.syncItemRecord: type mismatch for id "${t.id}"`,
            );
          return e.version === t.version
            ? !1
            : ((e.item = t),
              (e.version = t.version),
              (e.renderedOptionsRevision = -1),
              !0);
        }
        getMaxScrollTopForHeight(e) {
          let { paddingBottom: t, paddingTop: n } = this.getLayout();
          return Math.max(n + e + t - this.getHeight(), 0);
        }
        getMaxScrollTop() {
          return this.getMaxScrollTopForHeight(this.getScrollHeight());
        }
        shouldRebaseScroll() {
          return this.getMaxScrollTop() > Vx;
        }
        getPagedScrollHeight() {
          return this.shouldRebaseScroll()
            ? Math.min(this.getScrollHeight(), Lx)
            : this.getScrollHeight();
        }
        getMaxPagedScrollTop() {
          return this.getMaxScrollTopForHeight(this.getPagedScrollHeight());
        }
        clampPagedScrollTop(e) {
          let t = this.getMaxPagedScrollTop();
          return Math.max(0, Math.min(e, t));
        }
        clampScrollTop(e) {
          let t = this.getMaxScrollTop();
          return Math.max(0, Math.min(e, t));
        }
        getMaxScrollPageOffset() {
          return Math.max(
            this.getMaxScrollTop() - this.getMaxPagedScrollTop(),
            0,
          );
        }
        clampScrollPageOffset(e) {
          let t = this.getMaxScrollPageOffset();
          return Math.max(0, Math.min(e, t));
        }
        resolveScrollPageWindow(e, t) {
          let n = px(this.clampPagedScrollTop(t)),
            r = this.clampScrollPageOffset(e - n);
          return (
            (n = px(this.clampPagedScrollTop(e - r))),
            (r = this.clampScrollPageOffset(e - n)),
            { pagedScrollTop: n, scrollPageOffset: r }
          );
        }
        resolvePagedScrollPosition(e) {
          if (!this.shouldRebaseScroll())
            return {
              pagedScrollTop: this.clampPagedScrollTop(e),
              scrollPageOffset: 0,
            };
          let t = this.clampScrollPageOffset(this.scrollPageOffset),
            n = e - t,
            r = this.getMaxPagedScrollTop(),
            i = this.getMaxScrollPageOffset(),
            a = n > Vx && t < i,
            o = n < Rx && t > 0;
          return n < 0 || n > r || a || o
            ? this.resolveScrollPageWindow(e, o ? Math.min(Bx, r) : zx)
            : {
                pagedScrollTop: px(this.clampPagedScrollTop(n)),
                scrollPageOffset: t,
              };
        }
        needsScrollPageUpdate(e) {
          let t = px(this.clampScrollTop(e)),
            { scrollPageOffset: n } = this.resolvePagedScrollPosition(t);
          return n !== this.scrollPageOffset;
        }
        getPagedLayoutTop(e) {
          return this.shouldRebaseScroll()
            ? Math.max(e - this.scrollPageOffset, 0)
            : e;
        }
        getStickyHeaderOffset() {
          return this.options.stickyHeaders === !0 &&
            this.options.disableFileHeader !== !0
            ? this.itemMetricsCache.diffHeaderHeight
            : 0;
        }
        getScrollTargetRect(e) {
          let t = this.idToItem.get(e.id);
          if (t == null) {
            console.warn(`CodeView.scrollTo: unknown item id "${e.id}"`);
            return;
          }
          if (e.type === `item`) return { top: t.top, height: t.height };
          if (e.type === `range`) {
            let n = this.getRangeScrollPosition(t, e);
            if (n == null) {
              console.warn(
                `CodeView.scrollTo: unable to resolve range ${Cx(e.range)} for item "${e.id}"`,
              );
              return;
            }
            return { top: t.top + n.top, height: n.height };
          }
          let n = this.getLineScrollPosition(t, e);
          if (n == null) {
            console.warn(
              `CodeView.scrollTo: unable to resolve line ${e.lineNumber} for item "${e.id}"`,
            );
            return;
          }
          return { top: t.top + n.top, height: n.height };
        }
        normalizeScrollTarget(e) {
          if (e.type === `position` || e.align !== `nearest`) return e;
          let t = this.getScrollTargetRect(e);
          if (t == null) return;
          let n = e.offset ?? 0,
            r = this.getLayout().paddingTop + t.top,
            i = r + t.height,
            a = this.getScrollTop(),
            o =
              a +
              (e.type === `line` || e.type === `range`
                ? this.getStickyHeaderOffset()
                : 0),
            s = a + this.getHeight();
          if (!(r - n <= o && i + n >= s)) {
            if (r - n < o) return { ...e, align: `start` };
            if (i + n > s) return { ...e, align: `end` };
          }
        }
        resolveScrollTargetTop(e) {
          if (e.type === `position`) {
            let t = this.clampScrollTop(e.position);
            return t === e.position
              ? this.clampScrollTop(e.position - this.getStickyHeaderOffset())
              : t;
          }
          let t = this.idToItem.get(e.id);
          if (t == null) {
            console.warn(`CodeView.scrollTo: unknown item id "${e.id}"`);
            return;
          }
          if (e.type === `item`)
            return this.clampScrollTop(
              this.resolveAlignedScrollPosition(
                t.top,
                t.height,
                e.align,
                e.offset,
              ),
            );
          if (e.type === `range`) {
            let n = this.getRangeScrollPosition(t, e);
            if (n == null) {
              console.warn(
                `CodeView.scrollTo: unable to resolve range ${Cx(e.range)} for item "${e.id}"`,
              );
              return;
            }
            return this.clampScrollTop(
              this.resolveAlignedScrollPosition(
                t.top + n.top,
                n.height,
                e.align,
                e.offset,
                this.getStickyHeaderOffset(),
              ),
            );
          }
          let n = this.getLineScrollPosition(t, e);
          if (n == null) {
            console.warn(
              `CodeView.scrollTo: unable to resolve line ${e.lineNumber} for item "${e.id}"`,
            );
            return;
          }
          return this.clampScrollTop(
            this.resolveAlignedScrollPosition(
              t.top + n.top,
              n.height,
              e.align,
              e.offset,
              this.getStickyHeaderOffset(),
            ),
          );
        }
        resolveAlignedScrollPosition(e, t, n, r = 0, i = 0) {
          e += this.getLayout().paddingTop;
          let a = this.getHeight();
          return n === `center` && t + r < a
            ? e - (a - t) / 2 + r
            : n === `end`
              ? e - (a - t) + r
              : e - i - r;
        }
        getLineScrollPosition(e, t) {
          return e.type === `diff`
            ? e.instance.getLinePosition(t.lineNumber, t.side)
            : e.instance.getLinePosition(t.lineNumber);
        }
        getRangeScrollPosition(e, t) {
          let { range: n } = t,
            r = this.getLineScrollPosition(e, {
              type: `line`,
              id: t.id,
              lineNumber: n.start,
              side: n.side,
            }),
            i = this.getLineScrollPosition(e, {
              type: `line`,
              id: t.id,
              lineNumber: n.end,
              side: n.endSide ?? n.side,
            });
          if (r == null || i == null) return;
          let a = r.top,
            o = a + r.height,
            s = i.top,
            c = s + i.height,
            l = Math.min(a, s);
          return { top: l, height: Math.max(o, c) - l };
        }
        computeTargetScrollTopForFrame(e, t) {
          if (this.pendingScrollTarget == null) return e;
          let n = this.resolveScrollTargetTop(this.pendingScrollTarget);
          if (n == null) return e;
          let { scrollAnimation: r } = this;
          return r == null ? n : this.computeSpringStep(r, n, t).position;
        }
        computeSpringStep(e, t, n) {
          let r = Math.max(0, n - e.lastTimestamp),
            { omega: i } = this.getSmoothScrollSettings(),
            a = Math.exp(-i * r),
            o = e.position - t,
            s = e.velocity + i * o;
          return {
            position: t + (o + s * r) * a,
            velocity: (s * (1 - i * r) - i * o) * a,
          };
        }
        advanceScrollAnimation(e, t) {
          if (this.pendingScrollTarget == null) return;
          let n = this.resolveScrollTargetTop(this.pendingScrollTarget);
          if (n == null) {
            ((this.pendingScrollTarget = void 0),
              (this.scrollAnimation = void 0));
            return;
          }
          let r = this.scrollAnimation;
          if (r == null) return n;
          r.position += t;
          let { position: i, velocity: a } = this.computeSpringStep(r, n, e);
          ((r.lastTimestamp = e), (r.position = i), (r.velocity = a));
          let { positionEpsilon: o, velocityEpsilon: s } =
            this.getSmoothScrollSettings();
          return Math.abs(n - i) <= o && Math.abs(a) <= s
            ? ((r.position = n),
              (r.velocity = 0),
              (this.scrollAnimation = void 0),
              n)
            : r.position;
        }
        computeRenderRangeAndEmit = (t = performance.now()) => {
          if (e.__STOP || this.container == null || !this.isReady()) return;
          let n = this.getHeight(),
            r = this.getScrollTop(),
            i = r,
            a = this.pendingLayoutAnchor != null,
            o = this.getScrollAnchor(i);
          if (
            (this.layoutDirtyIndex != null &&
              (this.recomputeLayout(
                this.layoutDirtyIndex,
                this.pendingLayoutReset,
              ),
              (this.layoutDirtyIndex = void 0),
              (this.pendingLayoutReset = void 0),
              (a = !0)),
            a && o != null)
          ) {
            let e = this.resolveAnchoredScrollTop(o);
            if (e != null) {
              let t = e - i;
              ((i = e),
                this.scrollAnimation != null &&
                  (this.scrollAnimation.position += t));
            }
          }
          a && ((i = this.clampScrollTop(i)), this.syncContainerHeight());
          let s = this.computeTargetScrollTopForFrame(i, t),
            c =
              !a &&
              (this.renderState.scrollTop === -1 ||
                Math.abs(s - this.renderState.scrollTop) >
                  n + this.config.overscrollSize * 2);
          (c && (o = void 0),
            (this.windowSpecs = cn({
              scrollTop: s,
              height: n,
              scrollHeight: this.getScrollHeight(),
              fitPerfectly: c,
              fitPerfectlyOverscroll: this.getFitPerfectlyOverscroll(),
              overscrollSize: this.config.overscrollSize,
            })));
          let l = r;
          ((this.pendingScrollTarget != null && s !== l) ||
            this.needsScrollPageUpdate(s)) &&
            (this.applyScrollFix(s, l, this.windowSpecs), (l = s));
          let { top: u, bottom: d } = this.windowSpecs,
            { firstIndex: f, lastIndex: p } = this.renderState;
          if (f >= 0)
            for (let e = f; e <= p; e++) {
              let t = this.items[e];
              if (t == null)
                throw Error(
                  `CodeView.computeRenderRangeAndEmit: No item at index: ${e}`,
                );
              (t.top > u - t.height && t.top <= d) ||
                this.releaseRenderedItem(t);
            }
          let m,
            h = new Set(),
            g = this.findFirstVisibleIndex(u),
            _ = this.findLastVisibleIndex(d);
          for (let e = g; e <= _; e++) {
            let t = this.items[e];
            if (t == null)
              throw Error(`CodeView.computeRenderRangeAndEmit: missing item`);
            let { instance: n } = t;
            t.element == null
              ? ((t.element = this.acquireElement()),
                Ex(this.stickyContainer, t.element, m),
                n.virtualizedSetup(),
                Tx(t, t.element) &&
                  ((t.renderedOptionsRevision = this.renderOptionsRevision),
                  h.add(t)),
                (m = t.element))
              : (Ex(this.stickyContainer, t.element, m),
                Tx(
                  t,
                  void 0,
                  t.renderedOptionsRevision !== this.renderOptionsRevision,
                ) &&
                  ((t.renderedOptionsRevision = this.renderOptionsRevision),
                  h.add(t)),
                (m = t.element));
          }
          ((this.renderState.firstIndex = g <= _ ? g : -1),
            (this.renderState.lastIndex = _),
            this.flushSlotCoordinator(),
            this.flushManagers(h),
            this.reconcileRenderedItems(h),
            this.syncContainerHeight(),
            this.updateStickyPositioning());
          let v = o == null ? void 0 : this.resolveAnchoredScrollTop(o);
          o === this.pendingLayoutAnchor && (this.pendingLayoutAnchor = void 0);
          let y = v == null ? 0 : v - i,
            b = s,
            x = !1;
          if (this.pendingScrollTarget != null) {
            let e = this.advanceScrollAnimation(t, y);
            e == null ? (b = i) : ((b = e), (x = !0));
          } else b = v ?? s;
          (b !== l && (this.applyScrollFix(b, l, this.windowSpecs), (l = b)),
            x &&
              this.pendingScrollTarget != null &&
              this.isPendingTargetSettled(this.pendingScrollTarget) &&
              ((this.pendingScrollTarget = void 0),
              (this.scrollAnimation = void 0)),
            (this.renderState.scrollTop = px(l)),
            this.updateStickyPositioning(),
            this.validateStickyContainerHeight(),
            this.fixContainerFocus(),
            (c || this.scrollAnimation != null) && this.render());
        };
        flushManagers(e) {
          for (let t of e) t.instance.flushManagers();
        }
        syncContainerHeight() {
          let e = this.getPagedScrollHeight();
          this.container == null ||
            this.containerHeight === e ||
            ((this.container.style.height = `${e}px`),
            (this.containerHeight = e));
        }
        getStickyBounds(e) {
          let { firstIndex: t, lastIndex: n } =
            e == null
              ? this.renderState
              : {
                  firstIndex: this.findFirstVisibleIndex(e.top),
                  lastIndex: this.findLastVisibleIndex(e.bottom),
                };
          if (t === -1 || n === -1 || t > n) return;
          let r = this.items[t]?.instance.getAdvancedStickySpecs(e),
            i = this.items[n]?.instance.getAdvancedStickySpecs(e);
          if (!(r == null || i == null))
            return {
              stickyTop: this.getPagedLayoutTop(Math.max(r.topOffset, 0)),
              stickyBottom: this.getPagedLayoutTop(i.topOffset + i.height),
            };
        }
        applyStickyPositioning({ stickyTop: e, stickyBottom: t }) {
          let n = this.getHeight(),
            { itemMetricsCache: r } = this,
            i = t - e;
          ((this.renderState.stickyHeight = i),
            (this.renderState.stickyTop = e),
            (this.renderState.stickyBottom = t),
            (this.stickyOffset.style.height = `${e}px`));
          let a = ((Math.random() * r.lineHeight) >> 0) * -1,
            o = -Math.max(i + a, 0) + n;
          ((this.stickyContainer.style.top = `${o}px`),
            (this.stickyContainer.style.bottom = `${o + r.diffHeaderHeight}px`));
        }
        syncPagedScrollScaffolding(e) {
          this.syncContainerHeight();
          let t = this.getStickyBounds(e);
          t != null && this.applyStickyPositioning(t);
        }
        reconcileRenderedItems(e) {
          let { firstIndex: t, lastIndex: n } = this.renderState;
          if (t === -1) return;
          let r = -1,
            i = !1;
          for (let a = t; a < this.items.length && !(!i && a > n); a++) {
            let t = this.items[a];
            if (t == null)
              throw Error(`CodeView.reconcileRenderedItems: Invalid item`);
            (r === -1
              ? (r = t.top)
              : t.top !== r &&
                ((t.top = r), t.instance.syncVirtualizedTop(), (i = !0)),
              (e == null ? a <= n : e.has(t)) &&
                (t.instance.reconcileHeights() &&
                  ((i = !0), (t.height = t.instance.getVirtualizedHeight())),
                this.validateRenderedItemHeight(t)),
              (r += t.instance.getVirtualizedHeight()),
              a < this.items.length - 1 && (r += this.getLayout().gap));
          }
          i && r != null && ((this.scrollDirty = !0), (this.scrollHeight = r));
        }
        updateStickyPositioning() {
          let e = this.getStickyBounds();
          if (e == null) return;
          let { stickyTop: t, stickyBottom: n } = e;
          (n - t === this.renderState.stickyHeight &&
            t === this.renderState.stickyTop &&
            n === this.renderState.stickyBottom) ||
            this.applyStickyPositioning(e);
        }
        handleScroll = () => {
          e.__STOP ||
            (this.suspendScrollInteractions(),
            (this.scrollDirty = !0),
            this.notifyScroll(),
            this.render());
        };
        clearPendingScroll = () => {
          ((this.pendingScrollTarget = void 0),
            (this.pendingLayoutAnchor = void 0),
            (this.scrollAnimation = void 0));
        };
        handleResize = (e) => {
          for (let t of e)
            if (t.target === this.stickyContainer) {
              if (
                t.borderBoxSize[0].blockSize !== this.renderState.stickyHeight
              ) {
                let e = this.getScrollTop(),
                  t = this.getScrollAnchor(e);
                (this.reconcileRenderedItems(), this.updateStickyPositioning());
                let n = t == null ? void 0 : this.resolveAnchoredScrollTop(t);
                if (n != null) {
                  let t = n - e;
                  (this.applyScrollFix(n, e, this.windowSpecs),
                    this.scrollAnimation != null &&
                      (this.scrollAnimation.position += t));
                }
                this.pendingScrollTarget != null &&
                  this.isPendingTargetSettled(this.pendingScrollTarget) &&
                  ((this.pendingScrollTarget = void 0),
                  (this.scrollAnimation = void 0));
              }
            } else
              ((this.scrollDirty = !0), (this.heightDirty = !0), this.render());
        };
        getScrollAnchorViewportTop(e, t) {
          return e < t ? t + this.getStickyHeaderOffset() : t;
        }
        getScrollAnchor(e) {
          if (this.pendingLayoutAnchor != null) return this.pendingLayoutAnchor;
          let {
            firstIndex: t,
            lastIndex: n,
            stickyTop: r,
            stickyBottom: i,
          } = this.renderState;
          if (t === -1 || n === -1) return;
          let a = this.getHeight();
          if (!(r === -1 || i === -1))
            for (let r = t; r <= n; r++) {
              let t = this.items[r];
              if (t == null) continue;
              let n = this.getLayout().paddingTop + t.top;
              if (n + t.height <= e) continue;
              if (n >= e + a) break;
              if (n >= e)
                return { type: `item`, id: t.item.id, viewportOffset: n - e };
              let i = this.getScrollAnchorViewportTop(n, e) - n,
                o = t.instance.getNumericScrollAnchor(i);
              if (o != null) {
                let r = n + o.top;
                return {
                  type: `line`,
                  id: t.item.id,
                  lineNumber: o.lineNumber,
                  side: o.side,
                  viewportOffset: r - e,
                };
              }
            }
        }
        resolveAnchoredScrollTop(e) {
          let t = this.idToItem.get(e.id);
          if (t == null) return;
          let { paddingTop: n } = this.getLayout();
          if (e.type === `item`) {
            let r = n + t.top;
            return this.clampScrollTop(r - e.viewportOffset);
          }
          let r =
            t.type === `diff`
              ? t.instance.getLinePosition(e.lineNumber, e.side)
              : t.instance.getLinePosition(e.lineNumber);
          if (r == null) return;
          let i = n + t.top + r.top;
          return this.clampScrollTop(i - e.viewportOffset);
        }
        applyScrollFix(e, t, n) {
          if (this.root == null) return;
          let r = px(this.clampScrollTop(e)),
            i = px(t),
            { scrollPageOffset: a } = this,
            o = px(this.clampPagedScrollTop(i - a)),
            { pagedScrollTop: s, scrollPageOffset: c } =
              this.resolvePagedScrollPosition(r),
            l = s,
            u = a !== c;
          (r === this.renderState.scrollTop && r === i && l === o && !u) ||
            (this.suspendScrollInteractions(),
            (l !== o || u) &&
              ((this.scrollPageOffset = c), this.syncPagedScrollScaffolding(n)),
            l !== o && this.root.scrollTo({ top: l, behavior: `instant` }),
            (this.renderState.scrollTop = r),
            (this.scrollTop = r),
            (this.scrollDirty = !1));
        }
        isPendingTargetSettled(e) {
          let t = this.resolveScrollTargetTop(e);
          return t == null ? !0 : px(this.getScrollTop()) === px(t);
        }
        getScrollTop() {
          if (!this.scrollDirty) return this.scrollTop;
          this.scrollDirty = !1;
          let e = this.root?.scrollTop ?? 0;
          return (
            (this.scrollTop = this.clampScrollTop(e + this.scrollPageOffset)),
            this.scrollTop
          );
        }
        getHeight() {
          return this.heightDirty
            ? ((this.heightDirty = !1),
              (this.height = this.root?.getBoundingClientRect().height ?? 0),
              this.height)
            : this.height;
        }
        getScrollHeight() {
          return this.scrollHeight;
        }
        flushSlotCoordinator() {
          if (this.slotCoordinator == null) return;
          let { onSnapshotChange: e } = this.slotCoordinator,
            t = Ox(this.getRenderedItems(), this.slotCoordinator);
          kx(this.slotSnapshot, t) || ((this.slotSnapshot = t), e(t));
        }
        notifyScroll() {
          if (this.scrollListeners.size === 0) return;
          let e = this.getScrollTop();
          for (let t of this.scrollListeners) t(e, this);
        }
        findFirstVisibleIndex(e) {
          let t = 0,
            n = this.items.length - 1,
            r = this.items.length;
          for (; t <= n;) {
            let i = (t + n) >> 1,
              a = this.items[i];
            if (a == null)
              throw Error(`CodeView.findFirstVisibleIndex: invalid item index`);
            a.top + a.height > e ? ((r = i), (n = i - 1)) : (t = i + 1);
          }
          return r;
        }
        findLastVisibleIndex(e) {
          let t = 0,
            n = this.items.length - 1,
            r = -1;
          for (; t <= n;) {
            let i = (t + n) >> 1,
              a = this.items[i];
            if (a == null)
              throw Error(`CodeView.findLastVisibleIndex: invalid item index`);
            a.top <= e ? ((r = i), (t = i + 1)) : (n = i - 1);
          }
          return r;
        }
        recomputeLayout(e = 0, t) {
          if (this.items.length === 0) {
            this.scrollHeight = 0;
            return;
          }
          let n = this.getLayout(),
            r = 0;
          if (e > 0) {
            let t = this.items[e - 1];
            if (t == null)
              throw Error(`CodeView.recomputeLayout: invalid dirty index`);
            r = t.top + t.height + n.gap;
          }
          for (let i = e; i < this.items.length; i++) {
            let e = this.items[i];
            if (e == null)
              throw Error(`CodeView.recomputeLayout: invalid item index`);
            if (((e.top = r), e.type === `diff`)) {
              let n = e.instance.consumeCodeViewLayoutChanges(e.item.fileDiff);
              (n != null && (e.item.fileDiff = n),
                (e.height = e.instance.prepareCodeViewItem(
                  e.item.fileDiff,
                  r,
                  t,
                  e.item.annotations ?? [],
                )));
            } else
              e.height = e.instance.prepareCodeViewItem(
                e.item.file,
                r,
                t,
                e.item.annotations ?? [],
              );
            ((r += e.height), i < this.items.length - 1 && (r += n.gap));
          }
          (r !== this.scrollHeight && (this.scrollDirty = !0),
            (this.scrollHeight = r));
        }
        resetRenderState() {
          ((this.renderState.scrollTop = -1),
            (this.renderState.firstIndex = -1),
            (this.renderState.lastIndex = -1),
            (this.renderState.stickyHeight = 0),
            (this.renderState.stickyTop = -1),
            (this.renderState.stickyBottom = -1));
        }
        getFitPerfectlyOverscroll() {
          return this.getLayout().gap + this.itemMetricsCache.diffHeaderHeight;
        }
      }));
  });
function Gx(e, t) {
  return e == null || t == null
    ? e === t
    : e.top === t.top && e.bottom === t.bottom;
}
var Kx = e(() => {});
function qx(e, t) {
  let n = e.getBoundingClientRect(),
    r = t?.getBoundingClientRect().top ?? 0;
  return n.top - r;
}
var Jx,
  Yx,
  Xx,
  Zx,
  Qx,
  $x,
  eS,
  tS = e(() => {
    (Zt(),
      ln(),
      Kx(),
      (Jx = 1e3),
      (Yx = Jx * 4),
      (Xx = [0, 1e-6, 0.99999, 1]),
      (Zx = {
        overscrollSize: Jx,
        intersectionObserverMargin: Yx,
        resizeDebugging: !1,
      }),
      (Qx = 0),
      ($x = -1),
      (eS = class e {
        static __STOP = !1;
        static __lastScrollPosition = 0;
        __id = `virtualizer-${++$x}`;
        config;
        type = `simple`;
        intersectionObserver;
        scrollTop = 0;
        height = 0;
        scrollHeight = 0;
        windowSpecs = { top: 0, bottom: 0 };
        root;
        contentContainer;
        resizeObserver;
        observers = new Map();
        visibleInstances = new Map();
        visibleInstancesDirty = !1;
        instancesChanged = new Set();
        scrollDirty = !0;
        heightDirty = !0;
        scrollHeightDirty = !0;
        renderedObservers = 0;
        connectQueue = new Map();
        constructor(e) {
          this.config = { ...Zx, ...e };
        }
        setup(t, n) {
          if (this.root == null) {
            ((this.root = t),
              (this.resizeObserver = new ResizeObserver(
                this.handleContainerResize,
              )),
              (this.intersectionObserver = new IntersectionObserver(
                this.handleIntersectionChange,
                {
                  root: this.root,
                  threshold: Xx,
                  rootMargin: `${this.config.intersectionObserverMargin}px 0px ${this.config.intersectionObserverMargin}px 0px`,
                },
              )),
              t instanceof Document ? this.setupWindow() : this.setupElement(n),
              (window.__INSTANCE = this),
              (window.__TOGGLE = () => {
                e.__STOP
                  ? ((e.__STOP = !1),
                    (this.getScrollContainerElement() ?? window).scrollTo({
                      top: e.__lastScrollPosition,
                    }),
                    Kt(this.computeRenderRangeAndEmit))
                  : ((e.__lastScrollPosition = this.getScrollTop()),
                    (e.__STOP = !0));
              }));
            for (let [e, t] of this.connectQueue.entries()) this.connect(e, t);
            (this.connectQueue.clear(),
              this.markDOMDirty(),
              Kt(this.computeRenderRangeAndEmit));
          }
        }
        instanceChanged(e, t) {
          (this.instancesChanged.add(e),
            t && this.markDOMDirty(),
            Kt(this.computeRenderRangeAndEmit));
        }
        getWindowSpecs() {
          return (
            this.windowSpecs.top === 0 &&
              this.windowSpecs.bottom === 0 &&
              (this.windowSpecs = cn({
                scrollTop: this.getScrollTop(),
                height: this.getHeight(),
                scrollHeight: this.getScrollHeight(),
                overscrollSize: this.config.overscrollSize,
              })),
            this.windowSpecs
          );
        }
        isInstanceVisible(e, t) {
          let n = this.getScrollTop(),
            r = this.getHeight(),
            i = this.config.intersectionObserverMargin,
            a = n - i,
            o = n + r + i;
          return !(e < a - t || e > o);
        }
        handleContainerResize = (e) => {
          if (this.root == null) return;
          let t = !1;
          for (let n of e) {
            let e = n.borderBoxSize[0].blockSize;
            this.root instanceof Document
              ? e !== this.scrollHeight &&
                ((this.scrollHeightDirty = !0),
                (t = !0),
                this.config.resizeDebugging &&
                  (console.log(`Virtualizer: content size change`, this.__id, {
                    sizeChange: e - Qx,
                    newSize: e,
                  }),
                  (Qx = e)))
              : n.target === this.root
                ? e !== this.height && ((this.heightDirty = !0), (t = !0))
                : n.target === this.contentContainer &&
                  ((this.scrollHeightDirty = !0),
                  (t = !0),
                  this.config.resizeDebugging &&
                    (console.log(
                      `Virtualizer: scroller size change`,
                      this.__id,
                      { sizeChange: e - Qx, newSize: e },
                    ),
                    (Qx = e)));
          }
          t && Kt(this.computeRenderRangeAndEmit);
        };
        setupWindow() {
          if (this.root == null || !(this.root instanceof Document))
            throw Error(`Virtualizer.setupWindow: Invalid setup method`);
          (window.addEventListener(`scroll`, this.handleWindowScroll, {
            passive: !0,
          }),
            window.addEventListener(`resize`, this.handleWindowResize, {
              passive: !0,
            }),
            this.resizeObserver?.observe(this.root.documentElement));
        }
        setupElement(e) {
          if (this.root == null || this.root instanceof Document)
            throw Error(`Virtualizer.setupElement: Invalid setup method`);
          (this.root.addEventListener(`scroll`, this.handleElementScroll, {
            passive: !0,
          }),
            this.resizeObserver?.observe(this.root),
            (e ??= this.root.firstElementChild ?? void 0),
            e instanceof HTMLElement &&
              ((this.contentContainer = e), this.resizeObserver?.observe(e)));
        }
        cleanUp() {
          (this.resizeObserver?.disconnect(),
            (this.resizeObserver = void 0),
            this.intersectionObserver?.disconnect(),
            (this.intersectionObserver = void 0),
            this.root?.removeEventListener(`scroll`, this.handleElementScroll),
            window.removeEventListener(`scroll`, this.handleWindowScroll),
            window.removeEventListener(`resize`, this.handleWindowResize),
            (this.root = void 0),
            (this.contentContainer = void 0),
            this.observers.clear(),
            this.visibleInstances.clear(),
            this.instancesChanged.clear(),
            this.connectQueue.clear(),
            (this.visibleInstancesDirty = !1),
            (this.windowSpecs = { top: 0, bottom: 0 }),
            (this.scrollTop = 0),
            (this.height = 0),
            (this.scrollHeight = 0));
        }
        getOffsetInScrollContainer(e) {
          return this.getScrollTop() + qx(e, this.getScrollContainerElement());
        }
        connect(e, t) {
          if (this.observers.has(e))
            throw Error(
              `Virtualizer.connect: instance is already connected...`,
            );
          return (
            this.intersectionObserver == null
              ? this.connectQueue.set(e, t)
              : (this.intersectionObserver.observe(e),
                this.observers.set(e, t),
                this.instancesChanged.add(t),
                this.markDOMDirty(),
                Kt(this.computeRenderRangeAndEmit)),
            () => this.disconnect(e)
          );
        }
        disconnect(e) {
          let t = this.observers.get(e);
          (this.connectQueue.delete(e),
            t != null &&
              (this.intersectionObserver?.unobserve(e),
              this.observers.delete(e),
              this.visibleInstances.delete(e) &&
                (this.visibleInstancesDirty = !0),
              this.markDOMDirty(),
              Kt(this.computeRenderRangeAndEmit)));
        }
        handleWindowResize = () => {
          e.__STOP ||
            window.innerHeight === this.height ||
            ((this.heightDirty = !0), Kt(this.computeRenderRangeAndEmit));
        };
        handleWindowScroll = () => {
          e.__STOP ||
            this.root == null ||
            !(this.root instanceof Document) ||
            ((this.scrollDirty = !0), Kt(this.computeRenderRangeAndEmit));
        };
        handleElementScroll = () => {
          e.__STOP ||
            this.root == null ||
            this.root instanceof Document ||
            ((this.scrollDirty = !0), Kt(this.computeRenderRangeAndEmit));
        };
        computeRenderRangeAndEmit = () => {
          if (e.__STOP) return;
          let t = this.heightDirty || this.scrollHeightDirty;
          if (
            !this.scrollDirty &&
            !this.scrollHeightDirty &&
            !this.heightDirty &&
            this.renderedObservers === this.observers.size &&
            !this.visibleInstancesDirty &&
            this.instancesChanged.size === 0
          )
            return;
          let n = this.instancesChanged.size > 0;
          if (this.instancesChanged.size === 0) {
            let e = cn({
              scrollTop: this.getScrollTop(),
              height: this.getHeight(),
              scrollHeight: this.getScrollHeight(),
              overscrollSize: this.config.overscrollSize,
            });
            if (
              !t &&
              Gx(this.windowSpecs, e) &&
              this.renderedObservers === this.observers.size &&
              !this.visibleInstancesDirty
            )
              return;
            this.windowSpecs = e;
          }
          ((this.visibleInstancesDirty = !1),
            (this.renderedObservers = this.observers.size));
          let r = this.getScrollAnchor(this.height),
            i = new Set();
          for (let e of t
            ? this.observers.values()
            : this.visibleInstances.values())
            e.onRender(t) && i.add(e);
          for (let e of this.instancesChanged)
            i.has(e) || (e.onRender(t) && i.add(e));
          this.scrollFix(r);
          for (let e of i) e.reconcileHeights();
          ((n ||= this.instancesChanged.size > 0),
            n && this.markDOMDirty(),
            (n || t) && Kt(this.computeRenderRangeAndEmit),
            i.clear(),
            this.instancesChanged.clear());
        };
        scrollFix(e) {
          if (e == null) return;
          let t = this.getScrollContainerElement(),
            {
              lineIndex: n,
              lineOffset: r,
              fileElement: i,
              fileOffset: a,
              fileTypeOffset: o,
            } = e;
          if (n != null && r != null) {
            let e = i.shadowRoot?.querySelector(
              `[data-line][data-line-index="${n}"]`,
            );
            if (e instanceof HTMLElement) {
              let n = qx(e, t);
              if (n !== r) {
                let e = n - r;
                this.applyScrollFix(e);
              }
              return;
            }
          }
          let s = qx(i, t);
          if (o === `top`) s !== a && this.applyScrollFix(s - a);
          else {
            let e = s + i.getBoundingClientRect().height;
            e !== a && this.applyScrollFix(e - a);
          }
        }
        applyScrollFix(e) {
          (this.root == null || this.root instanceof Document
            ? window.scrollTo({ top: window.scrollY + e, behavior: `instant` })
            : this.root.scrollTo({
                top: this.root.scrollTop + e,
                behavior: `instant`,
              }),
            this.markDOMDirty());
        }
        getScrollAnchor(e) {
          let t = this.getScrollContainerElement(),
            n;
          for (let [r] of this.visibleInstances.entries()) {
            let i = qx(r, t),
              a = i + r.offsetHeight,
              o,
              s;
            a <= 0 ? ((o = a), (s = `bottom`)) : ((o = i), (s = `top`));
            let c, l;
            if (a > 0 && i < e)
              for (let e of r.shadowRoot?.querySelectorAll(
                `[data-line][data-line-index]`,
              ) ?? []) {
                if (!(e instanceof HTMLElement)) continue;
                let n = e.dataset.lineIndex;
                if (n == null) continue;
                let r = qx(e, t);
                if (!(r < 0)) {
                  ((c = n), (l = r));
                  break;
                }
              }
            if (n?.lineOffset != null && l == null) continue;
            let u = !1;
            ((n == null ||
              (l != null && (n.lineOffset == null || l < n.lineOffset)) ||
              (l == null &&
                n.lineOffset == null &&
                ((o >= 0 && (n.fileOffset < 0 || o < n.fileOffset)) ||
                  (o < 0 && n.fileOffset < 0 && o > n.fileOffset)))) &&
              (u = !0),
              u &&
                (n = {
                  fileElement: r,
                  fileTypeOffset: s,
                  fileOffset: o,
                  lineIndex: c,
                  lineOffset: l,
                }));
          }
          return n;
        }
        handleIntersectionChange = (e) => {
          this.scrollDirty = !0;
          for (let { target: t, isIntersecting: n } of e) {
            if (!(t instanceof HTMLElement))
              throw Error(
                `Virtualizer.handleIntersectionChange: target not an HTMLElement`,
              );
            let e = this.observers.get(t);
            e != null &&
              (n && !this.visibleInstances.has(t)
                ? (e.setVisibility(!0),
                  this.visibleInstances.set(t, e),
                  (this.visibleInstancesDirty = !0))
                : !n &&
                  this.visibleInstances.has(t) &&
                  (e.setVisibility(!1),
                  this.visibleInstances.delete(t),
                  (this.visibleInstancesDirty = !0)));
          }
          this.visibleInstancesDirty && Kt(this.computeRenderRangeAndEmit);
        };
        getScrollTop() {
          if (!this.scrollDirty) return this.scrollTop;
          this.scrollDirty = !1;
          let e =
            this.root == null
              ? 0
              : this.root instanceof Document
                ? window.scrollY
                : this.root.scrollTop;
          return (
            (e = Math.max(
              0,
              Math.min(e, this.getScrollHeight() - this.getHeight()),
            )),
            (this.scrollTop = e),
            e
          );
        }
        getScrollHeight() {
          return this.scrollHeightDirty
            ? ((this.scrollHeightDirty = !1),
              (this.scrollHeight =
                this.root == null
                  ? 0
                  : this.root instanceof Document
                    ? this.root.documentElement.scrollHeight
                    : this.root.scrollHeight),
              this.scrollHeight)
            : this.scrollHeight;
        }
        getHeight() {
          return this.heightDirty
            ? ((this.heightDirty = !1),
              (this.height =
                this.root == null
                  ? 0
                  : this.root instanceof Document
                    ? globalThis.innerHeight
                    : this.root.getBoundingClientRect().height),
              this.height)
            : this.height;
        }
        markDOMDirty() {
          ((this.scrollDirty = !0),
            (this.scrollHeightDirty = !0),
            (this.heightDirty = !0));
        }
        getScrollContainerElement() {
          return this.root == null || this.root instanceof Document
            ? void 0
            : this.root;
        }
      }));
  });
function nS(e) {
  let t = [];
  for (let n of e) {
    let e = oa.get(n);
    if (e == null)
      throw Error(
        `getResolvedLanguages: ${n} is not resolved. Please resolve languages before calling getResolvedLanguages`,
      );
    t.push(e);
  }
  return t;
}
var rS = e(() => {
  ua();
});
function iS(e) {
  for (let t of Array.isArray(e) ? e : [e]) if (!oa.has(t)) return !1;
  return !0;
}
var aS = e(() => {
  ua();
});
async function oS(e) {
  let t = [],
    n = [];
  for (let r of e) {
    if (r === `text` || r === `ansi`) continue;
    let e = ip(r) ?? np(r);
    `then` in e ? n.push(e) : t.push(e);
  }
  return (
    n.length > 0 &&
      (await Promise.all(n).then((e) => {
        for (let n of e) {
          if (n == null)
            throw Error(`resolvedLanguages: unable to resolve language`);
          t.push(n);
        }
      })),
    t
  );
}
var sS = e(() => {
  (rp(), ap());
});
function cS(e) {
  return Sp.getResolvedThemes(e);
}
var lS = e(() => {
  Cp();
});
async function uS(e) {
  for (let t of e) Zp(t);
  let t = await Sp.resolveThemes(e);
  for (let n = 0; n < e.length; n++) Qp(e[n], t[n]);
  return t;
}
var dS = e(() => {
  (Cp(), $p());
});
function fS() {
  return a(c.theme) ?? `system`;
}
function pS(e) {
  let t = (0, vS.c)(4),
    n;
  t[0] === e
    ? (n = t[1])
    : ((n = e === `system` ? _S : () => e), (t[0] = e), (t[1] = n));
  let r;
  return (
    t[2] === e
      ? (r = t[3])
      : ((r = e === `system` ? _S : () => e), (t[2] = e), (t[3] = r)),
    (0, yS.useSyncExternalStore)(e === `system` ? gS : hS, n, r)
  );
}
function mS(e) {
  return e === `system` ? [`light`, `dark`] : [e];
}
function hS() {
  return () => {};
}
function gS(e) {
  let t = window.electronBridge?.subscribeToSystemThemeVariant;
  if (t != null) return t(e);
  if (typeof window > `u` || typeof window.matchMedia != `function`)
    return () => {};
  let n = window.matchMedia(`(prefers-color-scheme: dark)`),
    r = () => {
      e();
    };
  return typeof n.addEventListener == `function`
    ? (n.addEventListener(`change`, r),
      () => {
        n.removeEventListener(`change`, r);
      })
    : typeof n.addListener == `function` &&
        typeof n.removeListener == `function`
      ? (n.addListener(r),
        () => {
          n.removeListener(r);
        })
      : () => {};
}
function _S() {
  return (
    window.electronBridge?.getSystemThemeVariant?.() ??
    (typeof window > `u` || typeof window.matchMedia != `function`
      ? `light`
      : window.matchMedia(`(prefers-color-scheme: dark)`).matches
        ? `dark`
        : `light`)
  );
}
var vS,
  yS,
  bS = e(() => {
    ((vS = o()), l(), (yS = n(r(), 1)), i());
  });
export {
  gm as $,
  Xv as A,
  xt as At,
  Hh as B,
  b as Bt,
  Ky as C,
  en as Ct,
  ny as D,
  Nt as Dt,
  zy as E,
  Kt as Et,
  $g as F,
  je as Ft,
  Ah as G,
  jh as H,
  v as Ht,
  e_ as I,
  ke as It,
  Mm as J,
  zm as K,
  Vg as L,
  bt as Lt,
  A_ as M,
  Me as Mt,
  t_ as N,
  De as Nt,
  ry as O,
  P as Ot,
  n_ as P,
  Oe as Pt,
  hm as Q,
  Hg as R,
  Ae as Rt,
  Gb as S,
  an as St,
  Wy as T,
  Zt as Tt,
  Th as U,
  y as Ut,
  Ph as V,
  Ee as Vt,
  Eh as W,
  Am as X,
  jm as Y,
  Im as Z,
  Ux as _,
  es as _t,
  dS as a,
  cm as at,
  fx as b,
  pn as bt,
  lS as c,
  kp as ct,
  iS as d,
  Cp as dt,
  pm as et,
  aS as f,
  Sp as ft,
  tS as g,
  _o as gt,
  eS as h,
  bp as ht,
  pS as i,
  im as it,
  Q_ as j,
  F as jt,
  $v as k,
  zt as kt,
  sS as l,
  wp as lt,
  rS as m,
  _p as mt,
  bS as n,
  lm as nt,
  uS as o,
  Xp as ot,
  nS as p,
  xp as pt,
  Bm as q,
  fS as r,
  um as rt,
  cS as s,
  Dp as st,
  mS as t,
  mm as tt,
  oS as u,
  Tp as ut,
  Wx as v,
  ts as vt,
  Gy as w,
  tn as wt,
  Wb as x,
  nn as xt,
  dx as y,
  fn as yt,
  Uh as z,
  C as zt,
};
//# sourceMappingURL=app-initial~artifact-tab-content.electron~app-main~page~pull-request-code-review~new-thread~b942ryfo-B5RKHLXM.js.map
